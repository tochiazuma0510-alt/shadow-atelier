{
  "schema": "mine-job/v1",
  "job_id": "REPLACE-ME-unique-id-with-date",
  "claim_class": "exploration",
  "map_ref": { "polestar": ["REPLACE-ME (例: P3)"], "band": 0 },
  "universe": {
    "generator": "ladder-family",
    "params": {
      "N_ord": "REPLACE-ME (例: 9)",
      "windows": "REPLACE-ME (窓数。既存driverを使う限り13固定)",
      "note": "REPLACE-ME (何の再走/延長かを1行で)"
    },
    "frozen_docs": [
      { "path": "search/_a13_ladder_driver_spec.md", "sha256": "REPLACE-ME (sha256sum で再計算した値)" },
      { "path": "REPLACE-ME (使う driver script)", "sha256": "REPLACE-ME" }
    ]
  },
  "pipeline": [
    {
      "predicate": "REPLACE-ME (v0は述語カード未整備なので driver 名+版で代用可)",
      "version": 1,
      "params": { "driver": "REPLACE-ME" }
    }
  ],
  "predictions": {
    "frozen": "REPLACE-ME (docs/notes/*_prediction*.md へのパス、無ければ declared_none に切替)",
    "sha256": "REPLACE-ME"
  },
  "resources": {
    "backend": "gap-ci",
    "shards": "none (driver に窓選択knobが無い限り非分割 -- 裁定237 修正2。knobがあれば具体的な分割方式を書く)",
    "timeout_min": 60,
    "checkpoint": false,
    "v0_driver": {
      "script": "REPLACE-ME",
      "sha256": "REPLACE-ME",
      "preamble": ""
    }
  },
  "outputs": {
    "cert_schema": "REPLACE-ME (driver が書く cert の呼び名。正本ドキュメントへのパスを書く)",
    "out_dir": "search/certs"
  },
  "crosscheck": {
    "mode": "auto-pair",
    "checker_backend": "py-ci",
    "checker_certs_glob": "REPLACE-ME (対付けする既存 checker cert 群の glob。無ければ crosscheck.mode を manual/none に)"
  },
  "ep_handoff": null
}
