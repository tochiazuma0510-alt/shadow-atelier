name: d972-burau-joint-v2

on:
  workflow_dispatch: {}
  push:
    branches:
      - sol/d972-dmtcp-provision-v420
    paths:
      - .github/workflows/d972-burau-joint-v2.yml
      - search/d972_b4_burau_joint_v2.py
      - search/check_d972_b4_burau_joint_v2.py
      - search/certs/d972_b4_word_key_artifact_v1_20260816.json

permissions:
  contents: read

env:
  PRODUCER_SHA256: c4e15bf8085853808568345917ad71504b5a53bfacdb116c7a1ee9a605e925bf
  CHECKER_SHA256: 05b23ea8201e16820496aa45cf891abadbd92e6448c9f2a3bc8fd4f17b94f471
  WORDS_SHA256: 564a921be8114bdeb963f679c121e8d9aa90e148c65e95e393874fcba843e9f9

jobs:
  joint:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    strategy:
      fail-fast: false
      matrix:
        config:
          - q2a1_full
          - q3a2_full
          - q4a2_full
          - q3a2_q4a2
          - q3a2_q4a2_q5a2
          - q3a2_q4a2_q5a4
          - q7a1_full
          - q7a2_full
          - q7a3_full
          - q7a4_full
          - q7a5_full
          - q7a6_full
          - q5a2_q7a1
          - q5a4_q7a6
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13.5'
      - name: Selftest and exact synchronized obstruction
        shell: bash
        env:
          JOINT_CONFIG: ${{ matrix.config }}
        run: |
          set -euo pipefail
          mkdir -p ci/out ci/wheels
          cat > ci/requirements-hashed.txt <<'EOF'
          sympy==1.14.0 --hash=sha256:e091cc3e99d2141a0ba2847328f5479b05d94a6635cb96148ccb3f34671bd8f5
          mpmath==1.3.0 --hash=sha256:a0b2b9fe80bbcd81a6647ff13108738cfb482d481d826cc0e02f5b35e5c88d2c
          EOF
          python -m pip download --disable-pip-version-check --no-input --only-binary=:all: --no-deps --require-hashes --dest ci/wheels -r ci/requirements-hashed.txt
          python -m pip install --disable-pip-version-check --no-input --no-index --no-deps --require-hashes --find-links ci/wheels -r ci/requirements-hashed.txt
          python -c 'import sys, sympy; assert sys.version_info[:2] == (3,13); assert sympy.__version__ == "1.14.0"; print("PYTHON_SYMPY_VERSION_PASS", sys.version, sympy.__version__)'
          ulimit -v 12000000
          python -c 'import resource; soft, hard = resource.getrlimit(resource.RLIMIT_AS); expected = 12000000 * 1024; assert soft == expected; print("JOINT_V2_VMEM_GUARD_PASS", "bytes", soft, "expected", expected, "hard", hard)'
          python - <<'PY'
          import hashlib
          from pathlib import Path
          expected = {
              Path("search/d972_b4_burau_joint_v2.py"): "${{ env.PRODUCER_SHA256 }}",
              Path("search/check_d972_b4_burau_joint_v2.py"): "${{ env.CHECKER_SHA256 }}",
              Path("search/certs/d972_b4_word_key_artifact_v1_20260816.json"): "${{ env.WORDS_SHA256 }}",
          }
          for path, wanted in expected.items():
              if hashlib.sha256(path.read_bytes()).hexdigest() != wanted:
                  raise SystemExit(f"source/artifact hash drift: {path}")
          print("D972_B4_BURAU_JOINT_V2_SOURCE_ARTIFACT_HASH_PASS")
          PY
          set +e
          python search/d972_b4_burau_joint_v2.py --self-test 2>&1 | tee ci/out/selftest-$JOINT_CONFIG.log
          self_status=${PIPESTATUS[0]}
          set -e
          test "$self_status" -eq 0
          ! grep -Eq '(^|[[:space:]])(Traceback|Error|Exception)' ci/out/selftest-$JOINT_CONFIG.log
          grep -Fxc 'D972_B4_BURAU_JOINT_V2_SELFTEST_PASS' ci/out/selftest-$JOINT_CONFIG.log | grep -Fx 1
          marker_probe="$(mktemp)"
          trap 'rm -f "$marker_probe"' EXIT
          expected_probe="D972_B4_BURAU_JOINT_V2_FINAL config=q3a2_full status=UNKNOWN_BURAU_JOINT_ALLPASS output=ci/out/d972_b4_burau_joint_v2_q3a2_full.json"
          printf '%s\n' "$expected_probe" > "$marker_probe"
          grep -Fxc -- "$expected_probe" "$marker_probe" | grep -Fx 1
          printf '%s\n' 'D972_B4_BURAU_JOINT_V2_FINAL config=$JOINT_CONFIG status=UNKNOWN_BURAU_JOINT_ALLPASS output=ci/out/d972_b4_burau_joint_v2_$JOINT_CONFIG.json' > "$marker_probe"
          test "$(grep -Fxc -- "$expected_probe" "$marker_probe" || true)" -eq 0
          rm -f "$marker_probe"
          trap - EXIT
          echo D972_B4_BURAU_JOINT_V2_MARKER_EXPANSION_REGRESSION_PASS
          receipt="ci/out/d972_b4_burau_joint_v2_${JOINT_CONFIG}.json"
          set +e
          python search/d972_b4_burau_joint_v2.py --config "$JOINT_CONFIG" --output "$receipt" 2>&1 | tee ci/out/full-$JOINT_CONFIG.log
          full_status=${PIPESTATUS[0]}
          set -e
          test "$full_status" -eq 0
          ! grep -Eq '(^|[[:space:]])(Traceback|Error|Exception|UNKNOWN_RESOURCE)' ci/out/full-$JOINT_CONFIG.log
          test -s "$receipt"
          python - "$receipt" "$JOINT_CONFIG" <<'PY'
          import json, sys
          r = json.load(open(sys.argv[1], encoding="utf-8"))
          allowed = {"CANDIDATE_B4_A_BURAU_JOINT_ZERO_FIBER", "UNKNOWN_BURAU_JOINT_ALLPASS"}
          if (r.get("schema") != "d972-b4-burau-joint/v2-synchronized" or
              r.get("final_marker") != "D972_B4_BURAU_JOINT_V2_FINAL" or
              r.get("config") != sys.argv[2] or r.get("status") not in allowed or
              r.get("row_count") != 972):
              raise SystemExit("joint receipt scalar gate failed")
          rows = r.get("rows")
          if not isinstance(rows, list) or len(rows) != 972:
              raise SystemExit("joint receipt row gate failed")
          keys = [json.dumps(x.get("target_key"), sort_keys=True, separators=(",", ":")) for x in rows]
          if len(set(keys)) != 972 or [x.get("row_index") for x in rows] != list(range(1, 973)):
              raise SystemExit("joint key/index gate failed")
          for row in rows:
              if row.get("fiber_size", 0) <= 0 or row.get("full_GT_identity_count") is None:
                  raise SystemExit("joint lossless fiber gate failed")
          print("D972_B4_BURAU_JOINT_V2_RECEIPT_GATE_PASS")
          PY
          expected_candidate="D972_B4_BURAU_JOINT_V2_FINAL config=${JOINT_CONFIG} status=CANDIDATE_B4_A_BURAU_JOINT_ZERO_FIBER output=ci/out/d972_b4_burau_joint_v2_${JOINT_CONFIG}.json"
          expected_unknown="D972_B4_BURAU_JOINT_V2_FINAL config=${JOINT_CONFIG} status=UNKNOWN_BURAU_JOINT_ALLPASS output=ci/out/d972_b4_burau_joint_v2_${JOINT_CONFIG}.json"
          candidate_count="$(grep -Fxc -- "$expected_candidate" ci/out/full-$JOINT_CONFIG.log || true)"
          unknown_count="$(grep -Fxc -- "$expected_unknown" ci/out/full-$JOINT_CONFIG.log || true)"
          test $((candidate_count + unknown_count)) -eq 1
          set +e
          python search/check_d972_b4_burau_joint_v2.py "$receipt" 2>&1 | tee ci/out/check-$JOINT_CONFIG.log
          check_status=${PIPESTATUS[0]}
          set -e
          test "$check_status" -eq 0
          grep -Fq 'D972_B4_BURAU_JOINT_V2_CHECK_PASS' ci/out/check-$JOINT_CONFIG.log
          echo D972_B4_BURAU_JOINT_V2_INDEPENDENT_CHECK_PASS
      - name: Upload synchronized evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: d972-burau-joint-v2-${{ matrix.config }}-attempt${{ github.run_attempt }}
          if-no-files-found: warn
          path: ci/out/
