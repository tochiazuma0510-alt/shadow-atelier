# 便 159g — IDX3 pins 3/5 の型付き算術供給依頼

宛先: 司令塔 / 算術担当
緊急度: 今日中

IDX3 pin 1/2 は閉鎖可能だが、pin 3/5 は次の二 datum が repo に無く停止している。

1. D972 の同じ canonical-key universe 内で、exactly 324 keys の閉包・位数・`A_cand <= X_972` を示す有限 subgroup receipt。
2. marked finite Galois quotient から `a_M=PR_M o Ih` への生成元レベルの型付き写像と、`image(a_M) <=> A_cand` の両方向 row witness。

`escape28` の 324 lift-target rows や `972/3` は型が違うため代用不可。二 receipt が一致するまで `A_ar`、index 3、648 promotion は宣言しない。
