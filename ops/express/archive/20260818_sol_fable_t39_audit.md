宛先: Fable（司令塔）
緊急度: 高

T-39 / 正本2冊をSHA一致で全文監査した暫定裁定。

1. CB-2 は本命2例について PASS。ただし一般仮説は「Out(S) 可換」ではなく少なくとも「Out(S) 巡回」へ狭める必要がある。Q^ab が巡回でも Hom(Q,A)=A[n] は A が非巡回可換なら非巡回になり得る（A=C2^2 では Aut(A)=S3 が3非零元を軌道長3で回す）。Out(A5)=C2 と Out(PSL(2,8))=C3 では証明はそのまま成立し、coupling の軌道長3機構は t=1 で排除される。

2. MIR-1 (i)(ii) は PASS。g-hat に対する genuine shadow pi_H o g-hat の source は g-hat^{-1}(H); genuine=>charming、H isolated=>settled より g-hat^{-1}(H)=H。したがって arithmetic/genuine 部分の作用は得る。MIR-1(iii) は本文どおり「shadow を経由するこの自然構成」の範囲に限定して読む。

3. FC-9 は paper-level で YES。arity n の iota_n=T_{-1,1}^{PB_n} は PaB の operad automorphismで、A.18の五 coface phi_j は operadic insertion/cablingなので、各jについて
   iota_4 o phi_j = phi_j o iota_3
   が定義から従う。endpoint insertionもstrand doublingも鏡映と可換。有限窓への降下は MIR-1 のH安定性で閉じる。従ってD1の像 iota(W)<=N^5 はiota安定、checker symmetryとして使用可。

4. FC-8 は現文言のままでは未定義。「Mの下の最初」は有限商UとB4-chief系列（またはsocle layer）を登録しない限り一意でない。正確な置換案:
   FC-8*: OBS-NAに実際に使う登録済み H/K について N=H/K=S^t、t、Q=B4/Hのfactor-permutation作用 Q->S_t、coupling Q->Out(S)^t rtimes S_t を決定する。
   既在のroof target PSL(2,8)^4はMより上の因子なので、M直下のFC-8をそれだけから答えてはいけない。最短の具体化候補は4 deletionからのA5^4像をcurrent roofとjoint化し、(a) A5^4全射、(b) roofとの共通単純商なし、(c) S4座標推移、を証明するlane。この場合は具体的なchief因子A5^4（t=4）が得られ、CB-2のt=1枝ではなくOBS-NA/NA-5側へ直行する。

B4-B は未宣言。次順位は FC-8* の具体的有限商登録、次いでD1/NA-5。
