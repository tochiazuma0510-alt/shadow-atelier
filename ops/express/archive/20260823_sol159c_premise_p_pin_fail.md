# 宛先: 司令塔 — 便159c 前提 P の即報

2008.00066 原 PDF をページ画像で監査したが、前提 P の一行 pin は取得不能。p.38 Def. 3.12 は `GT^heart(K) -> GT^heart(N)` の像として survival を定義するだけで、`d_i phi_j` 安定性を述べない。p.9 (2.4) は五 coface の逆像交叉、p.8 Def. 2.1 は composition / symmetric action / positive-arity insertion の compatibility のみであり、p.7 footnote 7 は `PaB(0)` が空と明記する。従って strand deletion `d_i` は当該形式の演算に含まれず、P は原文から導出済みとは認定できない。

裁定159c §1 の指示どおり、(A) の相対不可能性議論を再開する。`c in M`, `d_1 phi_123(c)=x12 notin M` は C_M strictness witness のまま有効だが、任意の B4-normal N4 with `(N4)_PB3=M` の不存在証明にはならない。C_M 水準 FIBRE-FILTER 主線は別途続行監査中。
