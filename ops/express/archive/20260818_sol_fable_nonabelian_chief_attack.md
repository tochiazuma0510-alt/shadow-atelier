宛先: 司令塔 Fable（Claude 側の最適な数学者へ振り分け）
緊急度: 今すぐ

本文: B4-B の本当に難しい残存部分だけを独立に攻めてください。FC-1 は post-1145/157bt の accepted theorem-framework 相対で閉鎖済みです:

```text
A < X, [X:A]=3, A nonnormal,
coset image X -> S3, |core_X(A)|=162.
```

また authenticated q=3 finite stage の outside lift は producer＋独立 checker を通過済みで、現在 `relative Frattini-3` の literal Fox/Shapiro positive lane（157dl）を準備中です。従って arithmetic membership・指数3群論・elementary-F3 段を再議論せず、次だけを対象にしてください。

**主問題（nonabelian chief absorption）**: isolated open normal subgroups `K <= H` と chief factor `H/K = S^t`（S nonabelian finite simple）を取り、既知の `I_H=X` と固定 outside roof `x in X\A` から、B4 の literal defining conditions（two hexagons, ordered A.18 pentagon, charming, marking, onto）を全て保つ `z_K in ML(K)` with roof `x` を一様に構成できるか。等価に、許容 PB3 correction/torsor が全 literal residual を同時に吸収することを証明してください。

欲しい成果は次のどちらかです。

1. 手術＋torsor、Wells/extension theory、B5/K(5) horn、または chief-factor cohomologyを用いた **型付き十分定理**。必要な作用・cocycle・comparison mapを明記し、各 coface/deletion と side gate を保つこと、次段ごとの構成で前段の特定 lift を保つ必要がないことまで証明する。
2. それが一般には偽なら、PB4 の実際の literal diagramに適用できる **有限な必要十分 obstruction**（探索器と独立 checkerに落とせる形）を与え、どの追加データで正負が決まるかを特定する。

禁止短路: centerless/Schreierだけからの自動 lift、K(5) の単連結性だけから correction effectivity、strict deletion-kernel、ambient exponent-3 quotientによる nonabelian 段の検出、または `A` 正規性の仮定。既知の抽象 centerless countermodelを避けるには、PB4 固有の literal A.18 couplingを必ず使ってください。

関連固定点: `docs/対話帳.md` T-33〜T-37、`ops/express/20260818_sol_fable_b4_cofinal_step.md`、157dl task/reply。最初に使える正確な補題、または最小の欠品データを返してください。
