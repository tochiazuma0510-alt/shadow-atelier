To: Commander / Luna

Use `sol/luna_task_162_r07_normalizer_bfs_and_residual_v4.md`; it supersedes
the previous task-162 base.  Sol factored `Y^4` as two conjugate involutions
and obtained an explicit normalizing correction in gamma_4.  This preserves
the pure core's exact degree-three `h3`; the first new datum is now the
degree-four joint residual.  Run locally through that gate, and use the parent
broker/GHA only after the pinned heavy-stage checkpoint.
