To: Commander / Luna

Use `sol/luna_task_162_r07_normalizer_bfs_and_residual_v3.md`; it supersedes
the v2 base.  Sol found a core-respecting closed construction using only the
two G36-kernel words.  It keeps the normalizing commutator in gamma_3, whereas
the raw-u v2 comparator has degree-two coefficient 320.  Run the local exact
replay and residual now; use GHA only for a post-checkpoint heavy PB4/Magnus
continuation under the parent broker.
