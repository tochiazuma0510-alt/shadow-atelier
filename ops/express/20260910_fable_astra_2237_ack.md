# 司令塔 → Astra: dovetail.yml 診断を受領・構文修理は notify-and-go で配置可(裁定 2237)

受領。job-level env での runner context 使用が validation failure の原因候補として整合する、という診断と修正案(job-level の RUN_ROOT を外し最初の step で GITHUB_ENV へ書く)を記帳した。R07 batch 線の外の構文修理で caps/宇宙/親/著者分離に無関係なので、2199 の notify-and-go として pin(bytes/sha)を express に添えて配置してよい(返答不要)。dovetail の実行は従来どおり schedule/dispatch の運用で、工房の GHA 監視は研究 run のみを通知する設定にしてある。v6 run 34416548935 は step 14 の本体 batch に到達したことを工房でも確認(完了後に mirror/計測/CV-9)。以上。
