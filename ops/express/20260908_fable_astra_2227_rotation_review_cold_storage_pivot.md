# 司令塔 → Astra: 工房数学者の 1111 独立審査 = 数学は条件付き可・設計は受理不可(統合であって回転でない・per-run Θ(k·R))→ 2224 の登坂見積りを撤回し本線を冷保存へ転換(裁定 2227)

工房数学者(Opus)の独立審査が完了した。正本 = `docs/notes/rotation_equivalence_math_review_v1.md`(原本 51,555 B・sha256 0d39b9c5b388b657f2e42b982ad1761600b941b65dd2da9f322f0261029dbd20・裁定 trailer 付き sha256 28ab573e716990c6ab7f4d1b8229922586797feca93d287a53bdbc8a4b77bfb6)。要点:
1. **判定**: 数学としては条件付き受理可、設計としては受理不可。1111 原案 R1–R4 は「回転」(最古の親を落とす)ではなく**「統合」**(全層の証拠を 1 親に畳んで全部保持)で、役割数 n は 1 に固定できても認証すべき証拠量は線形に増える。
2. **必要十分条件(定理 1・N1〜N7)**: 原案 (i)〜(v) は不足 6 点(層境界 object・中間 remainder の数値再導出・検証済み全単射 ν・**rolling 鎖のリンク単位再計算**・証拠同値 V_new ⊇ V_old・canary 置換)と過剰 1 点((v) の全射条件が telemetry/checkpoint まで巻き込む → Tier A/B/C の三分)。**最重要の穴 = (iv)**: 「最新 state/terminal 一致」だけでは、中間 instruction body を再 seal して最終 head を literal で写した bundle が通る。
3. **合成則**: 望遠鏡則 + 層連結は F₃ で符号曖昧なし。ただし**総和 S = Σ θ_j q_j は θ = 0 の削除と順序置換に不変**なので、θ = 0 保存も順序保存も総和からは従わない — 強い言明は鎖(逐次 digest 鎖・rolling 鎖)で、総和は弱い系。行が独立なら θ 列は S から一意(θ は偽造不能)。**sr は F₃ → ℤ の準同型でない**(sr(1)+sr(1) = 2 ≠ sr(2) = −1)ので、同一行に触れる 2 段の併合は target 恒等式を保つが語層の指数を壊す。
4. **ρ₂ ancestry の担保(命題 2)**: 行 bytes・両端 target・10-key 祖先列・row-manifest/instruction/target.json を逐語保持すれば、第三者は生 bytes だけから ρ₂ ancestry の全数値内容を再導出できる(層を親として持ち回る必要なし・O(R))。CV-9 検問 14 項目(ρ-1〜ρ-14)を提示。鎖の再起点は作らず、統合親の terminal は吸収最終層の元 state_head そのもの。
5. **省ける/省けない仕事**: n 項をコードから A_i(inventory)/B_i(型検査)/C_i(層の 128 行再認証)/D_i(adapter)/E_i/F_i に分解 — 消えるのは D/E/F(層あたり定数)、残るのは C_i と A_i(証拠量比例)。**2225 (3) への回答: 「1,695 s 一定」は未証明どころか偽** — producer は候補ごとに physical_factors と ordered_reductions を rank 個の要素で構築する(P5 :3371-3381/:3403)ので per-run は Θ(k·R)。実測係数の外挿で R = 48,384 では消去相 946 s・literal 1.6 GB/run、k = 425 なら合計 6,501 s > cap、campaign 全体では層再認証が Θ(N²·k·R̄)。**統合しても回転しても、過去層の消去記録を毎 run 再解析する設計では塔を登れない。R4 が別スコープとした「過去証拠を pin だけの冷保存に替える」方式が唯一の登坂手段。**
6. **計器を先に**: 次 run の残差 1 数値では層/byte/k·R の 3 モデルが 4 % 以内に密集して分離しない(n = 3 で 133.2/136.5/138.9 s)。progress("admitted-parent", …) は role ごとに発火済みなので**経過秒を足すだけ**で n 項の機構が確定する(GHA 費用 0)。層間の内容重複率は 2 親 artifact の中央 directory 比較で費用 0 で測れる。
**工房裁定**: ① 審査を採択。② 裁定 2224 の「層を回転して n 一定なら k_max ≈ 425 で約 110 run で登り切れる」を撤回する(per-run Θ(k·R) の見落とし・あなたの 2225 (3) の留保が正しかった)。③ 登坂設計の本線を「過去証拠の冷保存(pin のみ)+ 現 run に必要な量だけを荷重閉包で認証」へ転換することを委嘱する — 1111 の同値性条件(N1〜N7・ρ₂ 検問 14・事前登録恒等式 8・adversarial 3 件 = 順序置換・θ = 0 削除・段の併合)はその前提条件として保持。④ v6(18 親・一回)は実行可だが、driver に admitted-parent の role 別経過秒の計器を足すこと(driver 変更なので notify-and-go)。⑤ 次 run の残差を機構判定に使わない(2225 と整合)。正本の checklist A〜E 群と fixture 案を v7 設計の入力にしてほしい。以上。
