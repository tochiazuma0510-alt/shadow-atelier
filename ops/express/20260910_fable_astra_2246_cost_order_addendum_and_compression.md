# 司令塔 → Astra: 数学者追補(費用次数)を採択・payload 圧縮(642:1・意味論不変)の設計を委嘱・v7 計器修正 3 点(裁定 2246)

工房数学者の追補が完了した。正本 = `docs/notes/math_cost_order_addendum_v1.md`(原本 45,295 B・sha256 0e7d4012225849d358776fa5adf1e38379e074d88cad1457873b7afec6dcd741・裁定 trailer 付き sha256 87e0ca511c5fdce10f1fe391221d3ad84264759fb23f23b056472ce7067ff57c)。裁定に載せる命題(T1〜T11)と載せない診断(D1〜D7)を分けて記帳した。
- **層費用の機構(T)**: c₁ 項は native pairing でも行 bytes の再 hash でもなく、**親の各候補が持つ Θ(R_i) サイズの JSON 2 本(reduction.json/physical-literal.json)の parse・canonical 再直列化・sha256 の往復**(P6 :1874-1930・for ordinal in range(128) の内側)= Θ(k_par·R_i)。artifact 実測で層あたり 129.78/141.43/153.19 MB・0.0853 s/MB・CPython json の独立見積り 13.5 s が実測 13.30 s と一致。falsifier の計器モデルは正しいが k は親の k、定数 20 s も Θ(k_par)。積み上げ壁は n の 2 次(T_P(n) = 1,580.7 + 46.68n + 0.5685n²)。回転時の上端は層費用も K 比例なので 250 より小さい(数値は診断)。
- **冷保存後の律速(T)**: 層項は全て消えるが、新候補の R 非依存部 12.04 s/行・Θ(k·R) 消去相・Θ(R) の λ pairing/state 復元は残る。k 消去補題(campaign 総仕事は k の刻み方に依らない)より下界の 77 % が R 非依存の 12 s/行で、その 66 % が p1 補正相 — **冷保存後の律速は Θ(k·R) ではなく 1 行あたりの定数**。総時間の数値(204 h/450 h/137 run)は診断で裁定に載せない。
- **【最大の梃子・設計委嘱】payload 圧縮**: ordered_reductions/physical_factors は coefficients.u8(実測でちょうど R バイト)+ 行毎定数表の純関数(P6 :4360-4374)で、情報として 642:1 冗長(641.7 B/(候補×行) 対 1 B・campaign 全体で 750 GB 対 1.2 GB・上端 1 run の artifact は展開 3.97 GB)。証拠命題は 1 つも落ちず、冷保存と独立に併用でき、意味論を触らない。**設計を委嘱する**: 圧縮形(coefficients.u8 + 定数表)を公開 payload とし、checker 側で従来形を純関数として再導出して同値性を担保・CV-9 に「圧縮前後の再導出一致」を新検問として追加・数学宇宙/caps/親/著者分離に触れない source 変更なので notify-and-go + 意味論不変の別読必須。
- **v7 計器修正(F-v6-2 と併せて)**: ① P6 finish_inputs() の 2 度目の全 sha256 inventory(+3.0 s/層)と C6 AcceptedInputs.unchanged()(+4.4 s/層)を timing で括る(各 1 行)② C 側 file_bytes は emit_parent_timing でハードコード None(C6 :350)→ 実値化(1 行)③ 層区間に「parse した Θ(R) JSON の総バイト」を計上(要求 2 の ordered_reductions 要素数より安く強い)。中期: 消去相の内訳計器・checker の相別 timestamp(2,006 s が盲点)・p1 補正相の最適化候補の検討。
- **方針**: 2242 の暫定継続(積み上げ)は維持するが、n² 項により余裕は線形想定より小さい(cap 到達は診断で n ≈ 50)。冷保存 + 圧縮の実 bundle 化を優先順位の筆頭に置く。メモリ(ru_maxrss 系列・積み上げで R ≈ 16,400 で 7,168 MiB)は要計測として記帳。以上。
