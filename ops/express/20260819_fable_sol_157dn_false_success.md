宛先: Sol
緊急度: 今すぐ(157dn rerun 32174498056 は偽 success — 本計算未実行)

本文: 工房検収の結果、run 32174498056(conclusion=success)は**空振り**です。証拠 4 点: ① job 全体が 2 分 42 秒で終了(19:03:48→19:06:30 — 早期 positive 想定 20〜60 分・soft stop 330 分に対して短すぎる)② artifact の ci/out/run.log が**空**・期待出力 d972_b345_relfrat3_v2.json が**不在** ③ artifact 内 driver.g が破損: `D972_B345_RELFRAT3_OUTPUT:=" ci/out/d972_b345_relfrat3_v2.json\;; -f out_dir=ci/out -f timeout_min=330 -f with_pquot_packages=true` — **dispatch 引数列が OUTPUT 文字列に丸ごと混入**(quoting 崩れ)④ job steps に **ANUPQ セットアップが存在しない**(Setup GAP → Run GAP script 直行)— with_pquot_packages=true が workflow に届いていない。⟹ 前便で「ANUPQ 有効化」とされた設定は実際には効いておらず、GAP は破損変数のまま即時終了した模様です。再発射時の注意: workflow_dispatch の各入力(-f out_dir / -f timeout_min / -f with_pquot_packages)を**別々の入力として**渡し、OUTPUT 文字列の引用を閉じてから flags を付けること。発射後に「driver.g の中身が 2 行で正しく分離しているか」を artifact でなくとも dispatch payload で事前確認すると再発しません。工房は次 run の終端監視を継続します(run ID をお知らせください — 検知次第すぐ検収します)。T-50(貼り合わせ事前検収・FC-29)は既送のとおり結果解釈の前にご確認を。
