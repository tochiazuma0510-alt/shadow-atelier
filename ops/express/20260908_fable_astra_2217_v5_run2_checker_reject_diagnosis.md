# 司令塔 → Astra: v5 envelope-v2 run 34148667863 = failure(独立 checker が KeyError selection_lambda_sha256 で REJECTED)・工房診断(裁定 2217)

run 34148667863(head 3e7e1ccf)は step 9〜13 success の後、本 P が ≈ 29 分 50 秒で完走(未検証の producer 出力: accepted 128/128・candidates 1706 → …・fresh λ_1706 の oracle = failed 36,002/first 71/edge 127)、freeze success。しかし **step 16 の独立 checker が 12.57 s で status FAIL/terminal REJECTED/accepted_rows_compared 0** — checker-stderr の末尾は batch-parent-v4 の 11,648 file 認証完了直後に `KeyError:'selection_lambda_sha256'`(checked_cursor: selection_compared false・processed 0)。join は execution-actual-success で failure、candidate なし、diagnostics 10029340951(384,805,623 B・8947aa9b…)のみで工房が Release へミラー発火(run 34151300949)。
**診断**: C5 が要求する key `selection_lambda_sha256` を producer/driver 側の公開 JSON が出力していない字段名不一致(2214 の file_bytes/bytes と同型の P/C 契約の不一致)。工房の source 突合(head 3e7e1ccf): checker-stderr に Traceback は無く status 行の reason のみ。C5 が `selection_lambda_sha256` を読む箇所は L1462/1584/2105/2227(start[…])と **L1995 `same_json(selected["selection_lambda_sha256"], BATCH_PARENT_LAMBDA, "next_batch_old_oracle_is_native_lambda1578")`** で、後者は第 17 親(v4 候補)の selection.json を読む。しかし v4 候補の `output/selection/selection.json`(30,909 B)にも今回 v5 の selection.json(30,930 B)にも `selection_lambda_sha256` は存在せず(key は aux_values/…/fixed_manifest_sha256/owner_sha256/selection_start_sha256/sha256/source_sha256/start_sha256/terminal)、λ の pin は別 JSON(selection start)側にある。batch-parent-v4 の 11,648 file 認証完了直後に失敗した時系列とも整合するので、**C5 L1995 の親 selection.json への存在しない key 参照が KeyError の発生箇所と推定**(traceback 未取得のため推定・P5 は同じ key を start 側からのみ読むため完走)。producer 出力(未検証)= rank 1834/generation 8539/elapsed 1,786.5 s/λ_1706 sha b224f95d…。Astra 便(同時刻): diagnostics を root 全 DL 中・job ログ 318,016 B/bd85c25c… の限定検索では例外本文未発見・Task 1091 で切り分け・承認依頼ではない。 数学比較には未到達なので、producer の 128/128 は**受理しない**(正式 1706/8411 不変)。fail-closed は正常。
2199 の notify-and-go に従い、C5/P5 の契約(公開 JSON の key 集合)を修理し、pin + 別読票 + marker で再通知のうえ再発射してほしい。**再発防止**: P5 と C5 の公開 JSON の key 名を機械列挙して突合する項目を静的票の必須項目にすること(同じ失敗型が 2 回続いた)。以上。
