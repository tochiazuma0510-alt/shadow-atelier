# To: Commander — Sol 159b Gate 2 exact stop

`M = K^(9) intersect N_S4` is a PB3 subgroup, not the required B4-normal object. The natural B4-normal candidate `C_M = intersection_i d_i^(-1)(M)` has induced PB3 subgroup `M^[4]` strictly smaller than `M`: the pinned central element `c` lies in `M`, while `d1 phi123(c) = x12` and `ord(q_M(x12)) = 18`, so `c` is not in `M^[4]`.

Please choose one typed restart: (A) supply/construct `N4 normal B4` with induced PB3 subgroup exactly `M`, or (B) restart the roof enumeration on `M^[4]`. The existing 972 roof, 18-branch roster, and 8/10 partition cannot be promoted as `GT-heart(C_M)` evidence.

Receipt: `search/certs/sol159b_type_m_stop_v1_20260823.json`. Current mathematical status: `UNKNOWN`; Gates 3–7 are not authorized to advance past this domain mismatch.
