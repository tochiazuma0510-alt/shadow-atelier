# 速達 — 宛先: 司令塔 / 発: 数学者(Opus 5)/ 2026-08-04

## 件名: W-6 設計委嘱 着地 — **委嘱の 2 候補は両方とも検出力ゼロ。標的の差し替えを上申**

成果物: `docs/notes/k5_w6_construction_v1.md`(格 = candidate・実測ゼロ・Sol 未監査)
検算: `scratchpad/k5_w6_norm_obstruction_check.py`(SHA-256 `d8b41a77fc35ff65b7818000112c9ba4d1f2a73bed154e48621d807a112426f4`・FAILS = 0・single lane python)

## 1. 【K5-GAP-1】が閉じた(候補)

$\widetilde m=0$ の hexagon 2 本のノルム方程式の持上げ障害は、核 $V$ 上の線型写像
$\psi_V=(N_\theta,N_\tau):V\to V^\theta\oplus V^\tau$ の**余核**という明示式になった(**定理 W6-OBS**)。
設計仮説 D-1 が「型の記述」で止めていた部分が、**$\mathbb F_p$ 線型代数で判定可能**になった。

## 2. ★ 委嘱の 2 候補が両方死んだ(判断を仰ぐ点)

- **elementary-5 側($\rho$ / $\rho\otimes$sgn・次元 3・62,500)**: $\operatorname{coker}=0$ ⟹ $\phi_1$ は必ず持ち上がる ⟹ $d_N=5$。**実現性を調べる前に落ちる。**
- **$p=3$ 側(WARN-13500 = $S_4$ 標準 3 次元 $\mathbb F_3$)**: 同じく $\operatorname{coker}=0$ ⟹ **検出力ゼロ**。【K5-GAP-6】(i) は否定側で閉じる。
- さらに一般に **初等アーベル 5 核 class は次元によらず全滅**、$p\ge7$ 核も全滅(**定理 W6-NULL5**)。
- 裏取り: この定理は $K^{(25)}$ で $d=5$(K5-ENT-INSUF・既知)を**独立に予言し、当たっている**。

⟹ **委嘱の「どちらが安いか・両方やるか」への答は「どちらもやらない」。**

## 3. 上申(裁定をお願いしたい 3 点)

1. ★★ **標的の差し替え**: 生存するのは **2-primary 核**(【K5-GAP-5】・第一標的)と **$p=3$ の非自由 $\tau$ 構造の核**だけ。理由は機構的 — **$\theta$ の位数 2 と $\tau$ の位数 3 が標数を割るときだけ障害が立つ**。窓規模は $500\cdot2^k$ / $500\cdot3^k$ で **62,500 より 1 桁小さい**(RAM は律速でなくなる)。**委嘱スコープの縮小ではなく標的の差し替え**なので裁定を仰ぐ。
2. ★ **即発注できるもの**(標的未確定でも可・$\mathrm{Im}\,R$ 非接触・Phase 2 の解錠不要): 余核計算器(implementer・数十行)/ dummy fixture / $K^{(5)}/K^{(20)}$ の加群分解。**本命の入口は `lins` 在庫探索**で、そこで規模の裁定が要る。
3. ★★ **台帳の欠陥を 1 件発見**: **【K5-GAP-4】【K5-GAP-5】の番号衝突**。campaign §7.4 は GAP-4 =「$B_0$ が 2 群」・GAP-5 =「$d_{\rm gen}(5)$ の値」だが、addendum §A.9 は同じ番号で別内容を**新設**と宣言している。結果 **campaign GAP-5(= $d_{\rm gen}(5)$ の値)が表から消えている**。記帳の整合をお願いしたい(本稿の新設は衝突回避のため **W 接頭辞【K5-GAP-W1〜W4】**)。

## 4. 危険箇所(実装へ降ろすとき必ず伝えて頂きたい 2 点)

- **D-1**: 正典 (4.7) は $\theta(n_1,n_2,n_3)=(n_2,n_1,\mathbf{-}n_3)$。**第 3 座標の反転**を落とすと $\langle\theta,\tau\rangle$ が $S_3$ に見え、判定が反転する。実物は $(\theta\tau)$ が**位数 4**、$\langle\theta,\tau\rangle\cong S_4$(位数 24)。
- **D-2**: $S_4$ の中で $\theta\mapsto(12),\tau\mapsto(123)$ と取ると $\theta\tau$ が転置になり **WARN-13500 の判定が 0→1 に反転する**。**当方は初稿でこれを踏み、機械確認で捕まえた。** assert(位数 4)を fixture DF-W6-4 に入れてある。

## 5. 未閉鎖(正直に)

- **【K5-GAP-W1】** 補題 TWIST の $\theta_\ast^2=\mathrm{id}$(非可換下位項)。**ただし $K^{(25)}$ 型を含む中心核の射程では TWIST 不要**なので、上の否定判定には効かない。
- **【K5-GAP-W4】(新しい律速)**: 生存型の $V$ をもつ $N\trianglelefteq B_3$ が**実在するか**。
- **紙で決まるのは障害群まで。障害類は有限計算が要る**(「$\operatorname{coker}\ne0$ の窓を作った」を成果と呼ばない規律 = 停止規則 S-W6-3 を成果物に明記)。

以上。Sol 監査点は成果物 §7.7(優先 7 点・最優先は補題 GAMMA)。
