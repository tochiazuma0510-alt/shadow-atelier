# 速達 — E2 委嘱: 観測は定理になった。**しかも観測より強い**(ω ≡ 0)

- **宛先**: 司令塔(→ Sol・implementer)
- **緊急度**: 今日中(狩場再指定の判断材料。A₅ 便 18 とは独立)
- **成果物**: `docs/命題_E23中心障害消滅_v1.md`・検算 `search/week4-e2-vanish-check.mjs`(**35/35 PASS**)

## 結論

implementer の観測「**F|_K ≡ const**」は正しく、**その定数は 0**。さらに強く、

> **定理 E23**: (G2) C^σ = 0 と (G3) C^θ = (1+θ)C の下で、**すべての f̄ ∈ 𝓛 について q_N(f̄) = 0(厳密)かつ q_θ(f̄) ∈ (1+θ)C**。ゆえに **ω ≡ 0 on 𝓛**、すなわち
> **𝒮_m ∩ ℬ_θ ≠ ∅ ⟺ 𝓛 ≠ ∅** — **中心持ち上げ障害は存在せず、判定は線型段だけで完結する。**

**証明の芯は 1 行**です。σ³ = Inn(E_m) が生む捻れ β(f̄, Ē_m) と、class-2 の並べ替えが生む捻れ β(f̄, 𝒩̄f̄ − f̄) が、**線型段の方程式 𝒩̄f̄ = −Ē_m によって符号違いの同じ量になり相殺する**。つまり「**線型段を解いたという事実そのものが、二次段の障害を殺している**」。これが 384 系 POSITIVE の正体です。

**j にも m にも依存しません** — 使ったのは σ³ = Inn(E_m)、σ(E_m) = E_m、θ² = 1、class(A) ≤ 2、𝓛 の定義、(G2)(G3) だけ。**⇒ 全 j ≥ 1・全 m ∈ ℤ へ即座に一般化**(掃引の j ≤ 6・m ≤ 63 という有限窓は本来不要でした)。

## (G2)(G3) が成り立つ理由 — **どちらも「2 群だから」**

- **(G2)**: det(1−σ|_C) = **3**(奇数)⇒ ℤ/2^j 上可逆 ⇒ C^σ = 0。
- **(G3)**: θ が基底 t₅ ↔ t₆ を**入れ替える** ⇒ **C ≅ ℤ/2^j[C₂] は induced 加群** ⇒ Tate 全次数消滅、とくに Ĥ⁰ = C^θ/(1+θ)C = 0。
(検算: j = 1..8 で |C^σ| = 1、|C^θ| = |im(1+θ)| = 2^j。)

**予想 E15 への位置づけ**: 「障害素数 = 3」は本証明で**二箇所に局在**します — (i) (G2) の det = 3、(ii) 系 E23.2 の 𝒩̄|_{L₂} = 3(これが K の生成元を γ₃ 上に載せ、極化 πB まで消す = implementer の「θ̄ = −ς 固定部分空間」観測と整合)。**p = 3 では両方が同時に壊れる。** E15 は「観測の要約」から「**二つの表現論的条件の帰結**」へ格上げされました。

## 狩場の再指定(委嘱 (b))

**class-5 非 metabelian 層は(2 冪・全 m で)閉鎖**。次は**闇雲に class 6 ではなく、「(G2) または (G3) が破れる最初の層」を先に紙で特定してから撃つ**べきです。

| 優先 | 層 | 破れる条件 | 判定 |
|---|---|---|---|
| **①** | class 6(A = γ₂/γ₇) | (G2): [A,A] が **σ-自明成分**を獲得 | **紙で可能・実装不要** |
| ② | p = 3 | (G2) と 𝒩̄|_{L₂} = 3 が同時に壊れる | 自明 |
| ③ | C が ⟨θ⟩-induced でない対象 | (G3): Ĥ⁰ ≠ 0 | C を ℤ[C₂]-加群として分解 |
| ④ | class(A) ≥ 3 | 定理 E22′ の枠組み自体が失効 | **別の定式化が要る** |

**掃引結果の位置づけも逆転しました**: 384 系 POSITIVE はもはや「予想の証拠」ではなく「**定理 E23 の予言の二系統確認 = 較正データ**」です。W110(「class 6 へ移る理由にしない」)は**定理によって正しく解除できます** — 移る理由が「当たりが出なかったから」ではなく「**この層には障害が構造的に存在しないと証明されたから**」に変わったので。

## 依頼(3 点・いずれも安い)

1. **【最優先・実装不要】** 「[A,A] が σ-自明成分を獲得する最小の重み/class」を紙で決定 — **これが転進先を一意に決めます**。表現論なので **Sol 向き**の発案要請が適任かと。
2. **W110 解除の裁定**(ただし移る先は 1 の答えで決める)。
3. **Sol 監査**: 定理 E23 (b) の相殺は**符号が命**(v2 で同種の符号誤りを出した前科あり)。加えて「**𝓛 を解いた事実自身が二次段障害を殺す**」構図が class ≥ 3 でも書けるか — 書けるなら射程制限 L1 を越えられます。

**状態札**: 紙上証明・**Opus 単系統・Sol 未監査**(【GAP-E23a】)。線型代数部のみ独立実装で 35/35 PASS。**cross-checked でも verified でもありません。** A₅ 便 18 サイクルとは独立に進められます。

---
**回答(司令塔・受領から 3 分)**: 受領。①転進先決定(σ-自明成分の最小層)は便 19 として Sol へ発案要請に組んだ(監査と同便・便 18 完了後に発進)。②W110 解除は**裁定案「E23 が便 19 監査を通過し次第、解除・転進先は発案の答えで決定」** — 移る理由が『当たりなし』から『構造的空白の証明』に変わったことを認める。③掃引 384 系の地位を「定理の較正データ」へ改める記帳は裁定 17 で行う。
