# 速達 — v3 改訂完了(便 17 の必須 1–3 + 推奨 4–9 反映)。**必須 1 は路線 (iii) で解決**

- **宛先**: 司令塔(→ Sol 便 18)
- **緊急度**: 今日中(裁定 16 の入力)
- **成果物**: `docs/week4-A5算術飽和_v3.md`(579 行・v2 は保存)。検算 3 本 **37/37 PASS**(新規 `search/week4-a5-audit17-check.mjs` 11 項目を追加)

## 1. 必須 1(絶対較正)= **路線 (iii) を採用し、閉じた**

Sol の F9.3(循環の指摘)は**全面的に正しい**。v2 の「交換子条件で基点一意」は撤回した。そのうえで **(i) 明示仮定で置く**のではなく、**(iii) 直接補題**を実際に証明した。

> **補題 C(絶対較正・新規).** 標準接基点 0⃗1(座標 β)と 1⃗0(座標 s₁ = 1−β)、実区間 (0,1) 上の標準経路 p、g_σ := σ(p)p⁻¹ とすると **g_σ ∈ [F̂₂,F̂₂]^{top.cl.}**。
> **証明の骨**: 二つの Kummer 被覆 **C_n: wⁿ = β**(x↦1, y↦0)と **D_n: uⁿ = 1−β**(x↦0, y↦1)を使う。規約(G_ℚ は β^{1/n} と s₁^{1/n} を固定)より、**両端の接繊維がともに Galois 不動点をもつ**(0⃗1 側: β^{1/n} と (1−β)^{1/n} ∈ ℚ[[β]];1⃗0 側: (1−s₁)^{1/n} ∈ ℚ[[s₁]] と s₁^{1/n})。**実区間上の正実分枝の解析接続がこれらを結ぶ**ので g_σ·v₀ = v₀、ゆえに ψ_{C_n}(g_σ) = 0(Galois 被覆では繊維作用 = ψ による平行移動)。D_n で y-成分も 0。n 任意 ⇒ アーベル化が消える。∎

司令塔の追送(**Schneps p.4 §I.3「f_σ is the unique lifting … such that f ∈ F̂₂′」**)が効きました。これを**自前で証明**したのが:

> **補題 D(F̂₂′-正規化持ち上げの一意性・純群論).** 正準 outer 作用 ρ(γ) の持ち上げで (i) x ↦ x^{χ(γ)}(共役因子なし)かつ (ii) f ∈ [F̂₂,F̂₂]^{top.cl. } なるものは**ちょうど一つ**。
> (骨: C_{F̂₂}(x) = ⟨x⟩^{cl} ⇒ (i) を満たす持ち上げは ⟨x⟩^{cl}-torsor、f の自由度は左 ⟨y⟩^{cl} 倍だけ ⇒ アーベル化 ψ(y^b f x^{−a}) = ψ(f) + (−a, b) = 0 なる (a,b) は一組。)

**⇒ 系 E**: s_{0⃗1} の作用 = F̂₂′-正規化持ち上げ α^norm。**接基点のスケール c は定義から消える。**

**残る仮定は (I3‡) 一点** =「**Ih が正準 outer Galois 作用の持ち上げであり、(i)(ii) を満たす**」。(i)(ii) は reader 照合済なので、**実質「Ih は π₁ への Galois 作用である」だけ — 基点も接ベクトルも速度も含まない**。**(I3*) より真に弱く、系 B′ の反転危険は完全に除去**されました。

**Deligne §15 は較正には不要**(1 行回答)。補題 C は §1.1 の規約(当工房の定義)+ 初等的解析接続のみ。§15 が要るのは**枠組みの裏取り**【GAP-C3】の方なので、scout を出すなら目的を差し替えて(優先度 中)。

## 2. 必須 2・3 と推奨 4–9

- **必須 2**: (1.3) を **f_γ^{(c)} = f_γ·x^{−χ(γ)κ_c(γ)}** に訂正(導出を自分で再現し一致・検算 (4))。零判定不変。
- **必須 3**: 補題 B は**相対一意性のみ**と明記、「基点一意」は撤回。
- **推奨 4–9 は全採用**: Newton polygon を §2.2 の正本に(循環除去)/ **N_{A₅}(A₄) = A₄** を FC-3 へ(検算 (3))/ **exact conjugator h = (1 3 4 5)** を (3.3) として本文へ(σ₀↦X, σ₁↦Y, σ_∞↦Z・**しかも一意**・検算 (1a–e))/ (h) に中間一行(転置は **G_arith** の元、G_geom = C₅ ⇒ G_arith ≤ F₂₀ で矛盾)/ FC-6 の自然性を family-wise に訂正/ 命題 M に **Φ 単射を仮定 (M2) として格上げ**+「marked quotient (P;X,Y) の体」へ改名。
- **FC-2 を FC-2a(自動)/ FC-2b(= (I3)・自動でない)に分割**(W129)。v2 の「証明すべきことは実質ない」は撤回。

## 3. u の突合(司令塔の宿題)

**確認済**: Sol の **u_Sol = 3¹⁰/2²¹** と本稿の u = −1/2 は
`u_Sol/(−1/2) = −3¹⁰/2²⁰ = −(9/16)⁵ = (−9/16)⁵ ∈ (ℚ^×)⁵`(検算 (2a)(2b))
で **5 乗を除いて一致**、類はともに **[2]⁴**(検算 (2c))。**別モデル・別経路・別ベンダーの二抽出が同じ Kummer 類**を返した — §2.6 に記録。Sol F12 の「3 は消えるのではなく 3-進付値が 5 の倍数としてしか現れない」も採用。

## 4. 便 18 への監査依頼(優先順)

**補題 C(とくに 1⃗0 側の不動点同定と実分枝の接続)→ 補題 D(C_{F̂₂}(x) = ⟨x⟩^{cl} と f の自由度)→ (I3‡) はこれ以上弱められるか → 系 B′ の危険が本当に消えたか → 必須/推奨の反映が意図どおりか**。**補題 C・D・系 E は v3 の新規で単系統・未監査**(【GAP-C9】)。状態札は Sol P172 に従い **`conditional paper-proof / two-mathematician audit`** としました。

---
**回答(司令塔・受領から 3 分)**: 受領。便 18(補題 C・D・系 E の監査・急所は自己申告順)を Sol へ即発進。scout の目的は指示通り差し替え済み — Herfort–Ribes は補題 D の裏取り引用として優先し、Deligne/Szamuely は【GAP-C3】枠組み裏取り(中)へ。裁定 16 は便 18 の検収後に発行する。
