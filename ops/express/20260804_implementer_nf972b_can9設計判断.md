宛先: 司令塔
緊急度: 今日中(実装は続行中・ブロッキングではない)

本文:
NF-972 source map B 実装中。凍結仕様 v1 §1「can₉ = r^a s^ε の指数 tuple(a mod n・ε∈{0,1}・成分順固定)」の a,ε が単一ペアだと q₉(f)∈F₂/(K⁽⁹⁾∩F₂)=G9(位数2916)を injective に符号化できない(18通りしかない)。

調査の結果、G9=⟨x,y⟩⊂Sym(27)はMakeGn(9)の構成上、27点を9点×3ブロックに分け各ブロックの制限が必ずD₉=⟨r,s⟩(位数18)に入る(disjoint support の積で構成されているため)。よってG9はD₉×D₉×D₉(位数5832)の部分群(指数2、2916=5832/2)に埋め込め、"指数tuple"(複数形)・"成分順固定"の文言とも整合する。

**採用した設計**: can₉(g) = ブロック1,2,3それぞれの D₉ 正規形 (a_i,ε_i) の3つ組(固定順)。can₄ はブロック不要・単純に9点上の one-line image。

この読みは Sol 便100 P100-4.2 の逐語(「例えば」として提示された記述)からの合理的推論であり、凍結仕様の文言(a mod n・ε∈{0,1}・tuple)と矛盾しません。ただし司令塔/Sol の意図確認は経ていません。誤りなら差し戻しをお願いします。実装は上記解釈で進行します(可逆な設計選択なのでブロックせず続行)。

分離fixture②(片側generator swap)は「can₉内部でr,sの役割を入替」で実装します(q9側のみ・q4は不変)— 再enumeration不要で安価。こちらも解釈判断です。
