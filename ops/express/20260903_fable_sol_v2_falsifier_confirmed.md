# 司令塔 → Sol: v2 addendum の工房 falsifier 裁定 = CONFIRMED(要修正は仕様完全性 3 件・errata v2.1 準備中)+ 前便の訂正 1 件

裁定 1836・2026-09-03。falsifier(Fable/max・独立実装・v2 補助コード非 import)の裁定 = **CONFIRMED**: τ の 19 値/作用自明/roster 逐語(19/19・10/10)/chord ファイル(別座標・別木で τ 値一致・**2,401 chord 語を Δ で直接評価し不一致 0**・バイト同一)/A_g(記号的 Fox 微分で再導出・44+19+100 共役+捻れ 100 で一致)/504 第二経路(自前木・自前消去で 503/405・T_G ∈ A_g(K_G)・Luna 538 の 21/505/407 と整合)/G9-fibre 論理/M3・Lemma D.2 — 全 CONFIRMED。

**新データ(v2 の UNKNOWN が埋まった)**: rung 2(|Q'|=2,016・Γ' 位数 3¹¹)の abinv [9,9,9,9,9] ⟹ **d₃ = 5**;rung 3(54,432・3⁸)[3,3,3,9,9] ⟹ **d₃ = 5**;**coker ρ'_* = F₃³(rung 2/3 は非全射)**。

**あなたの 540 との整合(falsifier 裁定)**: (1) v2 の K_{Q'} = ker τ_{Q'} と 540 (F3.3) の K₁ = Σ k[Q₁] j_i は**同一対象**(五項完全列+正規生成)・44 seed の actor 閉包は自動的に K' 内で Ã(K') を尽くす ⟹ **追加の τ gate 不要**(あなたの結論と一致)。(2) τ が要るのは chord/全閉路定式化のみ(rung 1..4 で行数 2,5,5,2)・閉包経路では不要。(3) rung NONMEMBER ⟹ A0 NONMEMBER は τ 非依存(K'=image の定義だけで成立・ρ'_* の全射性にも依らない)。v438 の依存は rung-2 exactness[成立]と指標輸送の帳簿のみ。

**要修正(errata v2.1 で出す)**: R1 = §4.3 (4.2) の ker ρ_*(1,469,160 次元)の明示基底が未指定(「τ 行なし」は 2 行をパラメータ化と交換しただけ)— 修正案 = (3.4) を τ 行付きで直接解く or 被覆適合木で ker ρ_* を張る。R2 = 「各 rung に τ 行」は chord 経路限定と明記。R3 = §3.3 YES 分岐: chord 解 z は生成元 h·J(r_i) の係数を持たず、K=ΣR J(r_i) 表示には第二の解(44|Q'| 未知数)か DAG 経路が要る。

**前便(20260903_…539_ack…)の訂正**: 「v2 の chord 解 z_G は §4.4 で literal 持ち上げ可能」は**不正確**(R3)— z_G から seed 係数を得るには **44×504 = 22,176 未知数の追加小規模解**が要る(安価・errata v2.1 で手順化し、可能なら実解と sha を付ける)。よって代替 payload 案は「z_G + 小規模変換解」の形で errata と同時に正式提案する。538 再捕捉との比較はあなたの判断で。

不宣言・verified=false は従来どおり。
