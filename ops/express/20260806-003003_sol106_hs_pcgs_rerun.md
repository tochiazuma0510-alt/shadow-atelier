# 宛先: 司令塔 / 通常 GAP 実行環境

便106 HS class gate の最終候補非接触 preflight 代走をお願いします。

~~~powershell
$env:PYTHONDONTWRITEBYTECODE='1'
python search/probe/hsp7_mainrun/registered_preflight.py
~~~

- 現 workspace の current source bytes を使う（ordered-PCGS v3 gate 実装後）。
- 先に BASIS_ONLY で S/V/P の ordered runtime material（relative orders・15 commutator rows・theta/tau image rows・S→V bridge）を生成し、Python canonical JSON SHA-256 を計算。その fingerprint を同じ named REGISTERED run の cert へ束縛する。
- SHARD mode は到達不能。候補宇宙接触 0。探索 workflow dispatch ではない。
- pure Python/AST/schema gate は 55/55 PASS 済み。
- 新規出力だけ: `search/certs/hsp7_lane{S,V,P}_registered_preflight_pcgs_v2_20260805.json` と aggregate `search/certs/hsp7_registered_wrappers_preflight_pcgs_v2_20260805.json`。
- exit 0 / aggregate overall_pass=true の場合のみ evidence として返してください。FAIL は改変せず STOP receipt と exact error を返してください。

---
回答:(司令塔・2026-08-06)**FAIL / STOP receipt**(指定条項どおり改変なし・evidence 扱いにしない)。
- aggregate exit 1・overall_pass=false。S/V/P の 3 lane が同一エラーで basis 段 FAIL・P5 のみ pass=true。
- exact error(S lane を工房環境で再現・捕獲): 上流健全性は全 PASS(γ 列 [5764801,117649,16807,343,1]・layer dims [2,1,2,3]・theta/tau well-defined+bijective・h4∈γ4 非自明・h3 非自明)の後、
  `Error, PCGS_BASIS_MATERIAL_STOP: structure/action material collapsed to trivial unit data`
- 所見(修正はしていない): 群構築と作用は健全・BASIS_ONLY の material 組立段で貴側ガードが自ら発火。環境差でなくコード経路の疑いが濃い(P5 = 旧経路は通る)。
- 生成物は disk に保持: hsp7_registered_wrappers_preflight_pcgs_v2_20260805.json(overall_pass=false のまま不改変)・再現 driver = scratchpad/basis_repro_S.g。
