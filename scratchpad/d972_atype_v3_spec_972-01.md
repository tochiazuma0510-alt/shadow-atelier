# 972-01 §4/§10 逐語転記(A 型計器 v3 の建造仕様・2026-08-22・972 専任数学者)

(司令塔転記 — 監査係の建造用。数学者検算 7 本 `m972_check1..7.py` はセッション scratchpad
`C:\Users\81905\AppData\Local\Temp\claude\C--Users-81905-Desktop-shadow-atelier\d2b80bbe-2be7-426c-9dbe-a39ba301883a\scratchpad\` にある — 検算参照のみ可・production は独立に書くこと。)

## §4 正しい計器(972-01 逐語要約)

### 4.1 正書述語(2 系統・1170 セル一致実証済)
- **系統 A(Ad 形)**: τ²(g)τ(g)g = **c^m**(τ=Ad(δ)・δ=σ₁σ₂・δ³=c^s・s=1 実測)∧ f·θ(f)=1(θ=Ad(Δ))。g=y^m f。
- **系統 B(語水準)**: τ₀: x↦y, y↦(xy)⁻¹ を語に置換して τ₀²(g)τ₀(g)g = **1** ∧ f·θ₀(f)=1(θ₀: x↔y)。
- 両系統の判定(連言)は 1170 (語,m) セルで完全一致(差は Ad(δ) の中心因子 τ(y)=c(xy)⁻¹ — a+b≡0 のとき消える)。**RHS=c^m は Ad 形専用・語水準では RHS=1**。

### 4.2 較正(規約非依存の審判・必須)
**(3.53) 閉性**: λ=1 解の全組合成 [m₁,f₁]∘[m₂,f₂]=[2m₁m₂+m₁+m₂, f₁E_{m₁,f₁}(f₂)] が再び述語を満たすこと(素朴積では 36/36 失敗するのが正 — 素朴積との混同検出)。RHS 規約の判別子(m=0 canary はこの型の誤りに盲 — 数学者自身が実地例)。**6 つの λ 類すべてで通すこと。**

### 4.3 対象の基本量(確定済・v3 はこれを前提)
- **N_ord=18**(5 coface 全部で ord(x)=ord(y)=ord(c)=18。pc(Π₄[3])は指数 3 で原理的に 3 まで — 9=3×3 は見かけ)。
- χ_vir 像 = (ℤ/18)^× 全 6 単元(明示 witness 済: λ=1,17 は f=1・λ=7,11 は W=(xy)⁻²(yx)²=[-2,-1,-2,-1,2,1,2,1]・λ=13,5 は f″=W·E_{3,W}(W))。
- m-スペクトル現況 8/12 実現({0,3,5,6,11,12,14,17})・未確認 {2,8,9,15}(合成経路: m=9←(3,W)∘(6,f″)・m=15←(3,W)∘(12,·)・m=2←(12,·)∘(14,·)・m=8←(17,·)∘(9,·))。

## §10 建造仕様(優先度 1)

**対象系宣言 1 枚(4 項)**:
(a) 商 = 凍結 q3 の e4 = Q4×Π₄[3](e4=PB₄/N の同定は継承前提と明記)
(b) hexagon = 簡約 (3.10)+(3.11) の 2 系統併記(Ad 形 RHS=c^m/語水準 RHS=1)
(c) f-宇宙 = F₂/N_F₂(**5 coface 同時**評価)
(d) m ∈ **ℤ/18**

**必須ゲート**:
① θ ゲート(3.10)
② charming ゲート([f]=(0,0) — 非充足 f の判定は「簡約形の測定」であって GT-pair の測定ではないと cert に明記)
③ 5 coface 全部で評価(スロット 0 のみは必要条件・陽性は候補止まり)
④ 較正 = (3.53) 閉性を 6 λ 類すべてで(陰性主張時は陽性対照同梱 = INFO-1)

**禁止・廃止**: pc 商は陰性篩のみ(pc 類の任意代表語を full で評価する方式は陰性にも陽性にも健全でないため廃止)。素朴積での群性検査禁止(閉性は (3.53) で)。u から m を復元しない(m を状態として持つ)。

**発走前**: falsifier CV-9 判読(新述語 vs 正典 (3.3)(3.4) の同一性)を司令塔に要請 → 通過後に走行。

**走行後の出力**: m-スペクトル完成(未確認 4 点は合成経路で)+ **W 検証列**(① T^{F₂} 全射性(⟹ shadow か)② settled 性 ③ pentagon(972 は B₄ 系なので課せる)④ (3.53) 位数)。

**測定指示 M-1(v3 に同梱可)**: λ=1 ファイバー(m∈{0,9})を (3.53) で生成される群として BFS 列挙(正準化キー = 5 coface の e4 値 5 つ組・最短代表保持)。出力 = 位数・(3.53) 位数分布・族 18 元の指数・可換化像。
