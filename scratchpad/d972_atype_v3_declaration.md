DIR: (iv) の実掃引 = 反例側の判定計器 / FRAME: B₄-proper(pentagon 込み・972 系)

# A型計器 v3 — 対象系宣言(§BH.2 形式・972-01 §4/§10 準拠)

状態: 建造完了・**falsifier CV-9 未通過・本走行は司令塔のディスパッチ後**。以下は宣言+自己検査のみ。

## 対象系宣言(4項)

(a) **商** = 凍結 `ci/b345_157en_artifacts_32458556448/d972_b345_q3_chief_v1.json` の e4 = Q4×Π₄[3]。e4=PB₄/N の同定は継承前提(972-01 §4より — 本計器はこれを再検証しない)。

(b) **hexagon** = 簡約 (3.10)+(3.11) の2系統併記:
  - **系統A(Ad形)**: (3.10) f·θ(f)=1(θ=Ad(Δ)、Δ=σ₁σ₂σ₁=ローカル半捻り)∧ (3.11) τ²(g)τ(g)g=**c^m**(τ=Ad(δ)、δ=σ₁σ₂、g=y^m·f、c=A23·A24·A34、δ³=c^s、s=1実測)。
  - **系統B(語水準)**: τ₀: x↦y, y↦(xy)⁻¹ を自由語に直接置換し、τ₀²(g)τ₀(g)g=**1**(RHS=1)∧ f·θ₀(f)=1(θ₀: x↔y、単純互換)。
  - 両系統は(m,f)セル上で完全一致する(RHS差はAd(δ)の中心因子由来で、系統的にキャンセルする)。**RHS=c^m はAd形専用・語水準ではRHS=1** ——旧A型v1/v2はここを取り違えていた(ERRATUM-972-M-01 (a))。

(c) **f-宇宙** = F₂/N_F₂(自由群 F(x,y) の元。**5 coface 同時評価**——スロット0のみでの判定は必要条件に過ぎない)。

(d) **m ∈ ℤ/18**(N_ord=18、5 coface全部でord(x)=ord(y)=ord(c)=18。Π₄[3]は指数3のため見かけ上9だが、これは粗商の周期であり細対象e4自身のord ではない——ERRATUM-972-M-01 (b))。

## 必須ゲート(実装済み・self-test で確認)

① **θゲート(3.10)**: f·Ad(Δ)(f)=1 を先に検査。
② **charmingゲート**: [f]=(0,0)(abelianization (a,b) が (0,0) であること)。非充足fの判定は「簡約形の測定」であってGT-pairの測定ではないとcertに明記する。
③ **5 coface 全評価**: スロット0のみの陽性は候補止まり、5スロット全通過で初めてGT-pair候補。
④ **較正 = (3.53) 閉性を6λ類全てで**: m=0 canary(単一λ類のみの較正)はこの型の誤りに盲——数学者自身が実地例として指摘。6λ類全てで(3.53)閉性を確認する。

**禁止・廃止**: pc商は陰性篩専用(pc類の任意代表語をfullで評価する方式は健全性がないため廃止——旧v1のQ=⟨x,y⟩ BFS方式はこれに該当し撤回済み)。素朴積での群性検査禁止(閉性は(3.53)のみで判定)。uからmを復元しない(mは状態として保持)。

## 発走ゲート

**falsifier CV-9(新述語 vs 正典(3.3)(3.4)の同一性判読)は未実施。** 司令塔にディスパッチを要請する。通過後に本走行(m-スペクトル完成+W検証列)を行う。

## 実装ファイル

- `scratchpad/d972_atype_v3_production.py`(独立記述・エンジンとして `audit_P0_naive_judge_v4.py` の validated 済み部品(Ad(σ_k)テーブル・self_tests)を import して利用。参照専用の `m972_check1..7.py` は読解のみ・転写せず独自にロジックを書いた)。

## 単位規約(INC-13 教訓・PIN-AB-1型 assert)

本計器は可換化(abelianization)座標(a,b)を charming ゲートで使用する。実行冒頭で以下を assert する:
- 生成元 assert: x=push([1])=coface_slot[0]、y=push([2])=coface_slot[2] が e4 の実際の生成元画像と一致すること。
- 語順規約 W-1: 全て paper 積(左から右へ読む、`AB`はAを先に適用してからBを適用)。GAP側の `B*A` 規約とは混同しない——本計器はGAPを呼ばないため、この混同は構造的に発生しないが、念のためself-test内で明記する。
