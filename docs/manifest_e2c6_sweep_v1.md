# 事前登録 manifest — E2 class-6 二方向掃引 v1(2026-07-26・司令塔)

前提: class-6 作用表の二独立導出一致(1147 検査 0 FAIL・crosscheck/agree6_*.json = 機械可読の確定表)。裁定 17(転進)・Sol 便 19 F10(標的縮約)・裁定 19(v3)。

## 宇宙(事前登録・変更禁止)

- 対象: A_j⁽⁶⁾ = A⁽⁶⁾/𝔘_j(A⁽⁶⁾)、A⁽⁶⁾ = γ₂(F₂)/γ₇(F₂)。Ā_j = (ℤ/2^j)^15・C_j = (ℤ/2^{j−1})^6(W137 の添字規約)。
- **最小ゲート: j = 2 のみ**(C₂ = (ℤ/2)^6)。m ∈ {0, …, 63}(64 系)。j ≥ 3 は本ゲート全零のときのみ・別 manifest で。
- 標的: (C^σ)^θ の 2 方向 — **a = u₄ = [p,q](θ = −1)・b = u₁+u₂+u₃(θ = +1)**。j = 2 では各 1 ビット・計 2 ビット/系。
- 判定量: 線型段(𝒩̄f̄ = −Ē_m over Ā_j・rank 15)可解性 → 可解なら **ob = (q_θ)₊ − 3⁻¹(1+θ)q_N の (a,b) 成分**(便 19 F10 (10.2)・3⁻¹ は 2 冪上で存在)。
- **期待値: UNKNOWN**(E23 の消滅機構は class 6 で構造的に破れている — 𝒩_C ≠ 0・σ|_C 非 2×2 は Claude 側導出の実測。予測は登録しない — 観測先行)。
- 実現ギャップ維持: ob ≠ 0 が出ても有限許容対象への実現+charming なしに E15 反例ではない(裁定 17)。

## 実装要件

1. 入力は **crosscheck/agree6_claude.json(= 確定表)のみ**(導出文書・hall6.mjs は読まない — 系統分離)。GAP 側 route も同 JSON から独立に組む(二系統)。
2. 証明書: 系ごとに {m, linear_solvable, witness or unsolvability_certificate, ob_a, ob_b} を certificates/e2c6/ へ。node 独立検算器(agree6_sol2.json を入力に選ぶ — 入力段から系統分離)。
3. **反証 fixture(発射前必須)**: (i) 合成非零 — b ベクトルを摂動した人工系で ob ≠ 0 経路が実際に発火すること (ii) class-5 統制 — 射影で class-5 部分掃引を再現し全 POSITIVE(既知)と一致すること (iii) mass check。
4. 走行 cap: GAP 600 秒(推定は秒オーダー)。

## 発射条件

①実装+fixture 3 種 PASS ②falsifier の計画監査 PASS ③司令塔の発射合図。それまで 64 系本走査は禁止。

## 汚染申告(2026-07-26・事前登録衛生)

fixture (iii) 構築中に implementer が class-6 実データ(j = 2・m ∈ {0,1,2,3,5,7})を誤って走行させ、**「m = 2 で線型段不可解らしい」という事故的観測**が発生(即破棄・証明書なし・設計変更なし・本報告のみ)。本走査の当該セルの結果は本注記と独立に評価すること。発見主張にこの観測を使うことは禁止(正規の走行+二系統検算のみが根拠となる)。
