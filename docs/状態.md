# 状態.md — 工程スナップショット(正本・便サイクルごと全体書き換え)

**規律**: compaction 後は **本ファイル → docs/notes/session_20260812_campaign_index.md(本日戦役索引)→ 地図.md 冒頭 → 直近便カプセル**の順。**⓪ レーン復元の一次参照 = そのレーンの直近 2〜3 便+返書**(裁定 914)。**相 2 主張の正本 = docs/見立て_相2_v1_4_8.md**(d7ef14d・台帳 68)。P1 に触れる前に **p1_corpus_index_v1.md** 必読。

**最終更新**: 2026-08-12(裁定 969 まで・便 120 = v2 凍結・d_{S4} 測定発火時点の全体書き換え)

## 0. 北極星と教義

北極星 = 井原予想単独(G_ℚ ≅ ĜT)。相 2 = 反証全力。教義 = 確信は反証の全力でしか買えない・進捗 = 扉リストの縮小・**掃討教義(955): 候補地に確率順位を付けず開扉は全並行で潰す**・数値確信度は撤回済み(序数評価のみ)。

## 1. 反例戦線の現況(裁定 959/965/969)

- **① 972 屋根(合成もつれ)— ★r レジームへ収斂(970)**: TRIAD-972 = |X∖A| = 972 − 12·d₉·d_{S4}/r。d₉ = 9(candidate・T63 鎖)・d_{S4} = 9(条件付き receipt・970)⟹ **発火 ⟺ r≥3 — r が反例探索の主標的**。r 測定設計 = 便 120 P3 認可待ち(a 側 = P-K9U-1 条件付き・値の先行計算は prereg 禁止)。残 = R3-GAP-4/5。
- **② S4-ORDER — ★receipt 着弾(裁定 970)**: **ord([u₀⁻¹]₉) = 9・QUAR 不発火**(ds4_receipt_v1・per-prime 寄与 9/3/1・陽性対照 PASS)。条件付き読み = **前件 P1–P5(特に【S4R-GAP-1】u₀=u_{S4})の下で d_{S4}=9 = S4 窓算術飽和・直接反例扉は条件付きで閉**。残 GAP = S4R-GAP-1(C1′(S4))・GAP-2(札 2 算術論証)。
- **③ (F) = SL^±(2,691)×_{C₂}S₃**: 検分 GO・C2 前哨 PASS・次 = 段 1(H²)spec(順延中)。⚠留保 R-1: 「③が①へ非円分供給」は未証明。
- **④ χ 扉/c∉N 集落**: 三分 A15/B66/C2(較正済)・**δ>1 の深窓 15 に局在**・ISO-CENSUS-83 は札検分待ち・最安 witness = [1728,31095]。
- **K9(d₉)**: 条件付き封鎖 — receipt(P8-value)は prereg v2 準備完了・**発火 = Sol P2 PASS 後**。

## 2. K9 修理の現況(便 120 の眼目)

- **K9-COMPOSE = 修理完了候補**(Sol 検収待ち): T63-P1^fix+COMPOSE-GAP-1 全閉((d1) 内包・uniform (5′^b)・調達ゼロ)+T63-CONNECT(framework-conditional・M119-5 維持)。
- **K9-UNRAM = OPEN(分割設計済)**: U-odd(p≥5 = p∤2916 で素朴 specialization)+U-2(C₂/translation 分離)+GAP-3(U-odd 具体形 = 数学者宿題)。pro-3 橋は W-47 禁止。
- **P-K9U-1** = 条件付き凍結維持(前件 = 二系統)。

## 3. 便の状態

- **便 120 = 発送済(裁定 971)**: digest 17/17・起床実確認(+59KB/60s)・返信 watcher 武装済。返信 → 検収 → v1.4.9+認可測定の即発火。旧記載:(966/968・前哨 PASS): P2(K9-COMPOSE 検収 → PASS で d₉=9 HOLD 解除+receipt 発火認可)・P3(TRIAD-972 → r 測定設計認可)・P4(fullsweep 格上げ)・P5(UNRAM 分割)・P6(S4 非対称+QUAR 要件)。積荷 17 本・k12 追送方式。**発送 = 研究者の合図のみ**(直前 digest 再計算 → deliver_task.ps1+ReplyFile watcher)。
- 便 119 = 返書検収済(944): ISO-S4 PASS・R1 二橋 FAIL(→修理済・120 で再請求)・台帳 68。

## 4. 係と走行

1. **数学者 = a9b8c3225698d80ca**: queue = ①S4-RECON カード**済**(11fda0d)②**枝 P 実装指示書(現在)**③83 窓札検分 ④UNRAM-GAP-3 ⑤(F) 段 1 spec。v1.4.9 = Sol 返信後(台帳案 B)。
2. **実装係 = afef05585764e2a7e**: d_{S4} receipt **完了**(70de6e5・ord=9・QUAR 不発火)→ 待機。次候補 = 枝 P R-1/k12 完走処理/r 測定(Sol 認可後)。
3. **falsifier = aef33930088ebc070**: 待機(常設 = 前哨・POST-EDIT-CHK・CV-9)。
4. **GHA**: k12 T4/T5 = run 31565199573 走行中(完走 → cert 追送 → SNF-MOD-13)。
5. 監視 = 速達箱 monitor(b242rrlyh・常設)。

## 5. 規約(本日追加の正本 = 戦役索引 §4)

三分格付け(verified は Lean 予約)/IF-FIRST/DOMAIN-PIN/EXHAUST/CHI-CARRY/NAME-COLLIDE/TYPE-IMAGE^ρ/LOCAL-PIN・MARKING-COMPAT/三致(reopen+絶対パス)/凍結表 grep/POST-EDIT-CHK/機械配管・依存洗い出し/台帳 = 流通主張数/**陽性対照必須(961)**/**積荷同期(921)**/**完結儀式(913)**/**カプセル復元(914)**/**在庫 4 点検査(911)**/**W-43 細則(964)**/**記帳と送信の分離禁止(967)**。台帳 = 68(v1.4.8)・本日の自己捕獲群は案 B で Sol 採番待ち。

## 5.5 完結資産(主題別導線)

- **G1** = docs/宣言_G1.md。**P1** = 全文完結・Sol 監査済 — **p1_corpus_index_v1.md 必読**。**本日戦役(裁定 894〜969)** = **session_20260812_campaign_index.md**(閉じたもの/開いたもの/凍結中/新規約/型境界 4 事例)。
- 機械資産: k13 T2/T3 完(凍結 735⑥ 一致)・JAC-CHK-2 完・S4-SETTLED-54 完+fullsweep・census v2・z census — 詳細は各 cert。
- 他キャンペーン(壁・EP・B₄)の索引化 = 未着手(一括バースト待ち)。

## 6. 再開手順(compaction 後)

① 本ファイル → session_20260812_campaign_index.md → 見立て v1.4.8 → sol_reply_119+便 120 本文。② 走行係の実在確認(git log・gh run list — 通知不信)。③ **d_{S4} 測定結果の回収**(quar_triggered に注意 — true なら QUAR (Q1)-(Q5) 手順)。④ 便 120 = 合図待ち不変か確認。⑤ k12 完走なら cert 検収 → 追送。⑥ **P1・本日戦役の事項は各索引を先に読む**(同日 4 回手戻りの教訓)。
