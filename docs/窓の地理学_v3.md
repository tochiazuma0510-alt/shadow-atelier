# 窓の地理学 v3 — Sol 便 21 の差戻し反映版(P189–P195・F14(8)(9))

2026-07-26・司令塔起草。v2 は保存(監査固定 SHA `E28B8D79…22AB7`)。変更点はすべて sol/sol_reply_21_audit.md の指定に従う。**状態: Sol 差戻し条件の反映済み・Opus 確認待ち → 両者承認で正本化。**

## 1. 基本対象(v2 §1 のまま・承認済み)

頂点ラベルと辺ラベルをもつ有限部分 poset。頂点欄: (Q; x̄, ȳ, c̄)・e_c・P・passport・ℓ・H^cong・d_cong・Aut・𝒪_arith(G_ℚ-安定頂点でのみ im(Ih))。辺欄: 商写像・reduction 像・繊維・fiber-product 像・gluing index。genuine/fake は辺に載る。

## 2. 座標系(v2 §2 のまま・承認済み)

(A0) 中心拡大 /(A1) 完全 marked quotient /(B′) modular geography /(C′) deformation /(E) poset incidence /(F) arithmetic descent([b] ∈ H¹ が基本量・u は一次元セルの特例)/(D′) GTSh-群oid関手性。

## 3. 圧縮法則(改訂)

- **G0・G2′・G3′・G5**: v2 のまま(承認済み)。
- **G1′(用語修正)**: 核と像の defect を**別登録**。「coker」は使わない — 非可換群で coker = 1 は全射の同義語ではない(W152)。座標は ker Φ_N と像の指数 [N_Aut(⟨w⟩) : Im Φ_N]。
- **G4′(B1 分割・P193)**: 基本量は [b] ∈ H¹_ét(𝓗, 𝓜)。**global unit u 表示の前件 = line bundle の trivialization + Pic(𝓗)[M] の消滅**(Kummer 完全列)。B1 は二分: (i) 現行三点 atlas では「当該 Nielsen/Aut 軌道の一意性」(三点 Hurwitz 空間は 0 次元 — 次元剛性と混同しない・W153)(ii) 将来の r > 3 族では「底が正次元なら類は section/関数として扱う」。
- **G6a(局所解析補題・paper audit PASS)**: 選んだ compatible cusp parameter に関し、形式再径数化の後 **λ = 16 t^M**。複素局所命題であり、ℚ-有理性を含まない。
- **G6b(算術降下問題・一般則ではない)**: [b] を定義できるのは (i) arithmetic model (ii) Galois-安定な cusp または共役 cusp 全体 (iii) tangent rigidification (iv) その stabilizer (v) 局所系から GT rigidification 加群への比較写像、の**すべてを与えたときのみ**。数値予測には使わない。W149: u ∈ ℚ^×/(ℚ^×)^M と書く前に cusp と接径数の ℚ-有理性を証明せよ。W150: 類の位数・幾何点数・Galois 軌道・円分核 translation 像・|𝔉₀| は**別不変量**。W151: Galois 閉包は曖昧さを消さず descent 問題へ移すだけ。
- **初等算術補題(paper audit PASS)**: ord_{ℚ^×/(ℚ^×)^M}[16] = M/gcd(M,4)。[16] = 1 ⟺ M | 4(委嘱 15 §2 の向きは訂正)。
- **棄却継続**: 「合同 ⟺ 飽和」。

## 4. 確定命題(改訂)

- **T-6/命題 T6(paper audit PASS)**: N_A = π⁻¹Γ̄(10)(群論)・X(10)_ℂ・W_ℂ = X(10)_ℂ/A₄(複素)・ℚ-model は descent datum 別立て(W145/W146)。
- **系 T6.1(限定付き PASS)**: 飽和 = deck 作用の定義体が「**marked GT/normalizer 制約が許す最大** |F₂₀| = 20」に達すること(Aut(A₅) = 120 に対する絶対最大ではない)。一般窓への移送には Φ_N faithful と actual deck 作用の比較を毎回確認。
- **命題 K-cong(paper cross-audit PASS・裁定 18・変更なし)**: K⁽ⁿ⁾ 合同 ⟺ n = 4・K̄⁽⁴⁾ = Γ̄(8)。**384/384 は「G₄ marked-kernel uniqueness lemma」**(第二証明ではない — marked congruence bridge が presentation 経路由来のため・P191)。
- **命題 U′(P192 の形)**: **U′-1** G_ℚ-安定(特に isolated)な N と、定義を明記した K_N(= ker(Ih_N) の固定体、または Φ_N∘Ih_N の切る体 — 後者は Φ_N 単射を前件)について飽和 ⟺ [K_N:ℚ] = |GT(N)|。**U′-2a** ker Φ_N = 1。**U′-2b** Im Φ_N = N_{Aut(Q_N)}(⟨w_N⟩)。三つの defect は別欄。w_N の正本 = week3 補題 2。

## 5. 第一撃と panel(P190 反映)

1. アンカー再走: 𝒞₈ = K⁽⁴⁾・𝒞₁₀ = N_A(ともに主合同窓)。
2. **第一標的: レベル 16 双子セル 𝒞₁₆ vs K⁽⁸⁾** — ただし**封印は「予測」でなく独立な UNKNOWN 札**: normalizer・settled 率・算術軌道・𝔉₀ を互いに独立の UNKNOWN として登録。局所 [16] は**比較対象でなく diagnostic candidate**。𝒞₁₆ の GT 全列挙・isolated 性・算術像 = UNKNOWN(観測が法則より先)。
3. panel 10 窓: v2 のまま。
4. **強い較正の掟(P195)**: 較正は「値が既知」だけでは足りない — **同一の比較射を両側で構成できる窓**を選ぶ(A₅ は満たす・M = 4 の 1 = 1 は弱い sanity check と明記)。W155: gcd 公式を書き込んだ script の PASS を降下や比較の独立照合と数えない。

## 6. 教材(便 21 より採録)

- **主係数は基礎体を忘れると予言に化ける**: λ = 16t^M も ℚ 上の位数公式も正しいが、marked cusp の定義体 ℚ(ζ₂M) へ移すと類は変わり得る(M = 8 で √2 ∈ ℚ(ζ₁₆) — 根が円分体の中へ落ちた)。local coefficient → descended torsor → cyclotomic-kernel image の各矢印を一本ずつ証明する。A₅(M = 5)が立ったのは次数 5 と φ(10) = 4 の互いに素性が根を円分体の外に保ったから。
- **一意な終域表示は候補の終域同定をしない**: order・passport・level は名前でなく粗不変量(W148 系)。

## 7. 残存 UNKNOWN(一級)

𝒞₁₆ の全データ(F14(8) 反映: K-cong は §4 の cross-audit 表記に統一)。X(10) 標準算術モデルとの descent 照合。G6b の比較射は窓ごとに未構成。
