# 【S4-RECON 装置カード】$d_{S4}$ receipt — 橋・blind card・計算指示

**日付**: 2026-08-12 / **起草**: 数学者(Opus 5・後任)/ **委嘱**: 裁定 967 指示①(最優先)
**格**: candidate(紙・単系統・**Sol 未監査**)。⚠ **$\operatorname{ord}$ は計算していない**(カード凍結が先)。
**入力**: R3 札 3 / `surj_s4_v2.md` §4 / cert `u_meas_uloc_v2_20260731`・`u_meas_caseb_a5_20260731` / P8-CORR

> ## ★★ 判定(先に 3 行)
> 1. **橋は書ける**。⚠ ただし **framework-conditional** で、前件に **C1′(S4)**(開)と **$(Z_{18}$-link$)$**(S4 未登録)が入る。
> 2. ★★ **循環は避けられる** — $u_{S4}$ は**窓側(幾何)**、$d_{S4}$ は**算術像**で、橋は既存の比較鎖(`surj_s4_v2` §4)。**$u_9=3$ の失敗型(型 → 算術の密輸)とは経路が違う**(§1.3)。
> 3. ⚠★ **最大の未確認 = $u_0$(wac_v1 の測定値)と $u_{S4}$(比較鎖の入力)の同一性**。⟹ **カードの前件に置き、測定は止めない**(掃討教義)。

---

## §1 (a) $u_0$ の位数 ↔ $d_{S4}$ の橋

### 1.1 両端の型(**先に固定** — P8-CORR の表を S4 内で精密化)

| 対象 | 型 | 法 | 体 | 出所 |
|---|---|---|---|---|
| **$u_0$**(wac_v1) | ★ **幾何測定値**: 命題 U-LOC により $u_0=-c^{-1}$($C$ 上の局所展開の主係数)。窓 = **$N_{S4}$**(monodromy $PSL(2,8)$=9T27・cover degree 9/genus 2・分岐 $(3^3,3^3,9)$) | **9** | $\mathbf Q$ | `u_meas_uloc_v2` / litgate 覚書 §0 |
| **$u_{S4}$**(`surj_s4_v2` §4) | ★ **比較鎖の入力**(窓の局所 Kummer データ) | **9** | $\mathbf Q$ | `surj_s4_v2` §4 表 |
| **$d_{S4}$** | ★ **算術像**: $\lvert A_{S4}\cap(\text{translation }\mathbf Z/9)\rvert$ | — | — | ENT-GAP-8 / census |

★ **$u_0$ と $u_{S4}$ は法・体・窓が一致**(3 点)。⚠ **同一性そのものは未確認**(§1.4)。

### 1.2 比較鎖(**既存資産**・`surj_s4_v2` §4)

> $[L:K]=\operatorname{ord}\bigl([u_{S4}^{-1}]_9\bigr)\in\{1,3,9\}$、像位数 $\in\{6,18,54\}$(**framework-conditional**)

これに **census**(裁定 958/961)と **札 2 の構造**を重ねる:
$$\lvert A_{S4}\rvert=6\,d_{S4},\qquad d_{S4}\in\{1,3,9\}\quad(\text{census で三枝確定})$$
$$\boxed{\ \textbf{橋}:\quad d_{S4}\ =\ \operatorname{ord}\bigl([u_{S4}^{-1}]_9\bigr)\qquad(\textbf{framework-conditional})\ }$$

**前件**(R3 札 3 が挙げるもの + 私の追加):

| # | 前件 | 状態 |
|---|---|---|
| 1 | TB1–TB4 | 枠組み(継承) |
| 2 | **$(Z_{18}$-link$)$** | ⚠ **S4 は inventory 未登録**(`not_assessed`) |
| 3 | **C1′(S4)** | ⚠ **開**(dessin 6 個の同着問題・`c1prime_s4_design_v1.md`) |
| ★ 4 | **札 2**($A_{S4}$ が完全引き戻し)| ⚠ ★ **算術論証必須**(census では代替不可・裁定 961 確定)⟹ **複素共役 $(t_c,-1)$ の fiber 論法**が要る |
| ★ 5 | **$u_0=u_{S4}$** | ⚠ **未確認**(§1.4) |

### 1.3 ★★ 循環の検問($u_9=3$ の失敗型を避ける)

| | ✘ **$u_9=3$(撤回済・B116-1)** | ★ **本橋** |
|---|---|---|
| 出発点 | **標的群型** $\mathrm{Aff}(\mathbf Z/9)$ ⟹「この型の体は Kummer-円分型」 | ★ **幾何測定値 $u_0=-c^{-1}$**(曲線 $C$ の局所展開・**算術/幾何の実データ**) |
| 渡り方 | 型 → 実算術 Galois 群の**全射性**(密輸) | ★ **既存の比較鎖**(`surj_s4_v2` §4・前件明示) |
| 判定 | ✘ **循環** | ★ **循環しない**(型を経由しない) |

$$\boxed{\ \textbf{⟹ 型 → 算術の密輸はない。渡るのは「幾何測定 → 比較鎖 → 算術像」}\ }$$
⚠ **ただし前件 4(札 2)が型と算術を繋ぐ箇所**であり、そこは**算術論証(複素共役)**でしか埋まらない ⟹ **census を代用にしない**(裁定 961)。

### 1.4 ⚠★ 最大の未確認 — $u_0=u_{S4}$ か

| 一致する点 | ⚠ 未確認の点 |
|---|---|
| 窓($N_{S4}$・$PSL(2,8)$)/ 法(9)/ 体($\mathbf Q$) | ① **窓内の $H^{\rm fun}$ の選択が同じか**(= **C1′(S4)** そのもの — dessin 6 個の同着)② **逆元の位置**($u_0$ vs $u_{S4}^{-1}$)③ **正規化**(uniformizer/cusp の選択) |

$$\boxed{\ \Longrightarrow\ \textbf{同一性は }\textbf{カードの前件}\ \textbf{に置く。⚠ 測定は止めない(掃討教義)}\ }$$
★ **honest な言い方**: 「**$u_0=u_{S4}$ ならば** $d_{S4}=\operatorname{ord}([u_0^{-1}]_9)$」。⟹ 測定は **conditional receipt** として意味を持つ。

---

## §2 (b) blind receipt カード(**prereg 様式 v2 準拠**)

```
=== PREREG CARD: d_S4 RECEIPT (S4-RECON) ===
card_id     : prereg-dS4-receipt/v1
frozen_at   : <commit hash>        # ord 計算より前に commit
authorisation : 司令塔裁定 967

[0] 前件(prerequisites)★ 全て条件として明示・成立を主張しない
  P1 : TB1-TB4(枠組み・継承)
  P2 : (Z_18-link) — ⚠ S4 は inventory 未登録(not_assessed)
  P3 : C1'(S4) — ⚠ 開(dessin 6 個の同着問題)
  P4 : 札 2(A_S4 が完全引き戻し)— ⚠ 算術論証必須(census 代用不可・裁定 961)
  P5 : ★ u_0 = u_S4 の同一性 — ⚠ 未確認(§1.4)
  ⟹ 本 receipt は「P1-P5 の下での conditional receipt」である

[1] 測定対象(型を先に固定)
  window   : N_S4  (PB_3/N_S4 = PSL(2,8), |GT(N_S4)| = 54)
  object   : ord([u_0^{-1}]_9)  in  Q^x/(Q^x)^9
  法        : 9      体 : Q
  入力値    : u_0^{-1} = -3^6 * 5^9 / 2^8   (cert u_meas_uloc_v2_20260731・既開示)
  ⚠ NOT    : a_9 = [u_9^{-1}]_18(K^(9) 窓・法 18・体 Q(zeta_36))— 別対象(P8-CORR)
  ⚠ NOT    : E1 の R^cyc_formal 規約(S4 は dihedral 族外ゆえ適用外)

[2] 判定基準(★ ord 計算前に凍結)
  出力は ord ∈ {1,3,9} のみ(blind — u_0 の値の解釈はしない)
  d_S4 = 9  : ρ_S4 全射。972 は単独窓事由では発火しない
              (TRIAD-972 の (d_9,9,r) 行へ)
  d_S4 = 3  : ★★ QUAR 発動(下記 [4])
  d_S4 = 1  : ★★ QUAR 発動(下記 [4])
  ord ∉ {1,3,9} : ★ UNKNOWN(MISS でない)
              ⟹ census の三枝(共役不変量)と矛盾 ⟹ P5(同一性)か
                 比較鎖の破れ ⟹ 座標系の再検査へ
  ★ UNKNOWN は MISS に優先(prereg v2 の N-1 を継承)

[3] 同時に判定される命題(★ 事前に全列挙)
  R3 札 3 の橋      : d_S4 = ord([u_S4^{-1}]_9)
  札 2(FULLPRE-S4) : |A_S4| = 6 d_S4
  TRIAD-972         : |X\A| = 972 - 12 d_9 d_S4 / r の第二引数が決まる
  ⚠ d_9 と r は依然 UNKNOWN ⟹ 972 の値は決まらない
  ★ P8-CORR の別対象判定 : ord が 18 を法とする値と整合したら判定が誤り

[4] ★★ QUAR 条項(d_S4 < 9 のとき — 井原予想の直接反例候補)
  ⚠ N_S4 は isolated(便 119 P2)ゆえ GT(N_S4) は有限群で Ih_S4 は群準同型
  ⟹ d_S4 < 9 ⟺ GT(N_S4) \ im(Ih_S4) ≠ 空 = ★ 井原予想の反例そのもの
  (私の R3 検分 §2.2 — 札より強い形)
  発動時の手順(★ 測定前に凍結):
   (Q1) ★ 即時隔離: 値を便・地図・見立て・状態に載せない。流通禁止。
   (Q2) 前件 P1-P5 を 1 件ずつ再検査(特に ★ P5 同一性と P4 算術論証)
   (Q3) falsifier へ独立判読(仕様同一性・cert の値の抜き取り)
   (Q4) Sol 便 121 で監査請求(反例主張ではなく「前件つき測定値」として)
   (Q5) 研究者へ報告(北極星への直撃ゆえ判断は研究者)
   ⚠ (Q1)-(Q5) を通るまで「反例を得た」と書かない・言わない
  ★ 対称性の注意: d_S4 < 9 は Conj 5.1(dihedral 予想)には抵触しない
    (N_S4 は Dih の外)。抵触するのは井原予想(北極星)の方である。

[5] 禁止事項
  - カード凍結前に ord を計算しない
  - 測定後に判定基準・QUAR 手順を書き換えない
  - 前件 P1-P5 のいずれかが未成立のまま「d_S4 が決まった」と書かない
  - u_0 の値を K^(9) 側(a_9)の判定に流用しない(P8-CORR)
  - 格は cross-checked 止まり。verified は Lean に予約

[6] 出力(cert に載せる)
  ord_value            : 1 / 3 / 9 / other
  input_u0_inverse     : -3^6*5^9/2^8(入力の再掲・改変検知用)
  computation_method   : Q^x/(Q^x)^9 での位数(素因子指数の mod 9)
  prerequisites_status : P1-P5 の各状態(成立を主張しない・状態の転記のみ)
  quar_triggered       : true/false
  d_no_interpretation  : "machine value only; verdict は司令塔"
=== END ===
```

---

## §3 (c) 実装係への計算指示

```
=== 作業指示: d_S4 receipt の ord 計算 ===
根拠: 司令塔裁定 967 / カード = docs/notes/s4_recon_device_v1.md §2
★ 前件: 上記カードが commit されていること(prereg 順序)。
★ 停止線: ord を出したら止める。d_S4 の値としての解釈・流通はしない。

[D1] 入力の読み出し(改変検知)
  cert search/certs/u_meas_uloc_v2_20260731.json の measurement.u_0_inverse
  期待値 -1423828125/256 と一致するか(= -3^6*5^9/2^8)を assert

[D2] ord の計算(★ 秒級)
  Q^x/(Q^x)^9 における [u_0^{-1}] の位数
  = lcm over primes p of ( 9 / gcd(9, v_p(u_0^{-1})) )
  ※ -1 = (-1)^9 ゆえ符号は 9 乗類として自明(寄与しない)
  ⚠ この式が正しいことの確認も cert に含める(u_0^{-1} = ± prod p^{e_p} の形)

[D3] 出力
  cert (schema ds4-receipt/v1)。カードの [6] の全欄。
  ⚠ quar_triggered = (ord != 9) を必ず立てる。true なら司令塔へ即時報告し、
     値を他のどの文書にも書かない。
=== END ===
```

⚠★ **私は [D2] を実行していない**(カード凍結が先)。

---

## §4 【GAP】・申告

| # | 内容 | 重さ |
|---|---|---|
| ★★ **【S4R-GAP-1】** | **$u_0=u_{S4}$ の同一性**(= C1′(S4) の窓選択と直結)⟹ **receipt が conditional である理由** | ★★ 大 |
| **【S4R-GAP-2】** | 札 2($A_{S4}$ 完全引き戻し)の**算術論証**(複素共役 $(t_c,-1)$ の fiber 論法・census 代用不可) | ★ 中 |
| **【S4R-GAP-3】** | $(Z_{18}$-link$)$ の S4 inventory 登録 | 小(事務) |

★ **司令塔への回答**(裁定 967 の「橋が書けない/循環が避けられないなら honest に」): **橋は書けた・循環は避けられた**。⚠ ただし **conditional**(前件 5 件)であり、**$u_0=u_{S4}$ の同一性が最大の未確認**。⟹ **測定は走らせてよいが、出た値は「前件つき」以上には読めない**。

**帰属**: 札 3 = 発案係。$u_0$ の測定 = 実装係(wac_v1・裁定 268–272)。比較鎖 = `surj_s4_v2` §4(既存)。委嘱 = 司令塔(裁定 967)。
**本カードの新規部分**: ① **$u_0$/$u_{S4}$/$d_{S4}$ の型表と 3 点一致の確認** ② **循環検問**($u_9=3$ との経路対比)③ ★ **同一性を前件に置く conditional receipt の設計** ④ ★★ **QUAR 条項 (Q1)–(Q5)** と ★ **「$d_{S4}<9$ が抵触するのは Conj 5.1 ではなく井原予想」という対称性の注意** ⑤ 実装指示 [D1]–[D3]。
**申告**: ⚠ **$\operatorname{ord}$ 未計算**・走行ゼロ・**Sol 未監査**・**verified ではない**。
