# falsifier CV-9 判読(disc R₁ 不一致・裁定 1018→1020)— 収蔵

**判定: 別対象**(Sol = 設計に忠実/工房 = 設計逸脱)。乖離は二層: (iv) 真のバグ+(ii) 再パラメータ化の取り違え。

## 乖離 1【工房の −1 欠落(真のバグ)】
設計: F(1,w)=w¹⁸+P(w)+1=Q ⟹ P=Q−w¹⁸−1 ⟹ c₀=[w⁰]Q−1。工房 v2(r13_p1_tier2_gha.py@959a1ac9 :242-243、crosscheck 器 :72-73 に同文再入力):
```python
P_on_curve = sum(c[j]*w**j for j in range(0,9)) + 2*w**9
R1 = sp.expand(P_on_curve - 2*w**9)
```
B t²=1 由来の −1 が入っていない ⟹ 工房の R₁ = 設計の R₁+1。**無料の見張り**: cert の discR1_homogeneous_in_ab=true は設計 R₁(18 次と 0 次の混合定数項)では原理的に不可能 — この 1 欄だけで逸脱確定可だった。なお +2w⁹−2w⁹ は恒等相殺で「repair2 の c₉=2 事前代入」は R₁ に効いていない(γ は後段 resultant で課す設計だった)。

## 乖離 2【比較プロトコル】
工房 crosscheck 器は消去前の二変数式の a=1 切片を、Sol の消去後一変数式と比較(a=1 は一般に Γ 上にない)。Sol の意味論: b=ra・w=az・a⁹H(r)=2 ⟹ R₁/a¹⁸ = Q₀−z¹⁸−Hz⁹−H²/4 = **設計 R₁ の Γ 上制限 ÷a¹⁸ と完全一致**(constant_term_correction の宣言どおり)。

## 決定的機械検算(scratchpad/cv9_fast.py・厳密有理数)
| 組んだ R₁ の定数項 | Sol branch_I との比 |
|---|---|
| [z⁰]Q₀(工房・−1 欠落) | 非定数(cert の 4 値を再現 = 工房対象) |
| [z⁰]Q₀−H²/4(設計を Γ 上へ) | **定数**(分子 = 3¹⁴ = Sol の捨てた content)⟹ **Sol=設計** |
| [z⁰]Q₀−1(−1 復元・a=1 切片) | 非定数(消去してから比べる必要) |

## 訂正 2 件(裁定 1018 へ)
- 「独立再実装」は事実誤認: crosscheck 器は同数式の再入力(冒頭に自己申告あり)= 同バグ継承・独立性なし。露見は Sol と食い違ったため。
- 196/197 一致は証拠力ゼロ: disc は係数の次数 2n−2・重み n(n−1) の等重多項式 ⟹ 対象が違っても 196 に一致・197 = dense の言い換え。

## disc_t 疑惑 = 不発(工房は明確に disc_w)

## Sol 分類の有効性 = 有効・設計の答えと恒等
S₁(z)=a⁻¹⁸R₁(az) は重複度構造不変 ⟹ (o₁,o₂) は設計の R₁,R₂ のもの。μ₉ 勘定(ordered=9×deg)正・開条件対応(c0_nonzero=(e4)・H_nonzero=a≠0)・既約因子ごと分類で取りこぼしなし。

## 【重大→自力閉鎖】Sol 較正の空虚穴
人工可解対照(5 層 profile)が本番経路(NumberFieldOps+手書き gcd)でなく sympy sqf_list を通過・全整数根で数体演算不起動 ⟹ 陰性主張「4 層空」が答えを知る対照を本番経路で一度も通っていなかった。⟹ **第三算術(mod-p のみ・scratchpad/cv9_modp_control.py)で独立追試**: 4 因子 × 2 素数 × 2 根 = 全一致((6,9)(8,7)(6,9)(8,7))+branch 帰属の裏取り。mod p は根を併合しかしないので退化見落としがあれば差が出るはず — 出ず。o₁+o₂=15(17 次 Δ に二重根 1 個)は「1 次元 Γ 上で余次元 4 要求」の勘定と整合 = 空は期待どおり。

## Sol への割込 = 不要(無効なのは工房側)
工房の二変数 resultant 路の候補列挙 = 全部無効・破棄。修理対象 = 工房 v2 の −1・crosscheck 器のプロトコル。

## 上申 3 点
1. crosscheck 器の独立性の制度問題(再入力 ≠ 独立)— 1018 の記述訂正(1020 で執行済)。
2. 作業ツリーの r13_p1_tier2_gha.py が HEAD と別物(Sol 版未コミット)— 出所ハザード(1020 で HEAD 復元済・内容は sol 枝に保全)。
3. 潜在 fail-open 2 件(記帳のみ・今回実害なし): (a) degree>1 を a≠b の代理にしており r=1 以外の有理点因子が来ると誤って落ちる (b) S1_degree_8 偽(c₈=0)の因子は非 admissible 集計外だが設計は deg≤8 しか言っていない ⟹ 層が隠れうる。

**申告**: u/c 非接触・prereg 非計算・浮動小数点判定なし・GAP 走行ゼロ・2 検算は診断用(cross-checked 材料にしない)。
