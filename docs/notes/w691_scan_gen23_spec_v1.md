# W691-SCAN 仕様補完 — $(2,3)$-生成判定の方法指定(裁定 827)

**日付**: 2026-08-12 / **起草**: 数学者(Opus 5) / **状態**: candidate(Sol 未監査・判定語の発効は司令塔専権)
**入力**: 裁定 827(実装係 STOP・GL(2,7) カナリア完走)/ 原典 `xd2_bridge_2b_v1_erratum_a.md` §2.3
**答え**: **(a) が書ける。かつ (b) の「格が落ちる」は誤解 — 存在主張は乱択+決定的検証で証明になる。**

---

## §0 結論(3 行)

1. ★ **$d=3$ は理論で消える**(定理 GEN23-DET・§2)⟹ **3 群 → 2 群**($d=2,6$)。
2. ★ **残る 2 群に「全元対」は不要** — $(2,3)$-生成は**存在主張**だから**目撃者 1 対**で証明できる(§3)。$10^{16}$ は「全対を数える」問題の費用であって「生成するか」の費用ではない。
3. ★ **乱択で格は落ちない**: 乱択は**目撃者の探索**にのみ使い、判定は `Size(Group(a,b))` の**決定的**計算で行う ⟹ 陽性は**証明**。悉皆が要るのは**陰性主張**のときだけ(その場合は §2 の定理が代わりを務める)。

---

## §1 実装係の STOP は正しい(仕様の欠陥は起草者)

v1.1 §3 の「$(2,3)$-生成(**全元対**)」は、falsifier の実装警告(**共役類代表対では偽陰性**)を**そのまま仕様に転写した**もので、$q=7$ の**悉皆計数**には正しいが $q=691$ には移せない。**私が「対を数える」計算と「生成するか」の判定を区別せずに書いた**のが原因。STOP は正しい。

---

## §2 ★★★ 定理 — $d$ が奇なら $(2,3)$-生成でない($d=3$ の除外)

> ### 定理候補 **GEN23-DET**(candidate・本ノート・repo 初出)
> $q$ を**奇**素数冪、$SL(2,q)\le H\le GL(2,q)$、$D:=\det(H)\le\mathbf F_q^\times$ とする。$H$ が $(2,3)$-生成なら
> $$\boxed{\ D\subseteq\mu_6\quad\textbf{かつ}\quad \lvert D\rvert\ \textbf{は偶数}\ }$$
> **証明**: $H=\langle a,b\rangle$, $a^2=b^3=1$ とする。
> (i) $\det a\in D$ かつ $(\det a)^2=1$、$\det b\in D$ かつ $(\det b)^3=1$ ⟹ $D=\langle\det a,\det b\rangle\subseteq\mu_2\mu_3=\mu_6$。
> (ii) $\lvert D\rvert$ が奇なら $\mu_2\cap D=1$ ⟹ $\det a=1$ ⟹ $a\in SL(2,q)$ で $a^2=I$。$q$ 奇ゆえ $X^2-1$ は重根を持たず $a$ は対角化可能・固有値 $\in\{\pm1\}$ ⟹ $\det=1$ から $a=\pm I$ = **中心元**。
> ⟹ $\langle a,b\rangle=\langle\pm I,b\rangle$ は位数 $\le6$ の可換群 ⟹ $H$ 全体になりえない($\lvert H\rvert\ge\lvert SL(2,q)\rvert$)。∎

**適用**($q=691$、$H_d=\{M:\det M\in\mu_d\}$、$d\mid6$):

| $d$ | $\lvert D\rvert=d$ | GEN23-DET | 判定 |
|---:|---:|---|---|
| 1($=SL$) | 1(奇) | 違反 | ✘ **除外**(既知の結論を**一般証明で**回収) |
| **2** | 2(偶) | 通過 | ○ 候補 |
| **3** | 3(**奇**) | ★ **違反** | ★ **除外(理論)** |
| **6** | 6(偶) | 通過 | ○ 候補 |

> $$\boxed{\ \textbf{⟹ 枝 (ii) の標的は }H_2=SL^\pm(2,691)\ \textbf{と }H_6\ \textbf{の}\textbf{2 群だけ}\ }$$
> ★ **$SL(2,q)$ が $(2,3)$-生成でない**という erratum A §1 の主張も、本定理の $d=1$ の場合として**統一的に**従う(以前は「対合が中心 $-I$ のみ」という個別議論だった)。

---

## §3 ★★ 残る 2 群 — **目撃者 1 対**で足りる

### 3.1 費用の再解析(裁定 827 の $10^{16}$ の正体)

| 問い | 費用 |
|---|---|
| 「$(2,3)$-生成**対の個数**は?」($q=7$ で 2688 を出した計算) | $O(\lvert H\rvert^2)$ ⟹ $q=691$ で $10^{16}$〜$10^{18}$ **不可能** |
| ★ 「$H$ は $(2,3)$-**生成か**?」 | **1 対あたり $O(\text{Size 計算})$**。陽性なら**そこで終わり** |

$(2,3)$-生成は $\exists$ 主張である。**個数は要らない。**

### 3.2 目撃者法(仕様)

> ### 発注 **W691-GEN23**(W691-SCAN §1 の差し替え)
> **対象**: $H_2=\{\det=\pm1\}$、$H_6=\{\det\in\mu_6\}$ over $\mathbf F_{691}$($6\mid690$ ✔ ゆえ $\mu_6$ 存在)。
> 1. **候補生成(乱択・探索のみ)**:
> - $a$: 非中心対合。$a=g\,\mathrm{diag}(1,-1)\,g^{-1}$($g\in GL(2,691)$ 乱択)⟹ $a^2=I$・$\det a=-1$ ✔(構成から**保証**)。
> - $b$: 位数 3。$H_2$ では $\det b=1$ ⟹ $SL$ 内の位数 3 元($3\mid690$ ✔)。$H_6$ では $\det b=\omega$(原始 3 乗根)も可 — **両方試す**。
> 2. **判定(決定的)**: $S:=\mathrm{Size}(\mathrm{Group}(a,b))$ を GAP で計算し、$S=\lvert H_d\rvert$ か照合。
> - $\lvert SL(2,691)\rvert=691\cdot(691^2-1)=329{,}938{,}680$、$\lvert H_2\rvert=659{,}877{,}360$、$\lvert H_6\rvert=1{,}979{,}632{,}080$。
> - $2\times2$ 行列群($\mathbf F_{691}$)の `Size` は nice monomorphism/Schreier–Sims で**秒**。
> 3. **停止**: 陽性が 1 対出た時点で **$H_d$ は $(2,3)$-生成**(証明)。試行上限 = 2000 対で打切り ⟹ 打切り時は **UNKNOWN**(「生成でない」と書かない)。
> **期待試行回数**: $q=7$ の実測比 $2688/9690\approx27.7\%$ ⟹ 数対で当たる見込み(**予言ではなく費用見積り**)。
>
> ### ★ カナリア(GEN23-DET の検定・$q=7$ の既存ハーネスで即可)
> $$\boxed{\ H_3^{(7)}=\{M\in GL(2,7):\det M\in\mu_3\}\ (\text{位数 }1008)\ \textbf{は }(2,3)\textbf{-生成でない}\ }$$
> 既存の全元対ハーネスで**悉皆判定できる規模** ⟹ 定理 GEN23-DET の**独立検証**になる。**外れたら定理が誤り** ⟹ 即停止・報告。
> 併せて $H_2^{(7)}=SL^\pm(2,7)$ ○・$H_6^{(7)}=GL(2,7)$ ○ の既知 2 件と合わせ **$q=7$ で $d=1,2,3,6$ の 4 点全一致**を要求。

### 3.3 陰性が必要になった場合の予備(悉皆の代替)

万一 $H_2$/$H_6$ が 2000 対で当たらず**陰性を主張したくなった**ら、悉皆ではなく **Dickson による極大部分群排除**を使う: $\langle a,b\rangle$ が $SL(2,691)$ を含むことは「Borel(共通固有ベクトルなし)・トーラス正規化群(固有ベクトル対を保たない)・$A_4/S_4/A_5$(元の位数 $>5$)・部分体($q$ 素数ゆえ無し)のいずれにも入らない」で判定でき、**1 対あたり定数回の行列演算**。
⚠ ただし**陰性主張は $\forall$ 主張**なので、これでも全対が要る ⟹ **現実的には陰性は主張しない**(UNKNOWN で止める)。

---

## §4 観察「$2688=2688$」— 候補説明と 1 行検証(解釈不要とのことだが、安いので置く)

$B_3=\langle x,y\mid xyx=yxy\rangle\cong\langle u,v\mid u^2=v^3\rangle$、**$u=xyx=\Delta$、$v=xy$**。逆写像は $x=v^{-1}u$、$y=u^{-1}v^2$ ⟹
$$\boxed{\ (x,y)\ \longmapsto\ (u,v)=(xyx,\ xy)\ \textbf{は「braid 対」と「}u^2=v^3\ \textbf{を満たす対」の全単射で、生成性を保つ}\ }$$
$u^2=v^3=\Delta^2=c$ の像 $z$ は $\langle u,v\rangle=H$ の**中心元**。$H=GL(2,7)$ で生成対なら $x\sim y$(braid 共役)⟹ $\det x=\det y=\delta$ が $\mathbf F_7^\times=C_6$ を生成 ⟹ $z=\lambda I$ と $\det z=\delta^6=1$ から $\lambda^2=1$ ⟹ $z=\pm I$。
⟹ **$z=I$ の braid 対 $\leftrightarrow$ $(2,3)$-生成対**(全単射)。
> ### ★ 1 行検証(既存ハーネスで即可)
> **2688 個の生成 braid 対すべてで $(xyx)^2=I$ か**(= $z=-I$ が 1 つも出ないか)を確認せよ。真なら一致は**構造**(上の全単射)、偽なら**偶然**(数が偶然揃った)。
> **なぜ $z=-I$ が出ないのか**は現時点 **UNKNOWN**($z=-I$ は $B_3\to GL(2,7)$ で $c\mapsto-I$ を意味し、$c$ は $B_3^{\rm ab}$ で $6$ に落ちるが $-I$ は $SL$ 内 ⟹ 矛盾しない)。

---

## §5 FYI 3 件の受領(返答不要とのことだが 1 行ずつ・triage 用の所見のみ)

- **① FIB-MULT-2000**: ★ **$[1500,14]=5$ は $[750,6]$ の指数 2 倍で多重度も 5** ⟹ 「750 クリークが $\times2$ で持ち上がる」= **クリークの族性**の示唆。triage 順位は高い(繊維チャートの初の**構造**)。
- **② P-Q8FAM 42/42**: 指数 $24m$($m$ 奇 $\le83$)欠員ゼロ ⟹ Q8-FAM は**確定的**。指数式は $24m$ が正(私の原文も $24m$・追補 A §1.5 表と一致)。
- **③ WIN-CNOTN 83 件**: ★ **$M_5$ の孤独が解消** ⟹ 【HA-GAP-2】(「$c\notin N$ ∧ 窓 ∧ $j\nmid3$」の全数)が**測れる状態**に。**次の一手 = 83 件の $j$ 値算出**($e=2j$ の $j$)⟹ $j\nmid3$ の実地母集団が確定し、**P-CHI-1′ / TWIST-6-ABS の検定母集団**になる。編纂後 triage の筆頭に推す。

---

## §6 【GAP】・帰属

| # | 内容 | 重さ |
|---|---|---|
| **【W6-GAP-1】** ★新 | $H_2$/$H_6$ の $(2,3)$-生成は**未決**(目撃者探索で陽性が出れば証明・出なければ UNKNOWN) | ★ 中 |
| **【W6-GAP-2】** ★新 | §4 の $z=-I$ 不在の理由は UNKNOWN(1 行検証で事実だけは決まる) | 小 |
| **【BR-GAP-3】(継続)** | Dickson 分類は**引用**【要 pin】 | 小 |

**帰属**: STOP と費用の指摘・$GL(2,7)$ カナリア完走・$2688$ の観察 = **実装係/falsifier**(裁定 827)。**仕様欠陥(「全元対」の無条件転写)は起草者の責任**。
本ノートの新規部分 = **定理候補 GEN23-DET($d$ 奇 ⟹ 非 $(2,3)$-生成・$SL$ の除外を一般化)** / **$d=3$ の理論的除外(3 群→2 群)** / **「存在主張は目撃者 1 対で証明・乱択は探索にのみ使う」の設計原理** / **発注 W691-GEN23 と $q=7$ 4 点カナリア** / **§4 の全単射 $(x,y)\mapsto(xyx,xy)$ と 1 行検証**。

**novelty grep**: `GEN23-DET` `W691-GEN23` = 0 hit(本ノート初出)。

**検算**:
```bash
python -c "
q=691
sl=q*(q*q-1); print('|SL(2,691)| =',sl,' |H_2| =',2*sl,' |H_6| =',6*sl)
print('6 | q-1 ?',(q-1)%6==0,'  3 | q-1 ?',(q-1)%3==0)
print('q=7: |H_3| =',3*7*(49-1),'(悉皆可能規模 = カナリア)')
"
```
