# 段 1′(Ad) prereg カード **v1.1**(裁定 1011)

作成: 数学者(Opus 5)/ 2026-08-12 / 発注 = 司令塔裁定 1011
前版 = `F_stage1ad_prereg_v1.md`(e6466470)。前哨逐語 = `fals_F_stage1ad_audit_v1.md`(条件付き PASS)
⚠ 本書は **v1 への差分**です。v1 の §1〜§8 のうち**本書で置換される節を明示**します。
⚠ 数値出力なし($u$ 非接触・$c$ 未評価)。**NAME-COLLIDE 行**: $H_F$ は `pair_h2_design_draft_v1.md` §2 の $\bar G$ と**同一対象**・$|H_F|=|H_6|$(位数一致・構造別)・**P-PH2-4 は再掲**。

---

## §0 本改訂の要点

1. 処方 7 件 + 軽微 5 件を**逐語履行**(§1〜§3)。
2. ★ **系統 C を採録**し、**非当事者として検算 PASS**(§4)。⚠ ただし falsifier の自己申告「**著者も機構も別**」を**精密化**します — **完全独立ではなく段別**(§4.3)。
3. **F-GAP-7 を紙で閉鎖**(§5)。
4. **Sym² 規約分離テストを採用**(cal-6・§3.5)。

---

## §1 【重大 1】identity_witness 方式(v1 §6 の cert schema を置換)

系統 B は $H_F$ を**構成しません**(それが速い理由)⟹ `group_fingerprint` は本番経路で**埋まりません**。v1 §5×§6 の内部矛盾を解消:

```
★ 系統 B(安定元法)で走った場合の cert:
  group_constructed   : false
  group_fingerprint   : null                    ← 構成していないので埋めない
  identity_witness    : { p,
                          u1        : u(1) の行列,
                          torus_gen : d(λ) の生成元 λ とその行列,
                          lift_A    : 互換の持ち上げ A = diag(1,-1),
                          module_action : M の作用行列(sl_2 の e,h,f 基底で),
                          script_input_hash }
  identity_basis      : "paper_asserted"
      ★ 逐語: 「これらが H_F の Sylow / トーラス / 持ち上げであることは *紙の主張* であり、
                機械照合されていない」
★ 系統 A(cohomolo)で走った場合: group_constructed=true・group_fingerprint を従来どおり埋める
```
⟹ ★ **どちらの経路で出た値かが cert 単体で読める**(v1 の `module`/`r_value` と同じ思想)。

## §2 【重大 2】格付けの訂正(v1 §5 を置換)

> ★ **逐語**: 「$p=691$ では系統 A が走らないため、本番値は**単系統(系統 B)+紙の導出**であり、格は **candidate/single-run** に留まる。**cross-checked と称してよいのは cal-4/cal-5($p\le23$)の一致に対してのみ**。」

★ **系統 C を入れた後の格**(v1 の過大表現を訂正):
$$\boxed{\ p=691\ \textbf{本番値の格} = \textbf{「紙の二導出 + 単系統実装」}\quad(\textbf{cross-checked ではない・verified でもない})\ }$$
⚠ **verified は Lean に予約**(工房規律)。⚠ **cross-checked** は $p\le23$ の較正一致に限定。

## §3 要修正 1〜5 + 軽微 1〜5

### 3.1 【要修正 1】枝表のゲート行(v1 §3 の最上段に挿入)
> **適用前提**: `calib` 全 PASS **∧** `bound_violated=false`。**いずれか偽なら枝表を適用せず UNKNOWN+STOP**。
★ これで「`bound_violated=true` ∧ 和 $=1$ が (Ad-b) に落ちる穴」が塞がります。

### 3.2 【要修正 2】(Ad-b′) の fail-closed 化(v1 §3 の該当行を置換)
> **(Ad-b′)** $(0,1)$ = ★ **UNKNOWN + STOP**。$p$-一様な符号誤りは実測 4 点で排除済 ⟹ 疑う順は
> **(a)** 系統 B の $p=691$ 走行バグ → **(b)** 対象が $H_F$ でない(`identity_witness` を照合)→ **(c)** $p$ 固有効果。
> **段 2 の許可は、原因 (a)(b)(c) の同定 + 較正素数での再現まで出さない。**

### 3.3 【要修正 3】$n\ge1$(v1 §1.1 の箱を修正)
$$H^n(SL(2,p),M)\cong H^n(C_p,M)^T\qquad\boxed{(n\ge1)}$$
⚠ $n=0$ では偽($H^0(SL,M)=M^{SL}$ vs $H^0(C_p,M)^T=M^{P\rtimes T}=M^B$)。本カードは $n=2$ のみ使用。

### 3.4 【要修正 4】撤退条件 2 件(v1 §7 に追加)
| 事象 | 行き先 |
|---|---|
| 系統 B が cap 60 秒を超過 | ★ **UNKNOWN + 設計差し戻し**(線形代数が 60 秒を超えるのは設計の誤り) |
| `i0_observed = null` | ★ **UNKNOWN**(欠測を PASS に読まない) |

### 3.5 【要修正 5】cal-5 の反同語反復 + 【軽微 5】cal-6 新設(v1 §4 を置換)
```
[cal-5] 安定元法の独立実装。★ 逐語: y の T-重みを §1.2 から *読まず*、
        P の自己同型 u ↦ u^c の分解の持ち上げから導出すること。
        導出できなければ method:"paper_lemma" と申告し "stable_elements" を名乗らない。
        ★ p=691 走行と p<=23 較正走行は *同一スクリプト・同一コード経路*(差は p のみ)であることを
          input_hash で示す。
★ [cal-6] 新設(軽微 5・規約分離テスト): Sym^2(W) を det に一切言及せず構成し、
        (H_F, Sym^2 W), (H_F, Sym^2 W ⊗ det) を測る。
        予言 = ★ (0, 1) — 現行スイートに無い *唯一の出力形*。
        根拠(検算済): GL_2 上で det(Sym^2 W の作用)=det(g)^3、det(Ad)=1 ⟹ sl_2⊗det の det=det^3 ✔
                      SL 上 Sym^2 W ≅ sl_2(char≠2)⟹ ★ Sym^2 W ≅ sl_2 ⊗ det(GL_2-同型)
        ⟹ det の *向き* を独立に釘付けする(sl_2 側だけでは向きが縮退して見える)。
★ [cal-4] ⚠ 逐語: リハ素数を p=5 に落とすことを *禁止*。理由 = §3.6。
```

### 3.6 ★ 較正の非空虚性(逐語 §A の強化を採録・v1 §4 に追記)
最も起きやすい誤り = **双対規約の反転**($x$ を $\lambda^{+2}$ と取る)。すると $\mathfrak{sl}_2$ の $H^2$ 重みが $\lambda^{4}$ になり、
$$\lambda^4\ \text{自明}\iff (p-1)\mid4\iff p\in\{2,3,5\}$$
⟹ ★ **$p=7,11,23$ の実測 $(1,0)$ は規約を実際に判別している**(較正は空虚でない)。
⟹ ⚠ **$p=5$ 単独では判別不能** ⟹ **リハ素数を $p=5$ に落とすことを禁止**(cert に `calib_primes` を記録し $\{7,11,23\}$ を含むことを assert)。

### 3.7 軽微 1〜4
1. **和則の等号**: 1 次元 $\mathbf F_{691}[S_3]$-加群は $691\nmid6$ ゆえ半単純 ⟹ 既約 = 自明 か sgn の**一方** ⟹ **和 $=1$ ちょうど**($\le$ ではなく $=$)。v1 §2 予言 3 と §4 assert の不整合を解消。
2. **F-GAP-7 は紙で閉** ⟹ §5。
3. **LHS 退化の本文明示**: $\gcd(6,691)=1$ ⟹ $H^a(S_3,-)=0$($a>0$)⟹ $H^n(H_F,M)\cong H^n(SL,M)^{S_3}$。
4. **sign 成分 $=0$ の理由**: 691 が奇 ⟹ 2 が可逆 ⟹ 1 次元表現の等質分解で自明成分と sgn 成分が分離する。

---

## §4 ★ 系統 C の採録(逐語 §B)と、私の非当事者検算

### 4.1 系統 C(重み・Bockstein・持ち上げ選択を一切使わない導出)
1. **上界 $\le1$**: $\mathrm{res}$ が $p$-part 単射 ⟹ $\dim H^2(SL,\mathfrak{sl}_2)\le\dim H^2(C_p,\mathfrak{sl}_2)=\dim\mathfrak{sl}_2^P=1$。
2. **下界 $\ge1$**: $1\to\mathfrak{sl}_2\to SL(2,\mathbb Z/p^2)\to SL(2,p)\to1$(核 $=\{I+pX:\mathrm{tr}X=0\}\cong\mathfrak{sl}_2$・作用 $=\mathrm{Ad}$)。$u$ の持ち上げは**必ず位数 $p^2$** ⟹ 補元なし ⟹ **非分裂** ⟹ $H^2\ne0$。
3. **$S_3$ 符号**: $\tilde A=\mathrm{diag}(1,-1)\in SL^\pm(2,\mathbb Z/p^2)$ の共役が**拡大全体を保ち**、核に $\mathrm{Ad}(A)$・商に $A$ を誘導 ⟹ **拡大類 $[\mathcal E]$ は $A$-不変** ⟹ 1 次元空間で非零かつ不変 ⟹ **$A$ は自明作用** ⟹ $(1,0)$。

### 4.2 ★ 私の非当事者検算 — **PASS**(個数まで一致)
$u=\begin{pmatrix}1&1\\0&1\end{pmatrix}$ の $\det\equiv1$ なる**全持ち上げ**を悉皆列挙し $w^p$ を計算:

| $p$ | 持ち上げ数 | $w^p=I$ | $w^p\ne I+pE_{12}$ |
|---|---|---|---|
| 5 | 125 | **0** | 0 |
| 7 | 343 | **0** | 0 |
| 11 | 1331 | **0** | 0 |
| 13 | 2197 | **0** | 0 |

⟹ ★ **全持ち上げで $w^p=I+pE_{12}\ne I$** ⟹ 補元なし ⟹ **非分裂** ✔ falsifier の個数申告とも一致 ✔
★ 段 3($S_3$ 符号)も紙で追認: $[\mathcal E]\ne0$ かつ $A$-不変、$\dim=1$ ⟹ $A$ 作用は $+1$ ✔

### 4.3 ⚠ **独立性の精密化**(falsifier の自己申告「著者も機構も別」を訂正)

| 段 | 私の導出(系統 B 由来) | 系統 C | 判定 |
|---|---|---|---|
| **上界 $\le1$** | $\mathrm{res}$ 単射 | **同じ** | ✘ **共有 — 独立でない** |
| **下界 $\ge1$** | (v1 は下界を別途出さず、$T$-不変が 1 次元であることから直接) | 紙で書くと $w^p=u^p(I+pN_u(Z))$ で **$N\equiv0$** を使う ⟹ 私の §1.2 と**部分共有** | ⚠ **紙は部分共有・機械(悉皆列挙)は独立** ✔ |
| **$S_3$ 符号** | 重み + Bockstein + 持ち上げ $A$ の選択 | 拡大類の $A$-不変性のみ | ★★ **完全独立**(最も危険な段) |

$$\boxed{\ \textbf{正確な格}:\ \textbf{「}S_3\ \textbf{符号段は完全独立・下界段は機械側のみ独立・上界段は共有」}\ }$$
⟹ ★ **カード本文に「完全独立な二導出」と書いてはいけません**。上の段別表をそのまま載せます。
⚠ それでも価値は大きい: **最も規約依存が強く誤りやすい $S_3$ 符号段**が完全に独立に確認されました。

---

## §5 F-GAP-7 の閉鎖(紙)

> $|P|=p$ **素数** ⟹ 相異なる Sylow-$p$ の交叉は真部分群 = 1 ⟹ ★ **TI は自動**。

⟹ 安定元定理の前件のうち **TI は無条件**、残る実質は **$N_G(P)=B$** のみ(これは $SL(2,p)$ の Borel の標準事実)。
⟹ ★ **【F-GAP-7】閉鎖**。⚠ cal-5 の裏取り対象からも **TI を外し**、$N_G(P)=B$ に集中させます。
★ さらに 逐語 §A(1) の指摘により、安定元 = $N_G(P)$-不変は **Burnside の fusion control**($P$ abelian Sylow)から従うので、Cartan–Eilenberg の一般論より軽い前件で足ります。

---

## §6 更新後の予言表(凍結対象)

| # | 量 | 予言 | 導出 |
|---|---|---|---|
| 1 | $\dim H^2(H_F,\mathfrak{sl}_2)$ | **1** | 系統 B・**系統 C** |
| 2 | $\dim H^2(H_F,\mathfrak{sl}_2\otimes\det)$ | **0** | 同上 |
| 3 | 和則 | **$=1$**(ちょうど) | 半単純性(軽微 1) |
| 4 | $\dim H^2(H_F,W)=\dim H^2(H_F,W{\otimes}\det)$ | **0**(両方) | 補題 CENT-SCAL |
| ★ 5 | $\bigl(\dim H^2(H_F,\mathrm{Sym}^2W),\ \dim H^2(H_F,\mathrm{Sym}^2W{\otimes}\det)\bigr)$ | **(0, 1)** | cal-6・$\mathrm{Sym}^2W\cong\mathfrak{sl}_2\otimes\det$ |
| 6 | `calib_primes` | $\{7,11,23\}\subseteq$ 較正素数 | §3.6($p=5$ 単独は判別不能) |

⚠ **R-1 留保 維持**・⚠ **「窓資格」は非結論**(段 2/3/4 が残る)。

---

## §7 記帳・GAP

- **【F-GAP-7】★ 閉鎖**(§5・紙)。
- **【F-GAP-8】★ 閉鎖**(逐語 §A(2): $\beta$ は係数列 $0\to\mathbb Z/p\to\mathbb Z/p^2\to\mathbb Z/p\to0$ に自然 ⟹ $\mathrm{Aut}(P)$ と可換 ⟹ $y$ も $\lambda^{-2}$)。
- **【F-GAP-9】(小・新)** 系統 C の**下界段の紙の議論**は $N\equiv0$ を私の §1.2 と共有(§4.3)。⟹ **$N\equiv0$ を使わない下界の紙**があれば独立性が上がる。⚠ 現状は**悉皆列挙(機械)が独立性の担保**。
- **★ 記帳(暫定札 m1011-1)**: v1 §5 で「系統 A/B の一致 ⟹ cross-checked」と書きましたが、**$p=691$ では系統 A が走らない**ので本番値には二系統が存在しません ⟹ **格付けの過大**。教訓 **F-3**: **「二系統」を謳う前に、*本番値の経路* で両系統が走るかを確認する**(較正でだけ二系統でも、本番は単系統)。
- **申告**: GAP 走行ゼロ(python 整数演算のみ・$p=691$ 非接触)・$u$ 非接触・$c$ 未評価・**Sol 未監査**・**verified ではない**(candidate 格)。
