# 反証前哨 — レベル16双子セル列挙機 v1(発射条件②・計画監査)

監査者: falsifier(反証前哨)。対象: `docs/manifest_twincell_v1.md`・`search/twincell-enum.g`・
`crosscheck/check-twincell.mjs`・`certificates/twincell/*`・`docs/notes/実装_双子セル.md`。
実施内容: 全対象ファイルの通読(コード行番号レベル)、`node crosscheck/check-twincell.mjs` の
実行(実測)、BFS閉包サイズ(|Q_6|/|Q_8|/|Q_10|/|Q_16|)の第三系統(この報告のための使い捨て
node スクリプト、GAP コードも check-twincell.mjs も import せず一から再実装)による整数演算のみの
再計算。証明書は改変も追加もしていない。

---

## 1. 較正①「二構成全単射一致」— marked factor map 経由か

**この観点では穴を見つけられなかった**(保証ではない)。

- `search/twincell-enum.g` L182-210 `CheckMarkedBijection`: `GroupHomomorphismByImages(G1,G2,
  [x1,y1],[x2,y2])` で x↦x, y↦y を送る具体的準同型を構成 → `IsBijective(hom)` で全単射性を検査
  → 各 GT-shadow `(m,f)` について `Image(hom,sh.f)` を計算し、run2 の shadow 集合の中に
  `(m,phiF)` が実在するか **逐一走査で確認**(L196-204)。「同じ位数の群が2つ」で済ませていない
  — `IsSubgroup`/商群の位数比較は一切使われていない。
- `crosscheck/check-twincell.mjs` L285-312 `checkMarkedBijection`: GAP の `GroupHomomorphismByImages`
  の結果を信用せず、**BFS で得た G1 の全語**(derived subgroup に限らず全群、L288)を G2 側で
  同じ語として評価し、injective(像の重複なし)・surjective(像集合サイズ=|G2|)・
  shadow-preserving(各 G1 shadow の像が G2 の shadow 集合に実在)を**独立に**再計算している。
  GAP 側 helper のインポートなし(ファイル冒頭 L7-11 で明記、実際に import 文なしを確認)。
- 実測: `node crosscheck/check-twincell.mjs` を実行し確認(本監査で実行)。
  `CALIBRATION 1 ... {"ok":true,"sizesMatch":true,"injective":true,"surjective":true,
  "shadowMatched":4,"shadow1_count":4,"shadow2_count":4}` — 4件全てが実際に一対一対応。
  空虚性なし: 二構成(行列 mod 8 / D₄³)は実際に異なるコードパスで生成され(`BuildMatQuotient`
  vs 既存 `MakeGn`)、この機構は較正①でしか叩かれないが、**その一回できちんと機構全体
  (準同型構成・全単射判定・shadow 対応)を通している**。

## 2. 較正②「既知値20の再現」— bijection まで実装されているか

**【要修正】数値一致のみで、A5 置換構成との明示的 bijection は実装されていない。設計として自認済みだが、CLAUDE.md の鉄則(指数/位数一致は kernel 主張の証明にならない)には抵触する余地を残す。**

- `search/twincell-enum.g` L232-234 のコメントで明記: 「A5 置換構成との bijection は要件外だが、
  参考として付す」。実行部(L236-242)は `runC10mat.result.shadow_total = 20` の数値一致のみを
  `calib2KnownValueOk` として判定。
- `crosscheck/check-twincell.mjs` L364-365 も同様 — `observed.hexagon_free_certificate.
  shadow_total === 20` の数値比較のみ。
- `docs/notes/実装_双子セル.md` L84-86 で implementer 自身が「A5 置換構成(既存
  `certificates/A1.v2.json`)との bijection は要求されていないため実装していない」と正直に
  自認している。
- **判定**: manifest v1 自体がこの範囲を「要件外」と明記しているため manifest 準拠ではあるが、
  司令塔の発注文面(問い2)が要求する基準(数値一致だけでは kernel 主張の証明として不十分)には
  未達。𝒞₁₀ = N_A が既知の F₂₀(≅A5 の特定核)であることの**構造的**裏付けは、|GT|=20 という
  数値のみに依存しており、較正①のような「marked factor map」レベルの保証はない。
  → **発射前に要求**: 少なくとも A5 置換構成(`A1.v2.json`)との明示的 bijection
  (または同等の marked factor map)を較正②へ追加するか、追加しないなら「較正②は数値較正のみ、
  構造較正は範囲外」という限定を manifest 本文にも明記し、この限定が K⁽⁸⁾/𝒞₁₆ 標的窓の
  「settled」判定には使えないことを事前に断っておくべき。

## 3. 標的コードパス(𝒞₁₆・K⁽⁸⁾)と較正窓の構造同一性・既知値16の内部照合登録

### 3a. コードパス同一性: 概ね同一だが未実測

- `RunWindow`/`WriteTwincellCert` は較正窓(C8/C10)と標的窓(C16/K8)で完全に同じ関数を共有
  (L120-173 で定義、L219/224/240/295/304 で全窓が同じ呼び出し)。分岐は
  `BuildMatQuotient(L,...)`(L216/237/292)と `MakeGn(n)`(L221/301)の引数(L や n)のみで、
  特殊 if/case は確認できなかった。
- ただし **標的窓側(FIRE unlocked 分岐、L287-308)は司令塔いわく実行未確認**
  (`実装_双子セル.md` L75-79 が implementer 自身の自認)。「コードは同一構造」という主張は
  静的読解のみに基づき、実測による裏書きはまだない。これは implementer 自身が正直に書いている
  ため隠蔽ではないが、発射条件②の判断材料としては「未実測」の事実を明記する必要がある。

### 3b. 【重大】K⁽⁸⁾ の既知値 |GT|=16(Thm 4.3, 2^{2α-2}, α=3)が較正スイートの内部照合として一切登録されていない

- `search/twincell-enum.g` の FIRE unlocked 分岐(L290-307)を精査したが、K8 について
  `calib1KnownValueOk`(C8 用, L229)や `calib2KnownValueOk`(C10 用, L241)に相当する
  **既知値チェック変数が存在しない**。K8 は `RunWindow` → `WriteTwincellCert` を呼ぶだけで、
  `runK8.result.shadow_total = 16` を検証する `Print`/判定行が一つもない。
  コメント(L302)は「expect saturated |GT|=16 per Thm 5.3」と書いてあるが、これは**印字
  コメントのみ**であり、pass/fail に反映される較正判定ではない。
- `crosscheck/check-twincell.mjs` 側も同様: `calib1KnownValue`(L357-359, 値4を検査)・
  `calib2KnownValue`(L364, 値20を検査)は存在するが、K8 用の「shadow_total===16」を検査する
  対応コードが**存在しない**。`checkCert` 内の汎用チェック(証明書の claimed 値と再計算値の
  一致)は行われるが、これは「証明書が内部無矛盾か」の検査であって、「16 という**独立に
  知られている正解**と一致するか」の較正ではない(自己参照的で、Thm 5.3/5.4 の外部知識と
  突合していない)。
  `calibJson`(GAP 側の`certificates/twincell/calibration_summary.v1.json`, L337-358)にも
  K8 のセクションは一切ない(calibration_1/calibration_2/negative_fixture/bonus/fire_lock のみ)。
- **これは司令塔の発注(問い3)が名指しで要求した確認項目そのものであり、答えは「未登録」。**
  この観測量(K⁽⁸⁾の shadow_total)は答えを知っている問題(Thm 5.3 で証明済み)なので、
  較正としてただで使えるはずが使われていない。CLAUDE.md の「宇宙の事前登録」原則にも一部抵触
  する: manifest の表(L12)には既知値 `|GT|=16` が明記されているのに、実行コードには
  この値との突合ロジックが存在しない — 事前登録された既知値が較正ゲートとして**接続されていない**。
  → **発射前に要求**: FIRE unlocked 分岐(または本走査後の後処理)に K8 用の
  `calib3KnownValueOk := (runK8.result.shadow_total = 16)` 相当の判定を追加し、
  `calibration_summary.v1.json` と `check-twincell.mjs` の両方に反映すること。これがないまま
  本走査を発射すると、K8 の結果が理論値と食い違っていても機械的に検知されず、司令塔の目視確認
  だけに依存する体制になる(fail-closed ではない)。

### 3c. mod ±1 分岐・"charming" 法の L=16 固有分岐

- `MatCanonL`(L53-57)・`charmingSet := Filtered([0..nOrd-1], mm -> Gcd(2*mm+1, nOrd)=1)`
  (`RunWindow` L124)は L に依存しない汎用コードで、L=16 固有の if/case 分岐は見当たらない。
  この観点では穴を見つけられなかった。
- ただし本監査で独立に BFS 閉包サイズを計算したところ(第三の実装、node、GAP/check-twincell.mjs
  いずれの helper も import せず一から書いた使い捨てスクリプト):
  ```
  L=6  BFS size= 12
  L=8  BFS size= 32
  L=10 BFS size= 60
  L=16 BFS size= 256
  ```
  これは証明書の `pb3_index`(C8=32, C10=60, WRONG_LEVEL6=12)と完全一致。L=16 の 256 は
  未生成証明書のため直接比較はできないが、少なくとも「BFS 閉包構成自体」が L に対して
  一様に正しく動くことの整数演算のみによる第三者確認にはなる。

## 4. staged counts の排他性・hexagon 語規約(A5-CONV)

- `search/week3-battery-common.g` L315-372 `EnumerateReducedHexagon`(GAP 側の正本、twincell
  はこれを import して使い回している、L32/130 で確認)は各 `(f, m)` の組み合わせを
  h10_fail → (continue) / h11_fail → (continue) / generation_fail / pass の**厳密な
  if-continue 制御フロー**で分類しており、コード構造上、同一候補が複数カテゴリに二重計上され
  ることはあり得ない(L343-366、各 `continue` が次のカテゴリ判定に進むのを阻止)。
  `crosscheck/check-twincell.mjs` L172-190 の独立実装も同型の if-continue 構造。
  両者は「shadow_sum_identity」(candidate_total - h10 - h11 - genFail = shadow_total)を
  それぞれ独立に検算しており(GAP側 L135-137、node側 L247-248)、5証明書全てで
  `shadow_sum_identity:true` を確認した(本監査でも `node crosscheck/check-twincell.mjs` 実行
  で再確認、全 PASS)。
- L315-372 のコメント(week3-battery-common.g L722-728、本監査で確認)によれば
  `EnumerateReducedHexagon` は「quotient shortcut」(`GroupHomomorphismByImages` で実際の
  θ/τ 準同型を群要素に対して直接評価する方式)を使っており、そもそも語規約(prepend/natural)の
  区別が発生しない設計。一方 `check-twincell.mjs` の独立再実装(L133-161)は**語レベル**
  (BFS+prepend 規約)で θ/τ を再現しており、GAP 側とは異なる評価方式でありながら5証明書全てで
  数値が一致した。これは「A5-CONV」規約(`docs/週次定義ノート §1.5.4` 系)がこの独立系でも
  正しく再現されていることの傍証にはなるが、**この監査では A5-CONV 適合テスト
  (`ev(yx^-1) = (1 2 4)`)そのものを twincell 系列(行列 mod L 構成)に対して直接実行した
  形跡は見当たらなかった**(A5-CONV は既存の PSL 系列 `search/manifest_spec_v2_psl.md`
  `PU-F11` で別途通されているものであり、twincell の行列 mod L 構成に対する専用の
  適合テストではない)。
  → **【軽微】**: 数値一致(5/5 全証明書 PASS)は強い状況証拠だが、A5-CONV 適合テストを
  twincell 固有の構成(行列 mod L)に対して明示的に走らせた記録がない。既存 PSL 系列で
  通っている規約を「そのまま使い回して問題ない」という前提は、今回初めて a-系統(行列)の
  θ/τ 評価が絡む場面なので、発射前に一度 A5-CONV 相当の最小テストを twincell 側でも
  明示的に走らせておくとより安全(必須ではないが安価)。

## 5. 負例 fixture・BONUS 観測の扱い

### 5a. 負例 fixture(WRONG_LEVEL6)は実際にバグ検出力を持つか — 実測で確認 PASS(この観点では穴を見つけられなかった)

- 本監査で `node crosscheck/check-twincell.mjs` を実際に実行(改変なし)。結果:
  ```
  window C8_matrix_WRONG_LEVEL6_fixture: PASS
  ...
  [PASS] NEGATIVE FIXTURE (wrong level L=6) correctly rejected: bijection_ok=false, shadow_total=6
  ```
  「window ...: PASS」は**証明書の自己無矛盾性**(claim=recompute)の意味であり、これは
  正しく振る舞う(fixture は正直に作られているので自己矛盾はない)。実際の負例判定は別の行
  `NEGATIVE FIXTURE ... correctly_rejected` であり、`calibBad.ok=false`(bijection 不成立)
  かつ `shadow_total=6≠4` の**両方**が検出されている(L370-373)。総合判定 `all_ok`
  (L383)にも `negFixtureOk` が組み込まれており、これが false になれば `all_pass` は
  出ない設計になっている(fail-closed)。
  → **【軽微】表記上の紛らわしさ**: `window C8_matrix_WRONG_LEVEL6_fixture: PASS` という
  一行だけを見ると、負例が「合格してしまった」ように誤読され得る。実際は「証明書自体は
  正直(自己無矛盾)」という別の意味であり、負例としての合否は別行に出る。将来の読者(司令塔・
  Sol・研究者)向けに、per-cert PASS/FAIL の出力ラベルに「self-consistency」等の注記を
  加えるとより安全(機能上のバグではない)。
- 独立の第三系統(本監査用の使い捨て BFS 実装)でも |Q_6|=12(≠|Q_8|=32)を確認済み(§3c)。
  負例が「本当に違う群」であることは数学的にも裏が取れている。

### 5b. BONUS(Ȳ 符号反転)観測の扱い

- コード(`twincell-enum.g` L247-253)・実装ノート双方で「これは壊れていない正しい結果だった
  ため、負例として不採用にした」という判断過程が明記されている。数学的には Γ(2) の別の
  自由生成対を与えるだけで K⁽⁴⁾ の別実現になっている、という説明は妥当に見える
  (符号反転は `Ybar -> -Ybar` であり、`<Xbar,Ybar> = <Xbar,-Ybar>` は一般に同じ部分群を
  生成するので、驚くべき結果ではない)。この扱い自体(BONUS として隔離し、負例カタログには
  含めない)は誠実で、「本来 FAIL すべきなのに見過ごされた」という兆候は見当たらなかった。
  ただし、この BONUS が「頑健性の確認」としてのみ扱われ、**なぜ負例として機能しなかったかの
  数学的説明(部分群の一致)が厳密に証明されているわけではなく、実行結果からの帰納的説明に
  留まっている**点は留意事項として記載しておく(致命的ではない、UNKNOWN 扱いでも良い)。
  → この観点では穴を見つけられなかった。

## 6. 発射錠・証明書スキーマ・仕様ヘッダ

- **発射錠**: `search/FIRE_twincell.auth` の `IsExistingFile` 機械的ガード(L284-308)を確認。
  本監査時点でこのファイルは存在しない(`Glob` で確認、0件)ため標的窓は封鎖されている
  ことを直接確認した。ガードは「ドキュメント上の約束ではなく実行時ガード」という設計意図
  (ツール仕様ヘッダ L5-7)通りに実装されている。
- **証明書スキーマ**: 全 JSON に `"schema":"gtsh-cert/twincell-v1"`・`"generated_by":
  {"tool":"GAP 4.16.0","script":...,"date":...}` ヘッダあり(5証明書全てで確認)。
  `"isolated":"UNKNOWN"` が全証明書の末尾に存在(UNKNOWN の置き場が用意されている、確認済み)。
- **【要修正】出所ハッシュの LEDGER 未記載**: `provenance/LEDGER.md` の該当エントリ(L372-374,
  「2026-07-26 -- レベル16双子セル列挙機 v1」)には構築内容の説明はあるが、**証明書の
  SHA-256 ハッシュが記録されていない**。同じ LEDGER 内の直前セクション(E2C6, L369)では
  「証明書全118件連結SHA-256=...」と明記されており、これが工房の標準(CLAUDE.md「出所管理:
  ...全証明書ハッシュを記録」)。twincell の5証明書にはこの記録が欠けている。
  → **発射前に要求**: 5証明書(較正分)のハッシュを LEDGER に追記すること。標的窓解錠後の
  本走査証明書についても同様の記録を義務付けること。
- **【要修正】数値 cap・撤退条件がこのサブタスクの粒度では明文化されていない**:
  `docs/manifest_twincell_v1.md` の「発射条件」節(L24-26)には①②③の**ゲート条件**は
  あるが、標的窓の本走査自体について、最大位数・最大候補数・実測時間 cap・超過時の行き先
  (どこにエスカレーションするか)についての明記がない。`SizeScreen([4096, 0])`
  (twincell-enum.g L29)は GAP のメモリ確保画面サイズであり、計算量 cap ではない。
  本監査で独立計算した限り(§3c)|Q_16|=256 であり、実際の計算コストは小さいと推測される
  ため実害の可能性は低いが、CLAUDE.md の「撤退条件(先に書く)」原則・E2C6 系列の
  「600秒 cap」慣行と比べると、本 manifest にはこの粒度の明文化が欠けている。
  → 軽微〜要修正の中間: 実害は低いと見るが、工房の慣行(cap を先に書く)からの逸脱として
  記帳しておくべき。

## 追加チェックリスト(常設観点)まとめ

- **空虚性**: 較正①は実際に marked bijection 機構全体を通した実例(問題なし)。較正②は
  数値較正のみで「kernel 主張」レベルの機構(marked bijection)を一度も通していない
  (§2 で指摘、要修正)。K8 の既知値16は較正として一度も**登録されておらず**、機構を
  通す機会自体が存在しない(§3b、重大)。
- **事前登録**: manifest に窓・生成系・既知値は固定されている(表 L7-12)。silent cap は
  見当たらない(コードの if/case は L や n の値のみに依存し、暗黙の絞りは確認できなかった)。
  ただし §3b の通り、事前登録された既知値(K8=16)が実行コードに接続されていないという
  「登録と実行の間の断線」がある。
- **証明の型**: 負例 fixture は「見つからなかった」ことを非存在の証明として扱っていない
  (FIRE ロックにより標的窓は単に UNKNOWN のまま保持され、`"isolated":"UNKNOWN"` で
  一級の結果として記録される設計)。較正②の個数一致は kernel 主張の証明として不十分である
  ことを implementer 自身も自認しており(実装ノート L84-86)、隠蔽はない。
- **独立性**: `check-twincell.mjs` は `search/twincell-enum.g`・`week3-battery-common.g`・
  `gaplib_common.g` を import していない(ファイル冒頭で明記、実コードでも import 文なしを
  確認)。matrix-mod-L・D_n^3・BFS・marked bijection を全て一から再実装している。
  「照合済み(cross-checked)」という語は実装ノート・manifest 双方で正しく使われており、
  「検証済み(Lean)」との混同は見当たらなかった。
- **撤退条件**: FIRE ロックそのものは機能している(実測確認済み)。ただし数値 cap の明文化
  はこのサブタスクの粒度では見当たらない(§6、要修正)。
- **安価な数値反証**: 本監査で BFS 閉包サイズ(|Q_6|,|Q_8|,|Q_10|,|Q_16|)を第三の
  独立実装(整数演算のみ)で再計算し、証明書の `pb3_index` と完全一致することを確認した
  (§3c)。shadow_sum_identity(candidate_total - h10_fail - h11_fail - generation_fail =
  shadow_total)も5証明書全てで成立を確認した(node 実行、本監査)。

---

## 総合判定

**PASS 保留(要求事項リスト)**

以下を解消してから発射(FIRE_twincell.auth 発行)することを推奨する:

1. 【重大】K⁽⁸⁾ の既知値 `|GT|=16`(Thm 4.3/5.3, α=3)を、標的窓本走査の内部較正判定として
   `search/twincell-enum.g`(FIRE unlocked 分岐)・`crosscheck/check-twincell.mjs`・
   `certificates/twincell/calibration_summary.v1.json` の3箇所に明示的に追加すること
   (現状は印字コメントのみで pass/fail に反映されていない)。
2. 【要修正】較正②(𝒞₁₀=N_A)を数値一致のみで済ませている点について、A5 置換構成との
   marked bijection(較正①と同水準)を追加するか、少なくとも manifest 本文に「較正②は
   数値較正のみ、構造較正は範囲外」であり、この限定が標的窓の settled 判定根拠には
   使えないことを明記すること。
3. 【要修正】`certificates/twincell/*` のハッシュを `provenance/LEDGER.md` に追記すること
   (E2C6 系列と同じ慣行)。
4. 【要修正】標的窓の本走査について、数値 cap(最大候補数・実測時間の目安)と超過時の
   エスカレーション先を manifest に明記すること。
5. 【軽微】`check-twincell.mjs` の per-cert 出力ラベル(「window ...: PASS/FAIL」)が
   「証明書の自己無矛盾性」を意味することを、出力または README で一言注記すること
   (負例 fixture の PASS 表示が誤読されやすい)。
6. 【軽微】A5-CONV 適合テストを twincell の行列 mod L 構成に対しても明示的に一度走らせて
   おくこと(既存 PSL 系列での合格を流用している状態、数値上は5/5一致で状況証拠は強いが
   専用テストの記録はない)。

上記1・2・3・4を満たせば発射条件②は PASS としてよいと考える(5・6 は発射を止める理由には
ならない軽微な改善提案)。較正①の marked factor map 実装・FIRE ロックの実効性・staged counts
の排他性・負例 fixture の検出力については、この監査ではコード読解と実行確認の両方で
穴を見つけられなかった(保証ではない — 未実行の標的窓コードパス自体は実測未検証のまま
残っている)。
