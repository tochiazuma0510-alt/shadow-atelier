# falsifier focused 前哨((F) 段 1′(Ad) prereg カード)— 逐語収蔵

**出所**: falsifier(裁定 1009 委嘱・focused)。対象 = F_stage1ad_prereg_v1.md(e6466470)。**判定 = 条件付き PASS**(紙の導出 3 段はすべて正・処方 7 件の履行が条件)。機械前哨 = node 整数演算のみ(p=691 非接触・GAP 走行ゼロ)。収蔵 = 裁定 1011(検収時 file 化の恒久規律)。

## A. 紙の導出 3 段 — 全段 正(反証できず)

| 段 | 検算 | 結果 |
|---|---|---|
| (1) 安定元 | P abelian Sylow(\|P\|=p)⟹ Burnside の fusion control ⟹ 安定元 = N_G(P)-不変。P は SL に共役作用が内部なので H^n(P,M)^N=H^n(P,M)^T。M が F_p-加群 ⟹ 全体 p-torsion ⟹ 前件充足 | 正(ただし n≥1 — §C-3) |
| (2) 重み | u の sl₂ 上 Jordan size 3 ≤ p−1 ⟹ N=0・M^P=⟨e⟩(1 次元)、Ad(d(λ))e=λ²e、x∈Hom(P,F_p) は λ^{-2}、β は係数列 0→ℤ/p→ℤ/p²→ℤ/p→0 に自然 ⟹ Aut(P) と可換 ⟹ y も λ^{-2}。カップ積同変 ⟹ 重みは積 | 正(**F-GAP-8 も成立**) |
| (3) S₃ 符号 | Au(a)A⁻¹=u(−a)、Ad(A)e=−e ⟹ (−1)(−1)=+1。持ち上げ非依存(A′=gA, g∈SL ⟹ SL は H^*(SL,M) に自明作用)も正 | 正 |

機械確認(p=5,7,11,13,23,29,31,37,101 全 true): dim ker(U−I)|sl₂=1・ker=⟨e⟩/N|sl₂=0/Ad(d)e=λ²e/Ad(A)e=−e/AuA⁻¹=u(−a)/W 層同様。

**★較正の非空虚性(カードの主張より強い)**: 最も起きやすい誤り = 双対規約の反転(x を λ^{+2})は sl₂ の H² 重みを λ⁴ にし、λ⁴ 自明 ⟺ (p−1)|4 ⟺ **p=5 のみ** ⟹ p=7,11,23 の実測 (1,0) は規約を実際に判別(空虚でない)。同時に **p=5 単独では判別不能 ⟹ 「リハ素数を p=5 に落とす」禁止を明記すべき**(現状カードに無い)。

## B. ★独立導出(系統 C 提案 — 「同一人物の導出と実装」の実質的解)

§1.2 の重み計算・Bockstein・A の持ち上げを一切使わずに (1,0):
- **上界 ≤1**: res の p-part 単射 ⟹ dim H²(SL,sl₂) ≤ dim H²(C_p,sl₂) = dim sl₂^P = 1。
- **下界 ≥1**: 1→sl₂→SL(2,ℤ/p²)→SL(2,p)→1。u の持ち上げ w は必ず位数 p²(w^p ≡ I+p·E₁₂ ≠ I)⟹ 補元なし ⟹ 非分裂。**機械確認: p=5,7,11,13 で det≡1 の全持ち上げ(125/343/1331/2197 個)を尽くし w^p=I は 0 個・全て w^p=I+p·E₁₂**。
- **S₃ 符号**: Ã=diag(1,−1)∈SL^±(2,ℤ/p²) の共役が拡大を保ち核に Ad(A)・商に A を誘導 ⟹ 拡大類は A-不変 ⟹ 自明作用 ⟹ (1,0)。

⟹ F-GAP-8 と A 選択依存性が**本番値に対して二重化**。compute ゼロ・著者も機構も別。**カードへの追加を強く推奨**。

## C. 指摘(処方)

### 【重大 1】系統 B は H_F を構成しない ⟹ group_fingerprint が本番経路で埋まらない(§5×§6 内部矛盾)
処方(逐語): 系統 B の cert は `group_constructed: false`・`group_fingerprint: null` とし、代わりに `identity_witness = {p, u(1), d(λ) 生成元, 持ち上げ A, M の作用行列, スクリプト入力ハッシュ}` を必須欄とする。これらが H_F の Sylow/トーラス/持ち上げであることは紙の主張であり機械照合されていない旨を `identity_basis: "paper_asserted"` として cert に記す。

### 【重大 2】p=691 に二系統は存在しない ⟹ 格付け文言が過大
処方(逐語): §5 に一行 — 「p=691 では系統 A が走らないため、本番値は**単系統(系統 B)+紙の導出**であり、格は candidate/single-run に留まる。cross-checked と称してよいのは cal-4/cal-5(p≤23)の一致に対してのみ。」(系統 C を入れれば「紙の二導出+単系統実装」まで上がる)

### 【要修正 1】枝表のゲート行
枝表の最上段に — 「適用前提: `calib` 全 PASS ∧ `bound_violated=false`。いずれか偽なら枝表を適用せず UNKNOWN+STOP。」(和 ≥2 は STOP 行が拾うので N² 被覆自体は完全。bound_violated=true ∧ 和=1 が (Ad-b) に落ちる穴を塞ぐ)

### 【要修正 2】(Ad-b′) の診断順序と fail-closed 化
p-一様な符号誤りは実測 4 点で排除済 ⟹ (0,1) の疑い順は (a) 系統 B の p=691 走行バグ (b) 対象が H_F でない(identity_witness)(c) p 固有効果。処方: 「(Ad-b′) = UNKNOWN+STOP。段 2 許可は原因 (a)(b)(c) 同定+較正素数で再現まで出さない。」

### 【要修正 3】§1.1 の箱に n≥1(H⁰ では偽)
### 【要修正 4】撤退条件 2 件(系統 B cap 60 秒超過 ⟹ UNKNOWN+設計差し戻し/i0_observed=null ⟹ UNKNOWN)
### 【要修正 5】cal-5 の反同語反復
処方(逐語): cal-5 の実装は y の T-重みを §1.2 から読まず、P の自己同型 u↦u^c の分解の持ち上げから導出すること。導出できなければ `method:"paper_lemma"` と申告し `stable_elements` を名乗らない。p=691 走行と p≤23 較正走行は同一スクリプト・同一コード経路(差は p のみ)を `input_hash` で示す。

### 【軽微】
1. §2 予言 3「和 =」vs §4 assert「≤」不整合 — 等号の理由(1 次元 F_p[S₃]-加群は p∤6 ゆえ 1 か sgn の一方)を一行。
2. **F-GAP-7 は紙で閉**: |P| 素数 ⟹ TI 自動。N_G(P)=B のみ実質(cal-5 の裏取り対象から TI を外せる)。
3. LHS 退化(gcd(6,691)=1)の本文明示。
4. sign 成分=0 は 2 の可逆性(691 奇)— 一行。
5. (任意)規約分離テスト: Sym²(W) を det 非言及で構成 ⟹ Sym²W ≅ sl₂⊗det ⟹ 予言 (0,1) = 現行スイートに無い唯一の出力形 — det の向きを独立に釘付け。安価。

## D. 反証できなかった範囲
p=691 本番値未計算/系統 C は p≥5 一様の紙(691 実測でない)/LHS 退化は前回検算済みとして再検算せず/R-1 留保・段 2 以降はスコープ外/u/c 非接触・GAP 走行ゼロ。
