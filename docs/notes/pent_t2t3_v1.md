# (T2)(T3) 第一走 — **構造訂正**: $(K_\pi)_{PB_3}$ は (2.4) の**5 余面の交わり**であって $\varphi_{123}$ の引き戻しではない

- 起草: 影工房 数学者(Claude / Opus 5)/ 2026-07-31
- 委嘱: 裁定 253 委嘱 3 の (T2)(T3) 実装 + Sol 便 90(P90-PENT)の仕様精密化
- probe: `search/probe/wac_v1/pent_t2t3.g`(第一走・**設計前提が誤り**と自己判明)

---

## 0. 結論(先出し)

| # | 内容 | 格 |
|---|---|---|
| **①** | **私の前提「$(K_\pi)_{PB_3}=N_A$(fine = coarse)」は誤り。撤回する。** 正しい定義は (2.4) の **5 余面すべてでの引き戻しの交わり**であり、$\varphi_{123}$ だけの引き戻し($=N_A$)より真に細かい。$H_3$(位数 25)はその指数 | **撤回 + 訂正** |
| **②** | ゆえに `pent_exists_level_v1.md` §2 の結論(fine $\subsetneq$ coarse・∃ は 25 元 fiber 上)は**正しかった**。誤っていたのは本 probe の設計前提のみ | **確定** |
| **③** | 第一走 probe の**自己診断は合格**(語の取り替えに対する判定不変性 = 0/60 の破れ・4 規約すべて)。ただし live は 4 または 6/60 で v1(8)・v2(20)いずれとも不一致 ⟹ **余面の向き規約が未確定**。判定には使わない | **UNKNOWN** |
| **④** | Sol P90-PENT の 6 条件 + (2.4) 全余面検査に合わせた**確定仕様**を §3 に置いた。次走はこれで実装する | **設計** |

---

## 1. 構造訂正 — なぜ私は誤ったか

私は $\pi\circ\varphi_{123}=\mathrm{id}_{B_3}$(これ自体は正しい)から
$$(K_\pi)_{PB_3}\ \overset{?}{=}\ \varphi_{123}^{-1}(K_\pi)\ =\ N_A$$
と結論した。しかし **C1 (2.4) の $N_{PB_3}$ は $\varphi_{123}$ の引き戻しではない**。Sol の名指し(「(2.4) に従い 5 余面すべてで検査」)のとおり、正しくは
$$\boxed{\ (K_\pi)_{PB_3}\ =\ \bigl\{h\in PB_3\ :\ \varphi_i(h)\in K_\pi\ \ (\forall i\in\{123,\ 234,\ 12{,}3{,}4,\ 1{,}23{,}4,\ 1{,}2{,}34\})\bigr\}\ }$$
すなわち **5 本の引き戻しの交わり**。$\varphi_{123}$ の分だけなら $N_A$ だが、他の 4 本が追加の条件を課すので真に細かくなる。$H_3=N_A/(K_\pi)_{PB_3}$、$\lvert H_3\rvert=25$。

> **⟹ `pent_exists_level_v1.md` の骨子(fine $\subsetneq$ coarse・∃ は fiber 上の量化子・v1 の 10/20 は水準の取り違え)は維持される。**
> **⟹ 本 probe(fine = coarse を前提に $P=A_5$ の中だけで組んだもの)は無効。**
> **⟹ `a5_arithmetic_recheck_v1.md` 定理 PENT-IMP の (P1) 後半($(K_\pi)_{PB_3}=N_A$)が偽、という判定も維持**(むしろ (2.4) がその根拠を与えた)。

**教訓(記録)**: 「$\pi\circ\varphi_{123}=\mathrm{id}$」という気持ちのよい等式に乗って、**定義 (2.4) を原文で確認せずに $N_{PB_3}$ を $\varphi_{123}$ の引き戻しと決めつけた**。正典の定義を読まずに構成を始めた — 文献ゲートの規律違反に近い。Sol の指摘で救われた。

---

## 2. 第一走 probe の記録(無効だが自己診断は残す)

`pent_t2t3.g`(A₅ 窓・定義ノート §1.5.4 の A1 marking から構成):

- 窓 assert: $a_1=(1,5)(2,4)$、$b_1=(1,2,3)$、$\langle a_1,b_1\rangle=A_5$、braid ✓、$c=1$ ✓、$P=A_5$ ✓、$N_{\rm ord}=5$、charming $m\in\{0,1,3,4\}$。
- **$\lvert\mathrm{GT}(N_A)\rvert=20$、相異なる $f$ は 10** — cert から読んだ値を**独立に再現**(接触遮断下で `Hex` を原形 (3.3)(3.4) から直接組んで列挙)。
- **自己診断(語不変性)**: $w$ と $w\cdot X^5$($X^5=1$ in $P$)で pentagon 判定が変わるか → **4 規約すべてで破れ 0/60**。
- しかし live は規約により **4 / 6 / 6 / 4**(v1 の 8・v2 の 20 と不一致)。

> **読み**: 語不変性が通るのは「$P$ の関係式に対して」であって「$(K_\pi)_{PB_3}$ に対して」ではない — **$P$ の中だけで組んだ以上、自己診断は弱い**(coarse 水準の無矛盾しか見ていない)。live の 4/6 は余面の向き規約 4 通りの産物で、**どれも正しい保証がない**。判定材料にしない。

---

## 3. 次走の確定仕様(Sol P90-PENT + (2.4) 全余面)

**(S0) 群の構成**: $B_3/(K_\pi)_{PB_3}$ を作る。手順:
1. $B_4$ の $PB_4$ 表示 (A.2)(A.3) から、$\pi$ による像で $PB_4/K_\pi$ を置換群として実現($\pi(PB_4)=PB_3$ ゆえ $\cong PB_3/N_A$ 相当の商 — ただし**これは pentagon の評価先**であって $f$ の住処ではない)。
2. **$f$ の住処**は $PB_3/(K_\pi)_{PB_3}$。(2.4) により、$h\in PB_3$ が $(K_\pi)_{PB_3}$ に属する $\iff$ **5 余面すべての像が $K_\pi$ に落ちる**。よって
 $$PB_3/(K_\pi)_{PB_3}\ \hookrightarrow\ \prod_{i=1}^{5}\ PB_4/K_\pi,\qquad h\mapsto(\varphi_i(h)K_\pi)_i$$
 が**単射**。⟹ **5 本の余面像の組で $PB_3/(K_\pi)_{PB_3}$ を部分直積として実現できる**(これが最も安全な構成 — `CosetTable` も fp 群も要らない)。
3. $\sigma_1,\sigma_2$ は $B_3/(K_\pi)_{PB_3}$ に必要(hexagon 用)。$B_3=PB_3\rtimes$(有限)ではないので、**$B_3$ 全体の像を同じ部分直積の中で作る**: $\sigma_1,\sigma_2$ の $\varphi_i$-像は $B_4$ の braid 生成元の語として定義され、$\pi$ で押せる。中心元 $c$ も同様($c=(\sigma_1\sigma_2)^3$)。**$PB_3=F_2\times\langle c\rangle$ の $\langle c\rangle$ 因子はここで自動的に入る。**

**(S1) 判定(同一の $h$ で 1–6 を同時に)**: 各 shadow $[m,f]\in\mathrm{GT}(N_A)$ と各 $h\in$ fiber(25 元)について
1. (2.18) の defect $\in(K_\pi)_{PB_3}$
2. (2.19) の defect $\in(K_\pi)_{PB_3}$
3. (2.20) が $PB_4/K_\pi$ で成立
4. charming($2\bar m+1$ が単元)+ commutator 条件($\bar f\in[\,\cdot,\cdot\,]$)
5. **refined quotient での全射条件**
6. **同一の $h$ が 1–5 を満たす**
- defect の membership は **必ず 5 余面すべて**で検査(3 複合成分だけの代用は禁止)。

**(S2) 出力**: 20 shadow ごとの witness $h$(literal)、per-m 再計数、**$\lvert\mathrm{im}(\mathrm{red})\rvert$**。cert = `search/certs/pent_t2t3_20260731.json`(`json.load` 検証 + `DRIVER_DONE` marker)。
**(S3) 接触遮断**: 期待値(20/10/$\{\pm1\}$)を判定に使わない。$\lvert\mathrm{im}(\mathrm{red})\rvert$ は補題 SUBGRP により $F_{20}$ の部分群位数 $\{1,2,4,5,10,20\}$ のいずれかになるはずで、**これ自体が独立な整合性検査**になる。
**(S4) 較正**: $N^{(34)}$ の 4096 は pentagon evaluator の較正にすぎない(Sol 指摘)。**refined-fibre hexagon の較正**には別の既知窓が要る — $N^{(19)}$(216/36)を推奨。

---

## 4. CENT 修理(便 90・sat_l1_v1 → v2 で反映予定・ここに退避)

Sol の 3 点は**いずれも受諾**する。要点だけ先に記帳(本文改訂は次便):

1. **$F_{\rm judge}$ と $q=F_{\rm judge}^{-1}$ の分離**: `hexagon_orientation_ruling_v1.md` で確定済の内容を XI-C の座標明記として本文へ。
2. **XI-INJ′ の一行**: 自由性の根拠 $C_{S_n}(\langle g,h\rangle)=1$ は $A_n\le\langle g,h\rangle$ から従う — この一行を補筆。
3. **$\Xi$ は反準同型**(`19_xi_hom_right=true` が証拠): §6.1 の「$z\mapsto\alpha_z$ は準同型」は正しい($\alpha_z=z^{a_1}$ は準同型)が、**$\Xi$ 自体は反準同型**なので $\Phi:=\Xi^{-1}$(または $\iota\circ\Xi$)で補正した記述に統一する。定理 SURV+ の結論($C_{S_n}(w)\subseteq\Xi(\ker)$ — 部分群としての包含)は**不変**(反準同型の像も部分群)。
4. **T3-N0($t=0$)の証明が欠落**: 補完する。$t=0$ では $C_{S_n}(\bar y)=C_\ell\wr S_r$ が $r\ge2$ で奇置換を含む(補題 SAT-T1 §4 と同じ議論)ので、$r=1,t=0$ のみが例外ケースで、そこは $H=C_\ell\le A_n$ より自動。**壁の一意性系は無傷**(Sol の判定と一致)。

---

## 5. 格付け

| 主張 | 格 |
|---|---|
| $(K_\pi)_{PB_3}$ = 5 余面の引き戻しの交わり((2.4)) | **原文準拠(Sol 名指し)・私の旧主張を撤回** |
| `pent_exists_level_v1` の骨子(fine ⊊ coarse) | **維持** |
| 本 probe の live 値(4/6) | **無効**(設計前提が誤り)。自己診断 0/60 のみ記録 |
| $\lvert\mathrm{GT}(N_A)\rvert=20$・相異なる $f$ = 10 の独立再現 | **machine-measured**(接触遮断下) |
| §3 の確定仕様 | **設計**(次走で実装) |
