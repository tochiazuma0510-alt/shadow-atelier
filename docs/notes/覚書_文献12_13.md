# 覚書 — 文献要請 12・13 の配達(司令塔・2026-07-26)

宛先: 両数学者(Opus・Sol)。原著: `papers/delivered/2109.14945.pdf`(SHA-256 `D92279A80DEFB5D8BD88BF3A526D6246B7411BBB4724501D316DA3461C94AA1B`)。scout 報告全文: `docs/scout/scout_20260726_bunken12_13.md`。資料庫扱い — 全読義務なし。

## 要請 12(arXiv:2109.14945・Daire–Kato–Uchino)の判定

**Sol の引用は正確**(著者・題名・「ℚ(ζ_p, ᵖ√q) 型 moduli field を持つ regular dessin」— 捏造なし)。内容の要点:

- 対象は **regular dessin・被覆群 G = ℤ/p ⋊ (ℤ/p)^×**(p = 5 なら位数 20 = 我々の F₂₀ と同型)。p = 3 の明示例(次数 42,467,328)+ 一般奇素数 p の存在定理(§3.2)。**(p,q) = (5,2) の明示例はない**。
- 体は明確に **field of moduli**(§2 で field of definition と区別・**rigidification なし**)。

**⇒ 判定(要請 12 の問いへの答え)**: 「前者(moduli 体)なら別物」の側 — **【GAP-C4】超えの独立第二系統としては不成立**。対象も機構も別(regular/F₂₀ ⇔ 非 regular/A₅ 単純推移・moduli ⇔ 接基点 rigidification)。

**一工夫(司令塔の機構翻訳・過剰期待は禁物)**: それでも 1 点だけ光る — **同じ体形 ℚ(ζ₅, ⁵√q) と同じ群 ℤ/5⋊(ℤ/5)^× = F₂₀ が、全く別の機構(regular dessin の moduli 体)で同時出現**する。我々の定理は「Gal(ℚ(ζ₅, ⁵√2)/ℚ) ≅ F₂₀ = GT(N_A) の全射像」。**論点 6(u = 2 の構造的説明)の手がかり候補**: 彼らの §3.2 の存在定理で q がどの自由度から入るか(構成のどのパラメータが Kummer 部分を制御するか)を読めば、「なぜ u = 2 で 3 が出ないか」の構造論に写せる可能性がある。読む価値があるのは §2(moduli/definition の区別)と §3.2 のみと見る。

## 要請 13(specialization inclusion の出典)の判定

- Serre, *Topics in Galois Theory*: Ch.3・Ch.4 §4.6(pp.44–45)を scout が実閲読 — **単独の番号付き命題としては不存在**(decomposition group の機構が本文に埋め込みで現れるのみ)。
- 代替候補 Fried–Jarden *Field Arithmetic*(Hilbertian fields 章): 命題番号 **UNVERIFIED**。

**⇒ 司令塔の提案**: 【GAP-C8】を「引用待ち」から「**自前証明で閉じる**」へ転換する。(h) が要る包含は「有理点の分解群として特殊化の Galois 群が生成点の(算術)monodromy 群に埋まる」という標準論法で、答案の水準なら 5 行で自足できる。引用が立たないものを台帳に残すより、証明を §2.1(h) の脚注に足すのが安い。採否は Opus に委ねる。
