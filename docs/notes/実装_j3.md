# 実装ノート — E2 class-6 j=3 ゲート(2026-07-26・implementer)

前提: `docs/manifest_e2c6j3_v1.md`(発射保留・事前登録)。発注: 司令塔。
入力仕様として読取許可されたのは `sol/sol_reply_24_d2.md` **§F8 のみ**(§F7 は封印 — 実装者は未読)。

**改版履歴**: 初版(第一便)は「§F8 を逐語実装した」と申告したが、falsifier 監査
(`docs/notes/反証前哨_j3.md`)により**過大申告**と指摘された(§F8 の 6 項目中 3・4 のみ実装、
1・2・5・6 は未実装。加えて司令塔発注の中心的懸念である「前提破れ時の停止ガード」が
実走査コードパスに存在しなかった)。**本版は司令塔指示に従い、スコープ申告を
「実装済み項目の列挙形」に切り替える**(「逐語実装した」等の総称的主張はしない)。

## 実装済み項目(列挙形)

### GAP側(`search/e2c6j3-sweep.g`)

- 線型段(2 ブロック・30×15)を `BuildSnfData`/`TestAtJ`(`search/e2c6-common-data.g`、j 一般)
  で j=3(mod 8)に適用。
- `WMfun(m) = C(m+1,2) mod 2`、`LambdaOfK(k,m) = (k_p, k_s3+W(m)k_r2) mod 2`(便24 eq 8.3)。
- `QFirstCond(f) = (f_p, f_s3+f_w*f_r2) mod 2`(便24 eq 8.1 の実値、非線型のまま厳密評価)。
- `k_w ≡ 0 (mod 8)` 前提(便24 eq 8.2)の**検証**: safe な合成系(rhs=0、m=0,1,2,3,5,7,11)
  26 生成元で実測 PASS。
- **`CheckPremiseKw` ガード関数**: 生成元列を受け取り k_w≡0(mod8)を再検証、破れれば
  違反生成元を返す。**`RunRealSweepC6J3` は各 m でこれを呼び、違反時は
  `"claim":"precondition_violated_c6j3"` 証明書(`ob_mode:null`)を書いて当該 m の
  λ 適用を ABORT する**(黙って進まない)。この ABORT パス自体は、意図的に破った偽の
  生成元(k_w=1 を注入)を用いた GUARD fixture で検出動作を確認済み(実 64 系走査は
  fire lock により未実行のため、ガードの「本番での発火」自体は今回も未確認 — 検出ロジックの
  単体動作確認に留まる、と正直に記録する)。
- **重複度表の二系統**(λ-shortcut と brute-force)を全 fixture 系統で構築・完全一致を確認。
  λ-shortcut の出力には **2×d bit matrix(`lambda_bit_matrix`)と GF(2) rank
  (`lambda_rank`)を証明書に格納**(便24 F8 item 2)。
- **不可解系の dual witness**(便24 F8 item 1): `RunRealSweepC6J3` の不可解分岐で
  `y := U[failRow]; yM := y*rows; yb := y*rhs` を計算し証明書に格納(j=2 の
  `WriteUnsolvableCertC6` と同型)。この経路は、意図的に不可解な safe 合成系
  (`BuildLinearSystemC6AdversarialUnsolvableJ3` — 疑似乱数 rhs、実 Ē_m 不使用)で
  実際に発火させ、`yM≡0`・`yb≢0` (mod 8) を確認(fixture G1b)。
- **`ObFromQPair` の R=4 実演習**(司令塔発注 item3、便24 F8 とは別件): nonzero q_N の
  3 恒久ケース(F6 相当)を R=4 で実行(fixture G7)。結果: **ob mod 2 は q_N に依らず一定**
  (3 ケースとも確認)。**raw な R=4 座標(mod 2 を超えた a の意味論)は未解決のまま**
  (e2c6-sweep.g 自身の GAP-OB1 注記を踏襲 — 独自の公式は発明していない)。
- **周期性 fixture**(便24 F8 item 6、m/m+32、(3,35)(5,37)(21,53)(27,59)): `EmBar15 mod8`・
  `EmC6 mod4`・`W(m)` という**公開の Ē_m 公式データのみ**を両側で計算し証明書化。
  **一致・不一致そのものは pass/fail 判定に使わず「observed_equal」として記録のみ**
  (司令塔指示通り)。実測ではこの 4 組すべてで一致が観測されたが、これは fixture の
  合否条件ではない(観測事項として記帳)。
- **項目5(中心補正×8・直接群積 M8 との mass 照合)は未実装**。理由: 委嘱16 の語彙で
  「M8」は「群の積から θ(f)f と E_m N(f) を再計算して直接 =1 を確認する」検査を指し、
  これは fixture G3(route-G)がすでに closed-form との mod4 一致という形で部分的に
  カバーしているが、「中心補正 |kerΛ|=2^{e+1}=8 を掛けた full mass」という central
  extension の具体的な構成(kerΛ のリフト・8 個のファイバー元の直接列挙)は本パスでは
  設計されていない。**これは implementer の判断で自由に設計してよい範囲を超える
  (数学的構成の選択)と判断し、実装せずここに保留として明記する**。次便で司令塔/数学者に
  具体的な構成(central extension の生成元・関係式)の指定を仰ぎたい。

### 発射錠・ドリフト検査

- **発射錠の誤ハッシュ試験**: 誤ハッシュを書いた `search/FIRE_e2c6j3.auth` を実際に置いて
  `.\gap.ps1 search\e2c6j3-sweep.g` を実行 → `FIRE_e2c6j3.auth present but hash MISMATCH`
  → `[LOCKED]` を確認(壁時計 1 分以内、前回監査のタイムアウトは再現せず・原因は
  環境要因と推定、コード側の問題ではないと判断)。試験用ファイルは削除・現在不存在を確認。
- **コピードリフト検査の自動化**(`crosscheck/check-e2c6-common-data-drift.mjs` 新設):
  `search/e2c6-common-data.g` と `search/e2c6-sweep.g` の7ブロック(`BASIS21`・
  `ThetaTable21`・`SigmaTablePoly21`・`EmComponents21`・`KappaTerms`・`DThetaFormula`・
  `DSigmaFormula`)をブロック名+括弧深度で自動抽出しバイト比較。**全7ブロック
  byte-identical(ALL PASS)**。falsifier の手作業確認を恒久 fixture 化した。

### Node側(`crosscheck/check-e2c6j3.mjs`)

- `m6j3_multiplicity_table`: brute-force のみで独立再構築(λ-shortcut を信頼しない)+
  `lambda_bit_matrix`/`lambda_rank` の独立再計算・突合を追加。
- `linear_stage_empty_c6j3`(`fixture:"adversarial_unsolvable_synthetic_j3"`): 公開の
  theta_bar/sigma_bar 構造 + 同一の疑似乱数 rhs 式を独立に再構築し dual witness
  (`yM`,`yb`)を再計算・突合(実走査分は「未実行のため recheck 対象外」と明示、
  SKIP ではなく理由つきで区別)。
- `ob_synthetic_check`(G7): ObFromQPair(R=4) を独立再計算・突合。
- `periodicity_comparison_j3`(G8): EmBar15/EmC6/W を独立再計算し、証明書の
  `observed_equal` 自己申告が実際の計算と一致するかのみ検査(等号の当否は判定しない)。
- `precondition_violated_c6j3`: 構造的整形式検査のみ(本パスでは未発生 — 実走査は
  fire lock により未実行のため)。

## fixture 一覧(全 PASS、GAP・node 両系統、証明書13件)

precondition(k_w=0)・GUARD(注入違反検出)・G1(偽陽性検出器)・G1b(dual witness)・
G2(class-5統制+j=2射影一致)・G3(route-G, mod8還元)・G4(M系, R=4)・G7(ObFromQPair R=4
nonzero qN)・G8(周期性比較, 観測のみ)。

## 実行結果(原文)

```
.\gap.ps1 search\e2c6j3-sweep.g
[PASS] (inherited) theta^2 = id on all 21 BASIS21 generators
[PASS] (inherited) d_theta_formula matches theta_table C-columns
[PASS] (inherited) d_sigma_formula matches sigma_table_poly C-columns (m=0,7)
[PASS] k_w = 0 (mod 8) for all sampled K_m^(3) generators (26 generators checked, safe synthetic systems)
[PASS] GUARD: CheckPremiseKw correctly passes genuine safe kgens AND detects an injected k_w<>0 violation
[PASS] G1: sum(multiplicity table)=|L| AND lambda-shortcut matches independent brute-force enumeration exactly
[PASS] G1b: dual-witness path exercised on an unsolvable safe synthetic system (label=1), yM=0 and yb<>0 mod modulus independently verified
[PASS] G2a class-5 control: linear stage solvable at j=3 (mod 8) for ALL m=0..63
[PASS] G2b: ObFromQPair(R=2) byte-agrees with the frozen j=2 gate's own ratified F1/F2/F6 fixture outputs
[PASS] G2 (class-5/j=2 統制, combined)
[PASS] route-G PcpGroup collector IsConfluent (independently rebuilt from KappaTerms)
[PASS] G3: route-G matches closed-form q_theta/q_N mod 4 on mod-8-reduced Abar vectors, 45 evaluations (exact 45/45)
[PASS] G4: (1+theta)ker(N_C) at R=4 -- u4-component lies in R[2]={0,2}
[PASS] G4: (1+theta)ker(N_C) at R=4 -- u2 mod 2 = 0, t5=t6, u1=u3
[PASS] G4 (M-series, mod-4/R=4, combined)
[PASS] G7: ObFromQPair(R=4) exercised with NONZERO q_N -- ob mod 2 independent of q_N in all cases
[PASS] G8: periodicity comparison certificates generated for all 4 m/m+32 pairs
[LOCKED] real-universe sweep requires FIRE_e2c6j3.auth (commander issues at fire time)
total elapsed ms: 953
```

```
node crosscheck/check-e2c6j3.mjs
certificate crosscheck: certificates/e2c6j3/*.json (13 files)
[13件すべて PASS -- m6j3_multiplicity_table x4(lambda_bit_matrix/rank recheck込み)・
 linear_stage_empty_c6j3(G1b dual witness)・f7_routeG_crosscheck・ob_synthetic_check x3(G7)・
 periodicity_comparison_j3 x4(G8)]
ALL PASS (self-check fails=0, cert fails=0)
```

```
node crosscheck/check-e2c6-common-data-drift.mjs
PASS  BASIS21 / ThetaTable21 / SigmaTablePoly21 / EmComponents21 / KappaTerms /
      DThetaFormula / DSigmaFormula -- 全7ブロック byte-identical
ALL PASS (7 blocks checked)
```

```
(発射錠の誤ハッシュ試験、原文抜粋)
=== FIRE LOCK CHECK (j=3) ===
  FIRE_e2c6j3.auth present but hash MISMATCH (expected aea71b16..., got deadbeef00...) -- treating as LOCKED
[LOCKED] real-universe sweep requires FIRE_e2c6j3.auth (commander issues at fire time)
[PASS] fire lock CLOSED (real sweep NOT run this pass)
total elapsed ms: 922
(試験用 search/FIRE_e2c6j3.auth は直後に削除・不存在を確認)
```

## 実現ギャップ・スコープの正直な申告(更新版)

- **64 系本走査は未実行**。`search/FIRE_e2c6j3.auth` は作成していない(現在不存在を確認)。
- k_w≡0(mod8)前提: safe な合成系でのみ検証。ガード自体は注入違反での単体動作確認のみ
  (実走査での「本番発火」は fire lock 解禁後でないと確認できない)。
- **便24 F8 item 5(中心補正×8・直接群積 M8 との mass 照合)は未実装**(上記理由により
  保留、次便で設計指定を仰ぐ)。
- G7 は ObFromQPair(R=4) を実際に nonzero q_N で呼ぶ演習であり、**raw な ob_a の R=4
  座標意味論(R[2] 云々)そのものを解決したわけではない**(mod 2 の一致のみ確認)。
- G8 の周期性は「証明書生成+比較可能」の形にとどめ、成立/不成立を fixture の合否には
  していない(司令塔指示通り)。
- `search/e2c6-common-data.g` は `search/e2c6-sweep.g` の該当部分の逐語コピー
  (DRY 違反だが確定済みファイル不変を優先。ドリフトは自動検査で恒久確認済み)。

## 追記(2026-07-26 続報・便24 F8 item 5 実装 — 設計_F8項目5.md 対応)

司令塔から F8 項目5(中心補正 ×8・直接群積 M8 との mass 照合)の設計指定
(`docs/notes/設計_F8項目5.md`、Opus 起草)が届いた。実装した内容を列挙する。

### 実装済み(M8 系列、全 PASS)

- `LambdaOnC(z6,m)`(S1)・`LambdaTable(m)`(S2、(Z/4)^6 悉皆・SNF不使用)・`EmbC(z6)`・
  `G3RouteGQThetaFull21`/`G3RouteGQNFull21`(21成分フル、既存 G3CExtract は C 成分のみのため
  流用不可 — 設計指定通り新関数を書いた)・`IsIdentityInCellAj`(Abar成分 mod8=0 かつ C成分
  mod4=0 の「セル A^(j) での=1」判定、新関数)。
- **M8-a(torsor 律・必須)**: m∈{0,1,2,3}、12 z ベクトル×4m=48 検査。**符号は実測で決定**
  (+ で全一致、− は不一致 — 勝手に決め打ちせず S5 の指示通り実測)。**PASS**。
- **M8-b(ファイバー実現・必須)**: ob=0-quotient(下記参照)の証人ごとに、その証人**固有**の
  Λ-ファイバー(kerΛ ではなく、証人の Ξ に応じた `-Ξ(g)` のコセット)を直接群積で検査。
  **自己発見バグ**: 初回実装は全証人に一律 kerΛ を使っていた(Ξ(g)=0 の証人にしか正しくない) —
  修正後 **PASS**。
- **M8-c(mass 恒等式・必須・本体)**: `Σ|fib| = |kerΛ|・mult(ob=0)` かつ集合一致。**PASS**
  (m=0: |kerΛ|=8, mult=256, Σ|fib|=2048=8×256, 不一致0件)。
- **M8-d(負の統制・必須)**: ob≠0 の証人でファイバーが空・kerΛ の8元すべてが直接群積で
  「=1」に**ならない**ことを確認。**PASS**(空虚な検査でないことの実測)。

### 数学的発見(自己発見・設計を裏付け)

- **委嘱16 の Ob ≅ R[2]a⊕(R/2R)b̄ 意味論を実測で確認**: raw な `ObFromQPair` の出力
  (ob_a, ob_b) は、真の Ob=4元群のクラスとしては **ob_b を mod 2 で読む**必要がある
  (m=0 において (0,0) と (0,2) は同じ真のクラス — 実際どちらも Λ-ファイバーが非空)。
  この量子化正しい分類 `IsObZeroQuotient(ob) := (ob_a=0) ∧ (ob_b mod 2=0)` を使うと
  M8-c の mass 恒等式・集合一致が**厳密に**成立する。これは G7 で「未解決」と記録した
  R=4 座標意味論の**一部**(b の mod2 部分)を、この M8 系列が独立に裏付けた形になる
  (a 側の R[2] 意味論はこのデータセットでは a=2 の実例が出なかったため未確認のまま)。
- **M8-b/c/d は m=0 のみが盲検安全にテスト可能**(設計上の欠陥ではなく、テスト可能性の
  スコープ限界): 直接検証した — 合成系(rhs=0)でも F5 型(疑似乱数だが自己無矛盾な非零
  target)でも、m>0 では `q_N(f,m)` が σ-不変性(j=2 ゲート既知の M2 前提と同型)を満たさず、
  Λ-ファイバー判定がことごとく空になる(m=1,2,3 で総当たり検査、mismatch=全点)。
  これは「−Ξ(g)∈im(Λ) ⟺ ob=0」という等式自体が、**f が実際に本物の Ē_m(m) に対する
  アフィン解であることを前提とする**ためで、実 m>0 のデータなしには検証できない
  (m=0 は Ē_m(0)=0 恒等的に成り立つため、合成系がそのまま実系と一致する唯一の点)。
  司令塔の罠注意(「相補に出ても符号反転で PASS にするな」)を踏まえ、**この限界は
  「設計の破れ」ではなく「盲検制約下でのテスト可能性の限界」として区別して報告する**
  (M8-a は前提を要さない恒等式なので m∈{0,1,2,3} 全てで妥当に PASS した対比が根拠)。

### 証明書・照合

- `certificates/e2c6j3/fixture_M8c_massidentity_m0.json`(f0/K_generators/K_orders 込み、
  node が完全独立に L_m 再列挙+LambdaTable 再構築できる形)。
- `crosscheck/check-e2c6j3.mjs` に `m8c_mass_identity` の完全独立再検算を追加
  (LambdaTable を own 実装で再構築・ob を quotient 分類で再判定)。**PASS**。
- 全14証明書(既存9件+M8c 1件、G1b/G3/G7×3/G8×4/M8c)GAP・node 両系統 **ALL PASS**。

### 実行結果(原文抜粋)

```
[PASS] M8-a: torsor law ... sign determined empirically = "+"
[PASS] M8-b: fiber realization -- ... (64 group-product evaluations)
[PASS] M8-c: mass identity Sum(|fib|)=|ker Lambda|*mult(ob=0-quotient) AND set-match, m=0
[PASS] M8-d: negative control -- ...
[PASS] M8 SERIES combined
```

```
node crosscheck/check-e2c6j3.mjs
PASS  fixture_M8c_massidentity_m0.json: m8c_mass_identity (m=0, |L|=512) independently
      rebuilt LambdaTable + re-enumerated L_m -- ker_lambda_size=8 mult_ob0_quotient=256
      sum_fib=2048 set_mismatch=0 (cert claimed same)
ALL PASS (self-check fails=0, cert fails=0)
```

### 未確認・保留(正直な申告)

- M8-b/c/d の m>0 実測は依然として未実施(fire lock により本走査自体が未実行なのに加え、
  上記の通り盲検安全な代替系では原理的にテストできない — fire 解禁後、実 m データで
  初めて確認できる)。
- ob_a=2(R[2] の非自明元)側の実例はこのデータセットには一度も出現しなかったため、
  「a 側も R[2] の群構造として正しく振る舞うか」は M8 系列では未確認のまま。

## commit

未実施(発注に従い commit 禁止)。`search/FIRE_e2c6j3.auth` は作成していない。
