# PB₄ 線 初動①(窓の存在判定)— **【裁定 370 で (α) 確定・本稿の警報は取り下げ】**

> ## ★ ERRATUM(2026-08-01・裁定 370 = falsifier 非当事者判読を受けて)
>
> **本稿 §0-3 が提起した (β)(「窓が $PB_4$ 由来でない」)は撤回する。裁定 370 により (α)(等価な別規約)で確定した。**
> **effective source = 読解ノート v1.1 + 裁定 370**(本稿は経緯として残す)。
>
> **撤回する 2 つの根拠**:
> 1. **「$E$ で $c\ne1$(位数 5)なのに $c\mapsto1$ としている」は誤認。** $c=(s_1s_2)^3$ は $E$ で**位数 1**($c\in\ker\pi$)。よって $c\mapsto1$ は恣意的な正規化ではなく **$\pi$ の帰結**である。$N_{\rm ord}=\mathrm{Lcm}(5,5,1)=5$ は $\bar x,\bar y$ の位数だけから出ていた。私の読み違い。
> 2. **「正典 $x_{ij}$ では $\lvert Q_P\rvert$ が 7500 → 60 に潰れる」(事実 8)は私の対照実験のバグ。** `search/probe/wac_v1/gtpi_pb4_gate_stdwindow_20260801.g` **L35** で、**fwd 規約の生成元**を **rev 規約の行順**に流し込んだ(混成)。裁定 370 の指摘どおり、この組合せでは (A.5) の二表示が 5 成分すべてで不一致になり、**そもそも well-defined ですらない**。「60」は窓の性質ではなく私のバグの出力である。
>
> **確定した事実**(裁定 370): probe の窓 6 元はすべて $\rho=\pi\circ\mathrm{Rev}$ の像であり、`X13w` は正典 (A.5) の $x_{12}^{-1}c\,x_{23}^{-1}$ そのもの。部分群 literal 一致 + 核厳密一致。$\lvert Q_P\rvert=7500$ は 360 通り中 1 通り(分離あり)。
> ⟹ **$C_5^3$ 精密化は正典由来・$K_\pi$ の名札は正しい・定理 GTPI の解釈は生存。**
>
> **生存する本稿の内容**(取り下げない部分): §1 事実 1–4・9(延長 $\sigma_3\mapsto s_1$ が $E$ 内で一意・$\langle X_{ij}\rangle=A_5$)、および §2 の (2.4)/(A.16)/pentagon(行 906)の所在。**窓は存在する**という手 1 の主目的の結論は変わらない。
> **教訓(裁定 370 の教材)**: 対照実験は本実験と同じ規約で組まねばならない。**規約混成は本実験だけでなく対照実験でも起こる** — f/f⁻¹ 族の 5 件目は、私自身の反証実験の中にあった。⟹ 手 2 の凍結に **well-definedness 検査の同梱**((A.5) 二表示一致 assert)を必須条件として繰り込む。

---

## 【以下は 2026-08-01 の当初稿。裁定 370 前の状態として保存】

# PB₄ 線 初動①(窓の存在判定)— 停止条件に該当(当初稿)

- 起草: 影工房 数学者(Claude / Opus 5)/ 2026-08-01 · 司令塔 GO(初動 3 手)の**手 1** の報告
- probe: `search/probe/wac_v1/gtpi_pb4_window_gate_20260801.g` / `..._gate_followup_20260801.g` / `..._gate_stdwindow_20260801.g`(いずれも窓ブロック逐語移植・単系統)
- **手 2(CV-9 凍結 ^{PB₄})・手 3(Prop 3.3 実測)には進んでいない。** 停止条件「延びなければ即停止・報告」の変種に該当したため。

---

## 0. 結論(3 行)

1. **$B_3\to E$ は $B_4$ へ延びる。しかも延長は $E$ の中で一意**($\sigma_3\mapsto s_1$)。**窓は存在する** — $N:=\ker(\pi:PB_4\to A_5)\in\mathrm{NFI}_{PB_4}(B_4)$($\pi$ が $B_4$ 全体に延びるので $B_4$-正規性は自動)。
2. **しかし probe の $X_{13},X_{24}$ は、この延長のもとでの正典生成元 $x_{13}:=\sigma_2\sigma_1^2\sigma_2^{-1}$(2008 (A.5)・行 3682)と一致しない。**
3. **差は装飾ではない**: 正典 $x_{ij}$ で同じ 5 重構成をやり直すと $\lvert PB_3/N_{PB_3}\rvert$ が **7500 → 60** に潰れ、$C_5^3$ の精密化が消える。**すなわち $K_\pi$ と $N_A$ の区別そのもの(= P6-1 の主題全体)が、この 1 つの割り当てに依存している。**

---

## 1. 機械で確定した事実

| # | 事実 | 値 |
|---|---|---|
| 1 | $s_1s_2s_1=s_2s_1s_2$ | true |
| 2 | $\{t\in E:\ s_2ts_2=ts_2t,\ s_1t=ts_1\}$ | **ちょうど 1 個**、$t=s_1$(位数 10)、$\langle s_1,s_2,t\rangle=E$(360) |
| 3 | $A_{34}=t^2$ vs probe $X_{34}$ | **一致** |
| 4 | $A_{14}=ts_2s_1^2s_2^{-1}t^{-1}$ vs probe $X_{14}$ | **一致** |
| 5 | $A_{13}=s_2s_1^2s_2^{-1}=(1,5,3,4,2)$ vs probe $X_{13}=y^{-1}x^{-1}=(1,3,2,4,5)$ | **不一致** |
| 6 | $A_{24}=ts_2^2t^{-1}=(1,3,2,4,5)$ vs probe $X_{24}=s_1^{-1}ys_1=(1,5,3,4,2)$ | **不一致**(しかも 5 と**値が入れ替わっている**) |
| 7 | 5 重構成を **probe の $X_{ij}$** で | $\lvert Q_P\rvert=7500$、$\lvert[Q_P,Q_P]\rvert=60\ (A_5)$、$\lvert Z\rvert=125$ |
| 8 | 5 重構成を **正典 $x_{ij}$**(延長 $t=s_1$)で | **$\lvert Q_P\rvert=60$**、$\lvert[Q_P,Q_P]\rvert=60\ (A_5)$、**$\lvert Z\rvert=1$** |
| 9 | $\langle X_{ij}\rangle$ | 60 = $P_N$ |

**5 と 6 の値が入れ替わっている**のは、$f/f^{-1}$ 族の指紋(共役の向き)である: probe は $X_{24}:=s_1^{-1}y\,s_1$、正典 $A_{24}=s_1y\,s_1^{-1}$。

---

## 2. 正典側の該当条項(読んだ範囲の申告つき)

- **2008 (A.5)・行 3678–3682**: $x_{12}:=\sigma_1^2$、$x_{23}:=\sigma_2^2$、**$x_{13}:=\sigma_2\sigma_1^2\sigma_2^{-1}$**。
- **同 行 1187**: $x_{13}:=\sigma_1^{-1}\sigma_2^2\sigma_1=\sigma_1^{-1}x_{23}\sigma_1$(組紐関係により上と同値)。
- **2008 (2.4)・行 591**: $N_{PB_3}:=\phi_{123}^{-1}(N)\cap\phi_{12,3,4}^{-1}(N)\cap\phi_{1,23,4}^{-1}(N)\cap\phi_{1,2,34}^{-1}(N)\cap\phi_{234}^{-1}(N)$ — **5 本の引き戻しの交叉**。
- **同 行 906**: pentagon $=\ \phi_{234}(f)\,\phi_{1,23,4}(f)\,\phi_{123}(f)\,N=\phi_{1,2,34}(f)\,\phi_{12,3,4}(f)\,N$。

**probe の $c\mapsto1$**: probe は $X_{13}:=y^{-1}x^{-1}$ と置き、その結果 $\mathrm{cofc}[1]=x_{23}x_{13}x_{12}=1$ — すなわち**第 1 成分で $c$ を殺している**。$E$ の中で $c=(s_1s_2)^3\ne1$(位数 5)なので、これは正典 $\phi_{123}$(=素直な包含・$c\mapsto c$)**ではない**。

**★ 前便の訂正**: 追補 `gtpi_v1_addendum_upb4.md` で「cof[1..5] = 正典 5 写像」と述べたが、**その機械照合は同語反復だった**(比較対象を同じ式で書いていた)。cof 行と $\phi_\bullet$ の形の一致は**私の読み**であって機械確認ではない。しかも上記のとおり cof[1] は $c$ の扱いで $\phi_{123}$ と異なる。**訂正して謝罪する。**

---

## 3. 分岐(司令塔判断を要する)

| 読み | 内容 | 帰結 |
|---|---|---|
| **(α)** probe の割り当てが、正典の別の(等価な)規約のもとで正しい | 例: 反準同型 $\Psi$ に合わせて生成元側も反転させている / 別の $x_{13}$ 正規化 | $K_\pi$ は正真の窓。**P6-1 の解釈はそのまま**。PB₄ 線は手 2 へ進める |
| **(β)** probe の割り当ては $PB_4$ 窓から誘導されていない | 正典 $x_{ij}$ では $\lvert Q_P\rvert=60$ に潰れる(事実 8) | **$C_5^3$ 精密化が構成の副産物**であり、$K_\pi$ vs $N_A$ の区別が失われる。規約事故の **5 件目・かつ最深部** |

**重要な限定**: **どちらに転んでも 定理 GTPI 自体は倒れない。** GTPI は「凍結された $Q_P$ 上で凍結された `Chk6` が定める $\mathcal G$」についての定理であり、$Q_P$ が何を意味するかに依存しない。倒れうるのは**解釈**(「$\mathcal G=\mathrm{GT}(K_\pi)$、$K_\pi$ は正真の窓」)のみである。P6-1 の cert・凍結・紙はすべてそのまま有効。

**私の見立て**: (α) の可能性は十分ある。probe の $X_{13}=x_{23}^{-1}x_{12}^{-1}$ は「$c=x_{12}x_{13}x_{23}$ を $c\mapsto1$ で解いた式の**積順序を反転したもの**」であり、$\Psi$ が反準同型であることと整合する形をしている。**ただし $c\ne1$ なのに $c\mapsto1$ とする点は別問題**で、これは正規化の選択として正典に根拠が要る。

## 4. 進言

1. **手 2・手 3 を保留**し、まず (α)/(β) を裁定する。判定に必要なのは 2008 の $x_{13}$ 正規化と $\phi_{123}$ における $c$ の扱い(App A.5 前後 + (A.16)/(A.18) の明示式)の**逐条読解 1 本**。reader 係の仕事として軽い。
2. **この判読は非当事者に出すのが正しい**(CV-9-1.3.3)。私は $Q_P$ を前提に GTPI を書いた当事者である。
3. **便 98 への影響**: 監査点 A の分割(charming 閉鎖 / pentagon UNKNOWN)は維持だが、**「窓の由来」という新しい監査点 F** を足すべき。Sol にはこの gate 結果を原文で出すのが最も速い。
4. 付帯 (a)(`c5` の混成向き)の erratum は本件と独立に有効 — 別窓へ移す前に修理。

**格**: 単系統 GAP + 正典の逐語引用 2 箇所。**cross-checked ではない。** 停止して判断を待つ。
