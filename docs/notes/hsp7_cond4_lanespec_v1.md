# HS 発火条件 4 — helper 非共有三レーンの発注設計書(v1)

- 起草: 実装担当(短命)・2026-08-04
- 委嘱: 司令塔(裁定 435・「発火条件 4 の発注設計書を起草せよ・実装せず設計書のみ」)
- 正本参照: `sol/sol_reply_101_math28.md` P101-1(条件 1〜6)/ `docs/notes/hs_prop7_translation_v1.md` §8.6(P100-1.2 発火前チェックリスト・条件 4)/ §5.3 項目 2(探索と検証の helper 非共有)/ §8.7(定義 NW(p)・NW-P1〜P8・S-1〜S-7′)
- 本稿は**発注設計書**。実装・実行はしない。完成後の実装は司令塔が別係(実装担当の別インスタンス、またはミラーレーンごとに複数)へ発注する。
- 本稿が参照する「今回の走(2026-08-04)」= `search/certs/hsp7_cond2_p7_20260804.json` と `search/probe/hsp7_gap_v1/stage{1,2,3,4}*.g`(発火条件 2・裁定 435 検収済み)。

---

## 0. 位置づけ(発火条件 4 とは何か)

`hs_prop7_translation_v1.md` §8.6 の HS 本走・発火前チェックリスト(5 条):

| # | 条件 | 状態(本稿執筆時点) |
|---|---|---|
| 1 | NW-1 を省略記号なしの verbal subgroup として一意定義 | ★ 履行済(§8.7・紙) |
| 2 | p=7 の構造確認(NW-P3/NW-P5 の標的商での直接確認) | ★ 履行済(裁定 435・`hsp7_cond2_p7_20260804.json`) |
| **3** | P100-1.1 の m=0 finite dummy family を exact group element として構成し、hexagon/charming/SURJ/PENT を**別々に**判定する | 構成は §8.7.4 で履行済(紙)。**4 判定の分離実装**は本稿のレーン割当対象 |
| **4** | 探索レーン・full B₃/N hexagon レーン・K(0,5)/W PENT レーンを **helper 非共有**にする | **本稿の主題** |
| 5 | CV-9 判読まで D3/D4 を cross-checked と呼ばない | 遵守中(不変) |

条件 4 が閉じて初めて、条件 3(dummy family の分離判定)と NW-P6/P7/P8(次段較正走)を**独立三系統**の上で走らせられる。本稿はこの三レーンの**分割仕様**を決めるものであり、値の計算そのものは行わない。

---

## 1. 三レーンの定義

### Lane S(探索・Search)

- **入力**: 窓定義 NW(7)(`docs/notes/hs_prop7_translation_v1.md` §8.7.3 定義 NW(p) を p=7 で固定)。exact pc presentation of P = F₂/(γ₅(F₂)F₂⁷)(発火条件2 の stage1 相当を独自実装で再構築 — 継承しない、§3 参照)。
- **やること**: 簡約 hexagon **(3.10)(3.11)** だけを使って、P 内の charming candidate `(m, f̄)`(m ∈ 𝒳_N, f̄ ∈ [P,P])を**悉皆列挙**する。これは P100-1.2 条件 3 の「hexagon 判定」を担う。
- **出力**: GT-shadow 候補集合 `{(m, f̄)}` の cert(件数・列挙全リストまたはハッシュ・使用した簡約 hexagon の式の逐語)。
- **やらないこと**: full B₃/N 上の (3.3)(3.4) は評価しない。K(0,5)/W にも触れない。

### Lane V(検証・Verify)

- **入力**: 窓定義 NW(7)(Lane S と同じ紙の定義から独自に再構築 — NW-1a/NW-1b の証明を辿って B₃/N を**自前で**組む。Lane S の出力にある P の GAP オブジェクトそのものを import しない)。Lane S の出力候補リスト(**値だけ**を受け取る — `(m, f̄)` の組の集合、GAP オブジェクトや中間表現は受け取らない)。
- **やること**: **full B₃/N** 上で **(3.3)(3.4)** を評価し、Lane S の各候補が真に GT(N) の元であることを独立に再判定する。これが「探索と検証の helper 非共有」(定義ゲート項目 2)の検証半分。
- **出力**: 各候補について PASS/FAIL の再判定結果。Lane S との**不一致件数**(0 が期待値 — 不一致は品質問題であり S-9 停止の対象、§5)。
- **やらないこと**: K(0,5)/W には触れない。Lane S の中間コード(hexagon 判定関数の実装そのもの)を読まない。

### Lane P(pentagon・PENT)

- **入力**: 窓定義 NW(7)。Lane S(または Lane V — どちらの通過集合を使うかは司令塔が cert 突合時に指定。**両方に対して独立に走らせるのが望ましい**、§4)の候補**値**リストのみ。
- **やること**: K(0,5)/W(W = γ₅(K(0,5))K(0,5)⁷)上で `PENT_W([m, f̄]) ⟺ ρ⁴(f̄)ρ³(f̄)ρ²(f̄)ρ(f̄)f̄ = 1` を判定する。K(0,5) の構成(B₄ Artin 表示 → PB₄ の RS → Δ₄² での商 → sphere row-product 辞書 → ρ)は**発火条件 2 で既に構築・fail-closed アンカー全 PASS 済みの資産**(`search/probe/hsp7_gap_v1/stage1_pb4.g`, `stage2_k05.g`)であり、**Lane P だけがこれを継承してよい**(§3)。
- **出力**: 各候補の PENT PASS/FAIL、NW-P6(h₄ᵗ family の t=0..6 個別判定)・NW-P7(p=5 control)・NW-P8(N vs N₀ の c 依存性)は**すべてこのレーンの担当**(§5)。
- **やらないこと**: hexagon 判定はしない(受け取った候補集合を前提として扱う)。B₃/N 側の (3.3)(3.4) を再評価しない。

### interface 図(データフローのみ・実装は含まない)

```
        NW(7) の紙の定義(§8.7)
           |         |         |
      (各レーンが独自に P/B3N/K05W を実装)
           |         |         |
       [Lane S]  [Lane V]  [Lane P]
           |         |         |
      candidates  --(値のみ)-->  Lane V
           |                     |
      candidates  --(値のみ)----------------> Lane P
                                               |
                                        PENT 判定 + NW-P6/7/8
                                               |
                                     司令塔突合(3 cert を並べて手で比較)
```

Lane 間を流れるのは**候補の値(m の整数値・f̄ を語または正規形で表した文字列)のみ**。GAP オブジェクト・中間関数・helper モジュールは一切流れない。

---

## 2. import 禁止境界(何を読んでよい/いけないか)

| 対象 | Lane S | Lane V | Lane P |
|---|---|---|---|
| `docs/notes/hs_prop7_translation_v1.md`(定義・紙の証明) | ○ 読む(定義の一次典拠) | ○ 読む | ○ 読む |
| `sol/sol_reply_101_math28.md`(P101-1 契約文) | ○ 読む | ○ 読む | ○ 読む |
| **他レーンの driver スクリプト本体** | ✗ 禁止 | ✗ 禁止 | ✗ 禁止(Lane S/V のスクリプトを import・参照しない) |
| **他レーンの GAP セッション内オブジェクト・中間表現** | ✗ 禁止 | ✗ 禁止 | ✗ 禁止 |
| **他レーンの cert(候補の値のみ)** | (自分が生成する側なので該当なし) | ○ Lane S の cert から `(m,f̄)` の**値**を読む(判定関数は読まない) | ○ Lane S/V の cert から候補**値**を読む |
| `search/probe/hsp7_gap_v1/stage1_pb4.g`, `stage2_k05.g`, `stage3_gen_setup.g`, `stage4_eval.g`(発火条件2の既存資産) | ✗ 禁止 | ✗ 禁止 | ○ **継承可**(§3 で唯一の例外) |
| `search/probe/hsp7_v1/*.py`(§6 の python 単系統検算 — h₄・θ*・τ* の Lie 環側計算) | ✗ 禁止(群側と Lie 環側は別建て、混ぜない) | ✗ 禁止 | ✗ 禁止(Lane P も群元として評価する。Lie 環側検算は crosscheck の別範疇であり、このレーン分割には入れない) |
| GAP 共通 utility(`gap.ps1`・`gap_output_prelude.g` のような出力整形の prelude) | ○ 可(出力整形は「機構」ではないので helper 非共有の対象外) | ○ 可 | ○ 可 |
| ANUPQ の SetupFile バッチ手順(発火条件2で確立した回避策・裁定432/435で環境事実と確認) | ○ **手順は流用可**(付録 B の 5 行の文章記述のみを参照。`stage3_gen_setup.g` の**コード自体は読まない** — 生成済み `pq_setup_*.txt`/`PQ_OUTPUT_*.g` ファイルの使い回しも禁止) | ○ 同左(付録 B 参照) | ○ 同左(K(0,5) 側は既存資産を継承するので実質発火条件2のものをそのまま使ってよい、§3) |

**原則**: 「helper 非共有」とは**判定ロジックの実装を独立に書き直す**ことであり、ツールの使い方(GAP のバージョン・ANUPQ のバッチ回避策・出力整形)まで独立にする必要はない。混同しないこと(§3 の継承可否の線引きもこの原則に基づく)。

---

## 3. 既存 stage1-4 資産の継承先 — 提案: **Lane P に限定継承**

### 提案

`search/probe/hsp7_gap_v1/stage1_pb4.g`(B₄/PB₄ 構築)・`stage2_k05.g`(K(0,5) 構築・辞書・fail-closed アンカー)・`stage3_gen_setup.g`(バッチ setup 生成手順)は **Lane P にのみ継承させる**。Lane S・Lane V はこれらを import せず、それぞれ独立に(Lane S は P だけを、Lane V は B₃/N を)ゼロから構築する。

### 理由

1. **K(0,5) の構築は Lane P 固有の対象**である。Lane S(P のみが要る)・Lane V(B₃/N のみが要る)はそもそも K(0,5) 側のコードを必要としない — 継承の候補にすらならない。
2. **P 自体の構築(stage1 の一部・F₂ の p-quotient)は Lane P・Lane S 双方が要るが、★ 司令塔裁定によりこれは各レーンが独立著者で再構築する「要件」である**(初版は「望ましい重複」程度に位置づけていたが、裁定で明確化: **独立著者による再構築こそが CV-9 の趣旨**であり、単なる許容ではなく要求である)。Lane S・Lane V は stage1-4 のいずれも import せず、それぞれ P を(あるいは Lane V は B₃/N を)ゼロから構築する。**突合は共有 helper なしの数値アンカー(位数・LCS 層・アーベル化)で、司令塔集約 cert(§5.2)にて行う** — 各レーンの内部で「自分の P が正しいか」を他レーンのコードと比較することはしない。
3. **CV-9 の観点**では、Lane P が発火条件2の資産をそのまま使うことは「同一著者・同一 helper」の連続性を持つが、これは Lane P **単体の内部**の話であり、Lane S/V との**独立性**を損なわない(Lane P と Lane S/V が独立であることが helper 非共有の目的であって、Lane P 自身が発火条件2との連続性を持つこと自体は問題ではない — cert の著者表(§6)にその連続性を明記すればよい)。
4. 発火条件2で fail-closed アンカー(K(0,5)^ab=Z⁵・ρ well-defined・ρ⁵=id・ρ≠id)を全 PASS しているため、**Lane P が K(0,5) をゼロから作り直すコストは検証価値に対して割に合わない**(むしろ作り直すと「同じ辞書を独立に導出できるか」という**別の実験**になり、それは条件4の範囲外 — 辞書の精読照合は Sol ゲート案件、§0 の状態表参照)。
5. ★ **較正アンカーとしての cond2 cert の使用範囲(裁定)**: 各レーンは走行前に `hsp7_cond2_p7_20260804.json` の既知値(|P|=7⁸・LCS 層 [2,1,2,3]・|[P,P]|=7⁶ 等)を**突合欄限定**で参照してよい ―「自分の再構築した P がこの値と一致するはずだ」という確認にのみ使い、**生成コード(P をどう作るか・候補をどう列挙するか)には一切接触させない**。S-7′ の「期待値をコード生成に使わない」規律をレーン間関係でも維持する形。

### 継承しないもの(Lane P 内でも新規に書くべきもの)

- h₄ の評価・N_ρ の評価そのもの(発火条件2の stage4 は「m=0, t=1 の 1 点」しか見ていない。Lane P は NW-P6 に従い t=0..6 の 7 点全てを評価する新規コードが要る)。
- Lane S/V から受け取った候補集合全体に対する PENT_W の一般判定ループ(発火条件2は決め打ちの h₄ᵗ family のみ)。

---

## 4. NW-P6/P7/P8・S-6/S-7′/S-9・DUM-1/p family の担当割り当て

| 項目 | 担当レーン | 理由 |
|---|---|---|
| **NW-P6**(h₄ᵗ family, t=0..6, PENT PASS はちょうど 1 個) | **Lane P** | 定理 DUM-1/p (d) は K(0,5)/W 側の ν₄ の値そのものの主張。Lane P が担う。ただし family の各 t が「charming かつ簡約 hexagon を満たす」ことの確認(定理 DUM-1/p (a)(b))は Lane S/V が候補生成の一部として担当してもよい(§0 条件3 の分離判定の趣旨) |
| **NW-P7**(p=5 control) | **Lane P**。ただし ★ **別宇宙 NW(5) の versioned 事前登録が先に要る**(裁定・S-7′ の「別 version 事前登録」枠組み)。登録票の草案は付録 B。**期待に書いてよいのは W101-1.4 の限定形(「𝔥₄-座標の fiber 内分離が死ぬ」)のみ — 「全候補 100% PASS」は書かない** | 同上(ただし NW(7) とは別の登録票の下で走る。K(0,5)/W の p=5 版を新規に構築、実装を p でパラメトリックにしておけば追加コストは小さい) |
| **NW-P8**(N vs N₀・c の有無で full (3.3)(3.4) の判定が食い違う m の存在) | **Lane V** | N₀(c ∉ N₀)は簡約 hexagon の近道が壊れる窓であり、full B₃/N₀ 上の (3.3)(3.4) 評価が必要(NW-1b (5) 注)。これは Lane V の守備範囲そのもの |
| **S-6**(NW-P3/NW-P5 偽 → 停止) | 発火条件2で既に評価済み(不変)。条件4の走行では **Lane P が NW-P5 の family 版(NW-P6)を再評価するたびに** 同型のチェックを都度入れる | — |
| **S-7′**(NW-P2 相当の予言不一致 → INTEGRITY_STOP・同 run 内で書き換えない) | **全レーン共通**。各レーンの cert に自レーンの予言(NW-P6/P7/P8 の該当部分)との突合欄を持たせ、不一致が出たら該当レーンのみ停止し、他レーンは止めない(レーン間で問題を伝播させない — 各レーンの独立性を停止規則のレベルでも保つ) | — |
| **S-9**(★ 内容採用・裁定で正式採用。**S-8 は裁定433で数学者が較正失敗停止=CALIBRATION_FAILED用に既に鋳造済みのため改番**) | **司令塔突合工程**(§6)の停止規則 | 定義: 「Lane S と Lane V の候補判定が 1 件でも食い違えば、その候補は`LANE_DISAGREEMENT`として保留し、多数決や片方の優先で自動解決しない。3 レーン全体の cert 発行も止め、実装バグか NW-1 の紙の定義自体の誤りかを人手(数学者/falsifier)で切り分けるまで前進しない」。**理由**: helper 非共有三レーンの目的そのものが「判定の独立な一致」を検出することにあるため、不一致の**自動解決**(たとえば「多数決」「Lane V を正とする」等)を許すと、まさに検出したい種類のバグを握りつぶすことになる。既存の S-1〜S-7′ には「レーン間不一致」に対応する規則がなかったため本稿で提案し、**司令塔裁定で S-9 として正式採用済み**(番号衝突の教訓 = novelty grep の轍を踏まないこと自体もここに記録する) |
| **DUM-1/p family の分離判定**(hexagon/charming/SURJ/PENT を別々に判定・条件3) | hexagon: **Lane S**(簡約形で判定)+ **Lane V**(full 形で再判定)/ charming: **Lane S**(候補生成の前提条件として)/ SURJ: **Lane V**(full B₃/N 上の T_{m,f} で判定するのが自然。系 H8′ により全候補で自動 PASS が期待値なので「識別力ゼロ」であることを Lane V の cert に明記し、S-7′ 型の突合に使わない、§8.7.6 の申し送り厳守)/ PENT: **Lane P** | 4 判定を 3 レーンにきれいに配分することで、条件3(分離判定)と条件4(helper 非共有)を同時に満たす設計になっている |

---

## 5. 各レーンの cert 様式と司令塔突合の手順

### 5.1 cert 様式(共通骨格・レーンごとに固有欄を追加)

各レーンの cert は独立ファイルとする(1 レーン = 1 cert、または 1 レーン内で複数の走に分けても構わないが、レーンをまたいで 1 ファイルに混在させない)。命名案:

- `search/certs/hsp7_cond4_laneS_p7_YYYYMMDD.json`
- `search/certs/hsp7_cond4_laneV_p7_YYYYMMDD.json`
- `search/certs/hsp7_cond4_laneP_p7_YYYYMMDD.json`

共通骨格(`hsp7_cond2_p7_20260804.json` の schema を土台に拡張):

```
{
  "schema": "hsp7-cond4-lane{S|V|P}/v1",
  "DRIVER_DONE": true,               <- 軽微指摘(裁定435)への対応: 完走マーカーをトップレベルに明示
  "lane": "S" | "V" | "P",
  "generated_by": [...driver scripts, このレーン専用...],
  "imports_declared": [...このレーンが読んだファイルの網羅リスト。§2の禁止表と突合可能な形で...],
  "imports_forbidden_check": {"other_lane_scripts_referenced": false, "other_lane_gap_objects_referenced": false},
  "universe": {...NW(7)固定、p, class_bound, exponent...},
  "candidates_in": [...他レーンから受け取った値のみのリスト。cert内で完結し、他certへの参照はハッシュのみ...],
  "candidates_in_source_cert_sha256": "...",
  "lane_specific_results": {...レーン固有(§1参照)...},
  "stop_rules": {"S6":..., "S7prime":..., "S8":...},
  "cross_checked_status": {"status": "n/a", "reason": "single lane; 3レーン揃ってCV-9判読後に格上げ"},
  "provenance": {...}
}
```

### 5.2 司令塔突合の手順

1. 3 レーンの cert が揃った時点で、司令塔(または委嘱された係)が**3 ファイルを並べて**候補ごとに (Lane S の判定, Lane V の判定, Lane P の判定) の 3 つ組を作る。**この突合作業自体もレーンの外(第四の主体)が行う** — いずれかのレーンの driver に突合ロジックを埋め込まない。
2. Lane S と Lane V が全候補で一致することを確認(不一致は S-9 で停止・保留)。
3. 一致した候補集合に対して Lane P の PENT 判定を読み、NW-P6 の「ちょうど 1 個 PASS」を確認。
4. NW-P2 型の全予言(NW-P6/P7/P8)との突合表を 3 cert から機械的に合成し、`search/certs/hsp7_cond4_summary_YYYYMMDD.json` のような**四つ目の集約 cert**(これは判定を行わない、3 cert の値を転記・突合するだけの文書)を作る。
5. CV-9(falsifier による規約同一性判読)へ 3 cert + 集約 cert を提出。ここで初めて「cross-checked」を名乗れる(それまでは各レーン単体は single-lane candidate のまま)。

---

## 6. CV-9 判読に耐える独立性 — 著者・helper・規約の分離表

| | Lane S | Lane V | Lane P |
|---|---|---|---|
| **起草者(driver を書く実装インスタンス)** | 実装担当インスタンス #1(条件4発注時に新規起動。**発火条件2を書いた実装担当インスタンスの transcript を継続しない** — 新規セッションとして開始することを発注条件に含める) | 実装担当インスタンス #2(#1 とも #3 とも別) | 実装担当インスタンス #3、または発火条件2を書いたインスタンスの続き(§3 の継承理由により連続性を許容する**唯一のレーン**) |
| **数学的定義の出典** | `hs_prop7_translation_v1.md` §8.7(簡約 hexagon (3.10)(3.11)) | 同 §2.2 前半・定義ノート (3.3)(3.4)(full hexagon) | 同 §1.2-1.3(PENT-NORM)・§8.7.3(sphere row-product 辞書) — いずれも同じ一次資料だが**引用節が異なる**(条件式は共通の紙の定義であり、これ自体は「helper」ではなく「対象の定義」なので共有して構わない、§2 冒頭原則) |
| **GAP 構築の実装**(P や B₃/N や K(0,5) をどう GAP オブジェクト化するか) | Lane S 独自(P のみ) | Lane V 独自(B₃/N のみ、full 群として) | 発火条件2の資産を継承(K(0,5) のみ) — **他レーンとの重複なし**(P の構築だけは S と P で独立に二重実装されるが、これは「同じものを違う書き方で作って一致を見る」という意味で望ましい重複であり、CV-9 が問題視する「同じ helper を共有した見せかけの独立性」とは逆) |
| **停止規則の実装者** | Lane S の driver 内(自レーンの S-7′ 部分のみ) | Lane V の driver 内(同上) | Lane P の driver 内(同上)。S-9 は突合工程側(§5.2 の第四の主体) |
| **cert 生成コード** | Lane S 内で自己完結 | Lane V 内で自己完結 | Lane P 内で自己完結。**cert のフォーマッタ(JSON 整形)自体は共通スクリプトを使ってよい**(§2 の「出力整形は機構でない」原則) |
| **falsifier への提出物** | driver + cert + run log + `imports_declared` の実ファイルパス一覧(falsifier が §2 の禁止表と突合できるように) | 同左 | 同左 |

**CV-9 判読で falsifier が確認すべき核心**(本稿の設計が成立しているかどうかの判定基準):

1. 3 レーンの driver が実際に**別々のセッション/インスタンス**で書かれ、相互に import していないこと(git の commit 履歴・ファイルの起草者ログで裏取り可能な形にする)。
2. `imports_declared` が §2 の禁止表と矛盾しないこと(特に Lane S/V が発火条件2の stage1-4 を読んでいないこと)。
3. 3 レーンの判定が**独立に**導かれていながら一致することが、単なる「同じ間違いを 3 回コピーした」結果でないこと — このため各レーンの GAP 構築コードは実装スタイル(生成元の順序・relator の書き方等)が意図的に異なっていることが望ましい(完全に同一のコードが 3 箇所にあるなら、それは非共有の失敗を示す弱い証拠になる)。

---

## 7. 未決事項 → 司令塔裁定済み(反映記録)

初版 §7 で 4 件を未決として挙げたが、司令塔裁定により以下のとおり全件確定した。

1. **S-8 の正式採用可否** → ★ **内容採用・S-9 へ改番して正式採用**(S-8 は裁定433で数学者が CALIBRATION_FAILED 用に既に鋳造済みだった — 名前衝突。本稿の全 S-8 表記を S-9 に置換済み、§4/§5.2/§6 参照)。
2. **Lane S/V の「P の構築」二重実装が許容範囲か** → ★ **可・むしろ要件**。独立著者による再構築こそが CV-9 の趣旨。突合は共有 helper なしの**数値アンカー**(位数・LCS 層・アーベル化)で司令塔集約 cert にて行う(§3 に反映)。**各レーンは発射前に発火条件2 cert(`hsp7_cond2_p7_20260804.json`)の既知値を、突合欄限定の較正アンカーとして使ってよい**(生成コードには一切接触させない — S-7′ の「期待値をコード生成に使わない」規律をレーン間でも維持)。
3. **NW-P6/P7 の割当** → NW-P6 は Lane P のまま確定。**NW-P7(p=5 control)は別宇宙 NW(5) の versioned 事前登録が必要**と裁定(S-7′ の「別 version 事前登録」枠組み)。登録票の草案を付録 B に追加した。**期待に書いてよいのは W101-1.4 の限定形(「𝔥₄-座標の fiber 内分離が死ぬ」)のみ — 「全候補 100% PASS」は書かない**(§4 の表を修正済み)。
4. **発注の粒度** → ★ **並行発注を採用。3 レーン = 3 別係、全て新規インスタンス**。Lane P のみ stage1-4(発火条件2の資産)の限定継承可(継承ファイルを cert に列挙して明記すること、§3)。**Lane S/V は stage コードの読み取りを禁止**(§2 の禁止表のとおり・司令塔裁定で再確認)。

**追加修正(司令塔裁定・本版で反映)**:
- **環境ノートの共有可否**: ANUPQ SetupFile バッチ経路(裁定432/435 で確立)は**環境事実**であり全レーン共有可。ただし `stage3_gen_setup.g` のコード自体は読まない — **手順の文章記述のみ**を付録 B に自書して共有する(§2 の表にも追記)。

以上を反映した本版(v1・本改訂)をもって、実装・実行は行っていない状態のまま最終報告とする。この後 falsifier 前哨 → 3 レーン発注は司令塔が行う。

---

## 付録 A. NW(5) 事前登録票(草案・NW-P7 用)

★ **本票は草案であり、Lane P が NW-P7 に着手する前に司令塔/数学者の確認を要する**(本稿は発注設計書であり、この登録票自体を発効させるものではない)。

| 項目 | 内容 |
|---|---|
| **宇宙 ID** | NW(5)(NW(7) とは別 version。S-7′ の「別 version 事前登録から開始する」規律に基づく新規登録) |
| **窓対** | 定義 NW(p) を p=5, e=1 で固定。**N=𝒱(F₂)×⟨c⟩, N₀=𝒱(F₂)×⟨c⁵⟩**(定義 NW(p) の式をそのまま p=5 に代入。§8.7.3 の証明はどの素数でも紙のまま通る箇所と p=7 固有だった箇所を要区別 — 後述) |
| **窓の役割** | ★ **control 専用**。本走(NW(7))の dummy family に対する退化較正であり、独立した discovery 標的ではない(§2.4「篩条件: 窓の関与する標数から5を排除せよ」との緊張関係を明記: この排除規則は「discovery 標的としての p=5」を禁じるものであり、「NW(7) の control としての p=5」を禁じるものではない ― 両者を混同しない) |
| **対象 family** | NW(7) と**同じ構成手順**で作った h₄ᵗ(t=0..4, p=5 なので 𝒳_N の元は 4 個)。h₄ の語そのもの(交換子語の形)は p に依存しない定義(DUM-FIN)なので変更なし |
| **予言(書いてよい範囲)** | ★ **W101-1.4 の限定形のみ**: 「ν₄(j𝔥₄) ≡ 0 (mod 5) ゆえ 𝔥₄-座標による fiber 内分離が死ぬ」。**禁止**: 「family 全候補が 100% PASS する」という一般化(撤回済みの P-HSP-5/系 D4-PRED と同型の過剰主張になるため、§8.3.1/§8.3.4 の教訓をそのまま適用) |
| **停止規則** | S-6/S-7′ を NW(5) にもそのまま適用(NW(7) と独立の INTEGRITY_STOP)。NW(7) 側で S-6/S-7′ が発火していても NW(5) 側の走を自動停止しない(逆も同様 — 宇宙が違うので伝播させない) |
| **cert 命名** | `search/certs/hsp7_cond4_laneP_p5control_YYYYMMDD.json`(NW(7) の Lane P cert とは別ファイル) |
| **未決事項** | (a) NW(5) の P/Q の exact pc presentation(|P|・LCS 層・|Q|・dim γ₄(Q))を p=7 のときと同じ手順(B₄ Artin 表示→PB₄→K(0,5)→sphere row-product 辞書)で再構築する必要があるか、それとも p=7 で構築した K(0,5) の presentation(群としては p に依存しない K(0,5) それ自体)を再利用し p-quotient だけ p=5 で取り直せばよいか — **後者が自然**(K(0,5) は p に依存しない一つの群であり、NW(p) ごとに変わるのは verbal 部分群 W=γ₅(K(0,5))K(0,5)^p の p だけ)。ただし「K(0,5) の presentation の再利用」は Lane P 内部の話であり、Lane S/V との非共有原則には抵触しない。(b) この登録票の発効(=着手可の判定)自体は司令塔/Sol の追認を要する。 |

---

## 付録 B. 環境ノート(全レーン共有可・裁定432/435)

ANUPQ の対話 iostream(`InputOutputLocalProcess` 経由の `PqStart`/`Pq`)はこの Windows/Cygwin GAP 4.16.0 環境で `Error, failed to find any more of line (iostream dead?)` により機能しない(pq.exe 単体は健全・DLL PATH も正しい・パイプのバッファリングが疑われるが未確定)。回避策として、ANUPQ 純正の `SetupFile` オプション(`Pq(G : ..., SetupFile := path)`)でコマンド列をファイルへ書き出し、`pq -i -k -g < setupfile > log` の一方向 stdin リダイレクトで pq.exe を実行し、出力 `PQ_OUTPUT`(GAP 形式 2)を `Read()` でそのまま読み込む。この手順は ANUPQ 自身が生成・読解するものであり自作プロトコルではない。全レーンがこの手順を使ってよい(環境事実であり判定ロジックではないため helper 非共有の対象外、§2)。**各レーンは自分の窓定義から自分で setup file を生成すること**(生成済み `pq_setup_*.txt`/`PQ_OUTPUT_*.g` の使い回しは禁止 — `stage3_gen_setup.g` のコード自体も読まない)。
