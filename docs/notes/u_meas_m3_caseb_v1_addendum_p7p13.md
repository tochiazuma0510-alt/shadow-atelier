# case (b) — mod-$p$ 悉皆の回収と schema-v2 篩の結果(`u_meas_m3_caseb_v1.md` の追補 1)

**状態札: `measurement (partial) / 単系統・Sol 監査前 — 解は未確定。locus 仮説は未確認`**
起草: 影工房 数学者(Claude・Opus 5)/ 2026-07-31
委嘱: 司令塔「mod-7 悉皆の回収 → 篩(monodromy → 原始性 → 非分解 → それから $u$)」

> **本追補は $u$ に到達していない。** 篩を通った候補は「$\mathbf Q$ 上の単一解」としてまだ確定していないため、命題 U-LOC は**発火させていない**(規律どおり)。

---

## 0. 結論(5 行)

1. **v1 の探索は出力がパイプ緩衝で失われていた**ので、軌道正規化つき v2 を書き直して再走。**$p=7$ は 4.8 秒・$p=13$ は 213 秒**で完了(v1 比 ~6 倍高速)。
2. **基底の独立検証**: 全候補で $\deg\mathcal N=9$($p=7,13$ とも「$\deg\ne9$ の候補 0 件」)。**§2.2 の $L(9\bar P)^-$(次元 4・極位数 3,5,7,9)が正しいことの機械的裏づけ**。
3. **$\tau_2$ 条件が自動であることも実証**: 篩にかけた全候補で $\tau_1,\tau_2$ の両方が立方 ✓(v1 §2.3 の主張)。
4. **schema-v2 篩は機能した**: 非分解候補は $p=7$ で 1 個・$p=13$ で 3 個。**$p=13$ の 3 個のうち 2 個を Frobenius 巡回型で棄却**(型 $(4,4,1),(7,2),(5,4),(8,1)$ 等は $\mathrm{P\Gamma L}(2,8)$ に存在しない)。分解可能枝($c_5=c_7=0$)は定義から全棄却。**⟹ 各素数で生き残りはちょうど 1 個**(剛性と整合)。
5. **しかし $\mathbf Q$ への持ち上げは未成立**: 生存者が示唆する locus $b=a^2/4,\ c=8a^3$ を $p=19,31$ で走査すると**生存者 0**、CRT の有理再構成も破綻。**⟹ locus 仮説は棄却または悪い還元。$p=19$ の全域走査を発車済(結果待ち)。**

---

## 1. 探索(v2)の結果

`search/probe/wac_v1/u_meas_caseb_search2.py`。軌道正規化: $x\mapsto\alpha x$ が $(a,b,c)\mapsto(a\alpha^{-2},b\alpha^{-4},c\alpha^{-6})$、$(c_3,c_5,c_7,c_9)\mapsto(\alpha^3c_3,\alpha^5c_5,\alpha^7c_7,\alpha^9c_9)$ で作用するので、$(a,b,c)$ を悉皆走査する以上、$c$-ベクトルは各軌道の代表 1 個に絞ってよい($\gcd(7,p-1)=1$ なら $c_7\in\{0,1\}$ 等)。

| $p$ | $\tau$ | 候補数 | 時間 | ヒット総数 | うち**非分解**($(c_5,c_7)\ne(0,0)$) | $\deg\mathcal N\ne9$ |
|---|---|---|---|---|---|---|
| 7 | $\{1,2\}$ | 122,451 | 4.8 s | 16 | **1** | **0** |
| 13 | $\{4,12\}$ | 4,883,931 | 212.5 s | 36 | **3** | **0** |

> ### 【FINDING CB-8】分解可能枝の正体
> ヒットの大多数($p=7$ で 15/16、$p=13$ で 33/36)は $c_5=c_7=0$、すなわち
> $$t=\tfrac32+c_3\theta+c_9\theta^3\ =\ (\text{$\theta$ の 3 次多項式})\circ\theta$$
> で、$\theta:C\to\mathbf P^1$ は次数 3 ⟹ **$t$ は $3\times3$ に分解する**⟹ monodromy は非原始的 ⟹ $\mathrm{PSL}(2,8)$ ではない。
> **case (a) の偽解(前便)と同じ病理が、case (b) でも支配的な偽解族として現れた。** 篩の第一段として `c5 = c7 = 0` を即時棄却してよい(実装済)。

## 2. schema-v2 篩(`u_meas_caseb_sieve.py`)

**許容 Frobenius 巡回型**(GAP で算出):
$$\mathrm{PSL}(2,8):\ 1^9,\ 2^41,\ 3^3,\ 7\,1^2,\ 9\qquad
\mathrm{P\Gamma L}(2,8):\ \text{上に}\ +\ 3^21^3,\ 6\,2\,1$$
$\mathcal N_{t_0}(x)$($t_0\in\mathbf F_p$、分岐点を除く)の既約分解パターンが $\mathrm{P\Gamma L}(2,8)$ の型表に無ければ**即棄却**。

| # | $p$ | $(a,b,c)$ | $(c_3,c_5,c_7,c_9)$ | $\tau_1,\tau_2$ とも立方 | 非分解 | Frobenius 型 | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 7 | $(2,1,1)$ | $(1,5,1,5)$ | ✓✓ | ✓ | $\{(6,2,1),(9)\}$ | **SURVIVES** |
| 2 | 13 | $(3,12,8)$ | $(12,5,1,11)$ | ✓✓ | ✓ | $\{(6,2,1),(9)\}$ | **SURVIVES** |
| 3 | 13 | $(7,12,9)$ | $(0,1,1,11)$ | ✓✓ | ✓ | $(4,4,1),(7,2),(4,1^5)$ | **REJECTED**(型が $\mathrm{P\Gamma L}$ に無い) |
| 4 | 13 | $(9,0,4)$ | $(4,4,1,7)$ | ✓✓ | ✓ | $(5,4),(8,1),(6,1^3),(4,3,1,1)$ | **REJECTED** |
| 対照 | 7 | $(0,0,1)$ | $(4,0,0,2)$ | ✓✓ | ✗(分解) | $\{(3,3,3)\}$ のみ | **REJECTED** |
| 対照 | 13 | $(2,1,2)$ | $(7,0,0,2)$ | ✓✓ | ✗(分解) | $(6,3),(4,2,2,1)$ 等 | **REJECTED** |

> ### 【FINDING CB-9】**篩は実際に効く。**
> * **monodromy による棄却が 2 件**(候補 3・4)。**passport・種数・$\tau$-立方条件をすべて満たしながら monodromy が違う偽解**が実在することを、case (b) でも確認した(前便の case (a) 事件の再現)。**「passport が合う偽解は正解と同じ形の答を出す」教材の 2 例目。**
> * 分解可能対照の Frobenius 型が $(3,3,3)$ 一色($p=7$)なのは、$3\times3$ ブロック構造の直接の指紋。
> * **各素数で生存者はちょうど 1 個** — Nielsen 類数 1(剛性)と定量的に整合。

## 3. $\mathbf Q$ への持ち上げ — **未成立**

生存者 1(mod 7)と 2(mod 13)はいずれも
$$b=\frac{a^2}4,\qquad c=8a^3\qquad\Bigl(\Longleftrightarrow\ e=\frac b2-\frac{a^2}8=0\ \Longleftrightarrow\ f_6=q(x)^2+c,\ \ \theta\bar\theta=c\ (\text{定数})\Bigr)$$
を満たす($p=7$: $a=2,b=1,c=1$、$8a^3=64\equiv1$ ✓ / $p=13$: $a=3,b=12,c=8$、$a^2/4=9\cdot10=90\equiv12$ ✓、$8a^3=8\cdot27\equiv8$ ✓)。**この形は非常に示唆的**($\theta$ が定数ノルムをもち、$\mathrm{div}(\theta)=3\infty_--3\infty_+$ になる)。

**しかし locus $b=a^2/4,\ c=8a^3$ 上を $p=19,31,37$ で走査すると**(`u_meas_caseb_locus.py`):

| $p$ | 19 | 31 | 37 |
|---|---|---|---|
| locus 上の生存者 | **0** | **0** | 1($a=30$、$(c_3,c_5,c_7,c_9)=(25,35,1,2)$) |

スケール不変量 $J_i:=c_i^2a^i$ の残差 $(p=7,13,37)$: $J_3=(1,1,3)$、$J_5=(2,4,1)$、$J_7=(2,3,3)$、$J_9=(4,4,33)$。**CRT(法 3367)の有理再構成はいずれも約分されない分数を返し破綻**($37/37$、$21/7$、$-39/13$ 等)。

> ### 【判定 CB-10】**locus 仮説は確認できない。** 次の 3 択が残る:
> 1. **$b=a^2/4,\ c=8a^3$ は偶然**(2 素数の一致)で、真の解は locus 外 ⟹ $p=19$ の**全域**走査で生存者が出るはず;
> 2. locus は正しいが **$p=19,31$ は悪い還元**(モデルの判別式が割れる)⟹ 全域走査でも $p=19$ は 0;
> 3. 私の正規化のどこかが $\mathbf Q$ 上到達不能(命題 U-Q の「$\mathbf Q$-モデル」と、私の**偶六次モニック**モデルの間にズレ)。
>
> **⟹ $p=19$ の全域悉皆(4.8×10⁷ 候補・約 35 分)を発車済。本追補の時点で結果は未着。**
> **3 の点検(紙)**: $\bar P,\iota\bar P$ が $\mathbf Q$-有理 ⟹ 最高次係数は平方 ⟹ モニック化可 ✓;$\bar\psi$ は $\mathbf P^1_x$ 上で $\infty$ を固定する $\mathbf Q$-有理な対合 ⟹ 平行移動で $x\mapsto-x$ に正規化可 ⟹ **偶六次**は $\mathbf Q$ 上到達可能 ✓。したがって 3 は**紙上は否定される**が、機械で再確認したい。

$$\boxed{\ \textbf{$u$ には到達していない。命題 U-LOC は発火させていない。}\ }$$

---

## 4. 出所

| ファイル | SHA-256 | 内容 |
|---|---|---|
| `search/probe/wac_v1/u_meas_caseb_search2.py` | `ebc4aeabf9c13fb66d8f519d9e5019ae6edab3965c3349dbd07775ee857f9ba5` | 軌道正規化つき mod-$p$ 悉皆(v1 の出力消失を是正) |
| `search/probe/wac_v1/u_meas_caseb_sieve.py` | `064312fa811d93328452971ec86723a8bcbd00dff817abf9c66fdf281d1850a1` | schema-v2 篩($\tau_2$ 整合 → 非分解 → Frobenius 型) |
| `search/probe/wac_v1/u_meas_caseb_locus.py` | `b08be7b6a0ab824591b9d782a5ef19df959760a47110f37133630a296270f9fe` | locus 走査 + CRT + 有理再構成 |
| `search/probe/wac_v1/_ct_tmp.g` | (使い捨て) | $\mathrm{PSL}(2,8)$/$\mathrm{P\Gamma L}(2,8)$ の許容巡回型の算出(GAP) |
| ログ | `scratchpad_caseb_p7.log` / `..._p13.log` / `..._locus.log` / `..._p19.log`(走行中) | |

**単系統(python 自前 $\mathbf F_p$ 演算・浮動小数点不使用)+ GAP(許容巡回型)。cross-checked ではない。Lean verified ではない。**
**⚠ v1 の探索スクリプト `u_meas_caseb_search.py` は出力がパイプ緩衝で失われたため結果を採用しない**(自認)。v2 が正。

## 5. FINDING(追補)

| # | 格 | 内容 |
|---|---|---|
| **CB-8** | 測定 + 紙 | 偽解の支配族 = $c_5=c_7=0$(= $t$ が $\theta$ の 3 次式 ⟹ $3\times3$ 分解)。篩の第一段に固定 |
| **CB-9** | 測定 | **schema-v2 の monodromy ゲートが実際に 2 件棄却**。passport・種数・立方条件を満たす偽解が case (b) にも実在(教材 2 例目)。各素数で生存者ちょうど 1 個 = 剛性と整合 |
| **CB-10** | **未閉鎖** | locus $b=a^2/4,c=8a^3$ は $p=19,31$ で生存者 0・CRT 再構成破綻 ⟹ **未確認**。$p=19$ 全域走査の結果待ち |
| **CB-11** | 検証 | $\deg\mathcal N=9$ が全候補で成立 ⟹ **$L(9\bar P)^-$ の基底(v1 §2.2)が機械的に裏づけられた** |
| **CB-12** | 検証 | $\tau_2$ 条件の自動性(v1 §2.3)が全候補で成立 |

### 次の一手
* 【CB-e】**$p=19$ 全域走査の回収**(走行中)。生存者が locus 外に出れば CB-10 の枝 1、0 なら枝 2。
* 【CB-f】枝 2 なら $p=37,43,61$ を locus 上で走らせ、**より多くの素数で CRT**(法 $10^7$ 級)して再構成をやり直す。
* 【CB-g】幾何的 monodromy の確定: 現状は $\mathbf F_p$ 上の Frobenius 型(=**算術** monodromy の下限)しか見ていない。$(6,2,1)$ は $\mathrm{P\Gamma L}\setminus\mathrm{PSL}$ の型なので算術側は $\mathrm{P\Gamma L}(2,8)$ を含む — **幾何側が $\mathrm{PSL}(2,8)$ であることの確認は未了**(候補確定後に $\mathbf F_{p^k}$ で行う)。
* 【CB-h】文献: 本追補も**原著未読**(覚書のみ)。【CB-e】の結果次第で A1 §(mod-$p$ 探索と持ち上げ)と A6 を精読する。
