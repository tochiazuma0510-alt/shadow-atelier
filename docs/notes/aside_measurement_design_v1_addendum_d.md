# ASIDE-2 追補 D — 【C-GAP-2】の解釈・A 側正札・$k{=}12$ 判定表 v3(再凍結)

**状態札: `paper-math + 正札起草 / A 側は実測済(5 素数)/ S₁₂ は blind・値の推測ゼロ / 新規性 grep 実施済 / 封印非接触`**

> ⚠ **§8 正誤 E-1 を先に読むこと**(2026-08-06 追記・裁定 717)。**§3.2 の N-1 と N-3 は縮約された** — canon 超過は「深さ 6,7,8」ではなく **$v_{691}(D^{(6)})\ge1$ の 1 ビット**、プロファイルの新規部分は 5 数ではなく **$(744,858)$ の 2 数**。§3.2 の当該欄は §8 の文言で置換される。

- 起草: 影工房 **数学者**(Claude / Opus 5)・2026-08-06
- 委嘱: 司令塔(裁定 711 ⑥・712)「①【C-GAP-2】の解釈(回文プロファイル・lift 依存性会計・正札起草) ②$k{=}12$ 判定表 v3 の再凍結」
- 本体(`aside_measurement_design_v1.md`)および追補 C(85827ca)は**不改変**。本票は追補 C §4.4(判定表 v2.1)と §5(P-C-5)を上書きする。
- 入力正本:
  - `search/certs/aside2_prime_{677,691,701,998244353,2147483647}_v2_20260806.json`(e274278 / 844b88b)
  - `docs/scout/brown_prop64_lattice_verbatim_v1.md`(507e5ec・Prop 6.4/4.3 逐語・格子 UNKNOWN)
  - `docs/scout/brown_eq14_verbatim_v1.md`(ccf2f53・(1.6)(1.7)(8.8))
  - `docs/notes/aside_measurement_design_v1_addendum_c.md`(85827ca・定理 C-A・補題 C-2〜C-5)
  - `docs/notes/b_type_synthesis_design_v1.md` §1.1(SYN-0)・§1.2(W-1)・§1.4(SYN-1)・§2.1.2($\mathcal A_\bullet$ の定義)

> ## 非接触・規律の申告
> **$\mathcal S_{12}$ の実測値は一切扱わない。**§4 の判定表は「値 $\mapsto$ 読み」の写像を測定前に凍結するものであり、**どの値が出るかの推測を含まない**。ただし §4.2 の**命題 D-4 は仮定つきの下界定理**(推測ではない)であり、blind 運用への影響を §4.2 末尾で申告した。$\mathrm{Im}\,R$・$d_N$・封印 3 量は非接触。**発効判定語は司令塔専権**。

---

## 0. 一枚まとめ(8 行)

1. ★ **回文は定理である**(定理 D-1): $D$ は $\theta$-反対称(補題 C-2)、$\theta$ は深さ $d$ を $12-d$ に写す ⟹ **係数レベルで $\theta(D_d)=-D_{12-d}$**、ゆえに項数は必ず $d\leftrightarrow12-d$ で一致。$\{350,744,858,744,350\}$ の対称性は**測定の偶然ではない**。
2. ★ **台 $[4,8]$ も完全に導出できる**(定理 D-2): 構成上界 $[2,10]$ + 定理 C-A(深さ 2 消滅)+ **Prop 4.3 のパリティ**(深さ 3 消滅)+ $\theta$(深さ 9,10 消滅)。**実測の台と完全一致** ⟹ 未計画ながら **Prop 4.3 の bidegree (12,3) が機械で裏書きされた**。
3. ★ **深さ 5,7 の非零は Prop 6.4/4.3 と矛盾しない**(§1.3): パリティが縛るのは $\mathrm{gr}^D$(**先頭深さ成分だけ**)。$D$ の先頭は深さ 4 ゆえ、深さ 5〜8 の成分は**次数付き商の元ではなく**、パリティの管轄外。**filtered と graded の区別が要点**。
4. ★ **lift 依存性はゼロ**(補題 D-3): $\dim\mathfrak{grt}_m=1$($m=3,5,7,9$)ゆえ **lift の自由度が存在しない**($\sigma_{11}$ で初めて生じる)。$y$-正規化で $\sigma_m=\pm\tilde\sigma_m$ が**一意に確定** ⟹ $D$ は canonical。
5. ★ 4 素数でプロファイルが**完全一致** ⟹ 司令塔の読みどおり「有理的な深さ 4–8 構造の mod-$p$ 影」。より強く: **$D\in691\cdot\Lambda$ かつ $677,701,\text{大素数}\nmid$ 内容**という**ℤ 上の言明**が立つ(§2.3)— Brown が UNKNOWN とした**格子を具体的に指定した**ことになる。
6. ★ **自己監査**: 追補 C の **P-C-4 と P-C-5 は論理的に両立していなかった**(補題 C-5 により P-C-4 ⟹ 全深さ消滅 ⟹ P-C-5 の否定)。予言集合に内部矛盾があった — 起草者の誤り(§1.5)。
7. ★ **判定表 v3 の最大の発見は S 側ではなく橋**(§4.3): $\mathcal A_{12}@691=1$ は**括弧格子 $M$ の言明**であり、SYN-0 が要求する**算術像の第 12 層**へ渡すには「$\mathrm{gr}_{12}(H)=M$ の還元」という一段が要る。**これは未証明**【D-GAP-1】。渡らなければ段差は消える。
8. **新規性会計の最終文言**(§4.5): 新規 = 深さ 6,7,8 の消滅・格子の明示・一般素数プロファイル・近傍対照の 4 点。非新規 = $A_{12}@691=1$ そのもの。

---

## 1. 委嘱①(a) — 回文プロファイル $\{350,744,858,744,350\}$ の構造

### 1.1 実測(5 素数・cert 由来の生値)

| $p$ | $A_{12}$ | $A^{(2)}_{12}$ | $\theta$ | fixture | $D=0$? | $D$ 総項数 | 深さ 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 677 | 2 | 1 | ✔ | ✔ | 否 | 3046 | 350 | 744 | 858 | 744 | 350 |
| **691** | **1** | 1 | ✔ | ✔ | **是** | **0** | 0 | 0 | 0 | 0 | 0 |
| 701 | 2 | 1 | ✔ | ✔ | 否 | 3046 | 350 | 744 | 858 | 744 | 350 |
| 998244353 | 2 | 1 | ✔ | ✔ | 否 | 3046 | 350 | 744 | 858 | 744 | 350 |
| 2147483647 | 2 | 1 | ✔ | ✔ | 否 | 3046 | 350 | 744 | 858 | 744 | 350 |

深さ 2,3,9,10,11,12 は**全素数で 0**。

### 1.2 ★ 定理 D-1(回文は定理)

> $\theta$ は $X\leftrightarrow Y$ 交換自己同型。重み 12 の語 $w$ に対し $\mathrm{depth}(\theta w)=12-\mathrm{depth}(w)$。補題 C-2 より $\theta D=-D$。$\theta$ は語の**全単射**(1 語 $\mapsto$ 1 語・係数保存)ゆえ、深さ成分ごとに
> $$\boxed{\ \theta(D_d)\ =\ -\,D_{12-d}\qquad(d=0,\dots,12)\ }$$
> したがって **$\#\{$非零項$\}(D_d)=\#\{$非零項$\}(D_{12-d})$ が任意の素数で成立**。∎

**帰結**: $350{=}350$, $744{=}744$ は**測定の自由度ではない**。⟹ **新カナリア D-a**: 深さプロファイルが $d\leftrightarrow12-d$ で対称でなければ実装欠陥(STOP)。実測 5 素数はこれを通過している。

### 1.3 ★ 定理 D-2(台 $[4,8]$ の完全決定)

> **(i) 構成上界**: $\sigma_3\in\mathrm{Lie}_3$ の深さは $1..2$、$\sigma_9\in\mathrm{Lie}_9$ は $1..8$、$\sigma_5$ は $1..4$、$\sigma_7$ は $1..6$。Ihara 括弧は深さを加法的に写す ⟹ $\{\sigma_3,\sigma_9\},\{\sigma_5,\sigma_7\}$ ともに深さ $2..10$ ⟹ $\mathrm{supp}(D)\subseteq[2,10]$。
> **(ii) 深さ 2**: 定理 C-A(Ihara–Takao の有理関係)⟹ $D_2=0$(**全素数**)。
> **(iii) 深さ 3**: (ii) より $D\in\mathcal D^3\mathfrak g^{\mathfrak m}$。その $\mathrm{gr}^D_3$ における類は $D$ の**深さ 3 斉次成分**そのもの。Prop 4.3(逐語: 「$\mathfrak{dg}^{\mathfrak m}$ は $N\not\equiv r\ (2)$ の bidegree で消える」)より $\mathfrak{dg}^{\mathfrak m}_{12,3}=0$($12\not\equiv3$)⟹ $D_3=0$。
> **(iv) 深さ 9,10**: 定理 D-1 より $D_9=-\theta(D_3)=0$, $D_{10}=-\theta(D_2)=0$。
> $$\boxed{\ \mathrm{supp}(D)\subseteq[4,8]\ }\qquad\textbf{— 実測の台と完全一致}$$

**★ 副産物(未計画の較正)**: (iii) は**正典の定理(Prop 4.3)の bidegree (12,3) を機械が裏書きした**ことを意味する。$D_3=0$ は測定の出力であって入力ではない ⟹ **パリティ命題の独立確認**。(iv) と合わせ、$D_9=0$ も同時に確認された。

### 1.4 ★ 深さ 5,7 の非零は Prop 6.4/4.3 と両立する — filtered と graded の区別

司令塔の問い(「我々の $D$ は自由 Lie 実現の元であって depth-graded 商の元でない点の精密化」)に正面から答える。

> ### 命題 D-2′(パリティの管轄範囲)
> $\mathfrak g^{\mathfrak m}\subset\mathrm{Lie}(X,Y)$ は深さで**フィルター付け**られているだけで、**次数付けられてはいない**。一方 $\mathrm{Lie}(X,Y)$ 自身は深さで次数付き ⟹ 任意の元 $v$ は斉次成分 $v=\sum_d v_d$ に**一意に分解**される。両者の関係は:
> $$v\in\mathcal D^r\setminus\mathcal D^{r+1}\ \Longrightarrow\ [v]\in\mathrm{gr}^D_r=\mathfrak{dg}^{\mathfrak m}_r\ \textbf{の代表元は }v_r\ \textbf{(先頭成分)のみ}$$
> $v_{r+1},v_{r+2},\dots$ は **$\mathrm{gr}^D$ のどの類も代表しない**(そもそも $v\notin\mathcal D^{r+1}$)。
> Prop 4.3/6.4 は $\mathfrak{dg}^{\mathfrak m}$(= $\mathrm{gr}^D$)の消滅命題ゆえ、**先頭成分しか縛らない**。∎

**適用**: $D$ の先頭深さは **4**(実測 350 項・非零・Brown (8.8) の $(691/144)e_{12}$)。ゆえに
- **深さ 3**: 先頭候補だった ⟹ パリティが縛る ⟹ 0(§1.3 (iii))。
- **深さ 5, 7**: **先頭ではない** ⟹ パリティの管轄外 ⟹ **非零でよい**。$\mathfrak{dg}^{\mathfrak m}_{12,5}=\mathfrak{dg}^{\mathfrak m}_{12,7}=0$ と**矛盾しない**。
$$\Longrightarrow\ \boxed{\ \textbf{「奇数深さ 5,7 で 744 項が非零」は正典と完全に整合する。矛盾はどこにも無い。}\ }$$

> **誤読の型を台帳へ**: 「$\mathfrak{dg}^{\mathfrak m}_{12,5}=0$ だから深さ 5 成分は消えるはず」は **graded の言明を filtered の元の成分に適用した誤り**。追補 C §2 で捕獲した「depth-graded $\ne$ weight-graded」(【L4-GAP-1】)と**同じ型の第 2 例**である。

### 1.5 ★ 素数非依存性の意味 — 司令塔の読みは正しい(そして強められる)

4 素数(677, 701, 998244353, 2147483647)で**プロファイルが完全一致**(350/744/858/744/350・総 3046)。

> ### 命題 D-3
> $\sigma_m$($m=3,5,7,9$)は $y$-正規化された $\mathfrak{grt}_m$ の生成元(補題 C-4)であり、各素数で $\dim\mathcal S_m=1$・先頭係数が単数(cert の `sigma_norm_ok`)⟹ **mod $p$ の $\sigma_m$ は ℚ 上の $\sigma_m$ の還元**。ゆえに $D^{(p)}=D\bmod p$($D$ は ℚ(実際は $\mathbb Z_{(p)}$)上の**一つの固定された元**)。
> **系**: 4 素数で台が一致するのは、$D$ の**整数係数のうち非零なものがこれら 4 素数のいずれでも単数**だから。すなわち観測された $\{350,744,858,744,350\}$ は **ℚ 上の $D$ の深さプロファイルそのもの**(これら 4 素数が $D$ の係数を割らない限りにおいて)。

$$\Longrightarrow\ \boxed{\ \textbf{司令塔の読み「有理的な深さ 4–8 構造の mod-}p\textbf{ 影」は支持される。より強く、}p=691\ \textbf{だけが }D\ \textbf{の全係数を割る。}\ }$$

**追加の厳密構造**(素数非依存):
- **反転反自己同型 $\omega$**(語を逆順に読む)は $\mathrm{Lie}_n$ 上 $(-1)^{n-1}$ 倍 ⟹ $n=12$ で $\omega(D)=-D$(**機械照合済**)。ゆえに**回文語の係数は必ず 0**。深さ 4,6,8 の回文語数はそれぞれ 15, 20, 15(奇数深さには回文語が無い)。
- 各深さの全語数 $\binom{12}{d}=495,792,924,792,495$ に対する零の数 $145,48,66,48,145$。うち $\omega$ で説明されるのは $15,0,20,0,15$。**残余は構造的説明を持たない(UNKNOWN・判定に不要)**。

### 1.6 ★ 自己監査 — 追補 C の予言集合に内部矛盾があった

補題 C-5 は「$A_{12}@691=1\iff D\equiv0\pmod{691}$」を主張する。$D\equiv0$ は**全深さ成分の消滅**と同値である。したがって
$$\textbf{P-C-4}(A_{12}@691=1)\ \Longrightarrow\ \textbf{全深さ消滅}\ \Longrightarrow\ \lnot\,\textbf{P-C-5}(\text{差は深さ 4 のみ})$$
**P-C-4 と P-C-5 は同時に真になり得なかった。** 起草者(私)の誤りである。原因: P-C-5 を「Brown (8.8) は深さ 4 の話だから差も深さ 4 に局在するはず」という**文献の記述の場所**から書き、補題 C-5(自分で証明した同値)を通していなかった。
- **実害**: なし。IF-FIRST の分岐(「他の深さでも差 ⟹【C-GAP-2】」)は正しく発火し、測定は矛盾を正しく解消した(P-C-4 側が真)。
- **再発防止(規律提案)**: **予言凍結の前に、予言集合の内部無矛盾性を自分の補題で 1 回通す**(「予言間の含意関係を明示する行」を IF-FIRST 表に追加)。§5 の表ではこれを実施した。

---

## 2. 委嘱①(b) — lift 依存性の会計

### 2.1 ★ 補題 D-3(lift の自由度は存在しない)

> Brown p.3 の警告「$\sigma_{11}$ は $\{\sigma_3,\{\sigma_5,\sigma_3\}\}$ の有理数倍を除いてしか定まらない」は **重み 11 以上の現象**である。重み $m=3,5,7,9$ では
> $$\dim\mathfrak{grt}_m=1\quad(\text{一次資料 pin: IMT 表})$$
> ゆえに $\mathfrak{grt}_m$ は**直線**であり、lift の選択肢が**存在しない**(自由 Lie 環側にも重み 3,5,7,9 の分解が無い: $3{+}6,5{+}4,\dots$ に生成元が無く、$\{\sigma_3,\sigma_3\}=0$)。
> $y$-正規化(深さ 1 成分 $=y_{m-1}$)はこの直線上の**唯一の点**を選ぶ。Brown の $\tilde\sigma_{2n+1}$ も (1.6) より深さ 1 成分 $(-1)^ny_{2n}$ ⟹
> $$\boxed{\ \sigma_3=-\tilde\sigma_3,\quad\sigma_5=\tilde\sigma_5,\quad\sigma_7=-\tilde\sigma_7,\quad\sigma_9=\tilde\sigma_9\quad\textbf{(一意・選択の余地なし)}\ }$$
> ゆえに $D=-\bigl(\{\tilde\sigma_3,\tilde\sigma_9\}-3\{\tilde\sigma_5,\tilde\sigma_7\}\bigr)$ は **canonical な元**である。∎

> **注**: 逐語ノート §2.1 は「$\sigmã$ の一般定義 = **任意の lift**」を引いている(p.24: "Choose a lift")。**それでも本設定では曖昧さが生じない** — 一般論の自由度が、$\dim=1$ という具体的事実で潰れているからである。Examples 8.5 が「$\tilde\sigma_3,\dots,\tilde\sigma_9$ は canonical」と書いているのは、まさにこの事情の別表現と読める(**この読みは私の解釈・格 candidate**)。

### 2.2 どの主張がどの格の言明として立つか(白黒)

| # | 主張 | 舞台 | lift 依存 | 格 |
|---|---|---|---|---|
| 1 | $D_2=0$(全素数) | $\mathfrak{dg}^{\mathfrak m}_{12,2}$(**depth-graded**) | **無**(深さ 1 生成元は (1.6) で canonical) | **定理**(C-A・正典 (1.7) と一致) |
| 2 | $D_3=0$ | 同上 (12,3) | **無** | **定理**(Prop 4.3・機械で裏書き) |
| 3 | $D_4\ne0$(一般素数)・$=0$(691) | $\mathrm{gr}^D_4$ | **無**(補題 D-3) | **実測**(Brown (8.8) の機械検定) |
| 4 | $D_5,\dots,D_8$ の値 | **自由 Lie 環の斉次成分**($\mathrm{gr}^D$ の類ではない) | **無**(補題 D-3)だが**次数付き不変量でもない** | **実測**(canonical だが正典に対応物なし) |
| 5 | $D\equiv0\ (691)$ 全深さ | $\Lambda=\mathrm{Lie}_{\mathbb Z}(X,Y)_{12}$ | **無** | **実測**(正典保証は深さ 5 まで) |
| 6 | $A_{12}@p$ | 同上 | **無**(補題 C-3: 階数は正規化不変) | **実測** |

$$\Longrightarrow\ \boxed{\ \textbf{「lift 依存の量」は本測定に一つも無い。}\ \sigma_{11}\ \textbf{を使う量(重み }\ge14\textbf{)を測る段になって初めて lift 会計が要る。}\ }$$

### 2.3 ★ 格子の明示 — Brown の UNKNOWN に対する我々の答え

逐語ノート §2.3: 「合同がどの $\mathbb Z$ 格子上の言明かは論文未定義($\texttt{lattice}$ の出現ゼロ)= UNKNOWN」。本測定は**その格子を具体的に指定する**:

> $$\Lambda:=\mathrm{Lie}_{\mathbb Z}(X,Y)_{12}\ \ (\text{Lyndon 基底による }\mathbb Z\text{-格子}),\qquad
> D=\{\sigma_3,\sigma_9\}-3\{\sigma_5,\sigma_7\}\in\Lambda\otimes\mathbb Z_{(p)}$$
> **実測の帰結**(命題 D-3 経由):
> $$\boxed{\ 691\mid D\ \textbf{(}\Lambda\textbf{ の全座標で)}\ \wedge\ 677,\,701,\,998244353,\,2147483647\nmid\mathrm{content}(D)\ }$$

**限界の明記(過大主張の防止)**:
- 測定は $\bmod\ p$ のみ ⟹ **$v_{691}(D)\ge1$ は言えるが $=1$ は言えない**。
- 「$D$ の $\mathbb Z$ 上の内容が $691$ ちょうど」は**未測定**(他の素数は 4 本しか見ていない)。
- $\Lambda$ 係数の整性は $\sigma_m$ の $p$-整性(cert の `sigma_norm_ok` + $\dim\mathcal S_m=1$)から各素数ごとに従う。**全域的な $\mathbb Z$-整性は未確認**【D-GAP-2】。

### 2.4 ★「合同の延長」か「我々の lift の特殊性」か — **白黒: 合同の延長**

$$\boxed{\ \textbf{lift の特殊性ではない。lift の自由度がそもそも存在しない(補題 D-3)。}\ }$$
ゆえに深さ 6,7,8 での消滅は「$\tilde\sigma$ をうまく選んだから」ではなく、**canonical な元 $D$ についての事実** = Brown の合同式の深さ方向の延長である。
**残る条件つき**: 補題 D-3 は $\dim\mathfrak{grt}_m=1$(一次資料 pin)と $\mathcal S_m\leftrightarrow\mathfrak{grt}_m$ の**弱い同定**(直線としての一致)に依る【PIN-A-1】。この弱い形が崩れる可能性は低いが**未証明**。

---

## 3. 委嘱①(c) — 正札(claim card)起草

### 3.1 新規性 grep(実施記録)

| 検索 | 結果 |
|---|---|
| `provenance/CLAIMS.md` の `691` / `Ihara` / `深さ` | A 側・$A_{12}$・691 に関する既存主張 **ゼロ** |
| `docs/` の `回文` / `palindrom` | 3 件ヒットするがすべて **braid 語の回文性**(`bu_s35_embedding_v1.md`・`twin_witness_cv9_reading_v1.md`)および「撤回文」(`m2_family_identification_v1.md`)= **無関係** |
| `docs/` の `深さプロファイル` / `depth profile` | **自作 2 件のみ**(追補 C・`docs/状態.md`) |

⟹ **工房内に先行主張なし**。工房**外**の先行性は §3.2 の「非新規」欄で分離した。

### 3.2 正札 **A-691**(`provenance/CLAIMS.md` への提案行・採否は司令塔)

> **主張 A-691**: $y$-正規化された $\mathfrak{grt}$ 生成元 $\sigma_3,\sigma_5,\sigma_7,\sigma_9$ と Ihara 括弧 $\{f,g\}=D_f(g)-D_g(f)+[f,g]$($D_f(X)=0,\ D_f(Y)=[Y,f]$)について、$D:=\{\sigma_3,\sigma_9\}-3\{\sigma_5,\sigma_7\}\in\mathrm{Lie}(X,Y)_{12}$ は
> **(1)** $p\in\{677,701,998244353,2147483647\}$ で $D\not\equiv0$、その深さプロファイルは $4$ 素数すべてで**同一**の $\{4{:}350,\,5{:}744,\,6{:}858,\,7{:}744,\,8{:}350\}$(総 3046 項・台は深さ $[4,8]$)。
> **(2)** $p=691$ で $D\equiv0$ が**深さ 2〜12 の全域**で成立($A_{12}@691=1$、他 4 素数で $A_{12}=2$)。
> **(3)** プロファイルの回文性 $\#(D_d)=\#(D_{12-d})$ は定理(定理 D-1)、台 $[4,8]$ も定理(定理 D-2)であり、実測はこれらを通過した。

| 欄 | 内容 |
|---|---|
| **ID** | A-691 |
| **証拠** | `search/certs/aside2_prime_{677,691,701,998244353,2147483647}_v2_20260806.json`(e274278 / 844b88b)・独立 checker 全 PASS・fixture 表(追補 C §1.4)全通し・実装 `search/aside2_run_single_prime.py` |
| **二系統性** | 段 C′ の深さ 2 部分は**自前第二系統**(repo 非依存・整数演算)と一致。$H_k$ 次元 4 点(1,2,6,19)も第二系統で独立再現。**段 B′/段 E の重み 12 部分は単系統**(第二系統は $\sigma_7,\sigma_9$ を pentagon 無しには構成できない【C-GAP-3】) |
| **格** | ★ **candidate / partially cross-checked**(**verified ではない** — Lean 化ゼロ)。(2) の「全深さ」は **単系統実測** |
| **非新規部分(外部先行)** | $A_{12}@691=1$ **そのもの** = Brown 1301.3053 (8.8)+合同式(深さ 5 まで)・Furusho math/0011261 p.8(IMT)。深さ 2 の有理関係 = Ihara–Takao (1.7)。深さ 4 の 691 消滅 = (8.8) の $691/144$ |
| **★ 新規部分(canon 超過)** | **N-1** 合同の**深さ 6,7,8** への延長(正典保証は深さ 5 まで・裁定 709 ①)/ **N-2** 合同の**格子を具体化**($\Lambda$ と $y$-正規化 — 正典は UNKNOWN)/ **N-3** 一般素数での**深さプロファイル**(正典に対応データ無し)+ その回文性・台の**定理化** / **N-4** **近傍対照 677/701** による 691 特異性の実測分離(正典は近傍素数を論じない) |
| **限界** | $v_{691}(D)=1$ は未測定(§2.3)。$\mathbb Z$ 全域の整性は未確認【D-GAP-2】。SYN-0 への橋は未証明【D-GAP-1】(§4.3) |
| **日付** | 2026-08-06 |

---

## 4. 委嘱② — $k{=}12$ 判定表 v3(**再凍結**)

### 4.1 所与(A 側・実測済・§1.1)

$$\mathcal A_{12}@\{677,701,998244353,2147483647\}=2,\qquad \mathcal A_{12}@691=1$$
($\mathcal A_k$ の定義は `b_type_synthesis_design_v1.md` §2.1.2 = 「$\sigma_3,\sigma_5,\sigma_7,\dots$ が **Ihara 括弧で**生成する Lie 環の $k$ 次成分」。追補 C 命題 C-1 により、単純交換子版はこの定義を満たさない。)

### 4.2 ★ 命題 D-4(半連続性)— 不可能セルの確定

> $\mathcal S_{12}$ は、次数 12 の Lyndon 基底($\dim=\mathrm{Witt}(2,12)=335$)上の**整数係数線型系**($\theta$・$\tau$・pentagon)の核である。核の次元は法 $p$ で**下がらない**(階数は下がることしかない)。ゆえに $p\nmid$(系の分母)なるすべての $p$ で
> $$\dim_{\mathbb F_p}\mathcal S_{12}\ \ge\ \dim_{\mathbb Q}\mathcal S_{12}.$$
> さらに $\mathcal A_{12}\subseteq\mathcal S_{12}$(Ihara 括弧で $\mathcal S$ が閉じる)より $\dim_{\mathbb F_p}\mathcal S_{12}\ge\dim_{\mathbb F_p}\mathcal A_{12}$。
> **仮定** $\dim_{\mathbb Q}\mathcal S_{12}=\dim\mathfrak{grt}_{12}=2$(一次資料 pin + 模型の char-0 忠実性)**の下で**:
> $$\boxed{\ \dim_{\mathbb F_p}\mathcal S_{12}\ \ge\ 2\quad\textbf{(691 を含む全素数で)}\ }$$
> ∎(実装の $p\nmid6$ 分岐が分母の所在の傍証。)

> ### ⚠ blind への影響の申告(司令塔判断を仰ぐ)
> 命題 D-4 は**実測値の推測ではなく仮定つきの下界定理**だが、結果として「$\mathcal S_{12}@691=1$」を**不可能セルにする**(裁定 656 の判定表では live セルだった)。**blind の毀損と判断されるなら、本節を封じたまま判定表を運用してよい** — その場合 $(S{=}1)$ 行は「実測されたら仮定の破綻を意味する」行として読む。数学的内容は変わらない。

### 4.3 ★★ 判定表 v3 — **二層構造**(ここが v2 からの最大の変更)

$\mathcal A_{12}@691=1$ は**括弧格子 $M:=\mathbb Z\{\sigma_3,\sigma_9\}+\mathbb Z\{\sigma_5,\sigma_7\}$ の言明**である。SYN-0 が要求するのは**算術像という群の第 12 層** $\mathrm{gr}_{12}(H)$($H$ = Galois 像)である。両者の間には一段の橋がある:

> ### 【D-GAP-1】(新設・**判定表の要**)
> $L:=(\mathcal A_{12}\otimes\mathbb Q)\cap\Lambda$(飽和格子・階数 2)、$M\subseteq L$、実測は $691\mid[L:M]$ を意味する。
> - **読み (i)(工房の事前登録定義)**: $\mathcal A_{12}@p:=\dim_{\mathbb F_p}(M\bmod p\ \text{の像})$ ⟹ **$=1$ @691**。§2.1.2 の定義はこれ。
> - **読み (ii)(群論的)**: $\mathrm{gr}_{12}(H)$ は「$H\cap\gamma_{12}$ の元の先頭項の集合」であり、一般に**生成元の先頭項が生成する Lie 環より大きくなり得る**(古典的現象)。飽和まで大きくなるなら $\mathcal A_{12}@691=2$ で**段差は消える**。
> **どちらが正しいかは未証明。** SYN-0 の証明骨(層三角性・$\lvert\cdot\rvert=p^{\sum\dim}$)は読み (ii) を要求するが、$\mathcal A_k$ の定義(§2.1.2)は読み (i) で書かれている。**この不整合は本測定によって初めて可視化された。**

したがって判定表は**二層**で書く。

#### 層 1 — 格子言明(**橋を要しない・確定的**)

| $\mathcal S_{12}@\text{一般素数}$ | $\mathcal S_{12}@691$ | 層 1 の読み |
|---|---|---|
| 2 | 2 | $\dim\mathcal S$ は 691 で落ちない ⟹ **$691\mid[L:M]$ は $\mathcal A$ 側だけの現象**(= Brown 合同の格子論的内容の確認) |
| 2 | $\ge3$ | 691 で $\mathcal S$ が**跳ねる** ⟹ 線型系の階数が 691 で落ちる = **正典に対応物のない新現象**(S 側 691 合同) |
| $\ge3$ | 任意 | 一般素数で $\mathcal S$ が char-0 値 2 を超える ⟹ **模型の char-0 忠実性の破綻**か $\dim\mathfrak{grt}_{12}>2$ ⟹ **較正再検討**(S-ED-4 で 2 素数一致を先に確認) |
| 1 | 任意 / 任意 | 1 | **不可能**(命題 D-4 + $\mathcal A\subseteq\mathcal S$)⟹ 実装欠陥または仮定破綻 ⟹ STOP |

#### 層 2 — SYN-0 言明(**【D-GAP-1】の読み (i) を仮定した場合のみ**)

| | $\mathcal S_{12}@691=2$ | $\mathcal S_{12}@691\ge3$ |
|---|---|---|
| $\mathcal S_{12}@\text{一般}=2$ | ★ **$g(691)=1$ / $g(\text{一般})=0$** ⟹ **F_691 上でのみ SYN-0 の不等式が成立**。機構は **A 側の既知合同**(= 残響型) | $g(691)\ge2$ ⟹ 残響 **+ S 側超過** ⟹ 超過分は**真の信号成分** |
| $\mathcal S_{12}@\text{一般}\ge3$ | ★ **$g(\text{一般})\ge1$** ⟹ **A が char-0 自由値 2 のところで S が超過** ⟹ **真の B 型信号**(691 に依存しない) | 同左(かつ 691 でも超過) |

($g(p):=\dim\mathcal S_{12}@p-\dim\mathcal A_{12}@p\ \ge0$。)

### 4.4 ★ 判別条件 —「既知 A 低下の残響」vs「真の B 型信号」

> $$\boxed{\ \textbf{真の B 型信号}\iff \dim\mathcal S_{12}@p\ >\ 2\ \ \textbf{(=char-0 値)なる }p\ \textbf{が存在する}\ }$$
> $$\boxed{\ \textbf{既知 A 低下の残響のみ}\iff \dim\mathcal S_{12}@p=2\ \textbf{が全素数で成立し、段差は }691\ \textbf{でのみ }\mathcal A\ \textbf{の低下により生じる}\ }$$

**理由**: $\mathcal A_{12}@p\le2$ が常に成り立ち(自由値が上界)、一般素数では等号(実測)。ゆえに段差の原因は「$\mathcal A$ が 2 未満に落ちる」か「$\mathcal S$ が 2 を超える」の二択に**尽きる**。前者は 691 で実測済・**文献既知**。後者は**正典に対応物なし = 新規**。

**残響型の場合に言えること / 言えないこと**:
- 言える: 「$p=691$・class $\ge12$ の窓でのみ、$m\equiv0$ 層に $\mathrm{GT}^{\rm pent}\setminus\mathrm{GT}^{\rm arith}$ の余地がある(読み (i) の下で)」。
- **言えない**: 「B 型を発見した」「新しい算術現象を見つけた」「Deligne–Ihara に触れた」。

### 4.5 ★ 新規性会計の最終文言(裁定 709 ② の宿題)

> ### 凍結文(そのまま引用可)
> **本 ceremony が canon を超えるのは A 側の以下 4 点に限られる**: (N-1) 合同の深さ 6,7,8 への延長、(N-2) 合同の格子の具体化、(N-3) 一般素数での深さプロファイルとその回文性・台の定理化、(N-4) 近傍素数 677/701 による 691 特異性の実測分離。
> **$A_{12}@691=1$ という事実自体は canon 内**(Brown (8.8)+合同・Furusho/IMT)であり、**発見として書いてはならない**。
> **$\mathcal S$ 側が canon を超えるのは $\dim\mathcal S_{12}@p>2$ が出た場合のみ**。$\dim\mathcal S_{12}=2$ は $\dim\mathfrak{grt}_{12}=2$(一次資料 pin)の再現であり、**較正の成功**である。
> **SYN-0 の発火を主張するには【D-GAP-1】の読み (i) を明示的に仮定として書く**こと。仮定を書かずに発火を主張してはならない。

### 4.6 W-1 / SYN-1 への波及(条件つき)

読み (i) かつ残響型の場合:
- $k^*(p)>12$(一般素数)・$k^*(691)=12$ ⟹ **W-1 を満たす窓は $p=691$ に限られる**(class $\ge12$)。
- W-2($p>c$)は $691>12$ で自動的に満たされる ⟹ **Lazard 経路が生きる**。
- しかし規模は $\lvert P\rvert=691^{747}$(`b_type_synthesis_design_v1.md` §1.3 の $W(12)=747$)⟹ **構成不能**。
$$\Longrightarrow\ \textbf{「どこに開口部があるか」は決まるが、「作れる」ことは何も言えない。} \textbf{ SYN-1 の no-go(class}\le11\textbf{)は全 }p\textbf{ で不変。}$$

### 4.7 禁止文(v3・**追加 3 件**)

追補 C・裁定 708 ⑦の既存禁止文に加えて:
1. 「$\mathcal A_{12}@691=1$ を工房が発見した」— **canon 内**(§4.5)。
2. 「$\mathcal S_{12}@691=2$ だから SYN-0 が発火した」を**【D-GAP-1】の仮定を書かずに**述べること。
3. 「深さ 5,7 の非零は Prop 6.4 に反する」— **反しない**(命題 D-2′)。graded の言明を filtered の成分に適用する誤り。

---

## 5. IF-FIRST(S 側測定前・**値の推測を含まない**)

**内部無矛盾性の確認**(§1.6 の再発防止規律を適用): 以下の 5 項は互いに独立か、含意関係を明記した。$\mathcal S$ の**値**についての予言は 1 つも含まない。

| # | 凍結内容 | 種別 | 偽なら |
|---|---|---|---|
| **カナリア D-a** | $D$ の深さプロファイルは $d\leftrightarrow12-d$ 対称 | **定理**(D-1) | 実装欠陥 ⟹ STOP |
| **カナリア D-b** | $\mathrm{supp}(D)\subseteq[4,8]$ | **定理**(D-2) | 深さ 3 or 9 が非零 ⟹ Prop 4.3 の適用が破れる ⟹ 模型と $\mathfrak g^{\mathfrak m}$ の同定を疑う |
| **カナリア D-c** | $\dim\mathcal S_{12}@p\ \ge\ \dim\mathcal A_{12}@p$(全素数)= $\ge2$(一般)・$\ge1$(691) | **定理**($\mathcal A\subseteq\mathcal S$) | 違反 ⟹ `IMPOSSIBLE_CELL / STOP`(S-AS-4) |
| **カナリア D-d** | $\dim\mathcal S_{12}$ は 677, 701, 998244353, 2147483647 の 4 素数で**互いに一致** | 期待(S-ED-4 の拡張) | 不一致 ⟹ 実装欠陥 ⟹ STOP(**どの値かは問わない**) |
| **凍結 D-e** | §4.3〜4.5 の読み(層 1 / 層 2 / 判別条件 / 新規性文言)を**測定前の写像として固定** | 判定規約 | — |

---

## 6. 【GAP】台帳(更新)

| # | 内容 | 状態 |
|---|---|---|
| **【C-GAP-2】** | 合同の深さ 6 以上への伝播・整構造 | ★ **実測で決着(我々の格子上)**: 深さ 6,7,8 で消滅(§1.1)・格子は §2.3 で明示。**正典側の一般論としては依然 open** |
| **【D-GAP-1】**(新) | $\mathcal A_k$ の定義: 還元の張る空間(読み i)か $\mathrm{gr}_k(H)$(読み ii)か | ★ **open・判定表の要**(§4.3)。**SYN-0 の証明骨と §2.1.2 の定義の不整合** |
| **【D-GAP-2】**(新) | $\sigma_m$ の $\mathbb Z$ 全域整性・$v_{691}(D)$ の正確な値 | open(§2.3・過大主張の防止用) |
| **【PIN-A-1】** | $\mathcal S_k\leftrightarrow\mathfrak{grt}_k$ の同定 | 弱形(直線としての一致)で足りる(補題 D-3)。**強形は未証明** |
| **【C-GAP-1】** | $(2,-27)$ の $f_m$ 正規化 | open(測定に影響なし・低優先) |
| **【C-GAP-3】** | hexagon 単独では $\mathcal S_7$ を一意化できない | 否定的決着済(追補 C) |
| **【BSY-GAP-3】** | 自由性仮定 | 不変(本測定は自由性を仮定しない) |

---

## 7. 格と申告

- **定理 D-1・D-2・命題 D-2′・補題 D-3・命題 D-3・命題 D-4**: 証明つき。D-1 と $\omega(D)=-D$ は機械照合済。**Sol 未監査**。
- **正札 A-691**: candidate / partially cross-checked。**verified ではない**(Lean 化ゼロ)。
- **判定表 v3**: 測定前に凍結。**発効判定語(「発火」「$k^*$」「段差」「不均衡」)の使用は司令塔専権**。
- **$\mathcal S_{12}$ の値についての推測はゼロ**。命題 D-4 は仮定つき下界定理であり、blind 運用への影響を §4.2 末尾で申告した。
- **読んだ範囲**: `brown_prop64_lattice_verbatim_v1.md` 全文・aside2 cert 5 本・`aside2_run_single_prime.py`(段 B′/C′/E の該当部)・`b_type_synthesis_design_v1.md` §1.1–1.4・§2.1.2・LEDGER 裁定 708/709/711/712。**外部文献の直接参照はゼロ**(文献ゲート遵守)。
- **新規性 grep**: §3.1 に実施記録。工房内先行主張なし・工房外先行は §3.2 で分離(**§8 で縮約**)。

---

# 8. 正誤 **E-1** — 新規性会計の縮約(裁定 717・発案係 v2 の主張の検分)

**判定: 主張は正しい。採択し、精密化して適用する。**(2026-08-06 追記)

## E-1.1 検分 — 消滅の系譜(全 11 深さの経路と厳密性の階層)

$D$ の各深さ成分がなぜ消えるかを、**経路**と**厳密性の格**まで分けて棚卸しした。$D_d\equiv0$ の根拠は下から順に決まる(降下法)。

| $d$ | 消滅の種類 | 経路 | 根拠の格 |
|---|---|---|---|
| 2 | **有理**(全素数) | 定理 C-A(Ihara–Takao (1.7)) | 正典 + 工房の再導出 |
| 3 | **有理**(全素数) | 定理 D-2 (iii): $D\in\mathcal D^3$ の先頭成分に Prop 4.3(パリティ $12\not\equiv3$) | 正典 |
| 4 | mod 691 | (8.8): 深さ 4 成分 $=(691/144)e_{12}$、$691\nmid144$ | **正典** |
| 5 | mod 691 | 「propagates to depth five」= 深さ 4 消滅後の先頭成分にパリティ($12\not\equiv5$) | **正典**(導出は紙面になし) |
| **6** | mod 691 | **パリティは $12\equiv6$ で無強制**(逐語ノート §1.5 の導出値)・$\theta$ は自己鏡映($12-6=6$)で無効 ⟹ **無料経路なし** | ★ **実測のみ** |
| 7 | mod 691 | ★ **$\theta$ 鏡映**: 定理 D-1 より $D_7=-\theta(D_5)$ ⟹ $d{=}5$ から**自動** | 正典 + 定理 D-1 |
| 8 | mod 691 | ★ **$\theta$ 鏡映**: $D_8=-\theta(D_4)$ ⟹ $d{=}4$ から**自動** | 正典 + 定理 D-1 |
| 9 | **有理** | $D_9=-\theta(D_3)$ | 正典 + 定理 D-1 |
| 10 | **有理** | $D_{10}=-\theta(D_2)$ | 同上 |
| 11, 12 | **有理** | 定理 D-2 (i) の構成上界($\mathrm{supp}\subseteq[2,10]$) | 工房 |

> ### ★ 構造の一行要約(発案係の主張を精密化した形)
> **奇数深さは(下から順に)パリティが殺し、偶数深さは $\theta$ が $4\leftrightarrow8$ で対にする。$6$ は唯一の自己鏡映な偶数深さである。**
> ⟹ 降下は $d=2,3,4,5$ と進み、**$d=6$ で初めて正典の駒が尽きる**。$d=7$ 以降は $d\le5$ の鏡映で埋まる。

**厳密性の但し書き(発案係の主張に無かった精密化)**:
- $\theta$ 経路($d=7,8,9,10$)は **$\mathbb F_p$ 上で完全に厳密**(補題 C-2 は標数 $\ne2$ の Lie 環恒等式・格子問題を経由しない)。
- パリティ経路($d=3,5$)は **char-0 の消滅定理を mod 691 に適用**しており、Brown 自身の深さ 5 の一歩と**同じ未証明部分を継承**する(整形式が正典に無い =【C-GAP-2】の残余)。$d=3$ は有理消滅なのでこの問題を持たない。
- ⟹ **$d=7$ の「無料」は $d=5$ の正典の格をそのまま引き継ぐ**。$d=8$ の「無料」は $d=4$(= (8.8) 本体)の格を引き継ぐ。

## E-1.2 ★ 縮約後の **N-1′**(§3.2 の N-1 を置換)

$$\boxed{\ \textbf{N-1}'\ :\ \textbf{canon 超過は }\ v_{691}\bigl(D^{(6)}\bigr)\ \ge\ 1\ \textbf{ の \textbf{1 ビット}に尽きる。}\ }$$

- 旧 N-1「合同の深さ 6,7,8 への延長」は**過大**であった。$d=7,8$ は正典 + 定理 D-1 から**無料で従う** ⟹ 超過分ではない。
- 深さ $9$–$12$ は**有理消滅**(定理 D-2)ゆえ mod 691 の情報を 1 ビットも運ばない。
- 実測「$D\equiv0$ が深さ 2..12 全域」は依然**正しい報告**だが、その**情報内容**は $d=6$ の 1 ビット + 既知の再現である。

## E-1.3 ★ 同じ論法を **N-3** にも適用(自己整合のため)

$\theta$ 鏡映は一般素数のプロファイルにも効く。定理 D-1 より $\#(D_7)=\#(D_5)$, $\#(D_8)=\#(D_4)$ は**定理**であって観測ではない。

$$\{4{:}350,\ 5{:}744,\ 6{:}858,\ 7{:}744,\ 8{:}350\}\ \longrightarrow\ \textbf{独立な数は }(350,\ 744,\ 858)\ \textbf{の 3 つ}$$

さらに**深さ 4 の 350 は正典の対象の別表現**である: 補題 D-3 の同定の下で $D_4=-(691/144)\,e_{12}$(Brown (8.8))ゆえ、$D_4$ の台は **$e_{12}$ の Lie 語基底での台**にほかならない(正典は多項式表現 $\bar e_{12}$ を「118 項」と記す — 表現が違うので数値は比較できない)。

$$\boxed{\ \textbf{N-3}'\ :\ \textbf{プロファイルの新規部分は }(744,\ 858)\ \textbf{の 2 数}\ =\ \textbf{深さ 5, 6 の副先頭データ}\ }$$
（正典は $D$ の**先頭深さ成分しか**扱っておらず、副先頭成分に対応物を持たない。⟹ この 2 数は新データ。ただし補題 D-3 により canonical で、**新現象ではなく新測定値**。)

## E-1.4 縮約**されない**もの

| # | 内容 | 縮約されない理由 |
|---|---|---|
| **N-2** | 合同の格子の具体化($\Lambda=\mathrm{Lie}_{\mathbb Z}(X,Y)_{12}$・$y$-正規化) | 正典は格子を**一切定義していない**(逐語ノート §2.3: `lattice` 出現ゼロ)⟹ $\theta$ でも正典でも代替できない |
| **N-4** | 近傍素数 677/701 での $A_{12}=2$ | 正典は 691 のみを論じ、近傍素数の非退化を述べない ⟹ 「691 だけが特異」の**実測分離**は正典外 |

## E-1.5 RDV-1(発火済)への追加要請 — **1 ビットを正確な整数へ**

厳密有理計算が着弾すれば、E-1.2 の 1 ビットは**値**になる。以下 3 点を出力に含めることを要請する(判定文は書かない):

1. ★ **$v_{691}\bigl(D^{(6)}\bigr)$ の正確値**(N-1′ の中身そのもの)。
2. **$v_{691}\bigl(D^{(4)}\bigr)=1$ の確認** — (8.8) の係数 $691/144$($691\nmid144$)から**ちょうど 1** が期待される。$\ne1$ なら $e_{12}$ の原始性か補題 D-3 の同定を疑う ⟹ **(8.8) の係数水準の再現テスト**。
3. **各深さの内容 $\mathrm{content}(D^{(d)})$ の素因数分解**($d=4..8$)— 691 以外の素数が現れれば、それは**新しい番地の候補**(第 3 の特異素数)。現れなければ「691 のみ」が 5 素数の抽出検査から**全素数の言明**へ格上げされる。

## E-1.6 正札 **A-691** の文言差し替え(§3.2 の該当 2 欄)

> **★ 新規部分(canon 超過)**: **(N-1′)** $v_{691}(D^{(6)})\ge1$ の 1 ビット(深さ 7,8 は $\theta$ 鏡映で正典から無料・深さ 9–12 は有理消滅ゆえ情報ゼロ)/ **(N-2)** 合同の格子の具体化 / **(N-3′)** 一般素数の副先頭データ $(744,858)$(深さ 4 の 350 は $e_{12}$ の別表現)/ **(N-4)** 近傍素数 677/701 による 691 特異性の実測分離。
> **非新規**: $A_{12}@691=1$ 本体・深さ 2 の有理関係・深さ 4 の 691 消滅 — **加えて深さ 7, 8 の 691 消滅**(定理 D-1 の帰結)。

## E-1.7 起草者の所見

- 発案係 v2 の指摘は**私の定理 D-1 の帰結を私自身が新規性会計に適用し損ねた**もので、**正しい捕獲**である。定理を証明した直後に、その定理が自分の主張の一部を無効化することを確認していなかった(§1.6 の自己監査で挙げた「予言集合の内部無矛盾性を自分の補題で通す」規律の、**新規性会計版**の欠落)。
- **規律の追加提案**: 新規性会計にも同じ通し規律を適用する — **「自分が証明した定理で、自分の主張のどれが無料になるか」を主張凍結の前に 1 回通す**。本件はその第 1 例として台帳へ。
- 縮約後も **A-691 の学術的骨は保たれる**: 1 ビットとはいえ $d=6$ は**正典の駒が尽きる唯一の場所**であり、そこを機械が埋めたという構図は明快である。むしろ**主張が 1 点に絞られたことで検証可能性と防御力が上がった**。
