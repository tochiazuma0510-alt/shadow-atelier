# 実験 E-DIM5/6 — **実測結果**(裁定 640)

**状態札: `measured / candidate 格 / Sol 未監査(便 113)/ 窓ゼロ・GAP ゼロ・封印非接触 / 二素数一致 / 較正 4 項 ALL PASS / 予言は走行前に単独コミット済(cf5b9c6)`**

- 実行: 影工房 数学者(Claude / Opus 5)・2026-08-06
- 事前登録: `docs/notes/b_type_synthesis_design_v1_addendum_edim56_prediction.md`(commit **`cf5b9c6`**・**本実験より前**)
- cert(機械生成・手写しなし): **`search/certs/edim56_20260806.json`**
  SHA-256 `51c0e1ce2307c5c6b3c3dd839e5efbffd6fc7d97f68a16022cf4c3fc50817d80`
- 素数: `2147483647` と `998244353` の 2 本(S-ED-3)。**両者で全数値が完全一致。**

---

## 1. 較正(**先に通した** — S-ED-1)

| # | 量 | 期待 | 実測 | 判定 |
|---|---|---|---|---|
| 1 | $\dim\mathfrak t_k$($k=2..6$)= $\dim\mathrm{gr}_k(K(0,5))$ | 4, 10, 21, 54, 125 | **4, 10, 21, 54, 125** | ★ **PASS** |
| 2 | $\dim H_3,\dim H_4$ | 1, 1 | **1, 1** | ★ **PASS** |
| 3 | $\dim\mathcal S_3$(= D3-BLIND) | 1 | **1** | ★ **PASS** |
| 4 | $\dim\mathcal S_4$(= D4-POWER (b) の帰結) | 0 | **0** | ★ **PASS** |

> **較正 1 が特に重い**: $\mathfrak t=\mathrm{gr}(K(0,5))$ を「5 生成元 $T_0..T_4$ の自由 Lie 環 / Drinfeld–Kohno 2 次関係式の生成するイデアル」として一から構成し、$\mathrm{Witt}(3,k)+\mathrm{Witt}(2,k)=5,4,10,21,54,125$ を**次数 6 まで**再現した。$K(0,5)\cong F_3\rtimes F_2$ の構造予測と一致。**$\rho$・$j$・$\nu_k$ の実装がまとめて検証される。**

---

## 2. ★ 実測結果

| $k$ | $\dim L_k(x,y)$ | $\dim H_k$(hexagon 斉次) | $\dim\mathcal S_k$(hexagon∧pentagon) | $\dim\mathcal A_k$(算術側) | 差 |
|---|---|---|---|---|---|
| 3 | 2 | 1 | **1** | 1 | 0 |
| 4 | 3 | 1 | **0** | 0 | 0 |
| **5** | 6 | **2** | ★ **1** | 1 | **0** |
| **6** | 9 | **3** | ★ **0** | 0 | **0** |

### 2.1 予言の採点(IF-FIRST・`cf5b9c6` で凍結)

| 量 | 予言 | 実測 | 判定 |
|---|---|---|---|
| $\dim\mathcal S_5$(**採点対象**) | **1** | **1** | ★ **的中** |
| $\dim\mathcal S_6$(**採点対象**) | **0** | **0** | ★ **的中** |
| $\dim H_5$(参考) | 2 | 2 | 的中 |
| $\dim H_6$(参考) | 3 | 3 | 的中 |

$$\boxed{\ \textbf{4/4 的中(採点対象 2/2)。}\ }$$

---

## 3. 判定文(**裁定 640 の UNKNOWN 規律に従う**)

> ### 書いてよい文
> $$\boxed{\ \textbf{斉次(associated graded)水準で }\ \dim\mathcal S_k=\dim\mathcal A_k\ \textbf{が }k=3,4,5,6\ \textbf{で成立 ⟹ }k^*\ge7.\ }$$
> すなわち **命題 SYN-0 の判定条件($\exists k\le c:\dim\mathcal S_k>\dim\mathcal A_k$)は class $\le6$ では発火しない。**

> ### ★ 書いてはならない文(**逐語**)
> - 「class 5, 6 の窓に B 型は存在しない」— **命題 SYN-0/SYN-1 は candidate(単系統・Sol 未監査)**であり、これを断定に使えるのは便 113 の監査後。
> - 「$k^*=12$」「$k^*$ は 12 である」— **$k^*$ の上限は本実験では一切決まっていない**。$k\ge7$ は未測定であり、古典的知見(`b_type_synthesis_design_v1.md` §4)は**要文献確認 L-4** のまま。
> - 「NW(5,3) / NW(6,7) は空だと確認した」— 測ったのは**斉次次元**であって窓ではない。
> - 「$\mathcal S=\mathcal A$」— 測ったのは $k\le6$ の**次元の一致**であって、部分空間としての一致ではない($\mathcal A_k\subseteq\mathcal S_k$ と次元一致から等号は従うが、**$\dim\mathcal A_k$ の値そのものが【BSY-GAP-3】の自由性仮定に依存**する)。

### 3.1 非斉次留保の帰趨(**$k=6$**)

事前登録 §3 の ⚠ は「$\dim\mathcal S_6\ge1$ が出たら非斉次可解性が別途要る」というものだった。**実測は $\dim\mathcal S_6=0$** なので、真の解集合は「1 点 or 空」であり、**どちらでも自由度は 0**。⟹ **留保は不発火・$k=6$ の結論に影響しない。**($k=5$ は元から斉次で留保不要。)

### 3.2 副産物 — $H_k$ の列

$\dim H_k$($k=3..6$)= **1, 1, 2, 3**。hexagon **のみ**の斉次解空間の次元列であり、`hs_prop7_translation_v1.md` は $k\le4$ しか持っていなかった。**$k=5,6$ の 2, 3 は本実験が初出**(工房内 grep 済)。
pentagon が削る量は $\dim H_k-\dim\mathcal S_k=0,1,1,3$ — ★ **深さ 6 で pentagon は 3 次元まるごと削っている**(深さ 4 の「1 次元」より格段に強い)。**「PENT の情報量 = 深さ 4 の 1 次元」は深さ 4 に限った話であって、深さが上がると急速に強くなる**ことが実測になった。

---

## 4. 含意(candidate)

1. **合成標的としての class 5, 6 は降りる。** `b_type_synthesis_design_v1.md` §1.3 の規模表のうち $c=5$($\lvert P\rvert=7^{14}$)・$c=6$($7^{23}$)は、**作っても $\mathcal S=\mathcal A$ で B 型を含まない**(SYN-0 が candidate である限りの結論)。
2. **§3.1 の「窓ゼロの実験」が設計どおり機能した。** 群を 1 個も作らずに 2 つの class を落とした。**次に同じ手で $k=7,8$ を落とせる**(次数 7 は $\dim L_7(x,y)=18$、$\dim\mathfrak t_7=\mathrm{Witt}(3,7)+\mathrm{Witt}(2,7)=312+18=330$ — 5 文字の自由 Lie 環 $\dim L_7(5)=11160$ で、本実装の素直な延長で届く見込み)。
3. **交差窓ルート(§3.2 の $M=\mathrm{NW}(4,7)\cap K^{(7)}$)の相対的価値が上がった。** 冪零ルートは class 6 まで空と実測されたので、**層三角性を壊す (α) が唯一の近距離の的**という §1.5 の結論が強化された。
4. ★ **$\dim\mathcal A_k$ の自由性仮定【BSY-GAP-3】が唯一の弱点**として残る。$\mathcal A_k$ が自由より小さければ $k\le6$ でも差が出る。**L-4(文献)で $\dim\mathcal A_k$ の実際の値が分かれば、本実測はそのまま $k^*$ の判定になる。**

---

## 5. 実装の要点(再現性)

- $\mathfrak t_k=L_k(T_0..T_4)/I_k$。$I$ = 2 次の Drinfeld–Kohno 関係((R1) 非交差交換子 15 本・(R2) 三点関係 30 本)が生成するイデアル。対角 $D_i=t_{i,i+2}$ は球面関係 $\sum_{j\ne i}t_{ij}=0$ で $T$ に消去(5×5 の巡回行列 $I+S^{-2}$ が可逆)。$I_k=[L_1,I_{k-1}]$($k\ge3$)。
- 座標は **Lyndon 語係数**(Lyndon 基底の展開が単三角ゆえ $L_k\to\mathbb Q^{\mathrm{Witt}}$ は同型)。$\dim L_6(5)=2580$・$I_6$ の生成元 2850 本を法 $p$ で行簡約。
- $\rho:T_i\mapsto T_{i+3\bmod5}$、$j:x\mapsto T_0(=t_{12}),\ y\mapsto T_1(=t_{23})$、$\nu_k=\sum_{i=0}^4\rho^i$。
- $H_k$ は $\mathbb Q$ 上 Fraction で厳密に(次元 $\le9$)。$\mathfrak t$ 側のみ法 $p$(**素数 2 本一致・S-ED-3 遵守**)。
- **格の申告**: $\mathfrak t$ 側は**有理数厳密ではなく大素数 2 本での一致**。単一素数の結果を「厳密」とは書かない。⟹ 本結果の格は **`cross-checked candidate`(同一実装・二素数)**であり、**独立第二実装はまだ無い**(便 113 の監査点)。

---

**窓を 1 個も切っていない。GAP を起動していない。封印 3 量に触れていない。**
