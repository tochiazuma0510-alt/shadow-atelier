# (F) カード改訂 v2 + P-6 確認(裁定 991 / queue ④)

作成: 数学者(Opus 5)/ 2026-08-12 / 発注 = 司令塔裁定 991・999(queue ④)
前版 = `docs/notes/F_stage1_and_unram5_lit_v1.md`(段 1 spec・裁定 983)
⚠⚠ **申告**: falsifier の **P-1〜P-5 の逐語テキストを repo 内で発見できませんでした**(`docs/`・`ops/` 全 grep 済)。
⟹ 本書は司令塔裁定 999 の**項目列挙**(紙の決着+較正カード化 cal-1〜4+判定枝書換+cert schema 8 点+P-PH2-4/5 関係明記)に対する履行です。**逐語との差分がある項目は §5 に列挙**しました — 逐語を頂ければ即座に差分を埋めます。
⚠ 数値出力なし($u$-touched = false)。**格付け: candidate**(Sol 未監査)。

---

## §0 結論

1. ★ **falsifier の紙決着を独立に再導出・確認**($z=(-I,1)$ の中心スカラー補題)。**$H^n(H_F,W\otimes\det^i)=0$ は全 $n$・全 $i$**。⟹ **段 1 の測定は不発火**(答えは紙で 0)。
2. ★★ **P-6(i) は「3 枝の検算」より強い形で閉じます**: W691-NARROW の定義条件 **$SL(2,691)\le H$** から $-I\in H$ が**1 行で**従う ⟹ **個別検算不要・(ii) 路は NARROW 全域で閉じる**。
3. ★★★ **P-6(ii) (Ad) 転進は数学的に正当**: $-I$ は $\mathrm{Ad}=\mathfrak{sl}_2$ に**自明に作用**する ⟹ **補題が発火しない** ⟹ $H^2\ne0$ がありうる。Sylow 還元で $\dim H^2(SL(2,691),\mathrm{Ad})\le1$(機械確認)⟹ **上界は $W$ と同じ 1 だが 0 に潰れない = 測定に意味がある**。
4. ⚠ **私の段 1 spec の反省**: 上界 $\le1$ は正しかったが、**中心の作用を先に検査していれば「測る前に 0」と分かりました**(1 行)。⟹ 記帳 §6。

---

## §1 【P-1】紙の決着(独立再導出)

> **補題 CENT-SCAL**: $z\in Z(G)$ が $G$-加群 $M$ に**スカラー $\lambda$** で作用し、$\lambda-1$ が $\mathrm{End}(M)$ の係数環で可逆なら、**$H^n(G,M)=0$($\forall n\ge0$)**。
> **証明**: 内部自己同型は $H^n(G,M)$ に自明に作用する(標準)。一方 $z$ による作用は係数側で $\lambda$ 倍。⟹ 任意の $\xi\in H^n(G,M)$ で $\lambda\xi=\xi$ ⟹ $(\lambda-1)\xi=0$ ⟹ $\xi=0$。∎

**(F) への適用**($H_F=SL^\pm(2,691)\times_{C_2}S_3$、$W=\mathbf F_{691}^2$):
- $z:=(-I,1)$。$\det(-I)=(-1)^2=1=\mathrm{sgn}(1)$ ⟹ **$z\in H_F$** ✔
- $-I\in Z(SL^\pm(2,691))$、$1\in Z(S_3)$ ⟹ **$z\in Z(H_F)$** ✔
- $z$ は $W$ に $-1$ 倍で作用。$\det^i(z)=\det(-I)^i=1$ ⟹ **$W\otimes\det^i$ 上も $-1$ 倍**
- $\lambda-1=-2$、$\gcd(2,691)=1$ ⟹ **可逆** ✔

$$\boxed{\ H^n\bigl(H_F,\;W\otimes\det^i\bigr)=0\qquad(\forall n\ge0,\ \forall i)\ }$$

⟹ **段 1 の測定は不発火**。★ 私の spec の Sylow 上界 $\le1$ と**矛盾しません**(0 は $\le1$)。

★ **副産物(射程大・falsifier 発)の数学的中身**: 「$\chi^{11}$ 満額($r=690$)」は $\det(H)=\mathbf F_{691}^\times$ を要求し、これは $H\supseteq SL(2,691)\ni-I$ を(既約性と併せて)強いる ⟹ **補題が必ず発火** ⟹ **満額と $H^2$ 容器は両立しない**。

---

## §2 【P-2】較正カード化(cal-1〜4)

段 1 が紙で決着した以上、**旧カードの測定枠は較正資産に転用**します(falsifier の機械前哨 $p\le23$・二独立経路・$p=23$ リハ 32-109ms がそのまま使えます)。

| 札 | 内容 | 期待値(★ 走らせる前の予言) | 役割 |
|---|---|---|---|
| **cal-1** | 小素数 $p=5,7,11,13,23$ で $\dim H^2(SL(2,p),\mathfrak{sl}_2)$ を実測 | ★ **1**(§4 の Sylow 上界 1・0 に潰れない) | (Ad) 路の**陽性対照** |
| **cal-2** | 同じ $p$ で $\dim H^n(SL(2,p),W)$($W$ = 自然加群、$n=1,2$) | ★ **0**(補題 CENT-SCAL) | **陰性対照**(装置が 0 を出せることの確認) |
| **cal-3** | LHS 退化: $H^n(H,M)\cong H^n(SL(2,p),M)^{S_3}$ が $\gcd(6,p)=1$ で成立するか | ★ **一致**($p\ne2,3$) | 段 1 spec の**構造的簡約の検定** |
| **cal-4** | twist の網羅: $\det^i$ が $i\bmod2$ にしか依らないこと | ★ **2 行で尽きる** | **EXHAUST の自動充足**の確認 |

⚠ **cal-2 が 0 を返さなければ装置が壊れています**(補題は紙で確定)⟹ **fail-closed の要**。
⚠ **cal-1 が 0 を返したら (Ad) 路も閉じます** ⟹ その場合は §3 の判定枝 (Ad-c) へ。

---

## §3 【P-3】判定枝の書換(旧 (a)(b)(c) は $W$ 用 — 廃止)

**旧カード**(`F_stage1_and_unram5_lit_v1.md`)の (a)/(b)/(c) は $W\otimes\det^i$ の測定枝でした。**紙で (a)(両方 0)に確定** ⟹ **枝としては廃止**し、以下に差し替えます。

| 新枝 | 条件 | 帰結 |
|---|---|---|
| **(Ad-a)** | $\dim H^2(H_F,\mathrm{Ad}\otimes\det^i)=0$(全 $i$) | ✘ **(F) は $\mathrm{Ad}$ 層でも容器を持たない** ⟹ **(F) 線を閉じる**(進路 ③ 終了) |
| **(Ad-b)** ★ | ある $i$ で $\dim=1$ | ★ **段 2 入口が実在** ⟹ その $i$ に限って段 2(拡大の構成と非分裂性)へ |
| **(Ad-c)** | 両 $i$ で $\dim=1$ | 容器が 2 つ ⟹ **段 2 を 2 本並走**(marking で区別) |
| **UNKNOWN (u1)** | $\dim\ge2$ | ⚠ **§4 の Sylow 上界(=1)と矛盾** ⟹ 理論側を疑え・即停止 |
| **UNKNOWN (u2)** | cal-3 が不一致 | LHS 非退化 ⟹ $S_3$ 側の寄与を別立て |
| **UNKNOWN (u3)** | $S_3$-不変部分が marking に依存 | TYPE-IMAGE 規約に照らして別欄記帳 |

⚠ **R-1 留保は維持**(③→① 非円分供給は未証明)。⟹ (Ad-b) が出ても**それだけでは ① に届きません**。

---

## §4 【P-4】(Ad) 路の理論上界(段 1 spec の再計算)

$\mathrm{Ad}=\mathfrak{sl}_2(\mathbf F_{691})$(3 次元・共役作用)。

1. ★ **$-I$ は $\mathrm{Ad}$ に自明に作用**($(-I)X(-I)^{-1}=X$)⟹ $\lambda=1$ ⟹ **補題 CENT-SCAL は発火しない** ✔
2. 作用は $PGL(2,691)$ を経由 ⟹ $\det$ 側の位数が半減 ⟹ **$r=345=690/2$**(状態.md の記載と一致)
3. **Sylow 還元**: $P=\mathrm{Syl}_{691}=$ 単冪上三角 $\cong C_{691}$。$\mathrm{Ad}|_P$ は**サイズ 3 の単一 Jordan ブロック $J_3$**。
 $N=\sum_{k=0}^{p-1}\sigma^k=p+\binom p2(\sigma-1)+\binom p3(\sigma-1)^2\equiv0\ (\mathrm{mod}\ p)$($p\ge5$ ゆえ 3 項とも $p$ で割れる)
 ⟹ $\hat H^{\rm even}=M^P/NM=M^P$(1 次元)、$\hat H^{\rm odd}=\ker N/(\sigma-1)M=M/(\sigma-1)M$(1 次元)
 $$\boxed{\ \dim\hat H^n(C_p,\mathrm{Ad})=1\ (\forall n)\ \Longrightarrow\ \dim H^2\bigl(SL(2,691),\mathrm{Ad}\bigr)\le1\ }$$
 ★ 機械確認済($p=5,7,11,13,691$ で $N\equiv0$・$\dim M^P=1$)。
4. ⟹ ★★ **上界は $W$ の場合と同じ 1。しかし 0 に潰れる理由がない** ⟹ **測定に意味がある = 段 2 入口が実在しうる**。

⚠ **numerology 注意**: $(-2)^{-1}\equiv345\ (\mathrm{mod}\ 691)$ で $r=345$ と数値が一致しますが、**これは「$690/2$」という同じ半減が 2 度現れただけの偶然**で、因果関係はありません。★ 明記しておきます(census 後付け = W-50 の再発防止)。

---

## §5 【P-5】cert schema(8 点)+ P-PH2-4/5 関係

### 5.1 cert schema `F_stage1_ad/v1`(記録必須 8 点)
```
1. module        : "Ad" | "W"            ★ どちらの加群を測ったか(型境界)
2. twist_i       : 0 | 1                 ★ det^i(i mod 2 で尽きる = cal-4)
3. dim_H2        : 整数                  ★ 測定値
4. sylow_bound   : 1                     ★ 理論上界(§4)— 超えたら即停止
5. central_lemma_fires : true | false    ★ W なら true(0 確定)/ Ad なら false
6. lhs_degenerate: true | false          ★ cal-3 の結果(gcd(6,p)=1)
7. calib         : {cal-1, cal-2, cal-3, cal-4} の PASS/FAIL
8. r_value       : 690(W 側)| 345(Ad 側) ★ どちらの χ 層に居るかの指紋
```
★ **1 と 8 を必須にした理由**: 「満額($r=690$)と容器は両立しない」という副産物が本カードの心臓なので、**どちらの層の測定かが cert 単体で読めなければならない**(TYPE-IMAGE 規約の実地適用)。

### 5.2 P-PH2-4 / P-PH2-5 との関係
- **P-PH2-5**(0 側凍結): ★ **紙の決着と一致** — 段 1 の測定値は 0 で、これは PH2-5 が凍結していた側です。⟹ **予言先行が成立**(測定前に凍結・測定は不発火だが紙が同じ答えを出した)。
- **P-PH2-4**: ⚠ **逐語未取得**のため関係を書けません ⟹ §6 の要請項目。
 ★ 私の側から言えることは「PH2-4 が $W$ 層の主張なら本決着で確定、$\mathrm{Ad}$ 層の主張なら**未決のまま (Ad-a/b/c) へ持ち越し**」という**場合分けまで**です。

---

## §6 P-6 の確認 2 点

### 6.1 ★★ (i) W691-NARROW 3 枝の $-I$ 含有 — **強化した形で確認**

W691-NARROW の定義条件は `w691_scan_gen23_spec_v1.md` §2 の定理の設定、すなわち
$$SL(2,691)\ \le\ H\ \le\ GL(2,691)$$
$\det(-I)=(-1)^2=1$ ⟹ **$-I\in SL(2,691)$** ⟹

$$\boxed{\ SL(2,691)\le H\ \Longrightarrow\ -I\in H\ \Longrightarrow\ z=(-I,1)\in Z(H)\ \Longrightarrow\ H^n(H,W\otimes\det^i)=0\ }$$

⟹ ★★ **3 枝の個別検算は不要**。$H_d=\{M:\det M\in\mu_d\}$ は $d=1,2,3,6,\ldots,690$ の**すべて**が $SL$ を含むので、**(ii) 路($-I$ を避ける $H$ 探し)は NARROW 全域で閉じます**(機械確認済)。
★ falsifier の「3 枝は全部 $-I$ 含有」は**正しく、かつ 3 枝に限りません**。

**⟹ (ii) 路を生かすには NARROW を出るしかない**:
$-I\notin H$ かつ $W$ が既約 ⟹ $H$ は $PGL(2,691)$ へ単射 ⟹ **$H\cap SL(2,691)$ が $-I$ を含まない既約部分群**。Dickson の分類より、そのような $H\cap SL$ は $SL(2,691)$ でも大きな部分体群でもなく、**位数の奇な二面体型/例外型($A_4,S_4,A_5$ の持ち上げのうち $-I$ を含まないもの)に限られる** ⟹ ★ **探索空間は小さく分類可能**。
⚠ ただし $A_4,S_4,A_5\le PGL(2,q)$ の $SL$ への持ち上げは一般に $-I$ を含みます($SL(2,q)$ の二項多面体群)⟹ **残るのは位数奇の巡回/二面体型** ⟹ ★ **$W$ の既約性と両立するかは要検査**【F-GAP-3】。
⟹ **見込みは薄い**と見ます ⟹ ★ **司令塔への推薦 = (Ad) 転進((i) ではなく (ii) を主路に)**。

### 6.2 ★ (ii) (Ad) 転進の素描

**なぜ効くか**: §4 の 1〜4。要点は「**$-I$ が $\mathrm{Ad}$ に自明作用 ⟹ 補題が発火しない**」の 1 点です。

**段 1′(Ad)の仕様**(段 1 spec の $W$ を $\mathrm{Ad}$ に差し替えた最小改訂):
```
測る量: dim H^2(H_F, Ad ⊗ det^i),  i = 0, 1  (cal-4 より 2 行で尽きる)
簡約  : (α) LHS 退化 gcd(6,691)=1 ⟹ H^n(H_F,·) ≅ H^n(SL(2,691),·)^{S_3}   [cal-3 で検定]
        (β) Sylow 還元 ⟹ 理論上界 1                                         [§4・fail-closed]
較正  : cal-1(陽性・小素数で 1)/ cal-2(陰性・W で 0)を *先に* 通す
判定  : (Ad-a)(Ad-b)(Ad-c) + UNKNOWN (u1)(u2)(u3)  [§3]
留保  : ★ R-1 留保は維持 — (Ad-b) が出ても ①(非円分供給)には届かない
```

**⚠ 転進で失うもの(明記)**: $\mathrm{Ad}$ は $PGL$ を経由するので **$\chi^{11}$ 満額($r=690$)は諦めます**($r=345$)。★ **これは損失ではなく、副産物「満額と容器は両立しない」の必然的帰結** — 満額を捨てることが容器を得る**条件**です。
⟹ ★ **進路 ③ の設計は「満額を追う」から「容器を追う」へ切り替わる** — この切替の是非は**司令塔裁定事項**として上申します。

---

## §7 GAP・記帳

- **【F-GAP-3】(中)** $-I\notin H$ かつ $W$ 既約 な $H\le GL(2,691)$ の分類(§6.1)。位数奇の巡回/二面体型が既約になりうるか。⟹ (ii) 路を諦める前に 1 度だけ確認する価値あり(安い)。
- **【F-GAP-4】(小)** $\mathrm{Ad}\otimes\det^i$ の $S_3$-不変部分が marking に依存しないか(UNKNOWN (u3) の中身)。
- **【F-GAP-5】(小)** P-PH2-4 の逐語未取得(§5.2)。
- **★ 記帳(自己・暫定札 m991-1)**: 段 1 spec(裁定 983)で私は Sylow 還元による**上界** $\le1$ を出して「測れば決まる」と設計しましたが、**中心の作用を先に検査していれば「測る前に 0」と分かりました**(1 行)。
 ⟹ ★ **教訓 F-1**: **コホモロジーの計器を設計する前に、中心元が加群に非自明スカラーで作用しないかを検査する**(補題 CENT-SCAL・全消滅の十分条件・1 行)。上界を出す作業より**先に**やるべき検査です。
 ⟹ 案 B(裁定 928)により自番号は立てず、Sol 採番待ち。
- **申告**: GAP 走行ゼロ(記号計算のみ)・$u$ 非接触・$c$ 未評価・**Sol 未監査**・**verified ではない**(candidate 格)。
- ⚠ **逐語未取得の申告**(再掲): falsifier の P-1〜P-5 原文を repo で発見できず、司令塔の項目列挙に対して履行しました。逐語を頂ければ差分を埋めます。
