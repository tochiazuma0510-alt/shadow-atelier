# 読解ノート: 2008.00066 の x₁₃・c・φ・pentagon(逐語抽出・reader 2026-08-01)

状態: candidate(抽出のみ・判定なし)。発注 = 裁定 367(PB₄ ゲート停止の判定材料)。証拠画像はセッション scratchpad(p2008_*.png・150-300dpi 照合済)。

## 要点(逐語の索引)
1. **x₁₃ の定義本体 = (A.4)**(gate ノートの「(A.5)」は式番号ずれ・内容引用は正確): x₁₂:=σ₁²・x₂₃:=σ₂²・x₁₃:=σ₂σ₁²σ₂⁻¹。第二正規化 x₁₃=σ₁⁻¹σ₂²σ₁(p.16 Cor 2.8 証明・(A.6) 第 1 式と同値)。
2. **(A.5) = c の関係式**: c:=x₂₃x₁₂x₁₃=x₁₂x₁₃x₂₃=(σ₁σ₂)³=(σ₂σ₁)³・無限位数・PB₃ と B₃ の中心を生成。補助恒等式(Prop 2.3 前・Cor 2.8 で多用): x₁₃x₂₃=x₁₂⁻¹c・x₁₂x₁₃=x₂₃⁻¹c。
3. **x=x₁₂・y=x₂₃**(p.3/p.6・F₂=⟨x₁₂,x₂₃⟩≤PB₃ の暗黙同一視・PB₃≅F₂×Z で Z 因子 = ⟨c⟩)。⟹ x₁₃ の F₂ 成分は x₁₂⁻¹x₂₃⁻¹ 型(c 消去の向きで x₁₂⁻¹cx₂₃⁻¹ or x₁₂⁻¹x₂₃⁻¹c)。
4. **「c↦1」の明文条項は不存在**(全文走査)。ただし PB₃≅F₂×⟨c⟩ の射影が c を殺す(p.3 の同一視)。T̂(c)=c^{2m̂+1}(2.59)・T^{PB₃}(c)=c^{2m+1}N(2.29)・c の剰余位数は N_ord 定義に参加(Prop 2.3 ③)。
5. **(A.16)/(A.18)**: 5 写像 φ₁₂₃/φ₁₂,₃,₄/φ₁,₂₃,₄/φ₁,₂,₃₄/φ₂₃₄ の (x₁₂,x₂₃,x₁₃) 像の全表(本文参照)。c の像は非掲載(導出: φ₁₂₃(c)=x₁₂x₁₃x₂₃ 等・PB₄ 中心元ではない)。
6. **(2.4)**: N_{PB₃}=∩(5 本の φ⁻¹(N))・前提 N∈NFI_{PB₄}(B₄)。
7. **規約**: 合成 = 右から左(γ·η = g·h・p.45)・共役は σ⁻¹(·)σ 型(A.6)・GT 作用は f⁻¹(·)f 型((2.25)(2.26)・Remark 1.1 の y↦f̂⁻¹y^λ̂f̂)。
8. **pentagon 剰余版 (2.20)**: φ₂₃₄(f)φ₁,₂₃,₄(f)φ₁₂₃(f)N = φ₁,₂,₃₄(f)φ₁₂,₃,₄(f)N。hexagon 剰余版 (2.18)(2.19) も逐語収蔵(reader 報告原文)。

## 原文全文(v1.1 補完 — falsifier 指摘: 初版は宣言のみで逐語部が欠落していた・司令塔の収蔵ミス)

### §1 App A.1(x_{ij} の定義系)— PDF p.44
- **(A.1)** B_n 表示: ⟨σ₁,…,σ_{n−1} | σᵢσⱼσᵢ⁻¹σⱼ⁻¹ if |i−j|≥2, σᵢσᵢ₊₁σᵢσᵢ₊₁⁻¹σᵢ⁻¹σᵢ₊₁⁻¹ for 1≤i≤n−2⟩。PB_n = ker(ρ: B_n→S_n)・"This homomorphism sends the generator σᵢ to the transposition (i, i+1)"。braid 図の読み向きを定める条文は本文検索でヒットなし(Fig. A.1 に暗黙)= UNKNOWN。
- **(A.2)**: x_{ij} := σ_{j−1}…σ_{i+1}σᵢ²σ_{i+1}⁻¹…σ_{j−1}⁻¹
- **(A.3)**: PB_n 関係式(x_{rs}⁻¹x_{ij}x_{rs} の 4 分岐 — s<i or i<r<s<j / s=i / r=i<s<j / r<i<s<j)
- **(A.4)**: x₁₂:=σ₁², x₂₃:=σ₂², **x₁₃:=σ₂σ₁²σ₂⁻¹**(x₁₃ の定義本体)
- **(A.5)**: **c := x₂₃x₁₂x₁₃ = x₁₂x₁₃x₂₃ = (σ₁σ₂)³ = (σ₂σ₁)³**・"has an infinite order; it generates the center of PB₃ and the center of B₃."(§1 p.3 同文)
- **(A.6)**(300dpi 画像照合済): σ₁⁻¹x₂₃σ₁ = x₁₃, σ₂⁻¹x₁₂σ₂ = **x₂₃⁻¹x₁₂⁻¹c**, σ₂⁻¹x₁₃σ₂ = x₁₂(txt 版は崩れ・画像で確定)
- 第二正規化(p.16 Cor 2.8 証明): "Using the definition of x₁₃ := σ₁⁻¹σ₂²σ₁ = σ₁⁻¹x₂₃σ₁"
- **x=x₁₂・y=x₂₃**(p.3/p.6): "The F₂-factor is generated by the two standard generators x₁₂, x₂₃ … we tacitly identify F₂ … with ⟨x₁₂,x₂₃⟩ ≤ PB₃ … Occasionally, we denote the standard generators of F₂ by x and y."
- 補助恒等式(Prop 2.3 前・Cor 2.8 で多用): **x₁₃x₂₃ = x₁₂⁻¹c・x₁₂x₁₃ = x₂₃⁻¹c**

### §2 (A.16)/(A.18)・φ と c の扱い — p.48-49
- **(A.16)**: φ₁₂₃(h):=ou(id₁₂∘₁m(h))・φ₁₂,₃,₄(h):=ou(m(h)∘₁id₁₂)・φ₁,₂₃,₄(h):=ou(m(h)∘₂id₁₂)・φ₁,₂,₃₄(h):=ou(m(h)∘₃id₁₂)・φ₂₃₄(h):=ou(id₁₂∘₂m(h))
- **(A.17)**: φ₁₂/φ₂₃/φ₁₂,₃/φ₁,₂₃(PB₂→PB₃ 版)。Prop A.2: 9 写像は群準同型。
- **(A.18)** 全 15 値: φ₁₂₃:(x₁₂,x₂₃,x₁₃)↦(x₁₂,x₂₃,x₁₃)/ φ₂₃₄:↦(x₂₃,x₃₄,x₂₄)/ φ₁₂,₃,₄:↦(x₁₃x₂₃,x₃₄,x₁₄x₂₄)/ φ₁,₂₃,₄:↦(x₁₂x₁₃,x₂₄x₃₄,x₁₄)/ φ₁,₂,₃₄:↦(x₁₂,x₂₃x₂₄,x₁₃x₁₄)
- **(A.19)**: φ₁₂(x₁₂)=x₁₂・φ₂₃(x₁₂)=x₂₃・φ₁₂,₃(x₁₂)=x₁₃x₂₃・φ₁,₂₃(x₁₂)=x₁₂x₁₃
- **「c↦1」の明文条項は不存在**(全文走査)。(2.59): T̂(c):=c^{2m̂+1}・(2.29): T^{PB₃}_{m,f}(c)=c^{2m+1}N_{PB₃}・Prop 2.3③: N_ord の lcm に cN_{PB₃} の位数が参加。φ での c 像は非掲載(導出: φ₁₂₃(c)=x₁₂x₁₃x₂₃ 等)。

### §3 (2.4)/(2.5) 交叉式 — p.9
- **(2.4)**: N_{PB₃} := φ₁₂₃⁻¹(N) ∩ φ₁₂,₃,₄⁻¹(N) ∩ φ₁,₂₃,₄⁻¹(N) ∩ φ₁,₂,₃₄⁻¹(N) ∩ φ₂₃₄⁻¹(N)(前提 N∈NFI_{PB₄}(B₄))
- **(2.5)**: N_{PB₂} := φ₁₂⁻¹(N_{PB₃}) ∩ φ₁₂,₃⁻¹(N_{PB₃}) ∩ φ₁,₂₃⁻¹(N_{PB₃}) ∩ φ₂₃⁻¹(N_{PB₃})。Prop 2.2: N_{PB₃}∈NFI_{PB₃}(B₃)。

### §4 規約(合成・共役の向き)
- 合成(p.45): "their composition γ·η is the element … corresponding to g·h"(η 先・γ 後 = **右から左**)
- **(A.10)/(A.11)**: m(g)∈PaB(n)・i_k:=ρ(g)⁻¹(k)・m(g₁g₂)=ρ(g₂)⁻¹(m(g₁))·m(g₂)
- 共役: (A.6) は σ⁻¹(·)σ 型。(2.26): T^{B₃}(σ₂):=**f⁻¹(σ₂x₂₃^m)f** N。(2.25): T^{B₄}(σ₂):=φ₁₂₃(f)⁻¹(σ₂x₂₃^m)φ₁₂₃(f)N 等。Remark 1.1: x↦x^λ̂, y↦f̂⁻¹y^λ̂f̂。(2.58): T̂(x):=x^{2m̂+1}, T̂(y):=f̂⁻¹y^{2m̂+1}f̂。

### §5 pentagon/hexagon
- **(A.13)** 五角形(辺): 上経路 ((12)3)4→[id₁₂∘₁α]→(1(23))4→[α∘₂id₁₂]→1((23)4)→[id₁₂∘₂α]→1(2(34))・下経路 ((12)3)4→[α∘₁id₁₂]→(12)(34)→[α∘₃id₁₂]→1(2(34))。合成規約で **(id₁₂∘₂α)·(α∘₂id₁₂)·(id₁₂∘₁α) = (α∘₃id₁₂)·(α∘₁id₁₂)**。
- **(2.20)** pentagon 剰余版: **φ₂₃₄(f)φ₁,₂₃,₄(f)φ₁₂₃(f)N = φ₁,₂,₃₄(f)φ₁₂,₃,₄(f)N**(in PB₄/N)
- **(2.18)**: σ₁x₁₂^m f⁻¹σ₂x₂₃^m f N = f⁻¹σ₁σ₂(x₁₃x₂₃)^m N・**(2.19)**: f⁻¹σ₂x₂₃^m fσ₁x₁₂^m N = σ₂σ₁(x₁₂x₁₃)^m f N
- (A.14)/(A.15) hexagon 図の辺列も reader 原文どおり(β∘₁id₁₂ 系・転記は falsifier 判読時に照合済)。

※ 収載元 = 2026-08-01 reader 報告(150-300dpi 画像照合つき)。probe との等価性判定は本ノートの範囲外(falsifier 判読 = 裁定 370 で (α) 確定+Sol 監査点 F)。
