# A/B 判定計器 設計 vNext — 軌道機構 = $G_3$・発火の必要条件・rigidity gating

- 起草: 影工房 **数学者**(Claude / Opus 5)/ 2026-08-13
- 委嘱: 司令塔(裁定 1148)「(1) 軌道機構の定式化 (2) 共通商 $G_3$ 案の検討 (3) 前件・gating・**盲でないことの証明を先に**」
- **関連既存定理(委嘱様式・本稿が依拠するもの)**: 定理 **SPLIT-NULL** / 系 **SPLIT-NULL″**(`ihnec_v1.md` §6.4・裁定 374/388)/ 正典 **Cor 5.4**(fake = 有限証明書 1 個)/ **P-IHN-2**(`ihnec_v1.md` §6.5)/ **D-1**(`k5_w6_construction` §1.3: $\theta,\tau$ を $B_3/PB_3$ の作用と読むな)/ crown census(裁定 1041/1050)
- **規律**: u/c 非接触・封印 3 量非接触・prereg 非抵触($r=3$ という整数のみ)。数値は機械生成(§8)。

---

## 0. 結論(先に 4 行)

1. ★★ **軌道機構と $G_3$ 案は同一物である**。$G_3=PB_3/K^{(3)}$ は $S_3^3$ の指数 2 subdirect 積で、**座標巡回(=$\tau$)で不変**(機械確認)⟹ **$G_3$ は $\tau$-軌道束そのもの**。$[G_3,G_3]$ は**位数 27 の純 3 群**($r$ の住処と色が合う)。
2. ★ **SINGLE-BIT は「rigidity 判定」に変形する**: $\Theta_1$(dihedral 側)は**自動で rigid**、発火は **$\Theta_2$(S4 側)が $k\bmod3$ を決定するか**の一点に縮約される。
3. ★★ **盲でないことの必要条件を 3 つ証明した**(すべて SPLIT-NULL の直系・追加推測なし): **吸収を避ける**・**分裂を避ける**・**片側自明を避ける**。⟹ $E$ は $P\times G_3$ の**非分裂拡大**で、$V$ は**両因子が非自明に作用するテンソル型**でなければならない。
4. ⚠ **規模が最大の危険**: $\dim V=\dim V_P\cdot\dim V_{G_3}$ で $\lvert E\rvert=54432\cdot3^{\dim V}$。**$\dim V_P$ を先に測れ**(§6)。⟹ **推薦の第一手は構成ではなく在庫走査**(§6.1)。

---

## 1. ★ 型境界事故 **6 度目**の自己記帳(SPLIT-TWIN 輸入)

私は前便 §4 で `ribet_dig_campaign_v1_addendum_a.md` §3.3 の $E_p=C_p\rtimes_\psi(C_3\times S_3)$ を「$C_3\times S_3$ の $B_3$ 窓商」として輸入した。**false**:

$$\boxed{\ E_p\ \textbf{の }S_3\ \textbf{は}\ \textbf{外側商}\ B_3/PB_3\cong S_3\ \textbf{であり、純商 }PB_3/N\ \textbf{ではない。pure 部分は位数 }3p\ \textbf{(奇数)⟹ }S_3\ \textbf{へ全射不能。}\ }$$

⚠ **これは既存の明示警告に正面から抵触している**: `k5_w6_construction` §1.3 の **D-1**「$\theta,\tau$ を $B_3/PB_3\cong S_3$ の作用と読むな」(`bu_s35_embedding_v1.md` §7.2 が逐語引用)。また `r1_branch_retraction_v1.md` の **W-48 候補**「他文書の記述を、窓を確認せずに自窓の話として使うこと」にも該当。

| 系譜 | 内容 |
|---|---|
| 1 度目 | CHI-CARRY(切片 vs 表現) |
| 2 度目 | TYPE-IMAGE$^\rho$(型 vs 実像) |
| 3 度目 | B118-1(集合写像 vs 群準同型) |
| 4 度目 | 窓取り違え($N_{S4}$ の商曲線を $K^{(9)}$ と読む・`r1_branch_retraction_v1.md`) |
| 5 度目 | $B_3$ / $PB_3$ の取り違え(私・`c1p5_v2_diff_review_v1.md` §3) |
| ★ **6 度目** | ★ **外側商 $B_3/PB_3$ と純商 $PB_3/N$ の混同**(本件・SPLIT-TWIN 輸入) |

> **自分に課す追加規律**: **他文書から群を輸入するときは、まず「その群は $PB_3$ の商か $B_3/PB_3$ の商か」を 1 行で確認する。** 5 度目・6 度目は連続 2 便で同じ「窓の型」を外している。

---

## 2. 軌道機構の定式化(委嘱 1)

### 2.1 命題 ORBIT — **$\tau$-軌道束は $G_3$ に他ならない**

> ### 命題 ORBIT(candidate・機械確認つき)
> (a) $F_2\twoheadrightarrow S_3$ の全射核は **3 類**($\lvert\mathrm{Hom}^{\rm surj}\rvert=18$、$\lvert\mathrm{Aut}(S_3)\rvert=6$)。$\tau$ はこの 3 類を**巡回置換**、$\theta$ は **1 固定 + 互換** ⟹ $\langle\theta,\tau\rangle$ は 3 類に $S_3$ として作用 ⟹ **個別に $\theta\tau$-不変な核は 0**(便 128 実測)。
> (b) 3 核の交叉 $\tilde L:=L_1\cap L_2\cap L_3$ は $\theta,\tau$-不変であり、$PB_3/\tilde L$ は $S_3^3$ の subdirect 部分群。
> (c) ★ $PB_3/\tilde L\cong G_3=PB_3/K^{(3)}$(便 128: 位数 108・marked 同型)。
> (d) ★ **私の独立機械確認**: $\lvert G_3\rvert=108$、$[S_3^3:G_3]=\mathbf 2$、3 つの座標射影はすべて $S_3$ へ全射、**座標巡回($0\to1\to2\to0$)で $G_3$ は不変**。さらに $\lvert[G_3,G_3]\rvert=\mathbf{27}=3^3$(**純 3 群**)、$\lvert G_3^{\rm ab}\rvert=4$(便 127 の $\{4,16\}$ と整合)。

$$\boxed{\ \textbf{単独の }S_3\ \textbf{商は窓にならない。}\tau\textbf{-軌道を丸ごと束ねると窓になり、その束は正典の }K^{(3)}\ \textbf{である。}\ }$$

⟹ **委嘱 (1) の「軌道機構」と委嘱 (2) の「共通商 $G_3$ 案」は同一の対象である**。以下は統合して扱う。

### 2.2 ★ 色の一致(なぜ $G_3$ が正しい標的か)

| 量 | 値 | 意味 |
|---|---|---|
| $[G_3,G_3]$ | **位数 27 = 3³・純 3 群** | $r=3$ の住処と色が合う |
| $G_3^{\rm ab}$ | 位数 4(2 群) | 便 127 が殺した「可換 $C_3$ 商」はここに無い ⟹ **$G_3$ 橋は便 127 に抵触しない** |
| $\lvert GT(K^{(3)})\rvert$ | **12** $=2n_0\varphi(n_0)$, $n_0=3$ | $m\in\mathbf Z/6$ の 4 値 × $k\in\mathbf Z/3$ |

⚠ **前便 §3 の $Q_0=C_3\times S_3$($Q_0^{\rm ab}=C_6$)との関係**: $G_3^{\rm ab}$ は 2 群なので **$G_3\twoheadrightarrow Q_0$ は不可能**。しかし発火に必要なのは**窓群 $G_3$ が $Q_0$ を商に持つこと**ではなく、**shadow 群の Goursat 商が $Q_0$ になること**である。両者は水準が違う(§3 がその水準差を扱う)。**前便はこの 2 水準を混同していた** — §1 の型境界と同根の誤りなので明記する。

---

## 3. SINGLE-BIT の変形(委嘱 1 後半)

### 3.1 $G_3$ 橋のもとでの reduction 像

$K=K^{(l)}\cap N_E$($3\mid l$、$N_E\subseteq N_{S4}\cap K^{(3)}$)とする。$f$ の $G_3$-成分は両側から読めるので、shadow の対 $(t_1,t_2)$ が像に入る条件は

$$\Theta_1\bigl(R_1^{-1}(t_1)\bigr)\ \cap\ \Theta_2\bigl(R_2^{-1}(t_2)\bigr)\ \neq\ \emptyset,
\qquad \Theta_i:\ GT(\cdot)\longrightarrow GT(K^{(3)}).$$

- **$\Theta_1$ は自動で rigid**: $K^{(l)}\subseteq K^{(3)}$ ゆえ $\Theta_1=R_{K^{(9)},K^{(3)}}$ は $t_1$ の関数。**機械確認**: $GT(K^{(9)})\to GT(K^{(3)})$ は**全射**、各 fibre の大きさ **9**。
- ⟹ 制約は **$\Theta_2$ 側だけ**に懸かる。

> ### ★ 定理 vN-BIT(発火の必要十分条件・水準を固定した形)
> $$\boxed{\ \textbf{発火}\iff \Theta_2\ \textbf{が }t_2\in GT(N_{S4})\ \textbf{から }k\bmod 3\ \textbf{を決定する(rigid)}\ }$$
> 盲 $\iff$ $t_2$ の fibre 上で $\Theta_2$ の $k$-成分が $\mathbf Z/3$ 全体を走る。

**⟹ SINGLE-BIT は「屋根 972 の像を測る」問題から「$GT(N_E)\to GT(N_{S4})\times GT(K^{(3)})$ が graph 的か」という問題へ変形する。** 標的は $54\times12=648$ 対 ⟹ **屋根 972 の測定より安い**。

### 3.2 ★ 事前登録予言 P-vN-1(前件の破綻検出器)

$U$ 経由で既に共有される分を除くと、$GT(K^{(3)})$ の残り座標は $k\bmod3$(3 値)と $m$ 側の余り $C_2$(2 値)である。したがって素朴には指数 $\in\{1,2,3,6\}$ がありうる。しかし包含 (6) より $A_{\rm arith}\subseteq\mathrm{Im}R$、$\lvert A_{\rm arith}\rvert=324=972/3$ ゆえ

$$\boxed{\ \textbf{P-vN-1}:\ \lvert\mathrm{Im}R_{K,M}\rvert\in\{972,\ 324\}\ \textbf{のみ。}\ \mathbf{162}\ \textbf{や}\ \mathbf{486}\ \textbf{が出たら前件が破綻している。}\ }$$

⟹ $GT(K^{(3)})$ の $C_2$ 成分の一致条件は**自動でなければならない**。**これは走行前に凍結する予言であり、外れたら停止・報告**(値の先行計算はしない — 予言は上の 2 値のみ)。

---

## 4. ★★ 盲でないことの証明(委嘱 3・**候補より先に**)

$E:=PB_3/N_E$、$N_E\subseteq N_{S4}\cap K^{(3)}$。$G_3$ 可解・$P=\mathrm{PSL}(2,8)$ 非可換単純 ⟹ 共通商なし ⟹ Goursat より $PB_3/(K^{(3)}\cap N_{S4})\cong G_3\times P$。$V:=\ker(E\to P\times G_3)$ と置く。

> ### 定理 BLIND-vNext(3 つの脱落・すべて SPLIT-NULL の直系)
> 次のいずれかが起きれば **$\Theta_2$ は自由 ⟹ 計器は盲**(測定してはならない):
>
> | # | 脱落 | 条件 | 理由 |
> |---|---|---|---|
> | **(a) 吸収** | $K=K^{(l)}\cap N_E$ が $M$ の**真の細分でない** | $N_E=K^{(3)}\cap N_{S4}$ ちょうど | $K^{(9)}\subseteq K^{(3)}$ ゆえ $K^{(9)}\cap N_E=K^{(9)}\cap N_{S4}=M$。**$G_3$ が dihedral 因子に吸収される** |
> | **(b) 分裂** | $V=1$、すなわち $E\cong P\times G_3$ | 上と同値 | $PB_3/K$ が直積 ⟹ **定理 SPLIT-NULL** が適用 ⟹ 像は $m$-fiber の合併 |
> | **(c) 片側自明** | $V\neq1$ だが $V$ が $P$-自明 or $G_3$-自明 | $V$ が片方の因子の加群 | $E\cong P\times(G_3\text{-拡大})$ または $(P\text{-拡大})\times G_3$ ⟹ 再び直積 ⟹ **SPLIT-NULL** |
>
> ⟹ $$\boxed{\ \textbf{必要条件}:\ V\neq1\ \textbf{かつ }V\ \textbf{は }P\times G_3\textbf{-加群として}\textbf{両因子が非自明に作用する(テンソル型)}\ }$$
> すなわち $V\cong V_P\otimes V_{G_3}$($V_P\ne\mathbf 1$, $V_{G_3}\ne\mathbf 1$)を含む。

**⟹ この 3 つは Phase 2 / 2b / 2c の失敗をすべて説明する**:

| 便 | 選んだ $E$ | 脱落 |
|---|---|---|
| Phase 2 | $E=P$($N_E=N_{S4}$) | (a) 吸収($G_3$ 橋すら無い・$Q=1$) |
| Phase 2b | $E=2^6{\cdot}P$(**perfect**) | (b) 分裂($E$ perfect ⟹ 可解商なし ⟹ $Q=1$) |
| Phase 2c | $E=P\times C_3$ | (c) 片側自明(+ 便 127: $G_l^{\rm ab}$ に 3-part 無し) |

⚠ **証明できるのは必要条件のみ**。テンソル型を満たしても発火する保証は無い(**十分性は UNKNOWN**)。§3.1 の定理 vN-BIT が実際の判定式であり、それは**測るしかない**。

---

## 5. gating `entangled_roof_gate v2`(走行前の必須前件)

```
G-vN-0 : N_E ⊆ K^(3) ∩ N_S4 か                     … G3 橋の前提
G-vN-1 : K = K^(l) ∩ N_E ⊊ M か                    … ★吸収検査(定理 BLIND-vNext (a))
G-vN-2 : V := ker(E → P × G3) ≠ 1 か                … 分裂検査 (b)
G-vN-3 : V が P-非自明 かつ G3-非自明 か             … ★テンソル型検査 (c)  ← 本丸
G-vN-4 : Θ2 の rigidity                             … ★直接の発火判定(定理 vN-BIT)
         t2 ∈ GT(N_S4) を固定し、GT(N_E) の fibre 上で
         k mod 3 成分が単一値か(54 × 12 = 648 対の検査)
G-vN-5 : 規模ゲート |E| と |PB_3/K| が 8GB 内か      … ★§6 の警告
  ⟹ G-vN-1..4 のいずれかが false なら【測定を実行しない】(盲が定理として確定・偽測定ゼロ)
  ⟹ G-vN-4 は屋根測定より安い(648 対 vs 972 屋根)ので【必ず先に】置く
```

---

## 6. 次候補と規模 gating(委嘱 2 の実務)

### 6.1 ★ 推薦の第一手 — **構成ではなく在庫走査**

テンソル型 $V$ を構成すると規模が爆発する(§6.2)。**先に在庫を掃く**:

> **[vN-1] 在庫走査**: 既存の isolated 窓在庫($N_{S4}$ の細分・83 窓在庫・atlas)に対し **G-vN-0/1/2/3 を回す**。Sol の便 125 §1.1 の isolated refinement 列挙器がそのまま使える。
> **出力**: 「$N_{S4}\cap K^{(3)}$ の真の細分で、純商が $P\times G_3$ 上非分裂かつテンソル型のもの」の有無。
> **コスト**: 小(列挙 + Goursat 判定のみ・像を作らない preflight)。**空でも情報**(= その在庫にはテンソル型が無い、という登録済み陰性)。

### 6.2 ⚠ 規模 gating(構成路を採る場合・**先に測るべき最重要量**)

$$\lvert E\rvert=\lvert P\rvert\cdot\lvert G_3\rvert\cdot\lvert V\rvert=504\cdot108\cdot3^{\dim V}=54{,}432\cdot3^{\dim V},\qquad \dim V=\dim V_P\cdot\dim V_{G_3}.$$

> **[vN-2] 規模 gating(GAP 3 行・数分)**: $\mathrm{IrreducibleModules}(\mathrm{PSL}(2,8),\mathrm{GF}(3))$ と $\mathrm{IrreducibleModules}(G_3,\mathrm{GF}(3))$ の**非自明最小次元**を測る。
>
> | $\dim V_P$ | $\dim V_{G_3}$ | $\dim V$ | $\lvert E\rvert$ | 8GB 可否 |
> |---:|---:|---:|---:|---|
> | 1 | 1 | 1 | 163,296 | ○ |
> | 2 | 1 | 2 | 489,888 | ○ |
> | 7 | 1 | 7 | $1.19\times10^8$ | ⚠ **厳しい** |
> | 7 | 2 | 14 | $2.6\times10^{11}$ | ✗ **不可** |
>
> ⚠ **$P$ は完全なので $\mathbf F_3$ 上の 1 次元非自明加群を持たない**($V_P$ は $P^{\rm ab}=1$ を経由できない)⟹ **$\dim V_P\ge$(最小非自明 3-modular 既約次元)**。この 1 つの整数が路線の生死を決める。**測る前に候補を出してはならない**(3 連続の教訓)。

### 6.3 ⛔ 推薦しない(定理 BLIND-vNext で除外済)

$E=P\times G_3$ ちょうど(吸収 (a)+分裂 (b))/ $E$ perfect(分裂 (b))/ $E=P\times(\text{可解})$(片側自明 (c))/ $\mathrm{P\Gamma L}$ 系(前々便で機械確認済: $P$ への全射なし)。

---

## 7. 委嘱様式の提案(裁定 1148 の指示に沿って)

本稿は**関連既存定理名を冒頭 1 行に置く様式**で書いた。次委嘱でも同様にされたい:

| 次の委嘱に添えるべき定理名(私の見立て) | 理由 |
|---|---|
| **SPLIT-NULL / SPLIT-NULL″** | すべての盲目性判定の親定理 |
| **Cor 5.4**(fake = 有限証明書 1 個) | 探索の向きを間違えないため |
| **crown census 裁定 1041/1050**($\mathbf F_3^2$ の 4 直線) | $A_{\rm arith}$ の座標記述の正本 |
| **D-1**($\theta,\tau$ を $B_3/PB_3$ と読むな) | 型境界 5・6 度目の再発防止 |
| **命題 ORBIT / 定理 vN-BIT / 定理 BLIND-vNext**(本稿) | vNext 系の前件 |

---

## 8. 検算(機械生成の数値の出所)

`scratchpad/ab_entangle_coord.py` + inline(本便)

| 検査 | 出力 |
|---|---|
| $G_3$ の構造 | $\lvert G_3\rvert=108$・$[S_3^3:G_3]=2$・3 射影とも像 $S_3$(位数 6)・**座標巡回で不変 `true`**・$\lvert[G_3,G_3]\rvert=27$(**純 3 群 `true`**)・$\lvert G_3^{\rm ab}\rvert=4$ |
| $GT(K^{(3)})$ | 位数 **12**($=2n_0\varphi(n_0)$, $n_0=3$)・$m\in\{0,2,3,5\}$・$k\in\{0,1,2\}$ |
| $GT(K^{(9)})\to GT(K^{(3)})$ | **全射**・各 fibre **9** |
| $\mathrm{Hom}^{\rm surj}(F_2,S_3)$ | 18 本 ⟹ 核 **3 類**($18/\lvert\mathrm{Aut}(S_3)\rvert=18/6$) |
| $G_l\twoheadrightarrow S_3$(前便) | $l=9,27,36$ で各 **18** 本 |

**格付け**: §2(d)・§3.1 の全射性/fibre = **機械・単系統**(Sol 未監査)。§4 定理 BLIND-vNext = **paper-proof**(SPLIT-NULL の系・単系統)。§2(a)(b)(c) = 便 128 の引用。**verified ではない。** u/c・封印 3 量・prereg 非接触。

---

## 9. novelty grep 領収書(★ **概念語彙**で・自分の新規ファイルを除外した実測値)

| 主張 | grep(概念語彙) | ヒット | 判定 |
|---|---|---|---|
| 分裂屋根は検出しない | `SPLIT-NULL` | 279 | ★ **既出**(本稿は依拠・再発見しない) |
| $\tau$-軌道を束ねる構成 | `τ-軌道` / `軌道機構` / `束ねる` | 5 / 1(便 128) / 35 | ★ **既出**(`exploration_queue_candidates_v1.md` §88 に「θτ-軌道正規閉包」の札)⟹ **本稿の (c)(d) = $G_3$ との同定が増分** |
| **共通商を $G_3$ に取る** | `共通商 G_3` / `G_3 橋` | **0** | ★ **新規**(便 128 の示唆を定式化) |
| **テンソル型 $V$(両因子が非自明)** | `テンソル型` / `両因子が非自明` / `coupling.*module` | **0** | ★ **新規**(§4) |
| **rigidity 判定への変形** | `rigid` + `Theta` 系 | **0** | ★ **新規**(§3.1 定理 vN-BIT) |
| 外側商 / 純商の型区別 | `外側商` / `B_3/PB_3` | 33 | ★ **既出・明示警告あり**(D-1)⟹ §1 で自己記帳 |
| $\mathbf F_3^2$ の 4 直線 | `4 直線` / `crown` | 複数 | ★ **既出**(裁定 1041/1050)— 本稿は引用のみ |

---

## 10. 司令塔への回答(4 行)

1. **軌道機構 = $G_3$ 案**は同一物(命題 ORBIT・機械確認: $G_3$ は座標巡回で不変な $S_3^3$ の指数 2 subdirect 積・$[G_3,G_3]$ は純 3 群)。
2. **SINGLE-BIT は rigidity 判定に変形する**(定理 vN-BIT)。$\Theta_1$ は自動 rigid ⟹ **発火は $\Theta_2$ の一点**。標的は $54\times12=648$ 対で**屋根測定より安い**。予言 **P-vN-1**(像は 972 か 324 のみ・162/486 なら前件破綻)を凍結。
3. ★ **盲でないことの証明**: 3 つの脱落(吸収・分裂・片側自明)を**証明**した(すべて SPLIT-NULL の系)⟹ **$V$ はテンソル型**でなければならない。Phase 2 / 2b / 2c の 3 失敗がこの表で完全に説明される。**十分性は UNKNOWN**(正直に)。
4. ⚠ **次の一手は構成ではなく [vN-1] 在庫走査**(小コスト・空でも情報)。構成路を採るなら **[vN-2] 規模 gating を先に**($\dim V_P$ の 1 整数が路線の生死を決める)。**測る前に候補を出さない**。⟹ §1 に**型境界 6 度目**(外側商/純商)を自己記帳した。
