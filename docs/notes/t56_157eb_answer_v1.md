# 157eb rank-50 不整合の構造解読と最小拡張 seed 族の予言 — T-56 回答

**状態札: 数学者起草・司令塔検分前・Sol 未監査**
起草: Claude 数学者 / 2026-08-20 / 委嘱 = 司令塔(Sol `ops/express/20260820_sol_to_opus_157eb_result.md`)
入力: run 32317468871・receipt SHA `dfb0627849…`・独立 checker PASS。登録 104-seed(26 cube × 4 交換子型・深度 1)の affine span。**target 1〜5: rank 0**、**target6 = hexagon_1_coface_0: 33,293 eq / rank 50 / nullity 54・不整合**。classification = `unknown_not_obstruction`。
格: paper candidate。機械計算ゼロ。封印非接触。**B4-A / B4-B いずれも非宣言。**

---

## 0. 一行回答

**(1a) targets 1〜5 の rank 0 は紙から従う — 新事実ではなく補題 W(d) の当然の拡張である**(cube も交換子も $\Phi_3(H)=H^3[H,H]$ に入るので、$\mathbf F_3$-Frattini 層に線形化される標的には**恒等**)。**逆にこれは target6 が Frattini 層より深い層に住むことの証明になっている。**
**(1b) $50+54=104$ は seed span 上の rank-nullity ちょうど。33,293 は実装由来(生成方程式数)で不変量ではない。** 50/54 の内訳は「cube 側と相手側の $\mathrm{gr}_1$ での張る次元の積 − 双線型/反対称関係」と読むのが自然で、確定には FC-38 が要る。
**(2) ★ 予言 SEED-EXT: 不足は「深度」ではなく「被覆」である。** $\mathrm{gr}_2$ を**完全に張る** seed 族(= $\mathrm{gr}_1$ の完全基底に対する全 $[u_i,u_j]$ と全 $u_i^{[3]}$)で整合すると予言する。**深度 3(札 6)は不要**と予言 — これが札 6/札 8 の判別テストになる。
**(3) A 側**: branch-local ⟹ 段の fibre 空を主張するには base pair・D2-prefix の固定解除 + span の $\Lambda$ 全体化 + outside $g$(NA-5 なら Sylow 3 生成系)の全称化が必要。現状の `unknown_not_obstruction` は正しい格付け。
**(4) W-P0 (β) の最強形が観測された。W-FORM 弱形は「拡張族が交換子型に留まる限り」生存。cube 成分が入ると弱形は破れる**(重要な分岐)。

---

## 1. (1a) targets 1〜5 の rank 0 — 紙から従う

> **補題 SEED-Φ.** $\mathcal S\subseteq\Phi_3(H_{PB_3})=H_{PB_3}^{3}\,[H_{PB_3},H_{PB_3}]$ を任意の seed 集合とする。標的の係数加群が $H_{PB_3}/\Phi_3(H_{PB_3})$ の商に線形化されるなら、$\mathrm{span}(\mathcal S)$ のその標的への作用は**恒等(rank 0)**である。
> *証明.* $\Phi_3$ の元は定義により $H/\Phi_3$ で消える。線形化はこの商を経由する。∎

**適用**: 登録 seed は「cube」と「交換子」だけからなる。$g^3\in H^3\subseteq\Phi_3$ ✓、$[g,h]\in[H,H]\subseteq\Phi_3$ ✓ ⟹ **全 104 seed が $\Phi_3(H_{PB_3})$ に入る** ⟹ 補題 SEED-Φ より **Frattini 層の標的には rank 0** ✓✓。

**⟹ これは新事実ではなく、W-FORM カード補題 W(d)(「強形の補正は relative-Frattini-3 商で自明 ⟹ その層の残差を補正できない」)の、cube まで含めた当然の拡張である。** 予言どおりの観測であり、**装置の健全性の裏付け**として読むべきである。

> **★ 逆向きの帰結(こちらが本題)**: **target6 は rank 50 ≠ 0 なので、その係数加群は $H_{PB_3}/\Phi_3(H_{PB_3})$ の商では「ない」。**
> ⟹ **target6 は Frattini 層より真に深い層に住む**(Jennings/Zassenhaus 濾過で 1 段以上下)。これは測定から**証明として**従う。⟹ **FC-37**(target6 の層と係数加群の同定)。
> ⟹ 「深度 1 seed が 5 標的に恒等」は**新事実ではない**が、「target6 だけが深い」ことは**本 run が初めて確定させた構造情報**である。

---

## 2. (1b) 数 50 / 54 / 33,293 の解釈

- **$50+54=104$** ✓ = seed span の rank–nullity。⟹ **104 seed は線形独立**(span がちょうど 104 次元)であり、rank 50 = target6 への像の次元、nullity 54 = target6 に対して死ぬ seed の空間 ✓。
- **33,293**: 語 replay から生成された方程式の本数。$33293=13^2\cdot197$ で構造的な数に見えず、**実装由来のアーティファクト**と判定する。**不変量は rank/nullity の方**であり、receipt ではそちらを一次データとして扱うべきである。
- **50 の内訳(候補解釈・要確定)**: seed が「cube $u_i^3$ × 相手 $v_j$」の交換子型なら、その像は $\mathrm{gr}$ の中で $[\bar u_i,\bar v_j]$ が張る。$\{\bar u_i\}$ が $a$ 次元、$\{\bar v_j\}$ が $b$ 次元を張るなら
$$\text{rank}\ \le\ a\cdot b\ -\ (\text{反対称・Jacobi による重複}).$$
$26\times4=104$、$a=13,\ b=4$($26$ 本の cube が $13$ 次元を張る = 2:1)なら $ab=52$、そこから 2 を引いて **50** — **整合的だが確定していない**。⟹ **FC-38**($\mathrm{gr}_1$ における 2 つの span の次元を実測)。nullity 54 はこの双線型関係の空間として自動的に説明される。

---

## 3. (2) ★ 最小拡張 seed 族の予言(事前登録形)

### 3.1 理論的根拠 — 不足は「深度」ではなく「被覆」

$H_{PB_3}$ の Jennings/Zassenhaus 濾過 $D_1=H$, $D_2=[H,H]H^3$, … に対し、$\mathrm{gr}=\bigoplus D_i/D_{i+1}$ は**次数 1 で生成される制限 Lie 代数**である。従って
$$\mathrm{gr}_2\ \text{は}\ \{[\bar u,\bar v]\}\ \text{と}\ \{\bar u^{[3]}\}\ (\bar u,\bar v\in\mathrm{gr}_1)\ \text{で張られる}.$$
⟹ **$\mathrm{gr}_1$ の完全基底に対する全交換子と全 3 乗を seed に取れば、$\mathrm{gr}_2$ は完全に張られる。** 現行の 104 seed は「26 cube × 4 型」で、相手側が 4 型に限られている ⟹ **被覆が不完全である可能性が高い**(§2 の $b=4$)。

> ### 予言 **SEED-EXT**(事前登録)
> **段階 A(第一予言)**: seed 族を $\{[u_i,u_j]\}_{i<j}\ \cup\ \{u_i^{[3]}\}_i$($\{u_i\}$ は $\mathrm{gr}_1(H_{PB_3})$ の**完全基底**の持ち上げ)へ拡張する。
> **予言 A-1**: target6 の rank は $\dim\mathrm{gr}_2$ まで上がる(現行 50 から真に増える)。
> **予言 A-2**: **この段階で系は整合化する**(RHS が像に入る)。
> **段階 B(A-2 が外れた場合の第二予言)**: 次数 3 を足す。自由 Lie 代数の Witt 次元 $\dim L_3(r)=\frac13(r^3-r)$ より、生成元 2 本なら $[\xi,[\xi,\eta]],\ [\eta,[\xi,\eta]]$ の **2 本**、一般に $r$ 本なら $\frac13(r^3-r)$ 本。
> **予言 B-1**: 追加 $k$ 本で rank は**高々 $k$** 上がる。整合化に必要な最小 $k$ は、不整合が「RHS が像の外」という**1 事実**なので**理論上 1 本**で足りうる ⟹ **最小族の大きさを 1〜2 本と予言**。

### 3.2 検証手続き(receipt 直読可能な述語)

| 予言 | receipt の読み方 | PASS | FAIL |
|---|---|---|---|
| **A-1** | 拡張 run の target6 `rank` 字段 | rank > 50 かつ $=\dim\mathrm{gr}_2$(FC-38 で別途算出) | rank が 50 のまま ⟹ 被覆解釈が誤り |
| **A-2** | target6 の `consistent` / `classification` 字段 | 整合(lift 発見) | 不整合継続 ⟹ 段階 B へ |
| **B-1** | 次数 3 を $k$ 本足した run の `rank` | rank が $\le k$ だけ増え、ある $k\le2$ で整合 | $k$ を増やしても rank 不変 ⟹ 予言棄却 |

### 3.3 棄却条件(明示)

- **段階 A で rank が 50 のまま** ⟹ **被覆解釈(§3.1)を棄却**。その場合 target6 の層は $\mathrm{gr}_2$ ではなく更に深く、深度 3 以上が本質(⟹ 札 6 側)。
- **完全 $\mathrm{gr}_2$ でも不整合、かつ次数 3 の全基底でも rank 不変** ⟹ **Lie 型 seed では動かない** ⟹ 非 Lie 型(非交換子・非冪)の seed が要る ⟹ **W-FORM 弱形の破れ**(§5)。
- **段階 A で整合** ⟹ 予言 A-2 的中。**札 6(深さ 3 が本質)は棄却**、**札 8(軌道外 seed)は「被覆不足の別名」に格下げ**。

### 3.4 deck 札 6 / 札 8 の screening(生きたデータで)

| 札 | 判定 |
|---|---|
| **札 6 SOULE-BAND**(深さ 3 = 最初の自由不変量) | **予言により否定側に賭ける。** §3.1 の生成論から、不足はまず次数 2 の**被覆**として説明されるべきで、深度 3 が必要になるのは段階 A が外れたときだけ。⟹ **段階 A の結果が札 6 の生死を直接決める**(条件付き保留) |
| **札 8 VERSCH**(導手 = 軌道外 seed が要る最初の段) | **現象は観測された ✓**(登録 span で足りない)。**しかし原因の解釈は「軌道外」ではなく「次数 2 の被覆不足」と予言する。** 段階 A で整合するなら札 8 は**格下げ**、A が外れて B で整合するなら札 8 は**部分的に生存**(「軌道外」= 深度 3 の意味で) |

---

## 4. (3) A 側読みの併記(義務)

本不整合は **branch-local** である:固定 base pair × 固定 D2-prefix × 登録 104-seed span の内部の話であり、**段の fibre が空であることを一切主張しない**。

> **一行の全称化条件**: この不整合が **OBS★ 型の段障害証明書**(= fibre 空)へ転化するには、**(i) base pair と D2-prefix の固定を外し、(ii) seed span を補正領域 $\Lambda$ **全体**へ広げ、(iii) 屋根像が $A$ の外にある**全ての** $g\in\mathrm{ML}(H)$(NA-5 により Sylow 3-部分群の生成系で足りる)にわたって全称化し、(iv) side gate 込みで走査宇宙の完全性証明書を付す — この 4 条件をすべて満たしたときに限る。

**規律の確認**(solver-candidate philosophy): 探索器は候補発見器であり、**陰性を主張するときだけ登録レジーム**に入る。現 run は陰性を主張していない(`unknown_not_obstruction`)⟹ **格付けは正しい**。T-29 手戻り防止ルール 5(有限 slice の全滅を段の全滅と読まない)にも整合。

---

## 5. (4) W-P0 / W-FORM との整合記録

- **W-P0 (β) の最強形が観測された** ✓。W-P0 は「Frattini 層で真に補正が必要なら受理補正は強形($[H_{PB_3},H_{PB_3}]$ の元)ではあり得ない」だった。157eb はさらに強く、**$\Phi_3(H_{PB_3})$ に入る深度 1 span 全体**(交換子 + cube)が targets 1〜5 で無力であることを示した ⟹ **W-P0 と矛盾せず、その定量的裏付け**である。
- **W-FORM 弱形($PB_3^{ab}$ 像ゼロ)の生存 — 条件付き ✓**:
 - 拡張族が**交換子型に留まる**なら($[u_i,u_j]$、$[\xi,[\xi,\eta]]$ 等)、いずれも交換子ゆえ $PB_3^{ab}$ で消える ⟹ **弱形は生存**、FC-32 / LT-1 (P4) の自動性も保たれる ✓。
 - **しかし cube 成分 $u^{[3]}$ を実際に使う必要が出た場合**、$u^3$ の $PB_3^{ab}$ 像は $3\bar u\ne0$($\bar u\ne0$ のとき)⟹ **弱形は破れる** ⟹ FC-32 / LT-1 (P4) は**自動性を失い個別検査に戻る** ✗。
 > **⟹ 監視項目(新規)**: 拡張 run で受理された補正に **cube 成分が入っているか**。入っていれば LT-1 の前件 (P4) を再検査すること。⟹ **FC-40**。
 (なお現行 104 seed の「26 cube × 4 交換子型」が「cube の交換子」であれば全て交換子型 ⟹ 弱形は無傷。**この読みの確定も FC-40 に含める。**)

---

## 6. 新規の有限検査

| 番号 | 検査 | 重要度 |
|---|---|---|
| **FC-37** | target6 = `hexagon_1_coface_0` の**層と係数加群**の同定(Jennings 濾過での次数)。rank 50 ≠ 0 から「Frattini 層より深い」は確定済み ⟹ どの次数かを特定 | **最重要**(段階 A/B の分岐点) |
| **FC-38** | $\dim\mathrm{gr}_1(H_{PB_3})$、$\dim\mathrm{gr}_2$、および現行 seed の cube 側/相手側が $\mathrm{gr}_1$ で張る次元 $a,b$。**予言 $ab-(\text{関係})=50$ の検算** | 高(50/54 の確定) |
| **FC-39** | **段階 A の実走**:$\mathrm{gr}_1$ 完全基底に対する全 $[u_i,u_j]$ と全 $u_i^{[3]}$ で target6 を再走 ⟹ rank と整合性 | **決定的**(SEED-EXT の判定・札 6/8 の判定を兼ねる) |
| **FC-40** | 受理補正に cube 成分が入るか(= W-FORM 弱形の生死)。および現行 104 seed の型の確定 | 中(LT-1 (P4) の自動性に直結) |

---

## 7. 申告

- 手計算で検証: 補題 SEED-Φ、$g^3,[g,h]\in\Phi_3$、$50+54=104$、$33293=13^2\cdot197$、Witt 次元 $\dim L_3(2)=2$、$\mathrm{gr}$ が次数 1 生成であること、cube の $PB_3^{ab}$ 像 $=3\bar u$。
- **50 の内訳($a=13,b=4$)は候補解釈であり確定していない**(FC-38)。
- **UNKNOWN**: FC-37/38/39/40。SEED-EXT の段階 A の成否。札 6/8 の最終判定(段階 A 待ち)。
- 本 run は陰性を主張していないので、**A 側にも B 側にも一歩も動いていない**。`unknown_not_obstruction` の格付けを支持する。
- **B4-A / B4-B いずれも宣言していない。**
