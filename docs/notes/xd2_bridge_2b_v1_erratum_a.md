# xd2_bridge_2b_v1 正誤 A — §4.1「det=±1 群型に確定」の**撤回**と Ribet 標的での回復(裁定 821 / B-3)

**日付**: 2026-08-12 / **起草**: 数学者(Opus 5) / **状態**: candidate(Sol 未監査・判定語の発効は司令塔専権)
**入力**: `docs/notes/ben114_preflight_audit_v1.md` B-3(falsifier・GAP 実測)

---

## §1 撤回(誤りの型)

`xd2_bridge_2b_v1.md` §4.1 の
> 「⟹ (C) 道の作用群は $\det=\pm1$ 群型に絞られる」

を **撤回**する。

**誤りの型 = 非網羅な場合分け**。私は $SL(2,691)$ と分裂トーラス正規化群の 2 候補を排除したのち「**残る候補**」と書いたが、**候補の網羅を証明していなかった**(「$\det=\pm1$ 群型」は私が思いついた 3 番目の例にすぎない)。

**falsifier の反例(独立に検算・正)**: $GL(2,7)$。
| 検査 | 値 |
|---|---|
| $\lvert GL(2,7)\rvert$ | $2016$ |
| $(2,3)$-生成 | ✔(GAP 全元対判定) |
| $\det$ の像 | $\mathbf F_7^\times=C_6$ ⟹ **$\det=\pm1$ ではない**が $\mathrm{ord}(\det)\mid6$ ✔ |
| 生存道型の元 | 非分裂トーラス $\mathbf F_{49}^\times\cong C_{48}$ の位数 8 の元 $x$。固有値 $\{x,x^7\}=\{x,x^{-1}\}$、$\det=N(x)=x^{1+7}=x^8=1$ ✔、$r=8>6$ ✔ |
| 必要条件 ① | $p=7$, $r=8$, $p+1=8$ ⟹ $p\equiv-1\ (8)$ ✔(**非分裂側**) |

⟹ **$\mathrm{ord}(\det H)\mid6$ ∧ 非可換 ∧ $(2,3)$-生成**が SEC-MOD から出る全て。$SL(2,q)$ の除外(対合が中心 $-I$ のみ)は**正**だが、それは 1 個の除外にすぎない。

> ### ★ 訂正後の (C) 道の走査宇宙
> $$\boxed{\ \mathrm{ord}(\det H)\mid6\ \wedge\ H\ \text{非可換}\ \wedge\ H\ \text{は }(2,3)\text{-生成}\ \wedge\ \exists t\in H:\ \text{固有値 }\{\chi,\chi^{-1}\},\ \mathrm{ord}(\chi)=r>6\ }$$
> **実装警告(falsifier・必記)**: $(2,3)$-生成の判定は**全元対**で行う。**共役類代表対では偽陰性**(8/8 で FALSE を返した実走あり)。

---

## §2 ★★ Ribet 標的($p=691$, $r=690$)では**より強い絞りに回復する**

一般命題は弱まったが、**標的を固定すると絞りは戻る**(しかも今度は網羅的に)。

### 2.1 トーラスの型が確定する

$\{\chi,\chi^{-1}\}$ が $\mathbf F_p$ 上 Galois 安定 ⟹ **分裂**($r\mid p-1$)か**非分裂**($r\mid p+1$)。
$$p=691:\quad p-1=690\ \ (690\mid690\ \checkmark),\qquad p+1=692\ \ (690\nmid692)$$
$$\boxed{\ \Longrightarrow\ \textbf{Ribet 標的では非分裂は不可能 — }t\ \textbf{は分裂トーラスの元で }\mathbf F_{691}\ \textbf{上対角化可能}\ }$$
★ **$GL(2,7)$ の反例は非分裂側の現象であり、この機構は Ribet 標的へは移らない。**

### 2.2 Dickson 分類による網羅(今度は網羅を明示する)

$H_0:=H\cap SL(2,691)$ とおく。$\det$ で $H/H_0\hookrightarrow\mathbf F_p^\times$、$\mathrm{ord}(\det H)\mid6$ ⟹ $[H:H_0]\mid6$。$t\in H_0$(∵ $\det t=1$)で $\mathrm{ord}(t)=690$。

**$SL(2,q)$($q$ 奇素数)の部分群の分類**(Dickson・**古典的**・【要 pin】): (a) 巡回 (b) dicyclic(二面体の引き戻し) (c) $SL(2,3)$・$SL(2,5)$・二項八面体群 $\hat S_4$(位数 24, 120, 48) (d) Borel 型 $C_q\rtimes C_m$($m\mid q-1$) (e) 部分体型 $SL(2,q_0)$ — $q=691$ 素数ゆえ $SL(2,691)$ のみ。

- **(c) は排除**: 位数 $\le120<690$ ⟹ 位数 690 の元をもてない。
- ★ **(d) Borel 型は RIBET-SECTION で死ぬ**: $U\cong C_{691}$ は $H_0$ の**唯一の** Sylow 691 ⟹ $H_0$ で特性。$H_0\trianglelefteq H$ ⟹ $U\trianglelefteq H$ ⟹ $U$ は $G$ の**両端 $G$-正規な 1 次元切片**。$T=\{\mathrm{diag}(a,a^{-1})\}$ の $U$ 上の作用は $a/a^{-1}=a^2$ ⟹ 位数 $690/\gcd(2,690)=\mathbf{345}>6$ ⟹ **TWIST-6-ABS と矛盾**。∎
 ⚠ **$H_0=SL(2,691)$ には効かない**(そこでは Sylow 691 は正規でない)— **この区別が §1 で私が落とした種類の注意**。

> ### ★ 命題候補 **W691-NARROW**(candidate・本正誤)
> $$\boxed{\ H_0\in\{\ \text{巡回}\supseteq C_{690},\ \ \text{dicyclic}\supseteq C_{690},\ \ SL(2,691)\ \},\qquad [H:H_0]\mid6\ }$$

### 2.3 ★★★ 枝 (ii) は**わずか 3 群の検査**に落ちる

$H_0=SL(2,691)$ なら $H\supseteq SL(2,691)$ かつ $\det H\subseteq\mu_d$($d\mid6$)⟹
$$H=\{M\in GL(2,691):\ \det(M)\in\mu_d\},\qquad d\in\{1,2,3,6\}$$
$d=1$ は $SL$ で $(2,3)$-生成でない ⟹ **残るは $d=2,3,6$ の 3 群だけ**($6\mid690$ ✔ ゆえ $\mu_6\subseteq\mathbf F_{691}^\times$ 存在)。

> $$\boxed{\ \textbf{枝 (ii) の }W_{691}\ \textbf{標的} = \{M\in GL(2,691):\det(M)^d=1\},\ d=2,3,6\ \textbf{の 3 群}\ }$$
> ★ **$GL(2,7)$ はこの族の $p=7,d=6$ 版**($\mu_6=\mathbf F_7^\times$ ゆえ $GL(2,7)$ そのもの)。⟹ **反例は計画を壊すのではなく、正しい標的を指していた。**

**枝 (i)**(巡回/dicyclic)は $\lvert H_0\rvert\mid4\cdot690$ ⟹ $\lvert H\rvert\mid6\cdot2760=16560$ の**有限枚挙**。$H$ は分裂トーラス正規化群 $T\rtimes C_2$ の部分群に限られる。

### 2.4 発注仕様 **W691-SCAN**(実装係へ)

> 1. **枝 (ii)**: $d=2,3,6$ の 3 群 $H_d=\{M\in GL(2,691):\det M^d=1\}$ について ① $(2,3)$-生成か(**全元対**・共役類代表対は不可)② 位数 690 の $\det=1$ 元を含むか(分裂トーラスから自明に ✔)③ $B_3$ 商として実現するか(braid 関係を満たす生成対 $(a,b)$ の存在)。
> 2. **枝 (i)**: $T\rtimes C_2$($\lvert T\rvert=690^2$)の部分群のうち $\mathrm{ord}(\det)\mid6$・非可換・$(2,3)$-生成・位数 690 の $\det=1$ 元を含むものを枚挙。
> **カナリア**: $p=7$ で同手順を走らせ **$GL(2,7)$ が (2,3)-生成・$r=8$ の生存道型を含む**ことを再現(falsifier 実測との一致)。
> **停止条件**: ③ が全枝で偽 ⟹ **切片読みの χ 扉が全域で閉鎖**((B) 表現読みは**不変** — 混同しないこと)。

---

## §3 【GAP】・帰属

| # | 内容 | 重さ |
|---|---|---|
| **【BR-GAP-1】** | ★ **改訂**: 「$\det=\pm1$ 群型が実現するか」→「**W691-NARROW の 3 枝が $B_3$ 商として実現するか**」 | ★ 中 |
| **【BR-GAP-3】** ★新 | Dickson 分類の適用は**古典的定理の引用**(【要 pin】)。工房内に一次資料なし ⟹ **cross-checked と書かない** | 小 |
| **【BR-GAP-4】** ★新 | 枝 (i)(巡回/dicyclic)の枚挙は未実施 | 小 |

**帰属**: 反例 $GL(2,7)$ の発見・$(2,3)$-生成の実装警告 = **falsifier**(裁定 821・GAP 実測)。誤り(非網羅な場合分け)= **起草者**。
本正誤の新規部分 = **分裂/非分裂の確定($p=691$ では分裂強制・$GL(2,7)$ の機構は移らない)** / **命題 W691-NARROW(Dickson + RIBET-SECTION による網羅的絞り)** / **Borel 枝が死に $SL$ 枝が生き残る理由の区別(Sylow の正規性)** / **枝 (ii) が 3 群に落ちること($GL(2,7)$ がその $p=7$ 版であること)** / **発注 W691-SCAN**。

**検算**:
```bash
python -c "
p=7;  print('GL(2,7): |G|=',(p*p-1)*(p*p-p),' nonsplit torus order',p*p-1,' 8|(p+1)?',(p+1)%8==0)
q=691;r=690
print('p=691: r|p-1?',(q-1)%r==0,' r|p+1?',(q+1)%r==0,' -> split forced')
print('Borel twist order =',(q-1)//2,'> 6 -> killed')
print('mu_6 in F_691^x ?', 690%6==0)
"
```
