# 実験 E-DIM9/10/11 — **走行前の予想値宣言**(IF-FIRST・裁定 657)

**状態札: `IF-FIRST 凍結 / k=9,10,11 は 1 行も走らせていない / 本ファイルのコミット後にのみ走らせる / k=12 は別走(裁定 656 凍結済・本票は触れない)/ 封印非接触`**

- 起草: 影工房 数学者(Claude / Opus 5)・2026-08-06 / 委嘱: 裁定 657
- 述語・較正・停止規則・素数系列規約 v2 は `..._addendum_edim56_prediction.md`(`cf5b9c6`)と `..._addendum_edim78_prediction.md`(`346c264`)+ L-4b 追補を**逐語継承**。差分のみ本票。

---

## 1. ★ 予想値

| $k$ | $\dim L_k(x,y)$ | ★ $\dim H_k$ | ★ $\dim\mathcal S_k$ | $\dim\mathcal A_k$(**一次 pin 済**) |
|---|---|---|---|---|
| **9** | 56 | **19** | **1** | 1 |
| **10** | 99 | **33** | **1** | 1 |
| **11** | 186 | **62** | **2** | 2 |

**採点対象は $\mathcal S_9,\mathcal S_{10},\mathcal S_{11}$ の 3 値。**$H$ は参考(ただし §2 により今回は**導出値**であり、外れれば実装バグ)。

## 2. ★ 根拠 1 行 — $H_k$ は今回**推測でなく導出**である

$\mathrm{gr}_1=\langle x,y,z\rangle/(x{+}y{+}z)$ は $S_3$ の**標準 2 次元表現** $V$。$\theta$ は互換・$\tau$ は 3-巡回だから
$$H_k=\ker(1+\theta)\cap\ker(1+\tau+\tau^2)\ =\ \textbf{標準表現 }\rho\textbf{ の }\mathrm{Lie}_k(V)\textbf{ における重複度}$$
(自明表現は $\ker(1+\theta)$ に入らず、符号表現は $1+\tau+\tau^2=3\ne0$ で落ち、$\rho$ だけが両方を通り 1 次元寄与)。自由 Lie 環の指標公式 $\mathrm{ch}_{\mathrm{Lie}_k(V)}(g)=\frac1k\sum_{d\mid k}\mu(d)\,\mathrm{ch}_V(g^d)^{k/d}$ と $\mathrm{ch}_V=(2,0,-1)$ から
$$\boxed{\ \dim H_k=\frac{\mathrm{Witt}(2,k)-\mathrm{ch}_k(\tau)}{3},\qquad \mathrm{ch}_k(\tau)=\frac1k\!\!\sum_{d\mid k}\!\mu(d)\begin{cases}(-1)^{k/d}&3\nmid d\ 2^{k/d}&3\mid d\end{cases}}$$
★ **既測 6 点($k=3..8$: 1,1,2,3,6,10)を後付けで完全再現**(機械)。$\mathrm{ch}_k(\tau)=-1$ になるのは $3\mid k$ の一部($k=3,9,12$)で、そこだけ $\mathrm{Witt}/3$ 則が崩れる — **$k=9$ で 56/3 が整数でない理由がこれ**。

$\mathcal S_k$ の根拠: $\mathcal A_k\subseteq\mathcal S_k$(HSP-SOUND)+ $\dim\mathcal A_k$ が $w\le12$ で**一次資料 pin 済**(IMT 表・12/12 一致)⟹ **釣り合いに賭ける**(これまで $k=3..8$ で 6/6 成立)。

## 3. ★ 外れた場合の扱い(**裁定 657 の指定**)

| 事象 | 第一疑い | 手続き |
|---|---|---|
| **一般素数で $\dim\mathcal S_k\ne\dim\mathcal A_k$** | ★ **実装バグ**(数学的発見ではない) | **S-ED-4 で仲裁** — 大素数 3 本目で再走。3 本一致してもなお不均衡なら**司令塔へ即報・停止**(SYN-0 発火の主張は Sol 監査後) |
| $\dim H_k$ が §2 の式と違う | ★ **実装バグ確定**(式は既測 6 点を再現する導出値) | `CALIBRATION_FAIL / STOP`・$\mathcal S_k$ を報告しない |
| 大素数 2 本が食い違う | 実装バグ | S-ED-4 |
| $\dim\mathcal S_k>\dim H_k$ | 論理的にありえない | 即停止 |

> ★ **一般素数での不均衡を「B 型発見」と読まない。** char-0 の値は一次資料で確定しており、一般素数はその代理である。**真の未知は $p=691$ の $k=12$ のみ**(裁定 656 で別枠凍結済)。

## 4. 判定文(UNKNOWN 規律・継承)

> 書いてよい: 「斉次水準で $\dim\mathcal S_k=$(実測)。一次 pin 済の $\dim\mathcal A_k$ と一致/不一致」/ 全段一致なら「**$k^*\ge12$**(斉次水準・$k=12$ は別走)」
> 書いてはならない: 「class 9,10,11 の窓に B 型なし」(SYN-0 は candidate)/「$k^*=12$」/「$\mathcal S=\mathcal A$」(次元一致であって部分空間の等号ではない)/ $k=12$ に関する一切(別走)

**素数**: 大素数 2 本(`2147483647`, `998244353`)。$k=9,10,11$ には特異素数を**加えない**(番地は 12 と 16)。

---

**本ファイルのコミット時点で、$k=9,10,11$ の計算は 1 行も走っていない。**
