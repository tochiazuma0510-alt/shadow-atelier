# BIT-252 敵対判読(falsifier 逐語・裁定 663 で正本化)

著者: falsifier(opus/max)。司令塔指示により ops-clerk が transcript から逐語抽出(2026-08-06)。

---
Investigation complete. I have a full independent replication plus several defects.

---

# BIT-252 敵対判読 — 報告

## 結論(先に)

**VERDICT A(0/117,649)は私の攻撃を生き延びた。** 私は駆動器とは**別構成・別言語・別著者**で実験全体を再実装し、**survival = 0/117,649 を独立に再現**した。さらに最大の穴(窓 K 側に較正が一つも無い)を塞ぎ、そこも通った。ただし較正設計に**★の付け間違い**があり、「全部殺す系統誤り」に対する防御は起草者が思っているより**細い一本の糸**に依存していた。

## 私の独立再実装(GAP 非使用・ANUPQ 非使用・駆動器コード非再利用)

駆動器は ANUPQ の p-quotient で窓を作る。私は**切断 Magnus/Jennings 代数**で作った: F₂ → (F₇⟨X,Y⟩/deg≥n)ˣ, x↦1+X, y↦1+Y。Jennings により核は D_n = γ_n·γ₁⁷(7·1 ≥ n, n≤7)ゆえ
- n=5 ⟹ **P = F₂/(γ₅F₂⁷)**, |P| = 7⁸ = 5,764,801 ✓ cert 一致
- n=6 ⟹ **P′ = F₂/(γ₆F₂⁷)**, |P′| = 7¹⁴ = 678,223,072,849 ✓ cert 一致
- fiber = {1+ζ : ζ ∈ L₅}, rank 6 ⟹ **117,649** ✓ cert 一致

再現結果:

| 項目 | 駆動器 cert | 私(独立) |
|---|---|---|
| F-1 / F-2 / F-5 | T / T / T | T / T / T |
| F-3 | 7/7 | 7/7 |
| F-4(負) | 正しく FAIL | 正しく FAIL |
| F-6 | 49 | **49** |
| F-7 解集合 | [0,0,0],[1,4,1],[2,1,2],[3,5,3],[4,2,4],[5,6,5],[6,3,6] | **完全同一** |
| **R3 survival** | **0 / 117,649** | **0 / 117,649** |

**これは「独立 checker」が明示的に実施しなかった当の数値の独立再検証である。**

## ①「全部殺す系統誤り」の経路 — 列挙と潰し

**【最重要・現存していた穴 → 私が塞いだ】較正が一つも P′ で走っていない。**
F-1〜F-7 は**全て P** で評価されている(`HexPass(P,...)`)。実測 R3 は **P′** で走る。P′ 側で確認されたのは位数・θ′/τ′ が `fail` でないこと・φ の核の大きさだけ。**「P′ の hexagon 判定器が TRUE を返せるか」を示す fixture がゼロ。** 判定器が P′ で恒偽なら 0/117,649 は artifact だが、**それを検出する仕掛けが走行内に存在しなかった。**

塞ぎ方(理論に裏打ちされた無料の正対照): m = −1(≡6 mod 7), f = 1 で簡約 (3.11) は
$$\tau^2(y^{-1})\,\tau(y^{-1})\,y^{-1} = x^{-1}\,(xy)\,y^{-1} = 1$$
— **自由群 F₂ の中の恒等式**。ゆえに全ての商で成立(補題 PIN-A / 複素共役 shadow)。**必ず TRUE でなければならない。** 実測:

```
P'-F2 (m=6,f=1)  MUST be True (PIN-A identity)  : True
P'-F2 order-reversed variant [must be False]    : False
```
⟹ **P′ 側判定器は生きており、積順序も正しく、分離能もある。**

**【R-1 W-4 積順序の取り違え】排除済 — ただし防御は F-2 一本。**
`HexPass` は `t2v * t1v * ymf` = τ²(w)τ(w)w を GAP 順で書く。これが paper 積かを各 fixture で実測分離した:

| fixture | 正順 | 逆順 | 分離能 |
|---|---|---|---|
| F-1 (m=0,f=1) | T | T | 盲 |
| **F-2 (m=6,f=1)** | **T** | **F** | ★ **唯一の検出器** |
| F-3 (m=0,f=h₄) | T | T | 盲 |
| F-5 (m=0,f=g₁) | T | T | 盲 |
| **F-6 ★★** (117,649 掃引) | **49** | **49** | **盲** |
| **F-7 ★** (343 掃引) | **7** | **7** | **盲** |

理由: F-3/F-7 は γ₄(P) = 中心(class 4)ゆえ全て可換。F-5 の g₁ ∈ γ₃(P) で [γ₃,γ₃] ⊆ γ₆ = 1 ゆえ γ₃ は可換。F-6 は通過集合が f₂ = 0 を強制して可換部へ落ちる。
⟹ **票の ★ 付けは、票自身が最も恐れる故障モードに対して逆転している。** F-7 は「[a,b] の向き・基底 (v₁,v₂,v₃) の較正」という**本来の目的は果たしている**(私も独立に確認)が、**積順序は較正していない**。票 §6 の「F-7 が θ_*/τ_* の作用を同時に較正する」は積順序については**過大**。星無しの F-2 が唯一の砦。

**【R-2 τ-軌道和の項落ち(CAL-B4 型「半減」)】排除済。** τ²,τ,id の 3 項のうち 2 項だけ足す型の誤りは F-2 で必ず落ちる(例: τ(w)·w = (xy)y⁻¹ = x ≠ 1)。なお **CAL-B4 の「半減バグ現存」は repo 内に文書を発見できなかった**(grep で該当ノート無し)⟹ その前提自体は**判定不能**。ただし前提が指す故障モードは上記で閉じている。

**【R-3 窓の構成違い】排除済** — 別ルート(Magnus 切断)で位数・fiber・全 R0 が一致。

**【R-4 fiber の取り違え】排除済** — 独立構成の fiber も 117,649 で同じ答え。

**残る共有仕様リスク(CV-9)**: 両実装とも「簡約 (3.10)(3.11)」を使う。full (3.3)(3.4) との同値は Prop 3.4(前件 f ∈ [F₂,F₂])。h₄′ ∈ γ₄ ⊆ [P′,P′]、fiber ⊆ γ₅ ⊆ [P′,P′] ✓ ゆえ前件は満たされる。**これが唯一残る共有前提**で、私は独立に潰していない。

## ② g* ∈ 252 の定理的根拠 — 再導出

**(b) hexagon = 独立に再導出成功。** 自由 Lie 代数 gr₄(F₂)⊗F₇(dim 3)上で 𝔥₄ = v₁+4v₂+v₃ が (1+θ_*)𝔥₄ = 0 かつ (1+τ_*+τ_*²)𝔥₄ = 0 を満たすこと、および**次数 4 斉次 hexagon の解空間が厳密に直線 F₇(1,4,1)(7 点)であること**を、GAP も駆動器コードも使わず確認。表現論的にも: gr₄ = sgn ⊕ std ⟹ ker(1+θ)∩ker N は dim 1 ⟹ 7 点。**DUM-HEX は測定非依存に立つ。**

**(c) PENT_W 落ち**は DUM-1/p + **NW-P5(η = ν₄(j𝔥₄) ≠ 0 は実測)**に依存。⟹ g* ∈ 252 は「定理 + 実測入力 1 個」。票は依存表で「測定相対」と正直に書いており、**C-1(252 リスト参照の回避)は達成されている**が、「純粋な定理」ではない。裁定文では「252 所属は定理 + NW-P5 実測」と書くべき(軽微)。

## ③ fiber の線型構造 — 「0 は自然か」

条件は ζ について**アフィン**(票 §4.3 の主張は正しい。私も独立に確認)。gr₅⊗F₇(dim 6)上で実測:

| 量 | 値 |
|---|---|
| **d₅ = dim(ker(1+θ_*) ∩ ker N)** | **2** |
| dim im(N) | 2(= 自明 ⊕ 符号 = τ-不変部) |
| dim im(1+θ_*) | 3 |
| **dim N(ker(1+θ_*))** | **1**(符号成分のみ) |
| c_θ ∈ im(1+θ_*)? | **True**(3.10 単独は可解) |
| c_τ ∈ im(N)? | **True**(3.11 単独は可解) |
| **連立解の個数** | **0**(群水準の全掃引と一致) |

⟹ **可能な値は {0, 49} の二つだけ。** 障害は正確に **c_τ の自明表成分**(非零)である — 連立可解には c_τ ∈ N(ker(1+θ)) = 符号直線(dim 1)が要るが、c_τ は im(N) = 自明 ⊕ 符号(dim 2)にある。

**★ 起草者の BIT1-P5(B に賭けた)が外れた構造的理由が特定できた**:
$$\mathrm{gr}_4 = \mathrm{sgn}\oplus\mathrm{std}\ (\textbf{自明表成分なし}),\qquad \mathrm{gr}_5 = \mathbf{1}\oplus\mathrm{sgn}\oplus2\cdot\mathrm{std}\ (\textbf{自明表成分あり})$$
次数 4 では障害が住む空間そのものが零ゆえ Ψ = 0 は**自動**(PRE-1 の可解性は幸運ではなく構造)。次数 5 では自明成分が存在し一般に非零。**類推は原理的に移らない。**

**⟹ 0 は「自然」どころか本命。** c_τ ∈ im(N) を所与とすると可解確率は 7¹/7² = **1/7**。すなわち **VERDICT A の事前確率は約 6/7**。驚き値ではない。

## ④ 陽性対照(算術元 sweep)の設計検分

- **必要十分な最小対照は P′-F2(m=6, f=1)**。O(1)・定理裏打ち(自由群恒等式)・**全 fixture 中で唯一 W-4 分離能を持つ**・R3 と同一コード経路。**これを cert 必須欄にせよ。**(私が実行済: TRUE)
- **設計上の罠**: 算術元は m ≠ 0 では **m=0 の fiber に属さない**。ゆえに算術元 sweep が検証するのは「P′ の HexPass」であって「fiber 列挙」ではない。cert がこれを混同して書かないこと。
- P′ 側にも**負の fixture**(判定器が FALSE を返せることの実証)を 1 件入れ、二値とも出せることを示すこと。
- **BIT1-P3 を {0, 49} に鋭化せよ**(下記)。

## その他の発見

**【要修正 1】BIT1-P3 はバグ検出器として弱い。** 登録された許容集合は {0,1,7,49,343,2401,16807,117649}。理論が許すのは **{0, 49} のみ**。⟹ **1, 7, 343, 2401, 16807, 117649 の 6 値は理論的に不可能なのに登録上は合格**。バグがこれらを出しても S-B1-1 は鳴らなかった。(0 は両方に属すので今回の verdict は不変。)

**【要修正 2】票 §8 の cert 必須欄 `test_element_uid_sha256` が cert に無い。** cert のキーを全走査したが不在。IF-FIRST の要である「**どの元を試したか**」を固定する欄が欠落している。

**【要修正 3】「独立 checker」の緑が誤読を誘う。** `bit252_independent_check_result_v1_20260806.json` は `"all_ok": true` を掲げつつ、同じファイルの note に逐語「**R3 main fiber sweep (survival_count) NOT independently re-verified this pass**」。看板の数値が未検証のまま全体が緑に見える — **S3.5 の照合器と同型の失敗**。私の独立掃引で今回は閉じた。

**【要修正 4】tier-1 の証跡が repo に無い。** 結果は `"tier1_ran": true` だが、その入力 `search/certs/bit252_oneway_perm_export_v1.json` は**リポジトリに存在しない**。第三者が tier-1 を監査できない。

**【軽微 5】BIT1-P4(Lane G vs Lane L 一致)は未評価**(Lane L 未実装・駆動器は明示的に deferred と宣言していて正直)。**私の F₇ 線型代数計算が事実上 Lane L であり、Lane G と一致(0 = 0)する。** P4 は私の計算をもって充足と記録可能。

## 格付け意見

**VERDICT A(生値 0/117,649)を二系統一致として扱うことを支持する** — ただし条件:
(a) 私の独立再実装(別構成)を第二系統として証跡に含めること。現状の「独立 checker」は看板数値を検証していない。
(b) **P′-F2 を必須較正欄に追加**し、走行内で評価すること(今回は外部=私が補填)。
(c) BIT1-P3 を **{0, 49}** に鋭化。d₅ = 2 を cert に記録。
(d) `test_element_uid_sha256` を補充。
(e) 較正の ★ を付け替え(F-2 を昇格。F-7 の較正射程を「交換子規約・基底」に限定して明記)。
(f) g* ∈ 252 は「定理 + NW-P5 実測」と表記。

**Level 1 の解釈(g* ∉ im(ĜT_gen → GT(N)) ⟹ 非全射)**については、私は**群論部分を検証しておらず**、判読の射程外である。数値 0 が本物であることのみを支持する。**2401 脚注 2 への回答という格の主張は、Sol の監査と novelty ゲートを経るまで支持しない。**

---
検算スクリプトは scratchpad(`bit252_lie.py`, `bit252_indep.py`)。対象物は不改変・封印非接触。`search/`・`docs/` への書き込みなし。ANUPQ はこの環境では iostream 不通のため使用不能で、それが独立構成を選んだ理由でもある(結果的に強い独立性が得られた)。