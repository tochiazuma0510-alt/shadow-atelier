# pentagon 衝突の決着 v2 — **我々の evaluator は正しく、我々の「粗↔精 辞書」が逆向きだった**。真の値は **20/20**

- 起草: 影工房 数学者(Claude / Opus 5)/ 2026-07-31
- 委嘱: 司令塔(unblind 後の原子対決・裁定 276)— 第三実装(Dolgushev 本人の Package GT)の 20 witness と我々の 4/20 の矛盾を 1 元まで縮めて決着させよ
- 入力: `search/certs/pent_thirdparty_gt_20260731.json`(著者実装・N19 216/36 で外部較正済)/ `search/certs/pent_t2t3_v2_20260731.json`(我々)/ `search/probe/wac_v1/pent_t2t3_v2_20260731.g`(逐語移植)/ C1 = arXiv 2008.00066(原文)
- 本稿の機械: `scratchpad/pent_atomic_v2.py`(SHA-256 `2fb422fa…ae9f4a`)・`scratchpad/pent_atomic_v2b.py`(`193818af…0f76b8`)。**GAP を使わない Python 独立移植**(窓・余面・Ψ・Chk6 を probe から逐語移植し、構造値 7500/1500/5/60 を再現して自己較正)

---

## 0. 判定(先に 4 行)

| # | 問い | 判定 |
|---|---|---|
| **①** | どちらの evaluator が誤りか | **どちらも誤りではない**。**我々の精 evaluator は著者の 20 witness を 20/20 で受理する**(条件 c1–c5 すべて) |
| **②** | では何が食い違ったか | **我々の probe の「粗↔精 辞書」**。粗 shadow の列挙は**順方向**(GAP 語順)、精の評価器 `Psi` は**反転**(paper 語順)で、**同じ精元に別々の粗ラベルを付けていた**。probe は各粗 shadow $f$ の fiber を走査したが、精の解が乗っているのは **$\{f^{-1}\}$ 側の fiber** だった |
| **③** | 4 という数の正体 | $\#\bigl(\{f\}\cap\{f^{-1}\}\bigr)$ を 4 つの $m$ 層で合計したもの $=1+1+1+1=\mathbf4$。**生存 4 行は「自己逆元な $f$」ちょうど**(恒等 2 行 + $(1,4)(2,5)$ 2 行)— **cert の $f$ 欄だけで目視確認できる指紋** |
| **④** | 正しい値 | **20/20**。$\mathrm{red}:\mathrm{GT}(K_\pi)\to\mathrm{GT}_{\rm gen}(N_A)$ は**全射**。**v4(20 全 arithmetical)と F91-2.6 の算術鎖は無傷** — **PENT 衝突は消滅**する |

> **一行**: 因子 5 でも c 方向でもなく、**$f$ と $f^{-1}$ の取り違え**だった。工房が過去に一度踏んだ向きの罠(`hexagon_orientation_ruling_v1.md`: $f_{\rm judge}=f_{\rm hand}^{-1}$)の、**層をまたいだ再発**である。

---

## 1. 原子対決の実行(委嘱 1・2)

### 1.1 著者 witness を我々の evaluator に通す

著者の charming 20 件(`coarse_reduction.charming.per_entry_rows`)は、すべて **添字 0,1 のみ**の語 = $x_{12},x_{23}$ の語(**$x_{13}$ も $c$ も現れない**)。これを我々の Chk6 に**そのまま**入れる:

| 語の入れ方 | 我々の c1–c5 を全通過した件数 |
|---|---|
| **$w$(著者の語のまま)** | **20 / 20** |
| $\mathrm{Rev}(w)$ | 4 / 20 |
| $w^{-1}$ | 4 / 20 |
| $\mathrm{Rev}(w)^{-1}$ | 20 / 20 |

**⟹ 我々の精 evaluator(defect 語 D1/D2・pentagon・交換子・全射性)は、著者の 20 witness を完全に受理する。** 自己較正: 我々自身の恒等 witness($m=0,4$)も通過 ✓、構造値 $\lvert Q_P\rvert=7500$・$\lvert Q_F\rvert=1500$・$\mathrm{ord}\Psi(c)=5$・$\lvert[Q_P,Q_P]\rvert=60$ を再現 ✓($Q_P\cong A_5\times C_5^3$ の独立再確認)。

### 1.2 では probe はなぜ 0 と出したのか — **fiber が違った**

我々の `Psi` は語を**反転**してから代入する(`PsiAt(w,i) = MappedWord(Rev(w),…)`;宣言は `f_orientation = paper_order_words_reversed_at_evaluation`)。したがって
$$\mathrm{pr}_1\bigl(\Psi(w)\bigr)=\text{(語 $w$ を\textbf{逆順}に評価した粗置換)},$$
一方 probe の粗 shadow 列挙 `Hex(m,f)` は **順方向**(GAP の `*`)で解いている。**同じ語 $w$ に対して二つの層が別の粗置換を付ける。**

実測(著者の 20 語について):

| 層 | 粗置換の集合($m=0$ 層) |
|---|---|
| 粗列挙 `Hex`(順方向) | $\{(),\,(2,3,4),\,(1,2,3,4,5),\,(1,2,4,5,3),\,(1,5,3)\}$ = **我々の 20 の $f$** |
| 精評価 $\mathrm{pr}_1\circ\Psi$(反転) | $\{(),\,(2,4,3),\,(1,5,4,3,2),\,(1,3,5,4,2),\,(1,3,5)\}$ = **その逆元集合** |

($m=1$ 層でも同様に逆元集合。**著者の語を順方向に評価すると我々の 20 粗 shadow がちょうど再現される** — §1.4。)

**⟹ probe は「粗ラベル $f$ の fiber」を走査したが、精の解は「$f^{-1}$ 側の fiber」に乗っていた。** 交わりは**自己逆元の $f$ だけ**。

### 1.3 4 の指紋(**紙だけで確認できる**)

$$\#\{\text{生存}\}=\sum_{m\in\{0,1,3,4\}}\#\bigl\{f\ \text{粗 shadow}\ :\ f=f^{-1}\bigr\}
=\underbrace{1}_{m=0:\ ()}+\underbrace{1}_{m=1:\ (1,4)(2,5)}+\underbrace{1}_{m=3}+\underbrace{1}_{m=4}=\mathbf4 .$$
**実測の生存 4 行(id 0,8,13,15)は正確にこの 4 つ**(cert の `f_perm` 欄で目視可)。10 個の相異なる粗 $f$ のうち自己逆元は $()$ と $(1,4)(2,5)$ の 2 つだけで、残りはすべて 3-巡回/5-巡回(自己逆元でない)。
**これは偶然では説明できない一致であり、辞書逆転仮説の決定的証拠である。**

### 1.4 Kummer 候補の 1 元決着(委嘱の推奨対象)

我々の粗 shadow $(m=0,\ f=(2,3,4))$ に対応する著者の entry: 語 `[1,0,0,1,0,0,0,1,1,1]` $=y\,x^2\,y\,x^3\,y^3$、$m=0$。

| 量 | 値 |
|---|---|
| 順方向評価($x\mapsto\bar x,\ y\mapsto\bar y$) | $(2,3,4)$ = **我々の粗ラベルと一致** |
| 反転評価 $=\mathrm{pr}_1\Psi(w)$ | $(1,3,5)$ — **probe の 20 行のどれでもない**(我々の $m=0$ 層は $(1,5,3)$ を持つが $(1,3,5)$ は持たない) |
| 我々の c1,c2,c3,c4,c5 を $w$ 上で評価 | **1,1,1,1,1(全通過)** |
| probe がこの元を試したか | **試していない**(粗ラベル $(1,3,5)$ の fiber は 20 行に無いため走査対象外) |

**⟹ 「$(m{=}0,f{=}(2,3,4))$ は fine lift を持たない」は誤りで、持つ。probe はその witness を一度も見ていない。** 逆に、粗ラベル $(2,3,4)$ の fiber に解が無い(c1_pass$=0$)のも正しい — **その fiber は $(2,4,3)$ が粗 shadow でない限り解を持たない**からである(実際 $(2,4,3)$ は我々の $m=0$ 層に無い)。**両方の測定が、それぞれの座標で正しい。**

---

## 2. 委嘱 3 — (2.18) はどちらが正しく読んでいるか

**両方正しい。** 疑うべき 4 候補のうち:

| 候補 | 判定 |
|---|---|
| $A=\mathrm{Ad}(\sigma_1)$ の向き / Ad の作用順 | **シロ**。向きが誤っていれば著者の 20 witness が我々の D1/D2 を通ることはあり得ない(20/20 通過) |
| (2.18) が読まれる商(P/N vs refined) | **シロ**。我々は $\Psi$(= 5 余面の積・$\ker\Psi=(K_\pi)_{PB_3}$、C1 (2.4) と一致)で判定しており、著者の witness はそこで消える |
| $x_{13}$ の語($\sigma$ 語 vs $x_{ij}$ 語) | **本件では無関係**。Kummer 行は $m=0$ で、(2.18) は $\sigma_1f^{-1}\sigma_2f\equiv f^{-1}\sigma_1\sigma_2$ となり **$x_{13}$ も $c$ も現れない**(等価形: $f\cdot{}^{\sigma_1}\!f^{-1}\cdot{}^{\sigma_1\sigma_2}\!f\equiv1$)。$m=0$ 行で 0 と出た時点で、$x_{13}$/$c$ 起因は排除できていた |
| **粗↔精の語順規約** | **クロ**。ここだけが食い違っていた(§1.2) |

**⟹ (2.18) の読みは我々も著者も同一。**食い違いは方程式の読みではなく、**どの元に対して読むかの割り当て**だった。

## 3. 委嘱 4 — c5 について

我々の c5 判定は数学的に正しい(v1 §6.2 で C1 (2.34) との同値を証明済)。著者の 20 witness はすべて我々の c5 を通過する ✓。
$$\text{v1 の「8 行で c5}=0\text{」}\;=\;\text{\textbf{誤った fiber の上での}正しい測定}.$$
**補題 LIFT-INDEP(c5 は fiber 上で定数)は数学として真のまま**だが、それを適用した fiber が違ったため、そこから引いた**「上限 12/20」は撤回する**。正しくは各粗 shadow に対し、正しい fiber(= 解が乗る側)では c5 は通る。

---

## 4. v1 の主張の生死(**正直な棚卸し**)

| v1 の主張 | v2 での判定 |
|---|---|
| **補題 C-DEG**($\deg_c(D_1)=\deg_c(f)$、$\deg_c(D_2)=-\deg_c(f)$) | **生存(数学として真)**。証明は語のレベルの恒等式で、fiber の割り当てに依存しない。**外部整合**: 著者の 20 witness はすべて $x_{12},x_{23}$ のみの語 = $\deg_c(f)=0$ ✓(系 C-DEG′ と整合。ただしパッケージが $F_2$ を列挙する設計であることの帰結でもあるので「確証」とまでは言わない) |
| **系 C-DEG′**(GT-pair は $\deg_c(f)\equiv0$) | **生存** |
| **$Q_P\cong A_5\times C_5^3$** | **生存**(本稿の独立 Python 移植で再導出:7500/1500/60/125) |
| **補題 LIFT-INDEP**(c5 は fiber 上定数) | **生存(数学として真)** |
| 「c 方向仮説の棄却」 | **生存**。c 方向は確かに火元ではなかった(火元は $f\leftrightarrow f^{-1}$)。ただし **v1 の「棄却」は正しい結論を誤った理由づけの一部で支えていた** — v1 §4 の「死因は $F_2$ 方向」は**誤った fiber の上での正しい観察**であり、真の死因は**そもそも試していないこと**だった |
| **「上限 12/20」** | **撤回** |
| 「16 行は復活しない」 | **撤回**(20/20 で復活する) |
| v1 §1(借用定義 = C1 逐語一致・$c\mapsto c^{2m+1}$) | **生存**(原文照合) |
| v1 §1.4(settled は $PB_3$ 水準の必要条件のみ) | **生存**(未解決のまま) |

> **★ 自己教材(v1 の失敗型)**: 「**評価器が正しいこと**」と「**評価器を正しい対象に当てていること**」は別物である。v1 は前者を三重に確かめ(C1 原文・補題 C-DEG・実測 3 署名)、後者を**一度も確かめなかった**。粗↔精をまたぐ主張では、**同一元が両層で同じラベルを持つことの単体検査**(恒等元・cyclotomic・Kummer の 3 元 unit test は probe に**あった**が、いずれも**自己逆元 or 不在**で、この罠を検出できなかった)を、**自己逆元でない元**で行うべきだった。

---

## 5. 修理の指定(実装係へ)

**最小修理**: 精候補 $q$ の**粗ラベルを、粗列挙と同じ語順規約で計算する**。probe の該当箇所は
```
fib := Filtered(Elements(QP), q -> Image(pr1,q) = z[2]);      ## ← ここ
```
で、`Image(pr1,q)` は反転評価に対応する。正しくは候補語 $w$ に対し
```
coarse_of(w) := MappedWord(w, [gx,gy,gc], [xb,yb,()]);        ## 反転しない(Hex と同じ順)
```
を粗ラベルとして使う(あるいは `Psi` の `Rev` を外し、D1/D2/Pent を GAP 語順で書き直す — **どちらか一方に統一**すること)。

**再測定の予言(先出し・接触遮断)**
1. `lifted_total = 20`(全 20 粗 shadow が持ち上がる)。
2. 各行の witness は著者の該当語と**同一の $Q_P$ 元**になる(20/20 一致するはず)。
3. **c1_pass = c2_pass = 5**(現行の生存行と同じ値)が**全 20 行**で出る — c 方向は依然 1 点に固定(**補題 C-DEG**)。したがって **25 にはならない**。
4. c4_pass = 1、c5_pass = 125 が全 20 行。
5. `coarse_target_lift_set_size = 20` ⟹ $\mathrm{im}(\mathrm{red})=\mathrm{GT}(N_A)$(位数 20)⟹ **補題 SUBGRP の部分群条件も自明に成立**。

> ### 予言の 1 行事前検証(本稿で実施済・`scratchpad/pent_atomic_v2c.py`)
> Kummer 粗クラス $(m{=}0,f{=}(2,3,4))$ の**正しい fiber**(著者 witness が乗る側・$\mathrm{pr}_1=(1,3,5)$、125 元)を悉皆評価した:
> $$\text{c1}=5,\quad \text{c2}=5,\quad \text{c3}=5,\quad \text{c4}=1,\quad \text{c5}=\mathbf{125},\quad \text{all6}=\mathbf1 .$$
> **現行 cert の生存行($5,5,5,1,125,1$)と完全に同型の署名**であり、上の予言 3・4 をこの行について**先に実証**した。とくに **c5 が $0\to125$ に変わる**ことが、「上限 12/20」撤回の直接の実物確認である。
> (語切片の健全性: $Q_P$ の全 7500 元について $\Psi(\mathrm{word}(q))=q$ を検査・不一致 0。)

**回帰テスト(必須)**: **自己逆元でない粗 $f$**(例 $(2,3,4)$)で、粗ラベル ↔ 精ラベルの往復が恒等になることを assert する単体検査を入れること(3 元 unit test の第 4 元)。

---

## 6. 台帳・地図への含意(司令塔判断待ち)

1. **PENT 衝突は解消**。F91-2.6 の算術鎖(arithmetical ⟹ target-$K_\pi$ lift)と v4 の「20 全 arithmetical」は**両立**する。裁定 266 の「v4 と fine-lift=4 の両 suspension」は **fine-lift=4 の側を撤回**して解除できる(v4 は無罪のまま)。
2. **F91-2.5 の型付け**(GT($K_\pi$) が群か = isolated 性)は**別問題として残る**。ただし im(red) が 20 元すべてを覆うことは、群としての整合性(部分群位数 20 = $\lvert F_{20}\rvert$)と矛盾しない。
3. **本稿は「著者実装が正しい」ことの独立確認でもある**: 我々の(別実装・別言語・別規約の)精 evaluator が著者の 20 witness を 20/20 で受理した。**cross-checked と呼んでよい**(Lean verified ではない)。
4. v1 は**撤回ではなく訂正**(C-DEG・LIFT-INDEP・構造定理は生存)。v1 の §4–§5(上限 12・棄却の理由づけ)に本稿への差し戻し注記を入れた。

---

## 7. 格付け

| 主張 | 格 |
|---|---|
| 我々の精 evaluator が著者 20 witness を 20/20 受理 | **machine(本稿の独立 Python 移植)+ 自己較正**(構造値 4 件再現・恒等 witness 通過) |
| 食い違いの原因 = 粗↔精の語順規約(fiber の取り違え) | **proof 相当**(指紋: 生存 = 自己逆元 $f$ ちょうど 4 件・§1.3) |
| 正しい値 = 20/20 | **candidate → 実装係の再測定で確定させる**(予言は §5) |
| 補題 C-DEG / 系 C-DEG′ / LIFT-INDEP / $Q_P\cong A_5\times C_5^3$ | **生存(proof)** |
| 「上限 12/20」・「16 行は復活しない」 | **撤回** |
| 著者実装の正しさ | 本稿は**反証しない**(むしろ独立受理で支持) |

**読んだ範囲の追加申告(C1)**: v1 §8.2 に加えて (2.4)(2.5)・Prop 2.2 の主張部を直読。**PackageGT のソースは読んでいない**(cert の数値と語のみ使用)。
