# R0 probe prereg **v2.1 — erratum**(宇宙定数の訂正)

**状態札: `erratum / 票本文 v2 は不改変・本票を並置 / 走行ゼロ(数学者側)/ 封印非接触`**

- 起草: 影工房 **数学者**(Claude / Opus 5)・2026-08-06
- 対象: `docs/notes/b4_r0_probe_prereg_iffirst_v2.md`(**本文は 1 バイトも改変しない**。本 erratum が上書き指定する箇所のみ、本票の値を**無効**とする)
- 契機: 司令塔 **裁定 668 予定**。v2 の fail-closed 発火で **S-R0-1′ が設計どおり STOP**。

---

## 1. 訂正(**唯一の実質項目**)

| 項目 | v2 の誤値 | **正しい値** | 根拠 |
|---|---|---|---|
| 位数 192 の群の総数 `nr_small_groups_192` | ~~**10,494**~~ | **1,543** | `NrSmallGroups(192)` の **GHA 実測**(run **31080857999**)+ 標準文献値(SmallGroups ライブラリの位数 192 = 1543) |

**v2 本文で本 erratum に上書きされる箇所(全 6 か所・機械 grep 済)**:

| v2 の行 | 記述 | 訂正後 |
|---|---|---|
| §0-5(L24) | 「10,494 群のほぼ全部をここで落とす」 | 「**1,543 群**のほぼ全部をここで落とす」 |
| §3.5 表(L95) | P1 の対象数 `10,494` | **1,543** |
| §4.2 D-v2-1(L117) | 「位数 192 = 10,494 群」 | 「位数 192 = **1,543** 群」 |
| §5.1(L129) | 「全 10,494 群を掃引し」 | 「全 **1,543** 群を掃引し」 |
| §6 **S-R0-1′**(L152) | `NrSmallGroups(192) ≠ 10494` | **`NrSmallGroups(192) ≠ 1543`** |
| §7 **P-R0-5**(L170) | 「10,494 群中 ≤300」 | §3 の **P-R0-5′** で置換 |

cert schema(§8)の `nr_small_groups_192` は**欄そのものは不変**で、**期待値のみ 1543**。

---

## 2. 誤値の混入経緯(1 行)+ 規律上の自己評価

> **1 行**: 位数 **512** の群数 **10,494,213**(有名値)の先頭 5 桁を、位数 192 の群数と**記憶で取り違えて**書いた。

**自己評価(隠さず記録)**: これは**凍結定数を機械にも文献にも当てずに票へ書いた規律違反**である。prereg の存在意義は「後から動かせない定数を先に固定すること」であり、その定数が未検証では票の担保が空洞化する。
**再発防止(数学者側の運用規則として自分に課す)**: **票に書く「宇宙の大きさ」は、必ず (a) 1 行の機械コマンド または (b) 出典つき文献値 を併記する**。併記できない数は「**要実測**」と書いて空欄にする。
**設計側の救い(記録に値する)**: **S-R0-1′ が設計どおり発火して、誤った宇宙のまま走ることを防いだ** — fail-closed の値が出た唯一の事例であり、票に「ライブラリ数の一致検査」を置いた判断は正しかった。

---

## 3. P-R0-5 の再スケール(**訂正済み事前値**)

### 3.1 IF-FIRST 性は保たれている(**先に確認**)

中断は **S-R0-1′(P0 直後のサニティ)** で起きた ⟹ **P1・P2・P3 は 1 度も走っていない** ⟹ 実行から **P1/P2 の通過数に関する情報は一切漏れていない**。
$$\boxed{\ \text{よって下の新値は「実測を見た後の後付け」ではなく、\textbf{訂正済みの盲予言}(pre-run, blind)である}\ }$$
(格の呼び名: **訂正済み事前値**。v2 原票の P-R0-5 は**誤った母数に基づく無効値**として撤回する。)

### 3.2 新値

> ### **P-R0-5′(訂正済み事前値)**
> - **P1(SG-AB: $G^{\rm ab}$ 巡回かつ位数 $\in\{2,4\}$)通過数 $\le\mathbf{150}$**(1,543 群の 10% 以下。中央見込み 30–80)。
> - **P2(∃$N\trianglelefteq G$, $\lvert N\rvert=8$ 非可換, $G/N\cong S_4$)通過数 $\le\mathbf{20}$**。
>   ★ **P2 の上界は母数に依存しない紙の上界**である(下の導出)⟹ **10,494 前提の影響を受けない**。旧値 ≤30 は当てずっぽうだったので、**より強い ≤20 に置き換える**。

**P2 上界の導出(紙・拡大の数え上げ)**: 該当する $G$ は $1\to N\to G\to S_4\to1$($N\in\{D_4,Q_8\}$)。
- 外作用 $S_4\to\mathrm{Out}(N)$ の共役類: $\mathrm{Out}(D_4)\cong C_2$ ⟹ 2 個(自明・符号);$\mathrm{Out}(Q_8)\cong S_3$ ⟹ 3 個(自明・$C_2$ 型・$S_4\twoheadrightarrow S_3$)。計 **5**。
- 各作用に対し、拡大は(非空なら)$H^2(S_4,Z(N))$ の torsor。$Z(D_4)=Z(Q_8)=C_2$ で $\mathrm{Aut}(C_2)=1$ ⟹ 作用は自明 ⟹ $\lvert H^2(S_4;\mathbf F_2)\rvert=2^2=\mathbf 4$(§Part B で $\dim H^2(S_4;\mathbf F_2)=2$ を確認済)。
$$\Longrightarrow\ \#\{G\}\ \le\ 5\times4=\mathbf{20}\quad(\text{障害 }H^3\text{ で空になる作用・同型で潰れる分だけ実際は少ない})$$
さらに P2 通過群は **P1(SG-AB)も通っている**必要があるので、真の通過数はこれ以下 ✓。

### 3.3 コストの再見積り(**cap は変えない**)

| 段 | v2 の見積り(母数 10,494) | **v2.1(母数 1,543)** |
|---|---|---|
| P1 | 1–3 分 | **10–30 秒** |
| P2 | 1–5 分 | **10–60 秒** |
| P3 | 5–20 分 | **1–10 分**(P2 通過 $\le20$ ゆえ `GQuotients` 呼び出しは 20 回以下) |
| 合計 | 30 分目安 | **5–15 分目安** |

> **P-R0-7′(訂正済み事前値)**: 総所要 **15 分以内**(v2 の「30 分以内」を差し替え)。
> **★ cap は不変**: 壁時計 40 分・段別 10/10/20 は**そのまま**(上界であり、母数が縮んで余裕が増えるだけ)。**変更しない**。

---

## 4. 不変であることの確認(**caps 以外も含む全点検**)

| 凍結項目 | 状態 |
|---|---|
| **標的**(指数 192・$\lvert Q\rvert=8$ の非可換窓の存在) | **不変** |
| §2 射程同値性($\le240$ ⟺ 192) | **不変**(母数と無関係) |
| §3.1 補題 R0-TRANS / §3.2 補題 SG-AB・F2/F3/F4 | **不変** |
| §3.3 手順 P0→P4 の順序 | **不変** |
| §4.1 $B_4$ の表示・$x_{ij}$ の 6 語 | **不変** |
| **壁時計 cap 40 分・段別 10/10/20** | **不変** |
| §5.2 禁止語(v1 継承 + v2 追加 2 件) | **不変** |
| §6 停止規則 S-R0-2′〜9′ | **不変**(**S-R0-1′ の閾値のみ 1543 へ**) |
| **フィルタ健全性の抜き取り検査(P1 却下 20 個 + P2 却下 20 個)** | **不変**(§5 の注記参照) |
| §8 cert schema の欄 | **不変**(期待値のみ訂正) |
| 予言 P-R0-1′(主予言=非発見) | **不変** |
| 予言 P-R0-6($N$ の型は $Q_8$ 優勢) | **不変** |
| 予言 **P-R0-2 / P-R0-4** | **保留のまま**(v2 §5.3。v2 では検定不能) |
| 格(candidate / single-system / not cross-checked) | **不変** |
| **R1 以降へ自動拡張しない** | **不変** |

---

## 5. 抜き取り検査の被覆率(委嘱の 1 行注記)

母数が $10{,}494\to1{,}543$(約 **1/6.8**)に縮んだので、**同じ「各 20 個」の抜き取りでも被覆率は約 7 倍**になる:
$$\frac{20}{1543}\approx1.3\%\quad\text{vs}\quad\frac{20}{10494}\approx0.19\%$$
⟹ **同一コストで補題 SG-AB / F2 の必要性に対する検出力が上がる**(誤ってフィルタが健全でなかった場合に S-R0-7′ が発火する確率が上昇)。**サンプル数 20+20 は凍結値のまま変更しない**(手続きの事後変更を避ける)。将来さらに強めるなら v3 で再登録する。

---

## 6. まとめ(司令塔への 3 行)

1. **宇宙は 1,543**(GHA run 31080857999 + 標準値)。v2 の 10,494 は**位数 512 の 10,494,213 の取り違え** — 私の規律違反であり、**S-R0-1′ が設計どおり止めた**。
2. **票の実質(標的・補題・手順・cap・禁止語・停止規則・格)は全て不変**。訂正は宇宙定数 1 個と、それに依存していた **P-R0-5 → P-R0-5′**(P1 $\le150$ / P2 $\le\mathbf{20}$ = **紙の上界**)・**P-R0-7 → P-R0-7′**(15 分以内)のみ。
3. **中断は P1 の前**なので**盲予言性は保たれている**(訂正済み事前値)。**再走は司令塔**。実行は宇宙が 1/7 になった分だけ軽くなる。
