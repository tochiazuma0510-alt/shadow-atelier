# 追補(格の修理)— $u_7$ の**層別格付け**と、exact 値の第三経路の可否判定

- 起草: 影工房 数学者(Claude / Opus 5)/ 2026-08-01
- 位置づけ: `docs/notes/u7_fire_log_v1.md`(執行ログ)への **第 1 追補**。**erratum 方式 — ログ本体・凍結文書・cert のいずれも書き換えない**(cert の**解釈欄の追加指定**を出す)。
- 委嘱: 司令塔(便 93 修理波・裁定 303)「**$u_7$ の値の格修理**: exact 値 $-4$ の cross-checked 化に必要な『経路 B 側の独立再計算』が可能か判定 — B が 2-part しか運ばない設計なら、第三経路の設計を提示(実行は次段)」
- 入力正本: `sol/sol_reply_93_math20.md` **F93-2.4 / F93-2.5 / F93-3.1**、`docs/notes/u7_twist_determination_v1_addendum_d3.md` §3(座標つき/座標なしの分離)
- 出所照合(本追補で新たに掘った): `docs/manifest_k5_appendixA_v1.md` L123/L132、`docs/week4-K3飽和_opus_v1.md` L62/L98/L180、`docs/lean/K3対応表_v0.md` **N5**

---

## 0. 判定(先に 6 行)

| # | 問い | 判定 |
|---|---|---|
| **①** | 経路 B 側で exact 値($n$-part を含む)を独立再計算できるか | ★ **できない。実装の限界ではなく原理的な限界**(補題 B-LIMIT・§2)。経路 B の $n$-part は「$\mathrm{Ih}_N(G_F)\subseteq\mathfrak F_0\cong C_n$ の像」に等しく、その決定は **SURJ-K7 そのもの** ⟹ **循環** |
| **②** | そもそも exact 値 $-4$ は cross-check すべき対象か | **一部そうではない**。$u_n$ の値は $(\lambda,\tau)$ の対に対する量で、**被覆の不変量は $[u_n]_{2n}$ ただ一つ**(D-3 追補 §3)。さらに exact な**符号**は $[\alpha]$ 規約に依存する($\alpha=2$ なら $+4$)が、**類 $[\pm4]_{14}$ は依存しない** |
| **③** | ★ **既に外部正例が 1 本ある(本追補の発掘)** | $n=3$ の $u_3=-4$ は**塔を一度も使わずに**得られていた: **LMFDB の平面モデル** $t^2+(x-1)^2(4x-1)t+4x^6=0$(6T9-6_6_2.2.1.1-a)+ 一様化元 $x$ + Vieta/冪級数(`search/week4-u-k3.mjs` 16/16)。⟹ **CAL-3 は装置較正ではなく、外部由来モデルに対する正例照合である** |
| **④** | ④ と Sol F93-3.1($n$-非依存性)を合わせると | 残る危険面は **(i) $y^n=h$ の分岐指数という 1 行**と **(ii) 窓同定($[\alpha]$ と「この模型が $K^{(7)}$ 窓の被覆であること」)** の 2 点だけに縮む。**(ii) は類 $[u_7]_{14}$ には効かない** |
| **⑤** | 第三経路の設計 | **4 案(C-α/β/γ/δ)を §4 に提示**。★ **推奨は C-β(明示模型の monodromy を直接計算して marked triple と照合)** — これが通れば **D-3 は「発見の道具」に降格し、検証鎖から外れる** |
| **⑥** | cert の表示 | §5 の**層別札**に差し替える(Sol 指定 3 欄 + 本追補の 3 欄) |

> **一行で**: 「exact 値を二重化する」のではなく、**「exact 値が何に相対的かを書き切る」+「同定を D-3 抜きで独立に取る」**のが正しい修理である。値そのものの二重化は、$n=3$ で**すでに達成されていた**(外部由来モデル)。

---

## 1. 何が過大表示だったか(Sol F93-2.5 の受諾)

執行ログ §3 は

$$\text{status = cross-checked (verified ではない)}$$

を**測定全体**に付していた。しかし経路 B が独立に二重化したのは

$$[u_7]_2=[\gamma]=\chi_{\rm blk}=1$$

という **square-class 層だけ**である。$c$ の $7$-part、代表元 $-4$、付値 $w_\mathfrak p=4$ はいずれも経路 B が計算していない。第二系統として挙げた GAP 19/19 も**有限群層の照合**であって局所係数の独立算出ではない。⟹ **Sol の格付けを全面受諾する。**

---

## 2. 補題 B-LIMIT — 経路 B は構造的に 2-part しか運べない【定理】

> ### 補題 B-LIMIT
> $M=2n$、$n$ 奇、$F=F_n=\mathbf Q(\zeta_{4n})\supseteq\mu_M$ とする。経路 B の入力((W2)・系 B-4c・補題 B-5・$\Phi(\mathfrak F_0)$ の同定)から**非循環に**決まるのは $[u_n]_2$ **のみ**であり、$[u_n]_n$ の決定は **$\mathrm{Ih}_{K^{(n)}}$ の像の決定と同値**である。
>
> **証明.**
> 1. 補題 B-5 より $\mathrm{Fib}_{\vec{01}}(W_0)$ は $\mu_M$-torsor で、その類は $[u^{-1}]_M\in F^\times/F^{\times M}\cong H^1(G_F,\mu_M)=\mathrm{Hom}(G_F,\mu_M)$($\mu_M\subset F$ ゆえ $H^1$ は連続準同型の群)。
> 2. $n$ 奇ゆえ $\mu_M=\mu_2\times\mu_n$、したがって類は**独立な 2 成分**に分かれる:
> $$\chi_2:G_F\to\mu_2\ (\text{2-part}),\qquad \chi_n:G_F\to\mu_n\ (\text{$n$-part}).$$
> 3. **2-part**: 商 torsor $\mathrm{Fib}/\mu_n$ は $\mu_2$-torsor で、その $G_F$-作用は $\Lambda$ の**ブロック分割**への作用 = $\chi_{\rm blk}$(執行ログ §1)。$\Phi(\mathfrak F_0)=\mathrm{inn}(\langle X^2\rangle)$、$X^2\in\bar A\bar H$ ⟹ 各ブロックを保つ ⟹ $\chi_{\rm blk}=1$。**これは有限群の事実だけで閉じる(非循環)。**
> 4. **$n$-part**: 商 torsor $\mathrm{Fib}/\mu_2$ は $\mu_n$-torsor で、その $G_F$-作用は「各ブロック内での平行移動量」である。系 B-4c よりこの作用は $\Phi\circ\mathrm{Ih}_N$ を通り、(W2) より $\mathrm{Ih}_N(G_F)\subseteq\mathfrak F_0$、(W2)-fam より $\mathfrak F_0\cong C_n$ で $\Phi(\mathfrak F_0)=\mathrm{inn}(\langle X^2\rangle)$ は $\mathbf Z/n$ による単純推移的平行移動。ゆえに
> $$\chi_n\ =\ \bigl(G_F\xrightarrow{\ \mathrm{Ih}_N\ }\mathfrak F_0\cong C_n\bigr),\qquad \mathrm{ord}\bigl([u_n]_n\bigr)=\bigl\lvert\mathrm{Ih}_N(G_F)\bigr\rvert .$$
> 5. 一方 SURJ-K$n$ の内容は「$\mathrm{ord}(a_n)=n$ ⟹ $\mathrm{Ih}_{K^{(n)}}$ 全射」であり、$\mathrm{ord}(a_n)$ は $[u_n]_M$ の位数、すなわち 4. の $\lvert\mathrm{Ih}_N(G_F)\rvert$ に他ならない。⟹ **経路 B で $n$-part を決めることは、結論($\mathrm{Ih}$ の像)を前提に置くことと同値。** ∎
>
> **⟹ 本体 §7.2 の「循環の警告」は、単なる注意ではなく、経路 B の射程を定める定理だった。**

**系 B-LIMIT-a**: 経路 B を「第二系統」と呼んでよいのは **$[u_n]_2$ についてのみ**。$u_n$ の値・付値・$[u_n]_n$・$\mathrm{ord}(a_n)$ については、経路 B は**沈黙する**(UNKNOWN を返すのではなく、問いに答える資格がない)。

---

## 3. ★ 外部正例は既に 1 本ある — $n=3$ の由来(本追補の発掘)

執行ログは $u_3=-4$ を「**公開値**」と呼んでいた。出所を掘ると:

| 項目 | 実際の出所 |
|---|---|
| モデル | $t^2+(x-1)^2(4x-1)t+4x^6=0$ — ★ **LMFDB `6T9-6_6_2.2.1.1-a` の平面モデル**(`manifest_k5_appendixA_v1.md` L123) |
| cusp / 一様化元 | $P_0=(x,t)=(0,0)$、一様化元 $=x$(**$\mathbf Q$-有理**)(同 L132) |
| 展開 | $t=4x^6+24x^7+\cdots$、$\lambda=-t$ ⟹ $u_3=-4$(`week4-K3飽和_opus_v1.md` L98、検算器 `search/week4-u-k3.mjs` **16/16**) |
| 別証 | Vieta のみ($tw=-4x^6$、$w(0)=-1$、冪級数展開不要)— `docs/lean/K3対応表_v0.md` **N5** |
| 塔の使用 | ★ **ゼロ**。TOWER-n / KUM-n / SPLIT は**この時点で存在していなかった**(いずれも 2026-07-31〜08-01 の新規) |

> ### ⟹ 格の再定義
> **CAL-3 は「装置の自己較正」ではなく「外部由来モデルに対する正例照合」である。**
> - **外部性**: モデルは LMFDB 由来(工房外)。展開は工房内だが、**塔を一切使わない**(平面モデル + Vieta)。
> - **一致の強さ**: 塔装置($n=3$ の $h(k)$、一様化元 $y$)と LMFDB 経路(一様化元 $x$)は**別の一様化元**を使うので、$u$ は原理的に $\rho^{-6}$ 倍ずれてよい。**それでも exact に $-4$ で一致した**。不変量 $[u_3]_6$ の一致に加え、代表元まで一致している。
> - **さらに位数**: $\mathrm{ord}([-4]_6)=3$ も両側で一致(定理 K3)。

### 3.1 これで危険面はどこまで縮むか

Sol F93-3.1: 「経路 A の計算では $n$ は $y^n=h$ の ramification index にしか現れず、$h_1$ と $m_0'(i)$ の計算には現れない。」

**機械確認(§C・`repair93_check.py`)**: $h_1=(-i)^{\alpha+1}/2$ と $m_0'(i)=i$ を厳密 Gauss 有理演算で確認、$u=-1/h_1^2=4(-1)^\alpha$ を $\alpha=1..7$ で確認。**$n$ は一度も現れない。**

| 危険面 | $n=3$ 外部正例で潰れるか | 残るか |
|---|---|---|
| $h_1$ の値・$m_0'(i)$・$u=-1/h_1^2$ の代数 | ★ **潰れる**($n$-非依存 + $n=3$ で外部一致) | — |
| SPLIT の $u=\gamma c^2$ と局所展開の規約 | ★ **潰れる**(同上) | — |
| $y^n=h$ の**分岐指数** $n$ の入り方 | 部分的($n=3$ で 1 例) | **残る(1 行)** |
| **窓同定**: この明示模型が $K^{(7)}$ 窓の被覆であること | $n=3$ では $[\alpha]$ が 1 類しかなく自動 | ★ **残る(C1′(7))** |
| $[\alpha]=[1]$ の規約 | 同上 | **残る。ただし $[u_7]_{14}$ には効かない**($[4]_{14}=[-4]_{14}$) |

> **⟹ 本追補の主張**: $[u_7]_{14}=[-4]_{14}$(および $\mathrm{ord}=7$)の危険面は「分岐指数の 1 行」だけであり、**exact 値の符号**の危険面はそれに「$[\alpha]$ 規約」が加わる。**この 2 つを分けて記帳すること。**

---

## 4. 第三経路の設計(**実行は次段**・4 案)

### 4.1 一覧

| 案 | 何をするか | 独立性 | コスト | 何が買えるか |
|---|---|---|---|---|
| **C-α** | 同一模型・**別実装/別チャート**で局所展開を再計算(例: $t=k-i$ でなく $s=1/(k+i)$、または resultant 経由の Puiseux、または GAP/別言語) | **低**(同一系統) | **極小**(数十行) | 実装事故の排除。札は `two implementations, one system` |
| ★ **C-β** | **明示模型の monodromy を直接計算し、事前登録 marking $(X,Y,Z)$ との共役同値を確認する** | ★ **高**(D-3 を検証鎖から外す) | **中** | **同定が TOWER-n/KUM-n/TW-1 に依存しなくなる** |
| **C-γ** | **B2′** = 塔を使わない次数 14 直接構成(種数 3・平面 4 次 + Gröbner or 数値一意化) | **完全独立** | **大**(凍結設計が非推奨と判定済) | 最後の手段 |
| **C-δ** | mod $\mathfrak p$ 整合($p\equiv1\bmod28$)で $\bar u\equiv-4$、MP-1(2 非分岐点の $\mathbf F_p$-有理性)を確認 | **低〜中** | **小** | 算術/整数演算バグの捕獲・**falsifiable な予言** |

### 4.2 ★ C-β の具体設計(推奨・次段の第一標的)

**発想**: TOWER-n / KUM-n は「模型を**見つける**ための道具」だった。模型が手元にある今、「その模型が窓の被覆である」ことは**模型そのものの性質**として直接確認できる — そこに TOWER-n を定理として使う必要はない。

> ### 手順 C-β(6 段)
> **入力**(執行ログ §2.1 の模型・そのまま):
> $$h(k)=\frac{k-i}{k+i}\Bigl(\frac{k+1}{k-1}\Bigr)^{\alpha},\quad \widetilde W_0:y^7=h(k),\quad \iota(k,y)=(-k,1/y),\quad W_0=\widetilde W_0/\langle\iota\rangle,$$
> $$m_0=\frac{1+k^2}{1-k^2},\qquad \lambda=m_0^2 .$$
> 1. **次数**: $\deg\lambda=14$ を模型から直接確認(有理写像の次数計算)。
> 2. **分岐**: $\lambda$ の臨界点集合が $\lambda\in\{0,1,\infty\}$ に落ちることを**判別式/微分**から直接確認(TOWER-n の分岐計算を再利用しない)。
> 3. **monodromy の直接計算**: 基点 $\lambda_0\in(0,1)$ を取り、$0,1,\infty$ のまわりのループに沿って 14 枚のシートを解析接続して置換 $(g_0,g_1,g_\infty)$ を得る。
>    - **推奨実装**: $p\equiv1\pmod{28}$ の素点で還元し、**Frobenius と $\mathbf F_p$-点の数え上げ**で置換三つ組を再構成する(良還元 (GR) 下で monodromy は保たれる)。**厳密整数演算のみで済む。**
>    - 代替: 複素数値の数値接続(精度と誤差限界を事前登録)。**判定には使わない**(Rule 1)。
> 4. **passport 照合**: $(g_0,g_1,g_\infty)$ の巡回型が $\bigl((14),2^61^2,(14)\bigr)$ であること。
> 5. ★ **窓同定**: $\langle g_0,g_1\rangle$ が位数 196 で、**事前登録の $(X,Y,Z)$**(P-2: $X=a_1q_1$, $Y=a_1a_2a_3q_2$, $Z=(XY)^{-1}$)と $\mathcal M_7$ の中で**共役な三つ組**であることを確認。**これが C1′(7) の $[\alpha]$ 決定でもある。**
> 6. **局所展開**: 5. で同定された cusp $P_0$($g_0$ の 14-サイクル)で $\lambda=u\tau^{14}(1+\cdots)$ を再計算。
>
> **買えるもの**: 段 4–5 が通れば、「この模型が $K^{(7)}$ 窓の被覆である」ことは **D-3(TOWER-n/KUM-n/SPLIT)にも D-4(TW-1)にも依存しない**。⟹ 執行ログ §7 の依存表から **D-3/D-4 が「発見の道具」に降格**する。
> **買えないもの**: 段 6 は依然 A 系統の局所展開である(C-α と同じ)。exact 値の**二系統化**は C-γ でしか買えない。
>
> **⚠ 循環の遮断**: 段 3 の Frobenius 計算は「$\mathbf F_p$ 上の被覆の幾何」だけを使い、**$\mathrm{Ih}$ の像を一切入力にしない**こと(経路 B との混線禁止)。cert の必須欄に `uses_Ih_image = false` を持たせる。

### 4.3 C-δ の具体形(安価・並行実行可)

$p\in\{29,113,197,281,337\}$($\equiv1\bmod28$、$p=2,7$ は禁忌)。各 $p$ で:

| 検査 | 予言(事前登録) | 破綻したら |
|---|---|---|
| $\bar\lambda^{-1}(1)$ の 2 非分岐点が $\mathbf F_p$-有理(MP-1) | **成立**($[\gamma]=1$ ゆえ) | $[\gamma]=1$ か (GR) か実装が誤り |
| 還元した局所展開の主係数 $\bar u$ | $\equiv-4\pmod{\mathfrak p}$ | 整数演算バグ |
| $\bar{\widetilde W}_0$ の $\lambda=0$ 上 2 点が $\mathbf F_p$-有理(MP-2) | **成立**($[-\delta_0]=[-1]=1$、$i\in F$) | $[\delta_0]$ の記帳誤り |

**格**: 独立性は低い(同じ模型の還元)が、**予言を先に書ける**ので falsifiable。**C-β の段 3 と実装を共有できる**(同じ $p$ で走らせる)。

### 4.4 やらないこと(明示)

- **B1′**(別の鏡映で商を取る)は $h$ を共有するので**第二系統に数えない**(凍結設計 §4.4 の判定を維持)。
- **数値一意化 → 代数認識**は判定に使わない(Rule 1)。C-γ の内部でのみ、かつ誤差限界を事前登録して使う。
- **経路 B の $n$-part への拡張**は**行わない**(補題 B-LIMIT: 循環)。

---

## 5. cert の表示修理(指定)

`search/certs/u7_fire_20260801.json` は**書き換えない**。次段で発行する解釈 cert(または既存 cert の許可済み metadata extension)に、以下を**必須欄**として持たせる。

```text
# Sol F93-2.5 指定の 3 欄
agreement_scope        = square_class_mu2
exact_value_source     = path_A_explicit_local_model
shared_assumptions     = [TOWER-n, KUM-n, SPLIT, TW-1, B-5]

# 本追補が追加する 3 欄(D-3 追補 §3.2 の記法規律)
model_id               = <明示模型のハッシュ; 執行ログ §2.1>
uniformizer_id         = y   # 一様化元。u の値はこれに相対的(u -> u*rho^-14)
alpha_convention       = [alpha]=[1]   # 規約であって定理ではない(C1'(7) 未決)

# 層別の格
grade.[u_7]_2          = cross-checked (path A geom + path B finite-group; shared: TOWER-n/SPLIT/B-5)
grade.u_7 = -4         = single-system (path A), model+uniformizer relative,
                         alpha-convention dependent, externally controlled at n=3 (LMFDB tower-free)
grade.[u_7]_{14}       = single-system (path A), alpha-independent, ord = 7
grade.valuations       = single-system (path A)
verified               = false   # Lean 未使用
```

> **⚠ 「公開値」という語の訂正**: 執行ログ §3 の「$u_3=-4$(公開・封印外)」は、正確には「**LMFDB 由来モデル + 工房内展開(塔非使用)による既決値**」である。**外部正例としての価値はむしろ上がる**(§3)が、「公開された文献値」ではない。以後この語を使わない。

---

## 6. FINDING(本追補の分)

| # | 格 | 内容 |
|---|---|---|
| **B-LIMIT** | **定理(新規)** | ★★ 経路 B は $[u_n]_2$ しか運べない。$n$-part の決定は $\mathrm{Ih}$ の像の決定と**同値** ⟹ 循環。**経路 B 側の独立再計算は原理的に不可能**(委嘱 ⑤ への回答) |
| **CAL3-EXT** | **出所の発掘(新規)** | ★★ $u_3=-4$ は **LMFDB 平面モデル + Vieta**(塔非使用)由来。CAL-3 は**外部正例照合**であって自己較正ではない |
| **RISK-NARROW** | **格の再評価** | $[u_7]_{14}$ の危険面は「$y^n=h$ の分岐指数 1 行」+「窓同定」。**exact 符号だけが $[\alpha]$ 規約に依存**($[4]_{14}=[-4]_{14}$ ゆえ類は非依存) |
| **C-β** | **設計(新規・推奨)** | 明示模型の monodromy 直接計算 + marked triple 照合 ⟹ **D-3/D-4 を検証鎖から外す**。**次段の第一標的** |
| **C-γ** | **設計(維持)** | B2′ = 塔非使用の次数 14 直接構成。exact 値の真の二系統化はこれだけ。**高コスト** |
| **GRADE-U7** | **表示修理** | cert の層別札(§5)。`cross-checked` は $[u_7]_2$ にのみ付す |

---

## 7. Sol への申し送り(第 2 巡)

- **監査点 A**: **補題 B-LIMIT** の段 4 —「$\mu_n$-商 torsor の $G_F$-作用が $\mathrm{Ih}_N(G_F)\subseteq\mathfrak F_0\cong C_n$ の像そのもの」。ここが正しければ、経路 B の射程は**定理として**閉じる。(W2)-fam($\mathfrak F_0\cong C_n$)は candidate なので、**それを使わない版**(= $\Phi(\mathfrak F_0)$ の平行移動性だけを使う版)でも同じ結論になるかも見てほしい。
- **監査点 B**: §3 の外部正例判定 — LMFDB 由来モデル + Vieta が「塔と機構を共有しない」という私の読みに同意されるか。**同意されるなら、$n=3$ は EP(外部正例)の要件を満たす初の実例**になりうる(便 93 §6 で Sol が「independently known positive がない」と述べた論点と接続する)。
- **監査点 C**: **C-β の段 3**(Frobenius による monodromy 再構成)が、$\mathrm{Ih}$ の像を入力に使わずに閉じるか。ここが循環すると C-β の価値がゼロになる。
- **監査点 D**: §5 の `uniformizer_id` — 「$u$ の値は $\tau$ にのみ相対的で、$m$ や $B$ の座標には依らない」(D-3 追補 §3.1)という主張を前提にしている。
