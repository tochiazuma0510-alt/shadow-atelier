# 【ASM-α】判読書 v1 — 反証前哨による検問 Q-1〜Q-5 + 走査未達 1 件

**状態札: `falsifier verdict / 非当事者判読 / python 単系統の独立検算 2 本つき / Lean 検証ではない / 封印量非接触($n=5$ を宇宙から明示除外)`**

- 起草: 影工房 **反証前哨**(falsifier・Opus 5)/ 2026-08-04
- 委嘱: 司令塔(**裁定 445 起票**)「`framework_promotion_campaign_v1.md` の検問 Q-1〜Q-5(【ASM-α】= A7-fam の $\alpha=1$ 限定主張を $\alpha\in(\mathbf Z/n)^\times$ へ量化拡大する差し替え案の穴)+ 同ノートが自認する走査未達 1 件」
- 検問対象: `docs/notes/framework_promotion_campaign_v1.md` §5(札 B)
- 読んだ一次資料: `docs/week4-BFC攻略_opus_v2.md` §3・§5・§6.1–6.3・§7・§7.1 / `docs/notes/oddH_full_proof_v1.md` §1・§4・**§6(命題 ODD-P・帰結 F8)** / `docs/notes/hfun_functoriality_v1.md` 全文 / `sol/sol_reply_73_math.md` Q1.5(1.13)(1.14) **原文** / `docs/notes/c21_draft_v1.md` §3・§4 / `docs/notes/fam_u_assembly_v1.md`(C1/C3/M1/M2/`M2-exp`/`DICT-win` 行)/ `provenance/CLAIMS.md` A7-fam / `docs/notes/fam_u_assembly_addendum_C_draft_v1.md`
- **潰す義務はない — 列挙が職務**(裁定 445 の明示)

---

## 0. 判定(先に)

$$\boxed{\ \textbf{条件付き}\ }$$

**札 B(A7-fam 差し替え)の骨格は成り立つ。**「exact (TB4) と $(Z_{2M}$-link$)$ を層 3 の欄から構造的に外す」という目的は達成される。
**ただし量化拡大の範囲を `α≠0` と書いた瞬間に破綻する。** 適用可能な条件は 3 つ:

| # | 条件 | 根拠 |
|---|---|---|
| **条件 1** | 量化は **$\alpha\in(\mathbf Z/n)^\times$ に限定**する。§5.4 Q-4 の「**(または $\alpha\ne0$)**」を落とす | **F-1**(命題 ODD-P・帰結 F8・独立検算で確認) |
| **条件 2** | §5.3 の「非単元 16 ケースで PASS したことが**要点**」を**反転**する。これは拡大の根拠ではなく、**拡大してはならない証拠**である。検査に ordered passport を追加する | **F-1 / F-2** |
| **条件 3** | 一般 $\alpha$ の **(W4)** は「既在証明の突合」ではなく**新規導出**として起票する | **F-3** |

**追記 C 案への影響**: ★ **§C.4 の第 1 bullet 1 文のみ**。**骨格(【ASM-α】を未閉鎖のまま開き、$\alpha=1$ で供給する)は無傷**で、むしろ本判読により**正しさが裏づけられた**(§4)。

---

## 1. 検問 Q-3(最優先・誰も読み切っていなかった箇所)— **PASS**

> **設問**: BFC §6.2・§7.1 の証明が窓 $H$ を「(W1)–(W5) を満たす抽象的部分群」としてのみ扱っているか、それとも**どこかで $H^{\rm fun}$ の具体形(生成元の形)を使っているか**。使っていれば量化拡大は即死。

**走査範囲**(層 3 が実際に経由する鎖を全部読んだ — §6.2/§7.1 だけでは足りないため広げた):

| 箇所 | 窓 $H$ の入口 | 具体形の使用 |
|---|---|---|
| §5 命題 B-1(regular 可換 ⟹ 自己中心化) | なし($\mathrm{Sym}(\Omega)$ の一般論) | **なし** |
| §5 命題 B-2(B2-corr / B2-bij) | $\langle X\rangle$ 推移・$\mathrm{ord}(X)=M$・$N_P(H)=H$ のみ | **なし** |
| §5 定理 B-3 | (W1)–(W5) のみ | **なし** |
| §6.1 補題 B-4a | (W3) と $\bar N\trianglelefteq\hat F_2,\ \bar N\subseteq\tilde H$ のみ | **なし** |
| §6.1 補題 B-4b | (W1)(W2)(W5)+(CAL) のみ | **なし** |
| **§6.2 定理 B-4** | (TB2) の分裂・(TB1) の対応・B-4a・B-4b。$C_\gamma=\tilde Hc_\gamma$ の一意性 → cocycle 自動 → 閉部分半群 → 開部分群 | ★ **なし** |
| §6.3 系 B-4c | (TB1)(TB4 の左作用)・B-4a・B-2 (B2-bij) のみ | **なし** |
| §7 補題 B-5a / B-5b | 半局所 Dedekind の完備化・慣性軌道。$H$ 非参照 | **なし** |
| §7 補題 B-5 (i)(ii-loc)(iii) | (W4) 経由のみ(「$\langle X\rangle$ の像が $\mathrm{Fib}\cong P/H$ 上推移 ⟹ 幾何点 1 個・$e=[P:H]=M$」) | **なし** |
| **§7.1 補題 B-5ᵘ** | $\mathrm{im}(I_0)=\overline{\langle x\rangle}$ と後合成作用の 2 点のみ。4 段いずれも $H$ 非参照 | ★ **なし** |

> ### ★ 結論(Q-3)
> **窓 $H$ の具体形(生成元の形・$\alpha$ の値)を使っている箇所は 1 つも見つからなかった。** 窓の入口はすべて $N_P(H)=H$ / $[P:H]=M$ / $\langle X\rangle$ 推移性 / $\Lambda$ の $\Phi(\mathfrak F_0)$-安定性 という**抽象条件**を経由する。定理 B-4 の証明が使う有限性は「$\tilde H$ が開(= $[\hat F_2:\tilde H]<\infty$)」だけで、これは任意の $\alpha$ で成立する。
> ⟹ **量化拡大の「即死条件」は発火しない。** 数学者(Claude)の §5.2 の読みは、この点については**正しい**。

**⚠ 正直な範囲申告**: これは「**この観点では穴を見つけられなかった**」であって、走査の完全性の保証ではない。走査したのは上表の範囲(§5・§6.1–6.3・§7・§7.1)に限る。§8(補題 B-6 以降)は A7 の射程外(C-21 §3 (S1)(S2))なので**読んでいない** — もし将来 A7-fam の結論に橋 B-7 側を含めるなら、その走査は別途要る。

---

## 2. ★★ 重大な穴 — F-1(量化範囲)/ F-2(空虚性)

### 2.1 【重大】F-1 — 「$\alpha\ne0$」への拡大は **ODD-H §6 の明示警告に正面から抵触する**

**campaign §5.4 Q-4 は拡大範囲を「$\alpha\in(\mathbf Z/n)^\times$(または $\alpha\ne0$)」と書き、§5.3 は非単元 16 ケースの PASS を「要点」と呼ぶ。** ところが — **campaign が (W3) の出典として引いているまさにその文書** `oddH_full_proof_v1.md` の **§6** に、次が既に登録されている:

> **命題 ODD-P**($n$ 奇、$H=H_{j,\alpha,\beta}$、$d:=\gcd(\alpha,n)$、$j=2$):
> $$(\bar X,\bar Y,\bar Z)\ \text{の型}=\bigl(\,2n,\ 2^{\,n-1}1^2,\ (2n/d)^{\,d}\,\bigr)$$
> とくに **ordered passport が $K^{(3)},K^{(5)}$ と同じ $(2n,2^{n-1}1^2,2n)$ になるのは $j=2$ かつ $\alpha\in(\mathbf Z/n)^\times$ のときに限る**。

> **帰結 (F8)(逐語)**: 「**$\alpha$ が非単元の good 窓は、$K^{(3)}/K^{(5)}$ と同じ幾何型(ordered passport)をもたない。第 3 の印付き点での分岐指数が $2n$ でなく $2n/d$ に落ちる。「$e=n$ の全分岐 regular detector」等、passport / 分岐構造を前件にする下流の条項は、非単元 $\alpha$ の窓を自動的には含まない。** Sol の Q6.1 の提案 $H_n^{\rm fun}=H_{2,1,0}$ は $\alpha=1$(単元)なので安全側。」

**campaign v1 は ODD-P にも F8 にも一度も言及していない**(`grep "ODD-P\|passport\|非単元"` = §5.3 の称揚 1 行のみ)。

#### 独立検算(非当事者実装・campaign の script を一切共有しない)

- **script**: `…/scratchpad/falsifier_passport_probe2.py`(SHA-256 `8b4d48c1398fb869052189b90913d175650a674ecb436fce0e13da980ed9e368`・Python 3.13.14)
- **宇宙(事前登録・campaign と同一)**: $n\in\{3,7,9,11,13,15,21\}$($n=5$ は封印遵守で除外)× 全 $\alpha\ne0$ = **72 ケース**
- **手法**: $\langle X\rangle$ が $G_n/H$ の完全代表系であることを**先に検査**し、それを使って $X,Y,Z=(XY)^{-1}$ の巡回型を直接計算(campaign の 6 述語とは**別の量**)

```
ODD_P_closed_form_mismatches      : 0     ← 命題 ODD-P を 72/72 で独立再現
units_with_K3K5_ordered_passport  : 56/56 ← 単元 α は全て K3/K5 型
nonunits_with_K3K5_ordered_passport: 0/16 ← ★ 非単元 α は 1 つも K3/K5 型でない
```

| $n$ | $\alpha$ | $d$ | $\bar X$ | $\bar Y$ | $\bar Z$ | K3/K5 と同型? |
|---|---|---|---|---|---|---|
| 9 | 1 | 1 | 18 | $2^81^2$ | **18** | YES |
| **9** | **3, 6** | 3 | 18 | $2^81^2$ | **$6^3$** | ★ **NO** |
| 15 | 1 | 1 | 30 | $2^{14}1^2$ | **30** | YES |
| **15** | **3,6,9,12** | 3 | 30 | $2^{14}1^2$ | **$10^3$** | ★ **NO** |
| **15** | **5,10** | 5 | 30 | $2^{14}1^2$ | **$6^5$** | ★ **NO** |
| **21** | **3,6,9,12,15,18** | 3 | 42 | $2^{20}1^2$ | **$14^3$** | ★ **NO** |
| **21** | **7,14** | 7 | 42 | $2^{20}1^2$ | **$6^7$** | ★ **NO** |

⟹ **campaign §5.3 が「PASS したことが要点」と書いた 16 ケースは、ODD-H が「下流条項に自動的には含まれない」と名指しで警告した窓そのものである。**

#### なぜこれが ASM を壊すか(下流の実害)

| ASM の行 | 前件に passport / 単元性が入っている箇所 |
|---|---|
| **層 1 M1** | 「模型 $h_{\widetilde\alpha}$ が $F_n$ 上の Belyi 被覆・**passport $((2n),2^{n-1}1^2,(2n))$**」— **非単元 $\alpha$ の窓はこの型を持たない** |
| **層 2 M2-GEO** | 「模型の慣性三つ組と窓 $H_{2,\alpha',0}$ の標識三つ組が**同時共役** $\iff\alpha'\equiv\pm\alpha$」— 模型は $\widetilde\alpha\in\mathbf Z,\ \gcd(\widetilde\alpha,n)=1$ でしか定義されていない |
| **層 2 `M2-exp`** | $W^{\rm geom}_{n,\alpha}$ を $H_{2,\alpha,0}$ の **Nielsen/monodromy data** から定義 — 非単元 $\alpha$ では対応する模型が族に存在しない |

⟹ **$\alpha\ne0$ で登録すると、層 3(A7-fam)の domain が層 1/層 2 の domain より広くなり、両者が黙って乖離する。** これは Q-5 が恐れた「層 2 と層 3 の境界の破壊」が**実際に起きる**経路である。

> ### ★ 修理は安価である
> **FAM-U が要求する量化はもともと $\gcd(\widetilde\alpha,n)=1$**(assembly 追補 f94 §2)= $\alpha\in(\mathbf Z/n)^\times$ である。**単元に限定しても札 B は何も失わない** — exact (TB4) と $(Z_{2M}$-link$)$ を層 3 から外すという目的は完全に達成される。**失うのは「FAM-U が要るよりさらに広い」という宣伝文句だけ**であり、それは**そもそも要らなかった**。

### 2.2 【重大】F-2 — 空虚性: **6 述語のうち 5 本は識別力ゼロ**

campaign §5.3 の検査は 5〜6 本(部分群 / $\lvert H\rvert=2n^2$ / $[G_n:H]=2n$ / $\mathrm{ord}(X)=2n$ / $\lvert\langle X\rangle\cap H\rvert=1$ / $N_{G_n}(H)=H$)。**これらが $\alpha$ の値によって真偽を変えるかを直接測った。**

- **script**: `…/scratchpad/falsifier_discrimination.py`(SHA-256 `d4c630fcb2780f6e5fc95f8aca786888dd1eaa7b23b82ff59b685cffbe85e30f`)
- **宇宙**: $n\in\{3,7,9\}$ × **$\alpha=0$ を含む全 $\alpha$**

| 述語 | 識別力 |
|---|---|
| 部分群 / $\lvert H\rvert=2n^2$ / $[G_n:H]=2n$ / $\mathrm{ord}(X)=2n$ / $\lvert\langle X\rangle\cap H\rvert=1$ | ★ **$\alpha$ 全域($\alpha=0$ を含む)で恒真 = 識別力ゼロ** |
| $N_{G_n}(H)=H$ | 識別する — ただし **$\alpha=0$ vs $\alpha\ne0$ のみ** |

**$\alpha=0$ ですら 6 述語中 5 本を通す。** そして **単元と非単元を分離する検査は 1 本も存在しない。**

⟹ **「$\alpha\ne0$ の 72 ケース failures 0」は、量化拡大の安全性の証拠として機能しない。** 危険が実在する軸(単元性)を、この試験は**一度も通っていない**。陰性対照も $\alpha=0$ の 1 本だけで、**passport の陰性対照が無い**。
(前哨チェックリスト第 1 項「答えを知っている問題が実際にその機構を通るか」の典型的な不発。「何にでも当たる試験」。)

> **修理**: §2.1 の probe をそのまま較正スイートに足せば、**分離条件**(単元 ⟹ K3/K5 型 / 非単元 ⟹ $(2n/d)^d$)と**ダミー検査**(非単元を陰性対照に使う)が同時に入る。実測 72 ケースで数秒。

---

## 3. 要修正・軽微

### 3.1 【要修正】F-3 — 一般 $\alpha$ の (W4) は「既在の証明」ではない

campaign の状態札は「**本ノートは新しい数学的結論を主張しない — 既在の証明を突合し**」と書く。しかし:

| 前件 | campaign が挙げる供給元 | 実際の登録主張の射程 |
|---|---|---|
| **(W3)** | ODD-H §4 補題 H(3) | ★ **本当に既在**。ODD-H **(1.3)**: $N_{G_n}(H_{j,\alpha,\beta})=H_{j,\alpha,\beta}\iff\alpha\ne0$ — **最初から $\forall\alpha,\forall\beta,\forall j$ で登録済** |
| **(W4)** | HF-1(a)(b)(c) | ⚠ **HF-1 は主張が $H:=H_n^{\rm fun}=H_{2,1,0}$ 限定**(`hfun_functoriality_v1.md` §2 の命題文「$H:=H_n^{\rm fun}$ について」)。一般 $\alpha$ 版は **campaign §5.2 の表セルで新たに導出されたもの** |

⟹ **(W4) 行だけは「突合」ではなく「新規導出」である。** 状態札と §5.2 の実際の内容が食い違う。**格の記帳の問題であり、数学の誤りではない。**

★ **私の再検算(数学は正しい)**:
- **HF-1(a) の一般化**: $U_\alpha$ の元 $a_1^{\alpha v}a_2^ua_3^v$ の一意表示は**第 3 座標が $v$ を、第 2 座標が $u$ を決める**ので $\alpha$ の可逆性は不要。$\lvert U_\alpha\rvert=n^2$、$[P_n:H]=2n$。✓
- **HF-1(c) の一般化**: 偶冪 $a_1^{2k}\in U_\alpha$ ⟹(第 3 座標)$v=0$・(第 2 座標)$u=0$ ⟹ $\alpha v=0=2k$ ⟹ $k\equiv0$。✓ **$\alpha$ は $\alpha\cdot0$ としてしか現れない** — campaign の読みは正しい。
- **HF-3(a) の証明の穴(軽微)**: HF-3(a) は「HF-1(d) の計算を一般 $\alpha$ で繰り返す」と書きながら **$Q$ 側しか実際に書いていない**。$A$ 側も確認した: $a\cdot q_2(a)^{-1}=a_1^{2x_1}a_3^{2x_3}\in U_\alpha\iff 2x_1=\alpha\cdot2x_3\iff x_1=\alpha x_3$($2$ 可逆)⟹ $a=a_2^{x_2}(a_1^\alpha a_3)^{x_3}\in U_\alpha$。**一般 $\alpha$ で通る。** ✓

### 3.2 【要修正】F-4 — Q-4 は型が逆転している(**結論は変わらず、むしろ強くなる**)

campaign §5.4 Q-4:「$M=[G_n:H_{2,\alpha,0}]$ や $K=\mathbf Q(\zeta_{2M})$ が $\alpha$ に依存しないことも同時に主張することになる(私の検算では $[G_n:H]=2n$ が $\alpha$ 非依存なので $M,K$ も非依存)」

**BFC v2 §3 の (W1) 逐語**: 「$P:=\hat F_2/\bar N$、$X:=\pi(x)$、$\boldsymbol{M:=\mathrm{ord}(X)}$」。
⟹ **$M$ は $\mathrm{ord}(X)$ として定義されており、$[P:H]$ ではない。** $M$ は窓 $H$ を**一度も参照せずに** $N$ だけから決まる。(W4) は「$[P:H]$ が**その** $M$ に一致すること」を**要求する条件**である。

**正しい向きで書くと、campaign の主張はむしろ強くなる**:

$$M=\mathrm{ord}(X)=2n\ \text{(窓非参照)}\ \Longrightarrow\ K=\mathbf Q(\zeta_{4n})\ \text{と}\ \mathfrak F_0\ \text{(= (W2) の核)も窓非依存}\ \Longrightarrow\ \text{(W2)(W5) の}\ \alpha\ \text{非依存性が循環なしに従う}$$

逆向きのまま書くと、$\mathfrak F_0$ が $M$ に依存し $M$ が $H$ に依存する ⟹ **(W5) の議論が循環に見える**。記法を正すべき。

### 3.3 【要修正】F-5 — 追記 C 案への影響は **1 文だけ**

`fam_u_assembly_addendum_C_draft_v1.md` **§C.4 の第 1 bullet**:

> 「python 単系統 spot-check で $\alpha\ne0$ の **72 ケース(非単元 16 を含む)failures 0**」

この「**非単元 16 を含む**」が **F-1 の逆向きの記述**である(16 ケースは拡大の根拠ではなく、拡大不可の証拠)。**この 1 文を直すこと。**

★ **それ以外の追記 C は無傷であり、本判読で正しさが裏づけられた**:
- §C.4 が **【ASM-α】を未閉鎖のまま開き、$\alpha=1$ で供給する**という設計は **F-1 に対して既に安全側**である。
- §C.6-3「『A7-fam が $\alpha$ 一般で成立する』— 主張しない」も安全側。
- §C.1 / §C.2 / §C.3 / §C.5 は $\alpha$ の量化に触れていないため**影響なし**。

### 3.4 【軽微】F-6 — $\alpha$ の **3 つの型**が混在している(Q-1 への回答の続き)

| 型 | 定義 | 出所 |
|---|---|---|
| $\alpha\in\mathbf Z/n$ | 群ラベル。$H_{j,\alpha,\beta}=\langle a_j,a_1^\alpha a_{j'},a_1^\beta q_j\rangle$ | ODD-H (1.2) / hfun (1.1) |
| $[\alpha]\in((\mathbf Z/n)\setminus\{0\})/\{\pm1\}$ | **共役類**ラベル。回転指数比 $[r_\infty/r_0]$ | ODD-H (1.9) / ASM C3 / Sol Q1.5 |
| $\widetilde\alpha\in\mathbf Z,\ \gcd(\widetilde\alpha,n)=1$ | **模型の整数持上げ**。exact 符号 $u_{n,\widetilde\alpha}=4(-1)^{\widetilde\alpha}$ | ASM C6a / 追補 f94 §2 / 補題 LIFT |

⟹ **量化拡大の言明は第 1 の型($\alpha\in(\mathbf Z/n)^\times$)で書き、$\widetilde\alpha$ と混ぜてはならない。** ASM 自身が「**exact 符号 $u_{n,\widetilde\alpha}=4(-1)^{\widetilde\alpha}$ は模型・整数持上げ $\widetilde\alpha$・一様化元に相対的**」と明記しており、$\widetilde\alpha\mapsto\widetilde\alpha+n$ で符号が反転する(補題 LIFT)。**「$\alpha$ に依存しない」と言ってよいのは類・位数の水準までで、exact 符号は含まれない**(A7 の結論は「$[u]_M$ が well-defined」であって $u$ の値ではないので**実害はない**が、言明の型は正す)。
また共役類は $\varphi(n)/2$ 個なので、$\varphi(n)$ 個の窓を数えると**二重計上**になる。

### 3.5 【軽微】F-7 — Q-2 は**空振り(穴なし)**。ただし拡大用の引用先は差し替えた方が強い

数学者の懸念は「$\Lambda_n$ の定義が『$H$ の $G_n$-共役全体』でなく『指定された族』であれば読みが崩れる」。**原文を読んだ。崩れない。**

- BFC v2 §3 **(W5) 逐語**: 「$\Lambda:=\{H\text{ の }P\text{-共役}\}$ が $\Phi(\mathfrak F_0)$-安定」— **共役類そのもの**。
- Sol 便 73 **(1.13)(1.14) 原文**: 「$\Phi(\mathfrak F_0)=\operatorname{inn}(\langle X^2\rangle)$。**内自己同型は全ての $P_n$-共役類を保つ**。ゆえに **(W5) は $n-1$ 類を一つも削らない。全類 PASS。**」

⟹ **数学者の読みは原文で確認された。U-1 の二次引用リスクは解消。**

★ **ただし拡大用にはより強い引用先が同じ節にある**。(1.13) は「odd 族では**既存の命題 K5-1 により**」と、$K^{(5)}$ 名の命題を族へ適用する形である。その直後に Sol は**第二の独立論証**を書いている:

> 「Thm 4.3 の $\Phi_{m,k}$ の $A_n$ 上の線形部は $\operatorname{diag}(u,u,\pm u),\ u=2m+1$ なので、より強い $\Phi(\mathrm{GT}(K^{(n)}))$ も **$\alpha\mapsto\pm\alpha$ と $\beta$ の変更しか起こさない**。したがって各 (1.11) は full GT 像でも保たれる。」

これは **$\alpha$ 一様であることを明示的に述べており**、(1.9)($H_{j,\alpha,\beta}\sim H_{j,\pm\alpha,\beta'}$)と合わせて**任意の $\alpha$ で (W5) を与える**。**拡大版 (W5) はこちらを引くべき**($\Phi(\mathfrak F_0)\subseteq\mathrm{Inn}$ 経由より射程が明示的で、かつ $\mathbf Q$-版 (W5$^{\mathbf Q}$) まで同時に取れる)。

### 3.6 【軽微】F-8 — 検算スクリプトの部分群検査

`alpha_general_window_check.py` の `closed = all(mul(g,h,n) in H for g in Hgens for h in H)` は **$\langle\text{Hgens}\rangle\subseteq H$ を示すだけ**で、$H$ が自身の積で閉じることも $H=\langle\text{Hgens}\rangle$ も直接は検査していない。明示パラメータ化 $U_\alpha=\{(\alpha v,u,v)\}=\langle a_2,a_1^\alpha a_3\rangle$ から**実際には従う**ので結論に影響はないが、**テストとしては未検査**である。

---

## 4. 検問別の一覧(Q-1〜Q-5)

| # | 設問 | 判定 | 根拠 |
|---|---|---|---|
| **Q-1** | 窓の取り違え($\alpha$ の座標系・$j=2$/$\beta=0$/符号) | ★ **穴なし**(型注意 F-6 つき) | 群論の $\alpha$ は ODD-H (1.2)・hfun (1.1)・ASM C1 で**同一**($j=2\Rightarrow j'=3$、$H_{2,\alpha,0}=\langle a_2,a_1^\alpha a_3,q_2\rangle$)。$j=2$ は ordered passport の向きで固定(Sol Q1.5)。**$\beta=0$ は共役類の代表選択にすぎない**($A_n$ による共役が $\beta$ を $\mathbf Z/n$ 全体に走らせる — ODD-H 補題(1))ので $\Lambda$ は全 $\beta$ を含み、(W5) の読みと整合。符号は $[\alpha]$ が $\pm$ 類なので吸収 |
| **Q-2** | (W5) の自明性・二次引用リスク | ★ **穴なし**(空振り) | §3.5。原文で確認。**より強い引用先を推奨** |
| **Q-3** | B-5ᵘ / 定理 B-4 が窓の具体形を使うか | ★ **PASS** | §1。§5・§6.1–6.3・§7・§7.1 を逐行走査し**具体形の使用ゼロ** |
| **Q-4** | 主張文の量化子の位置($M,K$ の $\alpha$ 非依存) | ⚠ **要修正(型の逆転)・結論は成立** | §3.2。$M:=\mathrm{ord}(X)$ は定義上窓非依存。正しい向きで書けば主張は**強くなる** |
| **Q-5** | ASM 側の接続(`DICT-win` は変わるか) | ★ **$\alpha\in(\mathbf Z/n)^\times$ なら変わらない / $\alpha\ne0$ なら破損** | §2.1 の下流表。`M2-exp` は $H_{2,\alpha,0}$ の Nielsen/monodromy data 側、A7-fam は窓側 — **単元に限る限り数学者の読み(変わらない)は正しい**。非単元まで広げると層 2 に対応物が存在せず、**Q-5 が恐れた層境界の破壊が実際に起きる** |

---

## 5. 反証できなかった範囲(正直な申告)

1. **§1 の走査**は §5・§6.1–6.3・§7・§7.1 に限る。**§8 以降(補題 B-6・定理 B-7)は読んでいない**(A7 の射程外との C-21 §3 の判定に依拠 — これは**私自身の再走査ではない**)。
2. **(W1)(W2) の族定理本体**(`w2fam_v1.md` / `w2arith_v1.md` / `c2c4_closure_v1.md`)は読んでいない。campaign が「窓非参照」と書いた根拠を**独立には確認していない**(ただしこれらは $\bar N_n$ と $\mathrm{GT}(K^{(n)})$ の性質であり、構造上窓を参照しえない — 高い確度で正しいと読む)。
3. **(CAL)** の窓非依存性も二次引用のまま(`c2c4_closure_v1.md` §1.3 inventory I5)。
4. 私の検算 2 本は **python 単系統・非当事者実装**であり、`cross-checked` ではない。**Lean 検証でもない**。
5. **封印量非接触**: $n=5$ を両 script の宇宙から `assert` で明示除外。曲線・$\lambda$・$u$・窓データに一切触れていない。
6. 本判読は **CV-9 仕様同一性判読ではない**(裁定 445 は計画監査として起票)。ただし F-1 は実質的に「層 1/層 2 と層 3 が**同一対象**を指しているか」の問題であり、**単元限定でなければ「別対象」**である。

---

## 6. FINDING(重大度つき)

| # | 重大度 | 内容 |
|---|---|---|
| **F-1** | ★★ **重大** | 量化を $\alpha\ne0$ へ広げると **ODD-H §6 命題 ODD-P・帰結 F8 に正面から抵触**。非単元 $\alpha$ の窓は ordered passport が $(2n,2^{n-1}1^2,(2n/d)^d)$ で $K^{(3)}/K^{(5)}$ 型ではない。**独立検算 72/72 で ODD-P 再現・単元 56/56 が K3/K5 型・非単元 0/16**。campaign が「要点」と呼んだ 16 ケースは ODD-H が名指しで警告した窓。⟹ **層 1(M1 の passport)・層 2(M2-GEO / `M2-exp`)と domain が乖離。修理 = 単元限定(FAM-U はもともと単元しか要求しない)** |
| **F-2** | ★★ **重大** | **空虚性**。§5.3 の 6 述語のうち **5 本は $\alpha$ 全域($\alpha=0$ を含む)で恒真 = 識別力ゼロ**。識別するのは $N=H$ 一本のみで、それも $\alpha=0$ vs $\alpha\ne0$。**単元/非単元を分離する検査が 1 本もない**。⟹ 「72 ケース failures 0」は拡大の安全性の証拠にならない(危険軸を一度も通っていない) |
| **F-3** | ★ **要修正** | 一般 $\alpha$ の **(W4)** は既在証明の突合ではなく **campaign 内の新規導出**(HF-1 の主張は $H_{2,1,0}$ 限定)。状態札「新しい数学的結論を主張しない」と不整合。**(W3) は真に既在**(ODD-H (1.3) が $\forall\alpha$ で登録済)。数学自体は正しい(私が再検算・HF-3(a) の $A$ 側の欠落分も補って確認) |
| **F-4** | ★ **要修正** | Q-4 の**型が逆**。$M:=\mathrm{ord}(X)$((W1) の定義)であって $[P:H]$ ではない。正しい向きなら $M,K,\mathfrak F_0$ の窓非依存性が**定義から**従い、(W5)(W2) の議論が循環に見えなくなる。**結論は不変・むしろ強化** |
| **F-5** | ★ **要修正** | 追記 C 案 **§C.4 第 1 bullet の 1 文のみ**が F-1 の逆向き記述。**それ以外の追記 C は無傷**で、【ASM-α】を開いたまま $\alpha=1$ で供給する設計は F-1 に対し**既に安全側** |
| **F-6** | 軽微 | $\alpha$ の 3 型($\mathbf Z/n$ の元 / $\pm$ 共役類 / 整数持上げ $\widetilde\alpha$)の混在。拡大の言明は第 1 型で書き、exact 符号($\widetilde\alpha$ のパリティ依存)を含めない。類は $\varphi(n)/2$ 個(二重計上注意) |
| **F-7** | 軽微 | Q-2 は**空振り(穴なし)** — 原文で確認。ただし拡大版 (W5) は Sol 便 73 の**第二論証**($\Phi(\mathrm{GT})$ は $\alpha\mapsto\pm\alpha$ と $\beta$ 変更のみ)を引く方が射程が明示的で強い |
| **F-8** | 軽微 | `alpha_general_window_check.py` の部分群検査は $\langle\text{Hgens}\rangle\subseteq H$ しか示しておらず、$H=\langle\text{Hgens}\rangle$ と自己閉性は未検査(実際には成立) |

---

## 7. 司令塔への 3 行

1. **札 B は通す。ただし量化は $\alpha\in(\mathbf Z/n)^\times$。**「または $\alpha\ne0$」を落とせば F-1/F-2 は消え、札 B の目的(exact TB4・Z-link を層 3 から外す)は**完全に達成される**。
2. **§5.3 の 16 非単元ケースの意味づけを反転**し、ordered passport を較正スイートに足す(§2.1 の probe をそのまま使える・数秒)。
3. **追記 C は §C.4 の 1 文だけ直せば適用可。** 骨格(【ASM-α】を開いたまま $\alpha=1$ 供給)は本判読で**裏づけられた**。
