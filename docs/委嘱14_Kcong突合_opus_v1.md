# 委嘱 14 回答 v1 — 命題 K-cong の独立再計算・T-6 の正式命題化・便 20 への相互所見

2026-07-26 起草: Claude(数学者レイヤー・Opus 5)。**司令塔 委嘱 14(地理学 v2 の確定作業)。**
入力: `sol/sol_reply_20_geography.md`(便 20・全文)・`docs/窓の地理学_v2.md`・`docs/委嘱13_地理学テンプレ族化_opus_v1.md`(自作 v1)・`docs/week4-A5算術飽和_v4.md`。
検算: `search/week4-kcong-check.mjs`(node・独立実装・**15/15 PASS**)。
状態: **紙上(Opus 単系統)**。§1 は Sol 便 20 F8 の**独立再計算**なので、一致部分は **paper cross-audit(二人一致)** へ上げてよい。**`cross-checked`(機械二系統)でも `verified`(Lean)でもない。**

---

## 0. 結論(先に 5 行)

1. **命題 K-cong は正しい。全項を独立実装で再現した(15/15)。** 一語証人 (8.11) の行列・gcd(8 と 2)・$w\in\bar\Gamma(8)\setminus\bar\Gamma(16)$、$|G_n| = 4n^3$(奇)/$n^3/2$(偶)の**$D_n^3$ 内悉皆**、Wohlfahrt 指数超過、$n=2^\alpha$ での指数一致、$n=4$ の位数 32・中心 $C_2^3$・商 $C_2^2$・$\mathfrak{sl}_2(\mathbb F_2)$ 基底 — **すべて別経路で一致**。
2. **T6.2 との「衝突」は衝突ではなく、私の適用範囲の誤りだった。** 私の T6.2 は $\bar\Gamma(2)\twoheadrightarrow D_n$ についての主張で、**$n=4$ を除外していない**(条件は「5 以上の素因子」)。**真の誤りは別**: **$G_n = F_2/\bar K^{(n)}$ は $D_n$ ではない**($|G_4| = 32\ne8$、$|G_5| = 500\ne10$ — 悉皆で確認)。私は自分で立てた【GAP-D1】(「$K^{(n)}$ の $P$・marking を持っていない」)を放置したまま予測を出した。**T6.2 は $K^{(n)}$ には適用できない。撤回する。**(§2)
3. **したがって委嘱 13 §3.2 の「合同/非合同 ↔ 円分/非円分」という**逆転**の観察も撤回**。$K^{(4)}$ は**合同かつ飽和(2 冪・既知)**、$N_A$ は**合同かつ飽和(非円分)**、$K^{(8)}$ は**非合同かつ飽和**。**合同性は円分性とも飽和とも相関しない** — Sol の W147 に完全に同意する。
4. **T-6 を正式命題化した**(§3)。**complex 側(群論+曲線)と ℚ-model の descent datum を分離**し、W145/W146 を遵守。**新たな精密化**: $W_{\mathbb Q}$ の Galois 閉包は $X(10)_{\mathbb C}$ の ℚ-モデルだが、**その deck $A_5$-作用が定義される体はちょうど $L = \mathbb Q(\zeta_5,\sqrt[5]2)$** — これが定理 A₅ の幾何的な言い換えであり、標準 modular ℚ-model(定数体 $\mathbb Q(\zeta_{10})$)とは**別の descent datum** である。
5. **便 20 の軸改組・G0–G5 に全面同意**、そのうえで 3 点の合流案(§4): **(i) G4′ の族版** $[b]\in H^1_{\text{ét}}(\mathcal H, M)$(私の $u\in H^1_{\text{ét}}(\mathcal H,\mu_k)$ の一般化)、**(ii) G4′ の反証条件に B1(Hurwitz 空間が正次元 ⇒ $[b]$ は定数でなく関数)を追加**、**(iii) 命題 U を G1′ に合わせて二本に分離**(飽和 ⟺ $[K_N:\mathbb Q] = |GT(N)|$ / 統一則 ⟺ $\operatorname{coker}\Phi_N = 1$)。
 さらに **★ $\bar K_4 = \bar\Gamma(8)$ の実務的な果実**: 較正アンカー $\mathcal C_8 = K^{(4)}$ が**合同(レベル 8)になった**ので、**委嘱 13 §3.1 の D4 較正ゲートは q 展開の closed form で走らせられる**(§4.4)。

---

## 1. 任務 1 — 命題 K-cong の独立再計算: **全項一致(15/15)**

実装は `search/week4-kcong-check.mjs`(node・整数演算のみ・GAP/照合器を import しない)。$D_n$ の元を $(k,\varepsilon)$($\varepsilon=0$: $r^k$、$\varepsilon=1$: $r^ks$)で表し、$D_n^3$ の中で $x=(r,s,s)$, $y=(rs,r,rs)$ が生成する部分群を**悉皆列挙**した。

### 1.1 (b) 一語証人 (8.11) — **完全一致**

$X=\binom{1\ 2}{0\ 1}$, $Y=\binom{1\ \ 0}{-2\ 1}$ に対し(手計算でも 4 行):
$$ X^{-1}Y^2 = \begin{pmatrix}9&-2\\-4&1\end{pmatrix},\quad X^{-1}Y^2X = \begin{pmatrix}9&16\\-4&-7\end{pmatrix},\quad X^{-1}Y^2XY^2 = \begin{pmatrix}-55&16\\24&-7\end{pmatrix} $$
✓ Sol (8.11) と一致。$\gcd(w-I) = \gcd(56,16,24,8) = \mathbf8$、$\gcd(w+I) = \gcd(54,16,24,6) = \mathbf2$ ✓。PSL では $w\in\bar\Gamma(L)\iff w\equiv\pm I\ (L)$ なので
$$ w\in\bar\Gamma(8),\qquad w\notin\bar\Gamma(16) \tag{1.1}$$
✓(検算 b1–b3)。**群論側**: $rs$ は鏡映ゆえ $(rs)^2=1$ で $y^2 = (1,r^2,1)$、$x^{-1}y^2x = (1,r^{-2},1) = y^{-2}$ ⇒ $w = x^{-1}y^2x\cdot y^2 = 1$ in $G_n$ **全 $n$** ✓(検算 a2 が $n=3..10$ で確認)。したがって $w\in\bar K_n$ で、(1.1) と (8.9) は $2n\ge16$ で矛盾 ⇒ **$n = 2^\alpha\ge8$ は非合同** ✓。

### 1.2 (a) Wohlfahrt 指数論法 — **完全一致**

**exact level**(紙で再確認): $x^2 = (r^2,1,1)$ ゆえ $\operatorname{ord}_{G_n}(x) = 2\cdot\operatorname{ord}(r^2) = 2n/\gcd(n,2) = \operatorname{lcm}(n,2) = e_n$ ✓ Sol と一致。$T$ の $PSL_2(\mathbb F_2)\cong S_3$ での像は transposition ⇒ 偶数位数 ⇒ $\ell_n = \operatorname{ord}(T) = 2e_n$ = $4n$(奇)/$2n$(偶)✓。$\bar K_n\trianglelefteq PSL_2(\mathbb Z)$ ゆえ尖点は一様で、level = この位数 ✓。

**悉皆による指数**(検算 a1・**Sol の (8.4) を仮定せず独立に得た**):

| $n$ | $\lvert G_n\rvert$(悉皆) | 予測 $4n^3$/$n^3/2$ | index $=6\lvert G_n\rvert$ | $\lvert PSL_2(\mathbb Z/\ell_n)\rvert$ | 判定 |
|---:|---:|---:|---:|---:|---|
| 3 | 108 | 108 | 648 | 576 | **超過 ⇒ 非合同** |
| **4** | **32** | 32 | **192** | **192** | 上限内(候補) |
| 5 | 500 | 500 | 3000 | 2880 | **超過 ⇒ 非合同** |
| 6 | 108 | 108 | 648 | 576 | **超過 ⇒ 非合同** |
| 7 | 1372 | 1372 | 8232 | 8064 | **超過 ⇒ 非合同** |
| **8** | **256** | 256 | **1536** | **1536** | 上限内(→ §1.1 の一語で排除) |
| 9 | 2916 | 2916 | 17496 | 15552 | **超過 ⇒ 非合同** |
| 10 | 500 | 500 | 3000 | 2880 | **超過 ⇒ 非合同** |

✓ 奇素因子をもつ全 $n$ で超過(検算 a3)。$n=2^\alpha$ では $n=4,8,16$ で **index $=|PSL_2(\mathbb Z/2n)|$ がちょうど一致**(検算 a4)⇒ 合同なら $\bar K_n = \bar\Gamma(2n)$ しかない ✓ Sol (8.8)(8.9) と一致。

### 1.3 (c) $n=4$ の presentation 論法 — **完全一致**

**群論側**(悉皆・検算 c1–c4): $|G_4| = \mathbf{32}$、$x^2,y^2,z^2$ は**中心の involution**、$\langle x^2,y^2,z^2\rangle\cong C_2^3$(位数 8)、商の位数 4 ✓。
**modular 側**(検算 c5): $|PSL_2(\mathbb Z/8)| = 192$ から $[\bar\Gamma(2):\bar\Gamma(8)] = 32$、$[\bar\Gamma(4):\bar\Gamma(8)] = 8$、$[\bar\Gamma(2):\bar\Gamma(4)] = 4$ ✓。
**(8.14)**(検算 c6): $Z = (XY)^{-1} = \binom{1\ -2}{2\ -3}$、$Z^2 = \binom{-3\ \ 4}{-4\ \ 5}$ で
$$ \tfrac{X^2-I}{4} = E_{12},\qquad \tfrac{Y^2-I}{4} = -E_{21},\qquad \tfrac{Z^2-I}{4} = \begin{pmatrix}-1&1\\-1&1\end{pmatrix} $$
✓ Sol と一致。mod 2 で trace 0、張る空間の位数 8 ⇒ **$\mathfrak{sl}_2(\mathbb F_2)$ の基底** ✓。
**presentation の上界**: $x^2,y^2,z^2$ が中心 involution なら、中心部分群で割った商は $\langle x,y\mid x^2=y^2=(xy)^2=1\rangle = C_2^2$(位数 4)ゆえ全体の位数 $\le 8\cdot4 = 32$ ✓。$G_4$ と $H = \bar\Gamma(2)/\bar\Gamma(8)$ は**双方 32 を実現**するので、**双方が普遍群に等しく、marking を保つ同型**をもつ ⇒ 核が一致 ⇒
$$ \boxed{\ \bar K_4 = \bar\Gamma(8)\ } \tag{1.2}$$
✓(検算 c7)。

> **裁定**: **命題 K-cong は PASS。** 私の実装は Sol の (8.4) の位数公式すら仮定せず $D_n^3$ 内の悉皆から出発しているので、**独立性は高い**。状態札を **single-system paper candidate → paper cross-audit(二人一致)** へ上げてよい(機械二系統ではないので `cross-checked` ではない)。

---

## 2. T6.2 の自己監査 — **撤回**(私の誤り)

### 2.1 何が衝突していないか

私の T6.2(委嘱 13 §3.2)は
> 「$\bar\Gamma(2)\twoheadrightarrow D_n$(位数 $2n$ の二面体群)なる**合同**部分群の核は、$n$ が **5 以上の素因子**をもつとき存在しない」

であり、**$n=4$ は前件を満たさない**(4 に 5 以上の素因子はない)。**したがって $n=4$ での「衝突」は形式的には存在しない。**

### 2.2 何が本当の誤りか(こちらが重い)

**$G_n = F_2/\bar K^{(n)}$ は $D_n$ ではない。** 悉皆(検算 d1):
$$ |G_4| = 32 \ne 8 = |D_4|,\qquad |G_5| = 500 \ne 10 = |D_5|,\qquad G_n\le D_n^3\ \ (|G_n| = 4n^3\ \text{or}\ n^3/2). $$
すなわち **私は「dihedral 窓」という名前から $F_2/\bar K^{(n)}\cong D_n$ と決め打ちし、その仮定の上で予測を出した**。**しかも私は同じ文書で【GAP-D1】(「$K^{(n)}$ の $P$・marking・$N_{\rm ord}$ を 2405 §4 から確定していない」)を自分で立てていた。** 欠品を自覚しながら、その欠品に依存する予測を出したのが誤りである。

> **★ 撤回する主張**:
> - 委嘱 13 §3.2 **命題 T6.2 の $K^{(n)}$ への適用**(命題自体は $D_n$ についての主張として残しうるが、**本研究の窓とは無関係**なので盤面から降ろす)。
> - 委嘱 13 §3.2 の **「合同窓 $A_5$ = 非円分 / 非合同窓 dihedral = 円分」という『逆転』の観察**。$\bar K_4 = \bar\Gamma(8)$(合同)かつ 2 冪飽和既知、$K^{(8)}$(非合同)かつ飽和既知、$N_A$(合同)かつ飽和・非円分 — **三点が同時に立つので、合同性は円分性とも飽和とも相関しない**。**Sol の W147 に完全に同意**し、当該段落を撤回する。
> - 委嘱 13 §3.3 の「$u$ に相当する数は円分単数だろう」という予測は、**$D_n$ 仮定に寄りかかっていた分だけ**割り引く(§4.4 に置き換え案)。

### 2.3 教訓(★ 教材)

> **自分で立てた【GAP】に依存する予測を出さない。** 欠品札は「ここは未確認」という記録であると同時に、**「ここに依存する主張は書くな」という自分への禁止**でもある。今回は「dihedral」という**名前**から商を推測した — Sol の W148(粗データが一致しても marked kernel は一致しない)の一段手前、**「名前は不変量ではない」**という形の失敗である。

---

## 3. 任務 2 — T-6 の正式命題化(P186・W145/W146 遵守)

**複素側(群論と曲線)と ℚ-model(descent datum)を厳密に分離**して述べる。

> ### 命題 T6(レベル 10 合同窓)
>
> $\pi: PB_3\to PB_3/\langle c\rangle\cong F_2\cong\bar\Gamma(2)$ を中心消去とする。
>
> **(i)(群論・kernel)** $\bar N_A = \bar\Gamma(10)$、すなわち $N_A = \pi^{-1}\bigl(\bar\Gamma(10)\bigr)$。
> **根拠**: ①$\bar\Gamma(2)\twoheadrightarrow PSL_2(\mathbb F_5)\cong A_5$(初等行列 2 個で足りる・便 20 (9.1))、核は $\bar\Gamma(2)\cap\bar\Gamma(5) = \bar\Gamma(10)$。②放物生成元は mod 5 で非自明冪単 ⇒ marking は $(5,5,5)$。③**$\operatorname{Aut}(A_5)\backslash\operatorname{Sur}_{555}(F_2,A_5)$ が一元集合**(= $S_5$-軌道一意・**GAP 二系統確定済**)ゆえ kernel は一意。
> **⚠ 言明の精密化**: 使えるのは **$S_5 = \operatorname{Aut}(A_5)$-軌道の一意性**であって、$A_5$-軌道数 2 ではない。
>
> **(ii)(複素・Galois 曲線)** 窓の Galois 曲線(= $\lambda$-線の $A_5$-被覆の滑らかコンパクト化)は $\mathbb C$ 上 $X(10)_{\mathbb C}$ に同型。種数 13(合同側: $1+\frac{360\cdot4}{120}$;当工房側: RH で $2-2g = 36-60$)。
>
> **(iii)(複素・dessin)** 次数 5 の dessin は $W_{\mathbb C}\cong X(10)_{\mathbb C}/A_4$、種数 2。$\lambda=0$ 上の点は $A_4\backslash A_5/C_5$ の 1 個で分岐指数 5(完全分岐)。
>
> **(iv)(ℚ-model — descent datum は別立て)** $W$ は $\mathbf P^1_{\mathbb Q}$ 上の被覆として**一意な ℚ-モデル**をもつ(moduli 体 $=\mathbb Q$、$\operatorname{Aut} = C_{S_5}(A_5) = 1$ ゆえ捻れなし)。それが LMFDB `5T4-5_5_5-a`。
> **一方 $X(10)$ の標準 modular ℚ-model**(楕円曲線の level 構造のモジュライ)は**定数体 $\mathbb Q(\zeta_{10})$** をもち、**(iv) の descent datum とは別物**である。**$X(10)_{\mathbb C}$ の同定((ii))と標準 modular ℚ-model の同定を同じ一行に書かない**(W145)。
>
> **(v)(算術・W146 遵守)** $W_{\mathbb Q}$ の $\mathbf P^1_{\mathbb Q}$ 上の Galois 閉包は $X(10)_{\mathbb C}$ の ℚ-モデルであり、**その deck $A_5$-作用が定義される体はちょうど**
> $$ L = \mathbb Q(\zeta_5,\sqrt[5]2) $$
> **である**($\beta_\gamma = \mathrm{id}\iff\gamma\in G_L$・定理 A₅)。
> **$L$ を $\mathbb Q(X(10))$ の部分体と書いてはならない**(W146)。$L$ は **rigidified された接繊維の Galois 表現が切り出す数体**である。

**(v) の位置づけ(新規・本稿の寄与)**: (iv)(v) を合わせると、**定理 A₅ の幾何的言い換え**が得られる:

> **系 T6.1.** $\lambda$-線上の $(5,5,5)$/$A_5$ 被覆は、**幾何的連結な ℚ-モデルをもつが、その deck 作用は $\mathbb Q$ 上では定義されない**。deck 作用の定義体はちょうど $L = \mathbb Q(\zeta_5,\sqrt[5]2)$ であり、$[L:\mathbb Q] = 20 = |GT(N_A)|$。**「$G_{\mathbb Q}$ がレベル 10 合同窓を飽和する」とは、この定義体が最大($=|GT(N_A)|$ 次)になることに他ならない。**

**【GAP-T6d】** (iv) の「標準 modular ℚ-model と (iv) の descent datum が別物」は、**定数体の比較(ℚ(ζ₁₀) vs 幾何的連結)からの帰結として述べているが、両者の差を $H^1(G_{\mathbb Q},A_5)$ の類として明示していない**。W145 を守る限り実害はないが、明示できれば (v) がさらに鮮明になる。

---

## 4. 任務 3 — 便 20 への相互所見

### 4.1 軸改組(A0/A1/B′/C′/E/F/D′)— **全面同意**、2 点の補足

- **(A0) 中心・拡大座標の新設に強く同意。** 私の week3 補題 1(split-inner ⇒ $Q\cong P\times S_3$)・**補題 2($v_m = \bar\sigma_1^{2m+1}$ — $c\in N$ の全対象で成立)**は、まさに A0+A1 の道具である。**補足**: 補題 2 は「$w_N$ をどう定義するか」を $c\in N$ の窓で**一意に与える**ので、**G1′ の $\langle w_N\rangle$ の定義は補題 2 を正本にするのが安全**(窓ごとに $w$ を選び直すと normalizer 則が窓依存になる)。
- **(D′) を独立軸から関手性条件へ格下げ、に同意。** ただし **W143(partial arrows と connected component を落とさない)**は、私の FC-1(「isolated ⇒ $\bar N$ が $G_{\mathbb Q}$-安定」)が**まさに connected component が singleton であることを使っている**ので、**定理 A₅ の (I1) は P187 の主定義で書き直すべき**(「固定点」ではなく「連結成分が singleton」)。これは v5 で反映する。

### 4.2 G4′ と私の R^gen — **整合する。ただし条件を 1 本足したい**

Sol の G4′「scalar $u$ でなく $[b_{N,H}]\in H^1(G_{\mathbb Q},M_{N,H})$ を基本量に、Kummer 数は $M\cong\mu_p$ の一次元特例」は、**私の委嘱 13 §2.4 の破れ表 B1–B5 と一対一に対応する**:

| Sol G4′ の反証条件 | 私の B# | 一致 |
|---|---|---|
| $H$ の選択が Aut-natural でない | (FC-6 の family-wise 自然性) | ✓ |
| deck automorphism により descent が捻れる | **B2**(Aut(dessin) ≠ 1) | ✓ |
| $\Phi_N$ が非単射 | (命題 M の (M2)) | ✓ |
| actual Galois action と Ih action が一致しない | (補題 C/D/I3‡ = FC-2b) | ✓ |
| $M$ が一次元でない | **B3**(非全分岐 ⇒ 誘導加群)+ **B4**($C_P(w^u)$ 非巡回 ⇒ 非可換 $H^1$) | ✓ |

> **★ 追加提案 (i)**: G4′ の反証条件に **B1(Hurwitz 空間が正次元 = dessin が rigid でない)** を足してほしい。Sol の 5 条件は「**スカラーを比較してよいか**」の条件だが、B1 は「**$[b]$ が定数か関数か**」の条件で、層が正次元のときは**そもそも一点の値として登録できない**。
> **★ 追加提案 (ii)(族版の定式化)**: 私の委嘱 13 §2.3($u\in H^1_{\text{ét}}(\mathcal H,\mu_k)$、= 被覆の印付き点の余接直線の $k$ 乗と底の余接直線の比)は、**そのまま G4′ の族版に一般化する**:
> $$ [b]\;\in\;H^1_{\text{ét}}\bigl(\mathcal H_{\mathfrak S},\ \mathcal M\bigr),\qquad \mathcal M := \text{rigidification 加群の局所系} $$
> で、$\mathbb Q$-点 $s$ での特殊化が $[b_{N_s,H}]\in H^1(G_{\mathbb Q},M)$。**$\mathcal M\cong\mu_k$ の一次元特例が私の $u$。** 二つの提案を合わせた形で **G4′ を族の言葉で書く**のが v2 の姿だと考える。

### 4.3 命題 U と G1′ — **二本に分離すべき(私の修正)**

委嘱 13 §4 の命題 U は
$$ [K_N:\mathbb Q]\;\le\;|GT(N)|\;\overset{\text{統一定理}}{=}\;\bigl|N_{\operatorname{Aut}(Q_N)}(\langle w_N\rangle)\bigr| $$
と一息で書いたが、**Sol の G1′(核と余核を別々に事前登録)に従えば二本に割るべき**である:

> **命題 U′(修正).** $\Phi_N: GT(N)\to N_{\operatorname{Aut}(Q_N)}(\langle w_N\rangle)$ を canonical map とすると
> **(U′-1)(算術)** $\Phi_N$ 単射のとき **飽和 $\iff [K_N:\mathbb Q] = |GT(N)|$**。
> **(U′-2)(群論)** **統一則 $\iff \operatorname{coker}\Phi_N = 1$**(かつ $\ker\Phi_N = 1$)。
> **(U′-3)** 両方が成り立つとき、そのときに限り **飽和 $\iff [K_N:\mathbb Q] = \bigl|N_{\operatorname{Aut}(Q_N)}(\langle w_N\rangle)\bigr|$**。
>
> **★ 読み**: **統一則は「影の個数が算術の a priori 上限に届く」という群論命題**(Galois 像が必ず $N_{\operatorname{Aut}}(\langle w_N\rangle)$ に入るのは $\beta_\gamma(X) = X^{\chi(\gamma)}$ から自動)、**飽和は「算術が影の個数に届く」という算術命題**。**二つは別々に破れうる** — 委嘱 13 §4 の一息の書き方は、この二重性を隠していた。**修正する。**

### 4.4 ★ $\bar K_4 = \bar\Gamma(8)$ の実務的な果実 — 較正ゲートが**モジュラー計算**になる

委嘱 13 §3.1 の **D4(較正ゲート: 2 冪 dihedral で既知の飽和をテンプレが再現するか)** は、$\mathcal C_8 = K^{(4)}$ を使う。**K-cong により $\bar K_4 = \bar\Gamma(8)$ が確定した**ので、この較正は

> **レベル 8 合同窓での $u$(あるいは $[b]$)の計算**

になる。そして委嘱 13 §2.3 の「合同族では $u$ = **Hauptmodul の $q$ 展開の主係数**」という closed form がそのまま使える。$N_A$ で
$$ \lambda(\tau) = 16q^{1/2}-128q+\cdots\ \Longrightarrow\ u = 16 $$
が三系統目の裏取りになった(委嘱 13 §1.5)のと**同じ機械**を、$\bar\Gamma(8)$ 窓に当てる。

> **★ 事前登録すべき予測**: $\mathcal C_8 = K^{(4)} = \pi^{-1}\bar\Gamma(8)$ の $[b]$ は、**2405 Thm 5.3(2 冪 dihedral の飽和は既知)と整合する値を返さなければならない**。返さなければ **テンプレのどこかが壊れている**(強い反証テスト)。**これは当工房で初めての「答えが既知の GT 窓での較正」**であり、委嘱 13 §5 Q2 の要請そのものである。
> **注意(W147)**: ここで検証するのは「テンプレが既知の答えを再現するか」であって、**合同性から飽和を推論するのではない**。

### 4.5 panel への追加提案

- **$\mathcal C_8 = K^{(4)}$ を「合同アンカー」と明記**(レベル 8・$|G_4| = 32$・$x^2,y^2,z^2$ が中心 $C_2^3$)。$\mathcal C_{10} = N_A$(レベル 10)と合わせ、**panel に「合同アンカー 2 本」**を常設する。
- **$\mathcal C_{16}$ vs $K^{(8)}$ の双子セル**(Sol 指名)に賛成。**追加の検査項目**として「**両者の $[b]$ が(定義できるなら)一致するか**」を入れたい — 粗データが全一致して marked kernel が違う双子で、**算術不変量が分離するか**を見るのは G4′ の最良の試金石になる。
- **$K^{(3)}$(奇数側最小 open)**は非合同(index 648 > 576)なので、**モジュラー closed form は使えない** — テンプレの (III) 段は直接計算になる。**D1(2405 §4 からの定義抽出)は依然として最優先の欠品**。

---

## 5. 【GAP】と状態札

| # | 内容 | 状態 |
|---|---|---|
| **【GAP-K1】** | $G_n\le D_n^3$ の**生成元の取り方** $x=(r,s,s)$, $y=(rs,r,rs)$ は **Sol 便 20 (8.10) から採った**(2405 原文で照合していない)。私の悉皆はこの presentation を入力としているので、**この一点だけは Sol と共有の前提**である | **中**(唯一の非独立点) |
| **【GAP-D1】(継続・最優先)** | $K^{(n)}$ の定義・$P$・marking・$N_{\rm ord}$ を 2405 §4 の式番号つきで持っていない。**§2 の誤りの根本原因** | **最優先** |
| 【GAP-T6d】 | 標準 modular ℚ-model と (iv) の descent datum の差を $H^1(G_{\mathbb Q},A_5)$ の類として明示していない | 低 |
| **【状態】** | §1 は Sol 便 20 F8 の**独立再計算で全項一致** ⇒ 命題 K-cong は **paper cross-audit(二人一致)**。§2 の撤回は私の自己監査。§3 の命題 T6・系 T6.1、§4 の命題 U′ は **Opus 単系統・Sol 未監査**。検算 15/15 PASS(node 独立実装)。**`cross-checked` でも `verified` でもない** | — |

**検算**: `search/week4-kcong-check.mjs` — (b1)(b2)(b3) 一語証人 / (a1) $|G_n|$ 悉皆($n=3..10$)/ (a2) $w=1$ in $G_n$ / (a3) Wohlfahrt 超過 / (a4) 2 冪での指数一致 / (c1)–(c7) $n=4$ / (d1) $G_n\ne D_n$。**15/15 PASS**。

---

## 6. 司令塔への提案

**Q1(最優先・変わらず)**: **【GAP-D1】** — reader に 2405 §4 から $K^{(n)}$ の定義・$G_n$ の presentation((8.10) の生成元!)・marking・$N_{\rm ord}$・Thm 4.3 を §/式番号つきで抽出させる。**これは【GAP-K1】(Sol と私の唯一の共有前提)も同時に潰す。**

**Q2**: **$\mathcal C_8 = K^{(4)}$ での較正ゲート(§4.4)を正式に発注**。当工房初の「答えが既知の GT 窓での較正」で、テンプレの生死がかかる。

**Q3**: 地理学 v2 の panel に **「合同アンカー 2 本($\mathcal C_8$ レベル 8・$\mathcal C_{10}$ レベル 10)」** と、双子セルの追加検査項目「$[b]$ の分離」を入れる。

**Q4(語彙・★教材)**: 「**名前は不変量ではない**」を manifest に。§2 の失敗(「dihedral 窓」という名前から $F_2/\bar K^{(n)}\cong D_n$ と決め打ち)は、Sol の W148(粗データ一致 ≠ marked kernel 一致)の一段手前の失敗型で、**別項として登録する価値がある**。
