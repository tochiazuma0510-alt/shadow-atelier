# 窓の地理学 v2 — 統合版(Sol 便 20 × Opus 委嘱 13 × D1 抽出)

2026-07-26・司令塔統合。v1 は保存。発案者: 研究者本人。入力: sol/sol_reply_20_geography.md(分類側)・docs/委嘱13_地理学テンプレ族化_opus_v1.md(族化側)・docs/notes/抽出_Kn定義_D1.md(K⁽ⁿ⁾ 定義系)。

## 1. 基本対象(P182)

atlas の基本対象は**頂点ラベルと辺ラベルをもつ有限部分 poset**(点の表ではない)。
- **頂点の必須欄**: (Q; x̄, ȳ, c̄)・e_c = ord(c̄)・中心消去商 P・passport・geometric level ℓ・合同閉包 H^cong・合同 defect d_cong・Aut・算術軌道 𝒪_arith。G_ℚ-安定な頂点でのみ im(Ih) ≤ GT(N) を追記。
- **辺の必須欄**: 商写像・reduction 像・繊維の濃度と torsor 構造・fiber-product 像・交わりの gluing index δ_glue。**genuine/fake の判定は辺に載る**(頂点単体の不変量からは読めない — 便 20 F5)。

## 2. 座標系(6+1・便 20 の改組を採用)

(A0) **中心拡大座標**(必須新設: e_c・中心消去商・拡大類・θ/τ の中心への作用 — dessin 辞書は c = 1 の世界しか見ない)/(A1) 完全 marked quotient 座標(P + Aut(P)-軌道は**完全不変量**・passport はその粗い索引 — 二重計上しない W142)/(B′) modular geography(level ℓ・合同閉包・defect d_cong・素数冪局所像 — 合同/非合同は d_cong = 1 かの末端 1 ビット)/(C′) deformation 座標(graded module に加え H² 拡大類・交換子 pairing・実際の障害写像 — E23 の (G2)(G3) はこの一部を零化する定理)/(E) poset incidence(辺データ・(5.2) の fiber-product 余核)/(F) arithmetic descent(算術軌道・stabilizer・**[b] ∈ H¹(G_ℚ, M) が基本量・Kummer 数 u は M ≅ μ_p 一次元セルの特例**)/(D′) 以上すべてに課す **GTSh-群oid関手性**(分類関手 𝓘: GTSh → Str・isolated の主定義は π₀ の連結成分が単元 (7.4))。

## 3. 圧縮法則(反証可能形・G0–G5)

- **G0** 窓数 = B₃-admissible 生成三つ組数 / |Aut(P)|(Frobenius 計数だけでは generation と admissibility を数えない)。
- **G1′** normalizer 則は写像 Φ_N の**核と余核を別々に**事前登録(位数一致は相殺を見逃す)。
- **G2′** settled 率 = |Stab_{U_e}(ν_N)| / |U_e|(2/φ(e) は case B の stabilizer = {±1} の特例)。
- **G3′** 中央層消滅則(E23+ の層帰納・条件 (11.5))— 破れは「障害の居場所」であって非零の保証ではない。
- **G4′** torsor-cohomology 則([b] を族上で自然に・一次元セルでのみ u 表示)。**u-スクリーニング(v1 §5.5・研究者発案)はこの一次元セル上の標準工程として維持** — 前件(B1–B5/G4′ の 5 条件)を通った窓の u 検定は 1 窓 1 枚の宝くじ。
- **G5** CRT/gluing 則(δ_glue = 1 の前件つき候補・𝒞₁₂/𝒞₂₀ が最初の反証試験)。
- **G6(cusp-16 則・candidate・委嘱 15)**: 合同窓 Γ̄(2M) の尖点 torsor 主係数は**常に u = 16**(λ = 16q^{1/2} 由来)・類 [16] ∈ ℚ^×/(ℚ^×)^M の位数 = **M/gcd(M,4)**。前件: 合同・c = 1・Galois 被覆の尖点で測る(商被覆の B2 破れを構造的に回避)。実績: M = 4 「自明」予測 = 実測 𝔉₀ = 1 ✓(較正 ①)・M = 5 「位数 5」= 定理 A₅ ✓(較正 ②)。**封印予測: 𝒞₁₆(M = 8)で位数 2**(B5 が実データで効く初の点・K⁽⁸⁾ は非合同で適用外 — 双子セル分離の候補)。falsifier: 位数不一致・非合同窓への不当適用。状態: Opus 単系統 → 便 21 で Sol 監査へ。限界【GAP-C16b】: 較正 ① は 𝔉₀ = 1 ゆえ [b] 直接突合として一段弱い — 強い較正はレベル 16 本番(Q1)。
- **棄却**: 「合同 ⟺ 飽和」は事前登録しない(N_A = 合同+飽和・K⁽⁸⁾ = 非合同+飽和で既に両側反例)。

## 4. 確定・候補の地理事実

- **N_A = π⁻¹Γ̄(10)**(T-6・両数学者 PASS)。X(10)_ℂ・W_ℂ = X(10)_ℂ/A₄ は幾何同定 PASS。**ℚ-モデルの literal 同一視は descent datum 明記が条件**(W145)・L = ℚ(ζ₅,⁵√2) を ℚ(X(10)) の部分体と書かない(W146)。
- **u = 16 = λ の Fourier 主係数**(委嘱 13・三系統一致)。Tate 助変数の規約 = c = 16 再スケール = 系 B′ の危険な c(補題 C の事後正当化)。
- **命題 K-cong(paper cross-audit PASS・裁定 18)**: K⁽ⁿ⁾ 合同 ⟺ n = 4・K̄⁽⁴⁾ = Γ̄(8)(Sol 紙上 × Opus 悉皆独立 15/15・K1 は D1 で閉鎖)。T6.2 は撤回受理(「名前は不変量ではない」★教材)。**較正アンカー 𝒞₈ = K⁽⁴⁾・𝒞₁₀ = N_A はともに主合同窓** — 双方に Hauptmodul の q 展開 closed form が使える。
- **命題 U′(二分・裁定 18)**: (U′-1) 飽和 ⟺ [K_N:ℚ] = |GT(N)|(算術)/(U′-2) coker Φ_N = 1(群論)— 別々に破れうる。系 T6.1: **飽和 = deck 作用の定義体が最大**。
- 定理 R^gen(委嘱 13): 飽和 ⟺ u(s) の位数がちょうど k(前件 B1–B5)— G4′ の一次元セル版。
- 命題 U(委嘱 13): 統一定理 = 影の個数の a priori 上限・飽和 = 算術が上限に届く(同じ数の上下評価)。

## 5. 第一撃と panel(P183/P184)

1. **アンカー再走**: 𝒞₈ = K⁽⁴⁾・𝒞₁₀ = N_A(答え既知・毎バッチ先頭)。
2. **第一標的: レベル 16 双子セル** 𝒞₁₆ vs K⁽⁸⁾ — 同一 manifest の blind pair。観測前に normalizer 写像・settled 率・算術軌道の予測を**別々に封印**。
3. **law-validation panel v1(10 窓)**: 𝒞₄・𝒞₆(A₄)・𝒞₁₂/K⁽³⁾(**同 level 12・同 passport (6,6,6) の合同/非合同対・Conj 5.1 最小 open**)・𝒞₁₄(PSL₂(7))・𝒞₁₆/K⁽⁸⁾・𝒞₁₈・𝒞₂₀/K⁽⁵⁾。素数レベル系・mixed CRT 系・素数冪系を対で読む設計。

## 6. 発注(2026-07-26)

- **委嘱 14(Opus)**: (a) 命題 K-cong の独立再計算(特に一語証人 (8.11)・n = 4 の sl₂(𝔽₂) presentation・自身の T6.2 との整合裁定) (b) T-6 の正式命題化+descent datum 分離(P186) (c) 便 20 全体への相互所見。
- 次期実装(v2 確定後): 𝒞₁₆ の GT 全列挙系の設計(掃引宇宙事前登録)・class-6 作用表の二独立導出(E2 転進と共用)。

## 7. 残存 UNKNOWN(一級)

𝒞₁₆ の GT-shadow 全列挙・isolated 性・算術像 = 全部 UNKNOWN。K-cong は単系統。X(10) 標準算術モデルとの descent 照合未実施。
