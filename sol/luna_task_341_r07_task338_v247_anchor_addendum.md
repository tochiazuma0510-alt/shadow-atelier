# Luna task 341 - task338 corrected v247 anchor addendum

Role: Luna, binding addendum to active task338.  Read this FULL mail and
`sol/proof_r07_a4_anchored_relative_ideal_lift_v247.md` first to last.  Do not
create another output and do not execute anything.  The five task338 v13
outputs remain the sole permitted edits.

## 1. Superseded literal-cube base point

V247 supersedes v242 (1.3), (6.1), and every production use of
`s(g)[x,y]^3-s(g)`.  The literal cube projects to z0 but is nonidentity in all
ten actual roof coordinates, so it is not an actual A4 kernel word.

The v13 production schema and static path must require the independently
accepted A4 anchor `(least_index, projected_exponent, inverse_scalar,
literal_word=u_z)` and its exact gates

```text
rho0(u_z)=1,  rho1(u_z) in K,  q(rho1(u_z))=z0.
```

Given the A3 coefficients lambda_g, construct the actual base point only as

```text
kappa0 = sum_g lambda_g * (s(g)u_z - s(g)).
```

Bind the same roof/tower/task192/A4 identities and replay both words in every
pair.  Knowing only `q([x,y]^3)=z0` is a fatal old-ABI input error.

## 2. A6 pair output and five-case separation

On MEMBER, the production pair polynomial consists of the corrected base
pairs `s(g)u_z-s(g)` plus the live closure pairs `g s_i-g`.  Preserve their
ordered coefficients and roof-fibre replays.  Do not emit an old cube pair,
even behind a copied Boolean.

The five frozen SELFTEST cases test the finite joint-closure/kernel/Hd1
arithmetic and need no fabricated word anchor.  Keep that distinction explicit:
a synthetic linear fixture is not an A4 anchor or actual A5/A6 milestone.
Production may remain blocked at absent actual typed input, but the reachable
schema validator must already reject the superseded cube ABI and require the
v247 package.

## 3. Added actual-owner controls

Extend the identical producer/checker mutation roster with at least: changed
A4 anchor receipt identity, changed least index, changed projected exponent,
wrong inverse scalar, substituted literal cube, changed `u_z` word, failed
rho0 kernel replay, failed q=z0 replay, and changed base-pair order.  Route
them through the normal production-input/anchor/pair validators.

The task338 reply must state that v247 is binding, the frozen five cases are
synthetic arithmetic only, and actual A5/A6 remain 0/3.

`TASK341_R07_TASK338_V247_ANCHOR_ADDENDUM`
