# Task966 — selected-cycle E の独立checkerと限定GHA

役: Luna checker/workflow。Task960の最終凍結を先に完了し、root開始連絡後に実施。
変更可は `search/check_d972_r07_selected_cycle_materializer_v1.py`、
`.github/workflows/d972-r07-selected-cycle-materializer-v1.yml`、
`sol/luna_reply_966_r07_selected_cycle_materializer_checker.md` の三本だけ。
既存source/workflowを上書きしない。ローカル数値/Python import/AST/GAP、network、
git/credential/dispatch/追加agent禁止。rootだけがcommit/push/GHAを実施する。

Task965の全scope、Task963完成仕様、Task957 E、Task958、v547/v548、Task960の
最終親/全array ABIを読む。Task965の新算術sourceをimport/共有せず、式と公開ABIだけ
調整する。accepted lineageの旧reader/物理primitiveは保持前提として明記してよい。
対象はexact current snapshot上の一つのcomplete-oracle witness。宇宙拡張なし。

- 実oracle parentとcompletion snapshotの全tuple/entry/prefix/roster/EOFを結ぶ薄い
  admissionを自系で実装。26scan/insertと旧oracle全算術を再走しない。現在の全physical
  rows/両targetは実測し、元rho2DERIVEDを別に記す。未観測pinではfail closed。
- raw witness→ordered SLP→integer epsilon/omega→v547 normalizer→実Q0/Q2 endpoint/
  Fox chainを独立再導出。aux etaを保存し、Q0とGamma0、mod3とinteger divisionを分ける。
  六cycleのtau零と非零scalar、same raw chainを独立に確認する。
- 実raw-source算術はordinary finite27群係数/差分基底など別経路を採り、producerの
  binomial helper/SLP source/scatter/primalを共有しない。錨を実sourceに接続し、toyだけの
  独立性にしない。全6tag・四character・mixed degrees・shared etaを照合する。
- primalのold元embedded lead昇順→new元lead昇順、全8059alpha/events、全lower96776零、
  P1補正した全四top、四Bの和、oracle scalar=old lambda·G、全current物理消去/normalize/
  target/fresh separatorを独立再計算。全array/全JSON/roster/EOFとtyped refsを比較する。
- rawのsource-lower零とnormalized literalのphysical-lower零を区別し、target零も
  Task958 pendingのLINEAR_MEMBERSHIP_CANDIDATEまで。new direct wordの実施範囲、
  retained source-map/P1/Conn/TCB、verified=falseを両系成果に保存する。
- 公開ABIを965から受け、相手sourceを読まずに全出力比較と同じCLIを作る。現pinの
  mixed-generation/plain-targetの実canaryと、変更された少数interface canaryだけを使う。

新workflowは `[r07-selected-cycle-materializer-v1-run]` marker / 同じsol作業branch。
既存12親に実oracle candidateを一つ追加しlive exact tuple/ZIP digest/entryを固定する。
source pinとraw word rosterをexact bytesで認証し、必要source ASTはGHAだけ。
旧成功suite/cap1/resumeを増やさず、新producer一回/独立checker一回。内部1800秒/外40分
ずつ/job100分を初期の資源上限とし、各stage時間とI/O/語長/alpha supportを出す。
candidateは全checkerPASSの後だけ、diagnosticsはalways。UNKNOWNは一級terminal。
fresh parent sourceがcompletion-runと元producer-runの二つの由来を持つ点を保存する。
未観測oracle tuple/checker pinや未凍結source hashを残したままREADYとしない。

最終bytes/SHA/LFと全残gateをroot/965/967へ報告。966はworkflowを書くが発火しない。
最終行 `AUDIT_966_VERDICT:`、verified=false。
