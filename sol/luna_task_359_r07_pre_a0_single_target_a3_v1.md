# Luna task 359 - pre-A0 single-target actual A3 compiler v1

Role: Luna, bounded implementation only.  Read every numbered section first
to last before editing.  Do not run Python, Node, GAP, GHA, a workflow, git,
network, or any candidate command.  Read-only PowerShell inspection and
hashing are allowed.  Use `apply_patch` for repository edits.

V302 proves that the exponent-nine A3 target is independent of every
registered A0 roof correction.  This task turns that theorem into one actual,
pre-A0 producer/checker route.  It must not invent a task192 word or describe
the computational base point as an already constructed corrected lift.

## 1. Binding prerequisites

Read in full, in this order:

1. `sol/proof_r07_single_seed_endpoint_orbit_gate_v216.md`;
2. `sol/proof_r07_actual_two_word_endpoint_specializer_v225.md`;
3. `sol/proof_r07_three_area_class_a0_a3_retarget_v301.md`;
4. `sol/proof_r07_a18_area_invisibility_single_a3_target_v302.md`
   (7,340 bytes, SHA-256
   `ba508bbe96f34967ebe456c51285ecbe774861a864c369699bbf1dce2b9fc6c3`);
5. the complete accepted task198 receipt, `acceptance_v2` manifest, producer
   and checker attestations, checker verdict, and their three source owners;
6. the current task226 producer/checker and task227 producer/checker/driver,
   together with v220 delta38 and delta39.

The accepted task227 SELFTEST run is `33153010409` at head
`d1e34bb450bdee48633f64b251db5b14580ce798`; it exercised five independent
closure cases, the exact 486 ideal rows and 729 translates, both MEMBER and
NONMEMBER, 24 semantic mutations, and three empty-owner controls.  This task
reuses those frozen engines; it does not rewrite their mathematics.

## 2. Sole permitted outputs

Create only:

- `ci/in/d972_r07_pre_a0_single_target_a3_v1.prereg.v1.json`;
- `search/d972_r07_pre_a0_single_target_a3_v1.py`;
- `crosscheck/check_d972_r07_pre_a0_single_target_a3_v1.py`;
- `search/d972_r07_pre_a0_single_target_a3_gha_driver_v1.g`;
- `sol/luna_reply_359_r07_pre_a0_single_target_a3_v1.md`.

Do not edit task198, task226, task227, v220, v302, a workflow, or any previous
receipt.  Do not create an output receipt locally.  The new source is an
actual-production implementation, but it remains `UNEXECUTED` until a fresh
Sol(max) audit and a parent-dispatched GHA run.

## 3. Acyclic preregistration graph

The canonical ASCII preregistration owner has schema
`d972-r07-pre-a0-single-target-a3/v1/prereg/v1` and pins all immutable inputs,
the exact terminal vocabulary, caps, mutation roster, expected central rows,
and false conclusion flags.  It must not pin either new program or the new
driver.  Both programs pin the preregistration path, byte length, SHA-256 and
self seal.  The driver pins the preregistration plus the final producer and
checker identities.  Thus the graph is acyclic:

```text
accepted task198 + v302 + frozen engines + g760 provenance
                         -> P0
                         -> producer/checker
                         -> serial driver.
```

No arbitrary `--manifest`, absolute-path alias, mutable environment override,
or receipt-supplied source identity may replace a pinned owner.

## 4. Exact immutable authority roster

P0 and both sides bind at least the following exact identities:

```text
task198 receipt
  ci/in/d972_r07_seven_context_roof_presentation_v1.json
  31,017,244 bytes
  82f7955580039f2a0271896c928515d26996f636d8e73231331da6a37f6b19f5
task198 acceptance_v2
  ci/in/d972_r07_seven_context_roof_presentation_v1.acceptance_v2.json
  2,722 bytes
  cc8c16c8ad8f2d094868f0897bcca2a98adba75c18bf7ff397f0da67fd233ea4
task198 producer attestation
  81 bytes / b5ab577d14ed490af12e3921ee41cfea533abcbf92d60cc037f0d40035ba5090
task198 checker attestation
  95 bytes / 260eb23f73a8fb6b9cd2316aa4ea6c29a4a6db92e77d8c5c6f4f1dd6e7ff290e
task198 checker verdict
  150 bytes / ac841c5a979bbe89bdd47c73151ecabf29783793b7b288b4d08c4824596251de

task226 producer engine
  search/d972_r07_actual_two_word_endpoint_specializer_v2.py
  40,556 bytes / a1532740a7343bd8166c17947f6bd95203a4abdaaafd8e0d9607d3cdf202e6fb
task226 independent engine
  crosscheck/check_d972_r07_actual_two_word_endpoint_specializer_v2.py
  35,463 bytes / e49e4ee24b56e35f8c8120bad7579865e497d94f57b2af51664d562f50ffaa44
task227 producer engine
  search/d972_r07_typed_single_seed_endpoint_consumer_v2.py
  47,135 bytes / 755ba97e55266bcdb51796cc1a89a562efa782db48475d0e3479e82e325cde8e
task227 independent engine
  crosscheck/check_d972_r07_typed_single_seed_endpoint_consumer_v2.py
  34,200 bytes / 028e615bd71276c22cea2180b8ff59e53d8e9ee745c84a1912c862f217f2bb95
accepted task227 driver ancestry
  search/d972_r07_typed_single_seed_endpoint_consumer_gha_driver_v2.g
  5,387 bytes / 38352fd53e2aa2534e6b4d61c5a613c38fd65c4a6843fa5cb6dd2a04918cfe7d
```

Also bind the task198 producer/checker/driver identities stated inside its
acceptance manifest, both v302 identity and `g760` ancestry sources:

```text
search/d972_r07_760_l3_target6_v1.py
  53,284 bytes / 7048e73a02e76df5d49fd359c52d5be70ae99d70aa95ebe74b28c4a18f130fde
search/check_d972_r07_616_to_760_commutator_affine_rhs_v3.py
  33,409 bytes / f8c7fc7f5b5bbfffa0cf147a59313981c5a4b2c6c00504a9f773029097fdde5f
```

Authenticate the task198 manifest self seal, receipt self seal, raw canonical
bytes, all receipt/member/run/head/artifact/zip/terminal bindings, the two
attestations, checker verdict, and all three source owners.  A raw SHA match
does not excuse failure to decode the exact occurrence ledger and evaluator
ABI.  Read/parse task198 once per process; do not retain an avoidable second
31-MB copy.

## 5. Independent `g760` reconstruction and computational-base typing

Each side contains its own literal `W2`, `W3`, free reduction, inverse and
exponent-sum implementation.  Reconstruct

```text
g616 = red(W2 (W3^-1 W2)^8)
g760 = red(g616 y^36 x^-108)
```

and require length 616/digest
`3680e8bcbac37747467175454b082485b2ae296f1fb05244435d8f44979d4e90`,
then length 760/digest
`518f09820b8d7baee6b58ebf366cebf7b02a9a945cd1350cf8c701a4e6bc2b4d`
and exponent sums `[0,0]`.  The checker must not import the producer's word
constructor.

The producer dynamically loads only the pinned task226/task227 producer
engines under unique module names.  The checker dynamically loads only the
two pinned `crosscheck/` engines, never either producer engine.  Hash before
import; no `sys.path` name lookup or shared new helper is allowed.

Build the v216 ABI by calling the side-local task226 mathematical constructor
with `(g760, [], exact_task198_ledger)`.  This is a computational base point:

```text
mode = PRE_A0_COMPUTATIONAL_BASE_ONLY
correction_word_constructed = false
task192_consumed = false
f_role = BASE_REFERENCE_EQUAL_TO_G760
```

Require `f == g760`, `a == []`, `B_a == 0`, and every `rword_f/r_f` equal to
its base counterpart.  Retain fields needed by the frozen v216 ABI, but never
call this object an actual A0 correction, compatible lift, or fake.

## 6. Executable v302 area-invisibility replay

Do not copy only the theorem's final Boolean.  On each side independently,
use its own class-two multiplication, inverse, commutator and substitution
implementation to derive the signed central contribution of all eleven
literal task198 rows.  In residue coordinates modulo nine the exact
preregistered rows are:

```text
H1: [6], [6], [6]                         sum [0]
H2: [3], [3], [3]                         sum [0]
P:  [0,0,0,6], [0,6,6,0], [6,0,0,0],
    [0,0,3,3], [3,3,0,0]                 sum [0,0,0,0].
```

The signs must come from the authenticated occurrence ledger and the vectors
from the independently evaluated substitutions; neither may be read from an
output receipt.  Record every row, block product/sum and identity check.

As a second canary, let `zword=[x,y]^3` in the frozen commutator convention.
Construct the projected area representatives `zword^t`, `t=0,1,2`, through
the side-local task226 constructor and require all three exact
`bar_epsilon_1` blocks to equal the base target.  Label these values
`PROJECTED_AREA_REPRESENTATIVE_ONLY`; they are not assertions that `zword^t`
is an A0 roof correction.  Also require the base ABI target to be exactly
`1-R_B(g760)` through the side-local Fox/endpoint identities.

## 7. One actual closure and certificate

The producer passes the base ABI to the pinned task227 producer closure once,
then encodes exactly one production gate.  It must retain:

- the complete occurrence basis and ancestry;
- all canonical 486 ideal rows and all 729 translates;
- the block basis/echelon/remainder and exact rank data;
- for MEMBER, coefficient-bearing `c_i`, `lambda`, `kappa` and all four replay
  rows ending in the target;
- for NONMEMBER, a nonzero separating dual, zero pairings on the occurrence,
  486 and 729 rosters, and target pairing one.

The checker reconstructs the base ABI and invokes only the independent
task227 verifier.  It independently rebuilds the orbit, 486 roster, 729
translates, two-way span comparisons, block echelon, coefficient replay or
dual pairings.  It accepts exactly one of:

```text
R07_PRE_A0_A3_PROJECTED_MEMBER
R07_PRE_A0_A3_PROJECTED_NONMEMBER_DUAL
```

`UNKNOWN_INPUT` and `UNKNOWN_RESOURCE` are non-accepting.  The checker verdict
binds the physical receipt bytes/SHA, P0, all source identities, reconstructed
ABI SHA, central replay SHA, ranks, terminal and its independent result SHA.

An accepted MEMBER removes only this projected A3 filter.  An accepted
NONMEMBER obstructs every registered A0 correction on the fixed `g760`
branch.  Neither outcome asserts the pointed gate, exact PB endpoint zero,
mixed-prime/perfect-core continuation, cofinal lift, fake, or Ihara witness.

## 8. Cheap actual-glue mutation controls

The closure engine's 24 expensive semantic mutations were already accepted
in run `33153010409`; do not rerun a 486/729 closure for every wrapper
mutation.  P0 preregisters a small glue roster covering at least:

```text
task198 raw/manifest binding; task198 ledger sign; task198 prefix;
g760 letter/digest; computational-base mode; forbidden task192 binding;
one H1 central row; one H2 central row; one P central row;
one projected-area target; ABI seal/target; forbidden conclusion flag.
```

Each side mutates an extant independently constructed owner, runs the cheap
pre-closure validator, catches only its narrow registered rejection, and
records changed before/after SHA and first gate.  `MutationAccepted` remains
outside that catch.  Expected reasons are comparison data only, never control
flow.  The producer's records do not substitute for the checker's executions.

## 9. Resources, fresh output and serial GHA driver

One meter per process covers authority reads, DOM/canonical allocations,
dynamic imports, three projected-area builds, the one closure, receipt
serialization and optional verdict.  Reserve before allocation, enforce a
measured wall deadline and RSS cap, and return only typed UNKNOWN on a cap.
No sleep, retry, polling, process pool, local parallelism or subprocess is
allowed inside either Python program.

The driver runs producer then checker serially with `python -u`, fresh
`ci/out/` receipt/verdict paths, redirected logs, and a bounded external
timeout.  It hashes every pinned owner before execution, requires exactly one
producer terminal and one checker terminal, prints both logs on mismatch,
checks fresh output bytes, and ends in one explicit sentinel.  It must not
claim PASS from process exit zero alone.  Producer/checker use exclusive
fresh-file publication; stale paths and post-check substitution fail closed.

The expected workload is one closure, not the five closure cases of run
`33153010409`.  State the conservative wall/RSS/output caps and a one-case
runtime estimate in the reply, but measured values remain `UNEXECUTED`.

## 10. Reply

The reply records exact bytes/SHA-256 of all four machine owners and P0's self
seal, the complete authority/import graph, line-numbered central-table and
base-typing traces, the mutation matrix, resource formulas, driver commands,
and every limitation.  Explicitly state that no Python/GAP/GHA was run.

End exactly with:

```text
V302 ACTUAL CENTRAL REPLAY:          IMPLEMENTED or BLOCKED
PRE-A0 COMPUTATIONAL BASE ABI:       IMPLEMENTED or BLOCKED
ONE ACTUAL 486/729 CLOSURE ROUTE:    IMPLEMENTED or BLOCKED
INDEPENDENT MEMBER/DUAL CHECKER:     IMPLEMENTED or BLOCKED
CHEAP GLUE MUTATIONS:                IMPLEMENTED or BLOCKED
SERIAL GHA DRIVER / P0:              IMPLEMENTED or BLOCKED
EXECUTION / GHA:                     UNEXECUTED
ACTUAL A3 NUMERATOR:                 remains 0/3 pending accepted run
A0 COMMON / POINTED / EXACT PB:      OPEN
COFINAL LIFT / FAKE / IHARA:         NONE
```

`TASK359_R07_PRE_A0_SINGLE_TARGET_ACTUAL_A3_COMPILER_V1`
