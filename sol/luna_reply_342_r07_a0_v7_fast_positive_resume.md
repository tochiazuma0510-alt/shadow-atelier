# Luna reply 342 - R07 A0/v7 fast history-free positive resume

## 1. Result and authority

The complete v7 route requested by the commission is present by static
inspection.  Nothing in this commission was executed: no Python, Node, GAP,
GHA, workflow, network, or git command was run.  In particular, this reply
does not report a COMMON, a checker pass, a speed measurement, or an A0
witness.

The only mathematical success terminal implemented by both producer and
checker is

```text
R07_HISTORY_FREE_POSITIVE_FAST_RESUME_V7_COMMON_WORD
```

The two nonpositive terminals are typed `UNKNOWN_INPUT:<reason>` and
`UNKNOWN_RESOURCE:phase=...:cap=...:value=...:limit=...`.  They carry the
seven all-false claim flags.  No route emits a separator, NONMEMBER,
exhaustion, cofinal lift, fake, or Ihara claim.  A programming/protocol
exception remains a nonzero STOP.

## 2. Outputs and frozen identities

These are post-write byte identities obtained with read-only filesystem
hashing after the final producer-to-checker and driver pins were installed.

| output | bytes | SHA-256 |
|---|---:|---|
| `search/d972_r07_history_free_positive_fast_resume_v7.py` | 122,132 | `cddef2abe41aaad9b2c0dd81d6fcc67b603bbf70359888fdfd34bf1d9f069ef9` |
| `crosscheck/check_d972_r07_history_free_positive_fast_resume_v7.py` | 96,334 | `8afcf5653eefedfb1639ea9a0d30e6d7ba5fc3f337406295b4522c31ae61423c` |
| `search/d972_r07_history_free_positive_fast_resume_gha_driver_v7.g` | 10,156 | `232f67106d5d21c87e01b5889112c48c687186ddd6b3539f0664973b50e4d558` |
| `search/certs/d972_r07_history_free_positive_fast_resume_selftest_v7_20260829.json` | 2,783 | `5f6628d8413ebcc78a87196fa999b4f73122d4bca2c22efc0a9f637e59f483c6` |

This reply does not contain a recursive self-pin.  Its final physical
bytes/SHA are to be recorded externally after this last write; changing this
document to insert that hash would change the hash again.

The checker pins the final producer identity above.  The ASCII driver pins
the final producer, checker, fixture, commission/proof/audit authorities,
all transitive mathematical sources, the 1,328-byte manifest, and the
5,001,811-byte staged ZIP.  No unfinished or placeholder pin remains.

## 3. Literal import, call, and process graph

```text
GAP v7 driver (one invocation)
  -> frozen ZIP extraction helper (one Python process)
       sole member gate -> one immutable 86,368,039-byte raw path
  -> v7 producer (one Python process, --seconds 10800, W=2 or W=4)
       SourceRegistry.authenticate (one byte snapshot per pinned source)
       -> explicit build_light
            old E3/E4 + task176 packed codecs + ten contexts
            + g760 target + PB3/PB4 rows + complete 6,441 roster
       -> validate old physical checkpoint and compute each P_j=A_j*C
       -> inject P rows/order/formal expressions into fresh FormalReducer
       -> real-owner SELFTEST (separate bounded fork owners at W=2 and W=4)
       -> one production PersistentBoundaryOwner (Linux fork, reused by epoch)
            ACTIVE -> direct row replay -> formal n: symbol -> fresh dual
            complete zero -> one Q0-LATE build -> correction oracle
       -> COMMON candidate, or typed resource/input transport
  -> v7 checker (one Python process; never imports the producer)
       UNKNOWN -> physical transport/checkpoint only; no light/Q0 mathematics
       COMMON  -> independent checker-light runtime
                  -> reconstruct only selected old/new support
                  -> target/PB3/PB4/correction/all-eleven/direct-all-seven
       no checker subprocess
  -> exact terminal comparison
  -> sentinel write (literally the shell's final successful operation)
```

The producer does not call cached-v3 `main`, cached-v3
`rank_zero_resume_checkpoint`, live-v1 `build_runtime`, live-v1
`PositiveSearch`, or an old checker.  The checker authenticates the producer
as bytes but does not import it.  Immutable predecessor modules are compiled
from the authenticated in-memory snapshots under private module names.

The production boundary owner uses nonblocking `AF_UNIX` socketpairs,
length-framed canonical JSON, one absolute deadline, complete half-open
interval collection, and bounded terminate/join/kill/`Process.close`.
Timeout, blocked pipe, death, and partial-frame faults discard the whole
epoch.  There is no blocking `Pool.map`, polling sleep, per-epoch pool, or
per-pair shared-memory counter write.

## 4. Old checkpoint use and exact arithmetic

The staged source is opened no-follow, required to be a unique regular
physical file, hashed, and parsed once.  Its frozen facts are:

```text
raw bytes / SHA-256                       86,368,039
  c261aa967867a4870228eae467f46ee4afbfc236445890debd891bcef4a250ab
old self digest
  29bb74f3bd8048913a0365bc4c599f3731d32ba56967f3a238c7468b7fcfd123
old input digest
  f29eaf9b945adb3bde89395ae9cb9018309fe8f3938d32f55e6716574b861cfb
target digest
  968f0b8325fa0e741e2c304bb940b96239c3e2d3226e0ca56f7d61a53dd0d82b
P/pivot-row digest
  3c645f4e352c96691dd35d6202bdf5f8b2cce73b7eb5f1bdf33a8daa06ce9d28
fresh first-dual digest
  0960259714fa94ddd89e2ac4f582f040942ab7bd258185c0448c133e50b00f0c
normalized-semantics digest
  07c91e42c91648c5139ec13afd7fe0f44aff964612bae950d9dbd941b509b109
```

The exact discovery arithmetic frozen in the implementation is:

| quantity | exact value |
|---|---:|
| columns / rank | 2,896 / 2,896 |
| boundary / correction columns | 2,896 / 0 |
| block counts | 2,883 / 2 / 11 |
| identity seeds / ACTIVE records | 15 / 2,881 |
| raw sparse support total / max | 20,354 / 12 |
| ancestry entries total / max | 137,926 / 258 |
| ancestry-weighted contributions | 1,011,460 |
| computed P support total / max | 289,774 / 522 |
| future ancestry indices | 0 |
| zero/missing diagonal | 0 |
| duplicate/empty/wrong pivots | 0 |
| first dual support | 1,188 |
| first dual support types | exactly `(block,component)=(1,1)` |
| matching descriptors / all descriptors | 4 / 104 |
| first expanded boundary pairs | 4,752 |

Every `P_j=sum_i a_ji C_i` is a separate sparse product.  The serial parent
checks canonical ancestry, diagonal, registered pivot coefficient one,
earlier-pivot zeros, and least live key before direct injection.  The old
data are explicitly labelled `heuristic_discovery_only=true` and
`exact_cached_resume=false`; old elapsed time is never subtracted from the
fresh 10,800-second budget.  No old boundary provenance is directly replayed
at startup.

The initial formal owner is the actual 137,926-entry old-symbol expression
map, not a digest.  Every new literal row receives an `n:NNNN` symbol and an
actual pivot expression.  The hard formal cap is 2,000,000 entries.

## 5. Boundary and Q0-LATE implementation

The boundary owner derives typed nonzero support, the four matching
descriptor ids, and the 4,752 pair prefix.  Each literal pair computes
`t=g*h^-1`, checks `t*h=g`, accumulates in F3 at
`(block,t_blob,relator)`, and contributes to a globally lexicographic winner.
The parent uses the same once-decoded support to reconstruct every selected
contributor and the direct translated row.  It publishes literal pair,
support-codec, frame, accumulator, formal-ancestry, sampled RSS, and wall
accounting.  Worker result frames themselves are retained in the selected
epoch record, so changed-accumulator testing is not a hash-only model.

Q0 is absent from the light runtime.  Only after a complete boundary zero,
`ensure_heavy` first writes a last-safe light checkpoint and then constructs
qstates/qids/parents/letters, ten stores, memberships, adjusted-L data, all
ten coarse indices, and kernel states.  `heavy_input_sha256` is published
last.  A correction before that publication is a hard protocol stop.

The K=0 selector resumes by support-fibre cursor and resets the kernel cursor
when it advances to the next target.  The K!=0 selector uses the frozen W+1
prefix (or typed fair fallback).  Each hit carries a literal delta word,
ten coordinate blobs, roster identity, schedule state, direct joint-kernel
evaluation, eleven-occurrence/direct-all-seven equality, ACTIVE dual, and a
formal new-row symbol.

## 6. Checkpoint and physical transport

A resource cap after the fixed light/P prefix but before child startup writes
a P-bound pre-pool checkpoint with a clean no-child owner.  A cap during or
after Q0 uses the live `Search` checkpoint owner after bounded child cleanup.
Both bind source member identity, source snapshots, light/heavy phase, all
2,896 canonical P rows and their digest, complete formal ancestry, new live
records, target/remainder/dual, correction cursors, next clean epoch,
counters, cleanup, and all-false claims.  Resume restores the formal live
state and starts a fresh clean persistent roster; it never reconstructs an
old sequential Echelon trajectory.

Checkpoint serialization stores source references rather than an 86 MB
converted clone.  There is one canonical write, not the v6 repeated
deep-copy/fixed-point loop.  The checkpoint cap is 4,000,000,000 bytes.

The checker reads receipt and checkpoint through no-follow descriptors,
requires link count one, binds device/inode/size/mtime/bytes/SHA, and rechecks
pathname identity.  COMMON forbids the checkpoint sidecar.  UNKNOWN_RESOURCE
requires the exact canonical sibling and reference.  UNKNOWN_INPUT forbids
it.  The UNKNOWN branch authenticates P/formal/counter/phase transport but
does not construct E3/E4/Q0 or replay 2,896 mathematical rows.

## 7. Independent selected-support COMMON gate

For COMMON, the checker independently rebuilds the actual target from
H1/H2/P and reconstructs each selected record from literal provenance:

1. old seed/ACTIVE PB3/PB4 boundary rows, including identity translation or
   complete ACTIVE contributing pairs and `t*h=g`;
2. new boundary rows, including the complete worker epoch result;
3. new correction rows from literal delta/relator words, independent weighted
   eleven-occurrence formula, ten coordinates, schedule bounds, joint kernel,
   and direct all-seven H1/H2/P differences;
4. the complete F3 selected sum equal to the actual target;
5. coefficient two as the inverse conjugate, with an independent inverse-row
   replay;
6. the exact typed PB3/PB4 boundary preimage;
7. correction-product exponents, direct additivity, corrected word, and zero
   final residual.

Producer booleans, scalars, words, and digests are compared only after the
checker has derived the corresponding value.  One selected mismatch is a
hard checker rejection.  Unselected discovery rows cannot authorize COMMON.

## 8. Static work/memory bounds

No runtime timing or RSS measurement was made.  The mechanically fixed work
owners and caps are:

```text
old raw sparse entries held                  20,354
old formal ancestry entries                 137,926
independent A*C contributions              1,011,460
P sparse entries held                       289,774
initial boundary support / pairs          1,188 / 4,752
general boundary-pair cap                 8,000,000
per canonical transport frame cap         32 MiB
formal-expression cap                     2,000,000 entries
fresh retained-column cap                   250,000
fibre-scan / candidate-word caps    80,000,000 / 2,000,000
sampled parent+child RSS cap             5,700,000,000 bytes
checkpoint cap                           4,000,000,000 bytes
global delta order                         357,128,352
Q0 state count (only if heavy starts)         1,469,664
Gamma state count (only if heavy starts)              243
```

Worker memory is `O(S + N/W)` for decoded typed support and one shard
accumulator, with `W in {2,4}`; parent merge memory is `O(N)`.  RSS is named
honestly as a sampled parent RSS plus sampled child RSS sum, not exact unique
physical memory or PSS.

The first remaining production cost is therefore the fixed source
authentication/raw parse plus the 1,011,460 sparse A*C contributions.  After
that safe checkpoint boundary, the first live oracle cost is exactly 4,752
boundary pair products.  Q0 is not a startup cost and occurs only if that
complete epoch is zero.  These are static work counts, not elapsed-time
claims.

## 9. v6 duplicate work removed

The literal v7 graph removes the following audited v6 owners:

- the cached-v3 wrapper followed by the live-v1 resume constructor;
- both producer-wide 2,896-column rank/provenance insertions;
- both checker-wide 2,896-column replays (the audited 33,540,842 checker
  pivot-loop visits are not on the v7 COMMON path);
- eager qstates/stores/membership/adjusted-L/coarse-index construction before
  the first boundary result;
- per-layer rereads within the producer instead of one source byte snapshot;
- per-epoch pool construction, descriptor republication/rehashing, and scans
  of the 100 nonmatching descriptors in the pinned first epoch;
- repeated parent support decoding and winner-wide second support decoding;
- a shared-memory/cache-line write per pair;
- repeated 86 MB checkpoint deep copies, canonicalizations, and fixed-point
  reseals;
- the cached-v3 checker subprocess, mutable-path reopen, temporary COMMON
  checkpoint, and copied compact verdict fields;
- inheritance of old elapsed time and old cumulative counters as a fresh
  v7 budget.

## 10. Real-owner SELFTEST and physical mutation ledger

The fixture uses the production task176 E3/E4 packed codecs, actual reducers,
and actual Linux-fork owner.  Static inspection shows the following literal
cases, but none was run in this commission.

- W=2 and W=4 process cases: empty, one, short, typed-present filtering,
  constructed F3 cancellation, ACTIVE, zero, three serial duals, timeout,
  blocked send, death, partial frame, atomic discard, cleanup, and
  `Process.close`.
- Eight physical triangular mutations: future ancestry, zero diagonal,
  changed raw entry with its local digest resealed, changed ancestry
  coefficient, duplicate pivot, wrong pivot, hidden smaller pivot, and
  skipped P equation.
- Thirteen physical boundary/owner mutations: wrong typed support, missing
  interval, overlap, wrong orientation, changed literal accumulator, winner,
  scalar, cross-epoch frame, blocked/partial/dead worker, survivor, and
  counter reset.
- Six phase mutations: heavy-before-digest, fabricated heavy digest, stale
  correction cursor, zero promoted to negative, light resource checkpoint,
  and heavy transition.
- Seven physical selected-support mutations: omitted/changed row, changed
  coefficient, a dedicated literal coefficient-two inverse-word frame,
  copied equality Boolean, changed target, and changed boundary preimage.
- Eleven physical transport mutations: symlink, hard link, TOCTOU pathname
  replacement, unbound checkpoint, positive claim on resource, separator,
  cofinal, fake, and Ihara flips, terminal reseal, and stale output.

Mutations are written to physical files/frames before the actual validator.
The mutation harness catches only its narrow `CheckStop` (producer process
faults catch their typed transport/resource owner), not a broad same-shaped
post-hoc exception.  The UNKNOWN branch runs the transport-only mutations
without constructing positive mathematics.  The COMMON branch runs the
boundary, selected-support, and transport ledgers.

## 11. Driver and remaining execution boundary

The driver stale-rejects only its v7-owned raw/receipt/checkpoint/verdict/log/
terminal/shell/sentinel paths.  It extracts the exact sole ZIP member once,
runs exactly one producer and one checker, compares their one-line terminal
payloads byte-for-byte, enforces terminal-specific sidecars, and leaves the
sentinel `printf` as the shell's last successful operation.  It uses exactly
`--seconds 10800`; the workflow file was not edited.

Static inspection cannot establish Python syntax execution, process
availability, runtime resource use, a terminal, a checker verdict, or a
witness.  The first authorized next action is the pinned GHA run.  A COMMON
plus independent PASS would make the A0 numerator 1/1; until then it remains
0/1.

```text
IMPLEMENTATION:                       IMPLEMENTED
EXECUTION:                            UNEXECUTED
HISTORICAL/CACHED PATH PARITY:         NOT CLAIMED
HEURISTIC DISCOVERY BASIS:             IMPLEMENTED
Q0-LATE BOUNDARY:                      IMPLEMENTED
SELECTED-SUPPORT POSITIVE CHECKER:     IMPLEMENTED
ACTUAL A0 COMMON + CHECKER:            0/1
SEPARATOR / NEGATIVE CLAIM:            FORBIDDEN
LIFT / FAKE / IHARA:                   NONE
```

`TASK342_R07_A0_V7_FAST_HISTORY_FREE_POSITIVE_RESUME`
