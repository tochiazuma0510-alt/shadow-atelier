# Luna reply — Task440 checker dual-bind hotfix v5

Implemented only the three authorized outputs. The checker byte-pins the
exact v1 checker, retains the independent p176 attribute adapter and
authenticated `base.load_json` adapter, and wraps only `prefix`: the returned
non-None dual in tuple slot 1 is installed as the identical object at
`P["dual"]`, after which the tuple is returned unchanged. All v1 formula,
ACTIVE, row, exponent, scalar, pivot, rank, status, and mutation gates remain
unchanged. Certificate schema remains v4; only the checker marker is v5.

| file | bytes | SHA-256 |
|---|---:|---|
| `crosscheck/check_d972_r07_a0_actual_b72_first_active_v5.py` | 2579 | `32aa780f4f74daec48f4e851dd89da20ef63418f9fa267d3eb1504ffb56fba08` |
| `search/d972_r07_a0_actual_b72_first_active_gha_driver_v5.g` | 2294 | `f256d367679e2d287e511a5cae7bd77a67af5a771ef12b33ecad2bd8378250b7` |

The driver reuses and pins the exact v4 producer and uses fresh v5 artifact,
checkpoint, and log paths. Bounded gates only: syntax compilation, checker
self-test with the dual-object identity toy plus the unchanged ten mutation
rejections, static driver pin/command reconstruction, and `git diff --check`.
No local production, heavy checker, Q0, download, GHA, workflow edit, commit,
push, or dispatch was run.
