# Task973 — 完全oracle＋E動的継続器の限定source監査

役: Luna 敵対的な実装接続監査。Task967を最終freezeし、rootの開始連絡後に着手。
変更可は `sol/luna_reply_973_r07_complete_oracle_cegar_continuation_audit.md` のみ。
既存source/返信の変更、ローカル数値/Python/import/AST/GAP、git/network/credential、
dispatch/追加agentは禁止。完成blockの読取とmetadata/bytes/hashに限定する。

Task971/972、reply970を全文読み、971/972両新sourceと新workflowの完成blockを順次監査する。
Task957/958/963/964、v547/v548と2131七限定/2138八限定/2139射程訂正を保持する。
現在Eの実数値結果はrootが別途渡すまで未観測。source READYとruntime PASSを区別する。

重点は次の接続である。

- 起点の実oracle＋外部Eの全pin/rolling/plain-target/DERIVED来歴、動的attachが一回だけで
  rank/gen/offer/offsetとcompleted_stepsを二重計上しないこと。元rho2 hashの階層を正しく解く。
- 新fixed geometry/basis/reader manifestと各current snapshotを区別。新lambdaの全8059
  dual式、全六tag cochain、54433 chord/二aux、選ばれた同じwitnessへの再接続を省かない。
- rawの普通epsilon/omega/endpoint/同一六tag直接Foxから元lead昇順primal、同じP1 DAGの
  mod54、fresh tupleへの一回補正と全96776 lower零、四B、挿入順physical/一回normalize、
  target scalar0とfreshlambda全row/両targetまで、動的wrapperがfrozen算術の前提を維持。
- phase完了保存とloader、checkpoint/current snapshot/parent_state_head、manifestとHEADの
  公開順序、durable tail/orphanの型、cap累積/resume/UNKNOWN停止位置、二重適用の拒否。
  前lambdaのoracleを次lambdaで再利用しない。完成数値phaseの再実行をresume条件にしない。
- 全new prefixの独立checkerと全array/JSON/EOF比較、source/candidate境界、保持helper
  由来。producer新算術のchecker import/clone、固定pin monkeypatchがないこと。
- complete-zero/linear-target-zero/未計算/UNKNOWNの型、Task958全11slot pending、
  source-lower零とConn込みphysical-lower零、original rho2 DERIVEDの区別。
- workflowの実parent/source/data/CLI/result gate、内側期限<外側期限、診断と候補の保存、
  cap1→実resume、F-sc-3 v2 full --selftest一回、新interfaceだけのcanary、本番関数への接続。

ABI/canaryをproducerとcheckerへ伝えるときは規約・型・反例条件に限り、算術helper共有を
勧めない。未完成blockは保留として報告し、全完成済みに対する具体的な必須修正を随時rootへ。
第三被覆score/f/treeをq/κ/旧scanへ広げず、現lambdaのq1..3零を作用素恒等零と呼ばない。
確率/physical像の独立本数/残り反復数・所要時間を推測しない。Gamma/fixed packetを新release
条件に加えない。最終bytes/SHAと全残gateを読み、最終行 `AUDIT_973_VERDICT:`。
