# Luna task 328 - task315 A4/v3 actual local evaluator and certificate repair

Role: Luna, bounded implementation only.  Read all sections first to last.
Do not run Python, Node, GAP, GHA, workflows, git, or network.  Preserve all
v1/v2 files, staged authorities, workflows, and v220.  Edit only the five
versioned outputs named below.

## 1. Inputs and sole outputs

Read in full:

- `sol/luna_task_315_r07_a4_semantic_incremental_performance_rewrite_v2.md`
- `sol/luna_reply_315_r07_a4_semantic_incremental_performance_rewrite_v2.md`
- `sol/sol_task_323_r07_task315_a4_v2_code_performance_audit.txt`
- `sol/sol_reply_323_r07_task315_a4_v2_code_performance_audit_v1.md`
- `sol/sol_task_327_r07_task198_authority_bundle_v2_audit.txt` and its reply
  if present;
- `sol/proof_r07_a4_restricted_evaluator_sufficiency_v267.md`;
- the complete A4/v2 quartet;
- the complete task198 producer/checker and the accepted receipt;
- the new `acceptance_v2` manifest and exact checker-verdict member;
- task176 producer/checker and their pinned inputs; and
- v247 plus every paper/input pinned by A4/v2.

Create only:

- `search/d972_r07_word_independent_successor_kernel_v3.py`
- `crosscheck/check_d972_r07_word_independent_successor_kernel_v3.py`
- `search/d972_r07_word_independent_successor_kernel_gha_driver_v3.g`
- `search/certs/d972_r07_word_independent_successor_kernel_selftest_v3_20260828.json`
- `sol/luna_reply_328_r07_task315_a4_v3_actual_local_evaluator_repair.md`

Return `IMPLEMENTED / UNEXECUTED` only if the actual positive branch is
reachable by static trace and every gate below is present.  Otherwise return
`BLOCKED / UNEXECUTED` with the first exact missing owner/API.  No execution
is authorized here.

## 2. Exact authority-v2 consumption

Pin the final independently accepted authority-v2 manifest identity from
task327.  If task327 is absent or REJECT, return BLOCKED and do not substitute
the rejected old four-file manifest.

Authenticate exact resolved top-level paths, not merely containment and final
basename.  Reject symlinks, aliases, nested same-basename paths, absolute
paths, traversal, and output/input overlap.  Bind the receipt, manifest, both
attestations, the 150-byte checker verdict, all direct run/head/artifact/ZIP/
member/source identities, both manifest/receipt self digests, and literal
`accepted=true`, `independent=true`, `synthetic=false`.

Replay the actual receipt schema rather than v2's invented spellings:

- exact three local ordinal blocks `1..6318`, `1..104`, `1..19` and seven
  sealed chunks;
- the real normal-generation field names and values (including
  `Gamma_cayley_state_count`, Gamma order/normal-closure order 243, and Q0
  order 1,469,664 in their correct distinct fields);
- the complete bridge occurrence ledger including `block_index` and
  `block_slot`, its digest, seven blocks, eleven occurrences, four marked
  inverse replays, and ten-to-eleven map; and
- the full evaluator ABI, exact callable names, widths, semantics, row and
  coordinate digests, and every canary value/group-law replay.

No Boolean copied from the receipt is evidence without its underlying replay.

## 3. Actual lightweight local evaluator

Remove every unconditional
`LOCAL_AUTHENTICATED_EVALUATOR_NOT_STAGED`/positive-replay stop.  Do not call
task179 `build_runtime`, enumerate 1,469,664 Q0 states or 2,939,328 edges,
construct the raw Q0 section store, build Gamma's full Cayley graph, or copy a
second 6,441-row roster.

Construct the actual restricted evaluator directly from pinned lightweight
objects:

1. load the frozen E4 arithmetic module and the pinned q3 receipt;
2. reconstruct the actual E3/E4 quotients and 31-row context registry;
3. load task176's pinned group-codec primitives;
4. build the finite deletion map from `build_fine_deletion`, the two Q0 marked
   permutations in the q3 receipt, and `make_deleter`; and
5. expose exact ten-coordinate `eval`, `multiply`, `inverse`, `action`, and
   section-cocycle operations using task176's packed widths.

This restricted runtime need not answer arbitrary Q0 state-section queries:
all source words needed by A4 are already present with ancestry in the
accepted 6,441-row receipt.  If some v247/A4 operation genuinely requires an
unregistered arbitrary state section rather than a receipt word, identify
that exact call and return BLOCKED; do not rebuild global Q0 silently.

Authenticate the local runtime against every task198 ABI canary and group-law
identity.  Then evaluate each of the 6,441 accepted source words directly in
the ten contexts and reconstruct its seven typed defect occurrences.  Retain
only one parsed authority roster and compact cached actor values.  Cache the
forty actually used actor/context actions and bind their word/value digests.

The independent checker must reconstruct the same mathematics from the
pinned task176 checker/frozen arithmetic interfaces with independently
written glue.  It may not import the v3 producer, producer echelons, producer
runtime wrapper, or producer mutation oracle.

## 4. Exact coefficient-carrying total and boundary bases

Implement one live total `B+K` echelon and one boundary echelon.  Every pivot
row carries a fully scaled coefficient vector in the immutable raw-row label
space.  Apply every scale/add operation to both row and coefficients.  A raw
boundary or K row is reduced exactly once before insertion; do not pre-reduce
and then insert/reduce again.

For every accepted/dependent row, directly replay the returned coefficient
map against immutable raw rows and require equality with the original row.
Fix the v2 NONMEMBER sign.  NONMEMBER receipts must independently prove
all-row annihilation and nonzero target pairing in the authenticated typed
key universe; never enlarge it with an artificial key.

Keep raw `K:i` labels distinct from normalized pivot rows.  Compute action
and basis-change maps only after translating through the stored raw-row
transforms.  Provide two-way span equality modulo independently rebuilt
boundaries.  Never compare coordinate dictionaries from different bases.
The checker must use a genuinely different coefficient algorithm and replay
both directions rather than trust producer `replay` flags.

## 5. Word-bearing ancestry and v247

Use integer DAG node ids only.  For every initial or translated K rank raise,
the word-bearing node must represent the same normalized
`scale*(raw - old K combination)` as the stored row modulo the explicit
boundary combination.  Include normalization scale, all prior-K source
words, inverse/exponent-two factors, and the outer-first conjugator convention
in deterministic retained order.

Memoize DAG evaluation by node id and bound node/edge/expanded-word work.
Directly evaluate each materialized word in the actual local first-successor
runtime, not only synthetic H2(9), and prove its defect differs from the
stored basis row by exactly the exported boundary ancestry.  Independently
replay the depth-two noncommuting canary.

Run every v247 H2, least-index, inverse-scalar, powered-word, coarse,
successor, context, and anchor gate on these actual words.  Build the actual
invariant closure, actual K, and word-bearing anchor; do not emit a shaped
positive dictionary.  If closure or anchor is genuinely NONMEMBER, return its
typed mathematical terminal and full dual; if a cap is hit, return only
UNKNOWN_RESOURCE.

## 6. SELFTEST and twenty owner mutations

Repair `ToyRuntime.boundary_records` and the six-row duplicate-key collapse.
The positive synthetic route must reach and execute the complete kernel,
change-of-basis, word, anchor, envelope, and checker path.

All twenty registered mutations must change canonical bytes of their named
production-shaped owner, be correctly resealed, reach that exact gate, and
be rejected with a narrow registered reason/stage.  Do not catch broad
runtime/key/index/stop exceptions as success.  In particular invoke actual
canonical-byte/path/authority/normal-proof/bridge/evaluator validators for
mutations 1--7; coefficient/echelon/change-map/word/dual/action/v247 owners
for 8--16; a genuinely exceeded live cap for 17; actual positive and
nonpositive envelope validators for 18--19; and physical duplicate log-line
counting in the driver/checker fixture for 20.

The checker independently reruns every mutation and does not trust producer
owner/rejected flags.

## 7. Resource and performance contract

Charge input bytes before allocation/read where the API permits streaming;
stream/hash the 31 MB authority once, parse it once, and retain one object.
Avoid repeated whole-object canonicalization: compute one canonical payload
and reuse its bytes for self digest/write while accurately charging the final
serialization.  Charge all roster/row/DAG/checker work and include the seal
and write in the resource snapshot.

Remove duplicate boundary reductions, duplicate initial/action
serializations, recursive DAG re-expansion, and transient labels falsely
called replayable checkpoints.  `last_replayable_state` must name an actual
rank-zero reconstructible state or be null.  Bound sparse support before
large allocation.  No sleep, retry, poll, lock, pool, hidden subprocess, or
full global roster is permitted.

Give exact loop/query/reduction/serialization counts and defensible bounds in
terms of `n=6441`, terminal K rank `t`, boundary rank `b`, and sparse support.
Explain every deviation from the old claimed bound.  Production and checker
must each remain within the registered wall/RSS/serialized/support/DAG caps.

## 8. Driver, sealed verdict, and final accounting

The ASCII v3 driver must pin exact producer/checker/fixture and authority-v2
identities, stale-reject every v1--v3 owned output, invoke one producer then
one checker, require exact-one full-line terminals and equality, require a
nonempty sealed checker verdict file in addition to logs, enforce
terminal-appropriate sidecars, and write the sole sentinel last.  Do not
embed a recursive driver self-hash.

In the reply list exact final bytes/SHA-256, full dependency/import graph,
authority gates, local-runtime construction and counts, both basis
algorithms, actual positive reachability trace, all 20+20 mutation gates,
performance accounting, and:

```text
IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
SELFTEST / PRODUCTION:           UNEXECUTED
AUTHORITY-V2 INPUT:              accepted only if task327 PASS
ACTUAL POSITIVE BRANCH:          STATICALLY REACHABLE or BLOCKED
ACTUAL A4:                       remains 1/3 at most before execution
LIFT / FAKE / IHARA:             NONE
```

A future Sol(max) PASS may authorize only one bounded synthetic GHA SELFTEST.
Actual production requires a separate post-SELFTEST audit/authorization.

`TASK328_R07_TASK315_A4_V3_ACTUAL_LOCAL_EVALUATOR_REPAIR`
