# Task1010 — fixed-lambda batch 初回 WF の独立静的監査

宛先: 既存 packet_bounds_audit。Task996 の新 P/C source 全読了を維持し、Task1009 の新 workflow を別票で全文監査する。追加 agent は起動しない。変更できるのは `sol/luna_reply_1010_r07_fixed_lambda_batch_workflow_audit_v1.md` だけ。996 はその指定票へ追記して完了する。ローカル Python/import/AST/数値/GAP、network/git/credential は禁止。公開・GHA は root 単一 broker。

## 事前登録と独立性

Task1009 を全文読了し、994–997/1000–1004 の共通契約と照合する。対象は `.github/workflows/d972-r07-fixed-lambda-cycle-batch-v1.yml`、新 P `search/d972_r07_fixed_lambda_cycle_batch_v1.py`、新 C `search/check_d972_r07_fixed_lambda_cycle_batch_v1.py`、各作者票。全 P/C 本文を読める監査官であるが、新 P の私的 API/算術/source/返信を C/WF 作者へ渡してはならない。接続の不整合は root へ公開 ABI の字段・型・根拠位置として報告する。必要 source 修理は各作者が未公開版に施す。

初回は旧受理64段 run33990567016/1、rank1450/gen8155 を唯一の continuation 親とし、fresh、batch_size32/max_batches1/refill=false に固定する。正式96段 rank1482（工房2164）の採用はこの登録親を差し替える理由にならない。全15親・全 ZIP/API tuple/実 entry・全64 step/snapshot/3 invocation・元 owner/source/start/fixed を保持する。新 code は P/C 2＋保持 P9/C10 の21 Python と raw3。旧 continuation C v1 は含めない。初回 source pin は作者完成後の全bytesで root が中継する。

## 重点監査

- workflow trigger/name/path/marker が v1 に一致し、既存凍結 WF を変更せず、任意親・任意 cap を受け入れない。root 実 head を receipt に結び、read-only token と checkout credential 非残留を保持する。
- Task1009 指定の全 API/ZIP authentication、安全展開、取得前 early receipt、部分 intake の always-after、不足時 INCOMPLETE が実経路へ接続される。相対 POSIX 全文字列順と旧保存 inventory の copy-only adapter、旧 start の整数1、旧 refinement HEAD15字段を正しく区別する。
- acceptance exact6、15親全 inventory、七 anchor file、64 step/snapshot/3 invocation の canonical hash、code21/raw3、runtime、registration と公開字段型を照合する。portable identity は parents.path だけを除外し、実 run/host は invocation と execution receipt に保存する。公開 pin が数学本文の共有にならないことを保つ。
- 新 P 三群/C 三群と WF 自身の metadata canary を実 production helper/gate に結ぶ。順序・strict int/bool・欠損・誤 pin・不正 rank の拒否が試験の構築だけで終わらない。旧成功数値 suite は追加再走せず、出典 pin を保持する。未実行を PASS としない。
- P5400秒/C10800秒・各7168MiB、各新群300秒、outer P6000秒/C11400秒、job330分を保つ。全 CLI と公開 ABI の接続、各実 exit/開始終了/stdout/stderr/source/runtime、cooperative stop/UNKNOWN と失敗の区別を確認する。
- P 前後と C 前後で全親/source/raw/acceptance/driver、P output の全 file/dir/EOF を照合する。C output は別 root。新 private progress と public HEAD を混同せず、C partial PASS/durable_tail を candidate にしない。完成済 resume の旧 P invocation/result 不変と今回 C 入力を混同しない。
- candidate は一 P/一 C・全新群・full C partial=false・正規 final/HEAD/result・全保全 gate の成功を要する。selection/processed/dependent/accepted/skipped/rank は実 payload 由来とし、32 採用や rank1482 を期待値として固定しない。rank1450＋実採用数、少なくとも一採用、零 oracle/Linear 分岐の公開契約を区別する。
- 未計算 final lambda oracle/q は null のまま。新 batch 同語 adapter は未完成の明示を保ち、11 slot/80644 読み出しが済んだとしない。grade2 二字段 NOT_DECIDED/full A0=false/verified=false、C 限定照合と工房 CV9 は分離する。
- 全選択失敗列/q・lambda4 character/κ6 tag/aux/ω/scalar/修理三項/候補・phase 別資源は実保存値と全 hash に結ぶ。hidden/pending/orphan を全保存し完了 count へ加算しない。candidate と diagnostics が同じ root の場合は明記し、ZIP hash 差を内容の数学的差へ読み替えない。

## 完了

必要指摘は F# として根拠 source/公開字段/失敗経路を添える。全 WF と最終作者票を全文読了し、source 全bytes/SHA/LF/CR/final LFを再測定して最終票を root へ渡す。Source/WF を公開した後は上書き修理せず新 version。最終行 `AUDIT_1010_VERDICT:`。静的 PASS と新 GHA の実 PASS を必ず区別する。
