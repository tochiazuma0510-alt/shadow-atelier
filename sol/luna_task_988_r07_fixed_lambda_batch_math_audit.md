# Task988 — 固定 lambda の複数失敗弦を一括追加する案の数学監査

役: 既存 packet_bounds_audit。変更可は `sol/luna_reply_988_r07_fixed_lambda_batch_math_audit.md` のみ。
旧987/source/workflow/返信は凍結。ローカルPython/import/AST/数値/GAP/network/git/credentials/新agentは禁止。
rootが唯一のGitHub broker。静的source・紙・保存小metadataの読取りだけで、算術実行はGHAに限る。

裁定2153–2154と `docs/notes/cegar_resume64_cv9_reading_v1.md` を全文読む。正式受理は1450/gen8155、
64段・Separator・UNKNOWN_CAP・grade2 NOT_DECIDED、工房cross-checked限定8条、A0 actual0/1。
失敗弦総数35992–36549で横ばいという実測を受け、無条件128→256梯子は採用しない。
rootは完成986を登録し、一回限りの計測対照として実64親から絶対cap96へ範囲を縮めて起動する方針。
新985の同語readoutと同時に、本便で次の候補を数学上先に裁く。

候補の事前宇宙: 既存Q0/Q2・Delta・全四character/48384・固定P1/8059・全54433弦/2auxは不変。
既存complete oracleが実lambdaについて返す全弦残差を用い、その既定roster順の先頭非零32本以下を
同じlambda・同じbasis/fit/tau/section/cochainから選ぶ。各々を独立の実Omega語にmaterializeし、
既存source/primal/P1/four-Bでfull physical lower zeroとactual rowを確認する。
旧row spanを固定したまま各候補のrecipeを保存し、旧span＋先に採用した候補に対する線形独立性を
実消去で判別して独立分だけ順序どおり追加する。target減算係数とliteral recipeは全て保持する。
batch末尾で残余targetと新spanからSeparatorを再計算、または実Linear候補。完全零oracleは別の実終端。

必須監査:
1. 同lambdaで違反する各行は旧spanの外だが、相互独立は従わない点を証明/反例で明示。
   rank増分は採用数だけ、32や実時間改善は予言しない。lambdaを中途更新しない意味と、
   旧lambdaが新採用spanを殺さないことを既存one-row checkerへ偽装しない型を定める。
2. 各弦のtau/etaとwitnessを同じ固定basis/fitから作るとき、既存selectorの完全性/合法性が
   選定一本にしか成立しない隠れた前提がないか、sourceと紙に即して読む。
   全54433の評価と先頭32本のmaterialization capを区別し、capによるNONMEMBERを拒否。
3. P1補正・source lowerとphysical lower・mod54/18・signed omega・全11slot/positive条件の境界を保つ。
   同一lambdaで選んだ異なる語の正規化row/target更新の符号と順序、依存候補のrecipeの扱いを定める。
4. 既存保存64全prefixを凍結した移行方式: 別packetを作って一括appendする案と、新continuation型の案を比較。
   OLD snapshot λを新snapshot λと称さない。中断時に完全採用済だけcommit・未完はdiagnosticで再開可能。
5. 選定32弦が依存した場合の有限前進保証、kの事前登録、complete判定、線形次元48384の上界。
   2154のrank約55000という線形外挿はこの有限次元上界を越え得るので、実時間/必要段数の予測へ採用しない。

最小で実装可能なmath contract、必須receipt、独立checkerで省略できない式、失敗条件、射程を報告。
まだ実装/GHA/CV9/改善実測を主張しない。独立author P/Cへの共通契約はrootが伝える。
途中で重大な反例/欠落が見つかればrootへ先に送る。最終行 `AUDIT_988_VERDICT:`。
