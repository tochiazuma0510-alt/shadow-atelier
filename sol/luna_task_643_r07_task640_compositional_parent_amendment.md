# Luna Task643: Task640 compositional-parent amendment

Role: binding amendment to Luna Task640.  Read this mail completely and use
it together with every unchanged Task640 section.  The output quartet and
reply remain the four Task640 v3 files; create no fifth implementation file.
Do not run production, GHA, or git.

## 1. New accepted ruling

V479 is 12,280 bytes, SHA-256
`df6850c9e7c86a83ade26c37064a7deb38ec3c8d7907b1eec6ff0d5268b22986`.
Independent Sol(max) Task641 returned `PASS`; its reply is 12,498 bytes,
SHA-256
`498df880f86805cffab50756dc32435a2a79a3426071c7bdd290820a6dadddf7`.

Task641 explicitly decides the Task640 parent-composition question.  The
Task625 graph-to-leaf equation is already an accepted immutable parent after
its independent checker and Task639 audit.  When the Task640 workflow hashes
and reruns that exact parent checker against the exact Task554/Task595 inputs
and byte-compares its verdict with the uploaded verdict, the two Task640
executables need not each duplicate the roughly thousand-line Task625
graph-to-leaf traversal.

This supersedes only Task640 Section 2's demand that both new executables
reconstruct all 3,317 `C_T` graph roots themselves.  It does not weaken any
new precision-two arithmetic gate.

## 2. Exact replacement contract

The producer and checker must each:

1. bind the exact accepted Task625 run, manifest, fifteen receipts, source
   graph/ancestry, three roots, `R07LEAF1`, uploaded verdict, exact parent
   checker, Task639 and Task641;
2. independently stream-parse `R07LEAF1`, including ancestry binding, strict
   order, seed/coefficient/letter syntax, free reduction, record lengths and
   EOF;
3. independently reconstruct the 2,622-term registered prior `C_<1`, retain
   the source root `C_1=Compose(C_<1,C_T)` in that order, and combine old/new
   equal leaf keys only in the derived evaluation map;
4. independently perform all eleven endpoint gates, actual E3/E4 trie and
   signatures, direct all-seven canary, typed first-six restriction, target,
   every 32,260 lower coordinate, every 48,384 top coordinate and packing;
   and
5. fail closed on any exact parent or downstream mismatch.

The Task625 graph remains the authoritative noncommutative source witness and
is retained by exact receipt.  The leaf stream is only its accepted current
evaluation projection.  This amendment licenses no graph pruning, source
kernel claim, changed-quotient reuse, cofinality or self-promotion.

## 3. Independent arithmetic remains load-bearing

Do not use
`search/check_d972_r07_a0_first_rung_grade2_prebuild_v1.py` as the live
Task640 checker arithmetic owner: it transitively imports the forbidden
shared `d972_r07_a0_c2fourier_joint_floor_v1` helper identified by Task568.
The new checker must contain its own group/word, endpoint, truncated-ring,
PB3/boundary, target, occurrence-first aggregation and packing arithmetic.
The independently written v13 all-seven class may be used as a reading
source and minimally transplanted, but not imported at runtime.

Do not replace the removed graph duplication with new generic infrastructure
or production-sized fixtures.  Task640's bounded live-predicate fixtures,
resource gates, workflow, claim boundary and exact result requirements remain
unchanged.

