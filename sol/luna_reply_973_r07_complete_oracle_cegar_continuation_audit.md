# Task973 — 完全oracle＋E動的継続器の限定source監査

F0. **最終両sourceとworkflowの静的監査はPASS。新継続器のruntimeは未観測。** Task967の最終票をrootへ渡して凍結後、開始連絡を受けてTask973/971/972、reply970、971の先行公開ABIを全文読んだ。既読Task957/958/963/964とv547/v548の射程を保つ。途中項の親未観測・source未完成・残件は各項記録時点の状態であり、実親はF5、最終完成境界とfreezeはF18–F21に示す。変更したのは本返信だけで、旧967を含む凍結source/返信を変更していない。ローカル数値/Python/import/AST/GAP、git/network/credential/dispatch、追加agentはない。rootが実走とreleaseを別に担当する。

F1. **登録宇宙と2139の訂正を保持する。** Q2の54432点/108864辺、六tag/四character、五carryの核と二eta、全8059 P1、lower96776、四B/physical48384と受理Connは同じである。固定bundleと各lambdaのq/chi/kappa/cochain/tree/witnessを分け、現snapshotの全8059式・54433 chord/二auxのEOFを経て同じwitnessをEへ送るという公開ABIを確認した。dualはnew元lead降順→old embedded元lead降順、primalはold embedded元lead昇順→new owner/元lead昇順、physicalは挿入順消去と逆挿入順separatorであり、共通の一つの順序にまとめない。

`ops/express/20260906_astra_fable_section_cv9_scope_precision.md` と `ops/express/20260906_fable_astra_section_scope_ack.md` を全文読んだ。2138八限定と2131七限定の上で、2139が採用したとおりq1..3零は現lambdaの観測、残差度数は決定的集計として扱う。作用素恒等零、確率、physical像の独立本数、残り反復数/実時間を推論しない。第三実装のscore/f/tree全被覆をq/kappaや旧26scanへ広げない。次に実使用するoracle checker v2のfull selftest一回は新GHAの登録条件で、旧v1 receiptやserialization15件とは別である。

F2. **producerの初期永続化blockを読んだ。** 読取時の `search/d972_r07_complete_oracle_cegar_continuation_v1.py` は33,468 bytes / SHA256 `11f85f2b96d57e42a99c02c10de292a6647878da7b2fe7ca64c1668c745537a9`、LF581/CR0/finalLFで、bootまでが保存されている。これは未完成sourceの読取境界であり、最終freezeではない。新schemaのcanonical/seal、byte hash/EOF、packed3/u8/u32le、path/symlink、atomic file、PhaseStore/checkpointを読んだ。直接importは凍結producer Eの4f600aae…だけで、同系統依存をloadし、source/pin/schema定数は変更せずdeadline/progress hookを接続する。

phaseはsection/cochain/tree/raw/source/primal/p1/B/physicalの順に、owner/source/fixed/snapshotと直前phase manifest hashを結ぶ。完成directoryはpayload＋manifestのexact roster/bytes/EOFを読み、phase-telemetryのfinite非負時間とpayload bytes/EOFを確認する。commitはpending directory内のpayload→manifest後にdirectoryを公開し、その後のacceptには協調停止を挟まない。checkpointは同じsnapshot/physical parent headと順序付きphase hashes、current oracle/witnessを別字段に持ち、未計算はnullである。

まだ未完成のconsumerで確認する点は、各phase固有のexact roster/dtype/shapeをgeneric manifestの自己申告とは別に固定すること、同じsnapshotのdurable physicalをstep manifest/HEADへ回復して数値再走しないこと、HEAD外のpending/orphanの限定型、全new prefixの認証、cap累積である。PhaseStore単体の読取をもって、この全resume接続の完了やPASSとはしない。

F3. **外部Eの薄いattachとDERIVED startの初期接続は整合する。** read_external_eのpin表はE_ARTIFACT/E_FILES/E_SNAPSHOT/E_CHECKER_SHAが空なら入口で拒否する。未観測のrank1386を起点定数にしない。凍結Eの実serializerを再読し、generic sealのHEAD/result等、plain三字段target、rolling instructionのpredecessor、全payload roster/optional lambda、rank/gen/offer/physical offsetの型を新loaderと照合した。全親pin接続と実fixture shapeの最終照合は外部E成功後に行う。

attach_e_deltaはpredecessor=current physical head、offer=current generation、rank/genの一回増分、physical offset=current rank×12096、normalized lead/旧lead零、typed target/DERIVED親列を要求し、一行をrows/records/leadsへ一度追加する。bootは受理済みoracle/refinement startと外部Eのstart/owner/source/witnessを結び、attach後に新completed_stepsを0、last step manifestをnullとする。外部Eを新loopの一段へ二重計上しない。Separator時は保持check_final_separatorの実定義どおり全current rowsと両targetの直接pairingを読む。旧26scan/insertと外部Eのraw/P1/物理算術を再実行する入口ではなく、正確な成功親receiptを前提とする取り込みである。

原rho2 hashと受理target親列はanchorからstateの専用字段へ取り、current_derived/build_current_startが次のstartの直接字段を構成する。内側target_derivationだけを持つ前E separatorをそのままstart.lambda_rho2へ代入しない。各Eのresult/instruction/target/state/manifest hashを親列へ追加し、原rho2はDERIVED、現target dot1は直接測定として分ける。外部Eがlinear zeroならlambdaなし/Task958 pendingの型を保つ。余分なfixed数値bundle・次oracleを避けるmain分岐はまだ未保存である。

F4. この読取境界に確定した必須修正はない。次はfixed bundle、current全oracleとphase固有loader、動的E wrapper、step/HEAD/cap/resume/UNKNOWN tail、独立checkerの全new prefix、canary/CLI/workflow、実外部E pinsと最終hashを読む。完成していないblockを静的PASSやruntime PASSとして扱わない。complete-zeroはv548/Conn前提付きseparator候補、target-zeroはTask958同一語11slot pendingまで。source-lower零とConn込みnormalized literalのphysical-lower零、旧F-fo-1と共有入力/reader/transport/physical primitiveのTCBを保持する。Gamma/fixed complete packetは追加release条件にしない。

F5. **外部Eの成功親を実metadataで受領した。** root回収先 `%TEMP%/shadow-atelier-selected-cycle-run33981657987-candidate-a1` とpin表 `%TEMP%/shadow-atelier-audit163/selected-cycle-v1-candidate-33981657987-a1-pins.json` を読んだ。実tupleはrun `33981657987` / attempt1、head `444c71c9e554ae8feb9c8ee54df57d3df19ed66f`、artifact `9973974150`、ZIP 2,816,692 bytes / `884c039737cae9673c9e1d871c30713456c993b97f16a557e9c8c24078537f25`。ZIPと全29output files/8,731,365 bytesの照合はrootの観測であり、本便は十entryの実bytes/SHAを独立に読んで全件一致を確認した。

実checker-resultは30,071 bytes / `9f0d30a4481ea94f0aa1a4cd5aa120281dc3ebee1a0e8e1b01db162efbde7a77`、status PASS、PIVOT_CANDIDATE/Separator、rank1386/gen8091、selected scalar1/target scalar1、physical appends1、全8059/全lower96776/四B/六stage/full payload比較を記録する。state headは `5e760f6a7c04a5eaf800289ab5b05ae542dc33c09b502ab7f87958b5e836a6a8`、target SHAは `e902cf3b2d9a5a58ac47459877e017fa4d6a44c5868751b8690543665ae269c1`、lambda SHAは `a16f4c8289e78efa068cfe923f1ee9a0d7b71f8c71aede582ff0ff93cda0c8ad`。このEの事実を新継続器の一段成功へ数えない。

run-receipt（1,654 bytes / `7b8ac9c712d2c7a528c5c9c0fc39d260ca0755029c3519031f8fe00b6a804d2b`）はproducer/checker一回、元oracleとsource不変、旧算術未再走、source P4f600aae… / Ca6d52e0d… / workflow def1e181…を結ぶ。rawのみの直接replay、normalized/十一slot未replay、MEMBER/NONMEMBER未決を保持する。実separatorのlambda_rho2は内側target_derivationに原hash `b41b9e69fc1257bb1542062a2496bc94bd3cbe6b01e03aba653dae2e4af17c2e`を持ち、F3の専用DERIVED adapterを必要とする実shapeである。Eの新工房CV9はこの受領時点でpendingであり、格付けを先取りしない。

F6. **producerの固定bundle/current sectionと外側metadataを読んだ。** registered_phase_roster/validate_rosterがPhaseStore.acceptへ接続され、B.jsonの型付きscalar復元を追加する公開ABIを確認した。実productionがe引数を渡すことは後続runnerで読む。新fixed manifestは旧geometry/carryの認証済み配列bytesを新owner/sourceへ登録し、旧snapshot manifestを改称しない。全8segments・12readers、元row ID/lead/descriptor、mod54とcanonical indexを保持する。CachedOracleは別interface objectであり、凍結moduleのpin/schema定数は変更しない。

current_section_cachedは各lambdaで凍結producerのdense全P1収縮を改めて一巡し、全四q/values/chiからnew元lead降順のd1双対を作る。old全四d1 companionをbetaから引き、共有auxを含むold embedded元lead降順で解き、最後に全8059の等式を実rowから再測定する。元canonical IDと新/旧解順は別arrayで残る。新owner/source/start/snapshot/HEAD/checkpoint/oracle_completionは固定bundleと現head/target/lambdaのhashを分け、cap/時間をimmutable ownerへ入れない。complete oracleは三phaseのhashと同じsnapshotへ結ぶ。これらの完成blockに新しい必須算術修正はない。

その後保存された実E定数とrun/intake joinも読んだ。producerは実launch run/attempt/head、workflow、source/checker/result/intake hash、元oracle artifact/十entry、不変性をplain receiptsからjoinする。両新sourceに入った十entry定数はF5のpin表と一致する。読取中のsourceはproducer58,782 bytes / `8c247a8e769919dd412a83524705354f3e2748b91688682e610e57944c5ccd24`（LF955/CR0）、checker21,236 / `3db9aff0bde8ef3b54a5df55835e6b52d24caaf4a2d5029951613ffed8c15c4f`（LF374/CR0）で、final freezeではない。

F7. **checkerの最初の動的section/tree blockも読了した。** 独自E966と修理oracle v2を別のpin/名前でimportし、producer971の新helperは共有しない。FixedBundleは同系統の同一語mod54/8059 instructionsと全12blob/cacheを認証して開いたhandleを保持し、file identityの不変性を確認する。各lambdaの四rootと全8059 packed収縮は新規一巡し、new→old元lead双対と全8059最終等式を行う。producerのdense unpack/products/sumとは別の保持packed projectionであり、旧F-fo-1や共有TCBを遡及解消する主張ではない。

checkerのfixed_treeはgeometryからcarry potential/tau/最初の五独立chordを独立に作る。current_treeは毎回fresh f potential、全54433 chord値、fit/residual、二auxを計算し、aux優先・最初のfailed chordと五basisという決定的順序で同じ合法witnessを構成する。primalは保持する独立full-lower経路、correctedはraw tupleの新copyに同じalphaを一回だけ引き、全96776 lowerと別primalを比較する。これらの関数には必要な修正を見つけていない。ただし実cochain/ordinary27/raw sourceからこれらを呼び、全phase/step/checkpointを比較するmain経路はまだ未保存であり、全consumerの完成とはしない。

F8. **rootの新資源枠を受領した。** cap1を内部1800秒/外40分、その同outputのresumeを絶対cap32・内部5400秒/外100分、独立checkerの全new prefixを内部10800秒/外190分、job350分とする。これは事前登録された資源枠であり、成功段数や速度/残り反復数の予測ではない。cap1でterminalなら次oracleを強制しない。UNKNOWNと完了cursor、checkpoint/diagnosticsを保持し、実v2 full selftest一回を新workflowへ含める。実workflowの各引数・timeout・result gateと、phase/HEAD間の停止・resume/cap持越しは未完成tailで引き続き監査する。

F9. **checkerの実親directory受付に必須修正を発見し、修正後を再読した。** 初期 `read_external_e` は `path(args.e_root, "output")` を呼んでいたが、この保持pathはoracle v2→full-origin→packet v2→`check_d972_r07_actual_root_seed_materializer_v3.py:103–112` と辿ると `candidate.is_file()` を要求するfile専用helperである。実候補のoutput directoryは必ず拒否されるため、作者とrootへ直ちに通知した。新source内だけにdirectory用helperを作り、全componentの既存directory・非symlink・root内包含を確認してexternal output/fixed/phaseへ用いる修正を再読した。凍結helperは不変、payloadのfile読取は元のpathを使う。この指摘は解消済みである。

F10. **phase進捗記録の公開名と時刻の接続も揃った。** 初期reply971の `.telemetry` 表記とsourceの `.phase-telemetry` の差を通知し、公開ABIと両新sourceを後者へ統一した。各phaseは `begun_elapsed_seconds` / `ended_elapsed_seconds` / `elapsed_seconds` を当invocation開始からのmonotonic時間として記録し、finite非負・開始≤終了・差の整合を確認する。payload bytesはtelemetry自身を除き、manifest rosterにはtelemetryを含める。checkerは時間値を独自の実行時間と同一視せず、記録値の型・差と、独立再構成したpayload bytes/EOFを照合する。この時刻はphase算術とpayload準備の開始/EOFであり、後続fsyncまでのI/O時間とは呼ばない。

F11. **新checkerの実親admission、PhysicalState、外側serializerとproducerの全phase loaderを読んだ。** checkerは外部Eの27 manifest payloadとmanifest.json/HEADを合わせたoutput全29 files、および十entryのbytes/型/seal、run/source/intake/rolling/plain targetを結び、外部E一回attach後のcompleted_stepsを0に保つ。PhysicalStateは受理済み行と新行を別のoffsetで参照し、各attach後に全行のlambda消去と現/前target dot1を直接測る。新fixed/phase/source/start/snapshot/oracle/inner E metadataはowner、現head/target/lambdaと固定bundleを区別する。固定geometry/carryの独立再構成とexact bytes比較、phaseごとの全payload/EOF比較が保存され、CHECKED_CURSORは比較完了phaseだけを進める。新producerの算術helperをimportしない。

producerのrestore_section/cochain/tree/raw/source/primal/corrected/B/physicalを順に読んだ。元row ID/lead、全8059式、同じsnapshot/witness/raw-word、六tag chain hash、mod54、lower96776零、全四Bとinner rolling/resultを結ぶ。raw loaderは保存SLP recordを復元して後続の直接Foxに渡し、完了raw phaseの巨大語を再走しない。primal loaderの零lowerは保存された全96776零receiptを受ける型で、続くfresh P1補正の全lower比較を置き換えない。mod54 residueが[0,0]である条件は、各成分が0/18/36で正規化後F3値0という既存条件から従い、普通整数epsilonを零と主張するものではない。次に保存されたrunnerは実PhaseStoreへe引数を渡し、これらをfresh/resume両方へ接続するか、step/HEADと停止境界まで読む。

F12. **producerの全phase実接続とstep/HEADの保存・復元を読んだ。** snapshot_storeのproduction呼出しはPhaseStoreへeを渡し、F6で保留したexact phase rosterを常時要求する。finish_snapshotは完成phaseならbuilderを呼ばず、同じtyped loaderで戻す。新lambdaごとのsection→六tag cochain→全treeから同じwitnessをrawへ渡し、source/primal/fresh P1/四B/physicalまでF11のloaderを一段ずつ接続する。固定tau等だけを使うcurrent_tree_cachedも、現fのpotential・全54433 values/residuals/fit・二auxを改めて作る。保持Eの実signatureとresult/DERIVED字段を照合し、固定親pinの書換えはない。

全physical payloadの公開後からstep manifest、attach、HEADの公開までに協調停止を置かない。load_prefixはHEAD内の全new stepを九phase/rolling/target親列のexact bytesで薄く復元し、現snapshotにHEADより先行する完成physicalがあればE数値を再走せず一度だけcommitする。現lambdaは復元後に全row/両targetへ測る。checkpointはhash名の不変fileで、古いHEADの指す完了phase数も認証する。tree完了とoracle top metadataの間の停止は、完成配列から同じtopを補完する。pendingは名前・型を限定して診断として扱い、到達不能な番号やphase gapを完成値へ数えない。capは新completed_stepsの絶対上限で、target零/complete-zeroの型付き終了判定を優先する。停止後のResourceStop receipt、起動時の未完output、signal/CLIとの最終接続は未保存部分として保留する。

F13. **checkerの全九phase再計算とHEAD内の全new prefix接続も保存部分まで読んだ。** replay_snapshotは各current lambdaの独立sectionからordinary27 score/raw-edge cochain、完全tree、独自RawSLP/直接Fox/全source、元lead昇順primal、同じ8059語のmod54、fresh tupleへの一回P1補正、全96776 lower零、四Bのgrouped forward、挿入順physical/一回normalize/freshlambdaへ接続する。新inner E serializerはsnapshotのoriginal rho2直接字段と型付き親列を使い、plain target三字段とrolling hashを独立に作る。全array/JSON/manifest bytes/EOF比較は実計算結果から行い、未完cursorの数値をPASSへ数えない。保持raw_materializationがraw/sourceを一つの呼出しで計算する場合でも、cursorは実比較が終わったphaseだけを進める。

replay_head_prefixはHEADの新completed_stepsを起点に戻さず、各snapshotの全九phase→step manifest→PhysicalState.attach(new_step=True)を順に照合する。外部Eは別attach(new_step=False)である。current checkpointがあれば、そのphase prefixとoracle topの有無だけを再計算し、HEAD全字段と全row/両targetを最後に照合する。source変化・古いlambda/witnessの混用はowner/source/snapshot/前phase/physical parentの連鎖で拒否する。現在の完成blockに追加の必須算術修正はない。top-levelのdiagnostic roster、terminal/resource/canary/CLI/workflow、最終freezeを続けて読む。

F14. **producerのmain/resource/signal/canaryまで読んだ。** この読取境界は126,845 bytes / SHA256 `33191d1aa8c41750333338f1e364b5e5b0018bed92f4bbee45c8164f0d78c368`で、実file hashを確認した。まだ作者の最終freezeとはしない。signal handlerは停止flagを立てるだけで、physical→HEAD間へ例外を注入しない。入場・全prefix認証前の停止はcandidate=falseのdiagnosticとexit3、入場済みphaseの停止はHEADを結ぶUNKNOWN_RESOURCE resultとexit0として区別する。前回resultを今回の完了receiptへ読み替えないため、resultは今回の不変invocation fileの全bytes hashを `invocation_sha256` で直接参照する。cap/時間はinvocationにあり、ownerにない。

fresh outputと途中のimmutable metadataを照合し、HEADがまだ指していないcurrent snapshotのstartだけは同じ決定的expected bytesで補完できる。commit済みsnapshot startの欠落は拒否する。新metadata canary五件とinterface三群は、cap持越し/current witness gate、PhaseStore/snapshot_store/ensure_phase、実step/HEAD公開helper、plain target scalar0、linear zero≠MEMBERの実関数へ繋がる。短いmetadata probeを本番の大規模算術成功fixtureとは呼ばず、production array rosterへ渡すと拒否することも明示する。旧成功suiteを呼ばない。ローカルでcanary/AST/数値は実行していない。

F15. **workflowの保存済み前半を読んだ。** marker/job350分/事前登録三資源枠、実14親のrun/attempt/head/repository/workflow/artifactとTask554だけfailureの受付、実Eとoracle archiveのbytes/SHAと安全な展開、全14rootの唯一解決、19 executableとraw三file、実oracle/Eの十entryと全44/29output filesの保全receiptまでを確認した。19本は新二本とv2 checker一本を旧Eの16本へ前置し、元oracle14本・repair追加一本と別の来歴でjoinする。ASTと数値はGHA内だけである。新source最終pinと本番CLI/terminal/resume/checker/upload tailはまだ未保存・未確定で、release可とはしない。

`ops/express/20260906_fable_astra_selected_cycle_e_cv9_ack.md`（2142）を全文読んだ。実Eの工房判読は進行中で971–973の並行は妨げない。補正P1語・normalized physical語・targetの全語/11slot replay未実行、v2 full selftest継続義務を保持し、新rank1386の格付け完了を先取りしない。

F16. **2143 F-cy-3の限定紙上裁定：正規literal規約はv547(4.2)の符号付き代表である。** rootの依頼で `sol/proof_r07_table_free_endpoint_selector_v547.md` と `sol/proof_r07_section_corrected_homogeneous_dual_v548.md` を全文再読し、凍結E両側の実中央因子（producer:424–427、checker:439–442）を照合した。今後の同一語は次で固定する。v548 §5の `^omega(w)` は、この整数liftを用いたv547(4.2)の略記として扱う。

```text
s:F3->{0,1,-1},    s(0)=0, s(1)=1, s(2)=-1,
c=[r_x,r_y]=r_x^-1 r_y^-1 r_x r_y,
U(w)=w (r_x^3)^(-A(w)/6) (r_y^3)^(-B(w)/6),
R_can(w)=U(w)c^s(omega(w)).
```

証明は既存v547の前提内で閉じる。cの普通epsilonは(0,0)、omega(c)=2、Theta(c)はGamma0'の位数3元である。cube修理は普通epsilonを正確に消し、omegaを変えないため、`omega(R_can)=omega(w)+2s(omega(w))=0`。v547(3.8)/(4.3)からR_canはOmega∩[F,F]に入り、同じQ2 Fox chainを持つ。因子順とA/6、B/6の普通整数除算は変えない。

別の整数代表r=s(omega)+3kを使うと、自由群内で正確に `R_r=R_can c^(3k)` である。Theta(c)^3=1なのでc^(3k)∈Omega、普通epsilon0。r_x,r_yはQ0の核にあるため、各Q0 quotient上でFox additivityからJ(c)=0、従ってJ(c^(3k))=0でもある。この差はQ2 chainとetaの両方が零で、v548(5.1)の同じ(z,eta)を与えるから、受理済みsame-owner map Psiでの全source読み出しは一致する。これは**source同値の証明**であり、raw stream、SLP、祖先JSON、rolling hashの同一性ではない。

特にrを最小非負代表0/1/2と読む場合、omega=0または1なら指数が同じで同一のliteral構成になる。omega=2ではc²とc⁻¹の差がc³となる。cはomega(c)=2より非自明で、自由群はtorsion-freeなので、この二語を自由群の同じ語とは呼べない。有限Delta像とsource値の一致だけを保証する。現凍結E両側は最初からsを実装し、reply957 F6もこの規約を採っていたため、helper修理は不要。公開紙の略記を明示する訂正はrootが扱い、本便は旧紙/旧sourceを変更しない。非零omegaの新runtimeをこの証明の実測根拠にはしていない。

F17. **親の格付けを2143の限定付きcross-checkedへ更新する。** `docs/notes/cycle_mat_v1_cv9_reading_v1.md` 全365行と対応expressを全文読んだ。射程はrank1385→1386の一pivot、target scalar1、依然Separatorだけである。実wは(A,B,omega)=(6,0,0)で、非自明な修理はr_x^(-3)の一因子だけ。repair-y/repair-centralは空語、45 nodeのomega二次項も零で、中央修理が本番で非自明に試されたとは書かない。F16は紙の規約差を解くもので、この実走被覆を増やさない。

正式七限定の残部も保持する。envelope/words/transportの保持実装は両側で重要で、とくに同一envelope decoderが物理行を作るB表の復号に使われる。現lambdaのqはcharacter0だけ、kappaはtag0だけ・aux零という観測を作用素恒等零へ広げない。original rho2はDERIVED。B表そのもの、旧1385行、P1 cache/Task554からの5335 liftには第三実装の全再計算がなく、二系統一致の前提が残る。語長式の恒真部分とprimal lead検査の片側ごとの被覆差も独立な試験実績へ数えない。E966のselftest PASSと、これから実使用するoracle checker v2のfull selftest一回は対象が異なるため、後者の実GHA義務は継続する。新継続器の未来rank/収束/実時間は未観測である。

F18. **両sourceをmainまで読了し、producer最終freezeを照合した。** producer最終は126,940 bytes / SHA256 `67d2302c3c4d571a4ccbe524098fe97808f05069bfd2ed822408bde3e256733c`、LF1972/CR0/BOMなし/末尾LF。reply971最終は29,060 bytes / `965eefa0424b9a35413c2d9af446ddd014499fa51c7ae3fd991420f07999d1ae`、LF130/CR0/BOMなし/末尾LFである。実bytes/hashを独立に測った。F14からの最終差分は、実Task554 descriptorの文字列rootをPathへ戻す接続と、各basis segmentのbody SHAを実parent bodyへ結ぶ条件であり、当該FixedBundleを再読した。resultのinvocation SHA、pre-HEAD bootstrapだけの修復、完了physicalからHEADへの停止なしの順序も保持される。

checkerの読了境界は120,245 bytes / SHA256 `8c000f9b49d04447a09c701daf5907a35b7f2e883f1e36747308a6d4ded29b1f`、LF1701/CR0/BOMなし/末尾LFで、実hash一致。ただし作者のworkflowと最終freezeはまだ待つ。HEADに含まれる全新stepと現checkpointの全phaseを再計算した後、typed terminal、旧invocationから到達できるbefore-HEAD、今回resultのinvocation SHA、限定されたdurable tailを別々に照合する。HEAD前の完成physicalは到達性とbytesを調べるだけで、HEADのcompleted_stepsへ加算しない。資源停止は比較済みcursorとUNKNOWN_RESOURCE/candidate=falseを返し、未照合部分を全array PASSへ含めない。新metadata五件とinterface三群は、実PhysicalStateでの一行目target scalar0・二行目linear zero、stale snapshot、phase-before-HEAD/全EOF比較を使う。新sourceの算術・公開順序・CLI接続に追加の必須修正は見つけていない。ローカル実行はない。

F19. **2144の訂正受理と、workflowの自己試験対象を確認した。** `ops/express/20260906_fable_astra_cycle_cv9_correction_ack.md` と対応Astra側expressを全文読んだ。工房はF16の符号付きliteral規約を採用し、2143の「F-sc-3実質閉鎖」は対象誤りとして訂正した。E raw source全24塊の実測を旧oracleのq/kappa被覆へ読み替えない点も一致する。

rootは未公開workflowがoracle v2 full selftestを三件と数えた誤りを発見し、作者へ修正を依頼した。本監査でも凍結 `check_d972_r07_section_cochain_oracle_v2.py:1154,1162,1184,1208` の初期一件＋追加三件、計四件を確認した。E系の三群とは対象を分ける。workflowはcap1→同output resumeの保存・invocation gateまで読了し、code0のroot resultとstdoutが全bytes一致する条件、先行prefixの不変bytes、累積cap/同owner、今回invocationのbefore-HEADとmax値を確認した。早期exit3を前回resultへ読み替えない。この時点では四件gateの修正後、全prefix checker、前後不変性、最終receipt/upload、source/workflow最終pinが残る。

F20. **workflow末尾まで読了し、四件gate修正を確認した。** 読了fileは `.github/workflows/d972-r07-complete-oracle-cegar-continuation-v1.yml`、61,275 bytes / SHA256 `9f751fe1ea21d16b7758f9832d2dd091b73f0796128ceea505c8975031c096c1`、LF942/CR0/BOMなし/末尾LFである。新P/Cのbytes/SHA四字段はF18の実fileと一致する。v2 full selftestを一回だけ実行し、専用gateとrun receiptの両方が全四件PASSを要求する。新continuation側はproducer三groups、checker三tests、両側五metadata拒否として別に判定する。

本番はcap1内1800秒/外40分から、同outputのresume絶対cap32内5400秒/外100分へ進み、typed terminalならresumeを省く。最初の完了prefix全files/dirsとbefore-HEADを保存し、resume後にその不変bytes・同owner/source/start/fixed・累積count・今回invocationのhead/max値を照合する。独立checkerは内10800秒/外190分でHEAD全new prefixと現checkpointを再計算する。code0、stdout/report全bytes一致、全8059/54433/二aux/96776/四Bと全array/JSONの完了字段を必須とし、checker UNKNOWN_RESOURCEをPASSへ変換しない。

always手順は全19 executableとraw三file、実oracle/Eの十entryと全44/29 output files/dirs、producerのchecker前後全output bytesを照合する。早期失敗で基準receiptが未作成なら対応checked字段をfalseに保ち、数学PASSを示さない。候補uploadは全先行手順とrun receiptが成功した場合だけで、receiptはchecker/result/HEADのrank・generation・target・lambda・terminalと元sourceの由来を結ぶ。UNKNOWNも独立に完了prefixを照合できた場合の候補型として残し、MEMBER/NONMEMBERへ変えない。別のalways uploadはhidden pendingを含む全output、exit code、途中stdout/log、保存と進捗receiptを保持する。現942行の実行・保存条件に追加の必須修正はない。残る静的手続は作者の最終freezeとreply972の読了であり、runtimeは未実行のままである。

F21. **最終freezeを受領し、本便の静的判定を確定する。** producerとcheckerの両作者が、以下の実source/workflowを以後変更しないと宣言した。各値はF18/F20で独立に確認した実bytes/hashと一致する。全sourceはmainまで、workflowは全942行を読み、指摘済みのdirectory/file接続、telemetry、v2四件gateの修正後に残る必須修正はない。

| 最終対象 | bytes | SHA256 |
| --- | ---: | --- |
| `search/d972_r07_complete_oracle_cegar_continuation_v1.py` | 126940 | `67d2302c3c4d571a4ccbe524098fe97808f05069bfd2ed822408bde3e256733c` |
| `search/check_d972_r07_complete_oracle_cegar_continuation_v1.py` | 120245 | `8c000f9b49d04447a09c701daf5907a35b7f2e883f1e36747308a6d4ded29b1f` |
| `.github/workflows/d972-r07-complete-oracle-cegar-continuation-v1.yml` | 61275 | `9f751fe1ea21d16b7758f9832d2dd091b73f0796128ceea505c8975031c096c1` |

rootはF18/F20の実source数学PASSを根拠に公開手続へ進み、正式reply972の清書を新たな実走待ち条件にしないと指示した。本票もそれに従って確定する。reply971の最終本文とhashはF18のとおり読了・一致。reply972はこの判定時点で最終短報の保存中であり、未保存の本文・hashを読了済みとはしない。実行sourceに後続修理が必要になれば、新versionと別のdelta監査を要する。

これは登録GHAへ渡せるというsource判定だけである。新AST、v2 full四件一回、両新interface三群/metadata五件、実cap1→同output resume32、全新prefix/current checkpointの独立実照合は未観測のruntime gateとして残る。F16/2144の同一literal規約とF17の七限定・保持TCBを継承し、受理済みrank1386/gen8091を未来の新rankへ進めない。未観測のzero/aux/非零omega実被覆、収束・所要時間、MEMBER/NONMEMBERを主張しない。Task958の全11slot、full A0、grade2決着は本便のPASSに含まれない。

AUDIT_973_VERDICT: SOURCE_AUDIT_PASS_READY_FOR_REGISTERED_GHA; FINAL_PRODUCER_CHECKER_WORKFLOW_BYTES_MATCH; NO_REMAINING_REQUIRED_STATIC_FIX; V547_SIGNED_LITERAL_CONVENTION_ADOPTED_2144; NEW_RUNTIME_AND_CV9_PENDING; GRADE2_NOT_DECIDED; verified=false.
