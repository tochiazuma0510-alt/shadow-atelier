# Task947 -- resume Task945/946 after Sol163 takeover

The researcher requests execution of mail163, retaining mail162's campaign
work order. Root has read Tasks944/945, reply944 and workshop2096/2105/2110/
2117. Continue the already adopted bounded fixed44 packet plus root loop.
Read those task files FULL. The old Task929/930 agent names are historical;
the two newly assigned workers own their respective Task945/946 paths.

## Authorized files and separation

Producer worker: ONLY `search/d972_r07_fixed_root_packet_loop_v1.py` and
`sol/luna_reply_945_r07_fixed_root_packet_loop.md`.

Checker worker: ONLY `search/check_d972_r07_fixed_root_packet_loop_v1.py`,
`.github/workflows/d972-r07-fixed-root-packet-loop-v1.yml`, and
`sol/luna_reply_946_r07_fixed_root_packet_loop_checker_workflow.md`.

These paths did not exist at takeover. All old files stay immutable.
No numerical Python/GAP execution locally; read-only source inspection and
byte/hash metadata are allowed. No network, credentials, git, or dispatch
by workers. Do not access or forward credential environment variables.
Root is the sole release broker. Do not spawn further workers.
Coordinate only the public ABI and output schema; do not share new arithmetic
implementation. Each side independently uses its own accepted lineage.

## Necessary delta from Task945

1. Resolve workshop2117 M3-1 by the explicitly permitted DERIVED option.
   The new cert must distinguish direct pairings with the authenticated
   parent/current target remainders and all physical rows from the inherited
   original-rho2 implication. Emit an explicit mode/status `derived` for
   lambda_rho2, name the accepted target-derivation parents, and declare
   original_rho2_directly_read=false. Do not label this an actual rho2 dot.
   Do not restage rho2 or replay the historical target solve for this scope.
2. Retain Task945's lambda-independent raw seed2 SHA/support checks while
   retiring only the old scalar assertion. No permanent active-character
   assumption: derive all four B-adjoints after every complete append.
3. Report declared pair counts separately from actually nonzero-root blocks
   and informative pairings. ROOT_SEEDS_ZERO is only annihilation by the
   current lambda of this fixed list; it is not complete-image saturation.
4. Retain the conservative at-most176 append safety bound in Task945.
   Actual packet rank, number of further appends and next selected seed
   are unknown. Do not forecast them from stale seed35/36 values.
5. The new packet/loop changes the executed-versus-parent contract and TCB.
   Each reply lists its module set and which rules are executed or retained
   as premises, enabling the workshop's incremental CV-9 after a real run.
   New arithmetic is not externally cross-checked before that ruling.

Keep the same-owner resume contract small and actual: packet and each whole
step are durable before HEAD advances; verify owner/pins on resume; preserve
completed prefix at resource stop. Checker independently rebuilds the packet
and replays the newly produced prefix before candidate acceptance. A cap
does not imply an empty root result. No broad historical re-audit or generic
framework is requested.

Worker replies must identify exact files/bytes/SHA, public CLI, remaining
runtime gates, and honest incomplete work. Root will review the executable
and workflow before release. `cross_checked=false; verified=false`.
