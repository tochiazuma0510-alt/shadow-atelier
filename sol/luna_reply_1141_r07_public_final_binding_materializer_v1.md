Luna Task1141: the public final-binding binder and exact patch plan are prepared. No binder execution or final driver/workflow/registry materialization has occurred; actual formal receipt, inventory5, roster and final P/C opaque metadata remain root handback inputs.

All artifacts below are under `%TEMP%/shadow-atelier-audit163/task1141/`.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| bind_public_final_v2.py | 22142 | 6aae9da2cc93ee1eacb1bc8ea40dbe9b535b7a59e4dc697676d1201ee8f09857 |
| input-contract-v1.json | 13588 | ae138aea5404eab0189b4436b3b8e2ebb70dd6f291cb9503462aac336aac3fc0 |
| exact-public-patch-plan-v1.json | 23445 | 2e80ab6e0cfcdd3b50df4c8f1a7130ef1264e2043f034f5e904fd68417807e36 |

The contract is a new root-to-binder normalization envelope, not a fabricated source-author metadata schema. It requires the actual native typed JSON receipt pin, ordinary exit0, root's explicit full-completion assertion and concrete public receipt object; exact formal5; authenticated sorted safe roster with public evidence pin; final P then C public metadata/adoption pins; existing-shape P6/C6 source records, full177/165 region tables, comparison tables and all8/57 current retention slots; and C's actual final expected-P descriptor. P/C source descriptor paths are never opened. Unknown native/source-author inner schemas stay opaque and completion/adoption remains root's responsibility. No non-null placeholder or preparation source is substituted automatically.

The 14 registered driver/workflow raw spans were checked against all original byte pins. The binder uses8 driver plus6 workflow spans,72 enumerated current-only registry JSON value pointers, and exactly2 timing pointers (`P.public_contract.source`, `C.public_contract.source`). It preserves all other bytes, including three historical registries and timing declaration/root_adoption provenance. The conservative prepared implementation permits raw P/C changes only in the publicly designated ordinal0 enclosing region and keeps other body hashes/comparison meanings. Unexpected broader deltas fail before output, rather than becoming general source replacement.

The saved binder computes exact raw spans, LF/byte/SHA coordinates,131 complete driver regions and forward/reverse reconstruction records. It validates the entire handback and constructs outputs in memory before creating any output directory. A provisional workflow is formed with the existing ready guard false; the guard becomes true as the last semantic binding, followed by recomputation of the final driver and derived workflow pin. Only four new files can be written in a fresh Task1141 child directory. Existing caps,18-parent order, source paths, workflow name/marker, historical declarations, aliases and mathematical branches lie outside the allowed edits.

Static review covered all454 lines of v2; no import, compile, test or materialization run was performed under this source-only instruction. V1 remains preserved and unexecuted; v2 adds root-handback provenance, exact before/after LF coordinates and a fixed Task1141 output-root check. `source-only-final-handback-v1.json` records input pins, scope and reachable operations. The public base pins remained unchanged. No live-process operation, parent scan/restoration, private P/C read, mathematical source execution/import/AST, Git/GHA/network/credential use or repository placement was performed. Root may supply the actual handback under the already authorized flow; no new approval wait is introduced.

TASK1141_VERDICT: PUBLIC_BINDER_SOURCE_AND_EXACT_PLAN_PREPARED; ACTUAL_ROOT_HANDBACK_PENDING; NO_MATERIALIZATION
