# Luna task 342 - R07 A0/v7 fast history-free positive resume

Role: Luna, implementation and mechanical accounting only.  Read every
section first to last.  Do not run Python, Node, GAP, GHA, workflows, network,
or git.  Do not edit v1--v6, any sealed input, v220, or a workflow.  Create
only the five versioned outputs named in Section 1.  Return
`IMPLEMENTED / UNEXECUTED` only after the complete literal route is present
by inspection; otherwise return `BLOCKED / UNEXECUTED` at the first exact
boundary.

This is a fresh v7 owner, not a wrapper around cached-v3 `main`.  Its sole
goal is a sound explicit positive witness with a bounded resource transport.
It may never emit a separator, negative, exhaustion, fake, cofinal lift, or
Ihara claim.

## 1. Mandatory inputs and sole outputs

Read in full:

- `sol/sol_reply_337_r07_task325_v6_code_performance_audit_v1.md`
  (42,611 bytes,
  `035f3d987746f9662fb66da512889da7ad4f7ad899a3cc768e626093ce050f4a`);
- `sol/proof_r07_history_free_positive_common_word_verifier_v265.md`;
- `sol/proof_r07_two_way_basis_checkpoint_resume_v275.md`;
- `sol/proof_r07_triangular_checkpoint_basis_resume_v276.md`;
- `sol/proof_r07_boundary_first_lazy_runtime_resume_v277.md`;
- `sol/proof_r07_selected_support_positive_replay_v278.md`;
- `sol/audit_r07_task298_six_hour_cancel_no_artifact_v279.md`;
- task325 commission/reply and all four v6 files;
- the complete cached-v3 and live-v1 producer/checker/driver/fixture files;
- task298 v2 driver/reply;
- task175/task176 producers and their pinned actual inputs; and
- the exact staged run-33149728601 ZIP, manifest, and sole raw member.

Create only:

- `search/d972_r07_history_free_positive_fast_resume_v7.py`
- `crosscheck/check_d972_r07_history_free_positive_fast_resume_v7.py`
- `search/d972_r07_history_free_positive_fast_resume_gha_driver_v7.g`
- `search/certs/d972_r07_history_free_positive_fast_resume_selftest_v7_20260829.json`
- `sol/luna_reply_342_r07_a0_v7_fast_positive_resume.md`

The v7 producer and checker must not import one another.  Neither may execute
or call cached-v3 `main`, cached-v3 `rank_zero_resume_checkpoint`, live-v1
`build_runtime`, live-v1 `PositiveSearch(...,resume=...)`, or either old full
production checker.  Reuse immutable mathematical modules and small pure
primitives only after byte authentication; do not fork or edit them.

## 2. Terminal and authority boundary

Allowed production terminals are:

```text
R07_HISTORY_FREE_POSITIVE_FAST_RESUME_V7_COMMON_WORD
UNKNOWN_INPUT:<typed reason>
UNKNOWN_RESOURCE:<typed reason>
```

Only COMMON after the independent v7 checker passes is an A0 candidate.
UNKNOWN_INPUT and UNKNOWN_RESOURCE carry every mathematical flag false and
have no negative content.  A hard implementation exception is a nonzero STOP,
not a typed mathematical result.

The producer may use authenticated checkpoint rows as
`heuristic_discovery_only`.  It must not claim exact cached-v3 resume,
historical trajectory parity, an exact old span, or retrospective dual replay.
Every newly generated boundary/correction row is actual and directly replayed.
Every COMMON is accepted only by Section 8's selected-support reconstruction.

## 3. Single-read input and light runtime

The GAP driver must extract the exact sole ZIP member once with the frozen
bytes/SHA/member gates and pass one ordinary raw path to v7.  The producer
opens and parses those 86,368,039 bytes once.  Do not route the document
through multiple wrappers or serialize a converted 86 MB clone.

Authenticate all transitive source pins before mathematical work, but retain
one immutable byte/digest snapshot rather than rereading the same source at
each layer.  Build the v277 light runtime explicitly.  It contains:

- task175 raw H1/H2/P target and PB3/PB4 boundary rows;
- actual E3/E4 operations, packed codecs, old Fox evaluator, ten contexts;
- the complete 6,441 relation roster and joint-kernel evaluator; and
- the data required for direct literal correction and boundary replay.

Before the first boundary epoch the process must not call
`enumerate_q0_sections`, `scan_memberships`, `prove_L`, construct qstates,
parents, letters, ten Q0 stores, adjusted-L data, or any singleton-fibre
coarse table.  Publish a derived `light_input_sha256` over every live light
owner.  There is no placeholder `heavy_input_sha256`.

Use a fresh wall limit of exactly 10,800 seconds, matching the pinned old
checkpoint limit and leaving the hosted job time for bounded checking and
artifact upload.  The old elapsed time is provenance, never subtracted from
the fresh budget.  The old cumulative counters remain immutable lower bounds;
the fresh v7 counters are separately named.

## 4. Triangular sparse discovery basis

Authenticate schema/seal/source identities, column ids, canonical sparse
rows, canonical F3 ancestry maps, stored pivots, and typed provenance shape.
For the exact pinned checkpoint require and independently derive:

```text
columns/rank                              2,896
boundary/correction columns               2,896 / 0
raw sparse support total / max             20,354 / 12
ancestry entries total / max              137,926 / 258
ancestry-weighted contributions           1,011,460
computed pivot support total / max        289,774 / 522
future ancestry indices                   0
zero/missing diagonal                     0
duplicate/empty/wrong pivots              0
```

Compute every `p_j=sum_i a_ji*c_i` from immutable parsed sparse rows.  The
per-j products are independent: shard them across a bounded persistent Linux
fork roster with canonical interval collection, or give an equally explicit
bounded streaming implementation.  Do not perform historical sequential
`Echelon.add(c_j)` and do not scan every earlier pivot for every c_j.

The serial parent checks nonzero diagonal, exact coefficient/order codecs,
coefficient one at the registered pivot, zero at every earlier pivot, and no
smaller live key.  Inject the computed P rows and pivot order directly into a
fresh reducer.  Attach a load-bearing sparse formal ancestry owner in symbols
for the 2,896 old columns and every new actual row.  A persistent DAG is
preferred; a sparse map is allowed only with explicit size/work caps.  Never
replace ancestry by a digest or copied solution list.

Reconstruct the actual target from the light source, reduce it against P, and
construct a fresh exact heuristic dual.  Do not direct-replay all 2,896 old
boundary provenances at startup.  All P/basis/dual receipts must say
`heuristic_discovery_only=true` and `exact_cached_resume=false`.

If a v276 gate fails, the only permitted result here is typed UNKNOWN_INPUT.
Do not silently fall back to a long serial replay in v7.

## 5. Persistent complete boundary owner

Start one persistent Linux-fork boundary roster after the light basis exists
and before any heavy Q0 work.  Use the conditionally accepted v6 arithmetic,
not its wrapper/receipt/mutation code:

- canonical typed nonzero dual support;
- exact descriptor/type matching and expanded pair prefix;
- disjoint complete half-open interval cover;
- actual `t=g*h^-1` and direct `t*h=g` check;
- full F3 accumulation/cancellation at `(block,relator,t)`;
- global lexicographic winner `(block,t_blob,relator)`;
- parent reconstruction of all selected contributing pairs;
- direct translated-row construction and scalar pairing; and
- serial basis/dual epochs with no partial commit.

The exact first pinned epoch has 1,188 dual entries, all `(block,component)=
(1,1)`, four matching descriptors, and exactly 4,752 expanded pairs.  Derive
these values; do not scan or transmit the other 100 descriptors per worker as
if they matched.  Cache immutable descriptor/group values once.  Avoid a
shared-memory write on every pair, repeated roster hashing per epoch, repeated
support decoding in the parent, and per-epoch pool construction.  Publish
literal pair, codec, frame, accumulator, ancestry, RSS, and wall accounting.

Pipe sends/receives must be deadline-observable.  A worker that stops draining
a pipe, dies, returns partial data, or crosses the deadline causes atomic
epoch discard and bounded terminate/join/`Process.close` of every child.
Do not use blocking `map`, unbounded `send_bytes`/`recv_bytes`, polling sleep,
or an unbounded retry.  Name RSS honestly as sampled sum/PSS/cgroup rather
than exact physical peak unless the metric actually is exact.

An ACTIVE row is directly replayed, given a formal new-row symbol, added to
the live basis, and followed by a fresh dual.  A complete zero correlation is
only the transition to Section 6, never NONMEMBER or exhaustion.

## 6. Q0-LATE correction phase

Only immediately before the first correction-oracle call, construct the
v277 heavy runtime once from the already authenticated light owners:

```text
qstates, qids, parents, letters, ten stores,
memberships, adjusted-L generators, and used coarse indices.
```

Derive and publish `heavy_input_sha256` only after all mandatory heavy objects
exist and their exact identities pass.  A correction event before this digest
is a hard protocol failure.  Use the frozen positive v1/v3 normalized
eleven-occurrence schedule and caches, but inject the v7 live basis directly;
do not invoke the old resume constructor.

Take a sealed last-safe light checkpoint before heavy construction.  A cap
during Q0 building returns that checkpoint and typed UNKNOWN_RESOURCE, rather
than relying on a `PositiveSearch` object whose constructor never returned.
If heavy construction succeeds, retain it once for all later correction
epochs.  Every correction hit must have an actual literal word, direct joint
kernel evaluation, eleven-occurrence/direct Fox equality, and a formal
new-row symbol before insertion.

## 7. Atomic checkpoint and cheap nonpositive checking

A resource checkpoint must bind the exact old source member, light/heavy
phase, computed P sidecar or canonical P digest and replay data, formal
ancestry DAG, live columns, fresh target/dual state, next clean serial epoch,
counters, worker cleanup, and source snapshots.  It must be usable by this
same v7 without rebuilding a historical serial path.  Do not deep-copy,
canonicalize, and hash an 86 MB object repeatedly to solve a fixed point;
store immutable source references and new v7 state separately with exact byte
identities.

On UNKNOWN_INPUT/UNKNOWN_RESOURCE the v7 checker is a bounded physical
transport checker only.  It authenticates paths, no-symlink/no-alias policy,
bytes, seals, source snapshots, typed reason, counters, phase, checkpoint
binding, cleanup, and all-false claims.  It must not build Q0, reconstruct all
columns, invoke a positive checker, or import the producer.  COMMON forbids
every resource checkpoint sidecar.

## 8. Independent selected-support COMMON checker

When target reduction becomes zero, freeze and canonicalize the formal
solution in old-column and new-row symbols.  The producer emits a candidate;
it does not self-authorize.

The helper-nonshared v7 checker must independently, without producer imports:

1. reconstruct the frozen actual target from H1/H2/P;
2. reconstruct every selected old boundary/correction row from its literal
   typed provenance and require equality with the selected stored row;
3. reconstruct every selected new row and its ACTIVE or correction replay;
4. sum all selected rows with exact F3 coefficients and require the complete
   sparse target equality;
5. materialize the correction word with coefficient 2 as inverse;
6. reconstruct the complete typed PB3/PB4 boundary preimage;
7. independently check the joint kernel, exponents, all eleven occurrences,
   direct all-seven H1/H2/P identity, and every frozen positive side gate; and
8. derive its compact verdict fields rather than copying producer booleans,
   scalars, words, or digests.

Read the candidate through an immutable physical object: use no-follow open,
reject symlinks and unexpected hard-link count, bind device/inode/size/bytes/
SHA, and do not reopen a mutable pathname between validation phases.  If any
subprocess is unavoidable, pass an immutable copied byte object or file
descriptor and recheck the same physical identity afterward.  One selected
row mismatch is rejection, not fallback to trusting stored data.

The final v7 verdict and producer receipt must contain the exact same sole
COMMON terminal, but the checker must derive it.  Only this pair is eligible
for A0 1/1.

## 9. Real-owner SELFTEST and mutations

The bounded fixture may use small groups, but it must use the production
E3/E4 packed codecs and the actual live reducers/validators.  Run every normal
and fault case at W=2 and W=4 through real processes: empty/one/short support,
present-shape typed filtering, cancellation, ACTIVE/zero, three serial duals,
timeout, blocked pipe, worker death, partial result, cleanup, light resource,
heavy transition, cheap nonpositive checker, and selected-support positive
checker.

At minimum physically inject and narrowly reject:

- future ancestry index, zero diagonal, changed raw sparse entry, changed
  ancestry coefficient, duplicate/wrong/hidden pivot, skipped P equation;
- wrong typed support, missing/overlapping interval, wrong t orientation,
  changed accumulator/winner/scalar, cross-epoch frame, blocked send,
  partial/dead worker, surviving process, and counter reset;
- heavy call before heavy digest, fabricated heavy digest, stale correction
  progress, zero correlation promoted to negative;
- omitted/changed selected row or coefficient, wrong coefficient-2 word,
  copied sparse-equality Boolean, changed target, changed boundary preimage;
- symlink/hard-link/TOCTOU substitution, unbound checkpoint, positive claim on
  resource exit, separator/cofinal/fake/Ihara flip, terminal reseal, and stale
  output.

Mutate the physical frame/file/owner before its actual validator.  Post-hoc
same-shaped dictionaries, explicit raises, hash-change-only tests, broad
exception catches, and producer-supplied expected failures do not count.

## 10. Driver, performance report, and boundary

The ASCII GAP driver must pin every input/current file, stale-reject only its
own v7 outputs, execute one producer then one checker, use `--seconds 10800`,
require one exact producer/checker terminal and byte-equal payload, enforce
terminal-specific physical sidecars, and make the sentinel literally the
final successful shell operation.  It must leave several hours of the
six-hour workflow boundary for checker/upload and may not edit the workflow.

The reply must list bytes/SHA-256, import/call/process graph, exact current
checkpoint arithmetic, measured symbolic memory/work bounds, every removed
v6 duplicate, real-owner mutation ledger, and the first remaining cost.  Do
not claim execution, speed, or a witness from static code.

End exactly with:

```text
IMPLEMENTATION:                       IMPLEMENTED or BLOCKED
EXECUTION:                            UNEXECUTED
HISTORICAL/CACHED PATH PARITY:         NOT CLAIMED
HEURISTIC DISCOVERY BASIS:             IMPLEMENTED or BLOCKED
Q0-LATE BOUNDARY:                      IMPLEMENTED or BLOCKED
SELECTED-SUPPORT POSITIVE CHECKER:     IMPLEMENTED or BLOCKED
ACTUAL A0 COMMON + CHECKER:            0/1
SEPARATOR / NEGATIVE CLAIM:            FORBIDDEN
LIFT / FAKE / IHARA:                   NONE
```

`TASK342_R07_A0_V7_FAST_HISTORY_FREE_POSITIVE_RESUME`
