# Luna task 332 - task328 complete-boundary closure addendum

Role: Luna, bounded implementation addendum.  Read task328, task331, and this
mail completely.  Create only task328's five authorized v3 outputs.  Do not
execute code or use git/network.

## 1. New paper input

Read `sol/proof_r07_thirteen_seed_complete_boundary_closure_v269.md` and the
complete v163 boundary theorem.  V269 supersedes only the producer-side
repeated/lazy boundary hot path; every other task328/task331 gate remains.

## 2. Producer boundary algorithm

Load and authenticate the exact typed roster of two PB3 plus eleven PB4 base
boundary rows.  Before quotienting K, construct the complete boundary space
from these thirteen seeds by one coefficient-bearing queue closed under
`x`, `x^-1`, `y`, `y^-1`.  Retain immutable raw-base coefficients and
parent/action ancestry for every rank raise.  Queue exhaustion is required;
a cap is only UNKNOWN_RESOURCE.

The terminal rank `b` must have exactly `b` accepted rows and at most `4*b`
post-seed action candidates.  Reuse this one complete basis for all 6,441
defects, K actions, change-of-basis checks, word/boundary differences, and the
anchor.  Do not perform a new boundary correlation per target.

## 3. Independent checker algorithm

The checker must not copy the producer invariant queue.  Reconstruct the
boundary span by v163 support-inversion dual column generation from the same
thirteen literal base rows.  Require complete dual support processing,
`q=g*h^-1`, direct `q*h=g` replay, strict rank rise for every active column,
and complete-zero correlation before NONMEMBER.  Compare checker and producer
spans by two-way mathematical containment, never cross-basis coordinates.

## 4. Mutations and accounting

Add narrow owner mutations for a missing base boundary, changed block tag,
left/right translation swap, omitted inverse action, changed parent/action
ancestry, incomplete queue called complete, wrong support-inversion product,
and false zero correlation.  They extend rather than replace task328/task331
mutations.

The reply must give `b`, exact seed/action/accepted/reduction counts,
producer/checker boundary algorithms, two-way span transcript, and proof that
no quotient roster or repeated per-target boundary scan remains.  A4 stays at
most 1/3 until actual execution.

`TASK332_R07_TASK328_BOUNDARY_CLOSURE_ADDENDUM`
