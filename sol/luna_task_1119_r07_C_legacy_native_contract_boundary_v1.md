# Task1119 — C側独立の元native target operandとrolling契約

宛先: 既存 packet_checker / Luna。1117のroot依頼済みmixed current/history return追補を完成・新目録で凍結してから着手する。指定返信 sol/luna_reply_1119_r07_C_legacy_native_contract_boundary_v1.md、末行 TASK1119_VERDICT:。補助資料は新TEMP/shadow-atelier-audit163/task1119/。repo変更は指定返信だけ。新agent/Git/GHA/network/credential/既process操作、Python/GAP/AST/import/compile/source/数学/自己試験実行、実親の大規模scan/再hashは禁止。PS/.NETのraw source・小JSON・bytes/SHA照合だけ。

共通1115指示と裁定2230を保持。自系C6 419541 B/3996972ccfe8ba9c168b537ac274de96ff69a6fe27fb400e7de6a8e3a19a52ff、1117の凍結済み資料と以前から採択済みのretained共有TCBだけを根拠とする。P6/1113/1116/1118の私的source/設計/返信、1115作者の途中票は読まない。新たな相手実装を共有TCBへ追加しない。

現v6/18親/k128/同caps/計器/guardは変更0。1112は復元準備を終えて全typed受領中。本便はv6の追加待ち条件ではない。正式inventory5の後にrootから最終C6定数bindingが来たら、この調査を保存してそちらを優先する。未来のa_i/p_i/headは未定、1962を仮定しない。

## 今回閉じる限定境界

1. 1117 N03/O1・N04の未列挙native字段を自系から調べる。C6 AcceptedInputs→C.FIXED.validate_parent_generations、restore_physical_anchor、元base_pivot_metadata、old64のretained snapshot/step readerの実呼出し先を、採択済みsourceの全pinとrawで結ぶ。動的alias、元writer、実return→次consumerを列挙。関数やschemaの記憶補完はしない。
2. base state、delta/seed30、seed34、packet、refinement、external E、old64それぞれのinstruction/result.target/manifest/HEAD/native rootsを別型で記述する。全keyset・ordinary整数/trit・nullable・順序・file又はrow locatorの全参照先、各readerが実に比較するfieldを示す。元schema/sha256字段の不存在も原型のまま記述する。
3. base target.reductionsとseed target.new_reductionsの全operand型と参照先を特定する。全項・零・重複・整数指数を保持し、multi-rowのsumを新append一行へcastしない。各前後targetはどこから得るか、対応するnormalized rowとnative rootをどの文書から解決するかを明示。実sourceで閉じない参照はOPENのまま全roleをactive保持する。
4. 各nativeのrolling起点、元hの全predecessor、全bodyと除外字段、canonical規則、terminalを原writer/readerと結ぶ。全8059記録の履歴と元97のtarget record列は別範囲。全file SHA、JSON self-seal、物理h、snapshot/checkpointのhashを混同しない。new core/execution acceptanceを元hへ差し替えない。
5. 現C/retainedが実施する型/hash/metadata検査と、次版で課す全歴史transitionの数値式を別欄にする。原rho2の直接読取、元word/Γ、positive/lower-zero/A0は現限定のまま。全chain相対式の将来readerは未実装で、ここで数値再演をしない。
6. 公開型として使える内容を、自系helper全文の代わりにfield/式/locator/native grammarの表として納品する。rootがP側独立表と後で対照する。原小JSONを読む場合は先に範囲とpinを固定し、その小集合だけを読む。全親・全vector・全factorの走査を追加しない。

出力は新versionのsource-pin/raw-range・native型表・field/return/実consumer表・未決とactive保持範囲・全材料目録と短い返信。P1/maps/TCB最小閉包、具体引用条項、可用性B、実変換・性能は今回対象外。三分記帳、A/B、H2/H3の別、GHA反復の包括認可を維持する。新source/fixtureは作らず、実装前のnative契約の調査として閉じる。
