# Task943 -- narrow current-seed one-pivot release review

Reply ONLY `sol/sol_reply_943_audit_r07_selected_seed_materializer.md`.
Read FULL task940 and the new producer/checker v3 actual paths; old
accepted numerical parents/lineages are premises. Root reviews workflow.
Sources are being finally frozen by940/941; inspect the actual-path core
now if useful, then bind the final reviewed byte hashes before release.
Do not delay on optional cleanup, history, or the v548 paper oracle.

Necessary checks only:

1. Current scalar authority selects seed34/char0/value1 from generation8060
   rank1355, not stale seed30/root/support902. Complete raw-event/P1 fold,
   selected raw-row receipt, complete96776 lower-zero check, Task712 B and
   nonzero current-lambda pairing stay joined.
2. State reader is old1354 rows PLUS the saved accepted seed30 pivot, with
   valid literal parent references. Reduction respects insertion order;
   append is one new pivot, not a fake concatenated old-state copy.
3. Only the new target elimination is run. The final next lambda is tested
   against ALL1354 old rows plus both new pivots and the saved next target;
   final row-count receipt matches actual rank. Old target derivation is
   retained, not silently claimed freshly replayed. MEMBER_CANDIDATE is
   not full A0; no seed-only/orbit-completeness promotion.
4. No producer arithmetic imported by checker, stale ABI key mismatch,
   unbuffered instruction readline, retained multiple large Task554 bodies,
   old closure/target reconstruction, historical selftest or obvious
   unnecessary large copy on reachable paths. Accepted source-core lineage
   limitations stay disclosed; do not ask for a third arithmetic engine.

Report short PASS/FAIL and necessary fixes only, with exact frozen hashes.
No implementation, numerical execution, network, credentials or git.
Root will run the small serial canaries and actual pair on GHA.
