# Sol task 527 -- implement the actual rank111 lazy K=0 successor

Role: Sol(max), implementation strike team under the researcher's explicit
authorization to use Sol when Luna's implementation is incomplete.  This is
an implementation exception to the usual Sol/Luna division.  Do not run full
production, dispatch GHA, mutate workflows/git/releases, or overwrite any
existing file.  Write only these four versioned successors:

1. `search/d972_r07_a0_actual_tau_free_lazy_k0_seed_v5.py`
2. `crosscheck/check_d972_r07_a0_actual_tau_free_lazy_k0_seed_v9.py`
3. `search/d972_r07_a0_actual_tau_free_lazy_k0_rank111_resume_gha_driver_v13.g`
4. `sol/sol_reply_527_r07_a0_rank111_lazy_k0_actual_successor_v1.md`

The Task525 v4/v8/v12 files are a rejected transport sketch whose production
path always returns `UNKNOWN_RESOURCE`.  They may be read for the eight-member
transport envelope only.  Do not patch, adopt, or present them as an owner.

## 1. Narrow exact scope

Implement the audited v433 positive theorem only on the **actual current K=0
task445 lane**.  This is the fastest sound production subset.  Keep `K!=0`,
nonzero tau, S3--S9, unsupported coordinates/orders, and every unimplemented
case as typed claims-false `UNKNOWN_RESOURCE`; do not port the failed rank99
formula/model/scalar semantics.

Read these authorities in full:

- `sol/proof_r07_lazy_positive_formula_seed_selector_v433.md`,
  10495 / `3a8b5085e3a0a712dfd32c246cf472ca16616a2e3d7af494e4fcc8b30d02d940`;
- `sol/sol_reply_524_audit_r07_lazy_positive_compact_seed_selector_v433.md`,
  13304 / `3b028e05ac74310a2001494e0d112d0ab389bee82b83c5b8ed7cb84a91c39af5`;
- task445 producer v3, 12215 /
  `0140447110cfa568a77300bd7d43d51c622597704b7b91ed5209c63168c9ef37`;
- current independent checker v7, 3653 /
  `e1b80c586985f5113b300508f6bc78d055a37243e3fd6795b8f81148b0988de1`;
- Task521 rank111 resume driver v11, 8683 /
  `84db6c150d8ce764c411afa91a9cc9c31ad193ecaf719900faa9ebdbc32b5b7d`.

Also read Task525 and its STOP reply.  Code-pattern references may be used,
but the sole state, physical-row, formula, direct-pairing and update authority
is current task445.  Do not import Task416's rank60258 checkpoint or rank99's
custom `model179`/`formula_bundle`/scalar gate.

## 2. Exact input and migration

Derive the driver from v11 and retain the permanent release and its exact
eight-member manifest.  Resume from member 5:

`d972_r07_a0_actual_tau_free_rank_ladder_v10_output.checkpoint`,
85934 / `69a7ec3da4907f24af0f68c1975538b9ff9b6102f14e334f7c0725d2542dfd93`.

Before any new insertion, authenticate and replay the exact legacy prefix:

- accepted/rank/round = `68/111/73`;
- dual digest
  `56ccd1f3cc6b54fe340a69ce6a0ec99f5aeb3358ae80288c6b11c3f1ec664864`;
- remainder digest
  `9eed8114d9e3172c7a11153d9c5cd6e5fc2e5184a8d6e3681cce5c82a83b4326`.

The new v5 schema/binding/seal must preserve all 68 legacy source records
exactly.  It must resume both this one exact legacy input and a fully checked
v5 checkpoint.  Every accepted rise is durably written before the next round.
Never require a full boundary closure or the old 129-MB physical checkpoint.

## 3. Implement the current K=0 lazy path

Preserve task445's six actions first.  On an action hit compile zero seed
formulae.  Otherwise obtain exactly one current task445 tau-free adjoint and
walk the exact 44 compact seeds in printed order, one seed at a time:

1. compile only this seed's task445 occurrence formula and normalized
   epsilon/18 constant;
2. require current recognized coordinates in S0--S2 and normalized K=0;
3. enumerate the complete current task445 canonical support-fibre schedule,
   using the independently authenticated kernel order nine and frozen order;
4. do not build future seed data, an all-seed coordinate union, or eager
   un-conjugated identity rows;
5. at the first predicted nonzero, reconstruct the literal conjugate, direct
   task445 physical row and fresh row; require equality, exact exponent/N/E
   typing, direct `pair(current_dual,row) in {1,2}`, nonzero nonmutating
   remainder and predicted pivot;
6. insert exactly one row, update exactly once using task445's carry-forward
   API, durably checkpoint, and restart at seed 1 under the new dual.

The direct pairing is the authority: a formula prediction may nominate work,
but it cannot insert unless the independently reconstructed physical row has
the same nonzero scalar.  A formula/direct mismatch is `UNKNOWN` invariant
failure, not a no-hit.  Empty/zero/no-hit/unsupported/resource cases are
`UNKNOWN_RESOURCE`, never negative.  All A0/COMMON/NONMEMBER/fake/Ihara
claims remain false except that a fully replayable task445 COMMON candidate
may use only the pre-existing candidate boundary.

Record seed/formula digest, K=0, required coordinates, support cursor, literal
delta, exact exponent pair, direct scalar, row/pivot, pre/post dual/remainder
digests and counters for seeds/formulas/fibres/identity replays/adds/updates.

## 4. Independent checker

The v9 checker must not import a producer selector validator.  It may pin
shared low arithmetic owners, but it independently authenticates the exact
legacy/new checkpoint, rebuilds every selected seed formula and K=0 support
schedule, reconstructs literal/row/direct pairing, replays pivot/rank and the
single update, and checks result/checkpoint equality, binding/seal, terminal
cardinality and claims.

It must reject coordinated producer record mutations even if the attacker
recomputes the producer seal.  It must distinguish resource terminals from
formula/physical invariant failures.  It must explicitly reject any selected
K!=0 record in this v5 K0-only owner.

## 5. Driver and bounded tests

V13 retains v11's real release download, exact eight-member pinning, member-5
selection, stale-output gates, `set -euo pipefail`, `umask 077`, `ci/out`,
`ulimit -v 5200000`, one producer and one checker.  Use fresh v13 output
names and a new external boolean preamble.  Retain:

- producer `--seconds 7200 --rss-bytes 4800000000 --max-rises 64` under a
  7500-second outer timeout;
- checker under 3600 seconds;
- one exact final marker;
- no production SELFTEST.

Bounded repository-external tests must cover at least:

1. real imported call path, not a synthetic selector substitute;
2. exact roster count/digest and 43/45/6441 rejection;
3. action hit with zero formula work;
4. seed1 hit with seed2 untouched, one formula, no eager identity replay,
   exactly one add/update;
5. K0 empty/zero/duplicate/no-hit, unsupported coordinate/order, and K!=0
   claims-false resource stop;
6. formula/direct scalar, row/fresh, exponent/N/E, dependent row, pivot,
   stale-dual and second-insert mutations;
7. time/RSS before seed1 and mid-fibre preserving an already durable rise;
8. legacy prefix/count/rank/round/binding/seal/pre/post digest mutations and
   a coordinated selected row/scalar/seal mutation rejected by checker;
9. Python compile, fixtures, GAP parse, generated shell `bash -n`, exact
   eight-member fixture selecting member 5, and v11-to-v13 diff classification.

Do not perform the full rank111 replay locally.  Report exact outputs,
bytes/SHA-256, bounded commands/evidence, known limitations and one verdict:
`READY_FOR_INDEPENDENT_AUDIT` or `STOP`.  No implementation result changes a
v220 numerator.
