# Luna task 232 - R07 word-independent compressed successor kernel v1

Date: 2026-08-28

Role: bounded mechanical implementation.  Sol owns mathematics, acceptance,
git, GHA, and production brokerage.  Do not run Python, Node, GAP, git, GHA,
or network.  Do not edit a workflow.  Report only to
`sol/luna_reply_232_r07_word_independent_successor_kernel_v1.md`.

## 1. Objective and governing theorem

Read in full v145, v168, v188, v189, v190, v231, the final task193
producer/checker interfaces, and the final task198 producer/checker/reply.
Implement Theorem 2.1 of
`proof_r07_word_independent_successor_and_direct_pair_compiler_v231.md`:

```text
positive task198 complete marked roof presentation
  + fixed ten typed first-successor context evaluators
  -> complete exhausted word-bearing K=ker(Delta1 -> Delta0).
```

This task is independent of the task192 correction word.  It does not consume
a task192 or task193 actual-word receipt and does not construct `d1`, `e1`, a
pointed multiplier, endpoint, lift, fake, or Ihara witness.  It supersedes the
literal-Delta1-BFS architecture of task196 for the A4 kernel gate; do not
import task196.

## 2. Authorized files

Create only:

```text
search/d972_r07_word_independent_successor_kernel_v1.py
crosscheck/check_d972_r07_word_independent_successor_kernel_v1.py
search/d972_r07_word_independent_successor_kernel_gha_driver_v1.g
search/certs/d972_r07_word_independent_successor_kernel_selftest_v1_20260828.json
sol/luna_reply_232_r07_word_independent_successor_kernel_v1.md
```

The `.py`, `.g`, and JSON files are ASCII-only.  Do not change predecessors.

## 3. Production input and exact authentication

PRODUCTION consumes a positive independently accepted task198 receipt,
canonical member/manifest, and producer/checker attestations under exact
guarded `ci/in/` paths.  Authenticate canonical bytes, SHA-256, schema,
`ROOF_BRIDGE_ISOMORPHISM` terminal, seal, immutable run/head/artifact/member,
checker acceptance, dependency pins, `Delta0.order`, the complete 6,441-row
presentation, marked generators, evaluator ABI, ten typed coordinates, and
bridge ledger.  Do not require nonexistent receipt fields and do not accept a
nonempty byte string as an attestation.

Require every literal presentation row to evaluate to the roof identity by
the authenticated task198 evaluator, and require the complete-presentation
normal-closure/order certificate.  A list of identity words without that
certificate is insufficient.  SELFTEST cannot enter PRODUCTION.  Missing or
nonpositive input is typed `UNKNOWN_INPUT` before successor work.

Authenticate exact source identities before using the fixed task193/task179
affine/relative-Frattini arithmetic.  Reuse of a pinned arithmetic constructor
is allowed; no actual task193 word or receipt is required.  The checker uses
an independent implementation and must not import this producer.

## 4. Exact ten typed context maps

Build the ten successor evaluations from the literal source substitutions,
not from context names or receipt blobs alone.  With right-to-left paper
product `PP`:

```text
E3: (x,y), (x,z), (y,z), (u,x), (u,y)
     z=inverse(PP(x,y)), u=inverse(PP(y,x)), x=A12, y=A23.

E4: (A23,A34),
     (PP(A12,A13),PP(A24,A34)),
     (A12,A23),
     (PP(A13,A23),A34),
     (A12,PP(A23,A24)).
```

Their exact task198 context IDs are
`21,22,23,24,25,1,27,21,26,28`, with types
`E3,E3,E3,E3,E3,E4,E4,E4,E4,E4`.  Keep E3-C21 and E4-C21 distinct.  The
H1/H2 repeated E3 occurrence is a later ten-to-eleven insertion and must not
be added as an eleventh acting factor here.

For each coordinate construct the exact fixed extension
`E_{j,1}->E_{j,0}` and its elementary-abelian kernel quotient.  Directly
check marked multiplication, inverse, reduction, crossed-derivation order,
kernel exponent three, and the ten source substitutions.

## 5. Relator defects as quotient vectors

For every task198 relator word `r_j`, evaluate its ten successor values and
require all ten roof reductions to be identity.  Retain the resulting
kernel vector

```text
b_j = rho1(r_j) in V = product_j ker(E_{j,1}->E_{j,0}).
```

Represent every coordinate by its raw sparse affine/Fox chain modulo the
complete translated PB3/PB4 presentation-boundary image.  Tag all ten copies
before taking a direct sum.  Do not merge repeated algebra blobs across
coordinate tags.

Implement a fail-closed exact membership oracle for

```text
candidate in B_complete + Span_F3(retained K rows),
```

where `B_complete` is the direct sum of the full translated presentation
boundary spaces.  A positive answer retains both boundary coefficients and
K-row coefficients.  A negative answer retains a dual annihilating every
complete boundary direction and every retained K row, and pairing nontrivially
with the candidate.  A finite sample of boundary translates, a hash, or a
rank alone is not a decision.  Resource exhaustion is `UNKNOWN_RESOURCE`.

## 6. Invariant closure and literal ancestry

Insert the 6,441 defects in exact presentation order.  Whenever a defect is
independent modulo `B_complete` and prior K rows, normalize it, retain its
full coefficient/boundary ancestry, and enqueue it.  For every queued basis
row apply the exact coordinatewise conjugation action of
`x,x^-1,y,y^-1`, using the marked successor lifts, then test the result by
the same oracle.  Continue until the queue is empty.

Track ancestry through every row operation.  A basis vector must retain a
finite product of powers of literal conjugates of the original relators.
Directly evaluate that product in all ten successors and compare it with the
stored vector; also require roof identity.  Queue path labels without linear
coefficients are not ancestry.

The terminal certificate proves both containments by finite checks:

1. all 6,441 initial defects reduce into `B_complete + Span(K_basis)`; and
2. every four marked translate of every K basis row reduces into the same
   span.

Together with independence modulo `B_complete`, this is exact queue
exhaustion.  It does not enumerate `Delta0`, `Delta1`, or all of `K`.

## 7. Word-bearing elementary-abelian K certificate

If the accepted rank is `t`, emit a basis `k_1,...,k_t` with:

```text
literal source word/product ancestry;
ten exact successor values and roof reductions;
independence certificates modulo complete boundaries;
order-three replay;
pairwise-commutation replay;
generator-action matrices and inverse checks;
|K|=3^t and relative augmentation nilpotence bound 2t+1.
```

The order formula follows from the independently checked elementary-abelian
basis; it is not a replacement for those checks.  Export the basis source
words, action matrices, and relation-defect ancestries needed by the later A5
joint closure.  Do not export or claim a complete successor-state roster.

## 8. Independent checker

The checker independently authenticates task198 and rebuilds the ten
successors, substitutions, all 6,441 defects, complete boundary oracle, and
roof actions.  Use a different pivot order and process relators/generators in
a different deterministic order.  Replay every producer positive membership
chain and every load-bearing negative dual.

Independently verify terminal completeness by reducing all initial defects
and every marked translate of every producer basis row.  Compare producer and
checker K spans in both directions modulo complete boundaries, not by rank or
digest.  Reconstruct every basis word value, exponent-three/commutation fact,
action matrix, `3^t`, and `2t+1`.  The checker does not need to rediscover the
same arbitrary basis, only the same subspace with word-bearing certificates.

## 9. SELFTEST and destructive controls

Use a bounded noncommutative marked roof with ten genuinely typed contexts,
a complete presentation containing redundant relators, nontrivial boundary
equality, a noncentral roof action, and an elementary-abelian successor
kernel of rank at least two.  Exercise zero and dependent relator defects,
multiple rank raises, a dependent generator translate, full queue exhaustion,
and two different accepted bases with equal spans.

Every registered mutation is concretely applied once and rejected by the
relevant validator.  Include at least:

```text
task198 bytes/schema/terminal/run/head/artifact/member/checker;
one presentation word and completeness/order field;
one type/context ID/source substitution;
E3-C21/E4-C21 merge and repeated-E3 insertion;
paper-product order; affine multiplication/inverse/crossed order;
one roof reduction; one raw defect coordinate/block tag;
omitted translated boundary; boundary coefficient;
false positive membership and false negative dual;
one K-row coefficient/pivot/ancestry word;
one omitted relator defect; one omitted generator translate;
early queue terminal; generator action/inverse;
order-three/commutator/rank/order/nilpotence bound;
Delta1 BFS substitution; task192-word dependency;
SELFTEST-for-PRODUCTION; traversal/stale output;
resource stop changed to completion;
false d1/e1/mu1/lift/fake/Ihara.
```

Mutation lists recorded without execution are rejected.

## 10. Resources, driver, reply, and v220 mapping

Use one invocation-wide wall/RSS meter and live caps for input bytes,
relator evaluations, affine oracle rounds, boundary columns/support,
membership queries, accepted rank, queue actions, ancestry words/DAG nodes,
dual correlations, checker work, checkpoint bytes, and serialized bytes.
Checkpoints bind input identity, exact rank-zero replay order, accepted rows,
queue cursor, oracle transcript, and caps.  A resumed cap must strictly
increase.  `UNKNOWN_RESOURCE` never certifies a smaller K.

The serial GAP driver uses fresh `ci/out`, exact-one markers, exact terminal
equality, visible logs, and a nonempty sentinel only after independent
acceptance.  Expose SELFTEST and PRODUCTION without a workflow edit.

The reply lists exact identities, dependency cone, schemas/terminals,
resource envelope, mutation count, exact missing production inputs, and:

```text
A4 presentation input:       0/1 until task198 production is accepted/staged
A4 invariant closure:        0/1 until actual queue exhaustion is accepted
A4 word-bearing K:           0/1 until independent span/ancestry acceptance
A0/A2/A3:                    no dependency and untouched
A5 and later:                untouched
```

End by stating that no `d1`, `e1`, pointed multiplier, exact endpoint,
compatible lift, fake certificate, or Ihara witness was constructed.
