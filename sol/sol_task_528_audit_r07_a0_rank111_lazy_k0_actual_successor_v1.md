# Sol task 528 -- independent audit of the actual rank111 lazy K=0 successor

Role: Sol(max), independent adversarial implementation auditor.  This is one
bounded go/no-go audit, not a request for new architecture or an expanded test
framework.  Do not edit implementation files, run full production/rank111
replay, dispatch GHA, or mutate git/releases/workflows.  Write only:

`sol/sol_reply_528_audit_r07_a0_rank111_lazy_k0_actual_successor_v1.md`

Subject outputs from Task527:

- producer `search/d972_r07_a0_actual_tau_free_lazy_k0_seed_v5.py`,
  34773 / `94e9079c36592414d394f816d0d1190822157c017afecd9e75c9e19f8c7aa5aa`;
- checker `crosscheck/check_d972_r07_a0_actual_tau_free_lazy_k0_seed_v9.py`,
  27570 / `9b9bfbf72a312ed759861c854f1f5513342c037c2eb74b89bee8e09caa2f29c0`;
- driver
  `search/d972_r07_a0_actual_tau_free_lazy_k0_rank111_resume_gha_driver_v13.g`,
  8683 / `8f034abce4f469542206338e14502281dcc7ae1862338bafef5d431d74e5f63e`;
- Task527 reply and its bounded evidence.

Read Task527 commission/reply, v433, Task524, Task526, current task445
producer/checker, and all three subject files in full.

## 1. Scope and legacy-state authentication

Confirm the owner is genuinely current task445 and not Task416/rank99 code.
Audit the exact legacy member gate: outer bytes/SHA, v3 schema/binding/seal,
canonical 68-source prefix, 68/111/73, dual/remainder digests, N1=N2=0, all
tau zero and no unrecognized keys.  Check that migration preserves the exact
legacy records and that both legacy and sealed-v5 resume paths reconstruct the
same physical state before new work.

Check all schema/binding/seal/rank/round/counter relations and that every rise
is durably written before the next round.  Do not demand local full replay;
audit the executable route and bounded evidence.

## 2. Mathematical selector semantics

Audit the actual imported production call graph.  It must:

1. try six actions before formula work;
2. build one current tau-free adjoint per unchanged dual;
3. compile the exact 44 seeds one at a time in printed order;
4. require K=0 and S0--S2/order-nine support, leaving all other cases
   claims-false RESOURCE;
5. evaluate the **whole** current formula on each candidate, including all
   coincident terms, and continue on zero;
6. reconstruct literal conjugate, replay row and fresh row, exponent/N/E
   typing, direct nonzero pairing, nonzero remainder and pivot;
7. make one add and one task445 carry-forward update, checkpoint, clear only
   the relevant selective cache, and restart seed 1 under the new dual.

Specifically confirm that none of the quarantined rank99 custom
`model179`/formula/scalar/retain or batch-state semantics is imported and that
support-fibre membership alone is never treated as a nonzero formula value.
No no-hit/resource/unsupported branch may be negative.

## 3. Independent checker and mutation boundary

Confirm v9 does not import v5 or a producer selector validator.  Trace its
independent formula/support/literal/row/pair/pivot/update replay and exact
result/checkpoint/claim checks.  Rerun only small critical tests needed to
establish that K!=0, formula/direct mismatch, row/fresh, exponent/N/E,
dependent row, pivot, stale dual/second insert, legacy prefix/seal and a
coordinated row/scalar/reseal mutation are rejected.  Distinguish invariant
UNKNOWN from RESOURCE without promoting either.

## 4. Runtime and transport audit

Audit v13's exact source pins, permanent release, ordered eight-member
manifest and member-5 choice; generated shell syntax; stale paths;
`set -euo pipefail`; one producer/one checker; 7500/7200/4800000000/64 and
3600 limits; output/checkpoint survival; marker cardinality; and absence of a
production SELFTEST.

Also inspect the production hot path for accidental large work that defeats
the optimization: no eager all-44 formula list, all-seed coordinate union,
un-conjugated identity replay, full PB3/PB4 closure, global rank99 roster,
duplicate state update, unnecessary full copy, `gc.collect`, or checker before
producer completion.  Do not demand speculative micro-optimization.  State
the exact unavoidable resume/replay cost separately from per-round lazy work.

## 5. Verdict

Return exactly one:

- `GO_FOR_GHA_DISPATCH_ACTUAL_K0`, or
- `STOP_DO_NOT_ADOPT` with the smallest exact blocking defect(s).

GO authorizes only parent adoption/commit and one v13 GHA dispatch.  It is not
an A0/COMMON/lift/fake/Ihara result.  Report exact bytes/SHA, commands,
evidence, limitations and v220 consequence.  Use repo-external temporary
files and perform no git/network mutation.
