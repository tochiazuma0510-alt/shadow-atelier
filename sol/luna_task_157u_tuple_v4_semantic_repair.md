# Luna task 157u - versioned tuple-v4 semantic repair and independent checker

Role: Luna implementation. Do not run GAP or a full local campaign. Parent
alone will commit, push, and dispatch after independent audit.

Authorized new files only:

- `search/d972_b4_burau_fiber_v4.py`
- `search/check_d972_b4_burau_fiber_v4.py`
- `.github/workflows/d972-burau-tuple-v4.yml`
- `sol/luna_reply_157u_tuple_v4_semantic_repair.md`

Read replies 157l and 157m plus the complete v3 producer/workflow. The first
v3 GHA run 32048896462 was cancelled because v3 retained the now-refuted
premise that each frozen source representative itself belongs to `F2'`/`H'`.
The frozen words bind roof/key only: 956/972 have nonzero free abelianization,
including row 2 `(-4,-8)`.

Create a versioned v4; do not edit committed v3.

Requirements:

1. Preserve exact low-memory tuple/Schreier normal-closure enumeration. For
   each row, evaluate the frozen source word only to replay and authenticate
   its roof/key. Obtain an exact `h0 in H'` independently from the complete
   projected H' section over that roof; gate `h0` membership and exact roof
   projection. Enumerate the complete fiber `h0*K`, where K is the exact
   matrix-only kernel. Never require the frozen source word itself in H'.
2. Add truthful receipt fields (`source_word_role`, `hprime_preimage_role`,
   `fiber_reconstruction`) and a real frozen negative regression requiring
   row 2 abelianization `(-4,-8)` and exactly 956/972 nonzero pairs. Reject
   restoration of the invalid membership gate.
3. Keep q3 `(3,-1)` and q4 `(4,2)` exact calibrations, hash-locked Python
   dependencies, source/artifact/hash/order/972/negative gates, uncapped exact
   traversal, q5 `a=2,4`, UNKNOWN_RESOURCE failure, and attempt-unique always
   artifacts.
4. Add an independent checker that does not import the producer or share its
   tuple/matrix/product helpers. It must authenticate all frozen hashes and
   source-role metadata, independently replay all 972 roof/key source words,
   validate serialized `h0`, kernel elements/generators, complete reconstructed
   fibers, and raw five-A.18 defects. It must reject corrupt product order,
   row/key/source role, h0, kernel element, missing fiber element, and defect
   count fixtures. Be explicit about what completeness evidence is independently
   recomputed versus bound from the producer/calibration receipt.
5. The workflow runs producer then checker for both calibrations and q5 lanes;
   q5 authenticates both exact checked calibration receipts. Only complete
   producer+checker agreement may label a finite zero-fiber candidate
   cross-checked. All-pass remains UNKNOWN, never B.
6. Run YAML parse, both Python compile/help/self-tests, negative fixtures, and
   `git diff --check` on authorized paths only. No full calculation, commit,
   push, dispatch, or local GAP. Report hashes and PASS/FAIL.

Fail closed if a truly independent checker cannot be completed in this task;
do not silently downgrade the contract.
