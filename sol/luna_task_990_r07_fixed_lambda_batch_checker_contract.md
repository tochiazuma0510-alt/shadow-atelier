# Task990 — 固定lambda batchの独立checker契約（静的設計のみ）

役: 既存packet_checker。変更可は `sol/luna_reply_990_r07_fixed_lambda_batch_checker_contract.md` のみ。
source/WF/既存返信は凍結。ローカルPython/import/AST/数値/GAP/network/git/credential/新agent禁止。
新producer側のsource/989返信は読まない。既存C側sourceと共通数学・保存metadataだけを読む。
rootは985本走と986の一回限り絶対96対照を実行し、同時に988で数学条件を独立監査している。

先にrootの未送信cap96 REST body（%TEMP%/shadow-atelier-audit163/cegar-resume-next-dispatch-parent33990567016-cap96-v1.json）
10004 B/d5ad1f602a9efda6dd214a3875d897a696ecb30fc58f1bdbdf4b9fa57fa6aa1bを静的metadataとして確認。
旧cap128 v2からmax_appends文字列だけ96へ縮小、observed-parent/ref不変かを読む。986/987返信は遡及変更しない。

裁定2154/2155とTask988を全文読む。actual親=run33990567016/1・64段・rank1450/gen8155。
固定lambdaで全弦を評価し、既定roster順の先頭非零弦32本以下を各々Omega語化、全source/primal/P1/four-Bを通し、
旧spanと先採用行に対する実消去で独立分だけ順序どおり追加する候補。相互独立は実消去で判別する。
旧selection lambdaとbatch後separatorは別型。全弦零でもauxが非零なら一件fallbackかUNKNOWNを保持。

既存C側だけから独立実装の最小契約を提案する（まだ実装しない）:
- 固定section/cochain/treeを自前で再現し、選定32以下の全roster/witness/tau/etaを全EOFに結ぶ関数/差分。
- 各Eのraw/source/P1/physicalを再現して同selection lambdaと結び、旧span＋採用済への独立性を消去で判別。
- 旧lambdaをcurrent全row annihilatorと偽装しない新batch型、正規化行/target scalar/recipeの全符号恒等式。
- 旧64prefixと元owner/source/startを凍結し、新source責任範囲を別receiptへ置く最小移行方式。
- 中断/部分採用/依存候補の全保存、aux非零/COMPLETE_ZERO/Linearの型、実row数とbatch数の非混同。
- 識別力のある最小canary、ソース著者/AST/TCBの限界、全64再読との計算重複を削ってよい条件。

source候補名・CLI/入力/出力/schema案、既存Cの関数名と行番号付き根拠を報告。
共通wire契約はrootが988と本票から決めて両authorへ伝える。実装/GHA/CV9や未来pinを先取りしない。
最終行 `AUDIT_990_VERDICT:`。
