# 157ee reply — joint-kernel qstar closure certificate

## Outcome

The versioned producer/checker/driver bundle is frozen and ready for the
bounded GHA self-test followed by the full same-job run.  No full mathematical
run was executed locally.

The implemented positive structural certificate freshly rebuilds the frozen
157ed prefix and raw qstar functional, enumerates the exact 243-element normal
kernel generated by the 26 correction records, and checks three complete
relation layers:

- all 6,318 Cayley edge relations of the 243-state kernel;
- all 104 marked `x/y` action relations;
- 19 relators from independently certified factor presentations of the marked
  degree-36 quotient `Q0`.

The checker independently reconstructs the q3 quotients, contexts, prefix,
raw functional, states, sections, relations, and direct/NF canaries.  It uses
SymPy coset enumeration to require the two frozen factor presentations to have
orders 504 and 2,916; it does not import producer state IDs, sections, basis,
pool, or lambda values.

The only positive CLOSED claim is scoped to
`ker(Q0 x E3 x 31 E4 contexts)` against the registered fixed prefix.  The
receipt and checker explicitly forbid promotion to full D2, full H3, global
lift nonexistence, B4-A, or B4-B.

## Frozen files

- task: `sol/luna_task_157ee_b345_joint_kernel_qstar_closure.md`
  - SHA-256 `64a32c0b7e3d4efc41ddb8e0e7036282b0b5430d9ab46bbfe125b588478a95d4`
  - 11,226 bytes
- producer: `search/d972_b345_joint_kernel_qstar_closure_v1.py`
  - SHA-256 `06ba6cf361957db3e339d48d14b3d4fbc689de9642e3f96273fbe8f3160e76dc`
  - 67,945 bytes
- checker: `search/check_d972_b345_joint_kernel_qstar_closure_v1.py`
  - SHA-256 `9e721634d1f16be806e315eec263ec272bc023587f862703c094b7dd37c0111f`
  - 47,661 bytes
- driver: `search/d972_b345_joint_kernel_qstar_closure_gha_driver_v1.g`
  - SHA-256 `ad536c97644ba28e511ca7cb1f58192bddfecdfce6630fd76dde108589303ad4`
  - 10,906 bytes

The driver pins the producer, checker, task, 157ed producer/checker/driver,
q3 driver, and exact q3 artifact.  It runs q3 in an isolated GAP child, checks
the independent q3 checker, uses pipefail/tee and exact-one markers, and shares
one 18,000-second producer/checker budget inside the 330-minute job.

## Self-test record

The initial combined self-test passed, but subsequent static audit showed its
mutation coverage did not reach the completed production validator deeply
enough.  This was treated as a self-test-contract defect, not as mathematical
evidence.  The shared production packed decoder and completed-terminal core
were strengthened, and one explicitly corrective combined self-test was then
run.

Final-hash corrective result: PASS.

After `git diff --check` found one trailing blank line in the task, that
nonsemantic formatting byte was removed and the authenticated task/source pin
chain was refreshed.  A final pin-only combined canary was therefore run on
the hashes listed above; it also passed with the same markers.

- producer marker: nonabelian S3 fixture, 3 Cayley mutations, 6 completed-core
  mutations, all 4 terminal boundaries, and an extra-schema mutation;
- checker marker: independent SymPy S3 order, nonabelian packed Cayley fixture,
  3 Cayley mutations, all 4 terminal boundaries, and 3 schema mutations;
- driver marker: `B345_JOINT_KERNEL_QSTAR_GHA_DRIVER_PASS mode=selftest`.

Static final checks: Python AST PASS, driver ASCII PASS, exact source pins,
no placeholders, and `git diff --check` clean for the authorized files.

## Expected run cost and terminal interpretation

Expected same-job runtime is normally 8–15 minutes and conservatively
20–35 minutes, with RSS around 0.8–1.2 GiB.  A larger regression is a profiling
STOP, not permission to widen caps.

- `B345_JOINT_KERNEL_QSTAR_CLOSED`: exact fixed-prefix whole-joint-kernel
  qstar closure certificate.
- `B345_JOINT_KERNEL_QSTAR_ACTIVE`: a lexicographically first exact defining
  relation has nonzero qstar scalar and is direct/NF replayed.
- resource/input terminals carry no mathematical obstruction or lift claim.

B345_JOINT_KERNEL_QSTAR_CLOSURE_V1_READY_FOR_GHA
