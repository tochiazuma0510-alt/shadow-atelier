# Luna task 340 - task336 finite active-coordinate dual addendum

Role: Luna, binding addendum to active task336.  Read this FULL mail and
`sol/proof_r07_finite_active_coordinate_dual_oracle_v274.md` first to last.
Do not create additional outputs or execute anything; task336's five v4 files
remain the sole permitted edits.

## 1. Required active-universe construction

Build the dual only on the exact finite union of supports of the current B/K
basis and target.  Extend it by zero outside that registry.  Construct it by
back-substitution through the actual reduction projection, not by selecting a
raw remainder coordinate or inventing `__target__`.

The checker must independently reconstruct the active registry and use a
different nullspace/pivot convention.  Both sides prove all-current-row
annihilation and nonzero target pairing.

## 2. Full-D completeness and metering

Complete support inversion against every matching occurrence of all 65 tagged
base rows is the proof over the whole D, even though the dual has finite
support.  A selected translated row may introduce new coordinate keys; add
them before the next reduction.  Never allocate a vector indexed by all E3/E4
elements or claim that the active registry was fixed in advance.

Meter and export per round: active-key count, B+K rank, dual support,
all-row dot products, target dot, correlation-pair count, new-key count, and
the selected column or complete-zero accumulator.  Bind their chronology.

## 3. Added negative controls

Add actual-owner mutations for a deleted active key, unregistered nonzero dual
key, raw-pivot-coordinate functional, omitted matching occurrence, incomplete
translation key, premature zero correlation, and omitted new-key registry
update.  Extend rather than replace task336/task339 mutations on both sides.

The task336 reply must report the active-coordinate algorithm and exact
polynomial/sparse bounds without any factor equal to the full E3/E4 order.

`TASK340_R07_TASK336_FINITE_ACTIVE_DUAL_ADDENDUM`
