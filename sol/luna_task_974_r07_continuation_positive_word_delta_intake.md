# Task974 — 新oracle/E継続prefixの同一語readout差分intake

役: Luna producer側の限定read-only intake。変更可は
`sol/luna_reply_974_r07_continuation_positive_word_delta_intake.md` 一ファイルのみ。
既存971/972/973を含むsource/workflow/返信は全て公刊凍結、不変。
ローカル数値/Python/import/AST/GAP、network/git/credential、追加agentは禁止。
読取・source静的調査・JSON metadata/bytes/hashのみ可。rootが唯一のGHA broker。

実run33984832010/1、launch b8c9e95ddd0183d9e43b7fcc961cb251fdaea13e は
cap1→同output resume32がsuccess、18:56:51Zから独立全prefix checker中。
まだ新rank/target零/terminalは観測していない。受理済みrank1386/gen8091、grade2未決。
本便は実GHAを止めるgateでも、未観測positiveを仮定した結論でもない。

Task958/reply958の既済readoutを繰り返さず、971の新Eとsnapshot ABIを加えた差分を具体化する。
reply970/971、source971、source965、必要な自系source、v518/v547/v548、2144 signed代表を参照。
checker算術を読取/import/コピーしない。実E親はreply971のexact pinsを使う。

求める成果は以下四点。実装は本便では始めず、次の実装単位を直接書ける粒度まで限定する。

1. base/seed30/34/fixed44/旧26段/外部E/新loop全段のtarget係数と挿入順pivotを
   一つのreaderへ結ぶ実file/字段/ID規則。新loop stepとsnapshot番号、completed HEAD、
   hidden pending/HEAD外先行phase、plain targetとrolling instructionの境界を特定する。
   係数0・相殺祖先は保持。新loopのrankやscalar列を推定しない。
2. 各新Eの同じraw-root（signed central factorの順序を含む）から、記録順P1補正、
   physical reductions、一回のscaleを経てnormalized pivot wordを作る正確な式とsource入口。
   六cycle/二etaのsource lift、同じP1 DAG、四Bがone-word ancestryへどう結ぶか示す。
   mod3 leaf collectionをnonabelian語へ置換せず、係数2はliteral inverse。
3. oldfull-originと新Eを混ぜた最終挿入順target wordのreachable typed DAG、
   ancestor offsets/hashes/owner joinを定義。未保存で復元が必要な字段や欠品があれば
   実source行とともに明示。既存sourceを書き換えずに新consumerだけで可能かを判定する。
4. Task958 A/B/C（target reader、ordered compiler、normalized pair）の最小新entry pointと
   公開ABI案、E固有の反例canaryを挙げる。Dの直接11slotは別Task975が担当する。
   当該grade MEMBERに必要な残件、linear零との違い、全A0との違いを最後に明記。

新数値/実走時間の予測、全A0、cross-checked/verifiedの宣言はしない。
返信は実file/式/型と必須欠品を優先して簡潔にし、保存後bytes/hashをrootへ伝える。
最終行 `AUDIT_974_VERDICT:`。
