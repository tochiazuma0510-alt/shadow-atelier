# Task951 -- independently check the v2 parent repair and launch it

Read Task950 for the public repair contract and actual input locations,
Task947, your reply946 and workshop run33963515077/ruling2123 express.
Root alone handles git/network/credentials/GHA. Do not access those, run
local numerical Python/GAP, or spawn further agents. Keep authors separated.

Write ONLY `search/check_d972_r07_fixed_root_packet_loop_v2.py`,
`.github/workflows/d972-r07-fixed-root-packet-loop-v2.yml`, and
`sol/luna_reply_951_r07_packet_parent_layout_checker_v2.md`.
Published v1 and all old lineage files are immutable. Copy your accepted
v1 checker/workflow to v2, updating schema, producer name and exact pins.
The mathematical scope and producer cap1 -> same-owner resume176 -> full
independent checker schedule remain as Task947. No parent changes or local
numerical run, no new full audit requirements.

Independently inspect the actual seed30/seed34/base target JSON identities
and generation-specific flag shapes. The v1 checker did not directly read
the absent seed30 flag. Keep its existing independent rho2/base-target join;
support the explicit v2 legacy-premise receipt if its ABI changes. Reject
wrong/missing v3 premise flag and changed rho2 identity. Do not share new
arithmetic implementations with the producer.

Add a cheap metadata-only `--parent-layout-selftest` using the actual pinned
`--state-root`, `--delta-root`, `--seed34-root`. Exercise the independent
production metadata validation path on the real receipts, emitting PASS
and exact fixture identities, with focused mutated invalid cases. Coordinate
only this CLI/output ABI with Task950; no fake full-size fixture. Workflow
must run both actual-parent metadata checks after downloaded root discovery
and before packet arithmetic, preserving diagnostics of these checks.

Use marker `[r07-fixed-root-packet-loop-v2-run]`, new workflow name and
v2 artifact prefixes. Include only changed source pins after actual file
freeze. Retain all parent live identities, raw data byte pins, phase markers,
internal < outer deadlines and conditional candidate upload. Notify root
when ready for static delta review. Report full SHA/bytes and remaining
runtime/CV-9 gates. cross_checked=false; verified=false.
