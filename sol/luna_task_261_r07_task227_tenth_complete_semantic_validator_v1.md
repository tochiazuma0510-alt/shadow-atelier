# Luna task 261 — task227 tenth complete semantic-validator repair v1

Role: bounded implementation repair only. Read task248, task257, the current
five task227 files, and this rejection in full. Do not run Python, Node, GAP,
git, GHA, or network. Edit only the same five task227 files plus the existing
task227 Luna reply.

## Exact parent rejection after task257

The return still does not meet task248 Section 4 and must not run yet.

1. Producer `validate_encoded_case(..., focus=name)` first compares the
   mutated owner byte-for-byte with an unmutated reference and emits the
   expected gate. This is an owner-equality sentinel, not independent
   reconstruction of the mathematical condition. It makes the subsequent
   complete validation unreachable for every closure mutation.
2. Without that `focus` equality, the producer validator does not reconstruct
   the load-bearing MEMBER data. In particular it does not reconstruct the
   `c_i` row sum, lambda from occurrence ancestry, kappa=(z0-1)lambda, the four
   replay rows/digests, or the final quotient/MEMBER chain. Its NONMEMBER path
   likewise trusts serialized zero-pairing lists instead of recomputing all
   dual pairings/annihilation from the decoded dual and rows.
3. `terminal_vocabulary`, `resource_terminal`, and `forbidden_conclusion`
   mutate keys absent from every baseline encoded case. `set_path` creates
   them only in the mutant, so these are not extant owners. The checks against
   `SELFTEST`, `True`, and `False` are synthetic.
4. Producer/checker ABI validation merely requires an `orientation` key; it
   does not restrict it to the actual task226 vocabulary `direct|inverse`.
   Every H1/H2/P target block must also be parsed and compared with its
   canonical sparse encoding, not just accepted by keyset.

## Required repair

1. Remove the producer `focus`/reference-equality shortcut entirely. The
   complete validator must reject each mutation from independently decoded
   and recomputed semantics. Fixed expected gates remain preregistered; choose
   non-vacuous replacements and validation order so each owner reaches its
   own exact gate without aliasing an earlier unrelated check.
2. Producer must independently reconstruct all of the following from the
   encoded case and embedded ABI:
   - exact w, u0, and target;
   - occurrence basis, ancestry replay, exhausted generator orbit, canonical
     486 ideal rows, and all 729 translates, including both-way span/equality
     conditions commissioned in task248;
   - block images/echelon and target coefficients `c_i`;
   - lambda from `c_i` plus ancestry, kappa=(z0-1)lambda, all four replay rows
     and their digests;
   - MEMBER quotient zero and final target equality;
   - NONMEMBER dual pairings and annihilation against occurrence, 486, 729,
     block rows, with target pairing one.
   Do not substitute equality to an unmutated reference for any item.
3. Checker continues to perform its separate reconstruction and its own 24
   mutations. Audit its natural failure ordering so every fixed expected gate
   is actually the owning gate; no dynamic expected reason and no reference
   equality.
4. Put genuine baseline owner data in each production-shaped SELFTEST case:
   an exact case terminal (`PROJECTED_MEMBER_SEED` or
   `PROJECTED_NONMEMBER_DUAL`), a sealed typed resource-terminal canary with
   cap/phase data, and explicit false downstream conclusion flags. Validate
   them in both producer/checker. Then mutate one extant datum for the final
   three names. Do not invent a field only in the mutant.
5. In both implementations require orientation exactly `direct` or
   `inverse`, and parse plus canonicalize every H1/H2/P target block.
6. Preserve the 24-name roster, actual ancestry object, action
   `p*g*p^-1`, caps, typed UNKNOWN contract, actual production path, and false
   conclusions. Refresh driver/fixture/reply identities. Report UNEXECUTED;
   parent Sol will run GHA only after another whole-file audit.

A3 remains 0/3 until an actual accepted task226 package and production
MEMBER/NONMEMBER exist.
