# Task953 -- bounded intake for the complete grade2 oracle after fixed44

Mail163 retains WO-162-1: continue until the registered grade2 has a real
MEMBER/NONMEMBER decision. The fixed44 run deliberately cannot decide a
negative. While root runs v2, read the already proved v548/v543/v546/v547
and identify the smallest concrete next implementation. Do not delay v2 or
expand its scope. Root will adopt a next scope after its actual terminal.

Write ONLY `sol/luna_reply_953_r07_after_packet_oracle_intake.md`. No code,
local numerical runs, git/network/credentials or further agents. Published
sources and receipts are read-only. This is an implementation intake, not
a new full audit prerequisite or a claim that an oracle is already ready.

Compare two actual routes using existing files:

1. Fresh four-root full-origin scan: v541's accumulate_scalars already
   handles all44 +4*8059 origins with complete actor lower terms. Remove only
   stale lambda/active-character pins in a future version, seal/compare the
   full actor array, and materialize the first actual violation. All-root EOF
   would still need fresh dual closure plus all-origin tests. The old504
   orbit is NOT a current bound or reusable certificate without a new join.
2. v548's complete source cochain: interpolate kappa on the8059 actual joint
   P1 rows; form f=sum q_a Psi2[a]-kappa Psi1 on raw chains using the actual
   six occurrence/Fox/monomial maps, with b_aux on the two normalizers; test
   every54433 chord against the five carry rows and materialize a failed
   <=6-cycle combination via v547. No physical Conn multiplier is needed.

Root has read the v15 source helpers _SeedContext, _seed_affine_fox,
_seed_qnorm, _seed_evaluate_seed, and the v541 actor/accumulator path.
For route2, check whether the existing affine Q2 model and six marked
substitutions really induce a raw-chain adapter on the SAME legal source;
ordinary polynomial/Fourier coefficients can be evaluated per group edge,
but the lower terms, handedness, shared auxiliaries and exact nu must be
typed, not inferred from homogeneous Task712. An abstract extension alone
does not implement that adapter. Actual kappa interpolation must use the
joint lower coordinates, not assume ascending numerical leads.

Identify exact reusable producer/checker lineages, actual fixture shapes,
missing adapters and literal readout consumers. Existing local Task554
prepare is `%TEMP%/task554-prepare-33677346616-1-pinextract` (fixed digest
in existing accepted descriptors). Give a short actionable recommendation
and minimal candidate/negative obligations, without runtime predictions or
silent reduction of the registered universe. Flag genuine missing input;
do not invent new required inputs for the running fixed44 packet.
