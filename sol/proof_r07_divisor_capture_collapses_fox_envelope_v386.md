# R07 divisor capture collapses the Fox envelope v386

Author: Sol / 2026-08-30

Status: paper theorem extracting a weaker and more constructive alternative to
the full retraction requested in v384.  It is enough to prove that the actual
Fox divisors lie in the closed reachable module at every finite coordinate.
Then the error envelope equals the reachable module and the identity map kills
the saturation tower.  The finite membership data and its all-rung uniform
formula remain open.  No lift, fake certificate, or Ihara witness is declared.
\(\mathtt{verified=false}\).

## 1. Coordinatewise capture is global capture

Retain the fixed compatible divisor ABI of v384.  Thus

\[
 L_{\rm reach}\subseteq L_{\rm amb}
   \simeq\varprojlim_iL_{{\rm amb},i}
\tag{1.1}
\]

is closed, \(\mathcal D_{\rm Fox}\subseteq L_{\rm amb}\) is fixed before any
Newton choices, and

\[
 E_{\rm Fox}=
 \overline{\widehat\Xi\cdot
 (L_{\rm reach}\cup\mathcal D_{\rm Fox})}.
\tag{1.2}
\]

Put \(L_i=\pi_i(L_{\rm reach})\).

### Theorem 1.1 (COFINAL DIVISOR CAPTURE)

Assume that every actual Fox divisor is captured at every matched finite
coordinate:

\[
 \boxed{
 \pi_i(d)\in L_i
 \quad
 (d\in\mathcal D_{\rm Fox},\ i\in I).}
\tag{1.3}
\]

Then

\[
 \boxed{
 \mathcal D_{\rm Fox}\subseteq L_{\rm reach},
 \qquad E_{\rm Fox}=L_{\rm reach}.}
\tag{1.4}
\]

Consequently the retraction of v384 is the identity, and for every
\(n\geq0\),

\[
 L_{\rm reach}\cap\widehat J^nE_{\rm Fox}
 =\widehat J^nL_{\rm reach}.
\tag{1.5}
\]

#### Proof

V384 Lemma 2.1 identifies the closed compact submodule with the inverse limit
of its coordinate images:

\[
 L_{\rm reach}\simeq\varprojlim_iL_i.
\tag{1.6}
\]

For fixed \(d\in\mathcal D_{\rm Fox}\), the compatible coordinate family
\((\pi_i(d))_i\) lies in \(\varprojlim_iL_i\) by (1.3).  Therefore (1.6)
places \(d\) itself in \(L_{\rm reach}\).  Since \(L_{\rm reach}\) is a closed
\(\widehat\Xi\)-submodule, (1.2) now gives
\(E_{\rm Fox}\subseteq L_{\rm reach}\); the reverse inclusion is built into
(1.2).  Equation (1.5) is then tautological. \(\square\)

No compatible family of finite preimages is selected in this argument.
Membership of every coordinate image, closedness, and compatibility of the
divisor itself supply the global element automatically.

## 2. It suffices to capture primitive divisor generators

At coordinate \(i\), let \(G_i\subseteq L_{{\rm amb},i}\) be a finite set of
literal word-bearing primitive divisors.  Suppose the fixed Fox formulas give

\[
 \pi_i(\mathcal D_{\rm Fox})
 \subseteq \widehat\Xi_i\cdot G_i
\tag{2.1}
\]

and the reachable image is an \(\widehat\Xi_i\)-submodule.

### Corollary 2.1 (FINITE PRIMITIVE MEMBERSHIP TEST)

If

\[
 \boxed{G_i\subseteq L_i}
\tag{2.2}
\]

at every matched coordinate, then Theorem 1.1 applies.

#### Proof

Equations (2.1)--(2.2) and \(\widehat\Xi_iL_i=L_i\) imply (1.3). \(\square\)

The load-bearing point is (2.1).  One may not infer it from an endpoint rank
alone.  The fixed/moving prefix expansion and every ordered cross-term must be
written as an explicit \(\widehat\Xi_i\)-linear combination of the named
primitive divisors with the literal A.18 occurrence order retained.

## 3. The A4 anchor/outer split gives the primitive roster

At the first elementary-abelian edge, v339 gives a word-bearing split

\[
 K=\langle k_z\rangle\oplus K_0,
 \qquad
 K_0=\langle\ell_1,\ldots,\ell_{t-1}\rangle_{\mathbf F_3},
\tag{3.1}
\]

with source words \(u_z,v_1,\ldots,v_{t-1}\).  The relative augmentation ideal
splits into anchor and field-outer parts.  As a left group-ring ideal it is
generated by the finite primitive differences

\[
 \boxed{
 u_z-1,qquad v_1-1,ldots,v_{t-1}-1.}
\tag{3.2}
\]

Indeed, \(u_z^2-1=(u_z+1)(u_z-1)\), and the ordinary telescoping identity
generates every \(h-1\), \(h\in K_0\), from the \(v_j-1\).  Left translation
by the retained transversal and by the registered occurrence prefixes then
gives v339's full anchor/outer roster.

Let

\[
 \sigma_i:I_i\longrightarrow L_{{\rm amb},i}
\tag{3.3}
\]

be the actual fixed occurrence/Fox divisor map at coordinate \(i\).  It must
be authenticated as \(\widehat\Xi_i\)-linear on the registered source ideal,
not merely as an endpoint map.  Define

\[
 G_i={\sigma_i(u_z-1),
       \sigma_i(v_1-1),\ldots,
       \sigma_i(v_{t-1}-1)\}.
\tag{3.4}
\]

### Theorem 3.1 (ANCHOR/OUTER DIVISOR CAPTURE CRITERION)

Suppose:

1. the fixed A.18 prefix and cross-term formulas factor through the actual
   linear map \(\sigma_i\), so that (2.1) holds for (3.4);
2. each word in (3.2) is a legal registered common-source word with complete
   boundary and occurrence ancestry; and
3. the finite membership test gives \(G_i\subseteq L_i\).

Then every first-edge Fox divisor is intrinsically reachable.  If the same
three statements hold at every cofinal elementary-abelian edge, all those
edges satisfy (1.3), without constructing a finite retraction matrix.

#### Proof

The group-ring generation just proved and assumption 1 give (2.1).
Assumption 3 and Corollary 2.1 give finite divisor capture.  Applying Theorem
1.1 to the compatible all-edge family gives global capture. \(\square\)

The actual score matrix of v333 is one way to prove assumption 3.  Structural
onto is stronger than necessary: for divisor capture it is enough that the
finite set (3.4), or the smaller actual subset occurring in the fixed Fox
factorization, has zero class in the appropriate reachable cokernel.

## 4. All-rung elementary-abelian form

At edge \(m\), let v339's row reduction give

\[
 K_m=S_m\oplus K_m^0,
 \qquad
 S_m=\langle s_{m1},\ldots,s_{ma_m}\rangle,
 \qquad
 K_m^0=\langle\ell_{m1},\ldots,\ell_{mb_m}\rangle,
\tag{4.1}
\]

with literal source words.  The relative augmentation ideal is generated as
a left ideal by exactly

\[
 s_{m1}-1,\ldots,s_{ma_m}-1,
 \ell_{m1}-1,\ldots,\ell_{mb_m}-1.
\tag{4.2}
\]

Thus the unnamed even module is replaced by a finite word-bearing primitive
roster of size \(a_m+b_m=\dim_{mathbf F_3}K_m\) at that edge.  The projected
relative-dihedral component handles the \(s\)-part only under its established
typing/equivariance hypotheses.  The \(\ell\)-part is the actual field-outer
membership obligation.

The useful all-rung statement is therefore not that arbitrary finite
splittings are natural.  It is the uniform implication

\[
 \boxed{
 \text{literal primitive source words}
 \Longrightarrow
 \text{their actual divisor images lie in }L_i.}
\tag{4.3}
\]

If (4.3) is proved symbolically from the common A.18/Fox formula, finite
coordinate membership follows at every refinement and Theorem 1.1 supplies
the inverse-limit result.  If it is only computed at one coordinate, it does
not propagate by itself.

## 5. Relation to the live A0/A4 computations

A4 is the first physical owner needed to instantiate (3.1)--(3.4): it must
return an authenticated word-bearing kernel/action basis or a complete
negative.  Its current durable frontier is `next_row=25`; no such basis is yet
promoted.  Once positive, the same row reduction gives both the anchor and
outer primitive words.

A0 asks whether the fixed target is already in the exact common boundary and
correction span.  A positive A0 word can discharge a named membership in
(3.4), but A0 resource exhaustion cannot establish a missing divisor or a
negative.  The low-memory batched continuation of A0 and the A4 basis search
therefore supply finite inputs to Theorem 3.1; neither computation replaces
the uniform factorization (4.3).

## 6. Exact frontier

~~~text
COORDINATE DIVISOR CAPTURE => GLOBAL CAPTURE:        PAPER PROOF
GLOBAL CAPTURE => E_FOX = L_REACH / SATURATION ZERO: PAPER PROOF
A4 ANCHOR/OUTER PRIMITIVE ROSTER:                    PAPER REDUCTION VIA v339
ACTUAL A.18 DIVISOR FACTORIZATION THROUGH ROSTER:    OPEN
FIRST-EDGE WORD-BEARING A4 BASIS:                    GHA RUNNING
PRIMITIVE DIVISOR MEMBERSHIP / SCORE COKERNEL:       NOT COMPUTED
UNIFORM ALL-RUNG MEMBERSHIP FORMULA:                 OPEN
REGISTERED RELATIVE PRO-3 LIFT:                      CONDITIONAL
FAKE / IHARA WITNESS:                               NOT DECLARED
~~~

\(\mathtt{R07\_DIVISOR\_CAPTURE\_COLLAPSES\_FOX\_ENVELOPE\_V386\_AUDIT\_CANDIDATE}\)
