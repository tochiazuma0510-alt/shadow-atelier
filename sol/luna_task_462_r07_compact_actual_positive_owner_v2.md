# Luna task 462 -- repair compact positive owner onto the actual Task456 engine

Role: Luna implementation only.  Do not run production, dispatch GHA, edit
workflows, commit, push, or touch files outside the four outputs below.

Task460 v1 is rejected and must remain untouched.  Its synthetic
`r<index>:<free-word>` coordinates, inert Task456 constants, empty PB ledger,
and hand-written seed self-loops are not the occurrence-separated pre-C A5
module.  Its normal driver also supplies no target and can never return a
real MEMBER.  Do not repair or reuse that arithmetic.

The governing theorem remains
`sol/proof_r07_compact_k_a5_immediate_positive_dovetail_v419.md`.

## 1. Required versioned outputs

Create only:

1. `search/d972_r07_compact_direct_relator_a5_a6_positive_v2.py`;
2. `crosscheck/check_d972_r07_compact_direct_relator_a5_a6_positive_v2.py`;
3. `search/d972_r07_compact_direct_relator_a5_a6_positive_gha_driver_v2.g`;
4. `sol/luna_reply_462_r07_compact_actual_positive_owner_v2.md`.

## 2. Frozen actual arithmetic

Build exact-byte/hash/cardinality-guarded successors of the adopted Task458
positive-only owners, not a new coordinate model:

```text
zero v6 producer  2342 32cbc1a8e1faea0d4dc7a88a41a2ad3b535e7b2fd94b73ff286d78001262b96c
zero v6 checker   2334 a4db1b2b1ad5da1135c8ebcef1898c46fd07df7ebdbfa8778bd36a6098507bc3
zero v6 driver    2106 212c76f2ca2e06df1aae2b2d783a15fcf1d4e5041d70cba26198d64d9bd4d4d6
```

Their generated mathematical bodies are:

```text
producer 59382 83b31959a0c35bdeb1e2569e0ee384b116ed6ed0b7d57e9c363cecdc29fcfe87
checker  45888 cf44a9a8397eebf99271a4444bb41bd300fe5cfa60cc00696e9811a1469b52c7
```

Retain, unchanged, the actual Task193-v5 receipt/verdict firewall, Task198
AuthorityAdapter/Runtime/BoundaryLedger, DirectEngine occurrence package,
`relator_seed`, marked left-action, target oracle, translated outer PB
boundary, proof DAG, literal M construction, and MEMBER replay.  The actual
pointed target must come only from the authenticated Task193-v5 owner.

## 3. Replace only the relator-row view

Authenticate the Task411 producer and reconstruct its presentation:

```text
search/d972_r07_a0_compact_pc_invariant_owner_v1.py
68222 be17be107103a218123cd0e1eb8455377ca2b52a2e54ec629f3744ad4c2d32f9
```

Require exactly 44 literal words and digest
`7612682d024b61f873928ad122c9a5d7462c812a6633112f08706cda4412b6c8`.

The inherited Task198 AuthorityAdapter must first authenticate its complete
physical receipt exactly as before.  Then pass DirectEngine a read-only proxy
whose `rows` property is the 44 Task411 words while every other attribute,
including `receipt`, `identity`, and `meter`, delegates to the authenticated
Task198 authority.  Runtime and BoundaryLedger remain constructed from the
original authenticated authority.  DirectEngine alone receives the compact
row proxy.  Set its local relator count to 44 by an exact guarded transform;
do not alter the Task198 runtime's own 6,441-row authority validation.

The proxy rows need only the literal `word` field used by
`DirectEngine.relator_seed` and `_member`; preserve a stable 1--44 index and
bind the Task411 source identity/count/digest in every receipt.

Run the inherited actual sequence:

```text
44 actual relator_seed evaluations
actual pre-C and joint echelons
actual target oracle and translated PB-boundary correlation
actual four-letter marked left-action closure
actual positive _member replay and literal M
```

Never introduce `sparse_column`, synthetic coordinate names, a JSON target
argument, a hand-written action edge, or an assumed empty PB ledger.

## 4. Terminal and resource contract

- An inherited actual MEMBER remains MEMBER and retains all Task458 positive
  arithmetic and ancestry, plus the compact-roster owner binding.
- Exhaustion of this legal compact/action span is exactly
  `UNKNOWN_INCOMPLETE:compact_direct_span_exhausted`, with A5/A6/A7/fake/
  Ihara claims NONE or false.  It is not NONMEMBER.
- Resource and input stops remain separately typed.
- Use 14,400 seconds, 5,700,000,000 RSS bytes, and the inherited operation
  cap.  Patch both the parser default and frozen cap assertion exactly.
- This short race is honestly `resumable=false`; do not fabricate a
  checkpoint.  Task459/459a is the parallel resumable complete owner.  The
  driver must never advertise resume or create a fake state file.

## 5. Independent checker

The checker must be an exact successor of the Task458 MEMBER-only checker.
Authenticate and reconstruct the 44 words independently through the pinned
Task411 checker:

```text
crosscheck/check_d972_r07_a0_compact_pc_invariant_owner_v1.py
44831 7c1aea086ce264ad6f51983554a3a371ac481d07a2ec5f5d9a96ee270af6dfcf
```

After the inherited checker authenticates the full Task193/Task198 owners,
give its CheckerModel an analogous read-only 44-row proxy.  Retain the real
checker-side `check_member`, occurrence action, PB boundary, target equality,
and literal M replay.  Require the receipt's compact owner/count/digest and
reject every UNKNOWN/NONMEMBER terminal.  Do not call producer presentation,
column, action, boundary, target, or replay helpers.

## 6. GHA driver and bounded gates

The new direct driver must require the same Task193 receipt/verdict inputs as
Task458, use the inherited Task198 defaults under `ci/in`, exact-pin the v2
producer/checker, run one producer process, and invoke the checker only for
MEMBER.  Accept only these nonpositive terminal classes without a verdict:

```text
UNKNOWN_INCOMPLETE:compact_direct_span_exhausted
UNKNOWN_RESOURCE:*
UNKNOWN_INPUT:*
```

Require the NONE/false claim frontier for every nonpositive receipt.  No
production SELFTEST/FIXTURE or old 6,441-row DirectEngine loop is allowed.

Run locally only repo-external-cache `py_compile`, load-without-main, exact
generated-body hash/cardinality checks, a proxy delegation/immutability toy,
and static driver branch/process/cap scans.  Do not claim a synthetic MEMBER
fixture has tested the actual engine.  Report exact physical/generated pins,
the exact bounded commands, and state explicitly that production runtime is
unknown until GHA.

