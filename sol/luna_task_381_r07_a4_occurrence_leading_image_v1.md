# Luna task 381 - A4 occurrence-leading image compiler v1

> **STOP / SUPERSEDED BY task382.**  Do not implement this commission.
> Its source contract incorrectly promotes the full A4 kernel ideal to the
> legal nonlinear correction domain.  Task382 replaces it with an exact
> residual/common-word source extractor.

Commissioner: Sol / 2026-08-30

Reply to `sol/luna_reply_381_r07_a4_occurrence_leading_image_v1.md`.

Role: bounded mechanical implementation only.  Parent Sol owns the
mathematics, git, network, GHA dispatch and production promotion.  Read this
commission and the following files in full before editing:

```text
sol/proof_r07_a4_occurrence_leading_image_compiler_v358.md
sol/proof_r07_actual_a5_three_input_slice_compiler_v242.md
search/d972_r07_word_independent_successor_kernel_v12.py
crosscheck/check_d972_r07_word_independent_successor_kernel_v14.py
search/d972_r07_typed_single_seed_endpoint_consumer_v2.py
crosscheck/check_d972_r07_typed_single_seed_endpoint_consumer_v2.py
```

Edit only these four new delivery files:

```text
search/d972_r07_a4_occurrence_leading_image_v1.py
crosscheck/check_d972_r07_a4_occurrence_leading_image_v1.py
search/d972_r07_a4_occurrence_leading_image_gha_driver_v1.g
sol/luna_reply_381_r07_a4_occurrence_leading_image_v1.md
```

Do not edit a predecessor, workflow, proof, v220, ledger, cert, fixture, or
input.  Do not run production, GHA, git, network, SELFTEST, or a mutation
campaign.  Static Python compile, ASCII scan, frozen-owner restoration and
GAP `ReadAsFunction` parsing are allowed.  Keep the implementation
production-first and small; do not add general frameworks or audit-only hot
paths.

## 1. Exact mathematical universe

The only source is a future physical positive A4 receipt plus its independent
checker verdict from the current v12/v14 owner family.  Authenticate the
receipt terminal, all three A4 milestones, its authority identities, complete
`kernel.K_roster`, queue exhaustion, four action matrices, literal words and
the independent accepted verdict.  Never accept SELFTEST or a same-schema
synthetic object.

The v1 target is the **complete raw eleven-occurrence leading module before
printed block summation and before formation/Brunnian localization**.  Every
occurrence retains its block, ordinal, sign, orientation, prefix, PB3/PB4
type and coordinate key.  Do not call this `Q_loc`, `L/JL`, a lift, or a
witness.

Use the actual task198 occurrence ledger and the actual first-successor
coordinate arithmetic already frozen in the A4 owner.  For each A4 basis word
`u_i`, reconstruct the two roof-fibre values of `u_i-1` and compute the
literal roof-fixed occurrence column `Bhat(k_i-1)`.  Prefix conjugations,
inverse occurrences and signs must be applied occurrencewise.  A producer
field copied from the future A4 receipt is not a substitute for direct word
evaluation.

If the frozen owners do not contain enough information to define this column
without guessing an ABI, fail closed at `UNKNOWN_INPUT` and state the exact
missing physical field in the reply.  Do not invent an identity prefix,
identify ten contexts with eleven occurrences by position alone, or treat
the A4 kernel row itself as the Jacobian column without deriving that
equality.

## 2. Complete image closure

Seed a sparse echelon with every nonzero `Bhat(k_i-1)`.  Close rank-raising
rows under the simultaneous marked actions in this fixed producer order:

```text
x, x^-1, y, y^-1
```

Every retained row must carry a literal coefficient ancestry

```text
sum a * g * (k_i - 1)
```

and replay to the occurrence row.  Queue exhaustion is the only complete
terminal; a word radius or sampled prefix is `UNKNOWN_RESOURCE`.

Predeclare and reconstruct the complete raw target basis independently of
the image.  Return exactly one mathematical result after queue exhaustion:

```text
RAW_OCCURRENCE_ONTO
RAW_OCCURRENCE_COKERNEL
```

ONTO requires a literal preimage for every raw target basis vector and a
direct replay.  COKERNEL requires a full nonzero dual functional annihilating
every terminal image row and pairing nontrivially with a named raw target
basis vector.  The receipt must explicitly state that raw COKERNEL does not
imply localized non-surjectivity.

Use honest `UNKNOWN_INPUT` and `UNKNOWN_RESOURCE` terminals.  Add a compact
atomic checkpoint/resume only at natural row/queue boundaries, with exact
input identities and literal ancestry; do not serialize process-local group
objects.  No elaborate mutation harness or SELFTEST path is requested.

## 3. Independent checker

The checker must not import the new producer.  It may import only frozen
predecessor arithmetic after authenticating exact byte/SHA pins.  Rebuild:

1. every A4 word and roof-identity value;
2. every raw eleven-occurrence seed with signs/prefixes/orientations;
3. the invariant closure in action order `y^-1, y, x^-1, x`;
4. two-way span equality with the producer closure;
5. every producer literal ancestry; and
6. either all raw-basis preimages or the complete cokernel functional.

Independent echelon basis lists need not be literally equal; compare their
spans both ways.  The checker must print one terminal line and return an
accepted verdict only for a fully replayed mathematical terminal or an
honestly typed UNKNOWN receipt.

## 4. Driver and delivery

The GAP driver must expose progress from the producer and checker, enforce
one terminal line from each, preserve the receipt/verdict/checkpoint in
`ci/out`, and use exact frozen source pins.  Leave future A4 receipt/verdict
byte pins explicit and undispatched if the physical production artifact is
not yet available; do not fabricate them.

Record exact byte/SHA identities, static commands, reusable predecessor
functions, resource caps, terminal vocabulary, and any missing input in the
reply.  End with:

```text
A4 PRIMITIVE OCCURRENCE COMPILER:                 IMPLEMENTED OR EXACTLY BLOCKED
RAW ELEVEN-OCCURRENCE IMAGE TERMINAL:             NOT EXECUTED
LOCALIZED Q_loc = L/JL IDENTIFICATION:            NOT CLAIMED
COMPATIBLE LIFT / FAKE / IHARA WITNESS:           NOT DECLARED
```

`TASK381_A4_OCCURRENCE_LEADING_IMAGE_COMMISSIONED`
