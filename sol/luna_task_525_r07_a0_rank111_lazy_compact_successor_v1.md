# Luna task 525 -- rank111 lazy compact-seed A0 successor

Role: Luna implementation support.  Implement the audited v433 positive
selector against the **current rank111 task445 ladder ABI**.  Do not run full
production, dispatch GHA, mutate git/releases/workflows, or edit any existing
file.  Write only these four versioned outputs:

1. `search/d972_r07_a0_actual_tau_free_lazy_compact_seed_v4.py`
2. `crosscheck/check_d972_r07_a0_actual_tau_free_lazy_compact_seed_v8.py`
3. `search/d972_r07_a0_actual_tau_free_lazy_rank111_resume_gha_driver_v12.g`
4. `sol/luna_reply_525_r07_a0_rank111_lazy_compact_successor_v1.md`

## 1. Mathematical and implementation owners

Read and implement the complete contract in:

- corrected paper v433: 10495 /
  `3a8b5085e3a0a712dfd32c246cf472ca16616a2e3d7af494e4fcc8b30d02d940`;
- final Task524 audit: 13304 /
  `3b028e05ac74310a2001494e0d112d0ab389bee82b83c5b8ed7cb84a91c39af5`;
- current state/physical owner v3: 12215 /
  `0140447110cfa568a77300bd7d43d51c622597704b7b91ed5209c63168c9ef37`;
- current independent checker v7: 3653 /
  `e1b80c586985f5113b300508f6bc78d055a37243e3fd6795b8f81148b0988de1`.

For code patterns only, not state ownership, inspect:

- old formula-first compact owner v3: 6254 /
  `657f12e4c7f52dd8012e55a7e775a518c532f1d2b0e4735f88a9adfd7fb9e01c`;
- rank99 K-nonzero producer v7/v6:
  `4911/a66526af...bee22`, `14329/3173c9d9...3d90c`;
- rank99 independent checker v7/v6:
  `9067/8de4f573...c14f8`, `12191/2f579f81...e91da`;
- v431 paper: 9592 /
  `7b08f2526b00f4b12e67b9de57e03b7e87936050bfe8c3f9200130ed1ef850a4`.

Do **not** resume, retype, or import the old Task416 rank60258/
`D972-A0-LAZY-CP2` 129-MB checkpoint.  That is a different physical ambient
and the old occurrence/boundary lane.  It may supply a local algorithmic
pattern only.  The sole live state owner here is the task445 rank111 ladder.

## 2. Exact legacy input and new state identity

The driver must derive from current rank111 transport driver v11 and use the
same permanent release:

`https://github.com/tochiazuma0510-alt/shadow-atelier/releases/download/archive-gha-checkpoints/artifact_9826862037_gap-run-out.a0-rank111.zip`

ZIP: 37586 /
`8b740dbbc81f5d2e659371a81453ded56c6711ce8ace35a4af5255303e0095de`.
Retain and pin its exact eight-member flat manifest from v11.  The resume
input is member 5,
`d972_r07_a0_actual_tau_free_rank_ladder_v10_output.checkpoint`, 85934 /
`69a7ec3da4907f24af0f68c1975538b9ff9b6102f14e334f7c0725d2542dfd93`,
not member 1.

The producer must independently authenticate the legacy v3 schema, binding,
seal, 68-source prefix and replayed rank/round/dual/remainder.  Require before
the first new insertion:

- accepted/rank/round = `68/111/73`;
- dual digest
  `56ccd1f3cc6b54fe340a69ce6a0ec99f5aeb3358ae80288c6b11c3f1ec664864`;
- remainder digest
  `9eed8114d9e3172c7a11153d9c5cd6e5fc2e5184a8d6e3681cce5c82a83b4326`.

Migrate to a new v4 schema, binding and state seal.  Preserve the 68 legacy
accepted-source records exactly.  Future resumes must accept a fully checked
new v4 checkpoint as well as this one exact legacy input.  Every successful
rise is durably checkpointed before another round; a later RESOURCE terminal
must retain all earlier rises.

## 3. Positive lazy iterator

Preserve the current six-action search first.  If it returns a hit, compile
zero formulas.  Otherwise compute one current tau-free adjoint and process
the exact 44 compact seeds in printed order, one at a time:

- compile only that seed's complete occurrence formula and normalized
  epsilon/18 constant;
- never build a future all-seed coordinate union or eager un-conjugated
  identity row;
- skip an unsupported seed only as a whole and only as positive-incomplete
  work;
- for `K=0`, use the complete canonical support-fibre/kernel-order-nine
  schedule in the frozen order;
- for `K!=0`, port v431's distinct global cursors `0..W`, direct ten-
  coordinate literal reconstruction and recomputed `W`, with a disjoint typed
  cursor.  Do not call the rank99 batch owner as the new state owner;
- on the first candidate scalar, reconstruct literal conjugate, `replay_atom`
  row and fresh `seed_v12` row, require equality, exact exponent/N typing,
  no forbidden `E`, direct nonzero dual pairing, nonzero nonmutating
  remainder and predicted pivot;
- insert exactly one row, require exactly one rank rise/pivot, update exactly
  once, checkpoint, and restart at seed 1 under the new dual.

No finite no-hit/unsupported/resource route is negative.  It returns
`UNKNOWN_RESOURCE` with all claims false.  A literal/replay/seal/formula-
physical invariant mismatch fails closed as `UNKNOWN` or input rejection,
also with all claims false.  `COMMON_CANDIDATE` remains candidate until the
new checker independently accepts the complete replay.

Record typed selector provenance sufficient for independent replay:
seed/formula digest, K, required coordinates, support or global cursor,
recomputed W/qid/gid where applicable, literal delta, exact exponent pair,
direct scalar, row/pivot and pre/post dual/remainder digests.  Also record
diagnostic counters for formulas compiled, un-conjugated identity replays,
seeds touched, K0/K-nonzero candidates and add/update counts.

## 4. Independent checker

The v8 checker may pin lower arithmetic/runtime owners but must not import a
producer lazy-selector validator or trust producer row/scalar/cursor data.
It must independently:

- authenticate/migrate/replay the exact legacy prefix;
- reconstruct each new formula, normalized K, support/global schedule and
  typed cursor;
- reconstruct the literal word, all coordinates, physical row, direct
  pairing, exponent/N/E gates, pivot/rank and post-state;
- recompute W, global distinctness and qid/gid for K-nonzero;
- enforce one insert/update per fresh dual and the action-first boundary;
- check result/checkpoint equality, new seal/binding, terminal cardinality,
  and the exact false claim boundary unless a replayed COMMON_CANDIDATE is
  present.

## 5. Driver/resource boundary

V12 must retain v11's actual permanent-release download, exact eight-member
manifest, stale-output gates, `set -euo pipefail`, `umask 077`, source pins,
copy of member 5, `ulimit -v 5200000`, one producer and one checker.  Use the
new producer/checker, fresh v12 output names, and a new external boolean
preamble.  Preserve:

- producer `--seconds 7200 --rss-bytes 4800000000 --max-rises 64` under the
  7500-second outer timeout;
- checker 3600-second timeout;
- `ci/out` outputs and one exact final marker.

Do not run SELFTEST before production in the driver.

## 6. Required bounded tests

Implement the full Task524 mutation contract.  At minimum execute and report
separate live-route tests for:

1. roster 43/45/6,441 substitutions;
2. seed1 hit touches no seed2, compiles one formula, does zero eager identity
   replays and exactly one add/update;
3. K0 empty/zero/duplicate/no-hit plus both unsupported-order cases;
4. direct-pair, row/fresh, exponent/N/E, dependent row, pivot and stale-dual
   second-insert mutations;
5. K-nonzero zero-before-hit, hit-at-W, all-zero, W/cursor/qid/gid/ten-
   coordinate/distinctness/nonfresh/multirow mutations;
6. action hit with zero formula work;
7. time/RSS before seed1, mid-K0 and mid-global schedules with durable false
   claims; and
8. individual legacy prefix/count/rank/round/binding/seal/pivot/pre/post-
   digest mutations plus coordinated producer row/scalar mutation rejected by
   checker reconstruction.

Also require Python compile, producer/checker fixtures, GAP parse, generated
shell `bash -n`, exact eight-member transport fixture selecting member 5, and
a v11-to-v12 diff classification.  Use repository-external temporary files.
Do not perform the actual rank111 replay or production locally.

Reply with exact files, bytes/SHA-256, test evidence, known limitations and
one verdict: `READY_FOR_INDEPENDENT_AUDIT` or `STOP`.  No implementation
result changes the v220 numerator.
