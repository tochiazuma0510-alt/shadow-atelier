# Task954 -- bounded full-origin refinement after the actual fixed44 terminal

Read reply953 FULL, Tasks947/950 and reply950. WO-162-1 remains active.
Root adopts reply953's first route now: a fresh full-origin scan and correct
selected materializer, with a small durable loop. This is the next registered
universe, not a change to the published fixed44 run. No local numerical
Python/GAP, network, credentials, git, dispatch or further agents. Root alone
is the release broker. Coordinate only public ABI with Task955.

Write ONLY `search/d972_r07_full_origin_refinement_v1.py` and
`sol/luna_reply_954_r07_full_origin_refinement.md`. All published files remain
immutable. Use only your own accepted arithmetic lineage. No monkeypatch.

## Actual immutable start

Fixed44 v2 run33964709359/1, source/launch commit
fff114c41bd8748ad0e708919fe0820335c9cce8, workflow
.github/workflows/d972-r07-fixed-root-packet-loop-v2.yml, completed success.
Candidate artifact9969090590, name
d972-r07-fixed-root-packet-loop-v2-candidate-33964709359-1,
1855391 bytes, ZIP SHA256 b15b07150d23a1a291fff387f23c8c13cf3ab5ada2b5f95f2a886b0bdf44a428.
Actual local root: %TEMP%/shadow-atelier-task950-run33964709359-candidate-a1.
Producer cap1 and actual same-owner resume committed seed35/36/37; checker
independently rebuilt the whole packet and three new steps. Workshop CV-9
is pending at this task's issue; do not self-declare cross-checked.

Rank1359, generation8064, state head
7b7380a7ddb785910347df14f47ba4634cc5fa2fff7c32b722455a824d6cddda;
lambda SHA60ac649575400e98881c5de5d4ef2c6202d3cf577da1411042104254edb004e2;
target remainder SHA0a466426db600e191e9ee5563066dbb729492ab74d869dbf0ceeadc2b2f7f686;
next free1424/value2. Terminal ROOT_SEEDS_ZERO. q0 support2781 / SHA
f192e3a9c68a6dd555b591462ec7dd506ebf1d2f6005862ce31b7e611ae072bf;
q1/q2/q3 zero. These are start observations only, not future assertions.

Exact small entry pins (file bytes SHA256):
- output/HEAD 709 c48e8f673b7da860b57b0d413a3f49e2035831ecabd4f790f964e6ba1a2f2fc2
- output/result.json 4493 4cc9c95ac57db62de48095360e9f63056281176931f27ac184d2534a1d78d03b
- checker-result.json 4603 b8308d60ca9332a02d2ca503753e7c72db54d6509c62b28a9aee648f44a2ca60
- source-receipt.json 2037 513eea26bf5cf3288aaba3caaf4f8ca9095857d1af49154d4d7d69b23bd63886
- output/start.json 5025 041d9dc7abcbddc81df490a2ab77acb40715c9bb0d7ca3a004d498d9efd00d6d
- output/owner.json 8274 a8d206b0ae26f3bfcf102de2119f10bd7151d9dd9c6294d8e879656f1ced6f41
- output/packet/manifest.json 843 d5e3ef0c0d691131b6bd1293d066d6e994c572086dc0c89a6e5ec766a8474199

Read the actual step manifests/result/instruction schemas before implementing
the start adapter. Follow HEAD through exactly three complete steps and their
payload pins; distinguish embedded seal from SHA of complete JSON bytes.
Keep Task904 base +seed30 +seed34 +three packet steps as named immutable
derivation premises. Load their rows/targets and directly check current
lambda against all1359 rows and authenticated target. Do not rebuild old
Conn/P1 closure or replay old insert/target arithmetic. M3-1 stays explicit
DERIVED, including the three additional accepted target-delta identities.

## Registered full-origin scan, recomputed for each current lambda

For all4 characters derive fresh q=B*lambda and four homogeneous children
from actual Task712. Reuse the fixed44 packet for seed values (these may
become nonzero again after another pivot). For actors use the v541 COMPLETE
formula K_t b_i +T2,t z_i minus canonical ActRed top terms. Reuse the
correct own accumulate_scalars/actor_adjoint/lower-blob primitives, with
a new dynamic P1 contraction driver that has no old lambda/active-character
assertions. One P1 cache pass per full scan, buffered chunks, no full decoded
8059-by-lift matrix. Keep one Task554 body live at a time.

The ordered universe per character is seed0..43, then basis_i0..8058 times
the accepted four-actor order. Seal ALL seed44 and actor8059-by4 values for
ALL characters, with actual pairing counts and structural-zero skips stated.
The independent checker recomputes and compares these arrays, not only the
first-hit prefix. Select the first nonzero in character-major/origin order.
An all-root full-origin zero result is ROOT_ORIGINS_ZERO / grade2 NOT_DECIDED:
complete current dual closures and their full-origin tests remain unexecuted.
Never reuse the historical504 bound without a fresh closure/containment proof.

## Correct materialization and bounded changing-state loop

For a seed hit use its immutable packet row/ancestry. For an actor hit build
the COMPLETE filtered source actor on selected canonical(b_i,z_i), subtract
the exact globally combined ActRed lifts, and require all96776 lower
coordinates zero before plain character slice and B. Preserve raw source
order and cancelled literal references before numeric folding. Join the
selected scalar, full defect bytes, physical row and actual P1 ancestry.
The legacy generic materializer_v2 lacks K_t b and is NOT an arithmetic
consumer for this purpose. Read the literal conjugation orientation from
the actual accepted source/DAG ABI; do not guess it.

Then insertion-order physical reduction, one normalized append, one target
step, fresh lambda, final all-row sweep and both direct target dots as v2.
The result contains ordered literal/P1 references suitable for later complete
word replay. Full-word/PB4/other grades are not newly closed by a new pivot.

Implement a small same-owner durable prefix: immutable start/pins, sealed
scan and complete new step before HEAD, no large parent copies. Resume loads
the complete prefix without redoing completed scans/inserts. Checker replays
the newly committed scans/steps from the accepted start. CLI max-appends
default32, hard operational cap32; this is NOT a total mathematical bound.
Root workflow runs cap1 then actual resume cap32, with internal1800s and
outer40min per phase. Emit honest UNKNOWN_CAP/UNKNOWN_RESOURCE with complete
prefix; scan after the last permitted append. MEMBER_CANDIDATE still retains
the unfinished literal completion gates. Do not silently expand to dual BFS
or cochain/tree inside this implementation.

Focused canaries must use actual parent JSON layout and an actor path with
nonzero lower-to-top contribution, plus the existing changed-character/
durable-resume interface where affected. No broad old success suites.
Report exact source/CLI/schema/TCB and pending runtime gates, with
cross_checked=false; verified=false. Complete and freeze for root's GHA run.
