# Task1115 — 冷保存・現算術閉包の公開契約草案

宛先: 既存 packet_bounds_audit / Luna。1111を凍結したまま、以下のSol数学裁定を実装前の公開wire候補へ具体化する。指定返信 sol/luna_reply_1115_r07_cold_closure_public_contract_v1.md、末行 TASK1115_VERDICT:。補助資料は新TEMP/shadow-atelier-audit163/task1115/。新agent/Git/GHA/network/credential、Python/AST/import/compile/数学/自己試験実行、既存process操作は禁止。repo変更は指定返信だけ。P/C私的source/設計表/返信を読まない。これから作る公開ABI/型表/反例表は共有可能な数学契約で、相手の実装を含めない。

## 固定範囲とSolの設計判断

2227–2229は冷保存の設計委嘱である。現v6=18親/k128/同caps/計器は静的採択済み、全typed受領中で未実行。この便はv6の追加待ち条件ではなく、source/WF/親集合/実GHA変更0。公開出発例は受理済みrun34161493396/1/heada5b456a973f8a917f3af386d327061a02a0cf900、1834/8539、限定7、元97=32五key+65六key、後続384十key。次の具体変換元headは実際の受理済み状態から後着指定する。v6全128採用や1962を仮定しない。

1. 初期の公開候補は保守的な相対鎖をactiveに残す。全現在行/順序/offer/lead/target/lambda、必要な算術資産/P1/maps/native辞書は実bytes。元97の文法/順序/零と元前提は保持。後続の各native instruction全body・target JSON・row-manifest・元root/segment境界と必要な前後targetをactiveに置き、全登録相対式とrolling各リンクを実照合する。元のmulti-row更新をone-rowにcastしない。不要と証明していないTCB内fileはactive保持。
2. 過去の候補六相/literal/reduction/運用・fixture・checkpointの再読のうち、上の現命題に使わないbodyだけを引用候補とする。同じfileの別用途は併記する。過去λ1450/1578/1706の別pairingはACCEPTED_CITED候補、現最新親λによる全行/二targetと新候補・新λは実再導出。異なるλの問いを一つに数えない。
3. CURRENT_REDERIVED / ACCEPTED_CITED / OPEN_PREMISEを分離。出所pin・既採用判断の具体条項・元P/C/source/runtime/入力閉包と限定が揃うまで引用項目は未採択。元word/rho2・lower-zero・positive/A0の未決は残す。cold pinや保存先存在だけで真な命題を作らない。
4. native context-qualified logical occurrenceと物理blob共有を分ける。全旧ordinary file/dir/空dirに復元可能な写像を保持し、実今回active保全とcold可用性を別flagにする。未読の旧全file不変をtrueにしない。coreと実execution acceptanceを別にsealし、自己hash循環/新wrapperを元hとする偽装を排除する。
5. 1111の数学精密化を採る。Γを保持する有限・整礎typed operand DAG、順序付き子/重複/整数指数、三索引(i, accepted local j, candidate c_i(j))、global1450+Σprior a_i+j、D位置97+Σprior a_i+j、実a_i/p_iと採用0層、record-wise原bytes、各元rolling family/body/除外字段、foreign rootを明記。Σ式だけから順序や語を復元しない。hashの数学的単射性を主張しない。現runVの包含と既採用引用は別。
6. cold availabilityは未裁定TCB。初回はP変換器と独立Cが全原物→全出力・全復元を照合する。各runの可用性には、(A)全cold rawを実再hashする強い参照基準、(B)既全復元票＋immutable保存世代/全object存在・長さ等を信頼する新TCBの提案、を別policyとして具体化する。Bを既承認/現在のfull rehashとは呼ばず、必要証拠欠品はUNKNOWN_INPUT_MISSING、評価不能はOPEN、不正はREJECTEDとする。
7. H1 blob共有/H2旧再読の引用化/H3rank長factor codecは別変更。初期wireはH2のみを主眼にし、H3非採用。旧密な係数/因子の零・順序を変換時に失わない。k/capsは今回変えず、性能/成功run数は未測定。GHAはユーザーが反復利用を包括認可済み。宇宙と実source/pin/capsを事前登録するが、各試行の新たなユーザー許可を必須にする規則を足さない。親/ABI/WFの具体変更は既通知・裁定経路を守る。

## 納品

- 実行可能sourceではなく、新namespaceの公開仕様書と厳密な型表を作る。core manifest / execution acceptance / native context / segment / row occurrence+slices / active-cold inventories / full empty-directory map / accepted claim / unresolved premise / source-TCB / conversion receipt / current report / availability policyの全key・型・nullable・順序・root/path参照先を列挙する。既native JSONは再整形せず、そのschemaを参照する。共通D/H/ordinary整数/型tagを明示し、未定値はパラメータ、欠けた仕様はOPENとする。未採用設計schemaを実出力schemaやverifiedへ混ぜない。
- 全consumerには型object→field→参照対象→必要判定の表を作る。動的field/alias/別context・shared TCBを明記。自分は外側public契約著者で、P/Cの実装可能性や全内部file閉包を独立確認済みとはしない。
- 一回変換とper-runの判定条件を分けた公開義務表。元source/layoutを含む入力、再導出・引用・未決の命題、expected countsの式(未来の実数値ではない)、partial/DEPENDENT/LINEAR/採用0/欠品の枝を全列挙。
- 必要な正例/単一変異/目的拒否を事前登録する。零隣接入替え、段併合、異層local0、三索引ずれ、origin辞書差替え、循環、path二重割当、trit/EOF、instruction再seal、異なる旧λの代用、cold欠品、旧旗の偽昇格を含む。実fixtureは作らず、目的の通常helperへ到達する要件だけを示す。
- versioned材料目録と自己再読。短い指定返信にroot未裁定点を明記。Task1116/1117は別人の自系閉包調査であり、本便から読まない。rootが後で対照する。
