Task1191d C author Helmholtz — 第9公開 consumer の静的納品。

F1. 指定の1部品を完成し、Noether に source・ABI・全差分・公開固定値を先行共有した。C 数学最終 source と既存1191返信は不変。対象の実行/import/AST/compile/selftest、fixture生成、P private/source/diff/fixture/table、Git/GHA/network/credentials/新agent はすべて0。以下の native0 は有限 bytes/hash/JSON メタデータ処理のみであり、対象実行結果ではない。actual_runtime_result と actual_fixture_reception_result は null。

F2. 公開 source は C-ninth-public-driver-consumer-fragment-v1.py、6988 B / 84a46fb30fa4a5ff6798ccc9143c3c27c6fa6ecfe4bd5e04bb9065e5b96f804e。全114LF、CR0、ASCII、3連続区間。旧公開第8 consumer5495/7eb2593b…から17変更を全EOF正逆一致させた。Noether が追加する新第9部品であり、既存第8部品と数学 source は不変。

F3. checker_parent2346_contract_fixture_gate(entry) は exact2 root/selftest を受け、(observed_inventory,20 observation_rows) の tuple2 を返す。root は実 parent2346 directory の Path。selftest は現行 schema/PASS/list9 を確認し、tests[8] exact3 を全値比較する。上位 gate は current selftest whole11・旧8群を含む9群117拒否・fixture_scope・79 interfaces・actual C source D3/full registry を比較する責務を保持する。Noether との分担は明示確認済みで、root/Noether の全driver採用を先取りしない。

F4. 第9内は20件の positive/negative 外wrapper・全拒否exact4・case ledger exact9/各entry exact6・2 sealed JSON support・16-byte ASCII supportを全保存値と比較する。64 files、23 directories、登録empty directory1箇所、初回/終了scan全一致を要求する。各negativeは公開positiveの唯一変異で、expected_beforeを普通型までcanonical比較し、addは不存在必須、reseal前にcanonical no-opを拒否する。false→integer0をPythonの値等価で潰さない。保存不足・不一致・UNKNOWN/partialを成功値へ補完する分岐はない。

F5. Noether の実公開 wrapper1396/c5de0013…は fixture_root(label)/parent2346 と whole selftest を exact2 に渡す。公開 current_v10_final_cost_functions_v1.py100462/4436f188… のL134..147、L508..519で第8→第9の通常/終了 caller 順を照合した。終了は write=False で全保存値/D3を再比較する。全driver final pin/current carrier の最終結合は後続Noether/rootの仕事として null を保持する。

F6. 上位用 public-C10-selftest-fixed9-writer-literals-v1.json の /fixed_values が exact9。/selftest_schema と合わせた whole11 の seal 比較へ使える。実 C writer L11009 の連結文字列、L11010..11030 の79 interfaces、L11031..11032 のordinary固定値を bytes/JSONだけで抽出した。旧8群97拒否名は自己C9公開値と同一のまま、現Cの公開label/有限prefix生成のraw位置へ全対応し、第9の20件を現公契約へ結んだ。PASS等は writer が保存する場合の固定比較値で、実自己検査成功を示す票ではない。

F7. case10はsynthetic observed inventory arraysとその保存empty dir、case12/15はnative73 header/schema/sealだけ、case16はpath-only synthetic descriptor、case17/18/19はcurrent observation exact7 headerだけの責務。full2346 arithmetic/admission、実P/C kernel再実行、whole nested observation の肯定を追加しない。観測rowのtrueは保存値・目的labelとの一致を指し、ordinary_helper_reexecuted=falseを保つ。

F8. 自己全文別読は基点と共通helper b86457、現source5ac25d/cda505、20公契約0ad4e1、実wrapper d02546/登録とcaller f0a758、全raw patch/ABI意味32b0aa。有限metadataは3f1453/native0、55d5d5/native0。所見なし。Rootの独立採用とNoetherの全組立は別票で閉じる。

公開入力全15 pinは ABI票 /inputs と自己別読票に保存した。名称Pで始まる2 utility片も、Noetherが公開 require/canonical/read/patch 等だけと明示した共通metadata断片であり、P privateではない。P mathematical source/tableは読んでいない。

材料はすべて C:/Users/81905/AppData/Local/Temp/shadow-atelier-audit163/task1191d/C/ 以下。

| Material | bytes | sha256 |
| --- | ---: | --- |
| C-ninth-public-driver-consumer-fragment-v1.py | 6988 | 84a46fb30fa4a5ff6798ccc9143c3c27c6fa6ecfe4bd5e04bb9065e5b96f804e |
| baseline-to-ninth-full-forward-reverse-v1.json | 17182 | c79d0f5953bbc17dd449ae120df17a5c91616ce15c0f62159518a7108abae497 |
| baseline-to-ninth-full-raw-diff-v1.patch | 8270 | 076c10555520981af56b136f6fb656fb7b61b3051c0ebc798bb1728eaf5c7666 |
| export-selftest-fixed9-metadata-v1.py | 8331 | fd5a4ac6f3e865a3c179ca5758f9758ba77d00ba51e73ad960f318902c757a02 |
| fix-public-component-metadata-v1.py | 15920 | 739a17912c6fc1555d6dc9cc6959bb37e115d4fbb70bdd6f2bcd3491a9f2e1df |
| public-C10-final-EOL-v1.json | 508 | de3a20617e45a1faea07a517eb58bc84c6a0e08a26305f23e319a2695540ec29 |
| public-C10-selftest-fixed9-writer-literals-v1.json | 66353 | d47dbcb95f62ba0e9bec2e471f472c6c9fe8fa34a8bcf289c7a0d8db8613d76e |
| public-ninth-consumer-ABI-and-source-connections-v1.json | 36972 | 48e9466fc4ef720faa34f96453e2b8d4f7fc577ac3203c7c10cd8037e4e25bf0 |
| public-ninth-consumer-author-static-review-v1.json | 11666 | 3fdeab580284c21acb575a868f0f2424de81f0f617944eef3a5e9e5d3af1b708 |
| reply-public-prose-v1.md | 4322 | 6b9d174e011b8fc53962044fc316ae1affef9e8efef2a4093d50e32af6913ffc |
| reply-UTF8-prose-correction-v1.json | 1393 | 76f41d3bcbca7b13a77afa48365ed133f77a46a93b7b4c287bff63118b58881d |
| public-ninth-consumer-material-manifest-v2.json | 3954 | 57a79d0c2bc0c8cdac01050945366f328efcb9d888ade55ab2eebd9ce7528c1b |

Complete public source, exactly6988 bytes including its final LF:

```python
def v10_ninth_patch(value,operations):
    mapped=[]
    for operation in operations:
        row=copy.deepcopy(operation)
        if row['op'] == 'add':
            require(row.pop('key_must_be_absent',None) is True and 'require_absent' not in row,
                'C-ninth-public-add-key-must-be-absent')
            row['require_absent']=True
        mapped.append(row)
    return v8_seventh_patch(value,mapped)


def v10_c_ninth_values():
    def expand(value,variables):
        if type(value) is list:
            return [expand(item,variables) for item in value]
        if type(value) is not dict:
            return value
        operations = [key for key in value if key.startswith('$')]
        if not operations:
            return {key:expand(item,variables) for key,item in value.items()}
        require(len(value) == len(operations) == 1,'C-ninth-single-reserved-expression')
        operation = operations[0];item = value[operation]
        if operation == '$var':
            return variables[item]
        if operation == '$decimal':
            result = expand(item,variables);ordinary(result,0)
            return str(result)
        if operation == '$concat':
            parts = expand(item,variables);require(all(type(part) is str for part in parts),'C-ninth-string-parts')
            return ''.join(parts)
        require(operation == '$mod','C-ninth-finite-expression-domain')
        left,right = expand(item,variables);ordinary(left,0);ordinary(right,1)
        return left % right
    values = {}
    for name,row in C_NINTH_SERIALIZER['value_definitions'].items():
        if row['kind'] == 'literal':
            value = copy.deepcopy(row['value'])
        elif row['kind'] == 'seal':
            value = v7_sixth_seal(copy.deepcopy(row['unsigned_value']))
        else:
            require(row['kind'] == 'array_template','C-ninth-finite-definition-kind')
            ordinary(row['length'],0,993)
            value = []
            for part in row['parts']:
                ordinary(part['start'],0,993);ordinary(part['stop'],part['start'],993);ordinary(part['step'],1,993)
                value.extend(expand(part['item'],{part['index']:index}) for index in range(part['start'],part['stop'],part['step']))
            require(len(value) == row['length'],'C-ninth-complete993-template-length')
        values[name] = value
    return values


def checker_parent2346_contract_fixture_gate(entry):
    # The enclosing test_gate authenticates actual C/source-registry pins and
    # the whole selftest: old eight groups, fixture_scope and all79 interfaces.
    # This component only compares saved ninth-group metadata; it writes no fixture.
    require(type(entry) is dict and set(entry) == {'root','selftest'},'C-ninth-entry-exact-two')
    root,selftest = entry['root'],entry['selftest']
    require(isinstance(root,Path),'C-ninth-root-Path')
    require(type(selftest) is dict and selftest.get('schema') == 'd972.r07.fixed-lambda-cycle-batch.v10.selftest' and
        selftest.get('status') == 'PASS' and type(selftest.get('tests')) is list and len(selftest['tests']) == 9,
        'C-ninth-current-PASS-selftest-nine-groups')
    contract = C_NINTH_SERIALIZER
    ordinary(contract['case_count'],20,20)
    require(type(contract['cases']) is list and len(contract['cases']) == 20 and
        type(contract['case_order']) is list and len(contract['case_order']) == 20,
        'C-ninth-twenty-registered-cases')
    v7_sixth_same(selftest['tests'][8],{'name':contract['group_name'],'status':'PASS',
        'rejected_cases':copy.deepcopy(contract['case_order'])},'C-ninth-selftest-group-exact-three')
    values = v10_c_ninth_values()
    observed = scan(root);names,extras,ledger_rows,observations = {'case-ledger.json'},set(),[],[]
    for index,case in enumerate(contract['cases']):
        require(type(case['ordinal']) is int and case['ordinal'] == index and
            case['name'] == contract['case_order'][index],'C-ninth-case-order')
        base = safe_name(case['name']);positive = copy.deepcopy(values[case['positive_value_ref']])
        require(case['negative']['copy_positive'] is True and type(case['negative']['operations']) is list and
            len(case['negative']['operations']) == 1 and type(case['negative']['reseal_value']) is bool,
            'C-ninth-one-declared-mutation')
        negative = v10_ninth_patch(positive,case['negative']['operations'])
        require(canonical(positive) != canonical(negative),'C-ninth-mutation-not-noop-including-ordinary-types')
        if case['negative']['reseal_value'] is True:
            negative = v7_sixth_seal({key:value for key,value in negative.items() if key != 'sha256'})
        for stage,value in [('positive',positive),('negative',negative)]:
            name = base + '/' + stage + '.json';names.add(name)
            v7_sixth_read(root,name,v7_sixth_seal({'schema':contract['wrapper']['schema'],'value':value}))
        error_name = base + '/rejection.json';names.add(error_name)
        v7_sixth_read(root,error_name,case['rejection'])
        for support in case['support_files']:
            name = base + '/' + safe_name(support['file']);names.add(name)
            if support['encoding'] == 'canonical-json':
                v7_sixth_read(root,name,values[support['value_ref']])
            else:
                require(support['encoding'] == 'ascii' and (root / name).read_bytes() == support['value'].encode('ascii'),
                    'C-ninth-actual-support-bytes')
        for directory in case['preserved_empty_directories']:
            extras.add(base + '/' + safe_name(directory))
        row = {'case_name':base,**contract['case_ledger']['entry_fixed'],
            **{field:pin(root / base / file,'parent2346/' + base + '/' + file)
                for field,file in contract['case_ledger']['entry_D3_relative_files'].items()},
            'observed_label':case['rejection']['observed_label']}
        ledger_rows.append(row)
        observations.append({'case':base,'positive':row['positive'],'negative':row['negative'],
            'rejection':row['rejection'],'observed_label':row['observed_label'],
            'intended_label_reached':True,'whole_saved_positive_and_single_mutation_compared':True,
            'ordinary_helper_reexecuted':False})
    v7_sixth_read(root,'case-ledger.json',{**contract['case_ledger']['fixed_values'],'cases':ledger_rows,
        'rejected_cases':contract['case_ledger']['rejected_cases']})
    require(len(names) == 64 and len(observations) == 20,'C-ninth-registered64-files-and20-observations')
    v7_sixth_same([row['file'] for row in observed['files']],sorted(names),'C-ninth-all64-files')
    directories = v7_sixth_directories(names,extras)
    require(len(directories) == 23,'C-ninth-registered23-directories')
    v7_sixth_same(observed['directories'],directories,'C-ninth-whole-dir-and-empty-roster')
    v7_sixth_same(scan(root),observed,'C-ninth-before-after-whole-read')
    return observed,observations
```

Complete public raw patch, exactly8270 bytes. The separate JSON records every forward/reverse raw span:

```diff
--- public-baseline-C-eighth-consumer
+++ public-current-C-ninth-consumer
@@ -1,16 +1,16 @@
-def v9_eighth_patch(value,operations):
+def v10_ninth_patch(value,operations):
     mapped=[]
     for operation in operations:
         row=copy.deepcopy(operation)
         if row['op'] == 'add':
             require(row.pop('key_must_be_absent',None) is True and 'require_absent' not in row,
-                'C-eighth-public-add-key-must-be-absent')
+                'C-ninth-public-add-key-must-be-absent')
             row['require_absent']=True
         mapped.append(row)
     return v8_seventh_patch(value,mapped)
 
 
-def v9_c_eighth_values():
+def v10_c_ninth_values():
     def expand(value,variables):
         if type(value) is list:
             return [expand(item,variables) for item in value]
@@ -19,7 +19,7 @@
         operations = [key for key in value if key.startswith('$')]
         if not operations:
             return {key:expand(item,variables) for key,item in value.items()}
-        require(len(value) == len(operations) == 1,'C-eighth-single-reserved-expression')
+        require(len(value) == len(operations) == 1,'C-ninth-single-reserved-expression')
         operation = operations[0];item = value[operation]
         if operation == '$var':
             return variables[item]
@@ -27,39 +27,57 @@
             result = expand(item,variables);ordinary(result,0)
             return str(result)
         if operation == '$concat':
-            parts = expand(item,variables);require(all(type(part) is str for part in parts),'C-eighth-string-parts')
+            parts = expand(item,variables);require(all(type(part) is str for part in parts),'C-ninth-string-parts')
             return ''.join(parts)
-        require(operation == '$mod','C-eighth-finite-expression-domain')
+        require(operation == '$mod','C-ninth-finite-expression-domain')
         left,right = expand(item,variables);ordinary(left,0);ordinary(right,1)
         return left % right
     values = {}
-    for name,row in C_EIGHTH_SERIALIZER['value_definitions'].items():
+    for name,row in C_NINTH_SERIALIZER['value_definitions'].items():
         if row['kind'] == 'literal':
             value = copy.deepcopy(row['value'])
         elif row['kind'] == 'seal':
             value = v7_sixth_seal(copy.deepcopy(row['unsigned_value']))
         else:
-            require(row['kind'] == 'array_template','C-eighth-finite-definition-kind')
+            require(row['kind'] == 'array_template','C-ninth-finite-definition-kind')
+            ordinary(row['length'],0,993)
             value = []
             for part in row['parts']:
-                ordinary(part['start'],0,865);ordinary(part['stop'],part['start'],865);ordinary(part['step'],1,865)
+                ordinary(part['start'],0,993);ordinary(part['stop'],part['start'],993);ordinary(part['step'],1,993)
                 value.extend(expand(part['item'],{part['index']:index}) for index in range(part['start'],part['stop'],part['step']))
-            require(len(value) == row['length'],'C-eighth-complete865-template-length')
+            require(len(value) == row['length'],'C-ninth-complete993-template-length')
         values[name] = value
     return values
 
 
-def checker2218_fixture_gate(root,selftest):
-    contract = C_EIGHTH_SERIALIZER
-    expected_selftest = v7_sixth_seal({'schema':contract['actual_selftest_result']['schema'],
-        **copy.deepcopy(contract['actual_selftest_result']['fixed_values'])})
-    v7_sixth_same(selftest,expected_selftest,'C-eighth-whole11-selftest-all8groups-and69interfaces')
-    values = v9_c_eighth_values()
+def checker_parent2346_contract_fixture_gate(entry):
+    # The enclosing test_gate authenticates actual C/source-registry pins and
+    # the whole selftest: old eight groups, fixture_scope and all79 interfaces.
+    # This component only compares saved ninth-group metadata; it writes no fixture.
+    require(type(entry) is dict and set(entry) == {'root','selftest'},'C-ninth-entry-exact-two')
+    root,selftest = entry['root'],entry['selftest']
+    require(isinstance(root,Path),'C-ninth-root-Path')
+    require(type(selftest) is dict and selftest.get('schema') == 'd972.r07.fixed-lambda-cycle-batch.v10.selftest' and
+        selftest.get('status') == 'PASS' and type(selftest.get('tests')) is list and len(selftest['tests']) == 9,
+        'C-ninth-current-PASS-selftest-nine-groups')
+    contract = C_NINTH_SERIALIZER
+    ordinary(contract['case_count'],20,20)
+    require(type(contract['cases']) is list and len(contract['cases']) == 20 and
+        type(contract['case_order']) is list and len(contract['case_order']) == 20,
+        'C-ninth-twenty-registered-cases')
+    v7_sixth_same(selftest['tests'][8],{'name':contract['group_name'],'status':'PASS',
+        'rejected_cases':copy.deepcopy(contract['case_order'])},'C-ninth-selftest-group-exact-three')
+    values = v10_c_ninth_values()
     observed = scan(root);names,extras,ledger_rows,observations = {'case-ledger.json'},set(),[],[]
     for index,case in enumerate(contract['cases']):
-        require(type(case['ordinal']) is int and case['ordinal'] == index,'C-eighth-case-order')
+        require(type(case['ordinal']) is int and case['ordinal'] == index and
+            case['name'] == contract['case_order'][index],'C-ninth-case-order')
         base = safe_name(case['name']);positive = copy.deepcopy(values[case['positive_value_ref']])
-        negative = v9_eighth_patch(positive,case['negative']['operations'])
+        require(case['negative']['copy_positive'] is True and type(case['negative']['operations']) is list and
+            len(case['negative']['operations']) == 1 and type(case['negative']['reseal_value']) is bool,
+            'C-ninth-one-declared-mutation')
+        negative = v10_ninth_patch(positive,case['negative']['operations'])
+        require(canonical(positive) != canonical(negative),'C-ninth-mutation-not-noop-including-ordinary-types')
         if case['negative']['reseal_value'] is True:
             negative = v7_sixth_seal({key:value for key,value in negative.items() if key != 'sha256'})
         for stage,value in [('positive',positive),('negative',negative)]:
@@ -73,11 +91,11 @@
                 v7_sixth_read(root,name,values[support['value_ref']])
             else:
                 require(support['encoding'] == 'ascii' and (root / name).read_bytes() == support['value'].encode('ascii'),
-                    'C-eighth-actual-support-bytes')
+                    'C-ninth-actual-support-bytes')
         for directory in case['preserved_empty_directories']:
             extras.add(base + '/' + safe_name(directory))
         row = {'case_name':base,**contract['case_ledger']['entry_fixed'],
-            **{field:pin(root / base / file,'parent2218/' + base + '/' + file)
+            **{field:pin(root / base / file,'parent2346/' + base + '/' + file)
                 for field,file in contract['case_ledger']['entry_D3_relative_files'].items()},
             'observed_label':case['rejection']['observed_label']}
         ledger_rows.append(row)
@@ -87,10 +105,10 @@
             'ordinary_helper_reexecuted':False})
     v7_sixth_read(root,'case-ledger.json',{**contract['case_ledger']['fixed_values'],'cases':ledger_rows,
         'rejected_cases':contract['case_ledger']['rejected_cases']})
-    v7_sixth_same([row['file'] for row in observed['files']],sorted(names),'C-eighth-all49-files')
-    v7_sixth_same(observed['directories'],v7_sixth_directories(names,extras),'C-eighth-whole-dir-and-empty-roster')
-    v7_sixth_same(scan(root),observed,'C-eighth-before-after-whole-read')
+    require(len(names) == 64 and len(observations) == 20,'C-ninth-registered64-files-and20-observations')
+    v7_sixth_same([row['file'] for row in observed['files']],sorted(names),'C-ninth-all64-files')
+    directories = v7_sixth_directories(names,extras)
+    require(len(directories) == 23,'C-ninth-registered23-directories')
+    v7_sixth_same(observed['directories'],directories,'C-ninth-whole-dir-and-empty-roster')
+    v7_sixth_same(scan(root),observed,'C-ninth-before-after-whole-read')
     return observed,observations
-
-
-# Task1186: finite P production-key saved-metadata reception, never a helper rerun.
```

AUDIT_1191D_C_VERDICT: STATIC_PUBLIC_COMPONENT_COMPLETE_ALL20_AND_PUBLIC_CALLERS_JOINED_TARGET_EXECUTION_ZERO
