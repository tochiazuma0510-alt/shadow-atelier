# Task965 — complete oracle の一個の非零 witness を実体化する producer

役: Luna producer。Task959 の最終凍結を先に完了し、root の開始連絡後に実施。
変更可は `search/d972_r07_selected_cycle_materializer_v1.py` と
`sol/luna_reply_965_r07_selected_cycle_materializer.md` の二本だけ。
旧source/workflow、他人のファイルを変更しない。ローカル数値/Python import/AST/GAP、
network/git/credential/dispatch/追加agent禁止。source/JSON/byte/hash読取と指定実装だけ。
数値・AST・新interface canary・実本走はroot brokerのGHA。root以外へcredentialを渡さない。

Task963完成返信を実装仕様の正本とし、Task957 E、v547/v548、Task958 positive gate、
Task959の最終公開ABIを全文読む。同じ登録source/P1/Conn/四Bを維持する。
一個のlambdaでのoracleから一個のphysical追加を作る。非零やtarget変化を予言しない。
COMPLETE_ZERO_CANDIDATEならEの算術を開始せず型付きNOT_APPLICABLEで止める。

1. 実oracle candidateと成功checkerのexact run/attempt/head/artifact/ZIP/entryを
   rootから受領して固定する。未観測pinではfail closed。元current snapshotは
   full-origin completion33971897879/1、rank1385/gen8090の26段prefix。
   completion tupleと元producer source/stateの由来を分ける。凍結済み親の薄いloaderを
   使い、元26scan/insertや旧成功suiteを再走しない。全current rows/targetは読む。
   oracleの全roster/array hash/EOFを認証し、固定current lambdaへの結び付きを要求する。
2. witness aux x/y または最初のfailed chordと五基準chordから、一つのordered raw
   SLPを作る。Task963のIdentity/Letter/Ref/OrderedProduct/Inverse/IntegerPowerと
   tree wordを使う。係数2は符号代表-1とし、順序をsort/線形集約しない。
   同じSLPの普通整数epsilon、omega、実Q0/Q2 endpoint、tauを求める。
   chordはv547のC(w)を固定順で適用し、integer divisionの6可除性を要求する。
   auxはc_x/c_yをそのまま使いetaを消さない。actual36点Q0とGamma0を混同しない。
3. 同じraw SLPのQ2 Fox chainを実評価する。productはD(UV)=DU+ev(U)DV、
   inverseは-DUをev(U)^-1で左translateする。非閉prefixにclosed qnormを呼ばない。
   raw wordのbounded flattenを選ぶならTask963の実長上界・EOF/hash/resourceを保存し、
   P1/current巨大SLP全体をflattenしない。全6tag×四character×d0/d1/d2/etaを
   同じraw chainから作り、oracle witness scalarの実source評価と結ぶ。
4. primal canonical sectionはold元embedded lead昇順→new各owner元lead昇順。
   全8059alphaと別のordered nonzero events、normalized rowのscale非再乗算、
   oldの四d1 companion/shared aux、全96776 lower-zeroを要求する。
   P1 cache/index/instructionsはexact accepted refs。全四topを同じalphaで補正。
   subtract_liftsはin-placeであるため二重減算しない。数値alphaとliteral ancestryを分離。
5. Gは四Bの全和。old lambda·G=oracle witness scalar≠0を実測する。
   全current physical pivotsを挿入順で消去、非零residualを一度normalizeし追加。
   selected scalar/normalizing scale/target scalarは別字段。target scalar0も合法。
   generation/rankは親から+1で、新current全row/fresh separator/両target dotを確認。
   original rho2は明示DERIVED、直接再読した値と呼ばない。
6. 原raw SLP、補正P1 refs/events、旧physical row refs/ordered reductionsをつなぐ
   normalized rowのtyped instructionを保存する。raw corrected wordのsource-lower零を、
   Connも引いたnormalized literalへコピーしない。後者はphysical-lower零のreceipt。
   target零はLINEAR_MEMBERSHIP_CANDIDATEとTask958 gate pendingであり、MEMBERや
   full A0へ昇格しない。新whole-word directの実施範囲をraw/normalized/targetに分ける。
7. 一行delta schema `d972.r07.selected-cycle-materializer.v1` の公開CLI/array/JSON/
   canonical seal/manifest/HEAD ABIを先にreply965に書きTask966へ通知する。
   Task963の最小rosterを具体化。current parent plus one rowを次consumerが薄く読める
   完全なancestry、stage cursor/EOF、atomic complete output、UNKNOWN_RESOURCEを実装。
   次oracle/positive consumerは別実装であり、このtaskで無断増設しない。
8. 少数の新interface canaryは実production helpersを使い、omega積/逆/冪・負chord・
   mixed degreesと四character・old/new primalの元lead順・plain target型を狙う。
   既存成功suiteと旧26段の全数値を再走するcanaryは作らない。
   raw/source/primal/P1/B/physical各stageの時間・bytes・alpha support・実長を記録し、
   収束反復回数/end-to-end速度を予測しない。

checker authorとは式/公開ABI/実pinsのみ共有し、新算術helperを共有しない。
工房CV9の親受理水準とTCBをreplyに明記。未実行をPASS/照合済みと書かない。
最終sourceのbytes/SHA/LF、実装の限界、必要なGHA gateをroot/966/967へ通知する。
最終行 `AUDIT_965_VERDICT:`、verified=false。
