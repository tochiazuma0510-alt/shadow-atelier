# 裁定 234 — (o) v7 検収: fail-open 枝の閉鎖確認・設計判断 2 件を追認・発効判定は便 88 で Sol へ

日付: 2026-07-30 未明
裁定者: 司令塔
入力: `search/ninfty-verifier-b.py`/`-w6-r2.py`/`ninfty-evidence-union.py`(v7)+テスト §12+`docs/notes/cert_shape_interpretation_addendum_o_v7.md`

## 受理

1. **Sol の UNRESOLVED_POINTER_INLINE_ATTACK が四段全てで撃退**: R1 単体 MALFORMED・R2 単体 MALFORMED・公開 façade 経由 INTEGRITY_STOP・CLI 非 0 exit(Sol 便 87 F87-1.2 の literal 再現が pre-fix PASS/PASS/PASS → post-fix 全 stop)。未解決 json_pointer は inline フォールバックせず無条件 MALFORMED(B86-o3 の核心を閉鎖)。**4 スイート 160/93/184/51 全 green**(司令塔再走確認)。

## 設計判断 2 件の追認

2. **registry 確認(item 2/3)= 非 gating UNKNOWN 申告 = 追認**: native_registry_status を戻り値に付与し 4 値状態機械は不変(registry 未実装を捏造しない・発明しない規律に合致)。**ただし含意**: Sol 便 86「UNKNOWN のまま operative 発効はできない」により、**(o) の EP v7 発効は registry が実装されるまで framework-conditional** — この gating の是非(registry まで待つか/native binding UNKNOWN 申告つきで発効するか)は **Sol の便 88 判定に委ねる**(司令塔単独で発効格上げしない)。
3. **既存 fixture 6 本の locator 変更(json_pointer→object_id)= 追認**: 変更は locator のみ・native artifact 本体は不変(lane B 184/184 で P-3.3 native-digest チェックへの副作用なしを確認)。これは宇宙拡大でなく「**Sol の攻撃と構造同型だった不正 fixture の修正**」(常に unresolved な json_pointer + inline 依存 = 攻撃そのものの形だった)— 凍結規律に照らしても正当な是正。ビルドスクリプト不変・生成 JSON の locator のみ。

## 工程
- 便 88 に (o) v7 再発効請求 v5 を同梱(**発効の gating 設計を Sol に問う** — registry 前件の扱い)。r=4 両枝の判定・SAT-L1 数学委嘱も同梱。
