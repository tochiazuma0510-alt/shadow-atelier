# Luna Reply 809 — canonical P1 degree-two v6 measured time-cap repair

## 1. Terminal

`READY_FOR_ROOT_DISPATCH`

Only the commissioned v6 workflow and this reply were created. No Python executable, mathematics, workflow pin, GHA run, git state, or dispatch was changed.

## 2. Exact repair

The v5 workflow was cloned faithfully and only the eight commissioned version/cap lines changed:

- workflow name: `...-v5` → `...-v6`;
- fire tag: Task794/v5 → Task809/v6;
- launch step label: v5 → v6;
- candidate artifact name: Task794/v5 → Task809/v6;
- log artifact name: Task794/v5 → Task809/v6;
- outer job timeout: `75` → `100` minutes;
- `D972_LIFT_SECONDS`: `3540` → `5100`;
- inner GNU timeout: `60m` → `85m`, retaining `--kill-after=30s`.

After reversing exactly those eight substitutions, v6 is byte-equal to v5. Static counts found `timeout-minutes: 100`, `D972_LIFT_SECONDS: "5100"`, and `kill-after=30s 85m` exactly once each.

All executable paths, executable hashes, arithmetic/build command, exact source and semantic receipts, action pins, NumPy/thread settings, `D972_LIFT_MAX_RSS=7516192768`, both `ulimit -v 8388608` occurrences, the serial producer, terminal manifest checks, success-only candidate upload, always-upload logs, false claim flags, 8,059-row roster, and 292,444,992-byte cache expectation are unchanged.

## 3. Measured v5 evidence

Run/attempt `33833873366/1`, job `100902284260`, head `011780dea7ced10f36b65f428616c453fe87cf8a`, ended with inner-shell exit `124` at the 60-minute limit. The last complete checkpoint was cursor `7,040 / 8,059` (`87.3558%`), elapsed `3537.254897481` seconds, durable bytes `538,893,700`, RSS `5,223,956,480` bytes. `unknown-resource.json` records `{"phase":"degree2-lift","status":"UNKNOWN_RESOURCE"}`. This was not a memory stop and emitted no mathematical terminal.

The measured last 384-row interval was about 178.71 seconds. Linear projection places the remaining 1,019 rows near 474 seconds; the complete post-2,048 average gives about 656 seconds. The new 85-minute inner cap is therefore a measured bounded margin and does not alter the computation.

## 4. Static receipt

Only YAML/static pin checks were performed; no executable or production calculation was run.

| file | bytes | LF | CR | BOM | SHA-256 |
|---|---:|---:|---:|---|---|
| `.github/workflows/d972-r07-canonical-p1-dag-degree2-lift-v6.yml` | 27575 | 497 | 0 | no | `45762035913010fc08ef70092e451f4137b9ac8acd39a50aa77fc91be44addbb` |

## 5. Claim boundary

Canonical P1 acceptance, grade-two MEMBER/NONMEMBER, A0, COMMON, compatible lift, fake, and Ihara are not claimed. `verified=false`.
