# Task1144 — v6 public runtime metadata and exact key-set reception

Sol / 2026-09-10. Luna metadata implementation support. Reply only `sol/luna_reply_1144_r07_v6_public_runtime_receipt_v1.md`, final `TASK1144_VERDICT:`. New supporting files only `%TEMP%/shadow-atelier-audit163/task1144/`, versioned/CreateNew. No subagents, Git/GHA/network/credentials, repository source edits, mathematical execution/import/AST, or P/C private source/binder inspection. Root alone owns broker actions and final adoption. Do not pass sandbox_permissions. ASCII .ps1 only; prefer small Python standard-library metadata tooling to avoid the measured PowerShell traversal cost.

Main v6 run34416548935/1, head866c87eaec6bca55d2578906c0f338cb34582373, job102682595416 completed/success at2026-09-10T00:32:56Z. Candidate10131122423 and diagnostics10131139047 are downloading under root control. This is NOT yet new mathematical/CV-9 adoption. Previous formal v5 reception is DONE, and its 8-hour receiver must not be rerun or redesigned speculatively.

Concrete independent work now: read the public final driver `search/d972_r07_fixed_lambda_cycle_batch_v6_workflow_driver_v1.py` (2281118 B / 88af646a3ac524c75efb777ebdb5fcfb3391b03f652bb6ae518acac74ceccfc6) and public workflow `.github/workflows/d972-r07-fixed-lambda-cycle-batch-v6.yml` (30603 B / c02e1e391f99fbad59b5f91ccd5fb88d6b1f2a3f494bfe6e1006f75113115115). Register/pin before analysis. Derive a precise, bounded public runtime receipt contract: the actual final REPORT, final gate, P/C invocation exit/resource receipts, cost/parent-timing receipts, preservation and source-region results, original64/native v3/v4/v5 nine-key acceptance, and public selftest metadata group results. Record exact source locations, exact object key sets and scalar/list/null type rules, file name and descriptor bindings. Preserve all eighteen parent tuples and k128/max_batches1/no-refill/P5400/C10800/outer6000/11400/RSS7168/selftest300/360/job330min. Merely seeing PASS or GHA success is insufficient.

As soon as the short machine-readable contract exists, send its path/pin. Then prepare a small read-only metadata receiver SOURCE plus explicit input roster, using actual named public metadata files only (root will supply extracted paths/pins when downloaded). It must reject duplicate JSON object keys, booleans as integer counts, unknown/missing exact keys, mismatched pins, and invalid cross-report joins. Do not invent unobserved runtime schema; inspect the actual public files when supplied. All numerical claims in the eventual result must come from these files. No arithmetic/vector checking or independent-C claim, and no fresh parent/candidate/fixture mutations or directory restoration in this task. Do not decode P/C source bodies: if a descriptor needs raw SHA, report it for root to join or hash opaquely only after root supplies a path.

Distinguish this bounded public envelope receipt from a FULL typed parent reception: enumerate any required checks outside your bounded slice rather than silently treating them as passed. A whole new formal receiver is not requested. Root independently receives both outer ZIPs, enumerates every member/EOF/CRC, joins REPORT inventory and raw sources, and handles archive/extraction custody. Work alongside that reception without waiting for a final commander response. Avoid a long speculative rewrite; deliver the contract and source promptly, followed by actual metadata result when inputs arrive. This task supports the already requested commander incremental CV-9 under ruling2235 (18th parent, nine-key, fresh lambda1834 versus old lambda1706, two-literal reverse binding, mechanically enumerated public JSON keysets).
