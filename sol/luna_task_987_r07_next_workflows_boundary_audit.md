# Task987 — 同一語consumer985と保存再開986のGHA境界監査

役: Luna独立監査。984の最終source/返信freezeを先に完了し、その後に本便へ進む。
変更可は `sol/luna_reply_987_r07_next_workflows_boundary_audit.md` のみ。
旧返信/source/workflowの書換え、ローカルPython/import/AST/数値/GAP/network/git/credentials/新agentは禁止。
rootが唯一のGitHub/GHA broker。source/metadata/bytes/hashと静的読取りのみ。

対象はTask985/986と両新workflowの実保存本文（保存途中なら読了境界を記帳し、最終版と混ぜない）。
`.github/workflows/d972-r07-continuation-positive-word-readout-v1.yml`
`.github/workflows/d972-r07-complete-oracle-cegar-resume-next-v1.yml`
各source全文・公開CLI/ABI・親実metadata・全run/attempt/head/artifact tuple・scope/resourceを照合する。

985: 十六live親/root非包含/whole ZIP/全file前後不変、既存Task554 failureとTask712 envelope、
fresh rho2七payloadの元C1/root、固定source closure/raw/runtime、982/983の最終exact source pinsと
acceptance schema→P一回→同一word root D一回→全13 P出力/全D出力/EOF/同root normalized/実11slotを結ぶ。
新三群ずつのcanaryだけを実CLIのdeadline/memoryへ接続し、旧suite再走を入れない。
Pは整数資源引数、Dは自分の資源とP宣言を結ぶproducer-max-*も必要。SIG/resource exitを成功に変えない。
metadata pin未観測なら起動拒否、未来run successで穴を埋めない。
nonzero current residualでも全readoutを行う契約と、positive applicability/side/localization未完を分ける。
一般targetへsource lower96776零やfull P零を足さず、現32260/48384はfull filteredで全一致を要求する。

986: observed_parent JSONをenv経由でparseし、式/commandへ直接埋め込まない。callerのPASS文字列で
認証を代用せず、同repo/branch/登録workflow/live success/実ZIP/元owner/source/start/runtime/full Cの
実entryへ結ぶ。completion/resume64/resume-nextの三系列だけ、元14親と直前candidate全体の計15親。
実before count=n、rank=1386+n/gen=8091+n、全保存prefix/current checkpoint/旧invocationを取得し、
新mutable siblingへ全outputコピー、HEAD/result二file以外の旧bytes/dir不変を要求する。
絶対cap M>nと資源境界、一回P→新invocationちょうど一本→startから全C→旧steps[:n]/snapshots[:n]
全dict一致→旧全file保存→最終receiptの同run/head/source/親への接続を監査する。
COMPLETE_ZERO/LINEAR候補は必要な終端経路へ渡し、UNKNOWNを成功数学裁定へ変えない。
codeが同一でもparent-intake/source/static gateの差は全て読む。新metadata試験があればproduction経路との接続を確認。
coverageは実修理前wのomega/central全指数、q/κ/score/aux、lambda台、failed_chord/basisを読む。
fixed scope宣言と情報性、driverのmetadata集計と第三算術、source zeroとoperator zeroを分ける。

両方: checkout actual HEAD/persist-credentials=false、最小permission、always diagnostics/hidden/pending/
全source・WF・入力・ログ・exit・時間、retention30日、成功candidateは全gate実成功だけ。
公刊P/Cの算術・schema・scope・既存固定親がdriverで弱められていないか敵対的に読む。
984数学境界/2150受理/2151追補は保持。新語のGHA成功/CV9/grade2/A0/verifiedを静的監査で宣言しない。
最終source bytes/SHA/LF/CR/BOM/finalLFと、実親の観測pin部分の読取範囲を返信に記す。
初回の実parent pinが後で入るならその差分を再読する。最終行 `AUDIT_987_VERDICT:`。
