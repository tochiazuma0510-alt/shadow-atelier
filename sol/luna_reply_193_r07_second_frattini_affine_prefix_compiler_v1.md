# Luna reply 193 - R07 second-Frattini affine-prefix compiler v1

## 1. Governing objective

The five-file implementation consumes only an attested positive task186
receipt and the exact `exact_direct_replay.replay.corrected_word`; no g760
fallback, Jennings projection, or synthetic production word is used.

## 2. Authorized dependency cone

The producer authenticates task179 producer/runtime interfaces and the task186
checker pin. The checker independently authenticates task179 checker arithmetic
and reruns the pinned task186 independent production checker without importing
the task193 producer.

## 3. Lazy affine quotient

Actual typed crossed-product pairs use `(c,g)(d,h)=(c+g*d,g*h)` and
`(-g^-1*c,g^-1)`. Equality uses persistent rank-zero Echelon state and one
bounded, load-bearing task179 support-times-occurrence correlation per query,
with noncommutative `t=g*h^-1` and the explicit `t*h=g` gate. Positive chains
retain column IDs, pivots, ancestry, and rows; negative results retain full
decoded correlation entries and zero cancellations. The independent checker
replays queries chronologically before cache reuse. Checkpoints seal input
identity, source-rebuild cursor, active set, rosters, transcript, and caps;
resume is deterministic rank-zero replay.

## 4. Actual beta1 construction

The producer regenerates the two PB3 and eleven PB4 ordinary Fox rows and all
three ordinary defect rows, binds raw direct-row evidence before affine use,
constructs the two hexagons and the five distinct pcontexts, and uses exact printed order
`paper_product(c1,c3,c0,inverse(c2),inverse(c4))`. PB3 H1/H2 share one label
universe but retain external block tags. All 13 base rows and 3 defect rows
retain affine prefixes, identity receipts, Fox rows, and termwise D1 replay.

## 5. Receipt, checker, and SELFTEST

The checker independently replays representative equality modulo its own
boundary space at each query rank, reconstructs positive coefficient ancestry,
decodes every boundary correlation, validates negative full zero correlation,
recomputes literal words/base blobs/ordinary and marked-map transcripts, and
derives every Fox/D1 row from signed transitions. SELFTEST serializes and
independently enumerates a noncommuting successor Fox defect in addition to
distinct equal representatives, same-base dual separation, fixture equality,
and load-bearing mutation rejection. The existing pinned fixture still contains
the pre-presentation synthetic toy payload; because execution is prohibited,
its replacement payload/hash has not been generated, so final selftest GO is
not claimed here.

## 6. Driver and delivery

The GAP driver pins producer/checker/fixture/task186 checker, disables GAP
stream formatting before shell generation, runs task186 first, uses exact-one
markers, rejects stale outputs, compares byte-extracted terminals, and accepts
only matching PASS/typed UNKNOWN terminals; the fixed nonempty sentinel is
written after checker acceptance. Resource caps produce a sealed resumable
`UNKNOWN_RESOURCE` checkpoint; malformed input is typed `UNKNOWN_INPUT`. No
runtime/GAP/GHA execution was performed by Luna.

A strict `FIXTURE_GEN` bootstrap mode is now present in the driver. It pins the
producer and other normal dependencies while deliberately skipping only the
stale fixture pin, invokes producer `--selftest --fixture-output` with a
separate fail-closed log, and writes the distinct
`R07_SECOND_FRATTINI_AFFINE_PREFIX_COMPILER_V1_FIXTURE_GEN_PASS` marker only
after a nonempty canonical candidate exists. It does not run the checker or
claim SELFTEST acceptance; parent must run this bootstrap, inspect the
candidate, replace/pin the fixture, and then refresh the producer/checker/
driver identities. Bootstrap and final fixture identities remain pending.

The latest repair makes the selftest boundary transcript include every
rank-raising row in the 12-row family, uses integer ancestry IDs consistently,
and records/checks the explicit target-minus-base all-seven residual stack
against task186's direct row. Resume accepts only the registered `ci/in`
checkpoint path and requires a strictly enlarged cap; it replays the sealed
rank-zero prefix and checks transcript-prefix equivalence. Oversized checkpoint
serialization is emitted as a typed checkpoint-cap `UNKNOWN_RESOURCE` without
claiming a resumable checkpoint. These changes remain unexecuted pending the
parent's pin refresh/bootstrap.

Current identities:

* producer: 37982 bytes, SHA256 `7470762f9eeb62eb84f09c00fb3c0225622c0bce1632e075b4b84e15291a860a`
* checker: 32037 bytes, SHA256 `89d2a024188cd373e20802f079a946b9c1687ef47bd2a4e985c626568521d639`
* fixture-generation driver: 9799 bytes, SHA256 `4795d7b2122c12ddb4064d44343e9ee323728a1c8866b415f25d69fcf5c675f2`
* fixture: 327 bytes, SHA256 `3bf40c5b6e3635b474674af8cb9a7e477e80481727f01574f4a50cff0c0acb49`
* task186 checker: 54982 bytes, SHA256 `8898798d0d6a9e0b6cd67402e74ba0dc5048b4797a0f7a9657e58d70d553c488`

The first three identities above are refreshed for the fixture-generation
dispatch.  The fixture and therefore the final SELFTEST pins remain pending.

Parent Sol dispatched the bounded fixture-generation bootstrap as GHA run
`33103538697` at immutable head
`911499ccc00e4034927961fe21588e3a47f13d06` failed before producer startup:
the driver called GAP `Concatenation` with one argument in its `Exec` wrapper.
This is recorded as a wrapper-code failure only; no fixture, SELFTEST, or
mathematical result was produced. The three `Exec` calls are now direct
literal strings, and Parent Sol refreshed the driver identity above.

Parent Sol redispatched the fixture bootstrap as GHA run `33104083100` at
immutable head `64327a4efef2e715a789fa8fb2b7387b51d78271`. It failed with only
`Error, task193 missing completion`: `set -e` exited on producer failure before
the fixture-generation log was printed. This is a hidden producer/bootstrap
code failure only; no fixture or mathematical result was produced. The
FIXTURE_GEN producer and all normal SELFTEST/PRODUCTION producer/checker calls
now use `if ! ...; then cat log; exit 1; fi`; Parent Sol refreshed the driver
identity above before redispatch.

LAZY E3,1/E4,1 AFFINE COMPILER:               NOT EXECUTED BY LUNA
ACTUAL SECOND-RUNG beta1:                      NOT EXECUTED BY LUNA
SECOND-RUNG MEMBERSHIP / CORRECTION:           NOT ATTEMPTED
COMPATIBLE COFINAL LIFT / FAKE / IHARA:        NOT DECLARED
