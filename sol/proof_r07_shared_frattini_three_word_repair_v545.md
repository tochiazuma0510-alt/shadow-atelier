# R07: the same Frattini subgroup and a three-word endpoint repair (v545)

Author: root / 2026-09-05

Status: paper strengthening of v542 using the already marked Q0-kernel.
No numerical A0 decision, new GHA computation or table export is asserted.
`verified=false`.

## 1. Nested endpoint kernels, with fixed finite premises

Retain the marked quotients and notation

```text
F = F(x,y) -> Delta -> Q0 -> Q2,
Omega = ker(F -> Delta),
N0 = ker(F -> Q0),       N2 = ker(F -> Q2),
Gamma0 = N0/Omega,       Gamma2 = N2/Omega.
```

Thus Gamma0 is an actual subgroup of Gamma2, not only an abstractly
isomorphic group. Write Phi(H)=H^3[H,H]. The existing finite inputs are:

```text
|Gamma0| = 243,      |Phi(Gamma0)| = 27,
|Gamma0'| = 3,      Exp(Gamma0) = 9,
Gamma0/Phi(Gamma0) = <[gamma3],[gamma5]> = F3^2;

|Gamma2| = 6561,    Gamma2_ab = C3^3 x C9^2.
```

The Gamma0 statements and the specifically MARKED basis indices3,5 are
recorded in `sol/fable_reply_r07_a0_paper_closure_v2_addendum.md` Sections1.1
and1.3 and `scratchpad/a0_v2_gamma_output.txt` lines2--6. The exponent-nine
input is the one used in v459(1.3). The Gamma2 inputs are the `rung3` record
in `scratchpad/a0_v2_rungs_output.txt`, also used in v458/v542. This proof
uses those existing finite premises without rerunning or upgrading them.

Let `s1,...,s5` denote the pinned literal words `A0P_SRC` in
`scratchpad/a0_paper_words_v1.g` (also in its JSON source), in the SAME
ascending pc order as that addendum. They satisfy

```text
si in N0,       Theta(si)=gamma_i.
```

These are the five marked kernel-generator words, NOT compact relators
numbered3,5 and NOT a new enumeration of the44 compact seeds.

## 2. Equality, not two independent27-element groups

Functoriality of cubes and commutators gives

```text
Phi(Gamma0) <= Phi(Gamma2).
```

The Gamma2 abelianization has mod-three rank5, hence

```text
|Phi(Gamma2)| = 3^(8-5) = 27 = |Phi(Gamma0)|.
```

### Proposition2.1

```text
Phi(Gamma2) = Phi(Gamma0)                              (2.1)
```

as subgroups of Delta. In particular a Q2-legal cycle's unresolved endpoint
is in the SAME previously marked Gamma0 subgroup. A new Gamma2 repair table
is not a mathematical prerequisite.

## 3. Three fixed words replace the15-generator BFS

Put `a=gamma3`, `b=gamma5` and use the explicit commutator convention
`[a,b]=a^-1*b^-1*a*b` throughout this note. The marked Frattini-basis premise
implies that a,b generate Gamma0: a proper subgroup of a finite3-group lies
in an index-three maximal subgroup containing Phi, contradicting its image
being the entire Frattini quotient.

Since Gamma0' has order3, conjugation on it factors from a3-group to
Aut(C3), of order2, and is trivial. Gamma0 has class at most2. Consequently
its cubes and commutators are central, and, because Exp(Gamma0)=9,

```text
a^3, b^3, [a,b] are central of order dividing3.          (3.1)
```

The subgroup generated by (3.1) is Phi(Gamma0). Indeed in its quotient the
generators a,b commute and have exponent3, so the quotient is elementary
abelian. The reverse containment is the definition of Phi. Its order is27,
while three commuting order-three generators have at most27 products.
Therefore these three elements are independent and

```text
Phi(Gamma0) = <a^3> x <b^3> x <[a,b]> = C3^3.          (3.2)
```

Every h in this subgroup has unique coordinates

```text
h = (a^3)^alpha (b^3)^beta [a,b]^gamma,
alpha,beta,gamma in F3.                                 (3.3)
```

Define the three actual fixed source words

```text
U = s3^3,    V = s5^3,    W = s3^-1*s5^-1*s3*s5.        (3.4)
```

All three belong to Phi(N0), with endpoints a^3,b^3,[a,b]. In particular
their Fox cycles vanish at Q0 and EVERY quotient of Q0. They need not
themselves be in Omega. They are endpoint-repair factors, not already legal
corrections; their product with the initial word is what enters Omega.

## 4. Closed formula for the actual-kernel word

Let z be a cycle at Q2 with tau2(z)=0, and let w(z) be its fixed-order
Schreier word from v542. Then h=Theta(w(z)) belongs to Phi(Gamma2), hence
to (3.2). Obtain the unique coordinates of h^-1 in (3.3), and represent
field coefficient2 by the integer-1. Define

```text
C(z) = w(z) U^alpha V^beta W^gamma.                    (4.1)
```

Its endpoint is hh^-1=1, so C(z) is in Omega. Each appended factor is in
Phi(N0) <= Phi(N2), hence has zero Q2-Fox row. Since w(z) and all appended
factors have trivial Q2 value, Fox additivity gives

```text
J_Q2(C(z)) = z.                                        (4.2)
```

Thus the endpoint repair uses at most THREE signed fixed-word factors,
not up to26 BFS labels. The remaining finite coordinate readout is27
explicit endpoint products (3.3), or the equivalent three-coordinate map
using the already accepted Gamma0 multiplication. No new large group or
compact-seed coefficient solve is required. The27 marked products and their
matching to the old endpoint encoding have not been emitted as an artifact
by this note; formula(3.3) specifies exactly what would be emitted.

V459's two existing normalizer words c_x,c_y can then impose EXACT integer
exponent zero: if epsilon(C(z))=18(A,B), multiply by c_x^-A c_y^-B.
They leave the named Q2-Fox row unchanged. Neither this normalization nor
(4.1) is claimed to preserve a PB4 class, a finer than Q0 jet, or a full
Delta-Fox class.

## 5. What changed relative to v542

V542 needed arbitrary marked generators of Gamma2 and allowed a15-label
closure in its27-element Frattini subgroup. Here the subgroup is identified
with the OLD one, and the two old named source words s3,s5 supply the entire
repair by their cubes and one commutator. This removes that new-generator
selection and BFS obligation from the paper route.

It still does not solve the substantive physical equation A_Q2(z)=t_Q2.
The current seed30 on-demand materialization does not consume this theorem
and must continue without waiting for a chord table. Complete finite A0
also retains the remaining H floors, PB4 block and joint normalization;
no automatic lift across them is inferred.

```text
Phi(Gamma2) = Phi(Gamma0):                  PAPER PROOF
COMMON FRATTINI GROUP:                     C3^3
FIXED SOURCE REPAIR WORDS:                 s3^3, s5^3, s3^-1*s5^-1*s3*s5
MAXIMUM SIGNED REPAIR FACTORS:              3
NEW GAMMA2 GENERATORS / REPAIR BFS:         NOT NEEDED
MARKED27-PRODUCT ARTIFACT:                  NOT EMITTED HERE
ACTUAL TARGET PREIMAGE / A0 / COFINAL:      NOT SUPPLIED
verified=false
```
