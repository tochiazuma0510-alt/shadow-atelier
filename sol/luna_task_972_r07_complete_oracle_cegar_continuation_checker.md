# Task972 — 完全oracle＋E動的継続器の独立checker/GHA

役: Luna 独立実装。Task966の三ファイルを最終freezeし、rootの開始通知を受けてから着手。
本便の変更可は次の三ファイルのみ。公刊/凍結sourceとreply966は変更しない。

- `search/check_d972_r07_complete_oracle_cegar_continuation_v1.py`
- `.github/workflows/d972-r07-complete-oracle-cegar-continuation-v1.yml`
- `sol/luna_reply_972_r07_complete_oracle_cegar_continuation_checker.md`

rootが唯一のgit/network/GHA broker。ローカル数値/Python/import/AST/GAP、credential、
git/network/dispatch、追加agentは禁止。source編集/読取/bytes/hashだけ可。
既存のTask966修理が実走で必要になった場合はrootが優先順位と新委嘱を別に通知する。

Task971全文、reply970全文、Task957/958/963/964、工房2131/2138/2139の限定を読む。
source宇宙は同じQ2/六tag/四character、D=ker五carry＋二eta、8059 P1/96776 lower/
四B/48384 physical/Connで固定。producer971の新ABIは仕様として読めるが、その新算術helper
をimport/コピーしない。保持checker系統968/966の算術helperと別の動的wrapperを使う。

## 独立照合

1. 成功oracle completionと外部Eの**実観測された**run/head/artifact/entry pinから起動。
   値はrootが渡すまでmissing/fail-closed。元producer・completion・外部E・新runの由来、
   plain/generic-seal/rollingの型、original rho2のDERIVED親列をそれぞれ保存する。
   旧26scan/insertと外部Eは数値再走せず薄く取り込み、new completed_stepsを二重計上しない。
2. 新fixed owner/geometry/basis/reader bundleを独立に一度構成・認証する。旧snapshot付き
   geometry manifestを改称せず新固定manifestとのbytes結合を読む。可変q/chi/κ/P1値と
   cochain/treeを各snapshotのcurrent lambdaで必ず再計算する。
3. 各current oracleの全8059式、全score/f/aux、全54433 chord/二aux、tau rank/fit/residualと
   決定的ordered witnessを自系算術で再生し、全配列・JSON・manifest・dtype/shape/EOFを比較。
   ordinary27算術を実source/score経路へ接続する。現lambdaの非零characterが一つでも四つを
   登録して比較し、未計算/零/UNKNOWNを混同しない。
4. 各新EはTask966の独立RawSLP/Fox/source/primal/P1/four-B/physicalを動的stateへ繋ぐ。
   元rx/ryにはQ2 endpoint、rx³/ry³/commにはFox chain零。同じraw語のQ0/Q2/epsilon/omega/
   全六tag/四character/三grade/auxを読み、元lead昇順のprimalと全8059 mod54語法、全96776
   lower零、fresh tupleから一回のP1補正、四B合算、挿入順physical reductionと一回normalize、
   plain target scalar0、freshlambdaの全row/両targetを全数照合する。
5. phase/stepのowner/start/source/parent_state_head/rolling predecessorを独立に結ぶ。
   capは同outputの累積新E数、resumeで0へ戻らない。completed phaseの再利用は同snapshotの
   exact manifestを満たす場合のみ。未完phase、HEAD前のdurable tail、HEAD外のorphanを区別。
   root HEADが指す全new committed stepsとcurrent oracle/未完cursorを照合する。
   checker資源停止は最後の照合済みcursorを返し、未照合tailをPASS/受理に含めない。
6. complete-zeroは全oracle EOF＋現separatorの候補、target-zeroは線形候補＋Task958
   同一語11slot pending。最終MEMBER/NONMEMBER/全A0やLean verifiedを自動宣言しない。
   971のdynamic output型に合わせた新serializerを持ち、旧frozen helperのpinや定数を
   monkeypatchして別の対象に見せない。module progress/deadlineの接続は可。

## 新GHAと新interface gate

markerは `[r07-complete-oracle-cegar-continuation-v1-run]`、既存作業branchのpushまたは
workflow_dispatch。同時二発は禁止。実external E後の全parent tuple/source/dataをrootが
固定してからfreezeする。全CLI引数（oracle/e roots、candidate/report）、step名とterminal gateを
実schemaへ結ぶ。元workflowの旧marker/result gateを残さない。

- rootが渡す実14親artifactをlive metadata、archive/required fileのbytes/hash/roster/EOFへ結ぶ。
  旧Task554 runはfailureを正しく受理し、他親のsuccess/attempt/head/repository/pathを確認。
- 実import全sourceとraw3 dataをpin、ASTはGHA上でのみ実行。receiptに実本数と全由来を残す。
- **工房F-sc-3により、実使用するfrozen oracle checker v2の full `--selftest`を一回実行**。
  専用serialization15件PASSやv1旧receiptをこれの代わりにしない。過去全A–D本番は再走しない。
- 971/972の新interface canary（stale witness、cap持越し、phase→HEAD間停止/resume、plain
  target scalar0、target零≠MEMBER、source/owner変更）だけを両実経路へ繋ぐ。
- cap1の実新stepの後、まだSeparatorなら同outputを上位capでresumeする。rootが実E時間を
  見てcap/内部Meter/外側timeoutを指定するので、未観測の速度から値を予言しない。
  cap1時点でterminalなら余分なoracleを強制しない。checkerは全new prefixを独立照合する。
- 6h job/7GiB内で内側期限<外側期限<job期限、開始/EOF時刻とUNKNOWN位置を保存。
  producerの完了出力をcheckerの成否にかかわらずdiagnosticへ保存し、再走を不要にする。
  candidateは全新prefix checker PASS後のみupload。checkpointsとsource/runtime/launch receiptを
  明示し、verified=false/cross_checked=falseのまま工房CV9へ渡す。

F-sc-1のenvelope/words clone/context/transportと保持physical primitives、F-sc-2実零成分、
carry整合の片側性、q/κ第三未再計算等の限定は継承し、新wrapperで遡及解消したとしない。
残差分布から確率やphysical行独立本数を推論しない。必要な新型/ABIが971に未保存なら
具体的に問い合わせ、独立に完成できる関数を保存して進める。

root/Task973は完成blockを順次監査する。最終source/workflow/replyのbytes/SHA、実行コマンドと
new/retained TCBをreplyへ記す。最終行 `AUDIT_972_VERDICT:`、実走前はruntime未観測。
