# Luna reply 325 - task192 positive-only persistent parallel adapter v6

## Outcome and execution boundary

The complete task325 path is **IMPLEMENTED / UNEXECUTED** by static
inspection.  No Python, Node, GAP, GHA, workflow, network, or git command was
executed.  No task324 file or v1--v5 file was changed.

The implementation installs the commissioned hook at the authenticated
cached-v3 owner boundary.  It makes no retrospective epoch-replay claim.  A
production COMMON remains only a producer candidate until the v6 checker has
authenticated the physical inner cached-v3 COMMON receipt and obtained the
one exact full-line PASS from the pinned helper-nonshared cached-v3 checker.
All other production exits are typed UNKNOWN_INPUT or UNKNOWN_RESOURCE with
all seven claim flags false.

## Five authorized outputs

The four non-reply files were closed with these filesystem identities:

| path | bytes | SHA-256 |
|---|---:|---|
| `search/d972_r07_normalized_exact_common_word_positive_parallel_v6.py` | 127376 | `6f06465bc4599f91dee32ecab9624971c33461b12c7d38139684f578ee9d9218` |
| `crosscheck/check_d972_r07_normalized_exact_common_word_positive_parallel_v6.py` | 58516 | `fe3b83309eaff0531f0154a31a1a7a051171fb6a1ae8a3f706eb672f6659e47c` |
| `search/d972_r07_normalized_exact_common_word_positive_parallel_gha_driver_v6.g` | 10927 | `862ef3ea41b7683847f29a0cd1ddf7c95da8601c9dfdbabe3cf0cabc9d7898c5` |
| `search/certs/d972_r07_normalized_exact_common_word_positive_parallel_selftest_v6_20260828.json` | 4102 | `a6ade562478f86fcd986f119f4d349949c7a866332999acb1d9605a039fcb8ad` |

The fifth path is this reply.  Its identity is intentionally left for the
parent after close; it is not recursively self-pinned.

## Owner hook and unchanged mathematical owner

The concrete implementation points are:

- `PersistentProcessRoster` starts at producer line 818 and its atomic serial
  epoch method starts at line 1090.
- `_install_production_hook` starts at line 1632.
- The sole mathematical replacement is the subclass method
  `BoundaryDescriptorCache.correlation` at line 1647.
- The authenticated cached-v3 module binding is assigned at line 1707.
- Positive compact-view construction starts at line 1922; the outer
  production wrapper starts at line 1991.

The registered hook name is
`boundary_descriptor_cache_correlation_positive_parallel_v6`.  The subclass
inherits cached-v3 descriptor construction, translated-row caching, public
descriptor contract, and cache accounting.  Cached-v3/v1 remains the owner of
rank, echelon and dual updates, correction candidates, column retention,
COMMON construction, and ordinary checkpoint semantics.  No cached-v3 source
was copied, forked, or edited.

The v3 `PositiveSearch.__init__` owner authenticates inputs and replays a
resume before it constructs `BoundaryDescriptorCache`.  Therefore the hook
creates the one persistent Linux `fork` roster after authentication/resume
and before the first boundary epoch.  The same processes are retained across
serially dependent dual epochs; two dual epochs are never in flight together.

## Persistent epoch protocol

Each worker inherits once, through the initial Linux fork, the decoded group
runtime, the sorted descriptor tuple, cached `h`, cached `h_inverse`, group
handles, and the `(block,relator)` descriptor index.  An epoch frame contains
only:

1. the serial owner epoch context, epoch digest, and full dual digest;
2. the compact typed nonzero support buffer;
3. descriptor/support/prefix digests and exact pair count;
4. one disjoint half-open pair-index interval; and
5. the parent-owned deadline and cancellation event.

The typed support codec is a four-byte count followed by
`block:u16, component:u16, coefficient:u8, blob_length:u32, blob`.  Support
lists retain the live v3 dual insertion order.  Descriptor prefix offsets
give the exact v3 descriptor-times-typed-support global pair order; no
per-epoch pair roster is constructed or pickled.

Workers decode support once per epoch, stream only their interval, check
`t*h=g`, maintain a nonzero F3 sparse shard accumulator, and return a local
lexicographic winner and one local provenance witness.  The parent checks the
exact ordered cover and every epoch/dual/descriptor/support/prefix/result
binding, merges shard accumulators in interval order over F3, selects the
global `(block,t_blob,relator)` winner, reconstructs only that winner's full
v3 `contributing_pairs` through the resident relator index and O(1) typed
support lookup, calls the inherited translated-row owner, and requires the
direct v3 pairing scalar to equal the merged nonzero scalar.  The returned
dictionary is exactly the cached-v3 correlation ABI.  A zero-pair roster
returns `None`; a short roster dispatches only `min(pair_count,workers)`
nonempty intervals while retaining the same persistent roster.

## Deadlines, failures, counters, and cleanup

The parent waits on result channels, not a blocking `map`, unbounded `get`,
sleep loop, or polling loop.  Workers emit event-driven progress every 4,096
pairs.  A parent-owned shared exact-attempt array is updated after every
completed pair, so cancellation, timeout, EOF, or worker death does not lose
the final partial chunk.  Parent and child `/proc/<pid>/status` RSS values are
sampled on startup and on every progress/result event and checked as one
aggregate against the owner cap.

Every epoch records attempted, committed, discarded, and retried pairs and
epochs.  The staged checkpoint seeds attempted pairs from the cumulative v3
monitor, committed pairs from the safe boundary progress, and the 640-pair
interrupted suffix as discarded/retry-pending work.  Historical fields are
immutable lower bounds and all conservation equations are checked.  A
successful whole merge is the only commit.  A failure is charged and discarded
in full and has no retry loop; the next same-adapter resume begins at the clean
atomic boundary.

Production retains only `last_epoch` plus a SHA-256 epoch chain, never an
unbounded epoch list.  Serialization byte totals, maximum frame/in-flight
bytes, aggregate RSS peaks, RSS samples, worker restarts, and checkpoint bytes
are live fields.  Worker restarts are fixed at zero.  Cleanup records exactly
`started -> closing -> joined` or
`started -> closing -> terminating -> joined`, with a required empty live-PID
list.

UNKNOWN_RESOURCE binds exactly one canonical, resealed cached-v3 checkpoint
sidecar augmented by `positive_parallel_v6`, source-pin digest, counters,
last epoch, and `next_clean_epoch={atomic_cursor:0,prefix_accumulator:[],
partial_epoch_committed:false}`.  Its physical bytes are solved to a fixed
point in both the v3 monitor and adapter byte counters.  The same producer
accepts that sealed JSON on resume.  COMMON deletes/forbids every checkpoint
and binds exactly one physical inner-v3 sidecar.  UNKNOWN_INPUT has neither
sidecar.

## Positive receipt and independent checker

For COMMON, the outer receipt binds the inner relative path, basename, bytes,
SHA-256, v3 self-digest, v3 schema/terminal, and four immutable cached-v3
source pins.  Its compact view is derived from the inner receipt and contains
literal `c_star`, solution coefficients, exact words `c_star/v0/u0/h/c_exact`,
registered relators 3/9/12, all exponent data, A/B, joint-kernel replay,
direct all-seven replay, and explicit typed boundary coefficient ancestry.

The v6 checker does not import the v6 producer.  Its exact positive call site
starts at checker line 494; positive checking starts at line 516.  It first
authenticates current sources and physical inner bytes/seal/path.  It then
invokes exactly once:

```text
[sys.executable, -B, crosscheck/check_d972_r07_normalized_exact_common_word_cached_v3.py, <physical-inner-path>]
```

Acceptance requires return code zero, empty stderr, and stdout consisting of
the single exact line:

```text
R07_NORMALIZED_EXACT_CACHED_COLGEN_V3_CHECKER_PASS terminal=R07_NORMALIZED_EXACT_CACHED_COLGEN_V3_COMMON_WORD
```

Only after that gate does the v6 checker independently derive and compare the
compact view.  Nonpositive checking starts at line 584 and contains no v3
checker call: it checks transport, seal, pins, typed reason, counters,
resources, cleanup, checkpoint binding when required, and the all-false claim
vector.  SELFTEST likewise makes zero actual cached-v3 checker invocations.

## SELFTEST universe and mutation ledger

The fixture carries no copied expected F3 scalar.  Producer and checker derive
them separately in canonical C17.  The bounded real-process SELFTEST covers
empty descriptor, one descriptor, empty support, short support,
single-descriptor concentration, multi-descriptor cancellation with and
without an active key, the v3 lexicographic winner and indexed local
provenance, three distinct serial dual epochs through one persistent roster,
worker counts 2 and 4, exact cover/parity, live timeout, worker death, partial
return, and joined cleanup.  Synthetic positive and UNKNOWN_RESOURCE
envelopes exercise the transport gates without loading the large v3 runtime.

All 22 mutations are independently regenerated, materially changed, resealed
where applicable, and required to fail at the exact fixture stage/reason:

| mutation | exact stage | exact reason |
|---|---|---|
| wrong_dual_digest | `kernel.dual` | `dual_digest` |
| noncanonical_c17_alias | `kernel.dual` | `noncanonical_c17_codec` |
| missing_interval | `kernel.cover` | `missing_interval` |
| overlapping_interval | `kernel.cover` | `overlapping_interval` |
| changed_accumulator | `kernel.merge` | `accumulator` |
| changed_winner_provenance | `kernel.winner` | `winner_provenance` |
| changed_direct_scalar | `kernel.scalar` | `direct_scalar` |
| cross_epoch_result | `kernel.epoch` | `cross_epoch_result` |
| partial_return_accepted | `kernel.completion` | `partial_return` |
| child_left_alive | `cleanup` | `child_alive` |
| counter_reset | `counters` | `historical_reset` |
| unbound_checkpoint | `resource.checkpoint` | `checkpoint_binding` |
| changed_inner_receipt_digest | `positive.inner` | `inner_digest` |
| fake_v3_checker_terminal | `positive.checker` | `checker_terminal` |
| compact_view_mismatch | `positive.compact` | `compact_view` |
| positive_claim_on_resource_exit | `resource.claims` | `positive_claim` |
| separator_flip | `claims` | `separator` |
| cofinal_flip | `claims` | `cofinal_lift` |
| fake_flip | `claims` | `fake` |
| ihara_flip | `claims` | `ihara_witness` |
| terminal_reseal | `envelope` | `terminal` |
| stale_output | `transport` | `stale_output` |

## Present-shape hot-path accounting

The authenticated staged shape has 104 descriptors and 1,188 typed support
entries, hence the conservative present-shape maximum is
`104 * 1,188 = 123,552` expanded pairs in one fixed-dual epoch.  The 1,188
current typed element blobs are each 40 bytes, so the exact compact support
buffer is `4 + 1,188 * (9 + 40) = 58,216` bytes.  Approximately 2,896 rank
epochs are already represented as discovery state; they are not replayed or
asserted as proof history.

Resident once per worker: the forked decoded group runtime, 104 descriptor
templates, cached descriptor elements/inverses, group handles, and the
relator index.  Sent per epoch: one 58,216-byte present-shape support buffer
per active worker plus constant identity/interval metadata; no descriptor or
pair roster.  The explicit caps are 1 MiB for support, 32 MiB per request or
result frame, and 256 MiB total epoch serialization, with exact observed bytes
recorded.  At worker count 2/4 the raw repeated support payload is only
116,432/232,864 bytes before the bounded constant pickle envelope.  A worker
holds at most its interval accumulator; the parent holds the bounded shard
results and one merged accumulator.

The parent performs five bounded logical scans: one dual-to-typed-support
extraction, one compact support decode/prefix construction, one ordered shard
cover/merge scan, one merged-key winner scan, and one selected-relator bucket
provenance scan.  It never scans all expanded pairs.  Provenance lookup is
`O(number of descriptors in the selected (block,relator) bucket)` with O(1)
typed `(block,component,g_blob)` lookup.  The unchanged v3 owner then performs
its translated-row construction and direct sparse pairing.

Process count is exactly one parent plus W persistent workers, W in `{2,4}`;
only `min(N,W)` workers receive a short epoch.  There is no per-epoch pool,
full-roster encoding, repeated descriptor group decoding/inversion, linear
support provenance rescan, flattened ancestry growth, sleep/poll loop,
unbounded result history, or unbounded retry.

## Exact import and subprocess graph

- Producer imports only Python stdlib modules.  It authenticates and loads
  `search/d972_r07_normalized_exact_common_word_cached_v3.py` into the private
  module slot `_d972_task325_cached_v3_owner`; cached-v3 then uses its existing
  authenticated v1 loader.  The producer launches no subprocess.  Its only
  children are the dedicated `multiprocessing` fork workers running the
  top-level `_worker_main` from the already authenticated module image.
- Checker imports only Python stdlib modules, never the producer.  SELFTEST,
  UNKNOWN_INPUT, and UNKNOWN_RESOURCE launch no subprocess.  COMMON launches
  the pinned cached-v3 checker exactly once as shown above.
- The ASCII GAP driver creates one bash script, invokes one v6 producer and
  then one v6 checker, requires exact-one terminal markers and byte-equal
  terminal payload sidecars, and never invokes another v6 producer/checker.

## Immutable source pins

| label/path | bytes | SHA-256 |
|---|---:|---|
| task325 commission | 10172 | `b22813ff9aa5250af25412db34f98b6e40996115a874e895b497e3a8753e4bf8` |
| positive-only v140 | 10073 | `6d388a74c75d55d215b0035496c451aa9de5bbc7a8248c277e76021092b8562b` |
| history-free v265 | 10122 | `fd30ccb2458691ec7844d304f220a4be7d704259318c452f928f8088552ecb0a` |
| resume semantics v253 | 4110 | `a1d9f6d3d8cb31d8b261dd5cb1977865abfeba24bfc6aae7436d2a893e3ef19a` |
| map-reduce v254 | 6195 | `e9fc7a69525200e8e1c0e8152652229227877ba923378ade8afa199c4f4ee1a0` |
| adapter state v255 | 8814 | `06c93c46b48b681e0316d302058b72bc0b76fe9d12888cde3f7e45dc3a93ffa0` |
| counters/pool v256 | 4790 | `f5a0c6e625e5113e4213b62762267fc9a5437cafd9f9751e603b055c549c1251` |
| task319 audit reply | 20604 | `9b9908eadf0f8c8204f9397d2af0511ba98959a979a00058c7b28cae9c74f981` |
| task321 commission | 9128 | `681b7a1a4b8edcd6f788f8d01aca930d60f3e61330293e70a4db47df205d2cc9` |
| task321 reply | 12122 | `bd8104b462f35979af2fd2ee820ad08a1c1c165cac6e2558a8e4eba6e7946c8b` |
| task303 producer/checker/driver/fixture | 39234 / 32486 / 7971 / 1195 | `19a2970fcf072c25c606d0305fd999c8481353e0be20879de4be2aa26f6fb90c` / `530d67c854017a538fa2185b8bc5c48834a785f5bd6db38452db3551695cf1df` / `0ac1b26d1844fdc16cc2701c536f50fd5415a7ef2479e030ebde96af79af4902` / `4d481ba84e3c452c79f344e66a0eea5322ec8b64c15a81f1a290c22ce18e3fc9` |
| cached-v3 producer/checker/driver/fixture | 193704 / 154009 / 11548 / 276 | `f27b4971351832b8730fb8cce4e782e893a958dfb850203cc735c7bc3aa31f37` / `dfc8cbbd96a1da45f15e01607ed343b66a78a7201f4a80952fba33aaeb361e10` / `2f7ff7b459e46d014268907ff5ba5f03c035836e8f8df79a2c5f4cdc3b75351d` / `c49f434ad3daf1cc661ba45563dbb9557d436f91dca78c8ee0f47ed70332da12` |
| task298 driver/reply | 19682 / 9200 | `169da7aa149d68907abb435f380b9ec2994c2bc285c6a17f13431614a388f5ad` / `732c9b1d279e9201d4cce3b432b5a4805a60d346d6104865246ce0a3030af22f` |
| staged checkpoint ZIP | 5001811 | `f3ac82a04907983d987cc2a42d06fe3b612ec2040555f40be81200969358f566` |
| staged checkpoint manifest | 1328 | `6911dfe822662a17ae95c896f97573e553d15325631f1606bd0bf7f550e88302` |
| raw checkpoint member | 86368039 | `c261aa967867a4870228eae467f46ee4afbfc236445890debd891bcef4a250ab` |

The driver pins the current producer/checker/fixture and every row above.  It
stale-rejects all task-owned v4--v6 output prefixes, requires nonempty sealed
receipt/verdict/logs and exact terminal sidecars, enforces COMMON/resource/
input sidecar exclusivity, and writes the sole sentinel last.  It contains no
recursive driver self-hash.

```text
IMPLEMENTATION:                    IMPLEMENTED
EXECUTION:                         UNEXECUTED
ACTUAL PERSISTENT PRODUCTION:       UNEXECUTED
RETROSPECTIVE EPOCH REPLAY CLAIM:   NONE
ACTUAL A0 COMMON + CHECKER:         0/1
SEPARATOR / NEGATIVE CLAIM:         FORBIDDEN
LIFT / FAKE / IHARA:                NONE
```

TASK325_R07_TASK192_POSITIVE_ONLY_PERSISTENT_PARALLEL_V6
