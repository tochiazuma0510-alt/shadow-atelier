# Task961 — v548 complete scalar oracleの限定source監査

役: Luna / read-only数学・source監査。変更可は
`sol/luna_reply_961_r07_section_cochain_oracle_audit.md` のみ。
新sourceはTasks959/960の指定producer/checker、workflowのrelease確認はroot担当。
ローカル数値/Python import/AST/GAP/network/git/credential/dispatch/追加agent禁止。
source/JSON/byte/hashを読み、指定返信で報告する。実装は編集しない。

reply957の正式DESIGN_COMPLETEをrootが全文読みA–Dの数学ABIとして受理した。
その幾何、元lead順、全source pullback、tree/carry、完全zero判定を適用する。
新parentのrun33967668257/1は13:34:06Zから独立checker中。exact結果/pinは
rootが後送する。未観測のrank/terminal/sourceSHAでPASSを先取りしない。

sourceの完成済みblockから読んでよい。最終判定は全sourceと実parent layout、
rootが確認する固定byte/SHAが揃ってから。新しい真のblocking defectだけを
直ちに各作者とrootへ通知する。body/配列ABIの非算術部分は共有してよいが、
相手の新算術をそのまま渡さない。

必須焦点:

1. Q2 right edge / tagged Fox left multiplication / qnorm right X,XB、全六tagと
   actual transport、mixed degree0/1/2、shared eta。finite27等のchecker別経路が
   実source-edge算術に接続されているか。
2. 実fuda36点marking→mod9→rotation-left carry、全108864edge/phi整合、BFS/tree/
   全54433chord、五tau独立列の決定とfit。基底不足/部分EOFを零にしない。
3. joint kappaの新d1→旧d0/sharedaux、元lead降順、rowID/chi添字、free0、
   全8059最終等式。元normalizedrowに生成scaleを二重適用しない。
4. 全score/f/aux/chord arraysの独立再計算とbyte比較、falsezero/後端改変拒否。
   非零は最初のauxまたは高々6cycleでtau零とscalar非零、materialization pending。
5. 新accepted snapshotはmetadata/rows/targetを読み、旧scan/insertを再生しない。
   currentlambda全行/target dots、原rho2明示DERIVED、source/Conn/P1受理前提を保持。
   対象lambdaがないMemberは入力型不一致。古いλや零character固定は使わない。
6. whole output seal/roster/runtime pin、deadlineはUNKNOWNのみ。完走前の
   candidate/negative、工房CV9前のself cross-checked、Lean verifiedを拒む。

旧成功suiteの再実行や第三全pipelineは追加しない。必要canaryは新interfaceを
狙う少数。数学の保持前提、新TCB、未実行gateを明記して最終返信する。
最終行 `AUDIT_961_VERDICT:`。
