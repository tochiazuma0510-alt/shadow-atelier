# R07 post-endpoint pro-3 side-gate collapse v248

Author: Sol / 2026-08-28

Status: paper composition theorem after v174, v191, v197, v228, v94, and
v98.  It identifies which A9 predicates are automatic for the one
word-bearing Neumann multiplier and which nonlinear predicate remains.  The
actual task192 word, A5/A6 multiplier, and A7 endpoints have not all been
accepted.  No compatible lift, fake certificate, or Ihara witness is
declared.  `verified=false`.

## 1. The actual admissible normal subgroup

Let

\[
 F=F(x,y)
\tag{1.1}
\]

and let \(\Psi\) be the finite product of the frozen roof, marking, and
already-settled finite evaluation maps which the first correction is required
to preserve.  Put

\[
 U=\ker\Psi,
 \qquad C=U\cap[F,F].
\tag{1.2}
\]

Both \(U\) and \([F,F]\) are normal in \(F\), hence so is \(C\).  At a
registered relative pro-3 quotient let \(A_n^{\rm com}\) be the image of
\(C\) in the actual linear correction module, and put

\[
 A^{\rm com}=\varprojlim_n A_n^{\rm com}.
\tag{1.3}
\]

The word ``actual'' is load-bearing.  An ambient group-algebra vector with
zero exponent is not substituted for an element in the image of the common
word subgroup \(C\).

An accepted task192 production receipt is required to replay for its exact
correction word \(a\)

\[
 \boxed{a\in U,\qquad \operatorname{exp}(a)=(0,0),
        \qquad a\in C.}
\tag{1.4}
\]

The current implementation constructs the exactified word with exponent
zero, but (1.4) is stated here as an authenticated production premise rather
than inferred from a SELFTEST.

## 2. Context action cannot leave the admissible commutator domain

Let \(\Xi=\mathbf F_3[[\Delta_\infty]]\) be the completed diagonal context
algebra of v174 and let its action on corrections be the retained literal
source-conjugation action.  Every finite coefficient

\[
 \lambda=\sum_i b_i g_i\in\mathbf F_3[\Delta_n]
\tag{2.1}
\]

acts on a word-bearing correction value by a finite product of the retained
conjugates of that value, with exponent \(b_i\in\{0,1,2\}\).

### Lemma 2.1 (ADMISSIBLE ORBIT CLOSURE)

If \(a\in C\), then

\[
 \boxed{\overline{\Xi a}\subseteq A^{\rm com}.}
\tag{2.2}
\]

Every finite word-bearing value \(\lambda a\) is represented by a word in
\(C\).  In particular it is invisible under every map in \(\Psi\) and has
exact exponent vector zero.

#### Proof

For every source word \(G\), normality of \(C\) gives

\[
 GaG^{-1}\in C.
\tag{2.3}
\]

The subgroup \(C\) is closed under products and inverses, so the finite
word materializing (2.1) belongs to \(C\).  Reduction to every registered
finite correction module therefore lies in \(A_n^{\rm com}\).  Passing to
the inverse limit and then to the closure proves (2.2).  Membership in
\(U\) gives invisibility under \(\Psi\), and membership in \([F,F]\) gives
the exact exponent assertion. \(\square\)

Thus the correction-domain half of v174's side hypothesis is not a new
all-rung membership problem once the actual task192 word is bound to (1.4)
and the A6 action is replayed as literal source conjugation.  It is a finite
typing check on the retained words.

## 3. Neumann partial corrections preserve roof, mark, and charmingness

Let the accepted A6 polynomial be

\[
 M=\sum_{i=1}^t b_i(U_i-V_i),
 \qquad \pi(U_i)=\pi(V_i),
\tag{3.1}
\]

and let \(\mu\in\mathfrak j\subset\Xi\) be its compatible image.  Suppose
the three exact A7 endpoints vanish.  V197 then supplies finite boundaries,
and v228 gives the completed pointed identity required by v174.

For \(N\geq1\), put

\[
 q_N=\sum_{r=0}^{N-1}\mu^r a,
 \qquad c_N=-q_N.
\tag{3.2}
\]

### Theorem 3.1 (LINEAR SIDE-GATE COLLAPSE)

Under (1.4) and the literal word-pair/action binding above, every finite
partial correction in (3.2) has an actual word-bearing representative in
\(C\).  Consequently, for every \(N\),

\[
 \boxed{
 \Psi(c_N)=1,
 \qquad \operatorname{exp}(c_N)=(0,0).}
\tag{3.3}
\]

The inverse-limit correction \(c_\infty\) is therefore roof/mark preserving
and lies in the closed commutator subgroup.  On the matched relative pro-3
Frattini lane, the onto/automorphism gate also persists without a new
stagewise search.

#### Proof

Each \(\mu^r a\) is a limit of finite context-algebra actions on \(a\).
Lemma 2.1 puts every finite reduction in the actual image of \(C\).  Finite
sums are materialized by the ordered nested-word construction of v98 and
remain in \(C\), proving (3.3).  Cofinal nilpotence of \(\mathfrak j\),
proved in v173 and used in v174, makes (3.2) finite at every fixed quotient
and gives the compatible limit.

At the pinned E4 base the source tuple is onto.  Every matched pro-3
transition is a Frattini cover by v94 Theorem 3.1, and the correction is
invisible in the already fixed coarse coordinates.  V94 Theorem 2.1 then
propagates generation, which is the required onto/automorphism assertion on
this lane. \(\square\)

The theorem does not use a rung-by-rung choice of unrelated \(c_n\).  The
same finite polynomial \(M\), its ordered powers, and the one word \(a\)
determine all finite partial corrections.

## 4. The nonlinear predicate which is not removed

Equations (3.3) do **not** say that materializing the module value \(c_N\)
automatically preserves the exact nonlinear GT relations.  For every finite
partial correction one must still directly replay, in the registered word
order,

\[
 \boxed{
 \text{hexagon H1}=1,qquad
 \text{hexagon H2}=1,qquad
 \text{printed pentagon P}=1.}
\tag{4.1}
\]

The A7 endpoint identities and A8 Fox boundaries prove the completed
**linear** multiplier equation.  They do not identify the nested word
materialization with a solution of (4.1) without this replay.  A bounded
failure to find or normalize a word is `UNKNOWN_RESOURCE`; a literal
nonidentity in (4.1) rejects that partial construction but is not a fake
theorem for the whole roof fibre.

Formation support at mixed-prime solvable edges belongs to v220 B, and new
perfect-core accepted sets belong to v220 C.  Neither is silently counted as
an A9 pro-3 side gate.

## 5. Consequence for the v220 route

After an actual positive A7:

1. A8 is a total certificate extraction, not another existence search:
   v197 compiles \(q_{H1},q_{H2},q_P\) from the three zero endpoints.
2. The pointed Neumann formula and all-rung linear descent in A9 follow from
   v174/v228 with the same \(M\).
3. The actual A9 side-gate receipt need only authenticate (1.4), the literal
   action/materialization used in Lemma 2.1, and the nonlinear word replays
   (4.1).  Roof/mark, exact charmingness, and pro-3 onto are then consequences,
   not repeated searches.

This changes no current actual numerator.  It removes an apparent family of
later side-gate searches and isolates (4.1) as the remaining pro-3 nonlinear
check.

## 6. Fixed frontier

```text
ACTUAL-COMMUTATOR DOMAIN STABLE UNDER CONTEXT ACTION: PAPER PROOF
NEUMANN PARTIALS PRESERVE ROOF/MARK/EXACT EXPONENT:   PAPER PROOF / ACTUAL BINDING PENDING
ONTO THROUGH MATCHED PRO-3 FRATTINI LANE:             PAPER PROOF (v94)
A7 ZERO -> A8 BOUNDARY EXTRACTION IS TOTAL:           PAPER PROOF (v197)
A7+A8 -> POINTED ALL-RUNG LINEAR DESCENT:             PAPER PROOF (v174/v228)
NONLINEAR H1/H2/P WORD REPLAY FOR NEUMANN PARTIALS:   OPEN
ACTUAL A5/A6 M AND THREE EXACT A7 ENDPOINTS:           NOT COMPUTED
MIXED-PRIME FORMATION / PERFECT CORE:                  OPEN (B/C)
COMPATIBLE WITNESS / FAKE / IHARA:                     NOT CONSTRUCTED
```

`R07_POST_ENDPOINT_PRO3_SIDE_GATE_COLLAPSE_V248_PAPER_GRADE`
