# Task976 — 新継続器のcomplete-zeroから当該grade NONMEMBERへの数学監査

役: 数学監査・read-only source照合。変更可は
`sol/luna_reply_976_r07_complete_zero_terminal_math_audit.md` 一ファイルのみ。
公刊973を含むsource/workflow/返信は不変。ローカル数値/Python/import/AST/GAP、
network/git/credential、追加agentは禁止。source/紙/metadata/hash読取のみ可。

実run33984832010/1、launch b8c9e95ddd0183d9e43b7fcc961cb251fdaea13e は
cap1/resume32 success、18:56:51Zから独立全prefix checker中。
新terminal/rank/complete-zeroは未観測。rank1386/gen8091が受理済み起点。
本便は負の結果を推定する仕事でも実GHAを止める条件でもない。

v548全文、v547と2144 signed規約、reply957/964/970/973、凍結新P/Cの
complete-zero/current-result/HEAD/全prefix gateを読み、次を式と実字段に落とす。

1. 固定Q2全域・全四character・五carry kernel＋二etaの完全EOFから、
   lambda G R Psi=0が全M2上の零へ至る正確な命題。Conn/P1/source-map/linear imageの
   どの保持前提をどこで使うか、有限complete範囲以外への飛躍がないかを監査する。
2. current lambdaの全保存row消去、current targetとの積1、DERIVED original rho2との積1を
   既往target差分＋全新target差分で結ぶ数学的帰納。current snapshotとHEADの結合、
   witnessなしとfit全EOFの結合を、凍結checker実codeのgateごとに確認する。
3. complete-zero候補＋実checker全prefix PASS＋工房CV9が得られたと仮定して、
   当該grade NONMEMBERへ上げるためにまだ独立照合が必要な対象を最小限列挙する。
   2131/2138/2143のTCB・第三再計算不足と、今回新たに実測する範囲を区別する。
4. 非零omega実例や同一語11slotが負判定の追加必須条件なのか、正判定だけの条件なのかを
   紙上の論理から判定する。未実行positive条件を自動的にNONMEMBER gateへ加えない。
   一つのH/gradeの否定と全A0/無限cofinal/Lean verifiedを分ける。

可能な結論の短い正式文案と、絶対に判定を上げてはいけない反例（stale lambda、
欠落四scope、current checkpoint不一致、無効なDERIVED join等）を末尾に置く。
既存票の長い再録は避け、新規差分と実code行・式を優先。保存後bytes/hashをrootへ送る。
最終行 `AUDIT_976_VERDICT:`。
