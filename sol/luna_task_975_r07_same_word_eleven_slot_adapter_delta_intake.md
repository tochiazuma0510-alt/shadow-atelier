# Task975 — 同一ordered SLPの直接11slot adapterを具体化する差分intake

役: Luna checker側の限定read-only intake。変更可は
`sol/luna_reply_975_r07_same_word_eleven_slot_adapter_delta_intake.md` 一ファイルのみ。
公刊source/workflow/返信を変更しない。ローカル数値/Python/import/AST/GAP、
network/git/credential、追加agentは禁止。source読取・metadata/bytes/hashのみ可。
producerの新算術helperを読取/import/コピーしない。公開ABIと数学正本だけ共有する。

実継続run33984832010/1、launch b8c9e95ddd0183d9e43b7fcc961cb251fdaea13e は
cap1/resume32 success、全prefix checker進行中。新terminal/rankは未観測。
受理済みrank1386/gen8091、grade2未決。本intakeは実走の追加待ち条件ではない。

reply958 F3/F4は既済の前提として全文読み、同じ指摘を再列挙せず、Dの具体的欠品を解く。
v518と必要なv511/v516、現v9 endpoint runtimeと自系独立checker群を静的に辿る。

求める成果:

1. 現ownerとexact fresh-rho2 parentに合う、11slotのtyped endpoint/left Fox action/
   printed aggregationを実行する独立checker側の実source・関数・データ契約を同定する。
   producer-only ProducerAllSevenを新独立checkerとして呼ぶ案は不可。
2. `Rel / Act / ordered Prod / Inv / prior Ref / identity` のone-root DAGをflat展開せず、
   D(UV)=D(U)+ev(U)D(V), D(U^-1)=-ev(U)^-1D(U)で直接再生するため、
   各slotのtyped endpoint、Fox row key、prefix/occurrence/sign、inverse/actionのABIを示す。
   10unique coordinate→11occurrenceを保持し、同じroot hashから全slotを評価する。
3. 既存physical四Bとdirect11slot/printed PB-A18 aggregationのowner joinを明記する。
   六source tag、source lower96776、physical lower32260/top48384、endpointだけの一致を区別。
   今回の直接rho2 parentとDERIVED receiptの区別、必要artifact/source closureの最小一覧を出す。
4. endpoint演算だけ完成してFox/aggregationが未実装なら、実関数と欠けた操作を明記する。
   次のversioned実装をA（typed DAG evaluator）/B（direct printed aggregation＋rho2比較）の
   どこで切れるか、誤ったsort/inverse/duplicate coordinate/owner mixを拒否するcanaryを具体化。

Task974はtargetのordered compiler差分を担当する。連絡が必要なら公開ABIだけ共有する。
新数値/実走見積り/未観測MEMBER/full A0/verifiedを主張しない。
source追跡と必要欠品を優先して簡潔に保存し、実bytes/hashをrootへ通知。
最終行 `AUDIT_975_VERDICT:`。
