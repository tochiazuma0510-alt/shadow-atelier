# Task923 -- workflow for the v541 corrected root scalar batch

Role: implementation. Process all sections and write only
`.github/workflows/d972-r07-actual-grade2-root-scalar-batch-v2.yml` and
`sol/luna_reply_923_r07_filtered_root_scalar_workflow_v2.md`.
The researcher's standing authorization is to run the necessary A0
production work on GHA. Root alone commits/pushes/dispatches.

## 1. Start from the actual completed workflow

Copy/adapt `.github/workflows/d972-r07-actual-grade2-root-scalar-batch-v1.yml`.
Keep all actual P1, Task554, Task712 and separator artifact identities and
live metadata gates. Run33903333330/1 succeeded mechanically, but its direct
scalar formulas were wrong; v541 repairs them. Existing parent data remain
valid. Read v541 and task922 before changing the stager.

## 2. Version and stage the exact new lower inputs

Use producer/checker root-batch v2 paths, v2 schema/labels and a distinct
push marker `[r07-actual-root-scalar-batch-v2-run]`. Preserve dispatch and
the fixed sol branch. Producer/checker pins will be supplied by Task922;
use explicit pending placeholders until they arrive, then freeze real hashes.

The new arithmetic needs existing Task554 referenced blobs as well as the
five body/head pairs. There are eight prepare old-row blobs (four lower
width6056 and four grade companion width72576) and four new basis blobs
(width18144), total67011332 raw bytes. The current artifact downloads already
include these; preserve them and put exact body-authenticated receipts in
each state's `files` roster. Coordinate descriptor field names and order
with `/root/task920_seed2_materializer`, which is implementing task922.

Use fixed body hashes before interpreting blob descriptors and safe path
handling. Do not rebuild any parents, expand the full DAG or introduce a
new provider/receipt hierarchy. Keep producer/checker runs serial in a job.

## 3. Real production and useful output

Keep bounded selftests followed by the actual complete32280-origin root
scan and its independent checker. Retain progress logs and candidate upload
only after checker success; diagnostic upload should preserve useful output
on failure. Do not add expensive audit jobs or a synthetic-only success path.
Use existing time/memory settings unless a concrete issue requires change.

## 4. Delivery

No local Python while Task922 owns the local execution slot. Static YAML
and schema review are enough until code hashes arrive. No git, credentials,
workflow dispatch or external messages. Report byte/hash receipts and exact
run marker/dispatch command for root. Once code is available, verify paths,
schema and the extended body/blob roster agree; do not broaden auditing.
`verified=false`; no A0 or Grade2 result from workflow readiness.

## 5. Root handoff: parallel checker completion (2026-09-05)

To avoid serializing the remaining checker integration behind the producer,
root additionally authorizes Task923 to finish
`search/check_d972_r07_actual_grade2_root_scalar_batch_v2.py` after Task922
confirms a saved handoff boundary. Task922 stops checker edits and finishes
only the producer. Preserve the completed independent checker kernels;
complete its state fold, exact output reconstruction and bounded canaries
according to Task922's specification. No new framework, no producer-kernel
import, no parallel local Python. Record checker hash/readiness in this
reply as well as the workflow freeze. Final independent audit remains Sol(max).
