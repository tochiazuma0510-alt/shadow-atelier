# Luna task 506 - rank99 nonzero-constant literal global prefix v6

Role: Luna implementation only.  Implement the minimal v431 successor of the
audited rank99-v5 owner.  Do not edit v5, change the finite universe/search
mathematics outside the nonzero-constant branch, run production/GHA/git, add
full Q0 stores, or create a cache/framework.

Read `sol/proof_r07_rank99_tau_free_nonzero_constant_global_prefix_v431.md`
in full.  Its exact pin is 9,592 /
`7b08f2526b00f4b12e67b9de57e03b7e87936050bfe8c3f9200130ed1ef850a4`.

## 1. Outputs only

Create only:

1. `search/d972_r07_a0_dual_anchored_rank99_durable_discovery_v6.py`;
2. `crosscheck/check_d972_r07_a0_dual_anchored_rank99_durable_discovery_v6.py`;
3. `search/d972_r07_a0_dual_anchored_rank99_durable_discovery_gha_driver_v6.g`;
4. `sol/luna_reply_506_r07_rank99_nonzero_constant_global_prefix_v6.md`.

## 2. Frozen owner

Transform only this independently audited trio:

```text
search/d972_r07_a0_dual_anchored_rank99_durable_discovery_v5.py
  104031 25c308ec11b9f36cc9779dfec46058a4956068969d664ee582a26f9cb0db7c09
crosscheck/check_d972_r07_a0_dual_anchored_rank99_durable_discovery_v5.py
  71589 970ffe3a78687f3a27a222e089ae3d5e928bbfa048b9aef9f51fcf4c0b5d578d
search/d972_r07_a0_dual_anchored_rank99_durable_discovery_gha_driver_v5.g
  9425 bed9105b36fef5e59120d954029ec507b16f393ab2859a7599867a19156b1b5d
```

Preserve the audited v5 compiled/raw formula repair, C99 identity, v424
literal gates, action-first and printed-seed order, v426 flat chain, v427
variable 1--16 row close/rollback, D1--D6 repairs, resource caps, positive
COMMON checker boundary, and every existing support-fibre cursor.

## 3. One new positive branch

Delete only the global `not any(f["K"] ...)` stop.  For a formula with
`K != 0`, compute

```text
W = sum(sf.kernel_orders[coordinate] for (coordinate,target) in formula["merged"])
```

and require all coordinates are still 0--2 and `W < 357128352`.

If the current batch already has certified `rows`, stop the candidate scan,
close that ordinary variable-length batch through the existing v427 path,
update the dual once, and restart the outer round.  Do not scan a nonzero-K
formula under a nonfresh anchor.

At a fresh anchor, scan cursors `0..W` in order.  For each cursor use
`qid,gid=divmod(cursor,243)`, construct the literal word from the retained
Gamma section word and Q0 parent/letter section word, and directly evaluate
all ten coordinates.  Do **not** call the inherited historical
`sf.global_candidate`: the selective runtime intentionally has only three Q0
stores.  Do not build the other seven stores or another BFS.

Evaluate the audited compiled formula on the ten direct blobs.  Skip zero.
For the first nonzero scalar, use a new typed global-selector wrapper around
the unchanged literal/physical core: replay the occurrence row, nonmutating
reduction, literal conjugate and fresh seed equality, exact exponent,
forbidden-E, complete coordinate tuple, direct physical scalar, row digest,
predicted pivot and one add.  Require a rise, append exactly one record, close
that one-row v427 batch, update the dual and restart.  The theorem guarantees
that a fresh-anchor `0..W` scan cannot end without a rise; otherwise fail
closed as an invariant error, never a negative.

Use the disjoint record cursor

```text
["global_nonzero_constant", seed_index, cursor, W]
```

and leave the old integer-first support cursor byte-for-byte unchanged.
Producer and checker dispatch by the first entry.  A global record must carry
enough literal replay data to bind seed, word, cursor, `qid+1`, `gid+1`, `K`,
`W`, all required coordinates, scalar, row and pivot without trusting a
cached tuple.

## 4. Preserve the running v5 prefix

V6 production resume accepts exactly canonical C99, a closed exact v5
checkpoint, or a closed own-v6 checkpoint.  It must not require a restart
from C99 after the currently running v5 job.

Validate a v5 checkpoint under frozen v5 schema/binding/seal, rows, batches,
segments, rolling prefix and ledger before migration.  Migration changes only
top-level schema/binding and recomputes the top-level state seal; it changes no
row, batch, segment, profile, cursor, prefix/ledger digest or historical
identity.  The physical v5 file identity becomes the input identity of the
first new v6 segment.  Do not reopen historical ancestors.

The independent checker implements legacy-v5 validation and migration
separately.  It may exact-pin frozen arithmetic modules but must not import or
call the producer's resume/global-selector/seal/fixture helpers.

## 5. Checker and driver

The checker reconstructs a global cursor's `qid,gid`, word and all ten direct
coordinates, independently evaluates the compiled scalar including `K`, and
then runs every existing literal/physical/batch/segment check.  Reject wrong
cursor decomposition, word, any coordinate, `K`, `W`, scalar, rank/pivot,
legacy migration, or prefix.

Transform the v5 driver only for v6 paths, exact new pins, schema/markers and
external preamble.  Preserve the canonical configurable `search/certs/*.json`
input, one producer, conditional one checker only for COMMON, RESOURCE
candidate transport, fresh paths, no SELFTEST before production, and exact
limits/margins:

```text
14040 < 14220 < 14400 seconds
4200000000 < 4500000000 < 5120000000 bytes
ulimit -v 5000000 KiB
```

Use external preamble
`D972_R07_A0_DUAL_ANCHORED_RANK99_DURABLE_DISCOVERY_V6_RUN` and a matching
versioned optional input variable.  RESOURCE remains claims-false transport;
only an independent checker-approved COMMON may print global completion.

## 6. Bounded gates

No actual authority construction or production.  Run only AST/pin/diff,
producer fixture, checker self-test/pin-check, GAP parse and generated-shell
`bash -n`.  Fixtures must exercise the real production helpers and include:

- a synthetic support of size `W` whose first guaranteed outside point is
  cursor `W`, plus an earlier-hit case;
- ten-coordinate direct word evaluation while a sentinel makes inherited
  `global_candidate` fail if called;
- wrong cursor/qid/gid, word, each coordinate, `K`, `W`, zero scalar,
  direct-pair and dependent-row rejection;
- nonempty rows close before any global scan; empty rows retain one and close
  one; exactly one update per close;
- exact C99, one authenticated v5 BOOTSTRAP/closed migration, one appended v5
  batch migration, and own-v6 resume; mutation of each legacy seal/prefix/
  segment identity must fail; and
- v5-to-v6 diff confinement proving no candidate-order/resource/COMMON or
  old support-cursor drift outside the commissioned branch and migration.

Report exact pins and end with exactly one of:

`TASK506_R07_RANK99_NONZERO_CONSTANT_GLOBAL_PREFIX_V6_PASS`

or

`TASK506_R07_RANK99_NONZERO_CONSTANT_GLOBAL_PREFIX_V6_STOP`
