# Luna task 376: task375 endpoint-binding unblock

This is a versioned successor to task375's `STATIC_BLOCKED` reply.  Read and
apply both the original task375 commission and the new authoritative ABI
theorem:

- `sol/proof_r07_task193_to_exact_endpoint_literal_binding_v352.md`

The missing bridge is now explicitly authorized and mathematically fixed.
Resume creation of the same four task375 output paths; they do not presently
exist except for the reply, which must be replaced by a versioned final
task376 reply instead of editing the blocker report:

1. `search/d972_r07_direct_relator_a5_a7_fusion_v4.py`
2. `crosscheck/check_d972_r07_direct_relator_a5_a7_fusion_v4.py`
3. `search/d972_r07_direct_relator_a5_a7_fusion_gha_driver_v4.g`
4. `sol/luna_reply_376_r07_endpoint_binding_unblock_v1.md`

## Exact authorization

1. Convert v3 `M.pairs` to task292 `M_terms` exactly as v352 Section 4.
2. Reconstruct every occurrence from the executable task198 owner:
   `rho`, `r_o`, the stored signed prefix product `Q_o`, task179 prefix `P_o`,
   and `d_sources=[delta(r_o^-1)]`, with all metadata fixed by v352 Section 2.
3. Reconstruct `R_H1(f),R_H2(f),R_P(f)` from the pinned task193 constructor
   and serialize `epsilon_sources=[-delta(R_B(f))]` as v352 Section 3.  Require
   literal equality with task193's physical `relation_words` and pointed rows.
4. Load the frozen task292-v2 exact `compile_literal` core under a non-main
   name.  Do not call or patch its unconditional v2 `production_literal`
   blocker.  The v4 output schema owns the new accepted binding.
5. The producer uses task198-v12/task193-v3; the checker independently uses
   task198-v14 and reconstructs the literal input rather than accepting the
   producer serialization.
6. Keep all task375 v351 requirements, including the full raw row for each
   lift-null column, positive-complete fair translation, typed resource
   checkpoint/resume, and no global A7 negative from a bounded miss.

If a new load-bearing owner mismatch remains, stop at its first exact field
and report it.  Do not add SELFTEST, mutations, generalized adapters, retries,
duplicate producer runs, local production, GHA, git, or edits outside the
four paths above.
