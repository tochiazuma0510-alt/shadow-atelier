# Task419 addendum v1 - apply the exact quotient after literal aggregation

This addendum resolves one implementation ambiguity in task419. Read
`sol/proof_r07_a0_pb34_physical_quotient_terminal_v403.md`.

It is sound, and simpler, to let the authenticated task413 occurrence code
materialize a literal correction's signed-prefix physical row first, retain
the three block tags, and then apply the exact blockwise map

```text
Q3 on block 1, Q3 on block 2, Q4cen on block 3, identity on nu.
```

Do not invent or use a common quotient action after aggregation. Each
literal conjugate must still be evaluated by the direct occurrence formula
before quotienting. This supersedes task419 wording that could be read as
requiring invasive access to every internal occurrence before task413's
linear aggregation.

On a positive terminal, v403 permits `COMMON_WORD` from independently
replayed quotient equality plus selected six-action ancestry. Do not rebuild
the two PB3 closures or five central PB4 closures merely to materialize an
old-presentation boundary preimage.

This addendum changes no allowed output path and authorizes no additional
file, local heavy run, workflow edit, commit, push, or dispatch.

`TASK419_ADDENDUM_V1_PHYSICAL_QUOTIENT_AFTER_LITERAL_AGGREGATION`
