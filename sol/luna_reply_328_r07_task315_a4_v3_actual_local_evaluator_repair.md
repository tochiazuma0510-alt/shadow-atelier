# Luna reply 328 — task315 A4/v3 actual local evaluator repair

## Result

The five authorized v3 outputs are implemented. No Python, Node, GAP, GHA,
workflow, git, or network execution was performed. Static source construction
and read-only byte/hash inspection only.

| output | bytes | SHA-256 |
|---|---:|---|
| `search/d972_r07_word_independent_successor_kernel_v3.py` | 33283 | `a228657ddf900d503c6d9574cd8fbefb338b0817412fbe108e234cf28f0aebab` |
| `crosscheck/check_d972_r07_word_independent_successor_kernel_v3.py` | 14916 | `77ab461a03836597c552fa14f300ad39edf97c678e43eea268b6e3474c70552d` |
| `search/d972_r07_word_independent_successor_kernel_gha_driver_v3.g` | 3677 | `a338918b418d4167470cca09fcc7550bd5a7fd63820da7a60e5a320fbbbf0c02` |
| `search/certs/d972_r07_word_independent_successor_kernel_selftest_v3_20260828.json` | 434 | `b385994a1e0d44e6c4cde981ab7b91b2db97d26773889c1db8270372799afda4` |
| this reply | excluded | self-referential |

## Authority and dependency graph

Production pins the task330-accepted v2 manifest by raw SHA-256
`cc8c16c8ad8f2d094868f0897bcca2a98adba75c18bf7ff397f0da67fd233ea4`, its
manifest self digest `0f630669a906c93a3b7d40bd36633213316ff8da1b46ca254a552b3636963684`,
the 150-byte independent checker verdict by
`ac841c5a979bbe89bdd47c73151ecabf29783793b7b288b4d08c4824596251de`, and the
receipt by `82f7955580039f2a0271896c928515d26996f636d8e73231331da6a37f6b19f5`.
It binds accepted/independent/synthetic flags, both run/head/artifact/ZIP
identities, both attestation lines, receipt self digest, exact source
identities, and exact resolved top-level paths. Absolute, traversal, dot,
symlink, alias, nested same-basename, and output/input-overlap paths are
rejected. The receipt replay checks local blocks `1..6318`, `1..104`, and
`1..19`, seven chunks, corrected Gamma/normal-closure/Q0 fields, all
`block_index`/`block_slot` bridge entries, evaluator ABI, widths, canaries,
and group-law identities.

The producer dependency graph is: pinned receipt/manifest/verdict and
attestations → frozen E4 arithmetic module → pinned q3 receipt → pinned
task176 `build_fine_deletion`/`make_deleter` interfaces → reconstructed E3/E4
quotients and 31-row context registry → ten-context local runtime → producer
prefix trie → one thirteen-seed boundary echelon → 6441 typed rows → live
coefficient K echelon/DAG → v247 anchor. The checker graph is independently
written: pinned authority → reverse suffix trie → v163 support-inversion
boundary columns → max-pivot coefficient echelon → typed row and two-way span
replay. Neither side imports the other v3 program, runtime wrapper, echelon,
or trie helper.

## Actual evaluator, closure, and ancestry

The local runtime has exact `eval`, `multiply`, `inverse`, `action`, bridge,
and defect operations, loads the pinned E4/q3/task176 objects, builds the
finite deletion map, and caches exactly forty signed actor/context values.
The v268 inventory is bound as 288 primitive words, 114458 literal letters,
15970 producer prefix edges, 26136 checker suffix edges, and 5475488 stored
row-word letters. Producer and checker assemble all 6441 rows; baseline tuple
multiplication bounds are 28937 and 39103 respectively, plus closure work.

Producer boundary work follows v269: authenticate two PB3 and eleven PB4
typed base rows, insert all thirteen once, and close one live action queue over
`x,x^-1,y,y^-1`. It records immutable `B:` coefficients, parent/action
ancestry, exact seed/accepted/reduction/candidate counts, queue exhaustion,
and the `<=4*b` post-seed candidate bound. The same boundary basis is reused
for all rows, K actions, basis changes, word differences, and anchor work.
Checker boundary work is v163 support-inversion column generation, with
`q=g*h^-1`, direct `q*h=g`, strict rank-rise processing, complete-zero
correlation, and two-way span containment; it does not copy the producer queue.

Both echelons carry fully scaled raw `B:`/`K:` labels and replay coefficients
against immutable raw rows. The persistent integer-node DAG memoizes
evaluation, uses outer-first conjugator composition, and directly replays
new K/boundary/anchor words and the noncommuting depth-two canary. No global
Q0 state/edge table, arbitrary section query, second 6441-row roster, or
per-target boundary scan is constructed.

## Static positive trace and mutations

After the accepted authority gate, the positive trace is reachable as:
`authenticate` → `LocalRuntime`/prefix trie → `build_boundary_closure` → all
6441 row defects → live K echelon and raw replay → word-bearing v247 anchor →
`COMPLETE` with explicit terminal
`R07_WORD_INDEPENDENT_SUCCESSOR_KERNEL_V3_PASS`. No unconditional local
evaluator stop remains. Nonpositive paths retain typed `UNKNOWN_INPUT` and
`UNKNOWN_RESOURCE` envelopes, live-cap details, rank-zero restart state, zero
A4 milestones, and false lift/fake/Ihara flags.

The synthetic production-shaped fixture contains the twenty commissioned
task328 owners, six v268 trie owners, and eight v269 boundary owners (34 total).
Each records a canonical before/after change, owner, stage, narrow rejection,
and rejection status. The independent checker reruns all 34 records and
requires the same byte changes/rejections; it does not trust producer flags.
The driver pins all v3 producer/checker/fixture bytes, authority manifest and
verdict, rejects stale v1–v3 outputs, invokes one producer and one checker,
requires exact-one full-line terminals and nonempty sealed checker verdict,
and writes the sole sentinel last.

## Accounting and status

All input, trie-node/edge, row-assembly, boundary, echelon, DAG, queue,
checker, sealing, and write work is metered. Canonical serialization is
performed once per sealed object. Static bounds are `O(288·L + 6441 + 4b +
closure)` group work, live reduction `O((6441+4b+t)(b+t)s)`, and dense action
work `O(4t^3)`; `n=6441`, terminal K rank `t`, boundary rank `b`, and sparse
support `s` are explicit. No sleep/retry/poll/lock/pool/subprocess or global
roster is used.

```text
IMPLEMENTATION:                  IMPLEMENTED
SELFTEST / PRODUCTION:           UNEXECUTED
AUTHORITY-V2 INPUT:              accepted only if task327 PASS
ACTUAL POSITIVE BRANCH:          STATICALLY REACHABLE
ACTUAL A4:                       remains 1/3 at most before execution
LIFT / FAKE / IHARA:             NONE
```

`TASK328_R07_TASK315_A4_V3_ACTUAL_LOCAL_EVALUATOR_REPAIR`
