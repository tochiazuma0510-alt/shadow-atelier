# Luna task 171: R07 full-E4 joint-orbit selector preflight v1

Date: 2026-08-27
Role: Luna / bounded implementation and mechanical falsification only

## 1. Purpose

Read completely:

- `sol/proof_r07_full_e4_joint_orbit_selector_v109.md`;
- `sol/proof_pb4_eleven_relator_presentation_equality_v108.md`;
- task/reply/source/checker/full artifact for task 157ee/157ef; and
- task/reply/source/checker/full artifact for 157eg--157en.

V109 proves on paper that the unprojected target6 correction image of the
registered joint value kernel is generated by the full-context conjugacy
orbit of the finite task-157ee relation roster.  This task performs the
bounded mechanical/falsification preflight needed before an integrated GHA
solver is commissioned.  It must establish exact data paths, conventions,
canaries and resource estimates; it must not run the large orbit or claim a
correction.

## 2. Authorized outputs and noninterference

Create only new versioned files:

```text
search/d972_r07_full_e4_joint_orbit_preflight_v1.py
crosscheck/check_d972_r07_full_e4_joint_orbit_preflight_v1.py
search/certs/d972_r07_full_e4_joint_orbit_preflight_v1_20260827.json
sol/luna_reply_171_r07_full_e4_joint_orbit_preflight_v1.md
```

Do not modify v108/v109, tasks 168--170 or their assets, 157ee--157en,
workflows, CLAIMS, Sol replies, or any existing certificate.  No git, push,
workflow edit, GHA dispatch, full orbit enumeration, parallel Python/GAP, or
credential access.  Use a clean TEMP overlay and bounded serial execution.

## 3. Pin and reconstruct the exact objects

Pin every consumed file by path, bytes and SHA-256.  At minimum include:

```text
ci/b345_157ee_artifacts_32359956713/
  d972_b345_joint_kernel_qstar_closure_v1.json
SHA-256 1c3ad7a7124cee152eb40968cf212c14641a9f8720063c85f70533864898d0df

ci/b345_157en_artifacts_32458556448/
  d972_b345_target6_dual_colgen_v2.json
```

Reconstruct independently, without accepting names alone:

1. the full `E4=Q4 x Pi4[3]` multiplication/evaluation conventions;
2. the three target6 substitutions `(X0,Y0),(X0,Z0),(Y0,Z0)`;
3. the fixed g760 prefix action and raw `Sigma_E` formula;
4. the 31 context ids and 46 aliases in the registered joint map;
5. the exact finite relation-word roster underlying the 6,318+104+19
   task-157ee presentation layers, expanded to signed F2 words where the
   artifact makes this possible; and
6. the eleven now-exact PB4 D2 relators and full-E4 sparse-row encoding.

Fail closed and report the exact missing reconstruction edge if any auxiliary
record word cannot be expanded from the pinned artifact/source.  Do not infer
normal-closure completeness from counts alone.

## 4. Actual Fox-conjugation canaries

For a deterministic bounded sample containing every relation layer, several
word lengths, both source generators as conjugators, inverses, products, and
at least 100 total `(u,r)` pairs, compare from scratch:

```text
raw Sigma_E(u*r*u^-1)
```

with the row obtained by applying the ordered triple of full-E4 context values
of `u` to the three raw Fox gradients of `r`.  Require literal equality of all
sparse component/E4/coefficient entries.

Also construct pairs `u,v` with the same three full-E4 context values by
multiplying by sampled joint-kernel words, and require

```text
Sigma_E(u*r*u^-1) = Sigma_E(v*r*v^-1).
```

Exercise negative canaries for context order, left/right action, prefix
orientation, conjugator inverse, Fox inverse sign and sparse-key encoding.

Separately check on products of sampled joint-kernel words that `Sigma_E` is
additive, but reject an attempted extension of that assertion to the larger
projected-only kernel whenever a distinguishing canary is available.  If no
bounded distinguishing word is found, report `PROJECTED_KERNEL_RAW_ADDITIVITY
UNKNOWN`; do not assert it.

## 5. Exhaustive toy theorem check

Build a helper-nonshared small toy in which:

- `F2` maps onto a finite nonabelian marked group;
- a finite relation roster normally generates the kernel;
- three finite context maps have a nontrivial common kernel; and
- an exponent-mod-three side coordinate is present.

Exhaustively enumerate the finite relevant quotient/orbit and independently
check the equality in v109 Theorem 3.1 between:

```text
image of (Sigma,exp3) on the kernel
span of all relation-conjugacy orbit columns.
```

Include a case where the unconjugated relation rows span a strict subspace, so
the orbit is load-bearing.  The checker must build the toy by a different
route and import neither producer nor helpers.

## 6. Exact integration map and resource estimate

Read the 157eg--157en code paths and report, with function names and line
bindings:

1. where a second provenance-bearing column family can enter the target6
   echelon;
2. how a selected correction orbit column retains `(relation id, context
   state, section word)` and materializes the conjugate word;
3. how the two exp3 coordinates are kept outside PB4 D2 translations;
4. how raw target reduction, dual lift, complete correlation, transactional
   block commit and direct replay are reused without changing their signs;
5. which old 108-seed assumptions must be deleted or quarantined for the
   single fresh g760 target; and
6. how task-168/task-169 authenticated checkpoints can be inputs without
   pretending their projected coefficients determine the raw solution.

Measure bounded samples and give conservative estimates for bytes per orbit
column, raw support sizes, context-state serialization and correlation cost.
Separate:

```text
positive search: may stop after enough ACTIVE columns with full provenance
negative proof: requires complete correlation over both D2 and correction
                orbit families
resource stop: UNKNOWN
```

Do not manufacture a runtime estimate from one tiny toy; state the scaling
variables and an interval only where the actual sample supports it.

## 7. Tests and report

Run producer/checker serially twice from clean overlays and require
byte-identical certificates.  Mutation coverage must include every semantic
category in Sections 3--6 and every forbidden global claim.  The final reply
must list paths, bytes, SHA-256, exact commands/output, sample sizes,
mutation counts, reconstruction gaps and the recommended shape of the
integrated GHA successor.

Repeat verbatim:

```text
full-E4 orbit theorem received a bounded mechanical preflight only
no complete correction orbit was enumerated
positive target6 at one abelian relation-module layer is not literal A18
no cofinal lift / fake / Ihara witness declared
```
