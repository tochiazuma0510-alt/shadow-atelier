# Luna task 173: R07 all-seven raw bridge inventory v1

Date: 2026-08-27
Role: Luna / bounded static implementation inventory only

## 1. Purpose

Read completely:

- `sol/proof_r07_full_e4_seven_evaluation_orbit_selector_v110.md`;
- `sol/proof_r07_full_e4_joint_orbit_selector_v109.md`;
- `sol/proof_pb4_eleven_relator_presentation_equality_v108.md`;
- `sol/proof_r07_structural_ht1_ht2_ht5_discharge_v99.md`;
- the v71, v93 and v96 proof notes referenced there;
- `docs/week1-定義ノート.md`; and
- the authenticated task-157ee through task-157en producer/checker sources and
  reports.

Task 172 authenticates the target6 raw bridge.  This task inventories the
additional already-existing callable data needed to instantiate v110 for one
common correction word across:

```text
hexagon H1
hexagon H2
the five literal A.18 coface occurrences in printed order
```

This is an inventory, not a solver and not a mathematical promotion.

## 2. Authorized output and process boundary

Create only:

```text
sol/luna_reply_173_r07_all_seven_raw_bridge_inventory_v1.md
```

Do not modify any source, checker, certificate, workflow, proof, CLAIMS or
existing reply.  No git, GHA, GAP, Python, Node, subprocess, credential access
or generated scratch file.  Use static file reads/searches only.  Local Python
is reserved for task 172 and then task 169.

## 3. Literal relation formulas

For each of H1, H2 and the ordered A.18 pentagon, locate the exact executable
word formula already used in the repository.  Report:

- source path, function, and exact line range;
- every occurrence of the variable word `f`, its sign, substitution and
  literal order;
- every fixed braid/group factor between occurrences;
- whether the implementation is base-minus-corrected or
  corrected-minus-base and left or right correction; and
- whether an inverse occurrence is differentiated literally with the Fox
  inverse rule.

Do not infer H2 from H1 by symmetry unless an executable, pinned symmetry map
is present and its word equality is directly replayed.  Do not replace the
five cofaces by a commuted sum or by rho without the simultaneous theta
component and the v96 shear.

## 4. Typed context registry

List the ordered full quotient contexts required by all seven occurrences.
For every occurrence bind:

```text
relation block
slot ordinal
substitution word pair / coface map
31-context registry id if present
all matching names in the 46-alias table
source API which evaluates it in E3/E4
```

Duplicate contexts may share one registry row only after literal row/blob
equality.  For every missing occurrence name the exact callable/data edge;
generic `MISSING_CONTEXTS` is forbidden.  State whether the task-157ee joint
kernel is visibly contained in every required context kernel by those exact
bindings.

## 5. Raw residuals and exact boundaries

For each output block identify:

- the raw target/defect constructor for the fixed g760 base;
- the Fox-gradient constructor for a correction word;
- the fixed prefix transports and their source;
- sparse row component count and canonical group-element serialization;
- `D1` and raw presentation `D2` constructors;
- the exact presentation theorem currently available for that D2 image; and
- a direct word replay route for the corrected relation.

Distinguish PB3 from PB4.  V108 closes the marked PB4 eleven-relator
presentation boundary.  Do not silently transfer that theorem to a PB3 block.
If an exact marked PB3 presentation theorem is absent, identify the smallest
paper or mechanical gate required; do not label a one-sided span exact.

## 6. Minimal integrated successor

Give a concrete, file-level plan for the smallest bounded preflight which
would extend task 172 from target6 to all seven occurrences.  It must use:

- one g760 word and one right correction word;
- one ordered stacked sparse row, with block tags preventing accidental
  cancellation between equations;
- at least 100 actual conjugation canaries covering every distinct slot;
- nontrivial same-context/different-conjugate pairs;
- literal direct replay of H1, H2 and the noncommutative five-factor
  pentagon; and
- helper-nonshared reconstruction in the checker.

Estimate which objects can be reused by authentication and which must be
implemented fresh.  Do not estimate a full-orbit runtime from the bounded
inventory.

## 7. Report boundary

Write exact findings, source anchors, missing edges and a GO/STOP verdict for
writing an all-seven bounded preflight.  Repeat verbatim:

```text
static callable inventory only
target6 alone is not the all-seven common-word system
PB4 exactness is not silently transferred to PB3
no all-seven solve / cofinal lift / fake / Ihara witness declared
```
