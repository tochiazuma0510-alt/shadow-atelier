# Task950 -- repair the executed v1 mixed-generation parent join

Read Task947 and the workshop express for run33963515077/ruling2123.
The researcher says continue and authorizes GHA execution. Root is the sole
git/network/credential/dispatch broker. No local numerical Python/GAP run,
network, credentials, git, or further agents by this worker.

Write ONLY `search/d972_r07_fixed_root_packet_loop_v2.py` and
`sol/luna_reply_950_r07_packet_parent_layout_v2.md`. Published v1 is immutable.
Copy the v1 producer, making only this bounded repair and a schema v2 bump.
Retain all packet arithmetic, source lineage, root order, resume and claim
boundaries. Coordinate public ABI only with Task951's checker/workflow worker.

## Actual failure and required repair

Run33963515077/1 (commit25501f62c326290bafd223fe3b7a1d7b0ba51f0c) passed
the source/data pins, parent downloads and seven synthetic canaries. It
reached the all1356-row initial lambda sweep and then owner_and_tables raised
KeyError('target_derivation_accepted_as_premise') at v1 line352. No packet
or new step was completed. Actual seed30 rho2 lacks the flag; seed34 has true.
The frozen parents must NOT be modified, silently backfilled, or interpreted
using get(key, True). Reject unexpected schemas and a missing/false flag on
the actual v3 parent. The old v1 parent is admitted only through its exact
accepted result/target/payload pins and the explicitly named DERIVED chain.
Also join the accepted original-rho2 packed identity and base target manifest;
do not imply direct original-rho2 reading or historical target replay.

Actual local read-only fixtures:
- %TEMP%/shadow-atelier-seed30-run33946247365-candidate-a1
- %TEMP%/shadow-atelier-seed34-run33956437467-candidate-a1
- %TEMP%/shadow-atelier-task904-run33891714539-final
- %TEMP%/shadow-atelier-task945-run33963515077-diagnostics-a1

Inspect the real JSON shapes, including result.schema/target.schema and rho2
identity. Add a cheap metadata-only CLI `--parent-layout-selftest` receiving
`--state-root`, `--delta-root`, `--seed34-root`. It must load the frozen actual
receipts via exact existing byte/SHA pins, exercise the same repaired join
used in production, and emit PASS with fixture identity and explicit
legacy-no-flag/v3-flag-present facts. At least reject an in-memory false or
missing v3 flag and a wrong rho2 identity. This is a focused regression for
the real failure, not new numerical replay or a broad historical test suite.
The workflow runs it on downloaded parents before production. If the
smallest safe CLI varies, agree that public ABI with Task951 and tell root.

Keep M3-1 DERIVED, parent premise records and both new direct target pairings.
Report exact delta/read scope, source bytes/SHA, CLI and unresolved gates.
Do not claim runtime PASS locally, cross-checked or verified.
