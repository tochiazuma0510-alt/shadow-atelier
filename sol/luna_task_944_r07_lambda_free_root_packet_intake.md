# Task944 -- bounded next-continuation intake; no implementation yet

Root owns the mathematical design below. Reply ONLY
`sol/luna_reply_944_r07_lambda_free_root_packet_intake.md`, preferably under
100 lines. No code edits, numerical execution, network, credentials or git.
Do not repeat an old audit or propose a generic checkpoint framework.

## 1. Current actual result is retained, not recomputed

Seed34 materializer run33956437467/1 SUCCESS, source/head
b9ae78b0950b186463849c3ec874f6474f359851. Candidate9966542166,
zip984053 bytes, SHA
a4cb9f63a470636628d9ef02a5b5e55d90fe3b0a2c70f2012d32c9517d87defc.
It adds one actual pivot after the old1354 base and seed30 delta:
rank1356, generation8061, head
d467e4e60b8bff88272cddd4b01d630d763e863b4500015c7c6c077b23ddf26b.
Current lambda SHA
f7406d70211ab02acf08a895d127d17e7dab179454916a90ea40cb11152e12dd;
current target remainder SHA
46a6b8281587a13236fd9af00eab9825a2d956dd878613af14182b5f9ae94c49.
The target remains nonzero, free coordinate1419/value1. Full final1356-row
lambda sweep passed. Producer/checker each54 seconds, total job145 seconds.
Root downloaded the exact candidate/diagnostics outside the repo to
`%TEMP%/shadow-atelier-seed34-run33956437467-{candidate,diagnostics}-a1`.
Full new receipts are reply162 section18. New root values are not known;
rank1355's seed35/36 values are stale and cannot select the next seed.

## 2. Root's exact algebraic design

The accepted P1 basis and its canonical lifts are FIXED as S/lambda change.
For every registered raw seed s, set once

```text
D_s = raw_seed_s - sum_i SeedRed(s)[i] * canonical_P1_lift_i.
```

V541 requires its COMPLETE96776 source-lower coordinates to vanish. Then
the four direct top slices d[a,s] are legal pure defects, and for EVERY
later separator of the same fixed owner

```text
SeedPair(a,s;lambda) = <B_a^*lambda,d[a,s]>.
```

This is lambda-independent PRIMAL data, not stale contracted q/P1 values.
It permits one44-seed construction followed by cheap changing-lambda root
tests, without reparsing Task554/P1 ancestry for each new pivot. No actor
or orbit origins are added or silently treated as complete. All-root-zero
is only ROOT_SEEDS_ZERO, never a grade2 negative result.

Build each complete relation from all old/new character contributions,
preserving raw event order/cancelled literal roots before numeric collection.
Stream each P1/cache/lower row once and apply its coefficients to the44
accumulators; no8059-by241928 dense matrix. One Task554 body at a time.
The entire44 top-defect array is6386688 unpacked bytes or1596672 packed
bytes; full lower accumulators total4258144 unpacked bytes. These are just
array sizes, NOT RSS or runtime guarantees. A44-by8059 uint8 coefficient
array is354596 bytes if the dense small coefficient table is simpler.
Full lower-zero and every stored top packet need independent construction.

A subsequent bounded loop may use this fixed packet, the current saved
state and four fresh B-adjoints: first nonzero in fixed character/seed order
-> apply B only to that selected packet -> one existing pivot append and
target update -> next lambda. On all-root-zero stop at ROOT_SEEDS_ZERO;
on zero target stop at MEMBER_CANDIDATE. It need not construct all176
physical rows up front. A rank rise cannot happen more than176 times inside
this fixed root span; this is a list-length upper bound, not an A0/actor
completion count or an estimate. Persist each completed new pivot with its
literal parent references; never repeat the saved seed30/seed34 insertions.

## 3. Deliver the narrow implementation decision

Inspect the existing937/940 producer code and retained parent ABIs only.
Identify exact reusable entry points and the smallest missing functions
for the one44-seed packet and rank1356 state adapter. Decide whether the
same bounded implementation can immediately consume successive root hits,
or whether packet+one current-root result is the materially smaller first
handoff. Do not authorize your own implementation or narrow the44-seed
source; report the concrete tradeoff to root.

Give bounded live arrays / one-pass IO / selected literal ancestry / simple
durable-prefix layout, and genuine missing input ONLY. No new old-state
closure, historical target solve, third seed engine, source classification,
paper tree adapter or optional audit is a prerequisite. The old producer/
checker numerical lineages remain disclosed. Existing accepted artifacts
are not to be modified, concatenated or copied into new parent snapshots.
