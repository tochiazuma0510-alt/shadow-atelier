# Task958 — grade2 positive terminalの具体的readout intake

役: Luna / read-only。変更可は
`sol/luna_reply_958_r07_grade2_terminal_readout_intake.md` のみ。
ローカルPython/GAP/import/AST/数値実行、network、credential、git、dispatch、
追加agentは禁止。source/JSON/byte/hashを読み、指定返信へ結論を書く。

Task954/955はrootがsource fd04734d20d472e7c09f31de3f92f8a50d6d841aで
pushし、run33967668257/1が現在走行中。事前試験とcap1 phaseはsuccess、
実resume32が進行中。新rank/terminalはまだ与えていない。MEMBERを予測しない。
rootは本走結果とcampaign判断を、Task957は負判定側のv548接続を担当する。

受理済みfixed44親のrank1359/gen8064とTask954の新physical rows/target係数/
ordered materialization receiptsから、**target零に到達した場合**の当該gradeの
正式MEMBERに何を足すかを、v518と現在の実装APIで詰める。新しい実装は書かない。
Task954のproducer著者としてcheckerの算術実装は読まず、自系sourceと公開ABI、
正本の数学・監査文面を読む。

必読: v518全文、reply953 F7、Task954のsource/公開ABIと実fixed44 parentの
target係数、必要なv534/v511/v516の条項。関連実装として以下を候補に読む。

- d972_r07_a0_grade1_selected_slp_v2.py
- d972_r07_a0_psl504_member_payload_lift_v3.py
- d972_r07_grade2_p1_componentwise_semantic_replay_v5.py
- d972_r07_grade2_violation_materializer_v2.py
- 必要な場合のみ実際のphysical/eleven-slot evaluatorの最小依存箇所。

要求:

1. base target reduction＋seed30/34＋packet3段＋新prefixのtarget.scalarから、
   rho2のordered physical pivot係数を再現する具体式/参照payloadを示す。
   target scalar0も保持し、parent arithmetic前提と新しい係数readoutを区別する。
2. canonical P1 DAG、Conn raw rows、seed/actor defect、ordered physical reductions/
   scaleからv518の一つのSLPへ向かう、既存関数と未実装consumerを区別する。
   係数2はliteral inverse、projectorの固定順、actor t W t^-1とnested path順を保つ。
3. normalized exponent pairと同じwordの11 typed slotsへの直接replayについて、
   実際の既存entrypoint/データ型/必要artifactを示す。6 source tagsを11 slotsと
   呼ばない。旧retired laneのsource/modelを現ownerへ黙って持ち込まない。
4. linear target零、条件付きword存在、当該gradeの正式MEMBER、full H/P/A0を
   別々の条件として整理する。今回の登録gateを下げず、余分な無限cofinal前提も
   追加しない。不足が新consumerならそう明記し、既存functionの不存在を推測しない。

独立実装できる最小単位と具体的input/outputを簡潔に記す。実走時間の予想、
新数値やMEMBER/fullA0/cross-checked/verifiedの宣言なし。
最終行 `AUDIT_958_VERDICT:`。
