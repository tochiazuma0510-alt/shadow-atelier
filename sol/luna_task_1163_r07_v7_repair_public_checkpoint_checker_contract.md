# Task1163 — C独立のpartial/checkpoint受入metadata契約を有限export

宛先: Helmholtz / c6_final_binding、Luna。1159の限定継続。返信 `sol/luna_reply_1163_r07_v7_repair_public_checkpoint_checker_contract.md`、最終行 `AUDIT_1163_VERDICT:`。新材料 `%TEMP%/shadow-atelier-audit163/task1163/`。既存source/返信を変更せず、P/C実行・import・AST・compile・selftest、新agent/Git/GHA/network/credentialは禁止。自分のC sourceだけをraw/textとして読める。P private/source/deltaは不可。

1159修理C525657 B/ccd572ee1f0cd2504521526fbfbf830a95197d99044870cf19c7a6cc0ffb67a7のGHA run34518126217/1/head2f8ad063da52da90ed74fd230fb122f96ad79ec7は実行中。Noetherが1161で全受領器を静的組立中。既公開driverにはcomplete-zero/linear/processed0..128があるが、Cのresource-stop/rejected・partial checkpoint/durable-tailの純粋なmetadata受入・拒否境界が公開資料として不足している。

自分のC sourceから独立に次の公開metadata契約をexportする。根拠source pin、全関連関数のbyte/line範囲、exact keys/types/null条件とconsumer位置を全件添える。数学アルゴリズム本文・係数実値・private deltaを公開しない。

1. selection/start/manifest/HEAD/result/checkpointの各terminal/partial/current processed・accepted・dependent・sequence・ancestry・phase/step・durable_tailの受入条件と未到達条件。C自身のUNKNOWN_RESOURCE/rejected結果とPの部分prefixを混同しない。C complete comparison未了のdiagnosticをcandidate成功へ昇格できない境界を明示する。
2. 公開6 phasesとrows/ordered/reduction/sign/zero/trit等のnested metadataのexact schema/key・進行順と上限・presence/nullable/sealの規則。元1150受領器用公開v6 nested契約と同型の箇所・変更箇所を自分のv6/v7 C根拠から区別する。G06はwhole coefficient SHA/trit/order/source/zero/literal signだけ、u8要素・vector・pairing・solver計算は不可。
3. missing final/HEAD、途中checkpoint、partial true、C native非zeroの場合の安全なdiagnostic受領範囲を示す。保存値が自己整合するだけでC PASSとしない。実runの成否やrank2090・128全採用・file countを予測しない。

納品はNoetherがprivate sourceを開かず読める公開JSON/短い注釈と全入力pin・全consumer位置。存在しない枝やkeyを作らず、不明はUNKNOWNとsource位置を示す。受領器source自身やP/C source修理、新selftestは不要。有限raw/text/JSONによる表作成のみ可。表ができたらrootへ先に通知し、Noetherにはrootから渡す。
