# Luna task 354 - A0/v12a auditable deterministic SELFTEST bootstrap

Role: Luna, bounded implementation only. Read this mail and every numbered
prerequisite first to last before editing. Do not run Python, Node, GAP, GHA,
a workflow, git, network, or any candidate command. Read-only PowerShell
inspection and hashing are allowed. Use `apply_patch` for repository edits.

Task353 is honestly `BLOCKED / UNEXECUTED`, but its blocker is now removed by
an explicit two-version acceptance protocol. V12a is a SELFTEST-artifact
generator only. It must implement the complete finite task352 repair and
produce deterministic prospective receipt R and independent verdict V after a
fresh Sol(max) audit authorizes GHA. V12a can never run production or resume a
production search. A later v12b will pin the exact physical R/V identities and
is the only version that may expose production/resume.

## 1. Binding prerequisites

Read in full, in this order:

1. `sol/luna_task_353_r07_task352_a0_v11_finite_repair.md`;
2. all six task353/v11 outputs and the complete reply;
3. `sol/sol_reply_352_r07_task351_a0_v10_code_performance_audit_v1.md`;
4. task351 and all six frozen v10 outputs;
5. v287, v289, v290, then v275--v279 and v284;
6. task348/reply348 and the complete accepted task176 physical chain;
7. every q3/E3/E4/joint/old/bridge/task175/task176/v172/g760/PB4 owner
   pinned by v10.

Every finding and repair in task352 and task353 Sections 3 and 5--10 remains
normative. Raw checkpoint canonical self seal
`29bb74f3bd8048913a0365bc4c599f3731d32ba56967f3a238c7468b7fcfd123`
is PASS. Do not repeat the withdrawn LF mistake or modify any old owner.

## 2. Sole permitted outputs

Create only:

- `ci/in/d972_r07_history_free_positive_fast_resume_selftest_bootstrap_v12a.manifest.v1.json`;
- `search/d972_r07_history_free_positive_fast_resume_v12a.py`;
- `crosscheck/check_d972_r07_history_free_positive_fast_resume_v12a.py`;
- `search/d972_r07_history_free_positive_fast_resume_gha_driver_v12a.g`;
- `search/certs/d972_r07_history_free_positive_fast_resume_selftest_v12a_20260829.json`;
- `sol/luna_reply_354_r07_a0_v12a_auditable_selftest_bootstrap.md`.

Do not edit v1--v11, recovery owners, workflows, proof files, v220, or any
other path. V12a may copy audited arithmetic from v10, but it must be a new
source with all task352 defects literally repaired. A short fail-closed stub
is not an implementation and must be reported `BLOCKED`.

## 3. Noncircular two-version protocol

The v12a bootstrap manifest P0 pins exact final v12a producer/checker/fixture
bytes and SHA plus every frozen physical authority. It preregisters the
prospective R/V paths, schemas, deterministic constructors, and semantic
field sets, but deliberately records their bytes/SHA/self/semantic digests as
`TO_BE_GENERATED_BY_AUDITED_V12A_SELFTEST`. This is not an acceptance
preregistration and must say so.

V12a has exactly one mode, `SELFTEST_BOOTSTRAP`. Unknown modes, PRODUCTION and
RESUME fail before loading the heavy search. The producer runs the complete
ordinary baseline and every actual-owner mutation, then atomically emits a
fully deterministic candidate R. The independent checker reads R, reruns the
ordinary validators and mutation ledger without importing producer helpers,
and atomically emits deterministic candidate V. Host time, PID, inode, mtime,
RSS samples, random values and unordered iteration are excluded from R/V.
Diagnostics may go only to non-load-bearing logs.

P0 does not predict R/V hashes, and R/V do not claim acceptance, A0 COMMON,
separator, lift, fake or Ihara. Their terminal is
`V12A_SELFTEST_BOOTSTRAP_ARTIFACT`, with `candidate_only=true`,
`production_authorized=false`, and explicit `requires_v12b_physical_pin=true`.
The driver may write only a last bootstrap-artifact sentinel; its wording may
not contain PASS, COMMON, ACCEPTED or A0_COMPLETE.

After a fresh Sol(max) static PASS, GHA may run v12a once. A separate audit
will then inspect the physical R/V and commission v12b with exact paths,
bytes, SHA, self seals and semantic digests. V12b will rerun the semantic
validators before production and carry the validated summary into Search and
the final COMMON receipt. There is no manifest rewrite after execution and no
cryptographic cycle.

## 4. Complete finite repair is required now

Implement every task352/task353 repair rather than stopping at the old v11
epistemic blocker. In particular, static source must close all of these:

1. chronological `seen_pivots` triangular validation, explicit reader-meter
   policy, current fixture pin, and all suites actually called;
2. three separate q3 gates for literal 1..36 rows, conversion by `x-1`, and
   physical Q0 multiplication `right[left[i]]`;
3. strict separation of the 970-byte projected Gamma state from a full
   JointGroup diagnostic and separately typed codecs;
4. deterministic open-address selected-coordinate K0 cache with retained
   40/154-byte full-state equality, skip-on-miss/coarse-mismatch, accepted
   trivial kernels, no cursor/candidate-zero fallback, and exact incremental
   kernel edge replay;
5. independently derived heavy identity over every physical owner and actual
   selected Q0/Gamma/K0/dual/code state, never a copied 64-hex shape;
6. recursive DAG opcode normalization before hashing, externally pinned
   checkpoint carrier, exact ordinary provenance replay, truthful heavy
   completion/materialization split, and v290 additive/max accounting;
7. iterative bounded expansion, compatible receipt caps, durable final
   fsync-before-checkpoint-retirement, and narrow typed unknown terminals;
8. defended 10,800/7,200/3,600/21,600-second envelopes, bounded persistent
   light workers or a pre-heavy light image, cumulative IPC/STOP ownership,
   no ten-coordinate work after a known duplicate, shared L-bitset proofs,
   one old-owner DOM per process, phase release, indexed symbols, O(1) hot
   meters, and checker wall/RSS/allocation caps;
9. all task352 physical mutations: triangular/raw, process/fault,
   heavy/checkpoint, 13 boundary/process, 30 selected-correction, 7 positive,
   and 11 physical/terminal cases. Each changes a real owner/transport,
   invokes the ordinary validator, and matches an exact narrow first reason.

The producer and checker must have separately written composite validators.
Sharing frozen group primitives is allowed; importing the v10 producer,
checker, main, mutation harness, output validator, checkpoint validator or
search object is forbidden. No mutation-name branch, miniature substitute,
arbitrary-exception acceptance or copied ledger is permitted.

## 5. Deterministic R/V constructors

Define one explicit canonical R constructor and an independently written V
constructor. R binds exact v12a sources, P0, fixture, recovery-v2, frozen
owners, ordinary-validator summary, full mutation ledger and complete
task352-repair diagnostics. V binds R's physical SHA, byte length, self seal,
semantic digest, every same frozen identity, and its independently recomputed
summary/ledger. Both use sorted ASCII canonical JSON, exact self seals,
bounded atomic writes, file and directory fsync, and no telemetry.

P0 must pin the final source/fixture identities without a cycle: the sources
refer to P0 by path and validate its semantic schema and source identities,
but do not hard-code P0's physical SHA; the final driver pins P0 after all
four files are fixed. R/V need not bind driver SHA. Explain and statically
trace why this graph is acyclic.

## 6. Driver and reply

The ASCII GAP driver pins exact P0/source/checker/fixture and all frozen
authorities; accepts only an explicitly bound `SELFTEST_BOOTSTRAP` mode; runs
one producer then one checker under bounded timeouts; rejects stale v12a
outputs; requires exact-one full-line candidate terminals, sealed nonempty
R/V, byte-exact producer/checker binding, and writes the neutral artifact
sentinel last. No workflow edit, sleep, retry, poll, pool or local parallelism.

The reply must give exact bytes/SHA of all five machine files, complete
import/process/authority graph, formulas and static resource bounds, and
line-numbered traces for every item in Sections 3--5 and every mutation.
Measured runtime/RSS remains `UNEXECUTED`. If genuinely blocked, identify the
first missing physical owner/API after the two-version split; the absence of
pre-execution R/V hashes is no longer a blocker for v12a.

End exactly with:

```text
V12A BOOTSTRAP MANIFEST:         COMPLETE or BLOCKED
IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
SELFTEST BOOTSTRAP / GHA:        UNEXECUTED
PRODUCTION / RESUME:             FORBIDDEN UNTIL V12B
FROZEN INPUTS:                   PASS or BLOCKED
DETERMINISTIC R/V CONSTRUCTORS:  STATICALLY REACHABLE or BLOCKED
SOL(MAX) REAUDIT REQUIRED:       YES
ACTUAL A0 COMMON + CHECKER:      0/1
SEPARATOR / NEGATIVE CLAIM:      FORBIDDEN
LIFT / FAKE / IHARA:             NONE
```

`TASK354_R07_A0_V12A_AUDITABLE_SELFTEST_BOOTSTRAP`
