# Luna reply 192 - R07 normalized exact cached column generation v3

Date: 2026-08-28
Role: bounded mechanical implementation and static audit only.  No Python,
Node, GAP, Git, GHA, or network was run locally.

## 1. Objective and governing semantics

The task192 commission, task186 v2, task190 speed audit, their replies, and
the reached arithmetic dependency manifests were read in full.  This v3 is
only a bounded accelerator around the authenticated task179 schedule.  It
does not change the finite mathematical schedule or make a non-existence
claim:

```text
nu(word) = (exp(word)/18) mod 3
all PB3/PB4 translated boundary descriptors and support joins are retained
the complete positive weighted schedule is retained
rank-zero replay follows every resume/semantic conversion
all-eleven/direct all-seven, ancestry, and exactification gates remain literal
UNKNOWN_RESOURCE is the sole resource terminal
```

The raw v1 mod-3 exponent is used only as a vacuous negative control.  The
production hook counts signed letters as integers, enforces divisibility by
18, and then reduces.  No first-edge word or production mathematical result
was executed by Luna.

## 2. Authentication and immutable inputs

The two governing arithmetic papers are pinned in both producer and checker,
and are also listed in the driver:

| file | bytes | SHA-256 |
|---|---:|---|
| `sol/proof_r07_task179_exact_exponent_lattice_v156.md` | 10409 | `2da7903829e6782eb434aad5a254b86f7fa86e8132fd1f0bccb7eb7fab3f4d7d` |
| `sol/proof_r07_all_rung_exact_charming_lattice_selector_v157.md` | 8367 | `08e6d0e5fcac68400904c9844b19f1626c663f121a852a26f37a2d71a79a3ab8` |

The task186 v2 bundle is pinned as producer `63053/ec73db0a...fafab`,
checker `54982/8898798d...3c488`, driver `9630/a1c0fc03...1a7e4e`, and
fixture `234/34dd389d...940963`; the task190 instruction/reply are pinned as
`3699/36502b01...510d29` and `22022/6fe8ee26...dfe3f`.  The complete live
task179 v1 bundle is pinned as producer `123870/47116826...2845c7`, checker
`73780/de1d821c...164c1d`, driver `12872/48f95b79...5aa97b`, and fixture
`407/46a1d809...5c7c78`.  The v3 loader additionally authenticates every
entry in the reached v1 `module.PINS` closure before importing or patching the
live module; no v1/v2 source byte is modified.

## 3. Cached implementation

The producer has three deterministic bounded caches.  The eleven-slot Fox
cache records the signed substituted relation, typed slot data, complete
gradient/value, prefix inverse, base inverses/blobs, and integer exponent.
Its key binds input digest, semantics digest, roster layer/ordinal/literal,
slot, group type, block, coordinate, sign, lift, and label.  Three fixed base
all-seven gradients are cached; dual-dependent support joins, mod-3 merging,
weighted formula, pairing, and selected direct replay are recomputed.

The candidate cache validates all 243 Gamma rows, q0 section/path views, and
the ten typed coordinate values for each `(qid,gid)` candidate, with literal
parent/letter and coordinate replay on fill.  The checker independently
reconstructs each Gamma word from the authenticated joint-group
parent/generator transcript and each q0 word from the sealed state-id,
parent-id, and letter chain before evaluating coordinates.  The producer
candidate cache carries a basis epoch and is invalidated (and cleared) at
every retained-rank change, so no basis-dependent view survives a basis
mutation.  The boundary cache constructs
the complete sorted PB3/PB4 descriptor roster, decodes support once per
correlation, enforces `t*h=g`, preserves all cancellation/contributor data,
and memoizes translated rows by the full typed translation key.  Hit, miss,
eviction, resident-byte, and regenerated-literal counters are exposed; an
eviction can only cause recomputation.

Global candidates now retain q0/Gamma source words, q0 parent/letter paths,
and the complete Gamma coordinate row; selector-only section blobs remain
confined to canonical selector records so the pinned v1 selector dispatch is
not triggered for global candidates.
Kernel candidates are wrapped around the authenticated kernel multiplication
and retain q0/Gamma provenance plus the kernel word and coordinate-row
product; selector-only section blobs are omitted from this path as well.
Both paths receive literal source/coordinate replay and an explicit legacy
selector-dispatch predicate check.

The checker does not import the producer or consume its caches.  It has
independent Fox templates/gradients, typed row/blob decoding, complete
boundary descriptors, bounded coordinate/q0/Gamma/candidate values, and
literal direct replay.  The cached receipt contract now also carries the
exact v156/v157 proof-pin map, which the checker compares byte-for-byte.
The cached schedule selftest now binds its returned S3 fixture to a fresh
`toy_occurrence_column` replay, and the checker independently reconstructs and
compares that fixture metadata before accepting the schedule transcript.

## 4. Checkpoint and resume contract

Logical candidate/fibre work is chunked at at most 256 attempts.  A sealed
v3 checkpoint is produced at safe chunk/rank/terminal/resource boundaries.
Every epoch binds input, target, normalized callsites/digest, and dual
digests.  The canonical row cursor may cross only complete weighted rows; a
stored row is checked for formula digest, `K`, `W`, Delta order, all ten
kernel orders, support/kernel/global cursor bounds, and contiguous complete
state.  K=0 cursor checks remain coordinate-specific when runtime data is
available.  An interrupted suffix is metadata only: the serialized state is
the last safe complete chunk and explicitly records the repeat interval.

Resume accepts only a sealed v3 checkpoint with the exact semantic/cache
contract.  All stored columns and provenance are authenticated, then a fresh
v1 echelon is rebuilt from rank zero.  Stored pivots, reduced target, and
cache state are discarded; the fresh dual is derived and the safe complete
boundary/correction progress is retained only when its dual digest and
complete-chunk contract match.  The actual v1 loader then revalidates every
retained weighted formula against that fresh dual.  Any partial/repeated
suffix is removed from semantic progress (its typed history is retained only
for audit/counter accounting), while the safe prefix/cursors remain.  Nonzero remainders
derive the current exact dual; zero remainders reject stale dual fields.

## 5. Independent SELFTEST and controls

The producer SELFTEST keeps the normalized hook installed through the live
successor path.  It calls the authenticated AllSeven occurrence/direct
methods on an 18-fold word and observes E1=1 versus the raw-vacuous E tail,
then exercises a real PositiveSearch add-column, coefficient/ancestry
recovery, rank-zero checkpoint conversion, and the v1 checkpoint loader.
The bounded noncommutative cached and uncached schedules have identical
candidate/formula/ACTIVE/rank/solution/correction/direct/terminal transcripts;
the literal `c_exact` word and its direct replay;
logical resource counters are equal while physical cache hit/miss counters
are explicitly allowed to differ.  Resume is tested after each position 1--4
of the four-candidate toy chunk and records the sealed prefix plus suffix-only
resume without a skip.  The cache probe exercises a real bounded fill/hit/eviction
path and an authenticated ResourceStop record.  Production UNKNOWN_RESOURCE
receipts preserve the actual v1 monitor snapshot byte-for-byte and the
checker binds its registered limits, counters, and terminal value to that
snapshot.  Shared Fox counters no longer control per-store residency: the
template store is capped at 88 MiB, the base-gradient store at 8 MiB, and an
independent split-limit probe binds their aggregate to 96 MiB.

All 18 ordinary and 19 cache destructive controls are routed through actual
occurrence/echelon/direct-word or checkpoint replay validators.  They cover
cache key/type/slot/Fox/prefix/boundary/support/section/blob corruption,
literal regeneration, chunk/cursor corruption, stale dual/raw semantics,
old pivots, coefficient-two inverse replay, exactification, and resource
terminal substitution.  The checked-in v3 fixture remains an
`UNKNOWN_RESOURCE` local-execution guard, not a pass or mathematical result.
Global and kernel candidate paths now have separate literal provenance gates;
missing or stale q0/Gamma paths and stale kernel coordinate products are
rejected by independent replay.
The checker independently reconstructs the exact c-star/c-exact words,
registered r3/r9/r12 words, direct rows, ancestry corrections, and all
kernel/ordering gates before accepting a production receipt.

## 6. Serial driver and final identities

Static repair map for this turn: producer lines 305--345 enforce independent
per-store shared-stat cache caps; lines 820--910 add the candidate
basis epoch, q0 parent/letter path, and path-bearing candidate provenance;
producer lines 1588--1745 preserve full global/kernel provenance and literal
coordinate multiplication while keeping selector-only fields on canonical
records; producer lines 1868--1895 invalidate/clear that
cache after each retained-rank change; producer lines 2410--2450 bind the
literal c-exact schedule transcript; producer lines 1958--1967 preserve the
live UNKNOWN_RESOURCE monitor snapshot; producer lines 1349--1525 rebuild
the v1 echelon/dual from rank zero, retain only a dual-matching sealed
complete chunk, discard repeated suffix metadata, and pass retained weighted
state through actual v1 loader validation; the receipt separately records
whether the boundary prefix was retained.  Resume carries a typed authenticated
monitor history, restores the nonzero safe chunk end, carries cumulative
counters (with checkpoint size treated as a gauge), and restarts wall time;
CLI limits must match exactly; the checker binds monitor limits/counters,
monotone chunk coordinates, and the safe-end history independently.  The
prior repeated suffix is retained only as typed history and is never
restored as semantic progress.  Checker lines 330--360 replay
typed kernel coordinate products; lines 650--735 independently replay Gamma
and q0 state paths even on hits; lines 900--955 independently replay c-exact,
epoch, and legacy-dispatch controls; and lines 1500--1575 bind the current
dual digest, monitor carry-forward, and correction cursor equality with the
sealed chunk end, so no resumed cursor can advance beyond the safe chunk.
The fixture is unchanged; the driver terminal grammar was extended for the
typed `resume_rebuild` stop.  The four interruption positions now
assert nonzero safe chunk ends, suffix-only replay after the sealed prefix,
monotone counters, and
identical c_exact/direct/terminal transcripts.
The producer also preflights carried-over cap exhaustion before v1 search
assignment, writes the already authenticated safe checkpoint, and emits a
typed `UNKNOWN_RESOURCE:phase=resume_rebuild` receipt; the independent checker
and driver bind that terminal and checkpoint path.  The resume-rebuild cap
phase is registered for each cumulative work counter in the checker and
driver terminal grammar, with exact producer/checker terminal equality.

The ASCII GAP driver now constructs and regression-checks the only accepted
mode preambles byte-for-byte:

```text
D972_R07_NORMALIZED_EXACT_CACHED_COLGEN_V3_MODE:="SELFTEST";;
D972_R07_NORMALIZED_EXACT_CACHED_COLGEN_V3_MODE:="PRODUCTION";;
```

The bound mode is also required to be a GAP string before any shell is
constructed.  A bare `:=SELFTEST;;` token is rejected by GAP before the
driver can be read, so it is not an accepted launcher spelling.  The driver
pins the predecessor/proof/live manifests, rejects stale
outputs, runs producer then checker serially with direct log redirection, and
requires exactly one marker at each stage.  Every redirected producer and
checker invocation is wrapped in a fail-closed `if ! ...; then cat log; exit
1; fi`, so a nonzero process always exposes its diagnostic log while no later
success marker or sentinel can be emitted.  Production accepts only the
matching `COMMON_WORD`, registered `UNKNOWN_RESOURCE` phase/cap/value/limit,
or authenticated `UNKNOWN_INPUT` terminal; the driver and checker use the
same registered phase/cap table.  It compares the extracted producer and
checker terminal strings byte-for-byte before writing the fixed nonempty
sentinel, then reads and checks that sentinel after `Exec`.  The configured
envelope is 19,800 seconds, 5.7 GB RSS, and the registered v1/v2 resource
caps.  No timing or cache speedup is claimed or measured here; only the
expected benefit of avoiding repeated immutable Fox/group/descriptor work is
reported.

Parent Sol refreshed the final runtime identities after static audit.  The
reply hash is intentionally excluded from its own body to avoid a
self-reference:

| authorized file | bytes | SHA-256 |
|---|---:|---|
| `search/d972_r07_normalized_exact_common_word_cached_v3.py` | 164649 | `d394056420baed19a7692eee6efd0c05ff2dc642254226d81aace482ada21199` |
| `crosscheck/check_d972_r07_normalized_exact_common_word_cached_v3.py` | 134659 | `90a7bc6647da680449cd2cc7c56a747f6ba02fd3d8fd5adb1644f65550ceccf6` |
| `search/d972_r07_normalized_exact_common_word_cached_gha_driver_v3.g` | 11424 | `3c4bed9bf5b8d8713c62fa6eff405f77aa34494b496a4338c1f9ab0f8a7a0d22` |
| `search/certs/d972_r07_normalized_exact_common_word_cached_selftest_v3_20260827.json` | 276 | `c49f434ad3daf1cc661ba45563dbb9557d436f91dca78c8ee0f47ed70332da12` |
| `sol/luna_reply_192_r07_normalized_exact_cached_colgen_v3.md` | parent/out-of-band | intentionally not self-hashed |

The fixture-binding repair changes the producer/checker byte counts to
164649/134659; Parent Sol refreshed those two SHA rows above before dispatch.
The authenticated producer/checker pins are now also updated inside the
driver.  Parent Sol refreshed its 11424-byte identity above before dispatch.

Only the five authorized task192 files were changed.  GHA SELFTEST run ID
`33100220787` failed before producer startup because its launcher emitted the
unquoted `D972_R07_NORMALIZED_EXACT_CACHED_COLGEN_V3_MODE:=SELFTEST;;`
preamble (`SELFTEST` was an unbound GAP variable).  This is a launcher
syntax/runtime-contract failure with no producer, checker, mathematical, or
cross-checked result.  The corrected exact preamble is recorded above; no
local execution or identity/hash refresh was performed by Luna.

Parent Sol dispatched the corrected GHA SELFTEST as run `33101227112` at
immutable head `a1644ca9b1479b4fc0d6a6e397b9684607f2d5f3`, with the quoted
`SELFTEST` preamble requested through the PowerShell `gh -f` path.  That
launcher path stripped the embedded quotes: the runner again received the
bare token and failed before reading the driver.  It therefore has no
producer, checker, mathematical, or cross-checked result.

Parent Sol then dispatched run `33101597039` at immutable head
`28e4154674aacf53fe33518ed00359f608032b1d` by sending workflow inputs as
JSON on stdin, with the mode quotes encoded as `\u0022`.  The quoted mode was
accepted and the producer reached `TASK192_STAGE=PRODUCER_START`, but the
producer exited nonzero; the prior shell's `set -e` stopped before `cat` and
upload, so no producer, checker, mathematical, or cross-checked result was
obtained.  This is a diagnostic-wrapper failure, now repaired above.

Parent Sol dispatched the diagnostic-wrapper repair as GHA SELFTEST run
`33101963207` at immutable head
`33ecc99319cd42b1c30c8eb1046363cf4ea0b709`.  It reached the producer
SELFTEST but failed in `cached_schedule_selftest` because of the stale
undefined local name `fixture` (the function had bound `_fixture`).  It is a
producer code SELFTEST failure with no checker, mathematical, or cross-checked
result; the fixture binding and independent checker gate above repair it.

Parent Sol dispatched the fixture-binding repair as GHA SELFTEST run
`33102478643` at immutable head
`014183e6ac7bf319251c44579b1f60b44e511f2b`.  It failed before producer
startup because the driver's internal producer/checker pins still named the
pre-binding identities.  This is a fail-closed pin-drift code failure with no
producer, checker, mathematical, or cross-checked result.  The supplied
producer/checker identities are now installed above.

CACHED NORMALIZED FIRST-EDGE WORD:            NOT EXECUTED BY LUNA
V2 MATHEMATICAL SCHEDULE CHANGED:              NO
LIVE TASK186 RUN MODIFIED:                     NO
COMPATIBLE COFINAL LIFT / FAKE / IHARA:        NOT DECLARED
