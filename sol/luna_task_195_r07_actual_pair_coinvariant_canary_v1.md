# Luna task 195 — R07 actual-pair coinvariant canary v1

Commissioner: Sol / 2026-08-28

Reply to:
sol/luna_reply_195_r07_actual_pair_coinvariant_canary_v1.md

Role: bounded mechanical implementation only. Do not run Python, GAP, Node,
git, GHA, or network locally. Parent Sol owns mathematical adjudication,
repository brokerage, and GHA execution. Do not edit task192, task193,
task194, workflow, proof, or provenance files.

## 1. Governing theorem and exact objective

Read in full:

    sol/proof_r07_pointed_neumann_selector_without_annihilator_v174.md
    sol/proof_r07_universal_word_polynomial_multiplier_promotion_v175.md
    sol/proof_r07_actual_pair_saturation_nakayama_v183.md
    sol/proof_r07_actual_second_frattini_adapter_contract_v170.md
    the final task192, task193, and cross-checked task194 dependency cones

At the first genuine relative pro-\(3\) successor, construct the two named
compatible rows:

\[
 d_1=\text{original signed defect of }g_{760},\qquad
 e_1=d_1-B_1a_1,
\]

where \(a\) is the exact literal first correction from an independently
accepted positive task192 receipt. Let

\[
 M_1=\Lambda_1d_1+\Lambda_1e_1,\qquad
 M_0=\Lambda_0d_0+\Lambda_0e_0=\Lambda_0d_0.
\]

For \(K=\ker(\Delta_1\to\Delta_0)\), decide the exact v183 canary

\[
 \dim_{\mathbf F_3}(M_1/J_KM_1)
 \stackrel{?}{=}
 \dim_{\mathbf F_3}M_0.
\]

Independently and regardless of that answer, decide the pointed relative
multiplier membership

\[
 e_1\stackrel{?}{\in}I_0d_1,
 \qquad I_0=\ker(\Lambda_1\to\Lambda_0).
\]

A positive relative result must return one literal word-bearing coefficient
\(\mu_1\) reducing to the registered roof solution \(0\). This task is one
finite canary. It does not assert all-rung saturation or promote \(\mu_1\)
without the universal identity required by v175 or a structural all-rung
instance of v183.

## 2. Hard input gate

Production accepts exactly:

1. a helper-accepted positive task192 exact-word receipt and its immutable
   run/artifact identities;
2. a helper-accepted positive task193 production receipt compiled from that
   same exact word;
3. the cross-checked final task194 producer/checker identities if its exact
   sharded correlation is used.

Missing, resource-only, malformed, mismatched-word, or synthetic input is
UNKNOWN_INPUT. Do not substitute \(g_{760}\), a task193 SELFTEST word, a
roof row, Jennings data, one PB3/PB4 component, or a previously projected
residual for either \(d_1\) or \(e_1\).

## 3. Frozen modules and literal rows

Use the genuine seven separately tagged contexts and the common diagonal
source action. Reconstruct \(d_1\) by evaluating \(g_{760}\) through the
same task193 affine-prefix compiler used for \(e_1\). Reconstruct \(e_1\)
from the accepted corrected word, not from producer-supplied sparse rows.
For both rows retain:

1. the exact source word;
2. all two hexagon and five ordered pentagon substitutions;
3. affine prefixes and left-Fox signs;
4. the three block-tagged second-level rows;
5. termwise \(D_1=0\);
6. reduction to the authenticated roof row; and
7. direct comparison with the task193 receipt.

All equality in \(Z_1\) is modulo the complete translated two-PB3 and
eleven-PB4 boundary image. Any complete-correlation acceleration must merge
all shard coefficients modulo three before deleting zeros and before every
pivot.

## 4. Complete diagonal orbit spans

Let the common source generators act simultaneously in all seven contexts.
Starting from \(d_1,e_1\), close a sparse echelon under the actions of
\(x^{\pm1},y^{\pm1}\) until no rank grows. Retain for every rank-raising
column:

1. a literal common source word;
2. its seven context images;
3. the unreduced translated row;
4. complete boundary reduction ancestry;
5. pivot and orbit-parent ancestry; and
6. reduction to the corresponding roof orbit column.

This stable span is \(M_1\). Build \(M_0\) independently from the reduced
rows and the roof diagonal action. Equality of orbit counts, hashes, or
group orders is not a replacement for the two literal stable-span replays.

## 5. Complete relative-kernel generator gate

Construct a word-bearing generating set for
\(K=\ker(\Delta_1\to\Delta_0)\). A deterministic Schreier traversal of the
marked roof diagonal image is acceptable. A normal generating set is
acceptable only if its conjugates are saturated under the full
\(\Delta_1\)-action before use. Retain:

1. the marked roof state graph and prefix-closed source section;
2. every non-tree Schreier word used;
3. its identity image in \(\Delta_0\);
4. its exact affine image in \(\Delta_1\);
5. a generation/order or complete spanning proof for \(K\); and
6. mutations changing a tree parent, signed edge, word order, or kernel
   value.

An arbitrary short-word list or the support of a candidate \(\mu_1\) is not
a complete \(K\)-generator certificate.

## 6. Exact pair-coinvariant decision

Using only the certified \(K\) generators \(s_i\), form

\[
 J_KM_1=\sum_i(s_i-1)M_1.
\]

Compute exact dimensions of \(M_1\), \(J_KM_1\), \(M_1/J_KM_1\), and
\(M_0\), retaining all sparse action columns and echelon ancestry. The
terminal is exactly one of:

1. PAIR_SATURATION_PASS when the two dimensions in Section 1 agree;
2. PAIR_SATURATION_FAIL when they differ, including an explicit nonzero
   kernel class for \(M_1/J_KM_1\to M_0\); or
3. UNKNOWN_RESOURCE.

PAIR_SATURATION_FAIL disproves only this v183 edge condition. It is not a
proof that \(e_1\notin I_0d_1\), and it is not a no-lift result.

## 7. Direct relative-multiplier decision

Using the complete \(K\)-generator certificate, build

\[
 I_0d_1
 =\sum_i\Lambda_1(s_i-1)d_1
\]

by closing every \((s_i-1)d_1\) under the full diagonal action. Reduce
\(e_1\) through this complete stable span.

1. On membership, return a coefficient chain on literal common source words,
   reconstruct \(\mu_1d_1=e_1\) directly in all seven blocks modulo the
   complete boundary image, and prove that \(\mu_1\) reduces to the selected
   roof coefficient \(0\).
2. On nonmembership, return a fresh dual which annihilates every column of
   the complete \(I_0d_1\) span but pairs nontrivially with \(e_1\).
3. On resource exhaustion, return UNKNOWN_RESOURCE.

Do not infer the direct answer from the pair-saturation terminal. The receipt
must carry both decisions.

## 8. Independent checker and SELFTEST

The checker imports no task195 producer helper. It reconstructs the two
actual rows, diagonal actions, Schreier kernel words, orbit spans,
coinvariants, direct multiplier chain or dual, and all boundary reductions.
Partition expensive correlations differently from the producer.

SELFTEST must contain at least three noncommutative cases:

1. saturation passes and a chosen downstairs coefficient lifts;
2. saturation fails while \(e_1\) nevertheless remains in \(I_0d_1\),
   proving that saturation failure is not relative nonmembership;
3. relative multiplier membership fails with a complete dual.

Mutations must cover input-word identity, context order, left translation,
global-zero merge, orbit closure, kernel generation, \(s_i-1\) sign,
coinvariant dimension, reduction map, multiplier coefficient, boundary
ancestry, dual annihilation, stale round, checkpoint, and resource terminal.

## 9. Driver and final ledger

Use the generic GHA runner only; do not edit a workflow. The driver must pin
all inputs and current producer/checker/fixture files, reject stale outputs,
use exact-one producer/checker markers, compare exact terminals, and write a
nonempty sentinel only after independent acceptance. The driver must not
self-pin its own SHA inside itself; the immutable GHA head is its authority.

Final reply fields:

    TASK192 EXACT WORD INPUT:                    REQUIRED / NOT YET SUPPLIED
    TASK193 ACTUAL SECOND-RUNG INPUT:            REQUIRED / NOT YET SUPPLIED
    COMPLETE DIAGONAL ORBIT M1:                  NOT EXECUTED
    COMPLETE K GENERATOR CERTIFICATE:            NOT EXECUTED
    PAIR-COINVARIANT SATURATION CANARY:          NOT EXECUTED
    DIRECT RELATIVE FIRST-SUCCESSOR MULTIPLIER:  NOT EXECUTED
    UNIVERSAL ALL-RUNG PROMOTION:                NOT DECLARED
    COMPATIBLE COFINAL LIFT / FAKE / IHARA:      NOT DECLARED
