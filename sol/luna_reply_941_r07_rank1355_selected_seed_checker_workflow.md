# Task941 -- selected current root seed / one-pivot checker handoff

## F1. Frozen implementation; actual run remains with root

Read full Task940 and the earlier Task930 seed30 checker handoff. Implemented
only the authorized new checker, workflow and this reply. Successful old
versions remain unchanged. No local Python/GAP, numerical or syntax run,
network, credentials, git operation or GHA dispatch was performed.

The following are static source receipts, obtained with Get-Item/Get-FileHash:

| File | Bytes | SHA256 |
|---|---:|---|
| search/check_d972_r07_actual_root_seed_materializer_v3.py | 64626 | eca60918eb943edddc321054f04b8547b3e88e5f7421f4de1e09ea04d7ca2701 |
| .github/workflows/d972-r07-actual-root-seed-materializer-v3.yml | 21657 | 4eb1533a431976eed22df1f46e7c6964fdc6b0378d565223c01b670d0b00eb94 |

The workflow pins Task940's producer to 86643 bytes/SHA256
36cc620bdc1b772a4eb4067f7e5b490dab851fb41213c5cd8a5487855207a332.
Root and Task943 received these frozen identities. No known static blocker
remains; GHA syntax, bounded tests and actual comparison have not run here.

## F2. Exact authority and narrow replay

The current scalar authority is run33954712636/1 at
92c98486ab659f7e3358fc3c4afb53ab6b78293d, candidate9966008518,
31781 bytes, archive digest
sha256:148b028ec8b17543a85a563a8d0275fc93361168adda85d8147cd1dbc41207b3.
The manifest/result/checker/source receipts use the Task940 exact size/SHA
pins. Its manifest authenticates launch.json and every retained payload.
There are 176 serialized scalar records; the informative current calculation
has 44 character0 scalars and three other zero roots, not 176 independent
nonzero-root tests.

The selected seed/character/value are derived from the authenticated first
nonzero chronological record and joined to first_violation, RawDual, current
head/lambda, direct-seed receipt and per-seed relation receipt. The actual
fixed authority selects character0/seed34/value1, with first_violation SHA
4ac03a0cf24b5066987a29f5504fbd644ae071bd0ec30656cab218bf0c9ef1b6.
Its selected relation SHA is
f77749fb00a882ee6118d7f3f00a828f7575f4065ca1bc418c668e5841b489be.
These are retained parent results, not a new materialization claim.

No old scalar arithmetic is rerun. No seed30/seed2 selection, old q, support902,
or selected-support constant is used. After authenticating each Task554 full
body, only four selected-seed expressions and its root/body identity survive;
the next full body is not loaded until the previous body has been released.
The ordered seed-bearing raw event chain must match the selected relation
receipt before modulo3 coefficient collection. Cancelled literal roots remain
referenced; numerical replay uses only the derived nonzero coefficient support.

The selected raw seed is evaluated once using the accepted checker arithmetic.
Before subtraction, its selected character row's packed SHA, support and q-dot
must match the pinned direct receipt and saved direct value. The read parent
entries for character0/seed34 are SHA
5990517725f4a2cd3e52aab5e43ecf00d636d9d3805aa6259805aa858fdb9d82,
support1464. They are joined from the receipt, not introduced as general
constants. raw_seed.selected_direct_receipt records that join.

The checker authenticates P1 instructions in one explicitly 1MiB-buffered
scan, authenticates the degree2 cache and twelve lower blobs, and reconstructs
the raw seed minus its complete selected lifts. All96776 lower coordinates
must be zero. The complete filtered projector, plain selected-character
source, independently applied B row and current q/lambda pairings must agree.
There is no actor/lower-dot or orbit pass and no new actor engine.

## F3. Current state and final separator

The state reader authenticates the immutable rank1354 Task904 parent and
the accepted seed30 delta from run33946247365/1/artifact9963533999. It reads
old instruction ancestry only to authenticate the stream and recover saved
insertion-order pivot metadata; it does not rebuild old offers, Conn or any
old physical reduction. The row reader addresses old physical.bin for indices
0..1353 and the accepted seed30 normalized row for index1354, without writing
a concatenated state.

Current authority is rank1355/generation8060/head
36feb776736c6587ce9f64d6f5acb883385074a7cc2eed4c2ce7eb8675e71342,
with the Task940 current lambda and saved target remainder. The accepted
root-scalar checker directly binds that lambda to all1355 saved rows and the
saved target pairing1. Old state, prior delta and target derivations remain
explicit premises.

The actual new physical row is reduced by the old1354 pivots in insertion
order followed by the saved seed30 pivot. A nonzero remainder may append only
one normalized pivot: offer8060, pivot_id1355, generation8061, rank1356.
Raw and normalized rows, scale and all earlier pivot zeros are distinguished.
The saved current target receives only this one new elimination; its older
reductions are retained by parent references, never replayed or copied.

For a next Separator, reverse substitution is followed by a direct pass over
the FINAL lambda against all1354 old rows plus both delta pivots. Both the
parent current target remainder and the newly updated remainder must pair1.
The emitted direct_pairing includes the row count, SHA of the zero pairing
bytes and both target pairings. lambda_rho2_basis explicitly says
accepted-parent-target-derivation; no original rho2 body is downloaded or
re-solved. A zero updated remainder is only ConnectionMemberCandidate.

## F4. Candidate ABI and independence

The flat output retains the accepted eight/nine-file delta layout:
manifest.json, result.json, instruction.json, source-d.bin, physical-raw.bin,
physical-remainder.bin, physical-normalized.bin, target-remainder.bin, and
lambda.bin exactly for Separator. Parent-plus-one-pivot manifest fields now
reference the current accepted delta manifest/head. The state parent declares
immutable-state-plus-one-accepted-delta, retaining its base and delta receipts.

The checker independently reconstructs every result/instruction/manifest field
and every binary, then compares exact candidate bytes and the entire roster.
The ABI includes selected-seed ancestry/relation, dynamic selected support,
raw direct receipt, current parent chain, one target update, final direct
pairing receipt and existing literal-replay limitations. launch receipt uses
the agreed file label launch.json. An in-progress dangling declaration was
removed before this freeze; there is one main and one reject_test definition.

The new checker adapts its own accepted seed30 checker source
f4f8ba2d342cb60e2c70b708b8847768a78ebde40dd0a52879f460cb558eab36.
It neither imports nor executes producer arithmetic. Runtime imports are its
accepted checker-v2/v15 lineage and the successful rank1355 checker
f3c7ca2586a3734334b7d9823316601d18ab1d36561c72aa2c23c2aedfca0e62,
used only for the fixed delta reader, parent identities and freshness checks.
The old scalar execution path is never called. No global monkeypatch is used.
Shared accepted-lineage and derivation-premise limitations stay explicit.

## F5. CLI and single GHA execution path

Common parent arguments, with the four blocks in order:

```text
--scalar-root CURRENT_SCALAR --delta-root SEED30_DELTA --state-root TASK904
--prepare-root PREPARE --block-root B0 --block-root B1
--block-root B2 --block-root B3 --p1-root P1 --task712-root TASK712
```

Producer command is python -B -u
search/d972_r07_actual_root_seed_materializer_v3.py with those arguments and
--output-root FRESH_OUTPUT. Checker command is python -B -u
search/check_d972_r07_actual_root_seed_materializer_v3.py with those arguments
and --candidate-root FRESH_OUTPUT.

The workflow authenticates ten exact artifact tuples: P1, five Task554,
Task712, old physical state, accepted seed30 delta, and current root-scalar
candidate. It uses the successful parent acquisition/receipt pattern. The
current candidate contains its launch/source receipts, so neither current
diagnostics nor retired scalar/rho2 downloads are prerequisites.

Execution is serial: source pins/LF/BOM/ast gates, producer --selftest,
checker --selftest, actual producer, actual checker. The checker has four
tiny changed-interface canaries: selected relation/dynamic support, stale
head rejection, split-state insertion with one target update, and rejection
of a bad row in the final-lambda pass. No historical suite or additional
framework gate was added. These authored tests are unrun locally.

Progress is flushed visibly by phase/count and retained in logs. Each actual
program has a40-minute cap; the job has a90-minute cap. Candidate upload is
gated on checker PASS. Diagnostic upload is if:always() and retains small
receipts, summaries, logs and candidate output, not large parent copies.
Workflow dispatch is root-only; the work-branch push marker is
[r07-actual-root-seed-materializer-v3-run].

WORKER_RUN_ID=NOT_RUN; WORKER_COMMIT_SHA=NOT_CREATED_BY_WORKER.
Root records the eventual commit/run. No new pivot/target numerical result is
claimed here. No grade2/full A0/COMMON/COFINAL/FAKE/IHARA closure is authorized;
cross_checked=false, verified=false throughout.
