# Task1117 — C側独立のactive相対鎖と引用差分

宛先: 既存 packet_checker / Luna。1114/1110を凍結する。指定返信 sol/luna_reply_1117_r07_C_active_chain_closure_refinement_v1.md、末行 TASK1117_VERDICT:。補助資料は新TEMP/shadow-atelier-audit163/task1117/。repo変更は指定返信だけ。新agent/Git/GHA/network/credential/Python/AST/import/compile/数学/自己試験/既process操作/原物大規模再hashは禁止。Pの私的source/設計表/返信を読まない。

sol/luna_task_1115_r07_cold_closure_public_contract_v1.md を全文読み、Solの共通数学境界だけを使う。1115作者の未完成資料と1116は読まない。現v6/18親/k128/同capsは不変。本便はv6の全型受領/GHAへの追加待ち条件ではない。固定例1834/8539、後着実a/p/headを変数にして1962を仮定しない。

Solの初期cold公開候補は、現在行/offer/lead/target/lambdaと全登録相対target各式・元rolling各リンクの実照合をactiveに残す保守形である。必要instruction全body/target JSON/row-manifest/native root/層境界は実bytes。元97/元word/rho2/Γを既前提から昇格しない。1114 K02等の旧body/rolling全引用案を、単にそのまま採用済みとはしない。旧別λ1450/1578/1706の問いと現λ全R行の問いは区別する。

具体納品:
1. C6の全33依存/165区間/42callsiteを原索引にし、どの現native helperがどの相対式・rolling body/除外字段/predecessor/root/schema/中間targetを読むかを全直読field・戻り値・次consumerまで実sourceへ結ぶ。新active条件で残す範囲と引用へ移せる範囲を独立に起こす。raw範囲/名前/line/SHAを保存し、P結論への一致を主張しない。
2. K01–K15をこの保守条件へ再分類。残る元基点/8059/old64/native層のリンクと原97への条件付き命題を分ける。旧1450/1578/1706 pairingを引用するには何の実採用条項が必要かを具体化する。未読旧fileを all_parent_files_and_directories_unchanged=true、旧rechecked count、full original rho2/current mathへ補完しない。
3. 相対鎖の再計算に旧phase/literal/reduction全bodyが要るか、D/hashだけを必要とする経路かを実consumerから示す。hash参照だけで語を新再導出しない。共通TCB内のfile closureが未列挙ならそのroleをactive保持し、最小性や速度改善を先取りしない。16実fixed payload/P1/maps等はreceiptで代用しない。
4. 提案したcold availabilityのA全raw再hash基準/B未採択storage世代＋既完全復元票を区別し、Bが何を信頼するか、取得不能/長さ一致誤内容/世代差/複数context同blobの扱いを具体化。初回全原物と全復元の独立比較、logical occurrence/空dir写像、per-run current active保全とcold可用性を別命題へ分ける。
5. 反例検問: 隣接零2段の交換はtarget digest列だけでは通る、各record/rolling/originを要する。同じrowの段併合はsrを壊す。candidate ordinal≠accepted local/採用0層、native foreign root、有限DAGの循環/同content異辞書、full h最後だけコピー、異なる旧λの代用、cold欠品、OPENの偽昇格を具体判定へ結ぶ。実fixtureは作らない。
6. 全資料目録、公開ABI設計へ戻す欠品/未解決仕様を短い返信へ。現Cの実算を新しく回したとはしない。H1/H2/H3別、rank長要素走査とwalltimeは別、包括GHA認可を毎試行の新たなuser承認要件へ狭めない。

独立数学監査1111の役割は代行しない。公開契約の具体化に必要な自系C閉包の調査として閉じる。
