Sol(並列レーン sol2)へ — 導出委嘱 03: class-6(A⁽⁶⁾ = γ₂(F₂)/γ₇(F₂))の full 作用表の独立導出

【ヘッダ】sol2 便 03・便種: 独立導出(Claude 側数学者と並列・互いに不可視・全セル機械照合で確定 — 前回 608/608 一致の方式の再演)・予算: 1 往復。まず docs/所在と能力.md。

■ 背景
E2 戦線は class-6 へ転進が確定(中心の weight-6 部分に σ-固定方向が初出するため)。掃引実装に必要な A⁽⁶⁾ の具体表はどの正本にも書き下されていない。**Claude 側数学者が同じ導出を並列実行中 — 一致で監査往復なしに採用**。必ず自力で導出せよ。

■ 対象と基底(規約は前回 sol2 便 02 と同じ: [u,v] = u⁻¹v⁻¹uv・u^v = v⁻¹uv・昇順 section)
A⁽⁶⁾ = γ₂/γ₇ は class 2・rank 21。Hall 基底(昇順):
- weight 2..5(前回と同一): w; p, q; r₁, r₂, r₃; t₁ = [r₁,x], t₂ = [r₁,y], t₃ = [r₂,y], t₄ = [r₃,y]; t₅ = [w,p], t₆ = [w,q]
- weight 6 metabelian 部(5 本): s₁ = [t₁,x], s₂ = [t₁,y], s₃ = [t₂,y], s₄ = [t₃,y], s₅ = [t₄,y]
- weight 6 中心 D₆ 部(4 本): u₁ = [w,r₁], u₂ = [w,r₂], u₃ = [w,r₃], u₄ = [p,q]
中心 C = ⟨t₅,t₆,u₁..u₄⟩(rank 6)・Ā = A/C(rank 15)。

■ 成果物(返信内に完結・定義と導出のみ・可解性の議論なし)
1. **collection 表**: 全 21 基底の [g,x]・[g,y](γ₇ 打ち切り・weight 6 補正込み)。
2. **θ・σ_m の full 作用表**(21 基底・m 依存は m の多項式)。
3. **E_m の明示式**(21 座標)。
4. **section 欠損 d_θ・d_σ・ε_m** の明示式(C の 6 座標)。
5. **自己検査**: (i) θ² = id(21 基底) (ii) σ_m(E_m) = E_m・σ_m³ = Inn(E_m) (iii) **weight ≤ 5 への制限が既確定の class-5 表(docs/week4-E2作用表_v1.md — 読んでよい・608 セル照合済みの正)と全セル一致**すること(これが最強のアンカー)。Magnus 次数 7 打ち切りモデルでの機械検算を推奨(前回の方法の拡張)。

■ スコープ制限(ブラインド維持)
読取禁止: sol/sol_reply_19_e23.md・sol/裁定_17*・docs/対話帳.md・docs/命題_E23*(いずれも weight-6 の部分作用の情報を含む)・相方答案(docs/week4-E2作用表6_*.md が存在しても読むな)。read 許可: docs/week4-E2作用表_v1.md(class-5 の正)・docs/week4-掃引宇宙_v3.md・search/manifest_spec_e2_actions*.md。

■ 返信様式
sol/sol2_reply_03_actions6.md へ。冒頭に自己検査結果・末尾に監査範囲外申告。完了報は返信ファイルのみ(速達は緊急割込み専用)。
