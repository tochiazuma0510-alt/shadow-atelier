# Task981 — 保存continuationの別runner累積64再開・限定監査

役: Luna独立監査。変更可は `sol/luna_reply_981_r07_cross_run_resume64_audit.md` だけ。
Task978を最終freezeした後に着手する。公開票/既存source/workflowを修正しない。
ローカル数値/Python/import/AST/GAP、network/git/credential、追加agentは禁止。
source静的読取、JSON metadata、bytes/SHA照合だけ可。rootがgit/GHAの唯一のbroker。

Task980、reply979、Task977/978の最終票と新completion schemaを全文読み、
`.github/workflows/d972-r07-complete-oracle-cegar-resume64-v1.yml` の全完成blockを独立に監査する。
producer/checker算術は凍結971/C v2を再利用し、今回の新境界は別runnerでの保存継続である。

1. 元14親の正しいtuple/source/raw、Task554限定failure、新しい成功completion親のlive由来を保つ。
   成功run/artifact/entry pinsはrootの実観測だけ。未観測を空欄で起動拒否する間は静的構造と分ける。
   元失敗diagは旧証拠として残すが、成功親へ昇格させない。
2. source19＋新C=20/raw3、元P/修理C/今回のrunとruntimeの由来を区別する。
   Python全文 `3.13.15 (main, Aug  6 2026, 02:15:18) [GCC 13.3.0]` と NumPy2.5.1、
   全source/owner/start/fixedに一致してからP起動。旧selftest成功は実receipt認証、再走0。
3. completion親全体と別mutable outputを互いに包含させない。元2584file/420dir/346710509Bを
   hidden含め全copyし、同じrank1418/gen8123/completed32/Separator/HEAD hashを認証する。
   startはrank1386/gen8091/completed0。旧32 snapshotsはappend前、最終snapshot/checkpoint null。
4. Pは一回の `--resume --max-appends 64 --max-seconds 5400`、外100分。
   capは累積、旧builder再生成0と起動時の全保存row/scalar測定を区別する。
   新invocation一件でbefore32/旧HEAD/cap64/resumeを結び、既存二件を改変しない。
   HEAD/resultだけmutableとして退避し、それ以外の全旧prefix bytes/dirsを保持する。
   UNKNOWN_CAP/UNKNOWN_RESOURCE/二候補の型、exit3と旧result誤用、durable phase ahead HEADを点検する。
5. C通常CLIは旧32＋新追加の全after-prefix/current checkpointを独立に再生する。
   内10800秒/外190分/job330分/7GiB。全四scope/8059/54433/二aux/96776/mod54/四B、
   terminal/HEAD/invocation/stdout=report/全arrays JSONのgateを維持する。
   32<=after<=64、rank=1386+after、generation=8091+after、今回追加数=after−32を混同しない。
6. C前後の全output、元14親＋completion親、source/rawの不変。実run receiptと小receiptのhash join。
   全PASS後だけcandidate、alwaysは途中出力/hidden/元親/新旧code/log/failureを保存、retention30日。
   未走PASS/CV9/grade2/A0/verifiedを宣言しない。新3rd independenceも捏造しない。

必要修正は具体的な最短原因・該当行とともに980/rootへ送る。新regressionを自動追加せず、
既走suiteでは覆わない新境界で必要と判断した場合だけ根拠を提案する。
sourceレビュー完了と実runtime/CV9待ちを分け、最終bytes/SHAと読了範囲を残す。
最終行 `AUDIT_981_VERDICT:`。
