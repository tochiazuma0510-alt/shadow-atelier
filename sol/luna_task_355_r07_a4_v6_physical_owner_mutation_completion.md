# Luna task 355 - A4/v6 physical-owner mutation completion

Role: Luna, bounded implementation only.  Read this mail and every numbered
prerequisite first to last before editing.  Do not run Python, Node, GAP, GHA,
a workflow, git, or network.  Read-only PowerShell inspection and hashing are
allowed.  Use `apply_patch` for repository edits.

Task345 ended honestly at `BLOCKED / UNEXECUTED`.  Its algebraic and DAG core
is now a frozen implementation base, not an accepted result.  V6 has one
purpose: replace the incomplete in-memory mutation shell by the complete
producer/checker-separated actual-owner and physical-transport SELFTEST
required by task345 Section 9.  Do not redesign the A4 mathematics, rerun old
searches, or weaken any v5 validator.

## 1. Binding prerequisites

Read in full, in this order:

1. `sol/luna_task_345_r07_task343_a4_v5_bidirectional_dag_repair.md`;
2. all five task345 outputs and the complete reply;
3. `sol/sol_reply_343_r07_task336_a4_v4_code_performance_audit_v1.md`;
4. v285, v286, v288, v290, v291, v292 and v293;
5. the complete accepted task198 and task176 physical authority chains used
   by v5; and
6. v220 Delta113.

Every task345 requirement remains normative.  V5 is not accepted and may be
copied only as the starting source for a new version.  Do not edit any old
owner.

## 2. Sole permitted outputs

Create only:

- `search/d972_r07_word_independent_successor_kernel_v6.py`;
- `crosscheck/check_d972_r07_word_independent_successor_kernel_v6.py`;
- `search/d972_r07_word_independent_successor_kernel_gha_driver_v6.g`;
- `search/certs/d972_r07_word_independent_successor_kernel_selftest_v6_20260829.json`;
- `sol/luna_reply_355_r07_a4_v6_physical_owner_mutation_completion.md`.

Do not edit v1--v5, workflows, proof files, v220, or any other path.  A short
stub, an empty expected registry, or a wrapper around v5 is not an
implementation and must be reported `BLOCKED / UNEXECUTED`.

## 3. Preserve the frozen v5 core

Retain, with versioned schemas and fresh physical pins, the v5 authority
adapter, forward producer DAG, reverse checker DAG, fused 6,441-row bridge,
mixed B/K basis, dual/correlation calculation, discrepancy and action
recurrences, public word-bearing K roster, v280 anchor, typed checkpoint,
v290 accounting, atomic terminal route, and acyclic v293 driver graph.  Apply
only literal correctness fixes forced by the new mutation layer and record
each such change in the reply.

The driver must not pin its own bytes.  Producer and checker must not import
one another's mutation engine, validator result, transcript, arithmetic
instance, DAG, echelon, row state, node id, or digest.

## 4. Complete exact rejection registries

Keep the 48 task343 mutation names and order.  If a name must be versioned,
the fixture must contain an explicit bijective old-to-new table and the reply
must justify it.  The fixture must contain complete maps

```text
expected_rejections.producer[name]
expected_rejections.checker[name]
  = {normal_validator: <exact stage/validator>,
     first_rejection: <exact narrow reason>}
```

for all 48 names on both sides.  The expected map is comparison data only:
no mutation-name branch may use it to choose, manufacture, or bypass the
ordinary validator.  Catch only the registered narrow rejection type.  A
KeyError, arbitrary exception, missing precondition, or broad catch is a
SELFTEST failure, not an expected rejection.

Every baseline must first pass the same ordinary validator unmutated.  Every
record must prove that the actual canonical owner or physical bytes changed,
the registered ordinary validator was entered, the observed exception was
its first rejection, the reason equals the fixture exactly, the owner was
restored, and exactly one well-typed terminal envelope remains possible.

## 5. Physical-owner routes, not detached Python slots

Replace the current `OwnerRoute`-only evidence wherever the load-bearing
owner is physical.  Use bounded temporary files under an owned output/test
directory; start from the actual authenticated bytes or actual canonical
ordinary envelope, mutate that owner, reseal only derived local envelopes
needed to reach the intended validator, then call the same open/parse/
ordinary-validation path used outside SELFTEST.  Never modify a frozen
repository input in place.

Across the fixed 48 rows, producer and checker must independently cover all
of the following, with the fixture identifying the concrete owner path,
transport class, validator, and first reason:

1. physical authority byte, byte-length, SHA, resolved containment,
   traversal, link/reparse/no-follow identity and opened-handle recheck;
2. task198 row, seven chunk boundaries/digests, bridge occurrence fields,
   typed-coordinate ABI, row ordering and the 6,319 transition;
3. actor/inverse and forward/reverse DAG edge/state owners, all 65 seed
   families, occurrence and translation owners;
4. raw B/K pivot, inherited scale, dual pullback, correlation, zero/member,
   discrepancy `Q,c,s`, K ancestry, word/ledger DAG and action matrix;
5. per-item rho0/rho1/q and the independently derived v280 least-index
   anchor/change-of-basis owner;
6. resource cap, checkpoint save/restore/rebuild/canary, partial/stale
   checkpoint, atomic output, stale output, exact-one terminal, last sentinel,
   and input TOCTOU substitution.

An in-memory mutation is admissible only for a genuinely ephemeral algebraic
state whose ordinary validator consumes that same live state.  It is not
evidence for a physical owner.  A serialized copy is admissible only when the
ordinary parser and validator read that exact serialized owner; a detached
before/after dictionary comparison is not.

Platform behavior must be explicit.  Linux GHA exercises symlink/hardlink/
TOCTOU routes.  Windows must either use one real CreateFileW handle with
reparse/FileId/read/recheck or return its preregistered typed unsupported
input stop; it may not claim that two separate probes are one-handle
evidence.

## 6. Precondition and ordinary-route discipline

The unmutated actual route must establish every nonempty candidate/prior/
translation, MEMBER/ZERO/rank-rise, K/dual, anchor and checkpoint
precondition before a mutation is selected.  If the actual normal route does
not supply one of these owners, stop at that first named owner/API and report
`BLOCKED`; do not fabricate a miniature owner, choose an unrelated nonempty
object, or add a mutation-only typed flag.

Producer and checker build their baselines and mutation ledgers separately.
The checker may read the producer's sealed ordinary result as a claim, but it
must reconstruct its own physical owners and expected mathematical values.
Neither side may accept a copied `reached_normal_validator`, before/after
digest, first reason, or mutation result from the other.

## 7. Resource, checkpoint and terminal requirements

Meter physical bytes, canonical serialization, temporary output bytes,
opens/writes, mutation count, validator entries, restore work and current/
peak resources before expensive operations.  Mutation overhead has separate
caps and may not be hidden in the ordinary 14,400-second program budget.
Preserve task345's bounded terminal reserve and v290 counter composition.

SELFTEST must reject stale owned v1--v6 outputs, use atomic
flush/fsync/replace plus directory durability where available, and write the
sentinel last only after exact-one producer/checker terminal lines and a
nonempty sealed independent verdict.  `UNKNOWN_INPUT` and
`UNKNOWN_RESOURCE` remain typed nonmathematical terminals.  Hard exceptions
remain hard failures.

## 8. Driver, reply and audit gate

The ASCII GAP driver pins the exact final v6 producer/checker/fixture and all
frozen physical authorities, uses repository-relative Linux/Windows-safe
paths, and contains no self hash, workflow edit, sleep, retry, poll, pool,
local parallelism or hidden subprocess.  It must preserve the explicit outer
transport reserve beyond the inner producer/checker envelopes.

The reply must give exact bytes/SHA for all four machine/fixture files, the
complete import/process/authority graph, and line-numbered traces for every
task345 Section 11 item.  For each of the 48 names, give producer and checker
owner path/slot, physical or ephemeral classification, before/after digest
construction, ordinary validator and exact first reason.  State all formulas
and static counts separately from measured data.  Since execution is
forbidden, runtime/RSS/SELFTEST/production fields are `UNEXECUTED`.

If and only if all 96 routes and their normal baselines are statically
present, report `IMPLEMENTED / UNEXECUTED`.  A later fresh Sol(max) audit must
cover semantic independence, mutation ownership, Linux/Windows transport,
checkpoint recovery, exact performance bounds and avoidable slow work before
any GHA run.

End exactly with:

```text
V6 PHYSICAL MUTATION LAYER:     IMPLEMENTED or BLOCKED
IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
SELFTEST / PRODUCTION:           UNEXECUTED
AUTHORITY-V2 INPUT:              PASS / A4 1/3 only
ACTUAL POSITIVE BRANCH:          STATICALLY REACHABLE or BLOCKED
SOL(MAX) REAUDIT REQUIRED:       YES
ACTUAL A4:                       remains 1/3 at most before execution
LIFT / FAKE / IHARA:             NONE
```

`TASK355_R07_A4_V6_PHYSICAL_OWNER_MUTATION_COMPLETION`
