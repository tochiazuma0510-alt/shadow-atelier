# Task983 — 同一ordered語の独立11slotと現grade physical読出し

役: Luna checker実装。変更可は
`search/check_d972_r07_continuation_same_word_eleven_slots_v1.py` と
`sol/luna_reply_983_r07_same_word_eleven_slot_consumer.md` のみ。
公刊C v2/旧source/workflow/repliesは不変。rootがgit/GHAの唯一のbroker。
ローカル数値/Python/import/AST/GAP、network/git/credential、追加agentは禁止。
source編集・読取・metadata/bytes/hashだけ可。新producer982/旧P算術を読取・コピー・importしない。
公開ABI/schema/実入力metadataだけを982と共有し、既存独立C系統の実装を利用する。

reply975全文のDを実装する。reply974 F4/Task982のexact公開node ABIは982が先行保存して渡す。
その待ち時間には独立既存runtime、実11 occurrence、typed E3→Q2/PB3 normal map、
fresh-rho2/contextの入口を読んで具体化・保存する。最終ABIに未観測のfieldsを創作しない。
現在target零/grade2 MEMBERは未観測。独立Dの実PASS、新GHA、CV9を先取りしない。

1. source `check_d972_r07_a0_fresh_precision2_endpoint_signature_v9.py` の
   LocalPc/LocalQ/local_quotients/cfox/LocalWords、build_checker_light/IndependentAllSevenを
   実在確認して利用する。ProducerAllSevenは使わない。既存context/helperの保持TCBは明示する。
   actual Q3 chief/fresh-rho2/Task712/owner/context manifestsをbytes/hash/型に束縛する。
2. 入力は一つのword-manifestとordered JSONL/root/ancestor-index・parent/acceptance/fresh-rho2。
   全nodesのcanonical bytes/EOF/prior-only id+hash/op/args、literal辞書、全receipt joinを認証する。
   各slotが別語や別rootを選べる入口にしない。P側の計算結果はexpected比較値で、Cの答に流用しない。
3. 一般DAGのendpoint＋LEFT Foxを独立に再帰する。
   (a,J)(b,K)=(ab,J+L_a K)、inverse=(a^-1,-L_(a^-1)J)、Powerは整数符号を保つ。
   ActはD(P)+L_pD(W)-L_(pup^-1)D(P)であり、子の実slot endpoint=1を確認した時だけ短縮可能。
   Product順を保ち、単純な加算への置換やF3係数相殺で非可換語を消さない。
   巨大語はflattenせず、slot単位に逐次評価・必要nodeの寿命を管理する。
4. 全11 occurrenceを型付きに保持する。E3は実40-byte、E4は実154-byteの形式、
   同じcoordinate0のordinal1/5も別slot。全endpoint/Fox/root hashを保存する。
   native paper_productはprinted factorsの逆順: H1=3+2−1+、H2=6+5−4−、P=11−10−9+8+7+。
   全printed因子とsign/prefixを保持し、direct B(gW)-B(g)とprefix sumを独立に比較する。
5. **現gradeはv478(2.7)/§3のPB4-dropped二hexagon**である。
   全11を認証した上でfirst6だけを現lower32260/top48384へ型付きに写す。
   E3→Q2はv443の実signed mod3 readout/PSL matching、PB3 normalはv437の
   a=X,b=(YX)^−1,c=Y、(h e_a)→e_z augmentation−h a e_b−h a b e_cを実既存codecと照合する。
   C4 IndependentContext/prefix/aggregate等の実mapに接続し、Task712 owner joinを守る。
   5 E4のendpoint/Fox/printed A18 receiptは保持するが、未収載P零を現gradeへ新gateとして足さず、
   48384一致からfull P零も推論しない。fresh-rho2は実body/manifestを読みDERIVEDで代用しない。
6. 同じrootからnormalized pairを独立にmod54で読み、982のC receiptとroot/residue/18div/pairを比較する。
   source-lower96776、現physical-lower32260、top48384、11slotの型を分ける。
   実target零＋A履歴＋B一語＋C normalized零＋D direct現rho2一致が揃っても、
   選択laneの残るside/localization条件と工房CV9を未了のままMEMBER宣言しない。

新試験は一般Product/Inverse/Actの非単位子、重複ordinal、E3/E4型、endpoint≠Fox、
printed逆順、owner/EOF/同一root、mod54/18の誤結合に限定する。旧suiteの再走は不要。
CLIは親root群・word root・output・明示期限を持ち、FAIL/UNKNOWN_RESOURCEと部分結果を区別する。
全比較PASS以外では成功artifact用PASSを合成しない。candidate/cross_checked=false/verified=false。
最初に公開ABI接続と保持TCBを短く返書へ保存し、source blocksの区切りで報告する。
最後にsource bytes/SHA、実試験未走、正の実terminal未観測と残件を記録。最終行 `AUDIT_983_VERDICT:`。
