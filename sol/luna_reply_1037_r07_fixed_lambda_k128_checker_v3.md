# Task1037 — 同一固定 lambda k128 checker v3

F1. 新 `search/check_d972_r07_fixed_lambda_cycle_batch_v3.py` を **178,914 bytes / SHA256 `1aebf6e47807466ec56426a55e34d0c7f622a5896c40184540e4d153060946d7` / LF 2695 / CR 0 / ASCII のみ / BOM なし / 最終 LF / 末尾空白 0** で作者凍結する。変更対象はこの新 source と本票だけ。Task1032 の最終 helper・返信は先に完成して root へ引き渡し、凍結を維持している。

公開 Task1035（7,700 bytes / `f6623768da098ee5b3d65d6d249dcbcf1273aa47115a00f10d727ccc927e9239`）と Task1037（1,274 bytes / `98a42324c2e3c3721c84a0246ea000a6017f9671e185e3bfd6843840810ca9b2`）を全文読了した。既読の公開1025 / 997 / 1000–1004 / 1011 / 1030の契約を継承する。基点は自系 v2 **177,544 bytes / `4ada8490ef931e639159b2c3522510b6fc2da82551daa9a7aa3f1a1970d0ca90`**。基点の全文読了を継承し、新 source の全差分、変更した新二群と実呼先・CLIを静読した。PowerShell/.NET の静的文字列比較で、宣言した外側識別・登録・二群の変更をメモリ内で逆置換すると、旧全文・全 bytes・同 SHA へ完全一致する。旧 source/WF/票は変更していない。新旧 P 私的 source/票/fixture、ローカル Python/import/AST/数学/GAP/network/git/credential/GHA、新 agent は使用していない。

F2. 通常経路の差分は outer prefix `d972.r07.fixed-lambda-cycle-batch.v3`、`BATCH_SIZE=128`、`CHORD_FIRST_ROSTER_128_THEN_FIRST_AUX`、新 P/self path と予定 workflow path `.github/workflows/d972-r07-fixed-lambda-cycle-batch-v3.yml`、strict 登録境界・その説明だけである。max_batches=1、refill=false、partial_policy=`PRIVATE_PREFIX_FINAL_PHYSICAL_HEAD_ONLY` を維持する。C に `--batch-size` は追加していない。acceptance / invocation / owner / source / selection の新登録を通常 helper で照合し、float/bool/string を普通整数として受理しない。

同じ旧 run `33990567016/1`、head `c57c976c6ba4a9d57c2bd3c7de2a09b0d3cedd70`、artifact `9977040548`、ZIP 304,642,285 bytes / `a7ecd56dba33e35475d72486497b125fe983f4fb00a4fa91be813388373f5792`、旧 completed64 / rank1450 / generation8155 を起点とする。全15親の tuple・全file/dir・30受付entry・内包completion10entry・全historyを保持し、k64実成果のrank1514を初期stateや追加数学親へ入れない。保持Python19・raw3は不変、新P/C二本との全24fileを実acceptance/source pinへ結ぶ。新P pinはrootが実値から作るacceptanceで供給され、Cは登録pathと実全bytes/SHAを読む。旧batch v1/v2を数学helperとしてimportしない。旧親/inner相schema、旧artifact名、nonce32hex、int32/u32、元履歴中の32/64はそのままである。

F3. 全54433弦・2aux・four-character・全8059を同じ固定lambdaで読む。全failed列/全残差表を保持し、弦に非零があれば先頭min(128,m)、全弦零なら先頭非零aux一件、両auxも零なら同current lambdaのCOMPLETE_ZERO_CANDIDATEとする。依存候補を補充しない。各候補のraw/source/primal/P1/四B/reduction、六cycleと零係数、全Ref/祖先、signed普通整数/mod54、source lower96776/physical lower32260/physical48384、全係数と挿入順消去は旧本文の計算である。数値targetの負号、literal correctionの正号、外側scale一回を維持する。

新candidate/row local ordinalは0..127。通常 `row_source` / `compare_phase` / `registered_basenames` / `ProgressAudit` がBATCH_SIZEを参照し、private phase sequenceはbootstrap0、selection1..3、候補iの相pが `3+6*i+p`、最大771となる。128行目/ordinal128、sequence772、二相先を認めない。直後一相だけのdurable tail、公開countを実HEADへ固定する規則、deep copy、同v3内の厳密resume、完成packetのread-only再受付を保持する。初回実験の実行方針はfresh P一回・全C一回である。全旧行dot0と両target dot1、全新相/全row/final、全before/after/入力不変を引き続き要求し、全候補処理後のfinalizerだけが公開physical HEADを閉じる。

F4. 新第一群 `k128-version-registration-and-types` は通常 `check_registration` / `check_acceptance_header` / `check_executable_paths` / `compare_root_records` / `row_source` / `phase_at_sequence` / `compare_phase` / `ProgressAudit` / `invocation_records` / `registered_basenames` / `CandidateFiles` / `prepare_selftest_root` を使う。正128/1/no-refill、portable owner一件の再root、count0 bootstrap、先行一phaseとHEAD不変を小metadataで保存・再読する。旧v2 acceptance/owner/schema/source path/policy64/scope64を拒否する。変更可能な封印は作り直した保存fixtureから通常入口へ到達させる。

row127の正対照は **12,096 bytes** の先頭01・末尾1b・中間零の明示synthetic行を、他127行と区別して `BatchReductionState.row(127)` / `row_source` に渡す。`rows/000127/physical-normalized.bin`、exact descriptor、scopeを保存し、`CandidateFiles.json/object/compare` と `check_file_descriptor` で全bytes/EOF・hash・local offset・scopeを読み直す。ordinal127のreduction相にも同じpacked3/shape[48384] payloadとmanifestを保存して `compare_phase` を通す。128行目を実listに追加した負対照は、単なるIndexErrorではなく通常local offset境界へ到達する。sequence771の正対照と772の拒否、registered basenameの127包含/128非包含も接続した。これは実rank1450のprimal/reduction計算ではなく、明示したmetadata/保存reader fixtureである。

第一群の拒否登録は **28件**。順序は `batch-size-32`, `batch-size-64`, `batch-size-127`, `batch-size-129`, `batch-size-256`, `batch-size-float`, `batch-size-string`, `batch-size-bool`, `max-batches-two`, `max-batches-bool`, `max-batches-float`, `refill-true`, `refill-integer-zero`, `old-policy64`, `old-acceptance-schema`, `old-producer-path`, `old-checker-path`, `old-owner-schema`, `old-owner-scope64`, `row-offset-128`, `phase-sequence-772`, `candidate-ordinal128`, `two-phases-ahead`, `bootstrap-float-zero`, `bootstrap-bool`, `bootstrap-count-one`, `selftest-root-reuse`, `selftest-root-relative`。実拒否の成功は未観測である。

F5. 新第二群 `k128-full-roster-cutoff-and-restoration` は非単位permutation basisと非自明な五係数を持つ全54433行のsynthetic配列を、通常 `select_all_residuals` へ渡す。m=64/65/127/128/129で全failed数m・選定数64/65/127/128/128を要求し、各case最後の非零を全弦末尾へ置く。逆順edge rosterによりedge番号順と保存roster順を分ける。弦とauxの非零同居、弦零/先頭aux、弦零/第二aux、全部零を含む正8caseである。全五係数・六cycle・零係数を保持する。

`witness_records` / `batch_tree_payloads` / `selection_record` / `oracle_view_record` の全配列・descriptor・manifest・witness・view・selectionを保存し、通常 `CandidateFiles` / `compare_phase` / `compare_selection_publication` / `compare_candidate_roster` が全bytes/EOF/全rosterを読む。min式だけの試験ではない。第128witnessはm=128 caseの実synthetic末尾を指し、別fixtureでindex、係数と対応cycle、cycleが指定する語のedge、末尾順序を改変して全再封印後にreaderへ渡す。語改変は選定cycleのedgeを別実roster edgeへ替える固定word入力の変異であり、Eの全raw materializationをこの自己試験で再演したとはしない。

第二群の拒否登録は **8件**。順序は `over-limit-129-witnesses`, `old-first64-cutoff`, `whole-residual-table-last-byte-missing`, `failed-roster-last-index-missing`, `one-hundred-twenty-eighth-roster-index-changed`, `one-hundred-twenty-eighth-coefficient-and-cycle-changed`, `one-hundred-twenty-eighth-cycle-word-changed`, `selection-tail-order-changed`。m=129でも全failed/全残差末尾を保存し、128以降を全表から切り捨てないことを対照に含める。potential等の前相値はsynthetic protocol fieldであり、実Q/実Omegaの再演ではない。全正負fixtureを保存して終了し、実PASSは未観測である。

F6. selftest topは sealed `d972.r07.fixed-lambda-cycle-batch.v3.selftest`、body exact `status/tests/fixture_scope/production_interfaces_used/old_success_suites/actual_anchor_arithmetic_replayed/candidate/cross_checked/verified`。二testsはF4→F5の順、各exact `{name,status,rejected_cases}`。fixture_scopeは非空str、production_interfaces_usedは非空str list、old_success_suitesは普通整数0、actual_anchor_arithmetic_replayed=false、三assurance=falseを保持する。二群が実際に完了した場合にだけPASSを生成する。

GHAでの新二群のCLIは次のとおり。Cへbatch-size引数は渡さない。

```sh
python -B search/check_d972_r07_fixed_lambda_cycle_batch_v3.py --selftest \
  --selftest-root "$REPORT/selftest-fixtures/C" --max-seconds 300 --max-memory-mib 7168
```

`--selftest-root` はselftest時だけ必須で、通常時は拒否。TEMP/RUNNER_TEMP内の未存在root、regular既存parent、全ancestorのlink/junction不在、repo/sourceとの非包含をmkdir前に確認する。selftest時は数学親/acceptance/candidate/output引数を拒否し、正負caseのraw/empty dir/hidden/台帳を削除せず残す。全fixtureの三時点比較・全inner ZIP/entry/explicit dir/全再読・REPORTの明示二除外は公開1030/1035を新WFが引き継ぐ対象である。

通常CのCLIは既存全15root（`--block-root`四回）と `--acceptance` / `--candidate-root` / `--output`、`--max-seconds 10800 --max-memory-mib 7168 --producer-max-seconds 5400 --producer-max-memory-mib 7168`。C外側11400秒、P内5400/外6000秒、selftest内300/外360秒を維持し、k増加で枠を拡張しない。通常実行は既存deadline/resource bridge、成功exit0・資源exit3・不正exit1、失敗時のcomplete/candidate flags解除、candidate/親への書込み禁止を保持する。旧成功数学suiteを再走しない。metadata16は新登録/REPORTの回帰としてWFで別に扱う。

F7. 新k128の採用数、rank、独立率、実秒、RSS/I/O、run/artifact、二群28/8の機械PASSと本C全比較は未観測である。rank=`1450+accepted`、generation=`8155+accepted`であり、128採用やrank1578を予告しない。新lambda oracle null、grade2両字段NOT_DECIDED、full_A0=false、verified=false、LinearだけNEW_BATCH_SAME_WORD_ADAPTER_PENDING・他terminalはNOT_APPLICABLEを維持する。共有TCB/F-fo-1/F-flb-1/返信163 F8.89の限定、正式CV-9と機械結果の区別は継承し、未全文受領の2175–2176から追加昇格を推測しない。公開・実試験・独立最終監査はrootのbroker手順で進む。本票は作者のsource静的完了票である。

AUDIT_1037_VERDICT: CHECKER_V3_SOURCE_FROZEN_STATIC_COMPLETE; SAME_OLD64_FIXED_LAMBDA_K128_ONE_BATCH_NO_REFILL; NEW_TWO_GROUPS_REGISTERED_28_AND_8; NO_LOCAL_EXECUTION; INDEPENDENT_FINAL_AUDIT_AND_GHA_PENDING.
