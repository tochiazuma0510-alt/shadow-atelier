# Luna task 272 - task227 u0 owner-object repair v1

Role: bounded one-owner implementation repair. Read current task227 files and
GHA run 33147874908. Do not run Python, Node, GAP, git, GHA, or network. Edit
only producer, driver pin, and existing task227 Luna reply.

The first executable SELFTEST reached `recompute_package` and failed at the
expected-row check because it calls `.get` on `item["u0"]`. In an occurrence,
`item["u0"]` is the encoded sparse vector list; the typed provenance row is
`abi["u0"][i]`. Compare the reconstructed `expected_row` to that exact typed
ABI owner. Preserve the preceding occurrence-vector equality, the following
whole-roster equality, every mathematical condition, cap, mutation gate, and
terminal. Refresh driver/reply pins and report UNEXECUTED. Parent Sol will
rerun the full serial GHA SELFTEST.

A3 remains 0/3; this is a SELFTEST implementation owner only.
