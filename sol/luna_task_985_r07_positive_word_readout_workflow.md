# Task985 — 保存CEGARから同一語A/B/Cと独立十一slot Dを実行するGHA

役: Luna実装。Task982のsource/返信を完成・freezeした後、この便を続ける。
変更可は `.github/workflows/d972-r07-continuation-positive-word-readout-v1.yml` と
`sol/luna_reply_985_r07_positive_word_readout_workflow.md` のみ。公刊980/981/977/978・既存workflow/sourceは不変。
ローカルPython/import/AST/数値/GAP/network/git/credential/追加agentは禁止。
source/metadata/bytes/hashのみ可、rootが唯一のGitHub/GHA brokerを務める。
983の算術実装は読まない。公開reply983のCLI/ABI・実metadata・sourceのbytes/SHAだけを接続する。

## 対象と成功の意味

Task982のtarget履歴・ordered word・同root mod54/normalized pairと、Task983の独立実十一slot/printed/
full filtered物理値/元rho2比較を同じ一つのword-manifest/rootへ接続する。
現在の実保存入力はcompletion run33988391926/1（全32 C PASS・rank1418/gen8123・非零target）。
root手渡しは `%TEMP%/shadow-atelier-audit163/cegar-completion-run33988391926-a1-pins.json`、
全candidate rootも同JSONに記載。初回wrapperの入力をこの実tupleに固定して設計する。
rootがfreeze前に後続の実success candidateを別に供給した場合に限り、その実pin差分を再監査する。
まだ実結果の無いresume64 run33990567016を成功親に代入しない。

継承宇宙はrung2016→54432の現precision-two/grade2、PB4-dropped二hexagonの32260/48384。
同一語全11slotは保持する。一般targetにsource lower96776零を追加せず、full filteredを使う。
非零targetでも全実読出しと原点照合を行い、そのpositive applicabilityはNOT_APPLICABLEと表示する。
target零ならD条件とside/localizationの未完を区別する。これらのconsumerやworkflowだけから
grade2 MEMBER/full A0/COMMON/cofinal/fake/Ihara/verifiedや新三系統独立性を宣言しない。
UNKNOWN_RESOURCEとFAILは一級で、途中のword/root/十一slot/終端をPASSへ昇格しない。

## 入場と来歴

- marker `[r07-continuation-positive-word-readout-v1-run]` とworkflow_dispatch。
  checkoutは発火HEADへ固定しpersist-credentials=false。rootの既存GHA実行認可を使う。
- 元14親は980の全tuple/accepted Task554 failureを保ち、continuation成功親とfresh rho2の計16親。
  runtimeは今回実観測のPython全文3.13.15 build/NumPy2.5.1を固定する。
  全16のlive repository/head_repository/run/attempt/head/workflow/conclusion/name/id/size/digest/expiry、
  実ZIP全bytes/SHA、安全path/型/暗号化/重複拒否を使い、相互包含しないrootへ解決する。
- Task712はenvelope root（r07-grade2-maps-v4と二wrapper receiptを含む）、fresh rho2は元実artifact全体。
  元rho2の七payload/bodyとC1 rootを読めること。current residualやDERIVED rho2で代用しない。
- 全旧成功suiteの算術再走をしない。実successと保存receiptを認証し、今回source/consumer以外の
  materializer/oracle/CEGAR/Conn再列挙は行わない。P971.load_prefixを呼ばない。
- sourceは新982/983の最終実bytes/SHAと、983が実際に使う保持C9/C4/データ/全import closureを凍結。
  作者分離は保持、PはC算術をimportしない。workflowのASTはGHA内だけで行い、最終sourceをartifactに残す。
- 982 F4公開exact `.acceptance` schemaに従い、16role全file/dir/主receiptとselected七entry/
  HEAD/result/C全prefix/owner/source/start/fixed・actual rank/count/kind/state/target/lambda/terminalを結ぶ。
  consumer_sources二本と今回runtimeを含めてcanonical ASCII+LF/sealをGHAで生成する。
  未観測source pinやentryは空欄で起動拒否し、形だけのPASSを埋めない。
- 16親・元source/raw・acceptanceをreadonlyにし、word output/D output/driver receiptsは相互非包含。
  normalizer辞書は派生outputで、17番目のartifactにしない。親raw-wordのnormalizer receiptと元raw sourceの
  実bytes/辞書recipeの来歴をconsumerが保持できるよう同じ16親とsourceを供給する。

## 新境界だけのcanaryと実走

各新sourceの公開selftestを最初にGHAで実行する。実artifact layout/公開exact ABIを使った新境界だけ。
今回指摘されたLinear時null lambda、binary部分行とJSONL位置、base3の4-trit packing、
一般非単位Act/重複edge/zero-power edge/順序・scopeすり替え・同root mod54・typed十一slot等を、
各sourceの実装済みcanary群とその宣言scopeへ正確に結ぶ。fixtureが実positive terminalであると捏造しない。
canary内容をwrapperに別実装して水増しせず、983との共有は公開インターフェースだけ。

初期の資源境界はP982 internal5400s/outer100min、D983 internal10800s/outer190min、
job330min/virtual-memory7GiB。新canaryは合計internal600s以内/outer12minとして、残る受入/保存余裕を確保する。
CLIの正確なresource引数は完成稿と照合する。速度・終わる反復数・成功を予測しない。
canaryが実親全旧算術を暗に再走する場合は修理を要求し、予算を無言で広げない。

P982は一回、readonly 16親/実acceptance→新word output。C983は一回、同じ16親/acceptanceと
Pの全word output→別D output。全stdout/exit/実時間・current/root/type/EOF・manifest hashを別保存する。
P/Dの同root_id/root_sha256/word-manifest SHA・ancestor/pivot/target-history/normalized pairを結ぶ。
局所D成功・candidate・positive applicability・grade2/side条件を別字段で報告する。

## 保存と最終凍結

alwaysで全16親rosterと実全bytes、source/raw・acceptanceの前後不変、P/D出力全rosterを保存する。
候補uploadは新canary/実P/実D/全不変/完全manifest/同一root joinが全PASSの場合だけ。
always diagnosticsはhidden/pending/途中D/全word/ログ/exit/metadata/元sourceとworkflowを丸ごと残す。
旧16親payloadを新artifactへ全部二重格納する必要はないが、live/ZIP/全file認証来歴と元親tupleは欠かさない。
原producer/completion/次consumerのrun/head/sourceを混ぜない。retention30days、空archiveは拒否する。

まず本文をレビュー可能にし、最終source pinだけ欠けるならその箇所をpendingと明記する。
実値接続後に全文bytes/SHA/LF/CR/BOM/finalLFと正確な再現marker/CLIを返信985へ記す。
実GHA/CV9/positive候補はrootが観測して記帳する。最終行 `AUDIT_985_VERDICT:`。
