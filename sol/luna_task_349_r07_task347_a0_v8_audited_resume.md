# Luna task 349 - A0/v8 audited fast positive resume

Role: Luna, bounded implementation only. Read every numbered section and all
listed prerequisites first to last before editing. Do not run Python, Node,
GAP, GHA, a workflow, git, or network. Preserve every v1--v7 source, receipt,
checkpoint, fixture, workflow, proof, and v220 file. This mail authorizes only
the five new v8 outputs in Section 2 and no execution.

Task347's binding verdict is `REJECT / UNEXECUTED`. There is no frozen-input
forced exception before search. The first authorization/contract stop is the
unconditional production call to `run_real_owner_selftest`; do not describe it
as a runtime exception. The useful v7 mathematical core may be copied only
after its audited invariant is restated and every defect below is removed.

## 1. Binding prerequisites

Read in full, in order:

1. `sol/luna_task_342_r07_a0_v7_fast_positive_resume.md`;
2. all five task342 outputs;
3. `sol/sol_reply_347_r07_task342_a0_v7_code_performance_audit_v1.md`;
4. v265 and v275--v279;
5. `sol/proof_r07_a0_positive_carrier_to_a2_specializer_v284.md`;
6. the frozen ZIP, manifest, sole 86,368,039-byte member schema, q3 receipt,
   joint receipt, and every source pinned by v7.

Task347 supersedes reply342's production-mutation, checkpoint/resume,
independent-Q0, atomic-output, and performance claims. Preserve v276's
triangular equations, v277's persistent boundary owner and Q0-LATE
transition, v278's selected-support direct equality, and v284's future
positive carrier ABI.

## 2. Sole permitted outputs

Create only:

- `search/d972_r07_history_free_positive_fast_resume_v8.py`;
- `crosscheck/check_d972_r07_history_free_positive_fast_resume_v8.py`;
- `search/d972_r07_history_free_positive_fast_resume_gha_driver_v8.g`;
- `search/certs/d972_r07_history_free_positive_fast_resume_selftest_v8_20260829.json`;
- `sol/luna_reply_349_r07_task347_a0_v8_audited_resume.md`.

Return `IMPLEMENTED / UNEXECUTED` only if a literal static trace reaches a
fresh production search, an authenticated resume search, COMMON, every typed
UNKNOWN, and hard STOP without a placeholder, hidden v7-main call, schema
mismatch, fake mutation, or unrecoverable checkpoint. Otherwise return
`BLOCKED / UNEXECUTED` with the first exact owner/API. SELFTEST and PRODUCTION
remain unauthorized until a new Sol(max) full code-and-performance PASS.

## 3. Separate SELFTEST from production

PRODUCTION must not call, import through a wrapper, or recompute
`run_real_owner_selftest`, process faults, document mutations, or checker
mutations. The COMMON checker likewise performs only independent semantic
acceptance and bounded transport; it never reruns SELFTEST.

SELFTEST is a separate explicit mode with its own sealed output and exact
terminal. It may use the same production validators, but its result is not
embedded in a production receipt and production does not trust a copied
SELFTEST Boolean. The driver has disjoint SELFTEST and PRODUCTION routes and
requires an already accepted exact SELFTEST identity before a later
PRODUCTION can be commissioned. Do not execute either route in this task.

Retain the genuine Linux fork/AF_UNIX owner, timeout/death/partial-frame/
blocked-send/cleanup controls, but make every advertised mutation alter an
actual loaded owner before the corresponding normal validator. A post-hoc
same-shaped dictionary, explicit mutation-name branch, copied expected value,
or equality-only validator is not a physical mutation. The F3 cancellation
case must require the exact zero/cancellation result. Producer and checker
construct and mutate their own owners.

## 4. Linear triangular reconstruction

Rebuild the 2,896 P rows from the frozen A*C equations using exactly the
1,011,460 sparse contributions. Replace every quadratic owner identified by
task347:

- no linear `pivot in pivots` search;
- no scan of all earlier pivots for every P;
- no duplicated earlier-pivot gate in both caller and reducer;
- no `pivots[:ordinal-1]` copied slices.

Maintain a pivot set. For each computed P, inspect only its actual support
against that set, require its registered pivot and coefficient, then perform
one prevalidated direct injection. The static work meter must charge exactly
the 289,774 total P-support inspections in addition to A*C contributions.

Represent formal ancestry by hash-consed structural DAG nodes. Row reduction
combines immutable node ids; it does not repeatedly copy a flat symbol map.
Expand and independently replay only the selected finite COMMON support and
bounded checkpoint canaries. Preserve the exact old-to-P equations and
chronological pivot order.

Derive the initial target/remainder/dual exactly once on the production path.
Pass that immutable state into the search; do not rebuild it in constructor,
SELFTEST, and `run`. The resume path checks a freshly derived next-state dual
against the checkpoint exactly once.

## 5. Metered inputs and phase-safe caps

Authenticate paths with no-follow/link-count/fd-before-after/pathname recheck
as in v7, but add wall/RSS/input-byte gates before and periodically during:

- source registry and pin hashing;
- ZIP/raw member handling;
- the 86 MB read, decode, and JSON parse;
- `build_light` and the complete P build;
- ancestry DAG allocation and expansion;
- Q0/heavy construction;
- checkpoint/final serialization.

Where streaming is possible, hash/read once and avoid a list-of-chunks plus
`join` copy. Charge known file size before allocation. An early cap has a
typed phase-safe `UNKNOWN_RESOURCE` envelope which does not dereference an
unbuilt reducer/P/certificate. OOM, programming errors, and unexpected
exceptions are hard STOPs, never mathematical terminals.

Normalize typed input reasons to the driver's safe alphabet; a relative path
containing `/` may be stored in a structured diagnostic but must not be
interpolated into the terminal token.

## 6. Persistent boundary owner and sliced IPC

Keep one production persistent W=2/W=4 fork roster. For each epoch send each
worker only its disjoint support/descriptor slice, not four copies of the
complete 1,188-entry support. Workers still compute `t=g*h^-1`, check `t*h=g`,
accumulate in F3, and return typed frames. Parent coverage, overlap, scalar,
winner, contributor, child-cleanup, and full-epoch gates remain exact.

The first frozen dual still implies four descriptors and 4,752 mathematical
pairs; this is a formula/static identity, not a measured run. On ACTIVE,
avoid a second complete contributor pass by retaining a bounded lossless
winner/contributor accumulator during the one epoch, or explain and meter a
strictly necessary replay. No process SELFTEST occurs before the first real
pair.

## 7. Q0-LATE and independent positive provenance

Q0 is built only after a complete zero boundary epoch. Construct coarse
indices lazily for coordinates actually requested by the correction formula;
do not eagerly retain ten full state-id indices. Reuse or checkpoint immutable
Q0/Gamma/family structures rather than silently rebuilding them on resume.

On COMMON the producer still exports literal correction factors in canonical
order, coefficient-two inverse words, the exact finite sparse solution, joint
kernel/exponent conditions, and eleven-occurrence plus direct H1/H2/P replay.

The checker independently reconstructs every selected item. In addition to
v7's sound direct-row checks, it must:

1. parse the authenticated old checkpoint and bind each selected `o:NNNN`
   symbol to the literal chronological source record and its bytes/fields;
2. reconstruct the selected Q0 state from its id, the Gamma state from its id,
   the claimed K=0 fibre or K!=0 global cursor, membership, section word, and
   schedule relation; integer ranges or a 64-character digest are not proof;
3. reconstruct and bind the actual heavy-input identity used by that selected
   proof; and
4. compare the recomputed correction word, row, scalar, coordinates, fibre,
   and schedule with the receipt before the final sparse equality.

The checker may build only selected Q0/Gamma states; it need not materialize
all 1,469,664 states or all ten indices. It authenticates the producer bytes
but imports neither producer nor its live helper.

## 8. Replayable checkpoint and resume driver

Define a new checkpoint schema. A checkpoint is written only from a clean
owner state: stop and join the fork roster, prove no live PID/channel, then
atomically serialize. It binds the exact v8 code/input identities, resource
envelope, phase, P roster or deterministic lossless P reconstruction data,
formal ancestry DAG, new columns, remainder/formal solution, freshly derived
exact next dual, boundary epoch, and counters.

If heavy work has begun, also bind enough serialized immutable Q0/Gamma/
membership/lazy-index state to resume without rebuilding an advertised
`heavy_complete` owner; otherwise label it honestly as reconstructible and
record the exact rebuild frontier. Correction checkpoints bind every fibre/
global cursor to the exact current dual and heavy identity. Checkpoint size is
estimated and capped before whole-object canonicalization/write; an oversized
checkpoint produces one bounded terminal rather than recursively attempting
the same oversized write.

Resume performs actual restoration. It rebuilds the echelons/DAGs, compares
raw rosters and a freshly computed next-state canary/dual, recreates a clean
persistent roster, and continues from the recorded epoch/cursor. It must not
discard the restored object and restart from pair zero. Every checkpoint the
receipt advertises is restorable; transient live-PID checkpoints are
forbidden.

The ASCII driver supports both a fresh route and an optional authenticated
repository-relative resume sidecar supplied explicitly by a registered
environment/argument ABI. Resume input and fresh outputs are distinct; it
never overwrites the input checkpoint. It validates checkpoint bytes/SHA from
an accompanying portable manifest before passing `--resume`. State exact
staging requirements in the reply. No sleep, retry, polling, pool nesting,
local execution, or workflow edit.

## 9. Cheap UNKNOWN and atomic transport

UNKNOWN checking is a fixed-size false-claim/envelope validation plus one
streaming digest/size of an optional checkpoint sidecar. It does not rehash or
parse the 86 MB source, compile mathematical modules, parse a multi-GB
checkpoint DOM, clone it, or run mutations. The producer receipt includes the
portable checkpoint bytes/SHA and bounded progress summary needed by this
path. COMMON may perform the heavier independent checks in Section 7.

Raw extraction, checkpoint, producer receipt, and checker verdict all use an
exclusive same-directory temporary, flush/fsync, atomic replace, directory
fsync where supported, and failed-temp cleanup. Destination paths are
stale-rejected. Do not write a final owned path directly with `open('xb')`.

Meter and pre-cap final output. Avoid double whole-document canonicalization:
produce the canonical body once, derive the seal from it without rebuilding a
second giant object, then write the final encoding once. The checker uses the
same bounded transport discipline independently.

## 10. Six-hour decomposition

Encode hard outer deadlines and internal serialization reserves:

```text
producer including receipt/checkpoint serialization <= 10,800 s
checker including verdict serialization             <=  7,200 s
artifact/finalization reserve                        >=  3,600 s
hosted-job total                                      21,600 s
```

The producer mathematical deadline must be earlier than 10,800 seconds so it
can stop the roster and write a terminal/checkpoint. The checker has its own
enforced outer deadline. Driver sentinel remains the last successful write
only after exact-one equal full-line terminals and a nonempty sealed verdict.

Report formula/static counts separately from measurements. Because execution
is forbidden here, every runtime/RSS field says `UNEXECUTED`.

## 11. Required static traces and reply

Give exact bytes/SHA for all four machine files and a complete import,
process, and physical-owner graph. Give line-numbered traces for:

- fresh production through first 4,752-pair epoch;
- boundary ACTIVE, boundary zero to Q0-LATE, correction ACTIVE, COMMON;
- early input/light/P cap, boundary cap, heavy cap, correction cap;
- authenticated resume at every advertised phase;
- checker COMMON selected old/Q0/Gamma/fibre/schedule replay;
- cheap UNKNOWN with and without checkpoint;
- hard exception, atomic write failure, stale output, and final sentinel;
- every SELFTEST physical mutation and its actual normal validator; and
- exact removal of every unnecessary-work owner listed by task347.

Do not claim cached trajectory parity, a separator, exhaustion,
nonmembership, compatible lift, fake certificate, or Ihara witness. End with
exactly:

```text
IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
SELFTEST / PRODUCTION:           UNEXECUTED
FROZEN INPUTS:                   PASS or BLOCKED
FRESH SEARCH ROUTE:              STATICALLY REACHABLE or BLOCKED
AUTHENTICATED RESUME ROUTE:      STATICALLY REACHABLE or BLOCKED
ACTUAL A0 COMMON + CHECKER:      0/1
SEPARATOR / NEGATIVE CLAIM:      FORBIDDEN
LIFT / FAKE / IHARA:             NONE
```

Any execution requires a new Sol(max) audit of mathematical soundness,
checker independence, every physical mutation, both resume routes, atomic
transport, Linux/Windows portability, exact performance bounds, and avoidable
slow work.

`TASK349_R07_TASK347_A0_V8_AUDITED_RESUME`
