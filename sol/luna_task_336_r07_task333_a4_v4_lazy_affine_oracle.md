# Luna task 336 - task333 A4/v4 lazy affine/Fox quotient implementation

Role: Luna, bounded implementation only.  Read every numbered section and
every listed prerequisite first to last before editing.  Do not run Python,
Node, GAP, GHA, workflows, git, or network.  Preserve every v1--v3 source,
authority artifact, workflow, proof, and v220 file.  This mail authorizes only
the five new v4 outputs in Section 2 and no execution.

## 1. Binding prerequisites and supersession order

Read in full, in this order:

1. `sol/luna_task_328_r07_task315_a4_v3_actual_local_evaluator_repair.md`;
2. `sol/luna_task_331_r07_task328_ancestry_trie_performance_addendum.md`;
3. all five A4/v3 outputs and
   `sol/sol_reply_333_r07_task328_v3_code_performance_audit_v1.md`;
4. `sol/sol_reply_330_r07_task327_authority_normalization_erratum_v1.md`;
5. `sol/proof_r07_authority_ancestry_trie_evaluator_v268.md`;
6. `sol/proof_r07_full_marked_boundary_closure_erratum_v271.md`;
7. `sol/proof_r07_lazy_full_boundary_affine_kernel_algorithm_v272.md`;
8. v231 and v247, the A4/v1 producer/checker, task176 producer/checker and
   accepted receipt, task198 producer/checker and accepted five-member
   authority bundle, frozen q3 receipt, and
   `search/d972_b345_seedspan_triple4_v1.py`.

V271 supersedes every 13-seed or source-only boundary-closure assertion.
V272 supersedes eager full-marked closure as the production hot path while
retaining it as the completeness theorem.  Task333 is the binding defect
ledger.  A v3 helper may be copied only after its exact audited invariant is
reproved; do not preserve placeholder objects, shaped records, or declarations
whose values were not computed.

## 2. Sole permitted outputs

Create only:

- `search/d972_r07_word_independent_successor_kernel_v4.py`;
- `crosscheck/check_d972_r07_word_independent_successor_kernel_v4.py`;
- `search/d972_r07_word_independent_successor_kernel_gha_driver_v4.g`;
- `search/certs/d972_r07_word_independent_successor_kernel_selftest_v4_20260829.json`;
- `sol/luna_reply_336_r07_task333_a4_v4_lazy_affine_oracle.md`.

Return `IMPLEMENTED / UNEXECUTED` only if a static trace reaches every actual
positive and mathematical negative terminal without a constant placeholder,
unregistered owner, missing import, or schema mismatch.  Otherwise return
`BLOCKED / UNEXECUTED` with the first exact missing owner/API.  SELFTEST and
production remain unauthorized.

## 3. Exact authority adapter and one-pass ownership

Implement one explicit task198 schema adapter.  Rows, layer counts, normal
generation, row/chunk seals, and normal proof are under
`receipt["Delta0"]["presentation"]`; never invent top-level aliases.  The
bridge ledger is `receipt["bridge"]["occurrence_ledger"]` and includes its
actual block, block index, block slot, context, orientation, role, factor
sign, and ten-index spellings.  Authenticate all five authority-v2 members,
their exact resolved paths/bytes/SHA/seals, direct run/head/artifact/ZIP/member
identities, the three task198 source identities, all 6,441 ordinals and seven
chunks, normal-generation arithmetic, bridge maps/digests, evaluator ABI and
canaries, and the literal false witness flags.

Read/hash the 31 MB receipt once, parse it once, and retain one authority
object.  Charge bytes before allocation where streaming permits it.  Do not
copy the 6,441-row roster into the output.  Producer and checker implement
separate adapters and validators; the checker may not import the producer or
trust producer digests/Booleans.

## 4. Lightweight actual affine/Fox runtime

Do not call task179 `build_runtime`, enumerate Q0/Gamma states or edges,
build a Q0 section store, or invoke nonexistent packed-helper owners.  Load
the pinned frozen arithmetic directly:

- `old.reconstruct_quotients(q3)` supplies actual `e3` and `e4`;
- the five E3 and five E4 substitutions are the exact fixed v1
  `task232_contexts` words;
- `old.fox_gradient_without_sections(word, quotient)` supplies the roof and
  sparse left-Fox gradient; and
- `old.pure_relations(3/4)` supplies the two/eleven presentation relators.

Construct the two source actors in each of the ten contexts by applying the
actual fixed substitution and Fox evaluator once.  Construct signed inverses
by the affine law and compare each with direct inverse-word evaluation.  The
cached state is exactly

```text
(a,u)*(b,v) = (a*b, u + a.v)
(a,u)^-1    = (a^-1, -a^-1.u)
```

with actual quotient multiplication, inversion, support translation, and
F3 reduction.  A zero byte string, endpoint hash, bridge sign, roof tuple, or
dict shape is not a group/affine value.  Authenticate all actor roof blobs,
widths, identities, multiply/inverse laws, and direct task176/task198 canaries.
Every accepted presentation row must finish at roof identity; its ten tagged
gradients, not the roof, are its A4 defect.

## 5. Persistent ancestry evaluation, not 6,441 long rescans

Replay the three actual ancestry grammars (`Gamma_Cayley`, `action`,
`Q0_lift`) against every stored row word before using it.  Preserve the exact
authority inventory: 6,318/104/19 rows, 288 primitive words, 114,458 primitive
literal letters, 15,970 producer prefix edges, 26,136 checker suffix edges,
and 5,475,488 stored row-word letters.  Mismatch is `UNKNOWN_INPUT`.

The producer uses a persistent forward prefix trie; the checker independently
uses a reverse suffix trie and the opposite association order.  A node stores
its roof state plus immutable parent/edge affine delta.  Do not copy a full
sparse gradient at every trie node: materialize/cache only terminal affine
values and immutable sparse deltas.  Assemble and stream one of the 6,441
defects at a time into the quotient oracle.  Directly flat-evaluate a fixed
cross-layer sample, all actors and primitives needed by canaries, and every
new K/anchor word.  Trie construction that is not used by row assembly is a
fatal static defect.

## 6. Sixty-five tagged boundary families and lazy complete oracle

Construct exactly:

```text
5 PB3 coordinates * 2 relators  = 10 tagged seeds
5 PB4 coordinates * 11 relators = 55 tagged seeds
total                            = 65 tagged seeds
```

For each base relator compute the actual Fox gradient, require identity roof,
and inject its coordinate tag.  Preindex its occurrences by the complete
typed component and cache actual support inverses.  Do not merge equal words
across tags and do not apply bridge `factor_sign` to fabricate the ten-tagged
affine defect.

Maintain one chronological, coefficient-bearing discovered-boundary basis B
and current K basis L.  For each target, reduce against `B+L`.  If zero,
return MEMBER with exact raw B/K coefficients and direct replay.  If nonzero,
back-solve a dual in the authenticated typed universe with
`lambda(B+L)=0` and `lambda(target)!=0`.  Correlate the complete support of
that dual with every matching occurrence of all 65 base rows:

```text
t = g*h^-1
require t*h = g
accumulate a*lambda(i,c,g) under full key (i,j,t)
```

Choose the lexicographically first nonzero accumulator, construct that one
actual translated boundary column, prove strict rank rise, insert it into B,
and repeat.  If every accumulator is zero, return NONMEMBER only after
exporting/replaying the complete zero-correlation certificate.  Never insert
an artificial target key, enumerate E3/E4, prebuild all D, or replace group
products by integer-tuple arithmetic.

The checker independently reconstructs all 65 rows, sorts a flat
occurrence/support pair stream, uses a different pivot order, recomputes every
translation/product/correlation, and verifies every chronological B addition
and MEMBER/NONMEMBER terminal.  It must not require a nonunique producer
MEMBER coefficient vector to equal its own; it verifies semantics and the
canonical terminal data instead.

## 7. Exact K closure, coefficient replay, and literal words

Each initial streamed defect is queried modulo the whole D and earlier K.
Enqueue only a true K quotient-rank rise.  For each dequeued K basis row,
form exactly its four source conjugates under `x,x^-1,y,y^-1`, query them by
the same complete lazy oracle, and stop only at queue exhaustion.  These four
actions are for K only; they never substitute for full PB boundary translates.

Every raw/normalized B and K row has an immutable label, chronological
coefficient transform, parent/action provenance, and replay.  Define and test
one sign convention.  If reduction returns `remainder = input + old`, then
the normalized transform must use that same equation and scale on both row
and coefficients.  Retain zero-relation coefficients for dependent rows.
Producer and checker must each replay every accepted/dependent row from raw
rows and prove two-way spans without comparing coordinates in different
bases.

For every normalized K rank raise, materialize the deterministic literal
source word from the candidate word, increasing prior-K indices, explicit
inverse/powers 0/1/2, normalization scalar, and the registered outer-first
conjugator convention.  Its direct ten-context affine replay must differ from
the stored raw representative by exactly the certified boundary combination.
Use persistent integer DAG ids and bounded expansion; a word-shaped Boolean
or unexecuted `materialize` method is not evidence.

## 8. Actual v247 anchor and terminal discipline

Project each actual retained K word independently to H2(9).  Select the least
index with nonzero z0 exponent, compute the actual inverse scalar in F3,
construct the literal powered word, and directly replay every v247 coarse,
successor, context, kernel, least-index, and anchor gate on producer and
checker sides.  Do not hard-code index 0, scalar 2, eta zero, or any anchor
Boolean.  If all actual projections vanish, return `UNKNOWN_INPUT` because it
contradicts the accepted projected-kernel premise; do not call it a negative
A4 theorem.

Mathematical terminals are only complete MEMBER/NONMEMBER query certificates,
queue-exhausted K, and the replayed v247 anchor.  Cap exhaustion is only
`UNKNOWN_RESOURCE`; malformed authority/runtime is only `UNKNOWN_INPUT`.
Broad exception conversion to a passing terminal is forbidden.

## 9. Independent mutations and negative controls

Use one identical registered owner-name roster on producer and checker with
at least 34 mutations, including every task328/task331 owner and all eight
task333 missing boundary owners.  Each mutation must alter canonical bytes or
live mathematical state, be correctly resealed when appropriate, traverse to
its named production validator, and be rejected there with a narrow stage and
reason.  Include at least:

- authority path/member/seal/source/row/chunk/normal/bridge/ABI owners;
- primitive value, prefix/suffix orientation, inverse/action orientation,
  target inverse, and producer/checker row mismatch;
- tagged seed count/tag, translated-boundary product, support-inversion key,
  omitted occurrence, false zero correlation, rank-rise, and incomplete
  chronological transcript;
- echelon scale/sign/raw coefficient/dependent replay, K parent/action,
  word-DAG/materialization/direct affine replay, H2 least index/scalar/anchor;
- genuinely exceeded live resource cap, wrong terminal envelope, duplicate
  physical terminal, stale output, and premature sentinel.

Mutation success means the named owner rejected its mutant, not that a hash
changed or an arbitrary exception occurred.  The checker reruns mutations
independently and never trusts producer mutation flags.

## 10. Performance, serialization, driver, and reply

Meter actual input bytes, trie nodes/edges/terminal materializations, affine
sparse operations, row assemblies, quotient reductions, dual support,
correlation pairs, discovered boundary columns, K queries/actions, word-DAG
nodes/expanded letters, direct replays, RSS, canonicalization, and final
write.  Let `n=6441`, terminal K rank `t`, discovered boundary rank `p`, and
query count `Q=n+4t+q_anchor`.  There are exactly p active-boundary rank-rise
rounds and at most one complete-zero correlation per quotient query; report
the exact correlation-pair sum from v272 (7.1).  Do not claim `65+12b` as the
lazy hot-path bound, hide nested ten-context calls, recursively re-expand
words, repeatedly rebuild known bases, use exponential `3^r` enumeration, or
serialize a second full initial-row roster.

A replayable checkpoint, if implemented, contains the exact authority id,
next query ordinal, raw B/K rosters and coefficients, echelon rebuild data,
queue head, word DAG, and counters.  Otherwise set it null.  Labels alone are
not checkpoints.  The final sealed resource snapshot includes serialization
and write accounting.

The ASCII GAP driver must actually execute one producer then one independent
checker under a bounded timeout, pin their exact current bytes/SHA plus all
input identities, stale-reject every owned v1--v4 output, require exact-one
equal full-line terminals and a nonempty independently sealed checker verdict,
and write the sole sentinel last.  It may emit a shell only if GAP then runs
that shell and propagates its status.  No sleep/retry/poll/lock/pool/local
parallelism or hidden subprocess beyond this explicit pair is permitted.

The reply must give exact bytes/SHA for all four machine files, the complete
import/authority graph, static positive and negative reachability traces,
actual schema paths, actor/trie/base-row inventories, coefficient invariant,
query/correlation/K/word/anchor algorithms, all producer/checker mutation
results as unexecuted static routes, driver process flow, and explicit known
resource formulas.  End with exactly:

```text
IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
SELFTEST / PRODUCTION:           UNEXECUTED
AUTHORITY-V2 INPUT:              PASS / A4 1/3 only
ACTUAL POSITIVE BRANCH:          STATICALLY REACHABLE or BLOCKED
ACTUAL A4:                       remains 1/3 at most before execution
LIFT / FAKE / IHARA:             NONE
```

Any future execution requires a new Sol(max) full code-and-performance PASS.

`TASK336_R07_TASK333_A4_V4_LAZY_AFFINE_ORACLE`
