# Task940 -- selected root seed over the retained rank1355 state

## F1. Frozen producer handoff

Completed only `search/d972_r07_actual_root_seed_materializer_v3.py` and this
reply. The new file adapts the worker's accepted buffered seed30 producer
lineage; the executed v1 and buffered v2 remain unchanged.

```text
producer bytes: 86643
producer SHA256: 36cc620bdc1b772a4eb4067f7e5b490dab851fb41213c5cd8a5487855207a332
LF: 1651; CR: 0; BOM: absent; final LF: present
preserved seed30 v1 SHA256: 3ce9293e05f06bf343bd2a54af0ab84ae67f4b922a428cd3c73e38944d6de55c
preserved seed30 v2 SHA256: 01a74d8d772e64c9f4b1a2c52d028e6e653a6445e3039c4736e1ab6ba3ea484e
actual selected support: NOT COMPUTED HERE
actual rank1355 -> rank1356: NOT RUN
cross_checked=false; verified=false
```

Receipts are read-only PowerShell file-size, line-ending and `Get-FileHash`
observations, not Python syntax checks or execution results. Root and
Task930 received this freeze; Task930 owns the independent checker/workflow.
No genuine implementation blocker was found. GHA execution remains required.

## F2. Current authority and bounded arithmetic

The input is the accepted root-seed candidate run33954712636/1 at
head `92c98486ab659f7e3358fc3c4afb53ab6b78293d`, artifact9966008518,
archive31781 bytes/SHA
`sha256:148b028ec8b17543a85a563a8d0275fc93361168adda85d8147cd1dbc41207b3`.
The code authenticates its exact manifest/result/launch/checker/source
receipts and all19 listed payloads. Selection comes from its sealed first
violation joined to the chronological176-record stream, not a seed constant.
That stored authority selects char0/seed34/scalar1. The prior computation
provides44 informative char0 scalars and zero q1/q2/q3, not176 independent
tests. This materializer does not recompute that batch.

The selected relation receipt, read from that fixed result, has2756 ordered
events and SHA `f77749fb00a882ee6118d7f3f00a828f7575f4065ca1bc418c668e5841b489be`.
Task554 source-order events include seed, duplicates and source-relative
origin IDs; their count/head must match that receipt before F3 collection.
No selected-support number is assumed. All raw-event P1 roots, including
cancelled nodes, remain in the literal ancestry; only the resulting numeric
support is replayed. The P1 instruction reader retains its1MiB buffer and
local-block reduction-index interpretation. Only one Task554 body is live.

Before subtraction, the selected raw degree2 character slice is joined to
the already pinned direct receipt by packed SHA, support and scalar. For
the selected authority its saved raw SHA is
`5990517725f4a2cd3e52aab5e43ecf00d636d9d3805aa6259805aa858fdb9d82`
and support1464. These are parent facts, not a new replay result here.
No separate seed2 evaluation or historical actual-pin bypass is introduced.

The arithmetic path then requires all96776 lower coordinates zero, complete
defect projection equal to the selected plain character slice, Task712 B
evaluation, B-adjoint(current lambda)=current q, and the selected scalar
pairings. Actor/orbit arithmetic is absent.

## F3. One continuation, not a reconstructed state

The old Task904 rank1354 state is authenticated once and retains its
insertion-order pivot roots. The exact accepted seed30 delta
run33946247365/1/artifact9963533999 supplies the single additional pivot,
current generation8060/head
`36feb776736c6587ce9f64d6f5acb883385074a7cc2eed4c2ce7eb8675e71342`,
lambda and saved current target remainder. No concatenated parent state is
written. No historical Conn construction or old target elimination runs.

Only a nonzero newly reduced row may produce the rank1355-to1356 append.
Its literal reductions reference the old instruction roots and the accepted
seed30 instruction root. Exactly one new pivot is examined against the
saved current target remainder; the884 old plus one accepted seed30 target
eliminations remain parent references. Nonzero target gives a next Separator;
zero gives only `ConnectionMemberCandidate`.

After reverse substitution, the final functional is directly paired with
all old1354 rows, the accepted seed30 row, the new row, and both current and
updated target remainders. The rho2 pairing is explicitly inherited through
the accepted parent target derivation; the original rho2 payload is neither
downloaded nor asserted to have been directly replayed here.

## F4. ABI, CLI and remaining execution

Schema is `d972.r07.actual-root-seed-materializer.v3`. The flat delta keeps
the previous bin/instruction/result/manifest layout. `parents.state` names
the immutable base plus accepted delta, and all new parent-state head and
manifest fields refer to that current authority. No runtime paths are
embedded in the public parents. Ancestry adds `selected_seed_relation`;
the raw seed adds `selected_direct_receipt`; separator adds the explicit
final direct-pairing receipt and inherited-target explanation.

```text
python -B -u search/d972_r07_actual_root_seed_materializer_v3.py --selftest

python -B -u search/d972_r07_actual_root_seed_materializer_v3.py \
  --scalar-root CURRENT_SCALAR --delta-root ACCEPTED_SEED30 \
  --state-root TASK904 --prepare-root PREPARE \
  --block-root B0 --block-root B1 --block-root B2 --block-root B3 \
  --p1-root P1 --task712-root TASK712 --output-root FRESH_OUTPUT
```

Only the own accepted producer root-v2 and v15 are executable dependencies;
their exact hashes are pinned in source. There is no sibling arithmetic
import, global-constant monkeypatch, scalar-diagnostics prerequisite or rho2
stager. Three tiny changed-interface canaries cover a selected-authority
seal, nonmonotone base-plus-delta insertion/one target step, and a deliberately
bad row rejected by the final lambda check. They were not executed here.

Root must run the serial canaries, actual producer and independent checker
on GHA; candidate publication requires checker PASS. This worker used no
local Python/GAP, network, credentials, git, dispatch or new subprocess.
There is no new worker commit SHA or run ID. Grade2/full A0/COMMON/cofinal/
fake/Ihara remain undeclared; no mathematical promotion is claimed.
