# Task989 — 固定lambda batchのproducer移行契約（静的設計のみ）

役: 既存packet_producer。変更可は `sol/luna_reply_989_r07_fixed_lambda_batch_producer_contract.md` のみ。
source/WF/既存返信は凍結。ローカルPython/import/AST/数値/GAP/network/git/credential/新agent禁止。
新checker側のsource/990返信は読まない。既存P側のsourceと共通数学・保存metadataだけを読む。
rootは985本走と986の一回限り絶対96対照を実行し、同時に988で数学条件を独立監査している。

裁定2154/2155とTask988を全文読む。actual親=run33990567016/1・64段・rank1450/gen8155。
固定lambdaで全弦を評価し、既定roster順の先頭非零弦32本以下を各々Omega語化、全source/primal/P1/four-Bを通し、
旧spanと先採用行に対する実消去で独立分だけ順序どおり追加する候補。lambda同一違反は相互独立を保証しない。
旧selection lambdaとbatch後separatorは別型。全弦零でもauxが非零なら一件fallbackかUNKNOWNを保持。
grade2/whole positive/側条件/完全性の射程を広げず、rank増分/高速化を先取りしない。

既存P側だけから最小実装契約を提案する（まだ実装しない）:
- 固定section/cochain/treeの計算を共有し、各弦ごとのwitness/tau/etaを作る既存関数・差分箇所。
- 各Eのraw/source/P1/physical payloadの固有pathと親snapshotの接続、実消去/target scalar/recipe/rolling head。
- 別batch packet型で旧64prefixを保持する案と、新continuation版へ移す案の工数・不変条件・source/owner変更境界。
- 一度消去した行のlambda pairingが零でも独立な場合を拒否しない契約と、依存候補の全recipe保存。
- checkpoint/partial-file/中断/再開、候補32件に満たない時、Linear実零/COMPLETE_ZERO/aux fallbackの型。
- 数値preflightで識別力のある最小canary、最大メモリの大きな常駐配列、どこを再利用するとTCB共有になるか。

最小の追加source候補名・CLI/入力/出力/schema案、既存Pの関数名と実行番号付きで根拠を記す。
共通wire契約はrootが988と本票から決めて両authorへ伝える。実装許可や未来親pinを補わない。
最終行 `AUDIT_989_VERDICT:`。
