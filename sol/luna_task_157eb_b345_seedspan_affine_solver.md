# Luna task 157eb — affine seed-span solver for the common target-6 wall

## Role and objective

You are Luna.  Implement a new versioned positive-first lane which replaces the
bounded 4096-word trial by one exact linear calculation on the 104 authenticated
correction seeds from the same frozen construction.

The cross-checked v10 run evaluated all 4096 preregistered words.  Every word
first missed acceptance target 6, `hexagon_1_coface_0`, but the exact blocker
values formed 31 classes.  Therefore the common target slot is real, while a
common cokernel class was never proved.  The later 157ea run proved that the
explicit strong word

```text
s_strong = y^-18 x^-18 y^18 x^18
```

has five zero left-Fox cofaces and leaves target 6 unchanged even before
quotienting by the registered D2 prefix.  This is a structural canary, not a
lift.

Let `f0` be the frozen 20-letter word.  The v9 dictionary begins with 26
nonempty coarse-trivial cube words and constructs, in a frozen order, the four
commutators with `x` and `y` for each cube.  This gives 104 seed words.  The
4096 words are only a breadth-first prefix in the subgroup generated by these
seeds.  In the elementary abelian relative Frattini quotient, arbitrary
products of the 104 seeds are represented by coefficient vectors in
`F3^104`.

For each corrected-Def2.9 acceptance residual `R_j(f)`, define

```text
z_j       = D(R_j(f0))
delta_j,k = D(R_j(f0 * seed_k)) - D(R_j(f0))
```

and reduce these vectors by the same freshly reconstructed, immutable D2
prefix used by v7--v10.  Solve the simultaneous affine system

```text
R(z_j) + sum_k a_k R(delta_j,k) = 0,  a_k in F3,
```

in target order.  Here `R` is the full canonical remainder projection for the
fixed prefix: it must continue past nonpivot keys and eliminate every later
pivot, unlike the old first-missing reducer.

If the system has a solution through all 33 acceptance targets, construct the
canonical coefficient word and replay the actual 33 predicates and their
positive D2 certificates.  A direct replay PASS is a genuine positive
certificate.  If the affine system becomes empty, report only that the fixed
prefix has no solution in the authenticated seed span.  This is
`SEARCH_INCOMPLETE`, never a full-D2 obstruction, full-H3 nonexistence,
B4-A, or B4-B.

## Frozen inputs and provenance

Same-job q3 inputs:

```text
search/d972_b345_q3_chief_v1.g
  b95fc29b326c3d6a378249cdeb03595eed8d0211a7fe0358fc02447d70d5f755
search/check_d972_b345_q3_chief_v1.py
  ddb52ddae18327209692f0f6eb8b4f65cbdd446155be660a621de24274cc3f73
search/d972_b345_q3_gha_driver_v1.g
  c397cd837ff6814f7b7a8ca0604c6aed54fa0bc85bb577516ea1c6e7df83a831
q3 receipt
  3d37c8c5f1fae47c66877090f9f73d1a8ff4a826214ed610175cf6e8ac41da72
```

Mathematical/certificate implementations:

```text
search/d972_b345_relfrat3_wordexpr_memo_v9.py
  7dede323c3c52bc7cf7d99af6d542b3683823879a4bb3e340aca8ce53dcf196f
search/check_d972_b345_relfrat3_wordexpr_memo_v10.py
  264258dcb945401e3db10ecd4fedd7a8dd79a8d7b0f31dbc0cfbe643537eac2d
search/d972_b345_relfrat3_wordexpr_memo_gha_driver_v10.g
  a5e9bdb34d85669a6221e4b0fa8e4c3af0aee343aade59fde52013d05753afc0
search/d972_b345_strong_wform_inertness_v1.py
  d41123a8c4803f6ac67387ac9bbf1a32f797b90d6233605a5511713f215244be
search/check_d972_b345_strong_wform_inertness_v1.py
  a8345c6c27fea24147dc7c310bbda48ea5bc08b7a0a720ded961af13a5b961e8
```

Cross-checked v10 evidence is provenance, not an input checkpoint:

```text
run                  32261068150
receipt sha256       0675e0ac957796cd2a9facee805b8f5e258d36f0c824e68ba916fdedeeedeade
evaluated             4096
first failed target   6 = hexagon_1_coface_0, component 4 (all rows)
distinct blockers     31
```

Cross-checked 157ea evidence is also provenance only:

```text
run                  32278425502
receipt sha256       3857af02b3cb01c9df9652e8f27c174adfaf3c664e5300cb43eb803218b21701
terminal              B345_T53_STRONG_S_EXACT_TYPED_INERT
```

Fresh reconstruction must reproduce the v7 prefix bindings and counts:

```text
formula_sha256
  b43284edac5b4dae945bb3b30ac0f177dc47df8724cb32acd6057b26d82a27ef
stable_rounds_projection_sha256
  75a2894da0f19d0e541e27924ee63e220a6eca35e852b21088ee304ba42fc42d
translations_sha256
  a4b952bce888713e293587cd63d710465121e782448a3e2a571d80b992ea363f
columns_sha256
  cb57176146b926df16e508429db5aa1ff6b5b0ec691f2328371973681089b343
blocker_history_sha256
  b5f100e45e874ce5ee3270cd31350b4318cf40931055571ac70066e69d62de53
BFS translations      32768
directed translations 207
columns                362725
pivots                 362709
dependent columns      16
```

The fresh ordered 104-seed family must have

```text
count                  104
digest_obj(seed_words) e99602b0981251e4bb81ab0d2113791563bc9ec9df2a45828aea2880ec6d2f9e
```

Do not import an old basis, row, pool, blocker, gradient, proof DAG, or old
receipt as a mathematical input.

## Authorized files

Create only:

```text
search/d972_b345_seedspan_affine_solver_v1.py
search/check_d972_b345_seedspan_affine_solver_v1.py
search/d972_b345_seedspan_affine_solver_gha_driver_v1.g
sol/luna_reply_157eb_b345_seedspan_affine_solver.md
```

Do not edit q3, 157ea, v1--v10, workflows, claims, dialogue, receipts, or other
files.  Temporary diagnostics belong outside the repository.  Do not run a
full producer locally, production GAP, Git, GHA, or a workflow.  One combined
lightweight producer/checker selftest is authorized after the static audit.

## A. Exact seed family, typing, and registered universe

Reconstruct only the seed-building prefix of v9 `correction_dictionary`; do
not build the 4096-word BFS.

1. Take every nonempty word in the authenticated 27-element q3 correction
   fibre, cube it by literal threefold concatenation and free reduction, and
   deduplicate in first occurrence order.  Require 26 cube words.
2. For each cube `k`, in that order, append

```text
[k,x], [x,k], [k,y], [y,k]
```

   using `[a,b]=a^-1 b^-1 a b`.  Preserve the exact v9 nested-loop order.
3. Require 104 pairwise distinct reduced seed words, exponent sums `(0,0)`,
   authenticated E3 identity after the exact embedding
   `x -> PB3 generator 1=A12`, `y -> PB3 generator 3=A23`, and the frozen
   family digest above.
4. Reconstruct every correction-occurrence context used by the 33 acceptance
   WordExpr roots.  For every seed and every such context, directly require
   E4 identity.  This is the load-bearing premise which makes multiplication
   of corrections additive in `H4/Phi_3(H4)`.  Do not replace it with the
   label `all_words_in_coarse_J_H`, a digest, or only the six source tuple.
5. Also require the six source E4 images for `f0*seed_k` to equal the frozen
   source tuple for every seed.  Hence the pinned normalized inverse tuple is
   valid for any coefficient product.

The registered algebraic universe is precisely the image in the relative
Frattini module of the subgroup generated by these 104 ordered seeds.  It is
strictly larger than the 4096 BFS prefix and is not claimed to be all H3 or all
lift freedom.  Record this scope literally.

The occurrence/source tests are mathematical preflights, not authenticated
input-schema tests.  If a seed has a nonidentity occurrence or changes the
frozen source tuple, terminate `SEARCH_INCOMPLETE` with
`reason=affine_seed_preflight_unsupported`, the first exact seed/context/value,
and no obstruction claim.  Do not misclassify this as `UNKNOWN_INPUT`, and do
not silently delete the offending seed or shrink the 104-seed universe.

## B. Raw Fox-chain affine-linearity theorem and target-6 orientation canary

The load-bearing theorem must be proved first in the raw Fox chain module

```text
C1 = F3[E4]^6,
D(uv) = D(u) + pi(u) D(v).
```

Do **not** prove additivity only after passing to
`M4=H4/Phi_3(H4)=ker(D1)/im(D2)`.  Equality in `M4` may differ by a full D2
boundary which is absent from the registered fixed prefix, so it would not
justify an inconsistency statement for the prefix remainder map.

For a typed residual WordExpr, evaluate every node in the semidirect Fox object
`C1 semidirect E4`.  Every correction leaf and every registered occurrence
homomorphism has E4 component one.  Structural induction on FLAT, PRODUCT,
INVERSE, and SUBSTITUTE therefore gives the **raw equality**

```text
D(R_j(f0*c(a))) = z_j + sum_k a_k delta_j,k  in C1,
```

for every coefficient vector `a in F3^104`, before quotienting by any D2
columns.  Product uses the left action by the exact prefix E4 value; inverse
uses the corresponding negative translated gradient; substitution must bind
rank/arity and occurrence order.  Only after this raw theorem is established
may the linear fixed-prefix remainder map be applied.

Equivalently, if each occurrence of corrections `c,d` has E4 value one, then
for every fixed WordExpr residual the raw Fox differences satisfy

```text
Delta_R(cd) = Delta_R(c) + Delta_R(d),
Delta_R(c^-1) = -Delta_R(c),
Delta_R(c^3) = 0.
```

Encode the structural induction and exact opcode/orientation rules as a stated
certificate in the receipt and independently replay it in the checker.  For
every one of the 33 target constructors, the checker must rebuild the typed
tree, require all correction occurrence E4 values one, and compare the
symbolically derived raw constant/104 columns with direct base/one-seed raw Fox
evaluations.  This all-target equality, not a sample of seed pairs, is what
authorizes the `SEARCH_INCOMPLETE` affine conclusion.

The final positive path must still replay the constructed word, so the theorem
can never create a false PASS.  Add direct production canaries for fixed seed
pairs, inverses, and squares as extra falsifiers, comparing raw direct Fox
differences with sums/negatives/scalar-two predictions.  These canaries alone
are not the proof of the universal coefficient identity.

For target 6, use the exact convention

```text
A = f(x,y), B = f(x,z), C = f(y,z)
h1(f) = C * B^-1 * A
```

where `*` is ordinary left-to-right word concatenation; `paper_product`
displays the factors in reverse multiplication order.  For `f -> f*s`, write
`a=s(x,y), b=s(x,z), c=s(y,z)` and gate the free-word identity

```text
h1(f*s) h1(f)^-1
 = C (c b^-1) C^-1 * h1(f) a h1(f)^-1.
```

After coface 0, compare its direct left-Fox gradient with the module formula

```text
delta_6(s) = L_C ( [c] - [b] ) + L_h1 [a].
```

Because `h1(f0)` is E4 identity, the second action is the identity, but do not
silently drop it before checking that value.  Use left translation, not right
translation.  Reject inverse/order/sign mutations in selftest.

For all 104 seeds require exact equality between:

- direct `D(R_6(f0*seed_k))-D(R_6(f0))`; and
- the formula-derived vector above.

Reconstruct `s_strong=y^-18 x^-18 y^18 x^18` outside the seed family and
require the same formula/direct comparison to be the zero vector, reproducing
the 157ea raw-Fox canary without importing its receipt.

## C. Canonical full remainder projection

Rebuild the immutable prefix basis as in 157ea.  Add a provenance-free
`full_remainder` operation with this exact behavior:

Before using it, gate the echelon invariant on every stored basis row: the
registered pivot is the least canonical key in that row, its coefficient is
one, pivots are unique, and the row contains no key preceding its pivot.  The
basis is immutable during all affine probes.  This invariant is the reason a
free key moved to the remainder can never be reintroduced by eliminating a
later pivot; linearity samples alone are not a substitute for this gate.

1. Process vector keys in the same canonical `(component, exact E4 bytes)`
   order as the basis.
2. If the least working key is a pivot, eliminate it with the normalized basis
   row.
3. If it is not a pivot, move its coefficient to the remainder and continue;
   do not stop, and do not leave later pivot keys unreduced.
4. Return a canonical sorted sparse remainder over F3.

Gate linearity on nontrivial toy vectors and, in production, gate

```text
R(v+w)=R(v)+R(w), R(2v)=2R(v), R(column)=0
```

for deterministic canary samples including base columns, target 6, and seed
deltas.  The independent checker implements this reducer itself.  Pool IDs are
never public coordinates; serialize `(target ordinal, component, exact E4
bytes, coefficient)`.

Every base/one-seed gradient probe must run under a candidate-local element-pool
transaction.  Convert its full remainder to canonical bytes before rollback,
then remove the exact pool suffix and clear ID-keyed product/inverse caches.
The immutable basis may reference only pre-transaction IDs.  Accumulating 104
times 33 transient E4 elements in the persistent pool is forbidden.

## D. Incremental simultaneous affine solve

Use the corrected Def2.9 acceptance order exactly:

```text
1..5   charming_error_coface_0..4
6..10  hexagon_1_coface_0..4
11..15 hexagon_2_coface_0..4
16     ordered_A18_pentagon
17..27 S_relation_1..11
28..33 ST_generator_1..6
```

The 17 `T_relation`/`TS_generator` rows are diagnostics only and must never
enter the affine system or PASS predicate.

Start with the full coefficient space `F3^104`.  At each acceptance ordinal:

1. Reconstruct the base target and each one-seed target by typed WordExpr.
2. Require their E4 values to be identity.
3. Compute the exact raw left-Fox gradient, form the 104 direct differences,
   compare them with the typed raw-chain affine certificate, and take canonical
   full remainders.
4. Intersect the current affine coefficient solution space with all coordinate
   equations for this target.  Use deterministic F3 Gaussian elimination,
   fixed seed-column order 1..104, canonical coordinate-row order, normalized
   pivots, and free variables set to zero for the canonical solution.
5. Record target ordinal/name, base remainder size/digest, delta rank,
   accumulated constraint rank/nullity, consistency, and a lossless row-space
   certificate.  Emit progress at least every 10 seconds and after every
   target.

If the solution space becomes empty, stop without computing later targets.
This terminal is a complete result only for the fixed prefix plus the
registered 104-seed affine span.  It is not a full-D2 or full-H3 negative.

Candidate-local gradients and remainders must be released after their matrix
rows are absorbed.  Do not retain 104 times 33 full sparse vectors.  Use
registered caps for one target's live gradients, affine rows/nonzeros, word
expression nodes/edges, element pool, RSS, and wall time.  A cap is
`UNKNOWN_RESOURCE`, never inconsistency.

The matrix may retain only F3 rows in the 104 coefficient variables plus RHS,
and canonical coordinate bindings/digests needed to replay them.  It must not
retain pool-ID-keyed raw gradients after a target is absorbed.  On a later
positive replay, regenerate gradients from the selected typed expression.

## E. Canonical solution and actual positive replay

If all 33 stages remain consistent, choose the canonical coefficient vector
with free variables zero and construct

```text
correction = seed_1^a1 * seed_2^a2 * ... * seed_104^a104
candidate  = f0 * correction
```

in ordinary multiplication order.  Exponent `2` means two copies, not an
inverse spelling.  Use a typed expression DAG; do not flatten a long word only
to satisfy an old literal cap.  Record coefficient vector, nonzero support,
the exact seed-expression root, expanded count, and canonical digest.

In particular, do not call the old flat-sequence `build_wordexpr_candidate`
on this possibly long product.  Build `correction_expr`, then
`candidate_expr = flat(f0) * correction_expr`, and substitute expression roots
through the 33 target constructors.  Flat replay is an optional canary only
when the exact expanded count is within the registered flat-word cap.

Then perform a fresh direct replay, not the linear prediction:

- the same fixed roof row/outside binding as v9, with the correction reducing
  to the E3 kernel so the roof image is unchanged;
- `m=0`, `lambda=1`, `u=1`, exact friendly and free exponent-sum gates, and
  the corrected-Def2.9 convention that settlement and T/TS canaries are not
  acceptance predicates;
- correction occurrence and five-coface E4 identities;
- six source tuple equality and normalized inverse binding;
- all 33 acceptance quotient identities;
- all 33 raw Fox gradients;
- provenance-enabled membership solves against the same immutable D2 prefix;
- the direct gradients equal the affine predictions;
- all 17 T/TS diagnostics, recorded losslessly but excluded from acceptance.

Only if all 33 direct membership proofs serialize losslessly may the producer
emit a positive terminal.  The checker reconstructs the seed product,
WordExpr roots, quotient values, gradients, every translated-relator proof,
and the 33/17 split without importing producer helpers.

If the canonical solution fails any direct theorem equality, quotient gate, or
predicted gradient, this is an invariant failure and the job must hard-fail;
do not turn it into candidate rejection or UNKNOWN.  If proof serialization
hits a registered resource cap, return `UNKNOWN_RESOURCE` with no positive
boolean.

## F. Terminals and claim boundary

Use exactly four exclusive terminals:

```text
B345_SEEDSPAN_AFFINE_POSITIVE
B345_SEEDSPAN_AFFINE_SEARCH_INCOMPLETE
B345_SEEDSPAN_AFFINE_UNKNOWN_RESOURCE
B345_SEEDSPAN_AFFINE_UNKNOWN_INPUT
```

`POSITIVE` means one concrete correction in the 104-seed subgroup passed all
33 corrected-Def2.9 acceptance gates with independent D2 certificates.  It is
still one finite-stage lift and does not by itself prove B4-B, cofinality, or a
global profinite element.

`SEARCH_INCOMPLETE` is allowed for exactly two registered reasons: an
unsupported 104-seed occurrence/source preflight before the basis scan, or an
exactly inconsistent affine system relative to the fixed prefix.  It must say:

```text
claim_classification = unknown_not_obstruction
claim_scope = registered_104_seed_affine_span_against_fixed_D2_prefix
full_D2_claimed = false
full_H3_claimed = false
negative_claimed = false
B4_A_claimed = false
B4_B_claimed = false
```

`UNKNOWN_INPUT` is reserved for missing/drifted authenticated external inputs,
schema, or pins.  Internal theorem/ordering drift is a hard failure.
`UNKNOWN_RESOURCE` requires a closed registered reason, exact evaluated prefix,
current target/seed, cap and observed count, and the same no-claim fields.

## G. Independent checker and selftest

The checker may read older sources only as SHA-pinned text/import dependencies
where explicitly declared, but it must not trust producer booleans, matrix
rows, remainder rows, coefficients, or proofs.  It independently rebuilds:

- the 26 cube words and 104 seed order/digest;
- all occurrence-context E4 identity gates;
- target-6 free-word difference identity and left-Fox action formula;
- the strong-word zero canary;
- the fixed prefix and canonical full-remainder map;
- every evaluated target's affine matrix, rank, consistency, and canonical
  solution space;
- on POSITIVE, the concrete coefficient product, 33 gradients/proofs and 17
  diagnostics.

The one combined lightweight selftest must use the same production validation
core and include at least:

- wrong F2-to-PB3 image `y->[2]` rejection;
- seed reorder/sign/duplicate/digest rejection;
- a nonidentity occurrence-context rejection;
- `C`, `B^-1`, `A` order and left/right translation mutations;
- a reducer which stops at the first free key (must be rejected);
- later-pivot elimination after an earlier free key;
- inconsistent and consistent affine systems over F3;
- coefficient exponent `2` versus inverse spelling;
- a model-prediction/direct-gradient mismatch;
- diagnostic false while all 33 acceptance rows pass;
- positive proof mutation and all four terminal/claim mutations;
- resource stop before and during a target with exact partial schema.

Selftest fixtures may inject small quotients/presentations, but production
schema, remainder, affine solve, selected replay, proof, and terminal validators
must be shared and the injection boundary stated honestly in the reply.

## H. Driver and reply

The thin GAP driver must follow the proven q3-child pattern:

1. exact source pins and package root;
2. stale output/log/sentinel deletion;
3. q3 in an isolated child, exact receipt SHA and checker marker count one;
4. producer and checker under unbuffered `pipefail | tee`;
5. exit-zero sentinel only after success;
6. exactly one producer terminal, one checker marker, one final driver marker;
7. selftest/full modes exclusive;
8. ASCII-only GAP/PowerShell transport strings.

The reply must give all four final SHA-256/byte counts, the exact selftest
marker, caps, estimated runtime/RSS, semantic claim boundary, and a final
single marker

```text
B345_SEEDSPAN_AFFINE_V1_READY_FOR_GHA
```

Do not call a result `verified`; successful producer plus independent checker
is `cross-checked` only after a production artifact passes.
