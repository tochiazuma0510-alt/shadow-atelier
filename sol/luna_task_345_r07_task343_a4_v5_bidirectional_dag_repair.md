# Luna task 345 - task343 A4/v5 bidirectional DAG and independent closure repair

Role: Luna, bounded implementation only. Read every numbered section and
every listed prerequisite first to last before editing. Do not run Python,
Node, GAP, GHA, workflows, git, or network. Preserve every v1--v4 source,
authority artifact, workflow, proof, and v220 file. This mail authorizes only
the five new v5 outputs in Section 2 and no execution.

The task343 verdict is `REJECT / UNEXECUTED`; v4 SELFTEST is not authorized.
Do not wrap or call the v4 `main`, checker, mutation harness, trie, or output
validator. Mathematical routines may be copied only after the audited
invariant is restated and all task343 defects below are removed.

## 1. Binding prerequisites and supersession order

Read in full, in this order:

1. `sol/luna_task_336_r07_task333_a4_v4_lazy_affine_oracle.md`;
2. all five task336/v4 outputs;
3. `sol/sol_reply_343_r07_task336_a4_v4_code_performance_audit_v1.md`;
4. `sol/proof_r07_bidirectional_word_fox_dag_evaluator_v282.md`;
5. `sol/proof_r07_recomputed_anchor_and_adapted_a5_basis_v280.md`;
6. v268, v271--v274, v247, and v231;
7. task176 producer/checker, accepted receipt and artifact manifest;
8. task198 producer/checker/GAP driver and its accepted five-member
   authority-v2 bundle;
9. the frozen q3 receipt, pinned E4 arithmetic source
   `search/d972_b345_seedspan_triple4_v1.py`, and the A4/v1
   producer/checker.

Task343 supersedes reply336's reachability, mutation, independence, and
performance claims. V282 supersedes the unused-prefix-trie plus two flat
6,441-row replays. V280 supersedes any trusted `anchor_contract`,
`basis_projections`, `rho0_replay`, `rho1_in_kernel`, `q_z0_replay`,
`dynamic_least_nonzero`, or adapted-basis Boolean.

## 2. Sole permitted outputs

Create only:

- `search/d972_r07_word_independent_successor_kernel_v5.py`;
- `crosscheck/check_d972_r07_word_independent_successor_kernel_v5.py`;
- `search/d972_r07_word_independent_successor_kernel_gha_driver_v5.g`;
- `search/certs/d972_r07_word_independent_successor_kernel_selftest_v5_20260829.json`;
- `sol/luna_reply_345_r07_task343_a4_v5_bidirectional_dag_repair.md`.

Return `IMPLEMENTED / UNEXECUTED` only if a literal static trace reaches the
actual positive and every typed nonpositive terminal without a placeholder,
unregistered owner, hidden old-main call, schema mismatch, or synthetic
mutation bypass. Otherwise return `BLOCKED / UNEXECUTED` with the first exact
missing owner/API. SELFTEST and production remain unauthorized until a new
Sol(max) full code-and-performance PASS.

## 3. Exact failures which v5 must remove

The implementation and reply must explicitly trace every one of these
task343 findings; fixing only the first failure is insufficient.

1. The actual task198 canary key set is exactly
   `nonsplit_y_y_section_cocycle, source_2_2, x, x_action_y, x_inverse, xy,
   xy_section_cocycle, y`. There is no `y_inverse`. V4 dies before row 1 by
   requiring it. Bind only keys present in the authenticated schema; prove
   the missing inverse through the actual actor inverse law and direct
   inverse-word computation.
2. After that repair, actual row 6,319, the first `action` row, dies because
   the 288-word primitive corpus contains no singleton actor. Obtain signed
   actors from the authenticated 40-entry actor cache, never from a primitive
   trie terminal.
3. A K action which raises rank is absent from v4's final action matrix. A
   complete matrix must include the coefficient column induced by both
   MEMBER and rank-rise action records, transformed into the final ordered K
   basis.
4. If all actions are MEMBER, v4's MEMBER schema lacks the dual/correlation
   fields its checker unconditionally demands. Give MEMBER and active-dual
   rounds distinct typed schemas and validate the appropriate one only.
5. The checker trusts `all_row_dots`, `target_dot`, `complete_all_65`,
   rank-rise, selected-column, coefficient, and zero-accumulator declarations.
   Delete every such acceptance gate. These values may be reported only as
   recomputed diagnostics.
6. The checker's MaxPivot dual pullback applies elimination in descending
   order; the pullback must use the inverse operation order. Supply a small
   actual arithmetic canary where the two orders differ.
7. V4 does not authenticate task176's accepted receipt/manifest/checker,
   task198's physical three sources, or task198 inner row/chunk/bridge/ABI
   owners. V5 must bind and recompute them from literal owners.
8. V4 recursively rematerializes its word DAG without memoization or a
   pre-expansion cap, repeats `Psi(E)`, linearly scans ledger entries, and
   performs an unnecessary all-pairs/all-context long-word commutation pass.
   Replace these as specified below.
9. V4's 48 mutations are zero physical-owner tests; producer first hard
   stops at mutation 35 (`omitted_candidate_discrepancy`) by reading a
   nonexistent top-level `kernel`, and checker accepts miniature shaped
   dictionaries. V5 must use actual loaded owners and normal validators.
10. V4 has no replayable checkpoint, no checker resource envelope, direct
    non-atomic writes, duplicate large serializations, and incorrect final
    byte accounting.
11. V4's checker never establishes that the exported K roster is the
    chronological complete ordered word-bearing basis, and never computes
    per-item actual `rho0`, `rho1`, or `q`. Consequently v280 cannot consume
    it. V5 must repair this boundary without exporting a trusted anchor.

## 4. Physical authority binding and one-pass adapter

Producer and checker implement separate adapters. Each reads the 31 MB
task198 receipt bytes once, hashes once, parses once, and retains one object.
They authenticate all five authority-v2 files and, from the literal files,
recompute rather than merely compare declared strings:

- task198 producer/checker/GAP-source bytes and SHA identities;
- receipt/manifest self seals, run/head/artifact/ZIP/member identities;
- all 6,441 chronological row ordinals and the 6,318/104/19 split;
- seven chunk boundaries and hashes from their actual row slices;
- normal-generation arithmetic and the complete normal-proof fields;
- all 6,441 bridge occurrences from their literal block, slot, context,
  orientation, role, factor sign, and ten spellings;
- row, bridge, typed-coordinate, and ABI digests from canonical literal
  owners; and
- the actual evaluator canary roster and values.

Both sides also pin and authenticate the task176 accepted receipt, artifact
manifest, independent checker result/source, and producer source, including
the accepted run/head/artifact/member identities and direct ten-coordinate
canaries. A manifest's copied source identity is not a physical-source pin.
Resolve paths below the repository root, reject traversal and links/reparse
points where supported, open without following links, obtain identity from
the opened handle, and hash/read that same handle. Recheck identity before
close. State the POSIX and Windows mechanism and fail `UNKNOWN_INPUT` if the
required no-follow/identity property cannot be established.

Do not duplicate the 6,441-row roster in the producer receipt. Charge input
bytes before allocation where possible. Use a streamed canonical digest and
small typed summaries for complete-row equality.

## 5. Exact semidirect evaluator: forward producer, reverse checker

Use the actual ten typed contexts and the exact state law

```text
S(uv) = S(u) S(v)
(a,u)*(b,v) = (a*b, u + a.v)
S(x^-1) = (rho(x)^-1, -rho(x)^-1 delta(x)).
```

The producer builds the 15,970-edge forward prefix DAG of the authenticated
288 primitive words and evaluates every node once in every typed context.
It then assembles each of the 6,441 authority rows from exact primitive and
actor states according to its authenticated ancestry grammar. The 40 signed
actor states come from the actor cache. No terminal may walk again from root,
and no row may call a flat source-word substitute/evaluate loop.

The checker must not import the producer, its trie, dynamic arithmetic
module instance, echelon, row states, node ids, transcript, or digests. It
builds the 26,136-edge reversed suffix DAG from immutable primitive words,
uses right-associated suffix recurrence and independently written affine/Fox
collectors, and reconstructs all 6,441 rows. It may use the pinned public
group operations, but not a producer-owned composite helper. It recomputes a
streamed canonical digest/certificate and all quotient inputs itself.

For every row, both sides independently reconstruct its literal source word
from the three grammars and compare it with the authenticated stored word.
This is one linear word comparison, not a repeated affine substitution or
whole-prefix reduction. Duplicate words retain distinct chronological tags.
The exact work meters must distinguish literal parsing/comparison, DAG edge
evaluation, 19,408 row-piece products, typed-context products, and quotient
work.

Only actors, fixed canaries, newly accepted K words, the complete final
word-bearing K roster, and the eventual selected positive basis/anchor support
may receive a direct raw-word replay. Never flatly re-evaluate every one of
the 6,441 long rows after DAG assembly.

## 6. Sixty-five families and deterministic lazy full-D oracle

Construct all 65 independently evaluated tagged base rows:

```text
5 PB3 coordinates * 2 relators  = 10
5 PB4 coordinates * 11 relators = 55
total                            = 65.
```

Keep one chronological raw boundary basis B and ordered K basis L. Preindex
every base-row occurrence by `(context, component)` so the actual correlation
meter is the number of matching pairs `P_q`, not `O_total*ell_q`. For a live
target reduce against B+L. A zero remainder gives a typed MEMBER with exact
raw B/K coefficient replay. Otherwise independently back-solve an active
finite-coordinate dual, directly prove all live-row dots zero and target dot
nonzero, and correlate every matching occurrence of all 65 families using

```text
t = g*h^-1; require t*h = g;
accumulate coefficient*lambda(context,component,g)
under the full translated-column key.
```

A nonzero accumulator selects the canonical lexicographically first column;
construct it, prove strict rank rise, append it to B, and repeat. A complete
zero accumulator is a typed NONMEMBER only for the current B+L query and
creates the next K row. It is not a universal full-D obstruction.

Use distinct dataclasses/schemas for MEMBER, BOUNDARY_RANK_RISE, and
ZERO_CORRELATION/K_RANK_RISE. Do not populate irrelevant fields with empty
lists or Booleans. Maintain a compact canonical epoch digest chain rather
than serializing every dual, row, accumulator, and coefficient vector twice.

The checker independently rebuilds the chronological raw B/K evolution,
active registry, dual, 65-family correlation, selected columns, strict rank
rises, MEMBER relations, and complete zero accumulators. It may use a
different pivot layout, but must replay the producer's claimed semantic
objects and compare two-way spans and canonical terminal payloads. Correct
the dual pullback order and include a noncommuting-elimination canary.

## 7. Exact word-bearing K closure and complete action matrix

Preserve v273's sign convention and prove it in code. For a normalized new K
row, with the producer convention stated explicitly, compute and retain the
actual `Q,c,s` relation and exact recurrences for both its literal word and
raw boundary discrepancy. Every dependent row retains a zero-relation
coefficient certificate. Every parent/action reference is to an earlier
registered item.

Replace recursive word expansion by a hash-consed persistent word-and-ledger
DAG. Each node stores a precomputed/capped length, its ten exact semidirect
states, and a memoized boundary-ledger state. Product, inverse, power, and
outer-first conjugation are evaluated from child states. Reject over-cap
expansion before allocation. Compute `Psi(E)` once per immutable ledger id
and index raw keys for lookup. The checker reconstructs `Q,c,s`, parent,
action, word state, and ledger state independently.

Process each accepted K row under exactly `x,x^-1,y,y^-1` until the queue is
exhausted. When an action itself creates a new K row, retain the exact
relation expressing that action in the eventual final basis; do not omit its
matrix column. At finalization, reconstruct all four complete action matrices
on every final ordered basis vector, prove the two inverse laws and closure,
and have the checker reconstruct them independently.

Do not run all-pairs/all-context expanded-word commutation. Elementary
abelianity must follow from the authenticated quotient/module construction
and exact row arithmetic, with only a fixed finite direct-word commutation
canary. Report exactly what theorem and input owner justify this replacement.

For every final ordered K item export a bounded literal word (or a sealed
lossless DAG plus its independently materialized literal expansion), its
canonical actual `rho0`, `rho1`, and `q` values, raw and normalized row,
discrepancy id, and ancestry. The checker must independently prove:

- chronological rank rise and ordered-basis independence;
- spanning/completeness after queue exhaustion;
- literal word equals the DAG/recurrence;
- actual `rho0(u_i)=1` and actual `rho1(u_i)=k_i` in the pinned maps; and
- actual `q(k_i)=z0^a_i` with a canonical exponent `a_i`.

## 8. V280 consumer boundary: derive, never trust, the anchor

A4-v5 may report recomputed diagnostics, but it must not ask downstream code
to trust an anchor object. From the independently accepted ordered basis the
checker and future A5 consumer must be able to:

1. compute every `a_i` from actual `q(k_i)=z0^a_i`;
2. choose the least index `j` with `a_j != 0`;
3. compute `e=a_j^-1` in F3 and `u_*=red(u_j^e)`;
4. replay actual rho0, rho1, q and the endpoint of `u_*`; and
5. form and replay the invertible adapted basis
   `u_i~=red(u_i u_*^-a_i)` for `i != j`.

The producer may include these as convenience fields only; acceptance is
entirely from recomputation. If all `a_i` vanish, emit the typed failure
required by the authenticated v247 premise; do not declare a mathematical
negative. Do not consume or export supplied `base_pairs`. V280 assigns their
construction to A5 from the accepted A3 coefficient and `s(g)u_*-s(g)`.

## 9. Actual-owner SELFTEST and mutation contract

Keep the exact task343 48-name roster unless a name is versioned with an
explicit old-to-new table. The JSON fixture contains only the mutation
registry, expected normal validator/stage, and immutable input identities; it
must not contain a miniature substitute for the authority, B/K basis, dual,
ledger, or anchor.

SELFTEST loads the actual accepted inputs and first follows the unmutated
normal route. Each mutation then changes one actual canonical owner at the
earliest stage where that owner exists, reseals only derived local envelopes
when needed to reach the intended validator, and invokes that same normal
validator. There is no mutation-name branch, typed flag, explicit
`reject_mutation`, copied before/after field fallback, or arbitrary-exception
acceptance. A pass requires:

- the owner value or canonical bytes genuinely differ;
- the expected validator is reached;
- the first rejection has the exact narrow stage/reason; and
- the terminal envelope remains well typed and singly emitted.

Cover physical authority bytes/path/no-follow identity; row/chunk/bridge/ABI;
actor/inverse/DAG edge and row assembly; all 65 seed/occurrence/translation
owners; B/K pivot scale and dual pullback; zero/member coefficients;
discrepancy `Q,c,s`; word/ledger DAG and action matrix; per-item rho0/rho1/q
and v280 least-index derivation; live resource cap/checkpoint; terminal,
atomic write, stale output, and sentinel ownership. Producer and checker run
their own mutations and do not import or accept each other's mutation result.

## 10. Resource bounds, checkpoint, atomic transport, and driver

Meter and cap actual input bytes, parsed rows, literal comparison letters,
prefix/suffix nodes and edge-state products, row-piece/context products,
sparse affine operations, quotient reductions/support, active keys, actual
matching correlation pairs, B/K ranks, queue actions, word/ledger DAG nodes,
precomputed and expanded letters, selected direct replays, canonicalization,
RSS, checkpoint bytes, final bytes, and writes. Test caps before expensive
allocation and charge serialization before the final seal.

The expected pre-quotient affine work is linear in the authenticated input:
producer `10*15,970` primitive edge states plus the typed row-piece products;
checker `10*26,136` suffix edge states plus its independently assembled
row-piece products. Do not retain the v4 passes
`2*10*5,475,488=109,509,760`, the at-least
`13,980,547,956` growing-prefix visits, repeated all-live scans, O(t^2)
expanded commutation, or repeated large transcript serialization.

Implement an atomic replayable checkpoint before production is authorized.
It binds the exact authority and source identities, v5 code identities,
resource envelope, completed evaluator frontier, next row/query ordinal,
raw chronological B/K rosters and coefficient ancestry, deterministic
echelon rebuild data, active registry, queue head, hash-consed word/ledger
DAG, epoch digest, and counters. On resume, recompute and compare the
echelons and next-state canary before continuing. A label or last-state string
is not a checkpoint. Use temporary-file plus flush/fsync/atomic replace and
reject stale or partial checkpoints.

Producer emits one sealed typed receipt for every terminal, including
`UNKNOWN_INPUT` and `UNKNOWN_RESOURCE`, and exits so the driver always invokes
the checker. The checker independently validates the terminal reason and
canonical payload under its own wall/RSS/support/output caps. Hard exceptions
are fail-closed and never become mathematical terminals. All outputs are
atomic; a single sentinel is written last only after exact-one equal full-line
producer/checker terminals and a nonempty sealed checker verdict.

The ASCII GAP driver must pass an explicit actual-input path, input/output/
checkpoint paths, and resource bounds to both programs. It pins exact v5
bytes/SHA and every authority identity at runtime, uses repository-relative
paths so Linux GHA and Windows agree, rejects stale owned v1--v5 outputs, runs
one producer and then one checker with bounded timeouts, and propagates all
statuses. No sleep, retry, poll, pool, local parallelism, hidden subprocess,
or workflow edit is permitted.

## 11. Required static traces and reply

The reply must give exact bytes/SHA for all four machine files and a complete
import/authority graph. Give line-numbered static traces for:

- actual production from driver through row 1, row 6,319, every later row,
  B/K closure, complete action matrices, ordered-basis export, and v280
  derivation;
- MEMBER, boundary-rank-rise, zero-correlation/K-rank-rise,
  `UNKNOWN_INPUT`, `UNKNOWN_RESOURCE`, malformed/checkpoint resume, and hard
  exception routes;
- producer forward-DAG and checker reverse-DAG reconstruction of all 6,441
  rows without a flat second pass;
- checker-owned B/K, dual/correlation, discrepancy, word/action, and
  rho0/rho1/q calculations; and
- every physical mutation, with actual owner path, before/after canonical
  digest, reached normal validator, and first rejection.

Report formulas and static counts separately from measured runtime/RSS.
Because execution is forbidden, measured fields must say `UNEXECUTED`, not
zero. End with exactly:

```text
IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
SELFTEST / PRODUCTION:           UNEXECUTED
AUTHORITY-V2 INPUT:              PASS / A4 1/3 only
ACTUAL POSITIVE BRANCH:          STATICALLY REACHABLE or BLOCKED
ACTUAL A4:                       remains 1/3 at most before execution
LIFT / FAKE / IHARA:             NONE
```

Any future execution requires a new Sol(max) audit which includes semantic
independence, all physical mutations, Linux/Windows path portability, exact
performance bounds, avoidable slow work, checkpoint recovery, and the v280
consumer boundary.

`TASK345_R07_TASK343_A4_V5_BIDIRECTIONAL_DAG_REPAIR`
