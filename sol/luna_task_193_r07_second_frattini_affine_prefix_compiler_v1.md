# Luna task 193 — R07 second-Frattini affine-prefix compiler v1

Commissioner: Sol / 2026-08-27

Reply to:
`sol/luna_reply_193_r07_second_frattini_affine_prefix_compiler_v1.md`.

Role: bounded mechanical implementation only.  Do not run Python, GAP,
Node, git, GHA, or network locally.  Parent Sol owns mathematical audit,
repository brokerage, and execution.  Do not edit task179/task186/task187
or workflow files.

## 1. Governing theorem and objective

Read in full:

```text
sol/proof_r07_task179_relative_frattini_successor_v145.md
sol/proof_r07_recursive_relative_magnus_frattini_compiler_v168.md
sol/proof_r07_noncommutative_completed_fox_frattini_selector_v169.md
sol/luna_task_186_r07_normalized_exact_common_word_colgen_v2.md
sol/luna_reply_186_r07_normalized_exact_common_word_colgen_v2.md
the complete task179/task186 producer/checker dependency cone
```

Implement the exact v168 affine compiler for the first genuine successor

```text
E_{r,1}=P_r/Phi_3(K_{r,0}),  r=3,4,
```

and, given a helper-accepted positive task186 production receipt, materialize
the actual block-tagged second-rung defect

```text
beta_1 in (K_{3,1}/K_{3,2})^2 plus K_{4,1}/K_{4,2}.
```

This task stops after constructing the literal raw Fox representatives and
the complete second-level base-boundary template.  It does not decide
`-beta_1 in im B_1` and does not search a second correction.

## 2. Authorized files

Create only:

```text
search/d972_r07_second_frattini_affine_prefix_compiler_v1.py
crosscheck/check_d972_r07_second_frattini_affine_prefix_compiler_v1.py
search/d972_r07_second_frattini_affine_prefix_compiler_gha_driver_v1.g
search/certs/d972_r07_second_frattini_affine_prefix_compiler_selftest_v1_20260827.json
sol/luna_reply_193_r07_second_frattini_affine_prefix_compiler_v1.md
```

Pin exact bytes/SHA-256 of every source.  A production input is one explicit
task186 receipt plus its exact artifact identity and matching independent
checker terminal.  Missing, resource-only, malformed, or non-positive input
is `UNKNOWN_INPUT`; it may not be replaced by g760 or a synthetic word.

## 3. Lazy affine marked quotient

For each block type PB3/PB4, use the exact presentation and frozen
`E_{r,0}` marked quotient.  With the task179 left-Fox convention, represent
an `E_{r,1}` element as

```text
(raw sparse C1 Fox chain modulo D0, E_{r,0} element blob).
```

Implement multiplication and inverse from the crossed-derivation law.  The
successor is the subgroup generated by the marked PB generators, never the
whole ambient semidirect product.

Two affine pairs are equal exactly when their base blobs agree and their
translation difference belongs to the complete span of every left translate
of the exact 2 PB3 or 11 PB4 presentation-boundary rows.  Implement a
fail-closed exact equality oracle:

1. positive equality returns a literal full boundary coefficient chain;
2. negative equality returns a dual pairing nontrivially with the difference
   and a complete zero correlation against the entire translated family;
3. a resource stop is `UNKNOWN_RESOURCE`, never inequality.

Precompute immutable source descriptors, batch complete ACTIVE lists when
safe as in task191's governing invariant, and memoize results by the full
typed sparse difference.  Do not import task191 producer code; authenticate
and independently implement any adopted algorithm.  The independent checker
must not import this producer and builds its own oracle.

Use the equality oracle to intern only affine values actually encountered in
the required words.  Labels are assigned by first literal encounter after
exact comparison against every prior label with the same base blob.  Store
each positive merge chain and every negative separation receipt needed to
prove the canonical label partition.  Hash-only or syntactic equality is
forbidden.

## 4. Actual beta1 construction

From the accepted task186 `corrected_word`, reconstruct independently:

```text
the two printed hexagon relation words in PB3
the five-factor printed-order pentagon relation word in PB4
```

First evaluate each relation word in the lazy affine quotient and require
the identity.  This directly replays that task186 reached `E_{r,1}`.

Next evaluate, over the marked affine quotient `E_{r,1}`:

1. the complete exact PB3 presentation relator roster (2 rows);
2. the complete exact PB4 presentation relator roster (11 rows); and
3. the two hexagon and one pentagon relation words.

For every signed letter, intern the affine prefix value exactly and form the
left Fox one-chain.  Require `D1=0` for all 15 base-boundary and three defect
rows.  Store the three raw defect rows separately as

```text
beta1_H1, beta1_H2, beta1_P
```

and store the 2/11 second-level base boundary rows in their exact order.
Their full left-translate span is the definition of the quotient; do not
enumerate that full span and do not claim membership/nonmembership of beta1.

Directly replay every PB presentation relator as identity in the affine
marked quotient.  Authenticate every arity-3/4 insertion/deletion map used
by the three relation words at the marked affine values, or stop with a typed
missing-map input.  Do not substitute a Jennings `j` projection.

## 5. Receipt, checker, and SELFTEST

The production receipt retains:

```text
positive task186 artifact identity and exact corrected word
all presentation/marked-map identities
affine marked-generator values
complete encountered affine pair roster and canonical labels
every equality chain and inequality dual/correlation
all multiplication/inverse/prefix transitions
2 PB3 and 11 PB4 second-level base boundary rows
three block-tagged beta1 raw rows and D1-zero replays
literal hexagon/pentagon words and printed order
resource counters and resumable chunk state
```

The checker reconstructs all objects from pinned presentations and the
ordinary task186 word without importing producer helpers.  It compares
literal sparse rows, group operations, equality partitions, boundary chains,
full dual correlations, and D1 values.

SELFTEST uses a bounded noncommutative presentation with a nontrivial
relative elementary-abelian successor.  It must contain two syntactically
different affine pairs which are equal modulo the full boundary span and two
pairs with the same base value which are separated only by a complete dual.
It then constructs a genuine next-rung Fox defect independently by direct
finite enumeration.

At minimum reject mutations of: one presentation relator; one marked
generator; crossed-product order; inverse formula; one block tag; omitted
translated boundary; equality chain coefficient; negative dual; sampled
correlation; affine label merge/split; one prefix label; one Fox sign; one
PB relator order; one pentagon factor/order; one D1 entry; task186 word;
Jennings substitution; and resource stop changed to equality/inequality.

## 6. Driver and delivery

Provide serial SELFTEST/PRODUCTION bindings, exact-one producer/checker
markers, fresh outputs, fail-closed caps, sealed resumable checkpoints, and
artifact upload compatibility.  No workflow edit.  Give conservative GHA
wall/RSS estimates separately for prefix interning and beta1 construction.

The reply processes Sections 1--6 in order, lists exact identities of all
five files, and ends with:

```text
LAZY E3,1/E4,1 AFFINE COMPILER:               NOT EXECUTED BY LUNA
ACTUAL SECOND-RUNG beta1:                      NOT EXECUTED BY LUNA
SECOND-RUNG MEMBERSHIP / CORRECTION:           NOT ATTEMPTED
COMPATIBLE COFINAL LIFT / FAKE / IHARA:        NOT DECLARED
```
