# Task1123 — C の残る reader 返値用途を個別に追跡する

宛先: 既存Luna / packet_checker。Solは数学裁定と独立静読を担当する。返信は `sol/luna_reply_1123_r07_C_remaining_reader_return_use_closure_v1.md`、最終行 `TASK1123_VERDICT:`。新材料は `%TEMP%/shadow-atelier-audit163/task1123/`。

現行v6の最終bindingを最優先する。rootから正式inventory5が届いたら、この追加設計を新しい版で保存して既指定の最終bindingへ戻る。本便はv6追加gateではない。新agent、Git/push/GHA、network/credential、codex exec、既process操作、親の変更・全artifact再走査は禁止。Python/GAP/Lean/source/import/AST/compile/selftest/数学実行は行わない。PS/.NETの原文・JSON metadata・全file pin/指定raw範囲の読取りだけを使う。旧source・指示/返信・TEMP票を上書きしない。repoへの変更は本便の指定返信のみ、新材料は指定TEMPの新規名。末行のVERDICTを守り、全材料のbytes/SHAと完全目録、読了/未読/OPENを明示する。

正式出発点は run34161493396/1、head a5b456a973f8a917f3af386d327061a02a0cf900、1834/8539 cross-checked limited7、verified=false。未来の1962/採用数/λ/停止枝を仮定しない。source実装の不具合と、将来cold設計の未閉鎖義務を混同しない。

## 固定した前件と今回の宇宙

1117のsource-chain-closure-index-v1.jsonに列挙された62 readerを全件の出発宇宙とする。F8で個別追跡した7 helperの証拠は保持し、残55件を単なる族割当から、定義・実callsite・返値の次consumerへ具体化する。62の識別子/関数又はmethod範囲/旧族/既7か残55かを最初に確定し、途中で分母を変更しない。定義数とmethod数などの表現が異なる場合は元62への全対応を示す。

自系1114/1117/1119/1121の凍結票を引き継ぐ。C6は419541 B / 3996972ccfe8ba9c168b537ac274de96ff69a6fe27fb400e7de6a8e3a19a52ffのまま。1121 root票327515 B / 7a79fd56bcc3cca9e83e709d6086726f98e748669be625a91cf2d3f0f7639405と公開接続票213927 B / 59b09d31f76dcdc86cac8490448169592bc191a5ac99e4f223bace462b2f25bbは自系照合として読んでよい。公開1115 proposed.v2＋scope v3、1121 dependent優先erratumを保持する。相手P6/1113/1116/1118/1120/1122のprivate source・返値票・設計は読まない。相手pinはopaque descriptorのみ。

先に入力を事前登録する。追加sourceは既登録sourceの具体call/import/attribute行から同定できる既存repo sourceに限り、読む前に別登録とwhole pinを付ける。動的解決の前件が欠ければOPEN。新artifact/8059全stream/packed/vector payload走査へは広げない。

## 具体作業

1. 残55件それぞれの全文定義、引数の原点、元variant別返値全shape、全候補callerを読んで、ordinary current / saved historical validation / fixture又は到達未決を区別する。raw定義/call/代入/返値選択の行とbyte範囲を固定する。grepだけ、definition一覧だけ、1代表callerだけで全用途閉鎖と言わない。未知aliasは候補とOPENを残す。
2. calleeの返値を、直接key選択だけでなくobject全体のcanonical serialization/hash/比較/publication、mutable object更新、yield/closure、例外・guard・早期停止に追跡する。型/不正入力の拒否という用途も現在のobservableに含める。共用primitiveについて呼出側の全列挙がないなら、そのprimitive全体を不要とはしない。
3. 既7件のdeltaを必要時だけ確認する。complete_reduction_coefficients/literal_signs/reduction_payloads/row_source/batch_target_parent/parent_layer_receipt/ThinAnchorは個別返値と現在/過去の使用が違う。未使用返値keyと、別経路で同値の数を計算する箇所を同じconsumerとしない。literal_signsの2未選択keyをrootが既採択した範囲で保持し、勝手に削除しない。
4. 11 native族/N/K/public consumerへ全62を接続する。現在の原instruction全body/h、全target/ordered operands、row/source/P1/maps/辞書/control/実checkpointと実に導かれる歴史HEADを区別する。source全射が隠れたinput不在の証明とはならない。whole containing-file ACTIVEと、値の数学的必要性やファイル分割可能性を区別する。
5. CURRENT_REDERIVED・将来ACCEPTED_CITED候補・OPEN_PREMISEの分類はcallごと/条件ごとで行う。旧λ1450/1578/1706、current1834と未来実λを混ぜず、未実施の旧target数値式を現在のsource欠陥へ読み替えない。採用0層・candidate/local差・部分診断も通常到達可能な契約として保持する。
6. 全結果を、固定source/ordinary mode/条件付きの保守source-static用途閉包としてまとめる。解けない55の一部は具体OPEN/ACTIVE_KEEPでよい。全native catalog/Γ/全runtime/input閉包/最小TCB/B/変換/速度を採択しない。public数学契約と自系private caller・返値表は別材料にする。

成果は元62への全対応表、残55の個別返値・consumer・分岐/aliasの原文根拠、旧7の継承又は精密delta、current file-role保持と具体OPEN、全材料目録。source変更・数学実行・親変更0を保つ。
