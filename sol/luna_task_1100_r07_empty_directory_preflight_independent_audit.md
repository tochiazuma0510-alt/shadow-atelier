# Task1100 — 1099 認証済み空directory復元helperの独立静的別読

宛先: Luna / packet_checker（1092・1098完了後）。親Solのみbroker。主作業は修正版v5の発射であり、本便は新GHA発射の追加gateではない。

Task1099と最終reply1099を全文読み、作者が全材料をfreezeしてから、指定された新ASCII PowerShell metadata helperを別読する。まだ最終未着なら1099の契約と公開旧root/ZIP/empty名のscopeを読み、作者へ完成材料の所在だけ問い合わせてよい。実入力は読取専用。P/C数学本文を読む必要はない。

対象は登録旧v3 run34023589045と旧v4 run34120585268に属する36+38=74空directoryだけ。全outer ZIP bytes/SHA/EOF・全regular file名/bytes/SHA・全inner ZIP/explicit directory・全登録directoryとcontrols・containment/非reparse/typeを、最初のmkdirより前に認証する因果順を全文追う。既定guard=false、rootの実行時binding、全親事前認証後のparent-first mkdir、直前親再確認、CreateNew外側journal/receipt、実行後の全fileとdirectory再読、regular file不変、余計な名/既存file/相互重複/pathtraversal/reparse/型違いの拒否を重点にする。

外部processによる後刻の削除を防げるとは主張しない。原GHA ZIPが持たない空directoryを勝手に補完せず、outer controlに保存された完全inventoryとinner ZIP明示entryという来歴を区別する。削除/ACL/marker/cache/親改変/source実行を追加してはならない。エラー途中のjournalと最終成功receiptを混同しない。helper作者によるstatic票をそのまま独立根拠にせず、自系raw/metadataで確認する。

実行0（helper/dot-source/AST/import/compile/Python/GAP/source/数学/自己試験すべて0）、root/archive/dir/file変更0、Git/GHA/network/credential/process操作0。ツリー書込は sol/luna_reply_1100_r07_empty_directory_preflight_independent_audit.md のみ、独自証拠は %TEMP%/shadow-atelier-audit163/task1100/ の新規file。未読箇所・required finding・正確なsource pinと全EOF範囲を報告する。最終行 AUDIT_1100_VERDICT:。本便は静的採否のみで実復元や旧typed再起動を許可・実施しない。
