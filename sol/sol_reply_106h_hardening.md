**F106h 総合判定: 条件付き PASS／修理承認。** run `31033348812` の失敗 shard `92399304155`,`92399304864` はともに `gap-actions/setup-gap@v3.8.0` 内の `apt-get update` が `packages.microsoft.com` で 403・exit 100 となった infra flake であり、指定の `sudo rm -f ...` step を全 S/V/P lane 共通の **`shard` job の setup-gap 直前**へ入れることを承認する（現行 `collect` job には setup-gap/apt が存在しないので追加不要、将来置く場合のみその直前へ）。ただし provenance 上は source template と installed workflow の双方を同期し、新 normalized digest から class component bundle/class ID を再導出して三 shard manifest の class ID・self/file digest と versioned frozen manifest v3.1 の派生 binding を更新すること（数学・宇宙・range・STOP・容量 component は不変）；`bundle_static_audit`・binding/lock checks、一括 commit、fresh preflight-only S/V/P の candidate evaluation 0 を通した後の再 dispatch を認可する。
