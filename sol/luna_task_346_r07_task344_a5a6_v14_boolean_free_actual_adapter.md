# Luna task 346 - task344 A5/A6-v14 Boolean-free actual adapter

Role: Luna, bounded implementation only. Read this full mail and every
numbered prerequisite first to last. Do not run Python, Node, GAP, GHA,
workflows, git, or network. Preserve every v7--v13 source, fixture, receipt,
authority, proof, workflow, and v220 file. This mail authorizes only the five
new v14 outputs in Section 2 and no execution.

Task344's verdict is REJECT / UNEXECUTED. V13's five frozen arithmetic cases
are statically sound, but no actual production route exists. Do not wrap or
call the v13 producer/checker main, shallow anchor adapter, mutation harness,
or driver. Reuse finite-field routines only after restating and testing their
retained-basis invariant.

## 1. Binding prerequisites and supersession order

Read in full, in this order:

1. sol/luna_task_338_r07_task335_a5a6_v13_semantic_rebase.md;
2. all five v13 outputs;
3. sol/sol_reply_344_r07_task338_a5a6_v13_code_performance_audit_v1.md;
4. sol/proof_r07_actual_a5_three_input_slice_compiler_v242.md;
5. sol/proof_r07_recomputed_anchor_and_adapted_a5_basis_v280.md;
6. sol/proof_r07_compressed_pair_dag_exact_endpoint_v281.md;
7. sol/proof_r07_boolean_free_actual_a5_a6_compiler_v283.md;
8. v231, v238, v239, and v247;
9. the accepted task198 five-member authority, task192/task193 sources and
   latest accepted artifacts, task226/A2 producer-checker ABI, and the
   accepted task227/A3 consumer implementation;
10. task345's A4-v5 contract. Its future accepted producer/checker receipt is
    an input dependency, not something v14 may fabricate.

Task344 supersedes all v13 reachability, production, mutation, and resource
claims. V280 supersedes every standalone anchor/Boolean ABI. V281 supersedes
expanded duplicate-pair storage. V283 is the binding mathematical pipeline,
including endpoint-base-point invariance.

## 2. Sole permitted outputs

Create only:

- search/d972_r07_joint_slice_kernel_general_v14.py;
- crosscheck/check_d972_r07_joint_slice_kernel_general_v14.py;
- search/d972_r07_joint_slice_kernel_general_gha_driver_v14.g;
- search/certs/d972_r07_joint_slice_kernel_general_selftest_v14_20260829.json;
- sol/luna_reply_346_r07_task344_a5a6_v14_boolean_free_actual_adapter.md.

Return IMPLEMENTED / UNEXECUTED only if the five frozen cases reach their
arithmetic terminals and a literal static trace shows the actual adapter
would consume every upstream owner below, with no placeholder, copied
Boolean, trusted pair list, missing import, or schema gap. If an exact
accepted upstream ABI is unavailable, return BLOCKED / UNEXECUTED and name
the first missing field/owner; do not invent it. SELFTEST and PRODUCTION are
unauthorized until a new Sol(max) full code-and-performance PASS.

## 3. Exact v13 failures which must disappear

Trace and remove every item, not only the first failure.

1. The v13 wrapper commits this workstation's absolute Windows path. V14
   authenticates a canonical repository-relative path and records the
   observed absolute path only as output. Linux and Windows must accept the
   same committed fixture bytes.
2. Producer controls 25--34 mutate nested transcript fields but their owner
   locator reads nonexistent top-level fields. Checker controls 12--15 and
   25--34 have the analogous defect.
3. Producer controls 35--44 reference unassigned local values before their
   oracle. Checker controls 35--44 mutate only hard-coded placeholders.
4. The checker rejects the legitimate null normalized ancestry of a
   DEPENDENT record. Null is allowed exactly there; an ACCEPTED rank raise
   must have a normalized transform.
5. The driver never supplies --actual-input. Missing actual input currently
   produces a successful STATIC_BLOCKED line. V14 treats it as a sealed
   UNKNOWN_INPUT and never writes a success sentinel for it.
6. The optional actual adapter accepts arbitrary strings, small integers,
   three true flags, supplied base_pairs, and a substring canary. Delete this
   adapter completely.
7. No task198 authority, task232 context, task192/task193 row, accepted A4
   basis, A3 coefficient, actual joint matrix, A5 terminal, or A6 polynomial
   reaches v13 production. V14 must have one explicit owner path for each.
8. V13 repeats accepted-row reduction, builds up to 27 dense tableaus for
   five toy cases, canonicalizes/deep-copies whole receipts per mutation,
   has phase-corrupt meters, no live caps, nonportable RSS units, no
   replayable resource receipt, and no output-byte cap.

## 4. Portable actual envelope and common dependency cone

The driver requires an explicit actual-input path in PRODUCTION and passes
the same immutable opened bytes to producer and checker. The envelope is a
manifest of repository-relative member paths, exact byte counts, SHA-256,
schema/version, self seals, accepted producer/checker terminals, and common
roof/tower/lower-word identities. It binds:

- all five accepted task198 authority-v2 members and their three physical
  task198 source files;
- positive A2 and A3 producer/checker packages;
- task192 and task193 literal objects and their independent verdicts;
- one accepted A4-v5 producer/checker pair containing the complete ordered
  word-bearing basis; and
- the pinned arithmetic/map sources needed to evaluate words and rows.

Resolve every member beneath the runtime repository root, reject traversal
and links/reparse points where supported, open without following links,
read/hash the same opened handle, and recheck its identity before close.
Producer and checker implement separate envelope adapters. Neither accepts
a manifest's copied hash in place of physical bytes.

The composite cone must prove that all members use the same task192 lower
word, task198 roof/tower, Delta1-to-Delta0 edge, task232 context roster,
typed occurrence roster, and field convention. A mismatch is UNKNOWN_INPUT.
Missing or malformed JSON and built-in I/O errors are converted into one
sealed typed UNKNOWN_INPUT receipt which the independent checker can inspect;
there is no traceback-only expected route.

The SELFTEST fixture may bind the immutable v11 five-case source by portable
relative path and exact bytes/SHA. It may not present those width-13 objects
as actual packages.

## 5. Independent reconstruction of actual typed arithmetic

Producer and helper-nonshared checker independently reconstruct the ten
task232 substitutions, preserving distinct type, ID, tag, and occurrence:

    types: E3,E3,E3,E3,E3,E4,E4,E4,E4,E4
    IDs:   21,22,23,24,25,1,27,21,26,28
    E3:    (x,y),(x,z),(y,z),(u,x),(u,y)
    E4:    the five accepted task232 substitutions

Use the authenticated right-to-left product convention and the complete
ten-to-eleven occurrence reinsertion. Never merge E3-C21 with E4-C21.

From original task192/task193 and boundary bytes, reconstruct:

- d1 = -D1(g760), with its noncycle endpoint identity;
- e1 = -beta1^193, including every v239 sign;
- the eleven-occurrence vector w before block summation;
- all complete translated PB3/PB4 boundary reductions;
- simultaneous marked x,x^-1,y,y^-1 actions on both joint coordinates; and
- the post-action, F3-linear printed block map C.

Production dimensions and sparse coordinate keys are derived from these
objects. The literal width 13 and occurrence00-style labels remain
SELFTEST-only. Applying C before invariant closure is a fatal semantic error.

The checker shares only immutable upstream bytes and public finite group
encodings. It must not import producer composite evaluators, pivots, row
caches, transcript, adapted basis, prefix DAG, or mutation results.

## 6. Boolean-free A4-v5 consumer

Accept the complete ordered word-bearing basis (u_i,k_i), not a standalone
anchor. Authenticate the A4 producer/checker pair and its ordered-basis
completeness theorem. Then producer and checker independently:

1. parse and deterministically free-reduce every u_i;
2. evaluate actual rho1(u_i)=k_i and rho0(u_i)=1;
3. evaluate q(k_i), compare it with exactly 1,z0,z0^2, and derive a_i;
4. select the least j with nonzero a_j, compute e=a_j^-1, and derive
   u_star=red(u_j^e), k_star=k_j^e;
5. replay rho1, rho0, q and the required roof-fibre endpoint;
6. construct the ordered adapted basis
   k_star, k_i k_star^(-a_i) for i not equal to j;
7. construct its change matrix and inverse and replay both directions; and
8. prove that exactly the k_star seed can carry a nonzero occurrence
   endpoint while every other pointed seed remains present.

Supplied convenience fields are compared only after derivation. Booleans,
anchor labels, supplied exponents, formula strings, and package names never
control acceptance. If all a_i vanish, return UNKNOWN_INPUT under the
accepted A4 premise.

Cache each authenticated word evaluation once per typed map. Keep literal
word/DAG ancestry sufficient for downstream replay; a digest is not a word.

## 7. Local A3 base point

Authenticate the positive A3 canonical coefficient map
lambda=sum lambda_g g and its A2/task226 ancestry. For every nonzero
coefficient derive the D1 normal-form section s(g) locally from the
authenticated key. Reject a supplied base_pairs list as evidence.

Construct in canonical coefficient/key order:

    lambda_g * (red(s(g) u_star) - s(g)).

Independently replay both rho0 endpoints, both rho1 values, the q-image
g(z0-1), coefficient order, A3 ledger, and the complete matching endpoint.
Construct kappa0 and kappa0*d1 locally through the actual full-cokernel
action. V283 proves that this base-point choice leaves the A5 decision
unchanged; the code still replays the actual resulting target ancestry.

No enumeration of all 729 D1 elements is needed. Work is linear in the
nonzero support of lambda plus literal section and word lengths.

## 8. Actual v242/v283 joint closure

Before C, form every adapted seed

    ((k_i-1)d1, (k_i-1) odot w)

with exactly one possible endpoint-bearing seed. Maintain one FIFO queue of
actual rank rises and one sparse retained basis. For each seed and marked
action candidate record immutable raw theta/z/eta data, parent/action,
pre-rank, reduction coefficients, decision, normalization, normalized
ancestry, post-rank, and queue effect.

Use one reduction operation returning represented/remainder/coefficients.
Do not call contains and then reduce again during insertion. State and enforce
the sign convention from v13/task344. Pad every old transform when the raw
roster grows, retain dependent zero relations, and replay every accepted and
dependent candidate from original literal ancestry.

Stop only at queue exhaustion. A cap or word-radius stop is
UNKNOWN_RESOURCE, not closure. The checker reconstructs the complete raw
candidate stream with a different action/pivot order and proves both
containments of the occurrence-level joint span; rank equality alone is
insufficient.

Only then apply C, compute the full post-C nullspace, reconstruct theta and z
rows, build Hd1, and prove both span containments. Construct

    r0 = e1 - kappa0*d1.

MEMBER emits actual coefficients and complete replay to theta in ker Phi,
mu1=kappa0+theta, mu1*d1=e1, and Phi(mu1)=epsilon1. NONMEMBER emits a
functional which annihilates both independently reconstructed Hd1 spans and
pairs to one with r0. A NONMEMBER statement is restricted to this exact
authenticated lower word and input cone.

## 9. Canonical v281 A6 handoff

On MEMBER only, emit base-point pairs first and selected live closure pairs
as canonical factored records:

    (coefficient, prefix_DAG_node, kernel_word_index).

The prefix DAG stores authenticated earlier parent plus literal letter; the
kernel dictionary stores each adapted word once. Distinct literal prefixes
must not be combined merely because their finite-shadow values agree.
Producer and checker reconstruct every prefix A, red(Au_i), both rho0
endpoints, rho1 values, roof-fibre endpoint, the pair polynomial, and all
mu1 equations.

This terminal is A6 language only. It does not assert exact PB ZERO, a
compatible lift, fake, or Ihara. The future A7 positive boundary must expand
every U=red(Au_i), V=A and recompute the three exact PB chains independently.

## 10. Structurally independent finite linear algebra

Retain the sound v13 retained-basis signs, padding, post-C orientation,
MEMBER solve, and separating-dual mathematics. Producer uses one sparse
left/forward-pivot object per span. Checker uses a separately implemented
right/bottom-pivot object and ordinary polynomial nullspace/solve.

The checker reads original upstream bytes, rebuilds actual rows and actions,
allows normalized_ancestry=null exactly for DEPENDENT records, replays every
producer certificate, and independently proves closure, kernel, Hd1,
terminal, adapted-basis, local-base-point, and A6 facts. It never accepts a
producer terminal, rank, pivot, Boolean, cache, digest, or DAG evaluation as
mathematical authority.

Avoid exponential 3^r enumeration, repeated accepted-row reduction, and
rebuilding a known span merely to ask containment. Two-way independent span
checks remain mandatory.

## 11. Physical mutation controls

Use one versioned producer/checker owner roster. Preserve the sound v13
matrix and certificate controls, fix every nested locator, and route each
through the normal validator. Replace all placeholder anchor/base-pair
controls with actual-owner mutations covering:

- actual envelope member path/bytes/SHA/seal/common identities;
- task198/task192/task193/A2/A3/A4 producer-checker identity;
- task193 sign and d1 noncycle endpoint;
- task232 type/ID/tag and ten-to-eleven occurrence reinsertion;
- A4 basis word, k_i, rho1/rho0/q value, a_i, least j, inverse e, u_star;
- adapted change matrix, inverse, two-way word replay, one-endpoint seed;
- A3 coefficient/key, derived section, locally generated pair/order and both
  endpoints;
- pre-C seed, action, boundary coefficient, parent, decision,
  normalization, dependent record, queue, and premature C;
- post-C nullspace, Hd1, target, MEMBER equality, and dual pairing;
- v281 prefix parent/letter, kernel index/word, coefficient, and pair
  endpoint; and
- cap, checkpoint, terminal, atomic write, stale output, duplicate terminal,
  and sentinel.

Each mutation changes the actual object used by normal production, recomputes
only derived envelope seals needed to reach the intended gate, records
before/after canonical owner digests, reaches the expected normal validator,
and is rejected first at its registered narrow stage/code/reason. There is no
mutation-name branch, explicit forced raise, copied Boolean, placeholder
object, or arbitrary-exception PASS.

For SELFTEST, bounded production-shaped fixtures may be derived from the five
immutable cases, but controls of actual adapters must load actual-form
fixture members through the same adapter. Do not deep-copy a future large
receipt once per control; use immutable base bytes plus a bounded owner
overlay which still traverses the normal parser/validator.

## 12. Performance, caps, checkpoint, and meters

Let N be the actual chronological candidate count, W actual row width, r
terminal closure rank, d post-C nullity, h Hd1 rank, t A4 basis size, s A3
support, p used prefix-DAG edges, and L_K used kernel-word letters.

Report exact static formulas and actual measured counters separately. The
generic retained-closure bound is O(N*(W*r+r^2)); preprocessing adds the
literal A4 evaluations and O(t+s) group operations; v281 adds one evaluation
per used prefix edge and kernel word per typed occurrence plus actual support
contributions. Do not advertise width 13 as the production bound.

Use explicit meter phases: input, actual preprocessing, closure, post-C,
certificate replay, mutations, final I/O, and one separate bucket per frozen
case. A phase snapshot is a partition, not a cumulative number relabelled
per case. Normalize ru_maxrss units on POSIX and use a real Windows process
memory source or mark it unavailable; zero is not a measurement.

Check wall, RSS, candidate, field-operation, ancestry, expanded-letter,
canonicalization, checkpoint, and output-byte caps before expensive work.
Return a sealed last-replayable UNKNOWN_RESOURCE on cap. Missing/malformed
input returns sealed UNKNOWN_INPUT.

The atomic checkpoint binds input/code identities, phase, next ordinal, raw
roster, rebuildable echelon, queue head, retained ancestry/prefix DAG,
target state, and counters. Resume rebuilds and cross-checks the echelon and
next-state canary. Writes use temporary file, flush/fsync, and atomic replace.
Charge exact encoded bytes and final writes after detaching the nonrecursive
resource snapshot.

## 13. Driver and required reply

The ASCII GAP driver supports explicit SELFTEST and PRODUCTION. PRODUCTION
requires an actual input argument/path and passes it to both programs.
Repository-relative committed paths are portable; no workstation absolute
path is an equality premise. Pin exact v14 quartet bytes/SHA and all current
input identities. Stale-reject every owned v7--v14 receipt, verdict, log,
terminal, shell, checkpoint output, and sentinel.

Run exactly one producer followed by one independent checker under bounded
timeouts. Transport positive and nonpositive sealed receipts to the checker,
compare exact-one equal full terminal payloads, require nonempty independently
sealed verdicts and terminal-specific sidecars, and write the sole sentinel
last. No sleep, poll, retry, lock, pool, local parallelism, hidden subprocess,
or workflow edit is permitted.

The reply must provide:

- exact bytes/SHA for all four machine files and the import/data graph;
- static traces of all five frozen cases from arithmetic, including the
  legitimate dependent-null route;
- a line-numbered actual-input trace through every Section 4--9 owner, or
  the first exact missing upstream ABI if BLOCKED;
- producer/checker retained-basis and independence proofs;
- every physical mutation owner, digest change, reached gate, and first
  rejection;
- portable driver process flow, all terminal routes, cap/checkpoint recovery,
  operation formulas, and a list of removed unnecessary work.

Measured runtime/RSS fields must say UNEXECUTED, not zero. End exactly:

    IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
    SELFTEST / PRODUCTION:           UNEXECUTED
    FIVE FROZEN CASES:               STATICALLY REACHABLE or BLOCKED
    ACTUAL A5 / ACTUAL A6:           0/3 / 0/3
    LIFT / FAKE / IHARA:             NONE

Any future execution requires a new Sol(max) full semantic,
code-independence, physical-mutation, portability, and performance PASS.

TASK346_R07_TASK344_A5A6_V14_BOOLEAN_FREE_ACTUAL_ADAPTER
