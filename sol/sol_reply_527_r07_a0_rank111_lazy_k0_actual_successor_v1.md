# Sol reply 527 -- actual rank111 lazy K=0 successor

## Outcome

The commissioned current-task445 K=0 owner is implemented in exactly the four
new versioned outputs:

- `search/d972_r07_a0_actual_tau_free_lazy_k0_seed_v5.py`
- `crosscheck/check_d972_r07_a0_actual_tau_free_lazy_k0_seed_v9.py`
- `search/d972_r07_a0_actual_tau_free_lazy_k0_rank111_resume_gha_driver_v13.g`
- this reply

No Task525 file was overwritten.  No production run, full rank111 replay, GHA
dispatch, workflow edit, git mutation, release mutation, or v220-numerator
change was performed.

Executable-output fingerprints are:

- producer v5: 34773 bytes;
  `94e9079c36592414d394f816d0d1190822157c017afecd9e75c9e19f8c7aa5aa`
- checker v9: 27570 bytes;
  `9b9bfbf72a312ed759861c854f1f5513342c037c2eb74b89bee8e09caa2f29c0`
- driver v13: 8683 bytes;
  `8f034abce4f469542206338e14502281dcc7ae1862338bafef5d431d74e5f63e`

## Implemented owner

The producer pins and imports the actual task445 v3 owner.  It accepts only
the exact legacy member or a sealed v5 checkpoint.  Migration authenticates
the raw 85934-byte member, its 68-record canonical prefix
`684039158b841d607aa40617778b9267ea96d64a38d952f74e63791b23ea3932`,
state seal, `accepted/rank/round = 68/111/73`, and the required dual and
remainder digests.  The 68 legacy source dictionaries are retained byte-for-
byte at the canonical-object level and replayed through current task445 before
new work.

Selection is action-first.  A correction miss obtains one current task445
tau-free adjoint and touches the 44 compact seeds in the frozen printed order,
one at a time.  The K=0 formula scalar is evaluated on the **whole current
task445 formula**: current `m.model179`, current `occurrence_data`, and all
coincident matching terms are summed before deciding zero/nonzero.  Thus
support membership cannot bypass cancellation.  K!=0, nonzero tau, S3--S9,
unsupported order/coordinate, and other unimplemented cases terminate as
claims-false `UNKNOWN_RESOURCE`.

For the first nonzero nomination, v5 reconstructs the literal conjugate,
task445 physical row and fresh row; checks exact exponent/N/E typing; compares
the direct physical pairing with the nonzero whole-formula scalar; calls the
real `P["phys"].reduce(row)` nonmutating path; checks the predicted pivot; and
then performs exactly one `P["phys"].add` and one task445 carry-forward update.
The typed record contains the required seed/formula/support/kernel/literal,
coordinate, exponent, scalar, pivot, digest, and counter fields.  The sealed
checkpoint is atomically replaced immediately after each accepted rise and
before the next round.

The v9 checker does not import v5 or a producer selector validator.  It pins
the current independent v7/low-arithmetic chain, replays the exact legacy
anchor, independently reconstructs every subsequent action or complete
preceding K0 seed/support schedule, recomputes the whole formula and direct
physical row/pairing/remainder/pivot/update, and checks artifact/checkpoint
equality, seal/binding, counters, terminal cardinality and claims.  A selected
K!=0 record is explicitly rejected.  Resource terminals and physical/formula
invariant failures remain distinct.

Driver v13 is the v11 transport/runtime envelope with only the new versioned
paths, pins and markers.  The permanent release identity, ordered eight-member
manifest, one-indexed member 5 selection, stale-output gates,
`set -euo pipefail`, `umask 077`, `ci/out`, `ulimit -v 5200000`, 7500/7200
producer envelope, 4800000000-byte RSS request, 64-rise cap, 3600-second
checker envelope, and one exact checker marker are retained.  It has no
production SELFTEST.

## Bounded evidence

All writable fixtures were repository-external under `%TEMP%`.  The ordinary
entry-point checks were:

```text
python -m py_compile <producer-v5> <checker-v9>                 PASS
python -B <producer-v5> --mode FIXTURE --output <temp-json>    status=FIXTURE
python -B <checker-v9> --self-test                             SELFTEST_PASS
.\gap.ps1 <ReadAsFunction harness for driver-v13>              exit 0
```

The producer fixture imported the pinned task445 v3 fixture successfully,
authenticated roster count/digest `44 /
7612682d024b61f873928ad122c9a5d7462c812a6633112f08706cda4412b6c8`, and
rejected 43, 45, 6441 and a changed digest.  The checker self-test reports
`producer_selector_imported=false` and rejects K!=0, second insertion,
direct-scalar and normalized-epsilon mutations.

Repository-external inline import harnesses exercised the final `select_one`,
`direct_correction` and `commit` functions, rather than a substitute selector:

- actual action hit: zero seeds/formulas/identity replays and one add/update;
- actual seed-1 path: seed 2 untouched, one formula, no eager identity replay,
  one add/update;
- current task445 `PackedEchelon.reduce/add` plus task445 update:
  `PACKED_ECHELON_REDUCE_ADD_UPDATE_PASS 41 1 True`;
- K0 empty, zero, coincident-term cancellation, nonzero and frozen-order cases:
  `K0_EMPTY_ZERO_CANCELLATION_NONZERO_ORDER_PASS`;
- unsupported coordinate/order and K!=0: claims-false resource terminals;
- formula/direct scalar, row/fresh, exponent, forbidden E, bad N, dependent
  row, pivot, stale-dual and second-insert mutations: all rejected;
- time and RSS stops before seed 1 and mid-fibre: the already durable exact
  68-row prefix remained unchanged.

Migration tests rejected legacy count, rank, round, binding, seal, prefix,
pivot, pre-digest and post-digest changes (9/9).  V9 also rejected a coordinated
legacy row/scalar mutation after recomputation of the producer-visible seal.

The independently downloaded archive was 37586 bytes with SHA-256
`8b740dbbc81f5d2e659371a81453ded56c6711ce8ace35a4af5255303e0095de`.
A .NET `ZipArchive` fixture checked all eight entries in archive order, exact
names/bytes/SHA-256, and selected one-indexed member 5.  Its selected member was
85934 bytes with SHA-256
`69a7ec3da4907f24af0f68c1975538b9ff9b6102f14e334f7c0725d2542dfd93`.

The GAP-built `D527Cmd` (captured before execution) passed `bash -n`:
`GENERATED_D527CMD_BASH_N_PASS`.  `git diff --no-index` against v11 classified
the changes as version/path/pin/marker/status-contract substitutions; the
release/run/job/head/API identity, ZIP pin, complete member manifest, resume
member pin and resource envelope are unchanged.

## Known limitations and boundary

The full task445 setup was not replayed locally.  A bounded Windows attempt
entered the genuine pinned bootstrap and stopped at the pre-existing
`windows_same_handle_identity_unavailable` platform gate.  The intended Linux
path is the authenticated v13 driver and was deliberately not executed here.
The seed-1 live-selector harness instrumented its expensive dependencies to
force the bounded branch, while retaining the final imported selector,
task445 row representation and real `PackedEchelon` reduction/insertion path.

This owner intentionally says nothing negative outside the current K=0 lane.
No bounded result is a Lean verification, no result changes a v220 numerator,
and no unrun production outcome is claimed.

Verdict: `READY_FOR_INDEPENDENT_AUDIT`
