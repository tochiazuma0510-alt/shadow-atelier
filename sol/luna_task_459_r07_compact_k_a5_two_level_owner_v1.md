# Luna task 459 — compact K/A5 two-level owner v1

Role: Luna implementation and bounded fixtures. Do not invent mathematics,
run heavy local production, dispatch GHA, edit workflows, commit, push, or
touch files outside the four outputs below.

The frozen mathematical contract is
`sol/proof_r07_compact_roster_direct_relator_a5_a7_v418.md` together with the
physical A4 quotient model in
`sol/proof_r07_a4_restricted_evaluator_sufficiency_v267.md`. This is the
actual completeness repair, not another direct-relator relabel.

## 1. Required outputs

Create only:

1. `search/d972_r07_compact_k_a5_two_level_owner_v1.py`;
2. `crosscheck/check_d972_r07_compact_k_a5_two_level_owner_v1.py`;
3. `search/d972_r07_compact_k_a5_two_level_owner_gha_driver_v1.g`;
4. `sol/luna_reply_459_r07_compact_k_a5_two_level_owner_v1.md`.

## 2. Authenticated source owners

Reconstruct the 44 literal compact relators from the pinned Task411 producer
and its frozen public inputs; do not depend on the temporary downloaded GHA
artifact and do not copy a disclosed relator list into source. Require:

```text
compact_relator_count = 44
relators_sha256 =
7612682d024b61f873928ad122c9a5d7462c812a6633112f08706cda4412b6c8
```

Pin Task411 producer/checker/driver at their actual repository identities.
Use the accepted actual A4 ten-context/seven-block evaluator and complete
typed PB-boundary oracle from the current v17 lineage as arithmetic authority.
Use Task456 zero-base v5 only as an ABI/arithmetic reference for the
occurrence-separated A5 map; do not inherit its NONMEMBER theorem.
Task193-v5 authentication must remain fail-fast and exact.

If a named old wrapper has a known bad transport pin, select the nearest
accepted underlying actual arithmetic owner and record that choice. Do not
repair unrelated legacy checkpoint transports in this task.

## 3. First level: compact word-bearing K quotient closure

Implement the v418 K queue in the actual A4 quotient:

1. evaluate the 44 literal words as raw ten-context/seven-block A4 relation
   rows;
2. decide rank only modulo the complete typed PB-boundary image D; raw ambient
   independence is forbidden;
3. every retained quotient pivot stores its literal source word or a compact
   hash-consed ancestry DAG;
4. enqueue conjugation by x, x^-1, y, y^-1 exactly once per retained K pivot;
5. dependent candidates retain only counters and their exact D + K reduction
   ledger, never a large trace;
6. K exhaustion requires empty action frontier plus a complete zero-correlation
   boundary oracle for every declared quotient dependency.

Initial seed attempts are exactly 44 and action attempts are at most 4t for
final K rank t. Log both separately. A bounded/resource stop is UNKNOWN and
must preserve a resumable checkpoint.

## 4. Second level: stream accepted K pivots into A5

As soon as K accepts a word-bearing pivot (k_l,u_l), directly evaluate

```text
widehatPsi(k_l - 1)
source pair: (w u_l) - w
```

in the full pre-C eleven-occurrence A5 space. Start/continue the marked
left-action closure under x, x^-1, y, y^-1 and keep the A5 PB-boundary slack
separately tagged.

- A finite raw target hit may stop immediately and must emit the exact
  coefficient ancestry, literal M, and PB ledger.
- A5 NONMEMBER is forbidden unless the K action frontier, A5 action frontier,
  K quotient-boundary oracle, and A5 PB-boundary oracle all exhaust.
- Do not start A7 Schreier/lift-null enumeration in v1. A positive A5 output
  is the handoff to the already versioned fusion successor.
- Do not evaluate all 6,441 old relators before useful work. They may be a
  post-terminal streamed equivalence oracle only; positive selected support
  never needs unused rows.

## 5. Checkpoint, memory, and progress contract

Use one deterministic owner and bounded immutable candidate batches only.
Do not copy a reducer into workers. A checkpoint binds:

- all source owner identities and compact-roster digest;
- K quotient pivots, K-boundary ledger/oracle cursor, K action frontier;
- A5 pre-C pivots, A5 boundary ledger/oracle cursor, A5 action frontier;
- literal ancestry DAG and target remainder;
- exact K/A5 ranks, completed seed ordinal, and rolling self-seal.

Checkpoint after every completed compact seed and every bounded action batch,
so a resource stop resumes from visible progress. Log at least phase, compact
seed ordinal/44, K rank/frontier, A5 rank/frontier, both boundary cursors,
elapsed time, owner RSS, and checkpoint bytes. Keep total RSS below 5.7 GB.
No SELFTEST, mutation campaign, legacy replay, or eager boundary closure may
precede useful seed 1.

## 6. Independent checker and bounded gates

The checker must not import producer helpers. For MEMBER it independently
reconstructs the selected compact relators, K conjugation ancestry, raw A5
columns, selected PB support, literal M, and target equality. For NONMEMBER it
additionally reconstructs both exhausted action closures and both complete
boundary-zero correlations. UNKNOWN receipts carry no mathematical claim.

Run locally only compile/help/hash-pin/parse and tiny synthetic quotient
fixtures showing:

- a module generator whose conjugate raises K rank;
- a raw-dependent row which is independent before adding D but dependent
  modulo D;
- accepted K pivots stream once into A5;
- finite A5 hit before K exhaustion remains MEMBER;
- incomplete old direct-relator miss becomes UNKNOWN;
- checkpoint/resume reproduces both ranks/frontiers/digest;
- mutation of one conjugation edge, PB ledger item, or cursor is rejected.

If the existing A4 ABI lacks one exact operation, report the precise missing
field/function after implementing every independent part. Do not substitute
the A0 occurrence closure or A5 image as a supposedly faithful K coordinate
model.
