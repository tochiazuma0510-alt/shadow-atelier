# Luna task 174: R07 target6 simultaneous-context image census v1

Date: 2026-08-27
Role: Luna / implementation and heavy finite census only

## 1. Purpose and mathematical source

Read completely:

- `sol/proof_r07_full_e4_joint_orbit_selector_v109.md`;
- `sol/proof_r07_context_fibre_dual_correlation_v118.md`;
- task 172 and its final Sol-promoted producer/checker/certificate/reply;
- task-157ee through task-157en sources, checkers, drivers and reports; and
- task 169 and its current assets for relation-roster conventions.

For target6, compute the marked simultaneous context image

\[
 \Delta_E=\operatorname{im}\bigl(F(x,y)\to E_4^3\bigr)
\]

for the exact ordered contexts `(fxy,fxz,fyz)`.  Measure the three coordinate
projection images and kernels needed by v118.  This task does not build the
full D2/correction orbit or solve target6.

## 2. Authorized versioned outputs

Create only new task-174 files:

```text
search/d972_r07_target6_context_image_census_v1.py
crosscheck/check_d972_r07_target6_context_image_census_v1.py
search/d972_r07_target6_context_image_census_gha_driver_v1.g
search/certs/d972_r07_target6_context_image_census_preflight_v1_20260827.json
sol/luna_reply_174_r07_target6_context_image_census_v1.md
```

Do not modify task172, task169, predecessors, workflows, proofs, CLAIMS or
existing certificates.  No git, push, GHA dispatch or credential access.
The parent Sol session performs those operations.  Do not run the full census
locally.  Local execution is limited to small selftests and static preflight,
serially and only after the parent confirms that task169 is not using Python.

## 3. Authenticated marked input

Pin the final task172 assets and every predecessor imported.  Reconstruct:

- typed `E4=Q4 x Pi4[3]` multiplication, inverse, identity and canonical
  154-byte serialization;
- the exact g760 target6 context bindings, registry ids 1,2,3 and their alias
  rows;
- the two marked source generators as ordered triples in `E4^3`; and
- the quotient to the frozen task168 `Delta3` image, whose order is 27 and
  whose 28 Schreier words/digest are pinned.

Require direct evaluation of `x`, `y`, their inverses, a noncommuting product
and at least 20 pinned task172 conjugator/relation words in both the task172
context evaluator and this fresh triple evaluator.

## 4. Deterministic direct census

Implement positive-generator BFS of the subgroup of `E4^3` generated by the
two marked triples.  State identity is the concatenation of the three exact
154-byte blobs; hash equality alone is forbidden.  Retain compact parent and
letter data sufficient to recover a freely reduced source section for any
state during the run.

Use registered caps and a soft deadline.  The default full GHA state cap is
2,000,000; the cap is preregistered, not silently widened.  If BFS closes,
record:

```text
order_Delta_E
canonical sorted state-set digest
BFS discovery-order digest
transition digest for x,y,x^-1,y^-1
maximum/minimum/mean section length and section-parent digest
```

If the cap or deadline is reached before closure, return an exact resource
terminal with seen/frontier counts and digests.  A bounded miss is not an
order statement.

## 5. Projection and fibre census

When the full image closes, compute for each of the three coordinate maps
`pi_i:Delta_E->E4`:

```text
image order
kernel order
uniform fibre size check
canonical image digest
canonical kernel digest
lex-first nonidentity kernel section when the kernel is nontrivial
```

Also compute all three pair-projection image orders and kernels, the equality
pattern among coordinate maps, and the order/digest of the common kernel.
Require Lagrange identities and literal multiplication closure.

Project every state to task168 `Delta3`.  Require surjectivity onto all 27
states, exact marked-generator agreement and uniform fibre size.  This is a
typed quotient check, not an inference from divisibility of orders.

Report the raw direct-stream column count

\[
 6441\,|\Delta_E|
\]

with overflow-safe arithmetic, but do not infer runtime from it.  Report
projection-kernel sizes as the decisive input for choosing direct streaming
versus the v118 fibre-correlation algorithm.

## 6. Independent checker

The checker imports neither the new producer nor its helpers.  It may import
pinned authenticated predecessor APIs under fresh names.  For a COMPLETE
receipt it must independently enumerate the same subgroup using a different
generator order, compare the exact sorted state set and all projection/kernel
sets, verify full four-letter closure, replay a deterministic sample of at
least 1,000 parent sections (or all states when smaller), and reconstruct the
Delta3 quotient.

For an UNKNOWN_RESOURCE receipt it must reproduce the cap/deadline contract
and validate only the published bounded prefix; it may not promote the prefix
to a subgroup order.

Execute real negative mutations of generator order binding, one triple
coordinate, one E4 blob byte, one transition target, one parent letter, every
projection id, one kernel membership, the Delta3 quotient row, counts/digests
and every forbidden positive terminal.

## 7. Terminals and claim boundary

Use exactly:

```text
R07_TARGET6_CONTEXT_IMAGE_CENSUS_COMPLETE
R07_TARGET6_CONTEXT_IMAGE_CENSUS_UNKNOWN_RESOURCE
R07_TARGET6_CONTEXT_IMAGE_CENSUS_INPUT_STOP
```

`COMPLETE` means only that the finite simultaneous context group and its
projection fibres were independently cross-checked.  It does not mean that
one correction column is active or that target6 is soluble.

Every receipt states:

```text
full_D2_correlation_run = false
full_correction_orbit_correlation_run = false
target6_solved = false
all_seven_solved = false
cofinal_compatibility_proved = false
fake = false
Ihara_witness = false
```

## 8. Selftest and GHA driver

Use exhaustive small fixtures with:

- a nonabelian linked image strictly smaller than the direct product;
- unequal coordinate kernels;
- a nontrivial common kernel;
- a cap-triggered honest UNKNOWN; and
- a deliberately corrupted parent section.

The ASCII-only GAP driver runs producer then checker serially, uses
`pipefail`, validates exactly one terminal, and uploads only bounded receipts,
logs and hashes.  Budget the full producer and checker so that GAP setup and
artifact upload remain inside the existing 360-minute workflow limit.  Do not
request optional p-quotient packages.

## 9. Report

Report exact commands, pins, bytes/SHA-256, selftest results, registered caps,
GHA preamble and all UNKNOWNs.  Repeat verbatim:

```text
simultaneous context image census only
linked Delta_E is not replaced by E4^3
complete fibres enable but do not execute v118 correlation
no target6 solution / cofinal lift / fake / Ihara witness declared
```
