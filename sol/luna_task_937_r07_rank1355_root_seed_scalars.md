# Task937/938 — execute the adopted rank1355 root-seed continuation

Root adopts the complete Task936 intake without widening its scope.
Read FULL task936 and reply936, including every new parent receipt and the
stale-head/old-q pitfalls. Mathematics and sole git/GHA broker stay at root.

## 1. File ownership

Task929 worker implements ONLY
`search/d972_r07_rank1355_root_seed_scalars_v1.py` and reports to
`sol/luna_reply_937_r07_rank1355_root_seed_scalars.md`.

Task930 worker independently implements ONLY
`search/check_d972_r07_rank1355_root_seed_scalars_v1.py`,
`.github/workflows/d972-r07-rank1355-root-seed-scalars-v1.yml`, and reports to
`sol/luna_reply_938_r07_rank1355_root_seed_checker_workflow.md`.
Coordinate the small output ABI directly. Neither may import the other's
new arithmetic; reuse each distinct pinned v2/v15 lineage as reply936 states.

## 2. Exact implementation

Authenticate the single new rank1355 delta plus the immutable old physical
state; do not rebuild or copy the state/history. Stream-check new lambda
against1354 old physical rows and the new normalized pivot, and check its
pairing1 with the accepted saved target remainder. The delta checker binds
that remainder to rho2; no new rho2 staging/old target solve is needed.

Recompute four actual B-adjoint roots from NEW lambda. Read the P1 cache
once for all four fresh8059-entry contractions, then fold only all44 root
seeds through the five Task554 bodies, one body resident at a time. Preserve
the raw seed minus COMPLETE P1 formula v541. No old q/seed2 assertions,
fixed active-character pattern, old head fallback, actor lower-dot pass,
actor-origin scan, or orbit scan may remain reachable.

Emit176 root scalars in character-major/seed0--43 order. The first nonzero
is ROOT_SEED_VIOLATION with precise provenance; all zero is ROOT_SEEDS_ZERO
only, never a full-grade EOF/nonexistence claim. Actor/orbit execution counts
are zero. The source-word/lower-zero materializer is a later step for the
next selected seed; it is not silently supplied by this scalar computation.

## 3. GHA and handoff discipline

Use the exact CLI/file scope in reply936 F5 and unchanged parent tuples.
Expose buffered phase/count progress. Keep only a few bounded synthetic
canaries necessary for the changed new-head/dynamic-root/seed-only scope;
do not add historical tests or a new generic framework. Producer/checker
and tests run serially on GHA; no local Python/GAP, credentials, network,
git or dispatch by workers. Root reviews the small actual-path changes
and launches once after source freeze. Do not run buffered seed30 v2: its
successful materialization is already retained, not a prerequisite to repeat.

No A0/grade2/COMMON/fake/cofinal/Ihara claim is authorized. Keep the old
derivation and shared-lineage qualifications, cross_checked=false,
verified=false. Send root the exact frozen hashes, CLI and any necessary
execution blocker only. Optional cleanup does not hold this run.
