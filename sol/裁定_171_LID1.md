# 裁定 171 — judge v1.2 検収+FINDING LID-1(窓同一性)(2026-07-29)

## FINDING LID-1(最優先・回帰装置の獲物)
LowIndexNormalSubgroupsSearch は**同一セッション内でも列挙順が非決定**(指数 162 の 6 本で 3 回 3 様を実機確認)。よって window_id「idx-s出現順」は**実行を跨いだ同一性を持たない** — v2 の OPEN リストを v3 が、v3 の 17 窓を v4 が **id で照合した継ぎ目は不健全**(v2 で OPEN の部分群が v3 で未検査のまま残った可能性 = 帯結論に被覆穴の可能性)。idx162-s2 の食い違い(2 vs 6)はこの現象の実例と見られる(settled 却下 0 で KJ-1 とは独立)。

## 処方
1. **窓の同一性は「N の生成元語(a,b の語)」で持つ**: 証明書に生成元語を格納し、後続実行は語から同一部分群を再構成(列挙順に依存しない канonical ID)。
2. **miner v5 = 一発走り**: 単一 GAP プロセスで一度だけ列挙し、同一列挙の上で Hol 篩+(F2) kerchi+settled+judge を全部実施 — 帯結論の被覆穴を構造的に排除(帯は秒スケールなので安価)。従来の窓別記録は「run-local」に降格。
3. **KJ-1 の横展開**: 旧 EnumerateReducedHexagon(c∈N 商内)にも settled 抜けの疑い(idx126-s2 で 12 vs 6・crosscheck=false)→ v5 では c∈N 側も settled 込みの経路で統一。

## judge v1.2 検収
- settled 証明書欄・N5cong 改名・T-A universal 化+chi_surjective_assert 分離 = 受理。「universal 形は恒等式化し検査力は全射 assert へ移った」の申告 = 正しい設計理解として承認。
- **idx126-s2 = χ̃ 非全射の生きた実例**(像自明 vs φ(6) = 2)— Sol F79-2.1 の型警告の即日実証として記帳。
- **W-C-N5cong: 40/40 settled 達成** — Sol F79-5.5 の格上げ条件を充足。**GTSh(N₅^cong, N₅^cong) ≅ C₂×Aff(𝔽₅)(metabelian)を isotropy 群の同定として採用**(単系統 GAP+Sol の紙上同値 (F2)⟺Prop3.4 による支持・独立レーンは将来の P79-C で)。地図 delta: 形式型 → 同定へ復帰。
