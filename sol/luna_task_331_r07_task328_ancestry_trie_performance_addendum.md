# Luna task 331 - task328 ancestry-trie performance addendum

Role: Luna, bounded implementation addendum to task328.  Read this complete
mail and task328 first to last.  Do not run Python, Node, GAP, GHA, workflows,
git, or network.  Create only task328's five named v3 outputs; this addendum
does not authorize any sixth output.

## 1. New accepted prerequisites

Read in full:

- `sol/sol_reply_327_r07_task198_authority_bundle_v2_audit_v1.md`;
- `sol/sol_task_330_r07_task327_authority_normalization_erratum.txt`;
- `sol/sol_reply_330_r07_task327_authority_normalization_erratum_v1.md`; and
- `sol/proof_r07_authority_ancestry_trie_evaluator_v268.md`.

Task330 is `PASS / UNEXECUTED`, so task328's authority-v2 prerequisite is
satisfied and A4 input authority is 1/3.  This does not authorize execution
or a positive A4 claim.

## 2. Superseded hot path

V268 supersedes only task328's requirement to perform 6,441 separate long-word
group scans.  Preserve every authority, coefficient, boundary, word-DAG,
v247, mutation, resource, driver, and independent-checker gate in task328.

After parsing and authenticating the receipt once, replay every row's literal
ancestry formula (v268 (1.2)--(1.4)) against its stored reduced word.  Then:

- the producer deduplicates the 243 section words, 26 record words, and 19 Q0
  relators and evaluates them by one forward prefix trie;
- the checker reconstructs that corpus independently and evaluates it by one
  reverse suffix trie with right-to-left recursion; and
- both assemble and bridge all 6,441 rows and compare every typed row.

Pin and enforce the authority-bound inventory: 288 primitive words, 114,458
literal letters, 15,970 producer prefix edges, 26,136 checker suffix edges,
and 5,475,488 stored row-word letters.  A mismatch is fatal, not UNKNOWN.
Do not import a trie helper from the other side.

## 3. Required independence and direct replays

Use opposite trie orientations and opposite association orders.  Neither side
may trust a producer row value, rank, digest-only summary, or Boolean replay.
Both must validate all 6,441 ancestry equalities and typed outputs.  Directly
evaluate a bounded fixed cross-layer sample by the original full words as a
canary.

Every newly materialized K basis word, translated word, boundary-difference
word, noncommuting depth-two canary, and v247 anchor remains subject to direct
actual local-group evaluation.  V268 accelerates only the authenticated
initial presentation roster.

## 4. Performance and report amendments

Charge trie nodes, edges, terminal values, row assemblies, bridge conversions,
and all direct canaries.  The baseline initial-row bounds are at most 28,937
ten-context tuple multiplications for the producer and 39,103 for the checker,
plus cached inverses.  No 6,441-row independent long-word loop, duplicate
primitive trie, recursive trie re-expansion, or second authority parse is
allowed.

Add owner-specific SELFTEST mutations for at least: inconsistent section word
for one state, altered primitive terminal, wrong trie edge orientation,
wrong action orientation, wrong target inverse, and a producer/checker row
mismatch.  These may extend task328's twenty mutations; they may not replace
any commissioned owner.

The task328 reply must state both trie inventories, exact observed operation
counts, direct-canary roster, and that A4 remains at most 1/3 before actual
execution.

`TASK331_R07_TASK328_ANCESTRY_TRIE_PERFORMANCE_ADDENDUM`
