# Task995 — 固定lambda cycle batch独立C v1（992凍結後）

役: 既存packet_checker。992を完成・凍結してから実行する。
変更可は新 `search/check_d972_r07_fixed_lambda_cycle_batch_v1.py` と
`sol/luna_reply_995_r07_fixed_lambda_cycle_batch_checker_v1.md` のみ。旧source/返信/WFは不変。
ローカルPython/import/AST/数値/GAP/network/git/credentials/新agent禁止。新P source/994返信は読まない。

root著作のTask994 C1–C10を**P/C共通の数学・wire契約**として全文読む。988/990と競合する命名・保存・capは共通契約を採る。
prefix=d972.r07.fixed-lambda-cycle-batch.v1、一つの固定lambda/32候補/一batch、実初回親33990567016/1の64段。
P側の実装アルゴリズムは読まず、自系C/O/E/Lの凍結helperを保持し新selector/physical reduction/serializer/loaderを著述する。
旧one_physical_rowの旧lambda残差非零gateと各行Separatorを新候補へ転用しない。

独立CのCLIは共通元14親＋continuation-root/acceptance、--candidate-root P_OUTPUT、--output C_REPORT、
--max-seconds/--max-memory-mib、--producer-max-seconds/--producer-max-memory-mib、--selftest。
実本走初回C10800秒/7168MiB、P宣言5400/7168を受付・P owner/invocationへ結ぶ。型は有限正数/ordinary整数を明示。
全旧n親bytes/成功C/sourceとthin row providerを認証し、新selection lambdaの全旧row/targetのdotは今回直接読む。
新committed全候補をraw/source/P1/全B/実消去/依存/target数値負号とcorrection語正号まで全数比較。
公開physical HEADはfinalizer完成時だけ。途中はprogress/HEADの別型、candidate=false/partial=trueを保つ。
全処理後新lambdaの全row/両targetを直接dot、rho2はDERIVED。Linear tailと全零oracleのcurrent意味を保つ。

全JSON/array比較のexact ABIはroot共通契約と、その後にrootが配達する公開ABI追補だけから受け取る。
相手のsource/994返信を読んで合わせない。分岐やkey未定なら必要なfieldsの具体案だけrootへ送る。
未完成P outputは勝手に補わない。自分の三群canaryは990/共通C10をproduction helperへ接続しGHA未実行で記帳。
全TCB/新source/依存/rawのbytes/SHA・CLI/canary namesと射程、未実行を報告。最終行 `AUDIT_995_VERDICT:`。
