# Task960 — v548 complete scalar oracleの独立checkerとworkflow

役: Luna / 独立実装。Task959とTask957/reply957を全文読む。
変更可は `search/check_d972_r07_section_cochain_oracle_v1.py`、
`.github/workflows/d972-r07-section-cochain-oracle-v1.yml`、
`sol/luna_reply_960_r07_section_cochain_oracle_checker.md` の3本だけ。
ローカル数値/Python import/AST/GAP/network/credential/git/dispatch/追加agent禁止。
rootがGHAとgitの単一broker。自系checkerだけをimportし、producer算術を読まない。

現在run33967668257/1/source fd04734d20d472e7c09f31de3f92f8a50d6d841aが
実resume中。最終parent tuple/entry pinはrootが後送する。未観測値を予測しない。

Task959の数学上のA–Dを全て独立再計算する。共有可は公開CLI/JSON/array ABI、
正本の式、入力pinだけ。未完成のreply957は完成後全文読む。

- Q2全54432頂点/108864正edge、全六tag vertex map/edge整合、tree、五carry。
  graphはRIGHT product。tagged Fox prefixにはLEFT productを用いる。
- 現lambdaと全4 roots/8059 contraction、二段kappa、全8059最終等式。
  Task554の元leadを埋め込み、降順に解く。row IDの逆順にしない。
- 全source d0/d1/d2とshared etaのpullback、全score/f/b_aux配列。
  nonclosed raw edgeをclosed-word `_checker_seed_qnorm` に渡さない。
- 全54433 chordのtau/値/fit/residualと両aux。全部を再計算しbyte比較する。
  最初の違反だけで照合を打ち切らない。false EOF/partial→zeroを拒否する。
- 最初のaux、または高々6cycleのwitnessを自分の全配列から再導出し、tau零と
  scalar非零を照合する。witnessはmaterialization pending、新pivotを作らない。
- 完全零は保持source/Conn前提とv548の下のCOMPLETE_ZERO_CANDIDATE。
  actual current-target dotと明示DERIVED原rho2を混同しない。

新計算の独立性を明記する。finite group-coefficient/moment route等の異なる
source評価法を実raw-edge経路に接続し、同じbinomial helperをコピーする形に
しない。accepted group/index premiseは列記する。三系統全算術とは称さない。

workflowは現在のfull-origin v1の11 parent tuple/依存pinに、新refinement parent
tupleを加えた12親。新/旧source/data/runtimeを固定、ASTはGHAのみ。
marker `[r07-section-cochain-oracle-v1-run]`、branchは現作業branch。
actual-parent metadata両側と少数の変更interface canary→producer一回→checker。
cap1/resumeや旧成功suiteはこの単一snapshot oracleには不要。
各内部1800秒/外40分、job100分を初期運用枠とし、resourceはUNKNOWNのみ。
checker PASS後candidate upload、diagnostics always。最終親pin確定前はfreezeしない。

Task959が公開ABIを固めるまでは独立数学coreとworkflow骨格を進めてよい。
controllerやrootの作業と衝突しない。自分の指定3ファイル以外は変更しない。
新数値/cross-checked/verified/grade2 MEMBER/fullA0は未宣言。
最終byte/SHA、TCBと共通前提、新canary範囲、未実行gateを返信する。
