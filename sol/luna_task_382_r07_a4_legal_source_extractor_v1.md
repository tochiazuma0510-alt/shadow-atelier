# Luna task 382 - A4 legal relative-source extractor v1

Commissioner: Sol / 2026-08-30

Reply to `sol/luna_reply_382_r07_a4_legal_source_extractor_v1.md`.

Role: bounded mechanical implementation only.  Parent Sol owns mathematics,
git, network, GHA dispatch and promotion.  This commission supersedes the
stopped task381.  Read these files in full before editing:

```text
sol/proof_r07_a4_legal_actual_image_leading_gate_v359.md
sol/proof_r07_superperfect_residual_coinvariant_collapse_v151.md
sol/proof_r07_task176_joint_formation_residual_v149.md
sol/proof_r07_canonical_residual_action_materialization_v360.md
sol/proof_r07_word_independent_successor_and_direct_pair_compiler_v231.md
search/d972_r07_word_independent_successor_kernel_v12.py
crosscheck/check_d972_r07_word_independent_successor_kernel_v14.py
```

Inspect the frozen task157ee/task176/task198 inputs only as needed to locate
an already-authenticated word-bearing action of the canonical residual
`tilde-S`.  Do not edit them.

Edit only these four new delivery files:

```text
search/d972_r07_a4_legal_source_extractor_v1.py
crosscheck/check_d972_r07_a4_legal_source_extractor_v1.py
search/d972_r07_a4_legal_source_extractor_gha_driver_v1.g
sol/luna_reply_382_r07_a4_legal_source_extractor_v1.md
```

Do not edit a predecessor, workflow, proof, v220, ledger, cert, fixture or
input.  Do not run production, GHA, git, network, SELFTEST or a mutation
campaign.  Static Python compile, ASCII scan, frozen-owner restoration and
GAP `ReadAsFunction` parsing are allowed.  Keep the implementation small and
production-first; no general framework or audit-only hot path.

## 1. Exact input and claim boundary

The future production input is one physical positive A4 v12 receipt and its
accepted independent v14 verdict.  Authenticate exact authority identities,
all three A4 milestones, terminal/queue exhaustion, complete ordered
`kernel.K_roster`, word ancestry, the four marked action matrices/inverse
laws and the typed task176/task198 roof square.  Never accept SELFTEST,
synthetic data or a same-schema object.

The only mathematical output in v1 is

```text
C_rel = R_S(Delta1) intersect K = [R_S(Delta0), K].
```

Do not output the full `K`, `I_K`, a raw occurrence image, `A/JA`, `L/JL`, a
lift or a witness under the name `C_rel`.  Do not assume `C_rel=K`.

For the authenticated task176 roof, use v149's canonical
`R_S(Delta0)=tilde-S` and v151's formula.  The producer must locate one of
the following physical inputs:

1. authenticated source words generating `tilde-S`, evaluable in the A4
   marked `x,y` action; or
2. an equivalently authenticated complete action description whose image is
   proved to be exactly the canonical residual action.

A PSL projection, two convenient pure-factor words, a roof label, an order
504 expectation or a stable-derived prose claim is not by itself such an
input.  V360 supplies the missing bounded compiler: find the two inner
Gamma corrections by complete 243-state scans, then enumerate the 729
central pairs and retain the unique pair satisfying all five pinned complete
PSL(2,8) presentation relators.  Authenticate every owner used by that
compiler and replay its final values; do not replace any of these gates by
projection or order alone.  If the current owners lack a physical field
needed to execute v360, return

```text
UNKNOWN_INPUT:WORD_BEARING_CANONICAL_RESIDUAL_ACTION_NOT_AUTHENTICATED
```

and record the exact missing physical field/file.  Do not reconstruct a
large residual group or invent a complement inside this task.

## 2. Finite source extraction

If residual generator words `s_1,...,s_d` are authenticated, compose their
actions from the A4 marked matrices, checking inverse and word-order
conventions directly.  Form the block family

```text
(S_a - identity) e_i
```

for every residual generator and ordered A4 basis vector.  Exact sparse
row reduction over `F3` returns a basis of

```text
C_rel = sum_a image(S_a - identity).
```

Retain for every rank-raising row:

- its full vector in ordered A4 `K` coordinates;
- ancestry in pairs `(residual_generator_id, K_basis_id, coefficient)`;
- the literal commutator source word `[s_a,u_i]`, with the frozen convention;
- direct fine value and coarse identity replay;
- exact integer exponent sums `(0,0)`; and
- the change-of-basis/echelon pivots proving independence and span.

The receipt must distinguish two statements:

```text
literal commutator ancestry = an actual roof-trivial common word;
formation ancestry = existence of a Pi_S preimage for the same finite value
                     by v37, not identity with that literal word.
```

No extra side-gate intersection is part of v1.  Export a stable ABI on which
a later consumer can intersect physically registered homogeneous gates.

Return exactly one producer terminal:

```text
A4_LEGAL_RELATIVE_SOURCE_COMPLETE
UNKNOWN_INPUT
UNKNOWN_RESOURCE
```

Use a compact atomic checkpoint only if a genuine finite row reduction can
hit the registered cap.  Do not add checkpoint machinery to a static
UNKNOWN_INPUT path.

## 3. Independent checker

The checker must not import the new producer.  It may import frozen owners
only after authenticating exact byte/SHA pins.  Independently reconstruct:

1. the A4 receipt/verdict authority and typed roof square;
2. the canonical residual generator/action authority;
3. every residual word action from marked generators, using an independent
   word parser/composition route;
4. the block span in a different pivot and generator order;
5. two-way span equality, every producer ancestry and every pivot;
6. each literal commutator's coarse identity, fine K-coordinate and integer
   exponent sums; and
7. the equality with the directly generated commutator-action submodule.

An independent basis need not be literally equal; compare spans both ways.
On UNKNOWN, require the same exact missing authority after an independent
inventory.  Print one terminal line and accept only a fully replayed
mathematical terminal or an honestly typed UNKNOWN.

## 4. Driver and delivery

The ASCII GAP driver must expose producer/checker progress, preserve receipt,
verdict and any checkpoint in `ci/out`, reject stale output, and require one
terminal line from each process.  Leave future A4 receipt/verdict pins
explicit and undispatched until the physical positive artifact exists.

The reply must list every inspected candidate residual-action owner and say
why it is sufficient or insufficient, all exact source pins, static commands,
resource caps and terminal vocabulary.  End with:

```text
A4 AMBIENT K OWNER:                              FUTURE POSITIVE INPUT
CANONICAL RESIDUAL ACTION AUTHORITY:             FOUND OR EXACTLY BLOCKED
C_rel = [R_S(Delta0),K] EXTRACTOR:               IMPLEMENTED OR EXACTLY BLOCKED
OCCURRENCE IMAGE / A/JA / L/JL:                  NOT CLAIMED
COMPATIBLE LIFT / FAKE / IHARA WITNESS:          NOT DECLARED
```

`TASK382_A4_LEGAL_RELATIVE_SOURCE_EXTRACTOR_COMMISSIONED`
