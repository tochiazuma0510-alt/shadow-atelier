# Task1122 — P current reader と算術asset用途の閉包を具体化

宛先: 既存Luna / packet_producer。Solは数学裁定と独立静読を担当する。返信は `sol/luna_reply_1122_r07_P_current_reader_and_asset_use_closure_v1.md`、最終行 `TASK1122_VERDICT:`。新材料は `%TEMP%/shadow-atelier-audit163/task1122/`。

現行v6の最終bindingを最優先する。rootから正式inventory5が届いたら、この追加設計を新しい版で保存して既指定の最終bindingへ戻る。本便はv6追加gateではない。新agent、Git/push/GHA、network/credential、codex exec、既process操作、親の変更・全artifact再走査は禁止。Python/GAP/Lean/source/import/AST/compile/selftest/数学実行は行わない。PS/.NETの原文・JSON metadata・全file pin/指定raw範囲の読取りだけを使う。旧source・指示/返信・TEMP票を上書きしない。repoへの変更は本便の指定返信のみ、新材料は指定TEMPの新規名。末行のVERDICTを守り、全材料のbytes/SHAと完全目録、読了/未読/OPENを明示する。

正式出発点は run34161493396/1、head a5b456a973f8a917f3af386d327061a02a0cf900、1834/8539 cross-checked limited7、verified=false。未来の1962/採用数/λ/停止枝を仮定しない。source実装の不具合と、将来cold設計の未閉鎖義務を混同しない。

## 固定した前件と今回の宇宙

1116 F2–F7の16 caller単位・最初の未列挙asset境界を出発点とし、自系1113/1116/1118/1120の凍結原文・入力登録を引き継ぐ。P6は453749 B / 75401d4ded04e00187e9a45bfe87603309473a7c6625e6747ecf5c3bf4caa0d7のまま。1120の限定登録はrootで採択済み（原文書/owner/出現票123780 B / 53d0f2fcd47f214ca277ee8a8b039d281257e27a191359513c8852798d91a1c6、final delivery票32083 B / 8033e4c07342e5d05993251b4e2c0364fd20f8e744e1904d0c46326e46536e96）。このroot票は自系確認に読んでよい。公開1115 proposed.v2＋scope v3を使う。

先に入力を事前登録する。追加sourceが必要なら、登録済sourceの具体call/import/attribute行が指す既存repo sourceに限り、読む前に別追加登録とwhole pinを作る。動的に定まらないmodule/file/dispatch先はOPENで止め、任意に宇宙を絞らない。新artifact・8059全stream・packed/vector payloadへは広げない。C6/1114/1117/1119/1121のprivate source・返値票・実装案を読まない。相手source pinはopaque descriptorに留める。

## 具体作業

1. 1116の16 caller単位を落とさず、ordinary current launchでのcaller → 実binding/alias → callee → 返値/副作用 → 次consumerを表にする。callsiteの引数の原点、分岐条件、返値全shape/選択key/配列順/実file locator、source全文pinと定義/代入/callerのraw範囲を付ける。行検索で候補を発見しても、全callerを閉じた証拠にはせず候補関数本文を読む。
2. old14 boot/old64 snapshot_store/attach_stepの未列挙境界について、1118/1120で閉じたnative body/owner/初期値と、まだ残る内部asset/返値用途を分ける。FixedBundleの実16 payload＋manifest、Task554の8segment/12blob、P1/maps/辞書/三raw、source/runtimeを別roleとする。e.primal_section、e.corrected_source、l.current_section_cached、e.four_B、module.own_dependenciesを、実定義/実caller/返値の次用途又は最初の解けない動的境界まで進める。数値データを実際に読む・算術を行う必要はない。
3. 返値keyが未使用でも、そのobjectがserialize/hash/全比較へ渡ればそのbyte/値は用途を持つ。返値以外のmutation・closure・例外/早期停止・入力妥当性guardも記録する。通常currentと歴史cited候補が共用するhelper全体をcoldとしない。current actual inputは年齢や由来より優先する。
4. 原fixed query1450/1578/1706、current1834の全行/二target、現候補の係数/語/修正/新λは別の呼出しである。古い3queryの数値演算が引用候補でも、その返値/制御/現在のasset用途は別に閉じる。元97（32五key＋65六key）と後続384十key、旧8059/原multi-rowと後続one-row、採用0層/部分枝を保つ。
5. source/asset依存を辺の種類付きでまとめ、入力集合の上界としての保守閉包と、実に不要と証明された集合を区別する。未解決動的alias、分岐、返値escape、Γ/P1/maps境界は固有IDでOPEN/ACTIVE_KEEP。行数/関数数の全射だけで最小TCBやhidden input不在を証明しない。
6. 公開consumerへの返しは定義/必要入力role/三分法の短い数学契約とし、自系private caller表を別材料に保つ。rawを省けるという採択、converter/CoreManifest/B/TCB/速度の採択はしない。Aの全COLD EOFとBの既全復元＋個別immutable TCB＋現在存在/長さを維持する。

成果は全16単位の追跡結果、source/asset辺、次用途の原文根拠、具体OPEN表、旧表からの全delta、作者照合票と目録。各閉鎖は「この固定source・ordinary mode・列挙条件下のbounded source-static用途」までとする。閉じない行も成果として正確に返す。
