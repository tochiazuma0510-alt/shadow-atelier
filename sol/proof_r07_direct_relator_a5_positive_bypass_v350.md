# R07 direct-relator A5 positive bypass (v350)

Author: Sol / 2026-08-29

Status: paper theorem replacing the A4-basis dependency of the positive
A5/A6 route by the authenticated 6,441 defining relators themselves.  The
full A4 quotient basis is still required for the A4 milestone and remains a
useful reusable object, but it is not logically required to construct one
positive pointed multiplier.  A single fused raw-chain membership may carry
the presentation-boundary slack and the relator ancestry at once.  The
theorem does not assert that the actual target is MEMBER and declares no
compatible lift, fake certificate or Ihara witness.  `verified=false`.

## 1. The complete but redundant successor generators

Let

\[
 F=F(x,y)\mathrel{\mathop{\twoheadrightarrow}^{\rho _0}}\Delta _0
\tag{1.1}
\]

be the accepted task198 roof presentation, and let

\[
 R=\ker\rho _0
  =\langle\!\langle r_1,\ldots,r_m\rangle\!\rangle_F,
 \qquad m=6441,
\tag{1.2}
\]

where every `r_j` is retained as a literal signed source word with its
task198 ancestry.  At the first relative Frattini edge write

\[
 F\mathrel{\mathop{\longrightarrow}^{\rho _1}}\Delta _1
 \twoheadrightarrow\Delta _0,
 \qquad K=\ker(\Delta _1\to\Delta _0).
\tag{1.3}
\]

The group `K` is elementary abelian over `k=F3`.  Put

\[
 b_j=\rho _1(r_j)\in K.
\tag{1.4}
\]

V231 proves the complete generation statement

\[
 \boxed{K=k[\Delta _0]\langle b_1,\ldots,b_m\rangle.}
\tag{1.5}
\]

No independence assertion is made in (1.5).  In particular, computing an
echelon basis of the `b_j` modulo all translated PB boundaries is a
compression step, not part of the generation theorem.

Let

\[
 A=k[\Delta _1],
 \qquad I=\ker(A\to k[\Delta _0]).
\tag{1.6}
\]

### Lemma 1.1 (DEFINING RELATORS GENERATE THE RELATIVE IDEAL)

One has

\[
 \boxed{I=\sum_{j=1}^{m}A(b_j-1).}
\tag{1.7}
\]

#### Proof

The kernel of a group-algebra quotient is the two-sided ideal generated by
`k-1`, `k in K`.  Formula (1.5) says that `K`, as a normal elementary
abelian subgroup, is generated by the marked conjugates of the `b_j`.
The identities

\[
 uv-1=(u-1)+u(v-1),
 \qquad
 u^{-1}-1=-u^{-1}(u-1)
\tag{1.8}
\]

express `k-1` for every product of those generators as a sum of left
translates of their differences.  A right multiple is also a left multiple
of a conjugate, because

\[
 (b_j-1)g=g(g^{-1}b_jg-1).
\tag{1.9}
\]

The conjugate in (1.9) is again generated by the marked orbit in (1.5), so
the left side of (1.7) is already the whole two-sided kernel.  The reverse
containment is immediate. \(\square\)

At source-word level a primitive term of (1.7) is the literal roof-fibre
pair

\[
 \boxed{g(b_j-1)\longleftarrow (w r_j)-w,}
 \qquad \rho _0(wr_j)=\rho _0(w),
\tag{1.10}
\]

where `w` is any retained source word for the prefix `g`.  Thus the
redundant roster has better ancestry than an abstract quotient basis: every
primitive column already comes with its two source words.

## 2. The A5 image does not require an A4 basis

Keep v348's full pre-block action map

\[
 \widehat\Psi:I\longrightarrow V\oplus W,
 \qquad
 \widehat\Psi(\theta)
   =(\theta d_1,\theta\mathbin\odot w),
\tag{2.1}
\]

and the post-closure map

\[
 T(a,c)=(a,Cc).
\tag{2.2}
\]

Here `d1` and `e1` are the actual task193 rows with the pointed signs fixed
by v239, and the eleven occurrences in `w` are kept separate until `C` is
applied.  Both coordinates of (2.1) are `A`-linear before `C`.

For every defining relator form the primitive pre-`C` column

\[
 \widehat q_j=\widehat\Psi(b_j-1).
\tag{2.3}
\]

### Theorem 2.1 (DIRECT-RELATOR COMPLETE A5 IMAGE)

The marked invariant closure of the `m=6441` columns (2.3) is exactly

\[
 \boxed{
  \operatorname{span}_k\{g\widehat q_j:
       g\in\Delta _1,\ 1\le j\le m\}
       =\operatorname{im}\widehat\Psi.}
\tag{2.4}
\]

Consequently the zero-base A5 condition is equivalent to

\[
 \boxed{
 (e_1,0)\in
 \operatorname{span}_k\{T(g\widehat q_j):g,j\}.}
\tag{2.5}
\]

No A4 echelon basis, A4 independence decisions, or A4 action matrix is a
logical premise of (2.4)--(2.5).

#### Proof

By Lemma 1.1 every `theta in I` is a finite sum

\[
 \theta=\sum_{g,j}a_{g,j}g(b_j-1).
\tag{2.6}
\]

Linearity and equivariance of (2.1) send (2.6) to the right side of (2.4).
Conversely every displayed translate is the image under `widehatPsi` of a
legal element of `I`, proving equality.  Applying v348 Theorem 2.1 to that
same image gives (2.5). \(\square\)

The closure is taken under the marked generators `x^{+/-1},y^{+/-1}` in the
full pre-`C` space.  Rank-raising queue exhaustion reaches every translate
in (2.4) because those marked generators generate `Delta1`.  As in v348, a
positive target reduction may stop early, while a negative statement
requires complete queue exhaustion.

## 3. Fusing the presentation quotient into the one target solve

The machine does not store `b_j` as coordinates in a precomputed successor
group.  Instead it evaluates the literal word `r_j` in the raw affine/Fox
chain module.  Let

\[
 q:\mathcal C\twoheadrightarrow\overline{\mathcal C}
       =\mathcal C/\mathcal B
\tag{3.1}
\]

denote the direct sum of the ten typed PB3/PB4 presentation quotients;
`mathcal B` is generated by the complete 65 translated Fox-boundary
families.  Include the occurrence coordinate and all other quotient
coordinates in `mathcal C` and `mathcal B` as dictated by their actual chain
maps.  The group action preserves `mathcal B`.

Choose raw representatives `qtilde_j` of (2.3) and a raw representative
`ttilde` of the target `(e1,0)`.  For any finite action ancestry `a`,

\[
 q(\widetilde t)=q\left(\sum a_{g,j}g\widetilde q_j\right)
\tag{3.2}
\]

if and only if

\[
 \boxed{
 \widetilde t-sum a_{g,j}g\widetilde q_j\in\mathcal B.}
\tag{3.3}
\]

### Theorem 3.1 (ONE FUSED RAW-CHAIN MEMBER CERTIFICATE)

Maintain one ancestry-bearing echelon generated by

1. the marked action closure of the raw relator columns
   `g*qtilde_j`, tagged as coefficient columns; and
2. the complete translated presentation-boundary columns, tagged as
   equality slack.

If `ttilde` reduces to zero, its coefficients give simultaneously:

\[
 \theta=\sum a_{g,j}g(b_j-1)\in I,
 \qquad
 (e_1,0)=T\widehat\Psi(\theta),
\tag{3.4}
\]

and an exact PB-boundary ledger proving the raw equality.  The coefficient
ancestry alone materializes the A6 pair polynomial

\[
 \boxed{
 M=\sum a_{g,j}\bigl((wr_j)-w\bigr).}
\tag{3.5}

Boundary-slack coefficients certify (3.3) and are not inserted into `M`.

#### Proof

Zero reduction is precisely a finite equality of `ttilde` with a sum of
the two registered column types.  Moving the boundary terms to the other
side gives (3.3), hence (3.2).  Theorem 2.1 converts the coefficient part to
the legal `theta` in (3.4), and (1.10) gives (3.5).  Replaying every raw
column, boundary translation and coefficient proves the equality without
assuming independence of either column family. \(\square\)

The boundary family may be supplied lazily by the existing exact
support-inversion oracle.  All discovered boundary columns are shared by
the single joint solve.  A zero correlation is negative content only after
the marked coefficient closure and the complete boundary oracle both
exhaust; before then a miss or resource cap is `UNKNOWN`.

## 4. Why this is a genuine witness-path shortening

The current A4 producer asks 6,441 successive quotient questions.  For each
unresolved row it repeatedly constructs a dual, correlates that dual with
the complete boundary family, inserts one rank-raising boundary column, and
starts the row query again.  This computes a reusable independent basis of
`K`, which is stronger than one positive A5 certificate needs.

The fused route instead performs:

```text
task198 literal relators
  -> raw (r_j-1) pointed/occurrence columns
  -> one shared pre-C action closure plus boundary slack
  -> first joint hit of (e1,0)
  -> literal theta=mu1 and A6 pair polynomial M.
```

It removes all of the following from the positive critical path:

1. deciding the quotient class of every one of the 6,441 relators
   separately;
2. compressing those classes to an independent A4 basis;
3. building and replaying a complete A4 action matrix before A5 starts; and
4. translating A5 ancestry back from that basis to the original relator
   words.

It does **not** remove the exact PB boundary relation from the mathematics.
It moves that relation to the one equality where the witness actually needs
it.  Nor does it prove that the actual target is MEMBER or bound the number
of boundary columns needed before a hit.

## 5. Production and independent-checker contract

A positive direct-relator receipt retains:

1. the accepted task198 presentation owner and all 6,441 literal relators;
2. the actual task193 `d1,e1` rows and the full eleven-occurrence owner;
3. every raw seed used by the final ancestry and its direct word evaluation;
4. every marked parent/action edge used by that ancestry;
5. the exact coefficient list in (3.4)--(3.5);
6. the selected translated PB boundary columns and their raw coefficients;
7. direct replay of the raw equality (3.3), the printed-zero occurrence
   coordinate, the pointed equality and the roof-fibre condition in (1.10);
   and
8. the resulting factored A6 pair polynomial with ordered source words.

The independent checker reconstructs only the selected positive ancestry
and boundary support with its own signed-word/Fox/boundary helpers.  It need
not reconstruct unused A4 quotient pivots or exhaust an unused negative
closure.  Hashes bind the physical owners but never replace the raw sparse
equalities.

For NONMEMBER the checker additionally requires complete pre-`C` action
queue exhaustion, a complete boundary oracle and a dual annihilating both
column families.  The positive bypass must never turn an incomplete miss
into a negative A5 or fake statement.

## 6. Fixed frontier

```text
TASK198 6441 DEFECTS GENERATE K:                    PAPER PROOF (v231)
TASK198 (r_j-1) GENERATE THE FULL RELATIVE IDEAL I: PAPER PROOF
DIRECT-RELATOR CLOSURE = COMPLETE A5 IMAGE:          PAPER PROOF
ONE RAW MEMBER + BOUNDARY LEDGER -> mu1 AND M:       PAPER PROOF
A4 INDEPENDENT BASIS NEEDED FOR POSITIVE A5/A6:      REMOVED
A4 REUSABLE BASIS / A4 MILESTONE:                    STILL COMPUTING
ACTUAL DIRECT-RELATOR JOINT MEMBER:                  NOT COMPUTED
ACTUAL mu1 / M / EXACT THREE ENDPOINTS:              NOT COMPUTED
COMPATIBLE LIFT / FAKE / IHARA WITNESS:              NOT CONSTRUCTED
```

`R07_DIRECT_RELATOR_A5_POSITIVE_BYPASS_V350_PAPER_GRADE`
