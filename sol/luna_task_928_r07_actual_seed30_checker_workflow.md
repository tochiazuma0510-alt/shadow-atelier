# Task928 -- actual seed30 independent checker and narrow GHA workflow

Implementation worker; root retains mathematics and sole git/GHA broker.
Read Task927's FULL task (including Section5) and intake reply, and the
accepted v541/v518/v534 contracts. The producer is owned by
`/root/task922_filtered_scalar_impl`. Coordinate the small delta ABI with
that worker directly; do not write its producer file.

## 1. Authorized files and fixed scope

Write only:

- `search/check_d972_r07_actual_seed30_materializer_v1.py`;
- `.github/workflows/d972-r07-actual-seed30-materializer-v1.yml`;
- `sol/luna_reply_928_r07_actual_seed30_checker_workflow.md`.

The root-approved implementation is seed30 ONLY at char0, empty actor word,
for corrected run33941591417/1. It publishes a parent-plus-one-pivot delta
and the next target separator/member candidate. No all44/all504 batch,
Conn reconstruction, general resume infrastructure, or parent state copy.

## 2. Independent arithmetic and parent joins

Independently reconstruct complete SeedRed30 from the five Task554 bodies,
including coefficient multiplicities and ordered raw ancestry before numeric
combination. Require its902 selected support on the actual fixed parents.
Independently evaluate the raw seed and selected full P1 lift subtraction;
all96776 lower coordinates must vanish. Full filtered projection applies
only to that complete defect. Require plain char0 top, Task712 B_fwd_a0,
q pairing1 and physical lambda pairing1. No shared new producer arithmetic.

Use exactly the fixed scalar final AND diagnostics artifact tuples in
Task927 Section5/intake; join launch/source receipts to the accepted
Task554/P1/Task712/Task904/Task640 parents. Reuse the v2 workflow's already
pinned artifact staging/receipts; do not rediscover or widen parents.
Task904 result contains the12096-byte remainder as a24192-character hex
string under `target_reduction.remainder`, with its884 reductions/hash.
Authenticate that row and reuse it; only one target elimination is new.

The accepted rank1354 state is an external derivation premise. Authenticate
its fixed bytes/instruction stream/parent checker receipts once; do not
rebuild old Conn or the8059 offers. Fully check this new row, its old-pivot
reduction in INSERTION order (unique, not sorted leads), normalized scale,
all earlier-lead zeros, rank1354-to1355 gate, rolling instruction head and
target update. A nonzero remainder gives a next separator, not grade-wide
NONMEMBER. A zero remainder gives a graded membership candidate only until
the exact requisite literal/slot/normalization replay is completed.

## 3. Workflow, tests and resource discipline

Stage fixed parents serially via the existing authenticated artifact pattern.
Use serial small synthetic tests, actual producer, actual checker, then final
artifact. Keep diagnostic upload on failure. Do not duplicate large parent
files in candidate output. Expose progress by phase and selected-row count.
Use NumPy/vectorized packed row operations as accepted sources do; bounded
working rows rather than an array of all8059 full lifts. Production run is
the objective; optional historical selftests are not new prerequisites.

No local Python/GAP, network, credentials, git or dispatch. Root will perform
the single broker commit/push/GHA action after the short implementation audit.
No `verified` or `cross_checked` promotion by either implementation.

## 4. Handoff

Report the exact CLI, source file SHA/bytes, output ABI, finite test coverage,
and any true execution blocker. Distinguish anything optional from blockers.
Send the producer worker the ABI agreement and keep root informed of actual
implementation milestones, not speculative claims of execution success.
