Sol(max) task 380 - A0/v12c bounded complete repair after task376

Role: fresh researcher-authorized bounded implementation repairer. Read every
numbered section first to last before editing. The task376 auditor must not
implement this tranche. Do not execute the candidate. Preserve every task376
PASS finding and repair all seven load-bearing defect groups in one version.
Write only the six designated outputs below; do not edit frozen v12b owners.

1. Binding authority and exact scope

Read task354/reply354, task368 and its full STATIC REJECT, task372/reply372,
task376 and its full STATIC REJECT, and every v12b owner. Read the P0-pinned
v275--v290/task176/raw-checkpoint authorities used by the code. The v12c
scope remains SELFTEST_BOOTSTRAP only. No production/resume, common word,
actual A0 numerator, lift, fake or Ihara claim is permitted.

Retain these task376 PASS results without semantic weakening:

- chronological growing seen_pivots;
- actual rank-2896 current dual, full annihilation and nonzero target/remainder
  pairing;
- real Q0/Gamma/K0 selected search and ordinary 2896 -> 2897 transition;
- checker-side independent reconstruction of all 2896 raw columns, formal
  solution, initial/current dual, selected owner and transported identity;
- one bounded descending AncestryDAG weights/answer implementation;
- exact narrow expected-rejection semantics and false conclusion flags; and
- Linux/POSIX/native-driver/terminal/platform boundary.

2. Sole permitted outputs and one-way graph

Create exactly:

- ci/in/d972_r07_history_free_positive_fast_resume_selftest_bootstrap_v12c.manifest.v1.json
- search/d972_r07_history_free_positive_fast_resume_v12c.py
- crosscheck/check_d972_r07_history_free_positive_fast_resume_v12c.py
- search/d972_r07_history_free_positive_fast_resume_gha_driver_v12c.g
- search/certs/d972_r07_history_free_positive_fast_resume_selftest_v12c_20260829.json
- sol/sol_reply_380_r07_a0_v12c_bounded_complete_repair.md

The authority graph must be strictly

  immutable authorities -> canonical P0/fixture -> producer/checker -> driver.

P0 and fixture must not pin final producer/checker identities. Producer and
checker pin the final P0/fixture identities; only the downstream driver pins
the final physical identities of P0, fixture, producer and checker. Reject
cycles, aliases, retained v12b physical hashes, missing ancestry or partial
digests. Every physical authority path must occur exactly once: collapse the
v12b duplicate `fixture` / `fixture_v12b` rows to one authoritative fixture
record and reject any duplicate-keyed or duplicate-path inventory.

3. F1 - physical canonical P0 and fixture

Freeze P0 and fixture physically as

  canonical JSON (sort_keys, ensure_ascii, separators comma/colon) + one LF.

No BOM, CR, indentation or extra LF is allowed. Compute each root body seal
from the same compact canonical object with only the root self-digest member
removed. Both Python readers and the driver must require exact physical raw
equality to canonical(value)+LF before accepting semantic fields or seals.
Mutate whitespace/order/newline/canonical bytes through the ordinary reader
and require the registered first rejection.

4. F2 - bounded real mutation owners without full clones

Remove every full-frame/full-R canonicalize-parse clone or deepcopy from the
triangular 8, selected 30, boundary 13, positive 7, physical 11, phase 4 and
phase-positive 2 suites. Represent each mutation as an immutable authenticated
baseline plus a bounded owner-local typed delta/view. The same ordinary
validator must consume that real owner and return the exact first reason;
test-only Boolean predicates are forbidden.

Triangular mutations may touch only the required sparse chronological rows,
not serialize/reparse all 2896 columns per case. Selected/boundary/positive/
phase mutations may copy only the smallest extant subowner being changed.
No nested positive route may rerun an unrelated full selected validation.

Supply actual W4 normal, timeout, death, partial and blocked-send owners on
both producer and checker paths. One-child/W2 substitutes do not count.
Producer physical11 must mutate the actual constructed R owner and its real
publication binding, not a positive miniature frame. Preserve all mutation
counts, exact reasons, narrow catches, MutationAccepted hard failure and all
false conclusion flags. The independently compared complete ledger has exactly
75 cases (8+13+30+7+11+4+2); it must include both phase-positive cases and the
measured owner identities, event traces and physical digests, not merely a
fixture-shaped normalization that drops executed fields.

5. F3 - charge before allocation and reuse heavy owners

For pc_cache, reserve/charge a source-derived maximum entry budget before
pc.mul, bytes conversion or insertion allocation; release on duplicate or
failure and enforce the 131072 cap. Do not allocate first and account later.

Construct exactly two full Gamma values per process: the canary and actual
selected value. Ordinary baseline and all mutations must reuse authenticated
immutable values rather than reconstruct Gamma. Construct K0 and the selected
roster once per process and reuse bounded snapshots where semantics permit.
Checker parent/letter digests must stream authenticated compact byte owners;
do not build 1,469,664-element Python integer lists solely for hashing.
Inventory every remaining full Gamma/K0/coordinate pass and justify it.

6. F4 - strict deadline coverage

Install one elapsed-adjusted internal signal deadline before every material
build. Add real deadline/meter checks inside K0 state loops, selected qid/gid/
coordinate loops, raw-column replay, mutation delta construction/validation,
streaming hashes and cleanup. Each potentially long loop must be interruptible;
a check after it is insufficient.

Retain internal/external constants 9600/9900, 5400/5700 and 1200/1500 only if
the source proves a strict 300-second cleanup margin on every route. The driver
sum remains 17100 < 18000 < 21600. Restore prior timers after real elapsed
subtraction and fail closed on signal/platform/cleanup errors.

7. F5 - truthful live-memory and output reservation

Remove unexplained CPython/COW reserve constants as allocation proofs. Use
compact typed arrays/byte owners and bounded sparse maps instead of giant
Python int lists and full DOM clones. Before every allocation, reserve a
source-derived upper bound; cap every container cardinality and element width;
release at the actual last consumer. State simultaneous lifetimes for raw
bytearray, immutable bytes, ASCII fragments, DOMs, arrays, dicts/maps, COW
workers, IPC buffers and output serialization.

Enforce and read back a Linux RLIMIT_AS ceiling consistent with the declared
RSS/address-space contract before material authority construction. The hard
OS ceiling and the explicit payload ledger must be reported separately; it is
not an RSS observation.

Reserve the full 536870912-byte output cap before constructing R or V, then
recheck actual canonical encoded size and release unused reservation only
after publication or rollback. Recompute producer/checker peak formulas after
all clone/heavy-owner removals. A numeric identity without source lifetimes is
not sufficient.

8. F6 - fail-closed durable publication everywhere

Repair producer R, checker V, driver raw extraction, logs and final sentinel.
Prefer a same-bound-dirfd exclusive temp followed by renameat2
RENAME_NOREPLACE, or retain the temp until every post-link identity and
directory-fsync check succeeds. On every typed or untyped failure after the
final name becomes visible:

1. unlink the final name relative to the already-checked directory fd;
2. fsync that directory after rollback;
3. remove/fsync any temp entry; and
4. return nonaccepting even if rollback itself fails.

Successful paths require exact physical identity, file fsync and directory
fsync before an accepting terminal. UNKNOWN, timeout, mismatch, partial log,
raw alias or fsync failure must leave no R, V, accepted log, alias or sentinel.

9. F7 - driver, independence and static closure

The ASCII GAP driver must have no BOM/CR, use native directory creation,
reject stale v12c names before heavy work, pin fixed quoted paths, require
Linux/x86_64 and exact commands, enforce canonical P0/fixture bytes, stream
large R/V rehashes, hold no R+2V combination, and require exact-one full-line
producer/checker terminals. Bind the independent checker to its own raw
2896-column reconstruction; no producer summary is evidence.

Do not run Python, Node, GAP, GHA, workflows, git, network, imports, syntax
compilation, candidate code, mutations, RSS or subprocess tests. Read-only
PowerShell inspection and hashing are allowed. V12c remains UNEXECUTED and
requires a fresh independent static code/performance audit before GHA.

In the designated reply give exact bytes/SHA-256 for all six outputs, both
self seals, canonical physical proofs, line-numbered repairs F1--F7, complete
heavy-pass and simultaneous-memory formulas, and every command class not run.
End exactly with:

A0/V12C VERSIONED OWNERS:               COMPLETE or INCOMPLETE
TASK376 F1 CANONICAL GRAPH:              REPAIRED or OPEN
TASK376 F2 REAL BOUNDED MUTATIONS:       REPAIRED or OPEN
TASK376 F3 HEAVY OWNER REUSE:            REPAIRED or OPEN
TASK376 F4 DEADLINE COVERAGE:            REPAIRED or OPEN
TASK376 F5 LIVE MEMORY / OUTPUT RESERVE: REPAIRED or OPEN
TASK376 F6 DURABLE PUBLICATION:          REPAIRED or OPEN
TASK376 RETAINED PASS CLAUSES:           RETAINED or REGRESSED
CANDIDATE EXECUTION:                     UNEXECUTED
FRESH INDEPENDENT STATIC AUDIT:          REQUIRED
V12C SELFTEST_BOOTSTRAP GHA:             FORBIDDEN
PRODUCTION / RESUME:                     FORBIDDEN
ACTUAL A0 COMMON + CHECKER:              remains 0/1
LIFT / FAKE / IHARA:                     NONE

TASK380_R07_A0_V12C_BOUNDED_COMPLETE_REPAIR
