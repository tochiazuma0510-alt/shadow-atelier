# R07: five explicit legality rows and the exact grade-two deficit (v546)

Author: root / 2026-09-05

Status: paper derivation, submitted for independent audit. It makes the
five rows in v457/v543 explicit and locates the three-direction deficit of
v458. No actual physical target membership is inferred from source ranks.
`verified=false`.

## 1. Fixed groups, coordinate convention and existing premises

Use the marked quotients

```text
F(x,y) -> Delta -> Q0 -> Q2 -> Q1,
Q0 = P x ((Z/9)^3 semidirect A),
Q2 = P x ((Z/3)^3 semidirect A),
Q1 = P x A,             P=PSL(2,8), A=C2^2.
```

These are the decompositions in v439/v442/v458. The A action on the three
rotation coordinates is by the three DISTINCT nontrivial sign characters.
Let `N2=ker(F->Q2)`, `Omega=ker(F->Delta)`, and
`Gamma2=N2/Omega`. The existing finite premise is

```text
Gamma2_ab = C3^3 x C9^2,
dim_F3 Gamma2/Phi(Gamma2) = 5.                          (1.1)
```

Write the dihedral coordinates in ROTATION-LEFT order, independently of an
implementation's section order:

```text
(p,v,e)*(p',v',e') = (pp', v + epsilon(e)*v', e+e').     (1.2)
```

Here epsilon(e) is the diagonal vector of signs. A section-left/kernel-right
encoding `(p,e,k)` has `v=epsilon(e)*k`; this change must be made before an
implementation uses the formulas below. The proof does not mix the two.

Use the left-prefix Fox chains, with positive edge `(q,g):q->q*g`,
`g=x,y`, and boundary `partial(a,b)=a(X-1)+b(Y-1)`. Put

```text
R=F3[Q2], I=augmentation ideal of F3[V], V=(Z/3)^3,
Z=ker(partial:R^2->R), F^d Z=Z intersect (RI^d)^2.
```

The unaugmented cycle dimensions are v456 Theorem2.1. Its existing dirty
worktree file is read as a premise, not modified by this note.

## 2. Only one E3 coordinate is needed for the five-dimensional endpoint

In the fixed E3 marking, `e3=Q0 x pc3` and

```text
x |-> (X0,u1),     y |-> (Y0,u3).
```

The PC presentation `FUDA1_PC3` in `scratchpad/fuda1_a0_rmax_data.g`
lines14--21 has four independent relative-order-three generators and
`u3^u1=u3*u4`, with u4 central. Consequently

```text
H=<u1,u3> = {u1^a u3^b u4^c:a,b,c in F3},
|H|=27, H'=Phi(H)=<u4>, H_ab=F3^2.                    (2.1)
```

The last coordinates are exponent sums of x,y modulo3. In particular they
are NOT the normalized exponent epsilon/18 and NOT extra source auxiliaries.

The actual subgroup AC generated by the displayed x,y is Q0 x H. Here is a
paper proof avoiding a new group enumeration. Its two projections are onto.
For any subgroup C projecting onto G and H, put
`H0={h:(1,h) in C}`. The H-coordinate modulo H0 of a lift of g is well-defined
and gives a surjection `G->H/H0`. Apply this with G=Q0. Every quotient of H
is a3-group. Q0 has abelianization C2^2 (v439/v458), hence has no nontrivial
3-group quotient: any nontrivial finite3-group has a quotient C3. Thus
H/H0 is trivial, so C contains1 x H and consequently all Q0 x H.

Let `V9[3]=3(Z/9)^3`, the kernel of Q0->Q2. Surjectivity Delta->AC implies

```text
image(Gamma2 -> AC) = V9[3] x H.
```

Taking abelian exponent-three quotients gives a surjection

```text
Gamma2/Phi(Gamma2) -> V9[3] x H_ab = F3^3 x F3^2.       (2.2)
```

Both sides have dimension5 by(1.1), so(2.2) is an ISOMORPHISM. This is not
an assertion that the single E3 projection is faithful on Gamma2 itself.

Thus for a literal n in N2 the complete endpoint obstruction is exactly

```text
tau2(J_Q2(n)) = ( rot_Q0(n)/3 mod3,
                 epsilon_x(n) mod3, epsilon_y(n) mod3 ). (2.3)
```

Its first entry is a three-vector; division by3 is applied to an actual
rotation in3(Z/9)^3. Its kernel is J_Q2(Omega), by v542(1.4). Formula(2.3)
also identifies the A action: three nontrivial rotation characters and two
trivial exponent directions. P and V act trivially on these five coordinates.

## 3. Explicit carry cochains, without a new Delta table or chord scan

For a vertex q=(p,v,e) of Q2 choose coordinate representatives
`v_i in {0,1,2}` and the set section `s(q)=(p,v,e)` in Q0. Let the fixed
marked Q0 image of a positive letter g have rotation `k_g in (Z/9)^3`,
represented by integers0..8. The extension error on its edge is

```text
a(q,g) = s(q)*g0*s(q*g)^(-1) in V9[3].
```

Writing `[m]_3` for the representative0,1,2 of m modulo3 gives the explicit
three-component cochain

```text
c_i(q,g) = (v_i + epsilon_i(e)*k_(g,i)
            - [v_i + epsilon_i(e)*k_(g,i)]_3)/3 mod3.   (3.1)
```

Use actual INTEGER signs in the numerator before dividing. Negative edges
have `c(q,g^-1)=-c(q*g^-1,g)`, the ordinary oriented-edge convention.

To prove that this cochain gives(2.3), write the Q0 value of each successive
path prefix as `h*s(q)`, with h in the abelian kernel V9[3]. The next letter
updates h by adding a(q,g); the cancellation formula for a negative edge is
the same one. For a closed Q2 word n the final section is1. Therefore its
Q0 endpoint divided by3 is the sum of these edge contributions. This proves

```text
tau2(z) = (<c1,z>,<c2,z>,<c3,z>, aug(z_x),aug(z_y))     (3.2)
```

for every z in Z, first on literal Schreier cycles and then by linearity.
This supplies the complete five rows explicitly. It does not require the
729-element projected group or Delta to be enumerated.

For a fixed parity and letter, each c_i is a function of the SINGLE variable
v_i in F3. It therefore has a polynomial representative of degree at most2
over F3. Equivalently, its third finite differences vanish; differences in
any other rotation coordinate vanish at once. Pairing a cochain with
`(t-1)z` takes its finite difference under the left V translation. Every
product of three such differences consequently kills c_i, and the constant
augmentation cochains are killed by one difference. We obtain

```text
tau2(F^3 Z)=0,
tau2(F^1 Z) <= F3^3 x 0.                              (3.3)
```

This argument works for each fixed P-coordinate and parity. It involves no
averaging by |P| and no common actor on the aggregated physical target.

## 4. The three carry directions occur in degree2, not later

Choose a word r_i whose Q0 image is the STANDARD UNIT pure rotation t_i
(rotation coordinate1 at i, zero elsewhere), of order9. Such words exist
since x,y generate Q0; for a concrete
consumer the existing marked split generators provide word lifts.
Then r_i^3 is in N2, and its Q2-Fox row is

```text
z_i = J_Q2(r_i^3)
    = (1+t_i+t_i^2) J_Q2(r_i)
    = (t_i-1)^2 J_Q2(r_i) in F^2 Z.                   (4.1)
```

In(4.1), t_i denotes its order-three Q2 image. Equation(2.3) gives

```text
tau2(z_i) = (e_i,0,0).                                (4.2)
```

In particular the exponent coordinates vanish because epsilon(r_i^3) is
divisible by3. Combining(3.3),(4.2) and surjectivity of tau2 yields the
complete decreasing image filtration

```text
tau2(F^0 Z) = F3^5,
tau2(F^1 Z) = tau2(F^2 Z) = F3^3 x0,
tau2(F^3 Z) = 0.                                      (4.3)
```

The INDUCED filtration is essential: `F^d Z=Z intersect(I^d R)^2` need not
equal `I^d Z`. Although V acts trivially on the endpoint module and hence
tau2(I Z)=0, (4.1) has nonzero carry because J_Q2(r_i) is NOT a cycle.
Its endpoint t_i is nontrivial. Thus z_i is in F^2 Z but is not forced into
I Z. Replacing this filtration by I^d times the cycle module would erase
exactly the three directions being measured.

Let K=ker(tau2) with its induced filtration. For each d there is an exact
sequence

```text
0 -> gr_d K -> gr_d Z
  -> tau2(F^d Z)/tau2(F^(d+1) Z) ->0.                 (4.4)
```

For exactness in the middle, if tau2(z) is the image of some deeper z', then
z-z' is in K and has the same leading term. No strictness beyond the
induced filtrations is assumed in this argument. Thus the raw legal source
loses exactly2 dimensions in degree0, exactly3 in degree2, and none in any
other degree. The two losses in degree0 are trivial A-character directions;
the grade2 loss is one in each nontrivial character.

## 5. Exact dimensions of the legal transition spaces

V459's existing literal normalizers give the full augmented source
`K x F3^2`, where the extra normalized coordinates have filtration degree0.
The identity-occurrence/PB3 component used in v454/Task575 recovers the raw
Fox grades; the source occurrence embedding is filtration-preserving and
strict on this image. This is the same source filtration as v444/v457,
NOT filtration only after physical aggregation.

Using v456's cycle dimensions, the complete augmented legal source has:

| grade | trivial source character | each nontrivial character | total |
|---:|---:|---:|---:|
|0|505|503|2014|
|1|1509|1512|6045|
|2|3026|3022|12092|
|3|3526|3529|14113|
|4|3027|3024|12099|
|5|1511|1513|6050|
|6|505|504|2017|

In particular

```text
dim H^[2] = 12092,
(delta2,delta3,delta4,delta5,delta6) = (3,0,0,0,0).     (5.1)
```

The total is54430=54428+2, agreeing with v458. The grade2 value refines the
old interval12092..12095; all three previously unlocated deficit directions
are the carry obstructions, not unidentified later-grade survivors.

## 6. Practical effect and the remaining mathematical wall

For v543 the five t_e values are now obtained by summing(3.1) and the two
augmentation rows on the fundamental cycle. One can instead use the raw
edge cochain(3.2) directly, with a change of tree potential. No separately
exported abstract Gamma2 abelianization roster is needed to DEFINE them.
An actual consumer must still bind the concrete rotation coordinates,
section convention, marking and Fox handedness to its exact parents.

Together with v545, a chosen legal cycle can be read out as a literal word
using the old s3,s5 repair and normalizers. These are real paper reductions
of source construction, not new obligations for the seed30 production run.

The physical H1/H2 map need NOT map this large source onto its ambient
target. Its kernel can be large. Accordingly neither(3.2) nor the exact
ranks(5.1) prove that the fixed rho2 belongs to its physical image. The
existing lower fibre/Conn must still be retained. A0 also includes the
remaining finite H tower and PB4 block with joint normalization. No finite
success is promoted to a cofinal lift, fake certificate or Ihara witness.

```text
FIVE LEGALITY COCHAINS:                 EXPLICIT CARRY+EXPONENT FORMULAS
GAMMA2 FIVE-DIMENSIONAL QUOTIENT:       ONE E3 COORDINATE SUFFICES
TOTAL THREE-DIRECTION SOURCE DEFICIT:  ALL IN GRADE2
LATER LEGAL SOURCE GRADE DIMENSIONS:   EXACT ON THE STATED FILTRATION
ACTUAL ADAPTER/NUMERICAL ROW REPLAY:    NOT RUN BY THIS NOTE
PHYSICAL RHO2 MEMBERSHIP / FULL A0:    STILL UNDECIDED
verified=false
```
