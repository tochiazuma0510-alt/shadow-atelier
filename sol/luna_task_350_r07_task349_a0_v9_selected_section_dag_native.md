# Luna task 350 - A0/v9 selected-section authority and DAG-native resume

Role: Luna, bounded implementation only.  Read every numbered section and
every prerequisite first to last before editing.  Do not run Python, Node,
GAP, GHA, a workflow, git, or network.  This task authorizes exactly the five
new v9 outputs in Section 2 and no execution.  Preserve every v1--v8 source,
fixture, receipt, checkpoint, proof, workflow, and v220 file.

Task349 ended honestly as `BLOCKED / UNEXECUTED`.  Retain its useful repairs,
but do not copy its reply claims forward unless a literal v9 trace proves
them.  The two load-bearing stops are now constructive: v287 supplies the
selected task176 authority needed by the checker, and Sections 5--6 below
specify the remaining DAG/checkpoint repair.

## 1. Binding prerequisites

Read in full, in this order:

1. `sol/luna_task_342_r07_a0_v7_fast_positive_resume.md` and all five v7
   outputs;
2. `sol/sol_reply_347_r07_task342_a0_v7_code_performance_audit_v1.md`;
3. `sol/luna_task_349_r07_task347_a0_v8_audited_resume.md` and all five v8
   outputs;
4. v265 and v275--v279, then
   `sol/proof_r07_a0_positive_carrier_to_a2_specializer_v284.md`;
5. `sol/proof_r07_task176_selected_section_checker_v287.md`;
6. the task176 receipt, accepted checker verdict, manifest, recovery
   manifest, and every q3/E4/joint/old source already pinned by v8.

Task347's `REJECT / UNEXECUTED` remains binding.  In particular PRODUCTION
and SELFTEST remain separate, every physical mutation must reach its normal
validator, and no frozen-input forced exception exists before search.  All
unmodified requirements of tasks342 and 349 remain mandatory.

## 2. Sole permitted outputs

Create only:

- `search/d972_r07_history_free_positive_fast_resume_v9.py`;
- `crosscheck/check_d972_r07_history_free_positive_fast_resume_v9.py`;
- `search/d972_r07_history_free_positive_fast_resume_gha_driver_v9.g`;
- `search/certs/d972_r07_history_free_positive_fast_resume_selftest_v9_20260829.json`;
- `sol/luna_reply_350_r07_task349_a0_v9_selected_section_dag_native.md`.

Return `IMPLEMENTED / UNEXECUTED` only if a literal static trace reaches
fresh PRODUCTION, authenticated resume, every typed UNKNOWN, COMMON, and hard
STOP with every requirement below.  Otherwise return `BLOCKED / UNEXECUTED`
and name the first exact owner/API.  Do not run SELFTEST or PRODUCTION.  A
fresh Sol(max) code-and-performance PASS is required before execution.

## 3. Exact accepted task176 authority

Pin and physically authenticate these repository-relative inputs exactly:

| owner | bytes | SHA-256 |
|---|---:|---|
| `ci/in/d972_r07_all_seven_extension_section_census_v1.json` | 13649089 | `715441d8ecb1b4bb39a51cf3df15f04d6179ee6adeafa5b925485dbbe91f7f41` |
| `ci/in/d972_r07_all_seven_extension_section_census_v1.manifest.json` | 349 | `de62e5e55a2e348a3cce297764f7ff4bfedc10ebe2545f22cbc1551f15e1adc1` |
| `ci/in/d972_r07_all_seven_extension_section_census_crosscheck_v1.json` | 757 | `e6a45a34353ce1fb54c99b4f9cbc8b106f34bfc751dd50044f2a79da72cad5e5` |
| `ci/in/d972_r07_all_seven_extension_section_census_crosscheck_v1.recovery.manifest.json` | 2035 | `41d2cb72614ce7e2d5b2d7a9000e861414da1c749876b3d51f1ccf2ca63390a8` |

The checker must cross-bind the producer receipt, checker verdict, original
manifest, and recovery manifest, including their recorded original checker
identity and recovery reason.  A file hash without these internal bindings
is insufficient.

Decode the following task176 receipt owners with strict base64, bounded zlib,
declared exact raw length, end-of-stream/no-suffix, and raw/compressed SHA
checks before semantic use:

- `result.Q0_section.canonical_roster`:
  1,469,664 records, 36 bytes each, 52,907,904 raw bytes;
- its `parent_states_u32le`: 5,878,656 raw bytes;
- its `parent_letters_u8`: 1,469,664 raw bytes;
- its ten rows of literal x/y marked-generator coordinate blobs (40 bytes per
  blob in coordinates 0--4 and 154 bytes per blob in coordinates 5--9);
- `result.Gamma.ten_coordinate_states`: 243 records, 970 bytes each,
  235,710 raw bytes;
- its `section_parent_states_u16le`: 486 raw bytes;
- its `section_parent_record_u8`: 243 raw bytes; and
- its literal 26-entry `record_words` roster.

Enforce raw limits before allocation.  Reject duplicate keys, noncanonical
base64, malformed zlib, wrong length, trailing compressed data, bad SHA,
wrong cardinality, bad coordinate widths, invalid parent/letter values, and
nonterminating or nonchronological parent chains.

## 4. Independent selected correction reconstruction

Replace v8's producer-field checks in `validate_correction_provenance` by the
literal v287 construction.  The checker must not call the A0 producer or any
task176 executable helper for this reconstruction.  Implement the selected
parent walks, ten-coordinate arithmetic, encodings, coarse keys, group
orientation, and multiplication independently in the checker from frozen
constants and the decoded literal owners.  Authentication of task176 source
code is provenance, not permission to call its composite helpers.

For every selected correction term:

1. bind every old `o:NNNN` symbol to one chronological record in the opened
   raw old checkpoint, including literal record bytes and fields;
2. for `qid`, random-access its 36-byte task176 roster record, recover its
   literal word from the accepted u32/u8 parent arrays, independently replay
   the word, and compare the underlying state and all selected coordinate
   states;
3. similarly recover `gid` from the 243-entry Gamma parent/record owners and
   compare with the decoded 970-byte ten-state record;
4. for `K != 0`, require Euclidean cursor decoding
   `qid = cursor // 243 + 1`, `gid = cursor % 243 + 1`, complete cursor bounds,
   the exact pinned schedule, section product, membership, and word;
5. for `K == 0`, build only the coordinate coarse inverse demanded by the
   selected formula by streaming the 1,469,664 parent records once and reject
   duplicate coarse keys; for every one-coordinate Gamma value `a`, look up
   the section for `a^-1 * target`, require complete typed-blob equality, and
   prove the claimed `(qid,gid)` is the least base pair; then rebuild the
   relevant projection-kernel BFS of exact accepted order 1, 3, or 9 and bind
   the claimed kernel cursor, word, and ten-coordinate completion; and
6. independently reconstruct the coefficient-two inverse word, conjugate,
   scalar, eleven occurrences, direct H1/H2/P row, final correction row, and
   finite sparse equality.

The K=0 one-coordinate inverse may be cached within one checker run and in an
authenticated heavy checkpoint, but the other nine indices must remain lazy.
The checker may reconstruct all 243 Gamma records because this owner is
bounded.  It must not materialize ten 1,469,664-entry stores or invoke
`enumerate_q0_sections`.

The selected proof's heavy identity is derived from the physically opened
task176/q3/E4/joint inputs, decoded raw owner identities, coordinate index,
kernel BFS identity, and exact correction dual/cursor.  It is not a digest of
producer-supplied Q0/Gamma hex or a copied membership Boolean.

Add physical SELFTEST mutations which independently alter at least: selected
Q0 roster state, Q0 parent, Q0 letter, marked generator, selected Gamma
state, Gamma parent/record, qid, gid, cursor quotient, cursor remainder,
schedule kind, K=0 fibre nonleast member, kernel order, heavy input identity,
section word, and coefficient-two inverse word.  Each mutation must reach
the ordinary selected validator and be rejected for the semantic reason.

## 5. Fully DAG-native actual rows

Remove v8's remaining eager expansion and legacy flat records.  In
particular, `FormalReducer.add_actual` must return the pivot and normalized
hash-consed node id, not a flat expression.  `Search.add_actual` stores a
`pivot_node_id`; it does not call `ancestry.expand`, store
`pivot_expression`, or append `new_expressions`.

Maintain separate, monotonically metered quantities for:

- unique DAG node allocation;
- sparse row support inspections and coefficient operations;
- the number and total support of explicit expansions; and
- serialized DAG/checkpoint bytes.

Do not call a flat expansion to estimate DAG allocation.  Expansion is
permitted only for the final selected COMMON support and a fixed bounded set
of checkpoint canaries.  Every such call must be visible in the expansion
meter and capped before allocation.

The checkpoint contains a topologically ordered immutable DAG node table,
hash-cons identity canary, old pivot-to-node ids, new pivot-to-node ids,
normalized sparse rows, target/remainder node ids, coefficient solution node
ids, and the exact next-state dual.  It contains no complete flat ancestry
roster.  Resume validates node opcodes, operand-before-node order, scalars,
unique hash-cons keys, node bounds, pivot uniqueness/order, and bounded
canary expansions before restoring the reducer.

Recompute the initial target/remainder/dual once per fresh process.  On
resume reconstruct the DAG/echelon objects from the checkpoint and compare
one freshly derived next-state canary/dual; do not replay all prior actual
rows and do not restart a boundary or correction cursor at zero.

## 6. Single owned checkpoint sidecar

Replace v8's accumulating phase-versioned paths by one process-owned output
checkpoint sidecar.  Fresh startup stale-rejects that exact path before any
write.  Once ownership is established, every later clean checkpoint uses an
exclusive same-directory temporary, bounded canonical serialization,
flush/fsync, atomic replace, and directory fsync.  It never leaves
`.checkpoint.N.phase.json` siblings.

An authenticated resume input is a distinct explicitly registered path plus
portable manifest; it is never overwritten.  The receipt, UNKNOWN checker,
COMMON checker, and ASCII driver all refer to the one current output sidecar
by exact repository-relative path, bytes, and SHA-256.  COMMON success removes
only the owned output checkpoint after the final receipt is durable; typed
UNKNOWN retains and publishes it.  Hard STOP cleans temporary files without
deleting a pre-existing resume input.

Every checkpoint write occurs only after the persistent worker roster is
stopped and proved clean.  Restore recreates a fresh roster.  Preserve v8's
phase-safe resource envelopes, but never serialize an unbuilt heavy owner or
claim a phase is complete when its lossless data are absent.

## 7. Bounded physical I/O and transport

Replace all unbounded `Path.read_bytes()` on accepted authorities, manifests,
SELFTEST receipts, resume inputs, optional checkpoints, and final receipts by
bounded no-follow physical reads or streams.  Perform opened-before,
opened-after, and path-after `(dev,ino,size,mtime,nlink)` checks.  Charge known
size before allocation.  Large hashes and zlib/base64 decode are streaming
where possible and never keep both an unnecessary chunk list and joined copy.

The driver must stage every authority in Section 3 and all v8-pinned sources,
verify exact sizes/SHA values, expose separate SELFTEST, fresh PRODUCTION, and
authenticated-resume routes, and publish the sole output checkpoint when the
typed terminal requires it.  No sleep, retry, polling, nested pool, workflow
edit, or local execution is permitted.

UNKNOWN remains cheap: validate its fixed envelope and stream at most the
one referenced checkpoint sidecar.  It must not decode the task176 payload,
parse the 86 MB old owner, build P, import mathematical modules, or run
SELFTEST.  COMMON performs the selected semantic work in Section 4.

## 8. Preserve audited mathematical and process gates

Retain v8's chronological triangular gate and require exactly 1,011,460 A*C
contributions plus 289,774 P-support inspections.  Preserve disjoint IPC
slices, exact descriptor/support coverage, scalar accumulation, one-pass
ACTIVE winner retention, worker timeout/death/partial-frame/blocked-send
handling, Q0-LATE only after a complete zero boundary epoch, and the
10,800/7,200/3,600-second decomposition inside the 21,600-second hosted job.

PRODUCTION never runs SELFTEST or mutations.  A separately sealed accepted
v9 SELFTEST receipt is a driver prerequisite for future PRODUCTION.  OOM,
unexpected exceptions, assertion failures, code drift, owner drift, and
atomic-write failure are hard STOPs, never mathematical UNKNOWN or negative
claims.

## 9. Required adversarial static audit

Before reporting `IMPLEMENTED`, trace producer and checker source manually
and list exact line numbers for:

1. fresh production through the first 4,752 mathematical boundary pairs;
2. boundary ACTIVE, zero-to-Q0-LATE, correction ACTIVE, COMMON;
3. selected Q0 and Gamma decode, parent replay, K-nonzero cursor, K-zero
   complete fibre, old-record binding, and final sparse equality;
4. early input/light/P cap, boundary cap, heavy cap, correction cap, and
   checkpoint-serialization cap;
5. resume at every advertised phase with genuine object restoration;
6. UNKNOWN with and without a sidecar, hard exception, stale path, atomic
   failure, and final sentinel;
7. every mutation in Section 4 reaching the same ordinary validator; and
8. absence of `pivot_expression`, `new_expressions`, eager actual-row
   expansion, full ten-index Q0 construction in the checker, versioned
   checkpoint litter, unnecessary second contributor passes, duplicate
   target/dual builds, unbounded physical reads, and producer-checker helper
   sharing.

Also search for avoidable asymptotic or constant-factor work: copied large
containers, repeated canonical JSON construction, repeated full hashes,
linear pivot lookup, repeated word expansion, all-coordinate work where one
coordinate suffices, stale worker/process setup, and redundant checkpoint
replay.  Treat any such load-bearing slowdown as a blocker, not a future
optimization note.

## 10. Reply contract

Report exact bytes/SHA-256 for all four machine outputs, the complete
import/process/physical-owner graph, the line-numbered traces in Section 9,
and exact formula/static meters separately from runtime measurements.  Since
execution is forbidden, all runtime/RSS observations say `UNEXECUTED`.

Do not claim trajectory parity, a separator, exhaustion, nonmembership,
compatible lift, fake certificate, or Ihara witness.  End exactly with:

```text
IMPLEMENTATION:                  IMPLEMENTED or BLOCKED
SELFTEST / PRODUCTION:           UNEXECUTED
FROZEN INPUTS:                   PASS or BLOCKED
FRESH SEARCH ROUTE:              STATICALLY REACHABLE or BLOCKED
AUTHENTICATED RESUME ROUTE:      STATICALLY REACHABLE or BLOCKED
ACTUAL A0 COMMON + CHECKER:      0/1
SEPARATOR / NEGATIVE CLAIM:      FORBIDDEN
LIFT / FAKE / IHARA:             NONE
```
