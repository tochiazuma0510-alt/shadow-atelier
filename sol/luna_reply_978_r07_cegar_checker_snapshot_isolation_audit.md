# Task978 — checker snapshot 所有隔離・保存32段 completion の限定監査

F1. **最終静的判定は PASS。** Task977/978 を全文読了し、本票だけを変更した。新 v2/source/workflow の最終 freeze は F14、旧/new code の保存不足は F13 で解消した。公刊 Task973/976、producer/checker v1、旧 workflow は不変。ローカルでは source の静的読取、JSON metadata、実 bytes/SHA の照合だけを行い、Python/import/AST/数値/GAP、network/git/credential、追加 agent は使わない。v220 と公開・実走の記帳は root が担当する。新 GHA と新CV-9は未観測であり、本票の PASS はsource/workflowの限定静的監査に限る。

F2. **実故障と受理境界。** 実診断 root は `%TEMP%/shadow-atelier-cegar-run33984832010-diagnostics-a1`。run 33984832010/1、head `b8c9e95ddd0183d9e43b7fcc961cb251fdaea13e`、artifact 9975236748。元 ZIP の全認証・安全展開は root の記録を参照する。子は root の58-entry pin表（SHA `9aa71c473bffff9e377b7b19bff3b951e305b95bd5cab35e05d45e8366859086`）の全58本を実ファイルの bytes/SHA と照合し、差は0だった。実 checker-result（1533 B）全文は `FAIL`、`candidate=false`、`all_new_committed_arrays_and_json_compared=false`、reason `ValueError:cegar_checker:HEAD_entire_replayed_prefix_and_cursor`、cursor completed32/last phase physical。実 checker.log 末尾は step32 の `new_step_independently_replayed` と最終 current all-row 測定までである。これを完成 PASS に変更してはならない。実 producer result は `UNKNOWN_CAP`/Separator、completed32/rank1418/gen8123、実 HEAD は current snapshot/checkpoint とも null。これは保存候補の観測値であり、正式受理は rank1386/gen8091 に据え置く。complete-zero、grade2 MEMBER/NONMEMBER、A0 は未判定。

F3. **原因を実 source から独立確認した。** C v1 `PhysicalState.__init__`（L588）は入力親を deepcopy する一方、`derived`（L659–668）の `accepted_target_derivation_parents`、`summary`（L670–674）の同字段が `self.parents` をそのまま返す。`document`（L95–96）はこのネストを切らず seal に渡す。`root_start_owner`（L698–701）が生成した開始時 start と `snapshot_record`（L785–787）の各 snapshot に同じ mutable list が逃げる。`attach`（L645）はこの list に append する。実 start.json は外側親33件、lambda_rho2 内親33件、snapshot000000 は33件、snapshot000031 は64件、最終 result の DERIVED 親は65件だった。各物理 append が immutable な開始時記録を変えるべき理由はない。

`replay_head_prefix` は入口（L1102–1110）で正しい開始時 hash を照合・固定するが、32段後の `head_record`（L1062–1068、呼出 L1143）は可変になった start を再hashする。このため末尾の完全 HEAD 比較（L1144）が失敗する因果は root 診断と一致する。さらに各 snapshot の返却 receipt は `compare_committed_step` による attach の**後**に `sha(canonical(snapshot))` を計算する（L1117–1120）。したがって root start の hash だけをキャッシュする修理では、過去 snapshot の誤った receipt hash を残す。公刊 Task973 の静的 PASS はこの alias を見落とした。本票で明示し、旧票を遡及変更しない。

F4. **必要十分な修理の範囲。** C 側の `summary/derived` から外へ渡す親 metadata を深く所有隔離するのが妥当である。list だけの浅い copy は親 dict の字段変更を伝播させるため不十分。`summary` の `direct_pairing`、`measure` の戻り値、最終 checker receipt の `direct_pairing` も同じ mutable dict 境界として点検する。現行の `measure` は dict を置換するので、その箇所を今回の33→65故障の原因とは呼ばない。既存 `CHECKED_CURSOR` の failure/return receipt は deepcopy 済みである。新 regression は実 PhysicalState と実 serializer を通し、alias control では開始時／過去 snapshot の canonical bytes/hash が変わり、修正版では append と深い親字段 mutation の両方に対して不変、更新後 current state は進むことを要求する。完全 HEAD、九 phase、terminal、invocation の比較を弱める修理は認めない。

F5. **既走試験の実 receipt。** 元 `oracle-v2-full-selftest.json`（869 B / `094f69edc9a8aca33f4191b73b38453a5e758db73708e76ab0d262a8b75ffb44`）を全文読了し、四件 PASS を直接確認した。元 producer の三 interface 群と元 checker の三 interface 群も実 JSON で PASS を確認した。これらは元 source/run の記録であり、修理 v2 の新 regression や全32段 completion の成功を示さない。新 workflow では旧 suite と producer を再走せず、元試験・小 receipt と全保存 output の不変認証を維持し、新 C で全32段から最終 HEAD/terminal/invocation までを再照合する必要がある。

F6. **保存済み prefix と継承 bytes の限定照合。** 全32 step manifest の metadata を読み、step 順、predecessor manifest の実 bytes SHA、各 snapshot/start.json の実 bytes SHA、九 phase EOF の順を確認した。不一致は0。最終 manifest の実 SHA `b9732d0bdef2cd39f3b498f7da9297ec18022a8fa40e6fe3fc83ef61483dc3bf` は HEAD と一致し、開始時 start.json の実 SHA `87bd9b89c593d68fba65b765bfe9f17bcc47d52cc9afce6f53f8c131a24f816b` も HEAD と一致した。これは metadata chain の確認であり、32段の算術 PASS の代用ではない。実 preservation-result は output2584 files/420 directories/346710509 bytes、source/即時親/output 不変 true、checker status は別と記録する。元 source-receipt の19 source と3 rawを現ファイル bytes/SHA と照合し、全一致した。

F7. **v2 の最初の修理 block。** 保存された `check_d972_r07_complete_oracle_cegar_continuation_v2.py`（暫定 SHA `a8b887ff6ebc78befc3842c28db6c50017e751dea96f0269c2847061f3c34c34`）と凍結 v1 の全行差分を静的に読了した。この時点の変更は冒頭 Task 表記、`measure` の返却、`derived/summary` の親と direct_pairing、最終 direct_pairing の deepcopy だけである。算術・scope/pins・九 phase・完全 HEAD・terminal/invocation 比較に変更はなく、F4 の修理として妥当。継承 seal の実体は `check_d972_r07_fixed_root_packet_loop_v2.py` L108–110 の浅い `{**value, ...}` と確認した。snapshot から oracle-result / dynamic_delta_records の DERIVED へ移る二次参照も、元 snapshot が state から切り離されれば state append の影響を受けない。実装はまだ専用 regression / CLI / workflow の完成待ちであり、この暫定 hash を最終 freeze と扱わない。

F8. **専用 regression / CLI の source 判定。** 更新 v2 は129557 B / SHA `e985b4ca3922fc4f89fe7c313d969bf4dd2b525fb92b4ee3ce3920888e6821e3`（作者報告 LF1819/CR0）。新 `snapshot_isolation_selftest`（L1645–1758）と CLI 末尾を全文読み、fixture が呼ぶ凍結 `E.oracle_layout` / `E.one_physical_row` の必要字段も確認した。実 PhysicalState、実 root_start_owner/snapshot_record、実 one_physical_row/attach を通り、一段後に旧 snapshot の receipt hash を再計算する。旧 alias の control は canonical bytes の変化・古い seal の残留・sealed_bytes の拒否を要求する。修正版の start/snapshot0/derived/summary/measure と新 snapshot1 は、実 append および state の親 dict 字段・direct_pairing mutation 後に不変と要求し、逆向きの export mutation も state を変えない。current の rank/generation/head/step と親字段は更新され、最後の実 head_record が開始時 start SHA を保つことを要求する。紙上で fixture の入力形と接続に必須修正はない。CLI は新専用 mode、内部240秒を使用でき、FAIL1/Resource3 を保持する。実三件 PASS は GHA まで未観測である。

F9. **裁定2145。** `ops/express/20260906_fable_astra_cegar_continuation_ack.md` を全文読了。oracle v2 full四件の実 PASS による2138 F-sc-3の正式閉鎖を採用する。これは E三件や今回の新隔離regressionとは異なる対象。新 completion 成功後の増分 CV-9 に三規約表 diff（materializer/oracle/continuation）、32 step target.scalar 列・零 root 内訳・alias修理receiptを含める要望を継承する。旧 F-fo-1 / F-sc-1 等の継承TCB・独立性限定、F-sc-2の当該lambdaでの零と作用素恒等零の区別、2143/2144の signed literal / 同sourceと同wordの区別を変更しない。新metadata修理から算術独立性の追加閉鎖は導かない。

F10. **全 source 差分と workflow 前半。** F8 の新関数／CLI分岐、F7 の所有隔離、Task表記／一行コメントを除く全 source text が凍結 v1 と一致することを、PowerShell の文字列比較で確認した（実行・ASTなし）。新 producer import や算術 helper 変更はない。completion workflow の保存済み前半609行を全文読了した。元14親と失敗diagの15 artifact の live id/name/size/digest/run/head/repository を認証し、各 run の attempt/workflow/status/conclusion を役割別に照合する。Task554 と今回diagだけ failure を要求し、他の成功親へ例外を拡大しない。diag は expiry と全ZIP bytes/SHA/2636 entries、path/symlink/重複を確認して展開する。元19 source/3 raw の AST/bytes/SHA と新v2を20本目に加えた repair-source receipt は GHA 内で実行する設計であり、旧19本の順序付き由来joinと新runtime/launchを分けている。14 parent root 解決、E/oracleの実十entry・output rosters・既存receipt joinまでに必須指摘はない。新専用試験・新C一回・全保存前後不変・最終PASS・uploadは後半完成待ちである。

F11. **保存／試験／全32段実行 block。** workflow L610–872 を追加読了した。58 literal pins は source text から regex で取り出して実pin表と照合し、全一致。元19 source receipt の再構成 bytes と実旧receipt、repair receipt の先頭19本、3raw、記録されたproducer/runtimeのjoinを要求する。元 checker FAILのexact reason/cursor、元P cap1/resume32のexit0、旧oracle full4／P3／C3／両metadata5の実PASSを確認して保存する。元全2636 files/421 directories、そのうちoutput2584 files/420 directories/346710509 Bを実走前に全scanし、旧before/after rostersと一致させ、outputをcompletionへcopyし、元52個の非outputファイルもoriginal/へ保つ。旧FAILと旧ログはprevious-checker名で別保存する。14 parent rootsは大きい親も含め全files/dirsをbaseline化する。

新専用regressionは内240秒／外5分／7 GiB、exit0、実mode schema、全三件PASS、actual state/serializer/control flagsを要求する。その後の数値実行は新C v2一回だけで、コピーした保存outputに対し内10800秒／外190分／7 GiB、job230分。exit0かつstdout/report同一、status PASS、terminal UNKNOWN_CAP、32 steps/snapshotsの完全再生、全四character／8059／54433／2／96776／mod54／全四B、全HEAD/checkpoint等の完全比較フラグを要求する。新P、旧suite、旧E/旧scan再生の呼出は追加されていない。ここまでに必須修正はない。最終always保全／run receipt／uploadと最終hashはなお待ち。

F12. **workflow 全文読了と最後の限定不足。** 81739 B / SHA `72230f86c7f66c66101a847d27266ef07d52507223cd99ca74a6f58b83d61696` の1141行を全文読了した。always保全は20 source/3 raw、14親全files/dirs、元2636入力、元52receiptのcopy、元／copy両outputを別flagsで照合し、不一致はFAIL、不足はINCOMPLETEにする。最終joinは全flags PASS、旧FAIL1／新PASS0、32段の新 result/HEAD/state/target/lambda/hash、全過去snapshotの実start SHAと33+n親数、最終65親DERIVED／全1418 row pairing／両target=1、二invocationと実resume32、旧source19本／新20本とruntime／新旧launchを結ぶ。candidate uploadは全success後だけ、diagnosticsはalways/hidden含有/30日である。これらに追加必須指摘はない。

ただしこの版には Task977 §保存5 が明記した旧/new checkerの **code実体** を completion/へ保存する処理がなく、pins/receiptsだけであった。旧C v1と新C v2の実bytesを診断用subdirectoryへ保存して対応source receiptとbytes/SHA照合するworkflow限定追加を、rootと977へ必要修正として通知した。算術source、HEAD等のgateを変える必要はない。この不足は F13 の最終差分で閉じた。

F13. **最終差分を読了し、必要修正を閉鎖。** always処理は `completion/checker-sources/` に旧C v1と新C v2の実体をコピーし、コピー前後のbytes/SHAを元／新pinへ照合する。preservation receiptに二本を記録し、最終joinも二本の実保存bytes/SHAを再照合する。F12の不足は解消した。

root承認のcoverage追加は、新C全32段PASSの**後**だけ、既に比較したsnapshot/phase manifest/payloadの実hash・shape・byte数に結び付けて読み出す。`q.bin` は packed3 `[4,36288]`/36288 B、各character区画9072 Bであり、zero判定と `nonzero_packed_bytes` を区別する。`kappa.bin` の基数1,3,9,27の復号、d0区間0:24192、d1区間24192:96768、最後8aux、およびscore `[6,2,54432]` のタグ／component区画を、凍結実serializer・`source_scores` と照合した。元section manifestの実形も同一だった。ここでは source の再計算やproducer再走を行わず、κ/scoreの支持とb_aux、32 target.scalar、零root内訳を保存metadataから記帳する。三規約表の参照先は実在し、signed representative／整数6除算／mod54／18での正規化という既存規約を参照するだけで、新裁定・新literal算術を主張しない。

coverageのlambdaは snapshot000000–000031、すなわち各append前の32本である。最終rank1418は current snapshot=null のUNKNOWN_CAPであり、その最終lambdaの新complete oracleやroot配列を観測したとは扱わない。`operator_identically_zero_claimed=false` と `all_four_characters_informative_claimed=false` を保持し、支持分布から独立性・零確率・必要反復数を推定しない。新coverageとchecker resultのhash、scalar列、32行、character順、source再計算なしを最終receiptへ結ぶ。この追加にも必須修正はない。

F14. **最終freeze・射程。** 元1141行全文と追加各blockを読み切り、最終1252行のworkflowを監査済みとする。作者freezeをrootが確認した後、次の実bytes/hash/LFを子でも独立照合した。全てCR0、BOMなし、末尾LF。

| 対象 | bytes | SHA256 | LF |
| --- | ---: | --- | ---: |
| 新 `search/check_d972_r07_complete_oracle_cegar_continuation_v2.py` | 129557 | `e985b4ca3922fc4f89fe7c313d969bf4dd2b525fb92b4ee3ce3920888e6821e3` | 1819 |
| 新 `.github/workflows/d972-r07-complete-oracle-cegar-checker-completion-v1.yml` | 90880 | `31b4d8fba2f680ae5e949daf910eec9c3e1f7d4a28946aeecca43ea212817042` | 1252 |
| 凍結C v1 | 120245 | `8c000f9b49d04447a09c701daf5907a35b7f2e883f1e36747308a6d4ded29b1f` | 1701 |
| 凍結P v1 | 126940 | `67d2302c3c4d571a4ccbe524098fe97808f05069bfd2ed822408bde3e256733c` | 1972 |
| 凍結continuation v1 workflow | 61275 | `9f751fe1ea21d16b7758f9832d2dd091b73f0796128ceea505c8975031c096c1` | 942 |

新Cの修理はmetadata所有隔離と専用regression/CLIだけで、sourceの算術・scope/pins・九phase・完全HEAD・terminal/invocation gateは不変。新workflowはproducer0回、旧suite追加0回、新隔離regressionと新C全32段の一回照合を要求する。未解消の静的必須修正はない。新run/commit/artifactは本票freeze時未観測であり、根拠ある全prefix成功／受理／cross-checkedへ昇格するのは、その実receiptと増分CV-9をrootが受領してからである。現受理rank1386/gen8091、保存候補rank1418/gen8123、grade2/A0未判定と旧TCB限定を保つ。Task976の条件付きcomplete-zero論を今回のUNKNOWN_CAPへ適用しない。

AUDIT_978_VERDICT: PASS_LIMITED_STATIC_SOURCE_AND_WORKFLOW_DELTA; DEEP_SNAPSHOT_ISOLATION_AND_REGRESSION_ACCEPTED; DIAGNOSTIC_CODE_PRESERVATION_FIXED; EXACT_SAVED_32_PREFIX_REPLAY_REQUIRED; ORIGINAL_PRODUCER_BYTES_IMMUTABLE; NEW_RUNTIME_AND_CV9_PENDING; GRADE2_AND_A0_NOT_DECIDED; verified=false.
