# Luna task 284 - task232 task198 checker-pin refresh v1

Commissioner: Sol / 2026-08-28

Reply to `sol/luna_reply_284_r07_task232_task198_pin_refresh_v1.md`.

Role: bounded mechanical downstream-pin repair only.  Do not run Python,
Node, GAP, git, GHA, or network locally.  Parent Sol owns mathematical
adjudication, execution, commits, workflow dispatch, and artifacts.

Read this commission, the full current task232 producer/checker/driver, and
the current task198 checker before editing.  Edit only:

```text
search/d972_r07_word_independent_successor_kernel_v1.py
search/d972_r07_word_independent_successor_kernel_gha_driver_v1.g
sol/luna_reply_284_r07_task232_task198_pin_refresh_v1.md
```

Do not change the task232 checker or fixture, task198 files, proofs, ledgers,
workflows, inputs, artifacts, or any other file.

## 1. Exact reason

Task283 repaired task198's independent F2-to-PB3 checker wiring.  The current
authenticated task198 checker identity is

```text
crosscheck/check_d972_r07_seven_context_roof_presentation_v1.py
157253 bytes
sha256 001277d44dbbc2acd7e03c6ecb6c6419df84996ae188cbb4be7b18f7cfb56ca1
```

Task232's producer still pins the superseded 153420-byte / `46c566...`
identity in `TASK198_PINS`, so actual A4 production would fail before reading
the independently accepted task198 receipt.  This is only downstream source
authentication drift.  No task232 mathematics, receipt predicate, external
manifest predicate, attestation line, kernel closure, projection, mutation,
resource, or terminal rule is to change.

## 2. Surgical repair

1. Replace exactly the stale task198-checker byte/SHA row in task232
   `TASK198_PINS` by the authenticated identity above.
2. Refresh only the task232 producer byte/SHA pin in the task232 GAP driver
   forced by step 1.  The task232 checker and fixture identities must remain
   unchanged.
3. Preserve every other byte of executable logic if possible.  Do not add the
   task198 checker-only helper as a task232 runtime import; task232 only needs
   to authenticate the current task198 checker file already named by
   `TASK198_PINS`.
4. Keep the driver ASCII-only and both SELFTEST/PRODUCTION behavior unchanged.

## 3. Static audit and delivery

Record:

- the exact old and new downstream pin;
- final byte counts and SHA-256 for task232 producer, checker, driver, fixture,
  and reply (reply may omit its self-referential SHA);
- a statement that the only executable semantic change is the authenticated
  task198-checker identity and the forced driver producer pin refresh;
- all execution status as `UNEXECUTED`, and A4 as `0/3`.

End with:

```text
TASK232 TASK198 CHECKER PIN:                    REFRESHED STATICALLY
TASK232 PRODUCER/CHECKER SELFTEST:              NOT REEXECUTED BY LUNA
ACTUAL TASK198 INPUT / A4 PRODUCTION:           NOT EXECUTED BY LUNA
A4 / COMPATIBLE LIFT / FAKE / IHARA:            NOT DECLARED
```

`TASK284_TASK232_TASK198_PIN_REFRESH_COMMISSIONED`
