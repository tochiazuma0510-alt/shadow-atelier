# Task937 -- rank1355 root-seed scalar producer

## F1. Source handoff

Implemented only the authorized new producer
`search/d972_r07_rank1355_root_seed_scalars_v1.py`. The scoped actual path
authenticates the accepted rank1355 separator delta, derives four fresh
roots, makes one P1 contraction pass, and folds only the44 seed relations
through the five Task554 bodies. It does not execute actors, orbits, old
state construction or seed30 materialization.

```text
bytes: 31578
SHA256: 973ccd1d5d0f8fa5a28294589ff655620745f0cc988c09abe8a6178593e185bb
LF: 560; CR: 0; BOM: absent; final LF: present
```

These are read-only PowerShell file/hash inspections, not runtime tests.
The frozen source and exact output constructors were sent to Task930 for
the separately owned checker/workflow. No local Python/GAP, network,
credentials, git, GHA or test execution occurred. This worker has no new
commit SHA or run ID. Root remains the sole broker.

## F2. Scope and authority

The sole new state authority is successful run33946247365/1, head
`7f6dfaddf4150449e62a9b3e85def472fcb41c01`, artifact9963533999. Its exact
archive and manifest/result/instruction/checker/source pins are compiled
into the producer. Accepted Task904 files remain immutable premises.

The producer checks the single rolling append and explicit generation8060,
rank1355, head36feb776..., and lambda f83bbaa... . New lambda is streamed
against all1354 old physical rows, then the new normalized row; all1355
pairings must vanish. Its pairing with the accepted saved new target
remainder must be one. No old target solve or rho2 stager is needed.

Four actual B-adjoint roots are derived afresh; there is no fixed active
character pattern, old q/seed2 scalar pin, old-head fallback or global
constant mutation. Both Task554/P1 identities and Task712 tables join the
fixed delta's parent receipts. The producer reuses only its pinned
corrected v2/v15 lineage; the new checker is not imported.

All8059 contractions are recomputed for each new root in one buffered P1
cache pass. The instruction file is hash-streamed once without line/DAG
replay. The seed-only fold holds one large Task554 body at a time, includes
every old source and every new target/source contribution, and seals each
seed's ordered raw coefficient events before numeric subtraction. No actor
lower-dot pass is reachable. Direct seed evaluation uses the accepted
v541 raw evaluator with its old-q-specific pin disabled by the documented
`actual_pin=False` argument, never projection of the direct side alone.

## F3. Frozen ABI and CLI

Schema: `d972.r07.rank1355.root-seed-scalars.v1`. The fresh output is20 flat
files: launch/result/manifest JSON, one176-record scalars.jsonl, and four
copies of each binary family:

- `q-aA-root.bin`: 9072 packed bytes;
- `p1-values-aA.bin`: 8059 uint8 scalar entries;
- `direct-seeds-aA.bin`: 44 uint8 entries;
- `seed-scalars-aA.bin`: 44 uint8 entries; A=0,1,2,3.

Launch authority fields are root-path independent. Result embeds four
sealed character records, fresh raw-dual/source receipts, per-seed ordered
relation heads/counts, cache receipts and the complete scalar-stream seal.
Records are character-major/seed0--43; the first nonzero is
`ROOT_SEED_VIOLATION` with exact current-state/q/P1/relation provenance.
All176 zero is only `ROOT_SEEDS_ZERO`. Actor/orbit counters are zero and
materialization is explicitly false.

```text
python -B -u search/d972_r07_rank1355_root_seed_scalars_v1.py --selftest

python -B -u search/d972_r07_rank1355_root_seed_scalars_v1.py \
  --delta-root NEW_DELTA --state-root TASK904 --prepare-root PREPARE \
  --block-root B0 --block-root B1 --block-root B2 --block-root B3 \
  --p1-root P1 --task712-root TASK712 --output-root FRESH_OUTPUT
```

Fresh output must be disjoint from parents. Stdout is a single result JSON;
buffered phase/count progress is on stderr. Failed gates return REJECTED
and nonzero exit. Parent directories contain unchanged accepted artifacts,
not copies assembled by this program.

## F4. Execution boundary

Five bounded synthetic canaries are implemented but unexecuted here:
dynamic roots, explicit new head,176 seed-only ordering, all-root-seeds-zero,
and stale-head seal mutation. They do not load historical parents or run
historical tests. Root must run producer/checker/tests serially on GHA.

No new q or scalar value, runtime PASS, grade2 membership/nonmembership,
A0/COMMON/COFINAL/FAKE/IHARA result is claimed. The accepted derivation and
shared-lineage qualifications remain explicit; `cross_checked=false` and
`verified=false`. No extra semantic blocker was identified during this
source/static handoff; actual execution remains required.
