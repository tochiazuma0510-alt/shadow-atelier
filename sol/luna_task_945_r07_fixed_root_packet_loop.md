# Tasks945/946 -- adopt the fixed44 packet and bounded current-root loop

Root read/adopts FULL Task944/reply944. The algebra and fixed finite scope
are supplied there, using v541. This is an implementation handoff, not a
new mathematical prerequisite or a generic state framework. Read both FULL.

## 1. Ownership

Task929 implements ONLY
`search/d972_r07_fixed_root_packet_loop_v1.py` and
`sol/luna_reply_945_r07_fixed_root_packet_loop.md`.

Task930 independently implements ONLY
`search/check_d972_r07_fixed_root_packet_loop_v1.py`,
`.github/workflows/d972-r07-fixed-root-packet-loop-v1.yml`, and
`sol/luna_reply_946_r07_fixed_root_packet_loop_checker_workflow.md`.

Root alone does git/push/GHA. Use each side's own accepted arithmetic and
small reusable functions, never sibling math or global-constant monkeypatch.
Keep old files immutable and coordinate one small public ABI directly.

## 2. Fixed packet, produced once

Build all44 COMPLETE raw seed-minus-canonical-P1 defects, exactly as
Task944 section2 and reply944 F2/F3 specify. Collect/seal source-order
events before mod3 combination; preserve cancelled literal root references.
Read one Task554 body at a time. One buffered P1 cache/instruction pass,
one pass per lower blob, no decoded8059-by-full-lift matrix or broadcasted
44-by-full-lift temporary. Reuse one decoded row and bounded F3 scratch.
The lower/top accumulators and coefficient table have the explicit small
sizes in Task944. All44 full96776-coordinate lower parts must vanish.

Apply v453 direct slices ONLY after complete subtraction/lower-zero, never
the old mixed raw-seed projector. Retain the accepted structural slicing
premises, rather than creating a new expensive per-seed word-projector
closure. Independently reconstruct and compare the full packed packet.
Inside the already necessary44 raw evaluations, retain the lambda-INDEPENDENT
seed2 char0 row SHA/support pin from v541 (e67d0a0b...,568); do not assert
its scalar is zero for future lambda. Join packet char0 seed30/seed34 rows
to the saved actual source-d payloads as cheap regression byte checks.

Persist the immutable packet, one shared P1-root index, and per-seed ordered
ancestry. Later selected pivots reference these, not repeated full P1
ancestry bodies. The packet is independent of current lambda/state head.

## 3. Actual start and bounded loop

Start at the exact rank1356/generation8061/head d467e4e... in Task944 and
reply162 section18: immutable Task904 base plus seed30 and seed34 deltas.
Authenticate each named parent and the current remainder/lambda. No scalar
candidate parent, old target solve, old Conn rebuild, or historical insert
replay is needed. Do not assume rank1355's seed35/36 values survive.

For each current lambda, derive four fresh B-adjoint roots, pair them with
the fixed packet in character-major/seed0..43 order, then:

- first nonzero: apply B only to that selected packet, perform the existing
  insertion-order physical reduction/one normalized append/one target step,
  update literal references and next lambda; directly check FINAL lambda
  against all saved/new rows and both target remainders before continuing;
- all roots/seeds zero: ROOT_SEEDS_ZERO for this finite ROOT scope only;
- target zero: MEMBER_CANDIDATE only, retaining unfinished full-word gates.

Do not build all176 physical rows upfront. A fixed-root loop has at most176
new rank raises; run the terminal root test after the last allowed append.
Never turn a cap/resource stop into ROOT_SEEDS_ZERO or grade2 NONMEMBER.
No actor/orbit work, tree/physical-multiplier adapter or source classification
is part of this run. No old scalar/q/P1 contraction pass occurs per hit.

## 4. Durable progress and release

Seal/fsync packet completion, then every WHOLE new pivot step, before
atomically advancing a small HEAD. Keep new step rows/instructions/target/
lambda and parent references; do not copy the large immutable base state
or duplicate packet ancestry per step. A partial tail is not a committed
step. Implement a simple same-owner resume from this complete prefix,
loading saved packet/current state instead of recalculating completed work.
An unaccepted prefix remains candidate; independent replay must cover the
new prefix before acceptance. No generic chain/format migration framework.

Expose phase/count/seed/rank/step and elapsed progress with buffered logs.
Keep syntax/pins and a FEW tiny changed-interface canaries on GHA, then
actual producer/checker serially. Do not rerun old suites. Candidate upload
requires checker PASS; diagnostics always preserve the complete prefix.
Source/runtime freezes and actual input tuples must be in both replies.

No local Python/GAP, network, credentials, git or dispatch by workers.
No additional files or speculative cleanup. Send genuine execution blockers
promptly; otherwise implement/freeze the narrow scope. Keep inherited
lineage/parent-premise qualifications, cross_checked=false, verified=false.
Neither finite ROOT_SEEDS_ZERO nor a new pivot proves grade2/full A0,
COMMON, cofinal lift, fake or Ihara.
