# Task927 -- corrected actual seed30 materialization, narrow intake

Implementation worker only; root retains mathematics and sole git/GHA broker.
Read all numbered sections. Initially write only
`sol/luna_reply_927_r07_actual_seed30_materialization.md`.
No credentials/network, git, local Python/GAP, or large local computation.

## 1. Actual new input, not the superseded seed2

Corrected root run33941591417/1 is completed/success. Head
`2caaf1f33b6f36f8aa754f759ef0e5dccfaf5a74`, root source commit
`a68460cf0c1bdae9fde5d3a4fa6501d625d68388`.
Producer68 seconds, checker67 seconds. Final artifact9962060495,
253544 bytes, zipSHA
`1091f9946108ef6bf122143da58d32006eba54166ee995996efa177aa89a2ed2`.
Actual first corrected violation is char0, seed30, scalar1, empty actor word,
origin_id30 (checked prefix31). Violation seal
`cba44225c60f14e6203ea51a053f75a56b17e6cc33f146a9262609ac43c1c0f5`.
Seed2 is zero. Do not modify/reuse the old scalar formula.

Downloaded result directory:
`C:\Users\81905\AppData\Local\Temp\shadow-atelier-r07-root-v2-run33941591417-candidate-a1`.
All other exact parents and local locations are in your Task920 intake reply.
Read it rather than rediscovering the roster. Current S is the retained
rank1354 initial separator state, generation8059/head69fdcc8c.../lambda7522ee1f... .

## 2. Root's prescribed operation

Let a_i be the COMPLETE global SeedRed(30) obtained from all five Task554
bodies using v540's coefficient multiplicities and retained order. Form

```text
D = Eval_raw(seed30) - sum_i a_i * full_precision2_lift(b_i),
require D.d0 = D.d1 = D.aux = 0 (all96776 coordinates),
d = plain_character0_slice(D.d2),
G = Task712.B_fwd_a0(d).
```

V541 is the mandatory direct-side contract. The full filtered projector is
applied to the COMPLETE lower-zero defect only; its resulting top equals
the plain character0 slice above. Keep the v518 fixed four-projector word
order for literal ancestry. There is no actor path to implement for this
fixed actual root violation. Check `<q,d>=<lambda,G>=1` independently.

Reduce G against the ACTUAL authenticated S in insertion order, with unique
leads but no monotone-lead requirement; preserve earlier-pivot zeros,
normalizing scale and the old raw/normalized distinction. The resulting
nonzero remainder must raise rank1354 to1355 IF all gates pass. Then reduce
the actual rho2 in the new state to obtain its next separator or membership
candidate. No full Conn or boundary rebuild is needed.

Raw word descriptor is the compact seed30 minus the selected canonical P1
word roots, then the fixed full character0 projector; physical reductions
append the already authenticated S ancestry. Keep normalized exponent and
all-eleven-slot/full-A0 claims separate; six source-tag arithmetic alone
does not assert the other five PB4 slots or a full witness.

## 3. Bounded concrete intake now

Inspect the exact output shape and existing functions in corrected root v2,
Task920 intake, v518/v534, actual physical separator v2, and Task880's FAILED
old materializer v2. Give the smallest executable implementation outline:

1. exact complete SeedRed extraction and which already-local lower/cache
   readers can be reused;
2. actual scalar launch/result and current-S source joins needed to close
   Task880 F1/F3/F4, with fixed parent inputs rather than fixture authority;
3. how to append one pivot and obtain the next separator without rebuilding
   the original Conn or hashing/decoding the same large parent unnecessarily;
4. the smallest new producer/checker/workflow file set needed.

Do not start another general materializer or arbitrary resume framework.
No optional historical audits, all504 orbit pass, all44 new materializations,
or new parent closure. Source inspection should be brief (aim5 minutes);
tell root exact implementation blockers rather than broadening the task.
Root supplies the final code-file authorization after this intake.

## 4. Claim boundary

The scalar run's producer and checker pass, but workshop CV-9 grading is
separate. This intake is not actual materialization. `verified=false`.
A0/COMMON/COFINAL/FAKE/IHARA are not declared; Grade2 is not decided.

## 5. Implementation authorization after the intake (root, 2026-09-05)

The parent-plus-one-pivot delta ABI is APPROVED. Implement now, only in
`search/d972_r07_actual_seed30_materializer_v1.py` and this task's reply.
The separate Task928 worker owns the independent checker and workflow;
send that worker the minimal candidate JSON/payload ABI promptly, before
finishing your producer. No monolithic state copy or generic resume engine.

The missing diagnostic tuple is now fixed:
run33941591417/1, head2caaf1f33b6f36f8aa754f759ef0e5dccfaf5a74,
artifact9962060193,
name `d972-r07-actual-root-scalar-batch-v2-diagnostics-33941591417-1`,
266309 archive bytes, digest
`sha256:78f087944047f170162587c413fcb1202bb5796b8d4fdff19da73e6e2fd321cf`.
Local directory:
`C:\Users\81905\AppData\Local\Temp\shadow-atelier-r07-root-v2-run33941591417-diagnostics-a1`.
Source receipt is `receipts/source-receipt.json`.

Root confirmed that Task904 `output/result.json` contains
`target_reduction.remainder` as a 24192-character hex string (12096 packed
bytes), alongside the retained 884 reductions and the expected remainder
hash. Decode/authenticate that payload; do not derive a row from its hash
or repeat the old rho2 elimination. New target reduction is the single
elimination by the new normalized pivot. Preserve the parent reference and
old target reduction receipt in the delta, not a second full history copy.

Authenticate fixed accepted S parent bytes and checker/source receipts once
per independent program. Accepted old-state derivation is a premise; do not
rerun its 8059-offer/610996-reduction construction. Fully replay NEW raw
seed30 ancestry, lower-zero subtraction, physical reduction, normalization,
one-pivot append and new target step. Respect the nonmonotone insertion-order
leads and separate the formal graded word DAG from final eleven-slot A0
replay. Candidate output must keep Grade2 undecided unless the complete
required terminal is actually reached; a new separator is not NONMEMBER.

Reuse accepted readers where appropriate in the producer, with explicit
source pins. The checker may use its own accepted lineage, but neither new
producer nor checker may import the other's arithmetic. Do not make a
shared producer-derived helper the purported independent arithmetic.

Progress must be visible at parent loading, raw seed evaluation, selected P1
subtraction (periodic count out of902), physical reduction, target append,
and terminal. Use bounded synthetic tests only; do not gate this one-pivot
run on optional historical audits or all504 orbit enumeration. No local
Python/GAP execution, network, git or credentials. Root alone launches GHA.
