# 裁定 01 — Sol 定義ゲート便への司令塔裁定(2026-07-18)

対象: `sol/sol_reply_01_definition_gate.md`(監査結論: 条件付き PASS)。
司令塔の検算: N₅ control の位数系・H₃(位数 27)と L = K⁽³⁾∩N₀ の評価(|PB₃:L| ≤ 2916・raw 候補 ≤ 324)・Φ による rs/sr 慣習の同値・|GT(K⁽ⁿ⁾)| 再導出 — いずれも司令塔側の独立計算と一致。監査品質は高い。

## ゲート判定

**条件付き PASS を受理**。条件 1–5(下記)を較正スイート v2 として実装し、全通過をもって「定義系+既知例の再現」達成を宣言する。それまで宣言はしない。**実装作業(Task #5)は「スイート v2 の実装」として本日より解禁**(ゲート条件の充足自体が実装作業のため)。

条件(Sol 提示・受理):
1. 「混合位数の最小 open」の表現修正(正規化代表 α≥2 ∧ n₀>1 の最小 = n=12; 文字どおりの最小は n=6 = K⁽³⁾)→ 定義ノート v2 に反映済み
2. 較正項目 1 の完走(n=3..16、奇数 13,15 の doubled check、Prop 3.5 の marked factor map 検査)
3. Thm 4.3 列挙と同時に全 shadow の source kernel = K⁽ⁿ⁾ の**証明書**(Lemma 4.2 (4.11) の共役 triple 方式)— 個数一致だけでは不足
4. c ≠ 1 の control(N₅ = ker β₅、位数 30)をゲートに追加し、full B₃/N 上で (3.3)(3.4)・T(c) = c^{2m+1} を検査
5. 逆射 (3.54)・代表元不変性・reduction の関手性 (5.3) を明示ゲート化(branch suite: (q,n) = (8,4),(36,12),(12,4),(18,3),(9,3))

## 提案への裁定(ES7 双方向ループ)

- **P1(N₅ control 追加): 採用**。理由: Dih では c=1 のため中心項が一度も試されない、という盲点指摘は正しい。位数 30 で最小コスト。
- **P2(ゲート 6 項目の改訂): 採用**。理由: 「指数一致は settled の証明にならない」(罠 2)は司令塔の実装スケッチが実際に踏みかけた罠であり、source kernel 証明書の要求は正当。
- **P3(Dih 外第一撃を L = K⁽³⁾∩N₀ に変更): 採用(修正採用を受理)**。理由: 司令塔原案の「isolated 化を先に行う」は fake 判定に不要な指数先払い(Cor 5.4 は任意の細分に適用可)という指摘を認める。H₃ seed は |PB₃:N₀| = 27(奇数)により Dih 外性が位数だけで証明でき、事前評価も二桁安全。Week 3 の第一撃として原案を置換する。
- 軽微 3 件(Prop 3.6 の書き方・Conj 5.1 の isolated 依存注記・supporting evidence の限定表現): すべて採用、定義ノート v2 に反映。

## 警告 6 件の受領(実装規約に昇格)

1. GAP の `s*r` = 論文の `rs` を固定し、z̄ = (r²s, r⁻¹s, r) と ψₙ(xy) を fixture 化
2. PB₃/N を F₂/N_F₂ × ⟨cN⟩ に**分解しない**(一般 N では分裂しない)
3. u = 2m+1 の法 N_ord と法 2n を混同しない(Thm 4.6 の faithful parameter)
4. `Size(kernel) = Size(target)` を settled 証明に**使わない**(全射なら自動成立)
5. seed 群の非 dihedral 性だけで kernel の Dih 外性を判定しない(排他的不変量で証明)
6. fake の負判定には完全列挙の個数証明を必ず添付

罠 12 件(reply §6)は較正スイート実装の設計制約として全件参照する。

## 次便への申し送り

次の Sol 便(定例 or ゲート)の冒頭で本裁定を提示する(P1 採用・P2 採用・P3 採用)。次便の主題候補: スイート v2 の全通過報告と「達成宣言」の監査。
