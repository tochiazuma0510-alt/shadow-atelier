# Task1155 — P7 の実正式値への最終 binding 準備

宛先: 既存 Pauli / p6_final_binding。Sol は root broker。Task1153 を保存し、この独立した有限 metadata 作業を続ける。返信は `sol/luna_reply_1155_r07_P7_final_binding_preparation_v1.md`。成果は `%TEMP%/shadow-atelier-audit163/task1155/` に CreateNew、既存版は保存する。

P1153 を root が全 source 差分・205 raw領域・66保持・12433 consumer・792型・新13/15/14 wireで採択した。基点 source は task1153/draft02/d972_r07_fixed_lambda_cycle_batch_v7.py 552642 B / 2439db913d06f0cd9249f34ff3c2d1495ac709e03073269ef5426038e7f2b011。root票 `root-task1153-static-source-adoption-v1.json` 2855 B / 81ce8078b7093570724f29b8a152b097ec24d73510285d025ea55e5a46c5d8e9。

本便は最終値を推測する作業ではなく、実値が到着した時に root が実行する有限 raw binder の準備である。Task1138 の採択済み binder/契約/返信を参照し、今回の source・公開票へ versioned に適用する。旧 binder は凍結。変更予定は `BATCH_V6_INVENTORY_REGISTRATION = None` の None を root 発行の exact5 object の1行 canonical literalへ、`IMPLEMENTATION_COMPLETE = False` の False を Trueへ、という2 tokenのみ。guard周囲の宣言・唯一性・offset/length/rawを固定する。19親、歴史native、数値、caps、k128/max1/no-refill、C4 raw、旧6群、新計器、source全残余を変更しない。

入力は root が実 full metadata/native0/解放/journalを裁定した後に渡す、実formal5 JSONのwhole D3と実root受領採択票のwhole D3。exact5は files/file_bytes/directories/files_sha256/directories_sha256、非bool非負整数と64桁小文字hexの型・keys全一致。入力pinを前後でfresh一致させ、JSON duplicate/nonfinite/overflow等の受理範囲を明記する。実typed受領、native0や数学gradeをbinderが作らない。root採択票への意味判断はrootの責務として、その実bytesを保管・束縛する。未入力や不完全入力でfinal sourceや完了票を先行生成しない。

出力は新directoryのfinal P7 raw、全2patch before/after、whole EOF forward/reverse、205 current raw範囲と登録保持66の全一致、consumerの位置写像、変更したpublic metadataだけの全記帳、全final pin、最後にcomplete handback。既存公開契約・過去採択票は上書きせず、参照先の新source offset/pinを更新する最小公開 overlayを優先し、既存巨大表の再複製は不要。old/new consumerへの位置・意味の橋渡しを失わない。新event全schema/順序/失敗規約はraw不変として照合する。

今は1150実受領の修理・再実行と並行する。実1回目は44復元/全file前後照合を完了したがZIP格納順の過剰条件でnative1、typednull/formal5未発行。この値を成功として使わない。実formal5が来る前に2tokenを埋めない。binder自体の全source/契約・入力/出力・raw差分計画を先に納品すればrootが別読できる。追加の作者承認待ちは設けない。

数学sourceのimport/AST/compile/実行/selftestは禁止のまま。binderの実実行もrootが担当する。許可はraw bytes/text/typed JSON/pin作業のみ。C private、他作者のprivate本文、credential、Git/GHA/network、process操作、新agent、codex起動は禁止。P private source/deltaをC/driverへ共有しない。ユーザーの継続GHA認可は保持し、実最終bindingと別読完了後の実行はrootが進める。

AUDIT_1155_VERDICT: ROOT_COMMISSIONED_STATIC_BINDER_PREPARATION_ACTUAL_FORMAL5_PENDING
