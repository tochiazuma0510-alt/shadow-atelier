# Task931 — independent audit of v545/v546

Author: independent mathematical auditor / 2026-09-05.

Overall verdict: **PASS at paper level, conditional on the named existing
finite-group, marking and source-map premises.** No fatal algebraic gap was
found. The three later-source deficit directions are indeed all in grade 2
for the stated induced source filtration. This is not a numerical row replay,
physical membership decision, or release gate for seed30. `verified=false`.

## F1. Scope and section verdicts

I read the complete task and both papers. For their dependencies I inspected
v542, v543, v458, v459, v456, v454 and v457/v444; the marked Gamma0 basis and
the PC/E3 conventions were checked against the cited local records. Existing
finite computations are premises, not newly recomputed facts. No Python,
GAP, network, git, implementation or historical rerun was used.

| Audited part | Verdict |
|---|---|
| v545 §§1–2: actual nesting and equal Frattini subgroups | PASS |
| v545 §3: old marked generators and three-word basis | PASS |
| v545 §4: literal repair and exact exponent normalization | PASS; finite coordinate readout remains unexported |
| v545 §5: novelty and scope | PASS |
| v546 §§1–2: first-E3 quotient and five endpoint coordinates | PASS |
| v546 §3: carry formula, negative edges and filtration bounds | PASS |
| v546 §4: degree-two witnesses and graded kernel sequence | PASS; unit-rotation convention clarified in F6 |
| v546 §5: exact transition dimensions | PASS on the named v456 and strict occurrence-image premises, justified in F7 |
| v546 §6: physical boundary and production independence | PASS |

The induced-filtration warning added to v546 after (4.3) during this audit
was also read. It is correct and important. The final section-4 wording
also explicitly selects the standard unit rotation, as requested in F6.

## F2. Same Frattini subgroup and its marked basis — PASS

The inclusions are actual ones in the fixed Delta:

\[
\Gamma_0\leq\Gamma_2,
\qquad \Phi(\Gamma_0)\leq\Phi(\Gamma_2).
\]

From the given order and abelianization of Gamma2,
`|Phi(Gamma2)|=3^(8-5)=27`. Thus equality with the already order-27
subgroup Phi(Gamma0) follows by inclusion, not by an abstract group-type
comparison. This establishes v545 (2.1).

The local records identify `A0P_SRC[3]` and `A0P_SRC[5]` as the words
mapping to the marked Frattini basis `(gamma3,gamma5)`. They are not compact
relators 3 and 5. The finite-p-group maximal-subgroup argument therefore
does prove `Gamma0=<a,b>` for these actual elements.

Since Gamma0' has order 3, its conjugation action factors through a 3-group
into Aut(C3), hence is trivial. To make the cube-centrality step explicit,
class two and the derived exponent three give

\[
[a^3,g]=[a,g]^3=1,
\qquad [b^3,g]=[b,g]^3=1.
\]

The three elements `a^3,b^3,[a,b]` have order dividing 3. Their subgroup is
central, and the quotient by it is generated by two commuting elements of
exponent 3; it is therefore exactly Phi(Gamma0). Since Phi has order 27,
the resulting surjection `C3^3 -> Phi` is an isomorphism. Uniqueness of
v545 (3.3), including the commutator's displayed orientation, follows.

## F3. Literal repair and normalization — PASS

For `tau2(z)=0`, v542 gives a word `w(z)` with the specified Q2 cycle and
endpoint `h` in Phi(Gamma2). F2 puts this endpoint in the old marked Phi.
The three words

\[
U=s_3^3,\qquad V=s_5^3,\qquad W=s_3^{-1}s_5^{-1}s_3s_5
\]

belong to Phi(N0), so their mod-three Fox rows vanish at Q0 and its
quotients. They need not be in Omega individually. The coordinates used
in the appended product must be those of **h inverse**, as v545 (4.1)
correctly specifies. Representing coefficient 2 by exponent -1 is valid
on these order-three endpoints and does not affect Fox vanishing.

Consequently the final product has endpoint identity and unchanged Q2
cycle. The bound of three counts signed fixed-word factors, not expanded
letters or total computation time. No new Gamma2 generating roster or BFS
is needed mathematically.

The normalization then uses the exact existing facts
`epsilon(Omega)=18 Z^2`, `epsilon(c_x)=(18,0)`,
`epsilon(c_y)=(0,18)` and `J_Q2(c_x)=J_Q2(c_y)=0`.
For integer `epsilon(C)=18(A,B)`, the product
`C c_x^(-A) c_y^(-B)` has exact integer exponent zero. Reducing A and B
modulo 3 would not suffice for this assertion; the paper does not do so.

The 27 marked endpoint products still need to be matched to the actual
endpoint encoding for a concrete consumer. The paper expressly leaves that
artifact unexported. This is an interface boundary, not a missing existence
argument.

## F4. One E3 projection and the five-dimensional quotient — PASS

The displayed PC relation gives `[u3,u1]=u4`, with u4 central. Together
with the accepted independent order-three PC generators, it gives
`H=<u1,u3>` of order 27, with `H'=Phi(H)=<u4>` and
`H_ab=F3^2`. Its abelian coordinates are precisely ordinary x/y exponent
sums modulo 3 in the stated marking.

The subdirect-product proof of `AC=Q0 x H` is valid. In its notation H0 is
normal in H: conjugate `(1,h0)` by a lift in C of any h in H. Two lifts of
the same g differ by H0, so `Q0 -> H/H0` is a well-defined surjective
homomorphism. A nontrivial quotient H/H0 would be a finite 3-group and
would have a quotient C3. This contradicts `Q0_ab=C2^2`. Neither the
divisibility of |P| by 3 nor a semisimple P-module assumption enters here.

Compatibility of the marked projection with Delta -> Q2 now gives
`image(Gamma2)=V9[3] x H`. Its maximal elementary-abelian quotient has
dimension `3+2=5`. The induced surjection from Gamma2/Phi is therefore an
isomorphism, by the existing dimension-five premise. Thus (2.3) and the
kernel identity inherited from v542 are justified.

Conjugation by a Delta lift acts on V9[3] by its A signs and on H_ab
trivially. Hence the quotient characters are the three distinct nontrivial
A characters plus two trivial ones; P and V act trivially.

Do not identify this quotient readout with the repair-coordinate readout
in F3. Indeed, the first-E3 image of Gamma0 is H, so its image on
Phi(Gamma0) is Phi(H), of order 3. The order-27 repair subgroup cannot be
read faithfully through this projection alone. Neither paper claims that
it can: v545 retains the old actual Gamma0 endpoint encoding.

## F5. Carry formula and handedness — PASS

With the rotation-left multiplication of v546 (1.2), the rotation of
`s(q) g0 s(qg)^(-1)` is exactly

\[
v_i+\epsilon_i(e)k_{g,i}
 -[v_i+\epsilon_i(e)k_{g,i}]_3
\quad\text{in }\mathbb Z/9.
\]

The inverse section cancels the terminal parity action; no additional sign
multiplies this expression. It is divisible by 3, so division in the
integers followed by reduction modulo 3 yields (3.1). The sign must be an
integer sign before division, as the paper states.

For `q'=qg^(-1)`, the group equality

\[
s(q)g_0^{-1}s(q')^{-1}
 =\bigl(s(q')g_0s(q)^{-1}\bigr)^{-1}
\]

proves the claimed negative-edge rule. There is no extra parity twist.
The left-kernel decomposition `prefix=h s(q)` then updates h by addition
of this error on each edge, even though the kernel is not central in Q0.
For a closed path the final section is identity, proving the carry sum.
Schreier cycles and linearity extend it to all Z.

The chain convention is also consistent: a positive edge q -> qg has
boundary `q(g-1)`, so the left-prefix chains have
`partial(a,b)=a(X-1)+b(Y-1)`. This is the same left-module/right-boundary
convention used in v454/v456. A different implementation convention or
section-left/kernel-right encoding must still be converted explicitly.

Normality of V gives `RI^d=I^d R`. Each carry cochain, at fixed parity and
letter, depends on only one rotation coordinate and has polynomial degree
at most 2 over F3. Therefore any three left-V differences kill it on the
whole chain space. Constant augmentation cochains are killed by one such
difference. This proves (3.3) without assuming cycles are generated over I
by shallower cycles.

## F6. The exact graded endpoint losses — PASS

Take t_i to be the **standard unit** Q0 rotation on coordinate i. The Fox
product rule gives

\[
J_{Q_2}(r_i^3)
 =(1+t_i+t_i^2)J_{Q_2}(r_i)
 =(t_i-1)^2J_{Q_2}(r_i).
\]

Its endpoint at Q2 is identity, so this is in `F^2 Z`, and its carry is
e_i. For an arbitrary pure order-nine rotation `a_i e_i`, the literal
right side of (4.2) is instead `(a_i mod 3)e_i`, still nonzero. Thus the
earlier wording “pure rotation of order 9” needed the unit convention for
the displayed e_i equality. The author has now made that choice explicit;
none of the rank conclusions depended on this harmless normalization issue.

The cube witnesses and F5 give the complete image filtration (4.3).
Crucially `F^d Z=Z intersect (I^d R)^2`, not `I^d Z`.
V-triviality implies `tau2(I Z)=0`, but `J_Q2(r_i)` is not a cycle, so
the witnesses do not contradict it. The new warning after (4.3) correctly
records this distinction.

For the induced filtration on K, (4.4) is exact. Specifically, if
`tau2(z)` lies in `tau2(F^(d+1) Z)`, choose deeper z' with the same image;
then `z-z'` is in K and represents the same leading term. Injection of
`gr_d K` follows directly from `F^d K=K intersect F^d Z`.
The quotient losses are therefore exactly two trivial directions in
degree 0 and the three rotation directions in degree 2. Taking A-character
summands is exact because 4 is invertible in F3. Each nontrivial character
loses one dimension in degree 2, and no later degree loses any.

## F7. Transfer to the registered transition source and table — PASS

Here strictness is genuinely needed, but it follows from the cited
identity tag, not from injectivity alone. V454 (2.3) sends a raw pair to

\[
(v_x,v_y)\longmapsto
(-v_xa,\ v_y-v_xab,\ \operatorname{aug}(v_x)).
\]

The inverse on its image is
`v_x=-(first)a^(-1)`, `v_y=(second)+v_x ab`.
Multiplication by these group elements preserves the two-sided I
filtration. Thus vanishing of the first two identity-tag components below
degree d forces the raw pair to vanish below degree d. Conversely the
registered occurrence substitutions preserve this filtration, and their
prefix/Jacobian operations preserve it as well. This proves strictness on
the complete occurrence-separated image. The normalized coordinates are
retained in degree 0 and contribute nothing to positive-grade kernels.

V457 (2.1) identifies that complete source with the normal-generated legal
image, and v444 identifies its precision increments with the transition
spaces. Thus the induced filtration in F6 is the relevant one for those
increments; no physical-image strictness has been smuggled in.

Using the accepted v456 raw-cycle dimensions, F6 subtracts two from the
degree-zero trivial block. The two explicit normalizers restore exactly
these two augmented trivial directions. In degree 2 it subtracts one from
each nontrivial block and none from the trivial block. Therefore the
augmented table is

| Grade | Trivial | Each nontrivial | Total |
|---:|---:|---:|---:|
| 0 | 505 | 503 | 2014 |
| 1 | 1509 | 1512 | 6045 |
| 2 | 3026 | 3022 | 12092 |
| 3 | 3526 | 3529 | 14113 |
| 4 | 3027 | 3024 | 12099 |
| 5 | 1511 | 1513 | 6050 |
| 6 | 505 | 504 | 2017 |

The sum is `54430=54428+2`. Relative to the v456 later caps, the deficits
are exactly `(3,0,0,0,0)`, establishing v546 (5.1). This is a deduction
about the complete registered source; it is not evidence that a partial
implemented closure has already emitted all these rows or their ancestry.

## F8. Genuinely new content and retained boundary

Beyond v542, v545 identifies the repair subgroup with the old actual Phi
and replaces new Gamma2 generator selection plus BFS by three fixed signed
words. The finite endpoint-coordinate interface remains to be emitted.

Beyond v543, v546 supplies the five legality cochains themselves as three
local rotation carries and two ordinary exponent rows. Their tree-cycle
values can therefore be defined without an abstract abelianization table;
the marked coordinate adapter still needs authentication.

Beyond v458's total deficit budget of three, v546 proves where all three
directions occur, character by character: entirely in grade 2. It replaces
`12092 <= dim H^[2] <= 12095` by the exact value 12092 and leaves later
source grades at their raw-cycle caps.

None of these statements places the fixed rho2 in the physical image.
The complete lower fibre/Conn, actual physical map and a target preimage or
separator remain separate. Normalization and repair preserve the named
coarse Fox row, not an unproved PB4 or full-Delta class. The remaining H
floors, PB4 block and joint normalization are still required for finite A0;
cofinal lift, fake and Ihara conclusions are not supplied.

**Seed30 materializer production should continue independently.** This
audit requests no adapter implementation, no table export before that run,
and no historical recomputation. Only this reply file was written by the
auditor.

```text
SAME ACTUAL FRATTINI / THREE-WORD REPAIR:  PAPER AUDIT PASS
FIVE CARRY+EXPONENT ROWS:                  PAPER AUDIT PASS
INDUCED GRADE-2 DEFICIT (3026,3022^3):     PAPER AUDIT PASS
EXISTING FINITE/MARKED PREMISES:          RETAINED, NOT RERUN
ACTUAL ROW/WORD ADAPTER REPLAY:           NOT PERFORMED
PHYSICAL RHO2 MEMBERSHIP / FULL A0:       NOT DECIDED HERE
SEED30 RELEASE DEPENDENCY:                NONE
verified=false
```
