# Task955 -- independent full-origin refinement checker and workflow

Read Task954 and reply953 FULL. Root adopts the fresh full-origin route
after actual fixed44 rank1359 ROOT_SEEDS_ZERO. All actual start tuple/entry
pins and mathematical scope are in Task954. Your responsibility is independent
checker and GHA workflow. No local numerical Python/GAP, network, credentials,
git, dispatch or further agents. Root is the sole broker. Public ABI only
with Task954; no sharing new arithmetic implementation.

Write ONLY `search/check_d972_r07_full_origin_refinement_v1.py`,
`.github/workflows/d972-r07-full-origin-refinement-v1.yml`, and
`sol/luna_reply_955_r07_full_origin_refinement_checker.md`.
Use your own accepted checker lineage, keep published files immutable.

Independently authenticate the exact base/delta/packet-prefix1359 start and
its actual schemas. Treat its completed three steps and fixed packet as named
accepted arithmetic premises; hash/payload/chain join and current1359-row
lambda sweep are required, old packet/insert reconstruction is not. Carry
DERIVED rho2 honestly through the additional parent chain.

Independently rebuild all4 fresh full-origin seed/actor arrays for every
completed new scan, compare ALL arrays and ordered first hit. Execute the
complete selected actor (lower and top), its exact canonical ActRed subtraction,
all96776 lower zero, homogeneous slice/B, one physical append/target update,
fresh lambda and all rows/both targets. Check new literal ancestry/orientation
against actual accepted P1 instruction/DAG receipts. Preserve complete-prefix
and resource semantics. Roots can change active character; fixed44 seed values
can become nonzero again. ALL_ROOT_ORIGINS_ZERO is not NONMEMBER.

## Required independent anchor for the new actor path

Workshop2096's w_t lower block had only a near-clone forward actor anchor.
This new actor consumer must have a non-clone check of the COMPLETE selected
source actor, including nonzero degree0/1-to2 mixing. Prefer a direct finite
F3[C3^3] representation: invert the four-character Fourier transform (4=1
in F3), expand each monomial (e_i-1)^mu into the27 ordinary group coefficients,
apply actual affine left multiplication as a permutation, then extract the
ten degree<=2 binomial coefficients and transform characters back. Auxiliary
slots remain shared and are handled in their own declared source type.

The section-left/kernel-right group convention in accepted source is
(p,e,k)(p',e',k')=(pp', e+e', sign(e')k+k'). Use actual six tagged actor images
and PSL order. Do not call _checker_seed_act or copy the producer's polynomial
multiply/pull implementation as the claimed new anchor. Accepted group/index
inputs may be shared premises, with the new finite-27 mixed arithmetic visibly
different. If this is inappropriate for an actual type, tell root promptly;
do not substitute a homogeneous-only check. This is an actor-block anchor,
not a third complete arithmetic pipeline or Lean verification.

## Workflow and release

Follow v2 workflow's ten old parent tuples plus exact packet run33964709359/1
candidate9969090590 from Task954. Pin all new and retained executable/data
dependencies. Use marker [r07-full-origin-refinement-v1-run], matching new
workflow/artifact names, source/runtime freeze, actual-parent metadata canary
and a few genuine new actor/interface checks before production. Schedule
cap1 -> actual same-output resume cap32 -> independent new-prefix checker,
internal1800s/outer40min each, job130min. Candidate only after PASS;
diagnostics always retain complete prefix and resource receipt. Distinguish
stored/declared/actually paired counts. No clone or old source/P1 rebuild
hidden behind a new filename. Report byte/SHA pins and remaining gates.
