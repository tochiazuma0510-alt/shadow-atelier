# Task1080 — Luna: v5 全 envelope 受領器の静的草案

宛先: packet_checker。Sol は root / audit163。Task1078 は immutable freeze のまま保持する。本便の実装は TEMP の新 task1080 以下だけ、返信は sol/luna_reply_1080_r07_v5_root_receiver_static_draft.md。既存受領器・親 artifact・作業ツリー source/WF は変更しない。

## 目的と権限

工房2206は run34120585268/1 を rank1706/gen8411、cross-checked 限定7条で受理し、2207で新17親/P5/C5/WFの notify-and-go を認可した。次の v5 run を root が受け取れるよう、metadata 専用 PowerShell 5.1 / ASCII 受領器を先に具体化する。現在 root が session5057/PID11292 で実行中の task1074 受領器は触らず、終了を待つ作業もしない。Git/network/credential/Python/GAP/AST/import/compile/数学 source 実行は禁止。許される検討は raw ASCII/byte ranges/typed JSON/ZIP metadata だけ。独自 helper を実行して実 artifact を受理しない。

## 入力

- 本便、返信1074、1077/1078の公開 interface v1/v2、task1079と後着の公開 WF metadata。P5 私的本文は読まない。自分の C5 と旧 receiver 本文は読める。
- TEMP/shadow-atelier-audit163/task1074/audit-r07-batch-v4-metadata-v5.ps1 = 261800 B / dcccf94a7eb3458d6cf709478a449d3377f86e5b90edfa0a9f011d82122ac411。これを旧受理契約の基準とし、全 raw 保持区間/全変更区間を分類する。
- 1074 の registered-producer-body-inheritance-v2.json、registered-producer-current-regions-v2.json は旧 P4 用。新 P5/C5 の raw registry metadata は後着固定票を使う。opaque source hash/range を扱うことと私的 P 本文読むことを区別する。
- root 実 v4 全受領票は未完成。provisional-parent-inventory は設計参照だけで正式代替しない。新 run/attempt/head/id/ZIP bytes/SHA/最新 P/C/WF/driver は未定なので placeholder と実行 guard=false を維持する。

## 必須契約

v5 schema / 17役 / plain acceptance8key、旧 v3親 batch-parent と追加 v4親 batch-parent-v4 を別 namespace のまま受理する。原1450→1578→1706、97→225→353、二層の128+128/1536 phases/1544 checkpoints/2 invocations、previous target は v4 start.target、current/lambda は v4 final を保持する。今度の候補が128行すべて独立とは仮定しない。private checkpoint/durable tail/complete final/LinearMembershipCandidate/UNKNOWN_RESOURCE の既受理境界を保つ。typed incomplete と失敗診断用の別 scope を complete success へ流用しない。

旧 v4 に対する全入場・固定 reference 16 payload の原64への解決・全 file/dir EOF・source/TCB/registry・復元空directory・全 invocation/checkpoint/row/phase の scope は削らない。以前の parent-result の一部分だけで受理しない。旧 parent を毎回再計算する数学処理は追加しない。root 既受領票 pin の新たな信頼方式やキャッシュ最適化を無断で加えず、必要なら設計 finding として先に返す。

新4群 P[30,10,6,7]/C[28,9,6,7]。第四群は P28 files/C15 files の公開 v2 契約で、保存された observed_error の厳密な理由まで接続し、fixture を実1706算術へ昇格しない。旧 DEPENDENT 継続116fileと陰性到達点の限定も保持。cost receipt は task1079 の公開確定keysetで全実 input pin/field/秒/phase counts を接続し、P residual/C total/P+C total を区別する。負残差は隠さず、仮説234秒等を候補成功条件にしない。call coverage NOT_MEASURED/第三独立性なし/verified=false を保持。

## 引渡し

未実行 helper の immutable snapshot、全差分、全 raw 区間 index（EOF 分割と旧保持部分を再構成できるもの）、公開材料全 pin index、自己全静読の所見を提出する。新 helper 本文の仕様が未定の部分は明示し、未接続 block を完成と数えない。後着 WF/keyset や source pin だけが未定なら normal guard を閉じて freeze し、後の定数差分を別 snapshot とする。root が別読してから実行する。

返信の最終行は AUDIT_1080_VERDICT: 条件付き。root receiver の完了/今度の実行成功を先取りせず、新数学矢印・candidate・cross-checked の成果を作らない。
