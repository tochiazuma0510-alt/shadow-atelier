# 裁定 77 — 便 65 検収: 二 artifact の型修理 5 件(2026-07-28)

## 受理
spec v6 本体は前便どおり PASS 維持。二 artifact に blocker 5:
- **B65-1(数学的型誤り)**: Bézout 1=Σuᵢgᵢ は**非交差(和が単位 ideal)**の証明であって点同一性の証明ではない(反例: (x) と (x−1))。W-2 の witness メニューから「Bézout で同一性」を除去し、同一性は相互 ideal inclusion(両方向の表現係数)に限定・Bézout は「別点である」ことの証明として別 witness 型に再配置。
- **B65-2**: [25]/[26] の割当が排他的でない(native 不一致なら equality witness も必ず不成立 — 同一原因が二 code に写る)。原因帰属の優先規則(native 不一致が検出されたら [26] を primary・[25] は native 一致下での witness 失敗に限定、等)で排他化。
- **B65-3**: 閉包が共通の untrusted input(certificate・native inputs 自身)を飲み込み、正しく独立な二 verifier が必ず [11] になる。**shared input と shared implementation の型分離** — declared_untrusted_inputs[](入力クラス・TCB とは別)を新設し交差検査から除外(入力は共有が正しい)。
- **F7.1**: 「depth 0 のみは不備」が偽(自己完結 leaf 1 個の正しい fixpoint を拒否)。規則を「深さの見た目」でなく「各 node の outgoing dependency 証明と fixpoint 性」へ。
- **F7.2/7.3**: exact blob identity と rebuild lineage の接続・初期 TCB の実値列挙。
F8 で一部値は発行可能と明示 — freeze は次便。

## 配分
B_FC 著者 = contract v2+manifest v2(上記 5 件)。完了後 便 66。
