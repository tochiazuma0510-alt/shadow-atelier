# 裁定 2227 snapshot(2026-09-08・司令塔記帳)

- 2227: **工房数学者による回転同値性計画 1111 の独立審査 → 採択・裁定 2224 の登坂見積りを撤回・登坂設計の本線を「冷保存(pin のみ)」へ転換**。報告 = `scratchpad/math_rotation_equivalence_review_v1.md`(51,555 B・sha256 0d39b9c5b388b657… 工房 pin)・正本 = `docs/notes/rotation_equivalence_math_review_v1.md`(原本 + 裁定 trailer・sha256 28ab573e716990c6…)。**判定: 数学としては条件付き受理可・設計としては受理不可**。(1) 原案 R1–R4 は「回転」(最古の親を落とす)ではなく**「統合」**(全層の証拠を 1 親に畳んで全部保持): 役割数 n は 1 に固定できるが認証すべき証拠量は従来どおり線形に増え、2225 の設計問題には答えていない。(2) **必要十分条件(定理 1)**: 認証対象 Σ = (anchor; 順序付き行 bytes 列; target; 祖先列 D; state_head; λ; 出所解決写像)に対し Φ(P₀, S₁..S_m) = Φ(P₀, B) の条件 (N1)〜(N7)。原案 (i)〜(v) は**不足 6 点**(① 層境界 object = segment table ② 中間 remainder の数値再導出 ③ 検証済み全単射 ν ④ **rolling 鎖のリンク単位再計算** ⑤ 証拠同値 V_new ⊇ V_old ⑥ canary 置換義務)・**最重要の穴 = (iv)**「最新 state/terminal 一致」だけでは中間 instruction body を再 seal して最終 head を literal で写した bundle が通る(state_head が計算結果でなく引き写しになる)・**過剰 1 点** = (v) の全射条件は telemetry/checkpoint(output の 23 %)まで巻き込む → Tier A(荷重閉包)/B(hash のみ)/C(冷 pin)の三分を要求。(3) **合成則(補題 1〜3)**: 望遠鏡則 + 層連結(前件 t_out_i = t_in_{i+1})は F₃ で符号曖昧なし。**核補題 3 = 総和 S = Σ θ_j q_j は θ = 0 レコードの削除と順序置換に不変**(検算済)→ I₂ から θ = 0 保存も順序保存も従わない・強い言明は鎖(逐次 digest 鎖と rolling 鎖)で総和は弱い系・行が一次独立なら S から θ 列は一意(rank 24/24 検証)= θ は偽造不能・**sr は F₃ → ℤ の準同型でない**(sr(1) + sr(1) = 2 ≠ sr(2) = −1)→ 同一行に触れる 2 段の併合は target 恒等式を保つが語層の指数を壊す → 順序保存の根拠は非可換な語層に置く。(4) **ρ₂ ancestry の担保(命題 2)**: 行 bytes・両端 target・10-key 祖先列・row-manifest/instruction/target.json を逐語保持すれば第三者は生 bytes だけから ρ₂ ancestry の全数値内容を再導出できる(層を親として持ち回る必要なし・O(R))→ CV-9 検問 14 項目(ρ-1〜ρ-14)・再導出できないのは各行の語水準閉包(Ω 語 → P1 → literal)と出所証言のみ・10-key record は行 digest を持たないので row-manifest の逐語保持が必須・鎖の再起点は作らず統合親の terminal は吸収最終層の元 state_head そのもの。(5) **省ける/省けない仕事**: n 項をコードから A_i(inventory)/B_i(型検査)/C_i(層の 128 行再認証)/D_i(adapter)/E_i/F_i に分解 → 消えるのは D/E/F(層あたり定数・小)・残るのは C_i と A_i(証拠量比例)。一次所見: 基点 15 role(ZIP 1,669,392,893 B・9,150 file)は非 k 固定費 26.1 s に収まるのに batch 親 1 層(377,383,320 B・11,648 file)は 44.4 s = byte あたり 7.5 倍/file あたり 1.3 倍の乖離 → 純 byte/純 file モデルでは説明不能・残差は batch 層固有の C_i = Θ(k·R_i) で統合親でも消えない。**2225 (3) への回答: 「1,695 s 一定」は未証明どころか偽** — producer は候補ごとに physical_factors と ordered_reductions をちょうど rank 個の要素で構築(P5 :3371-3381/:3403・コードから確定)→ per-run は Θ(k·R)・実測係数の外挿で R = 48,384 では消去相 946 s・literal 1.6 GB/run・k = 425 なら合計 6,501 s > cap・campaign 全体では層再認証が Θ(N²·k·R̄)・最終 run だけで ≈ 1.14 × 10⁹ 消去記録 → **統合しても回転しても、過去層の消去記録を毎 run 再解析する設計では塔を登れない。R4 が別スコープとした冷保存(pin のみ)方式こそ唯一の登坂手段**。(6) **追加条件**: v7 CV-9 checklist = A 群(対象同値 7)/B 群(数値再導出 9・中間 remainder 全数照合と θ 一意性検査は新設)/C 群(弱化検出 6)/D 群(canary 12・層形 7 件が空虚化するため置換)/E 群(費用計器 5)・事前登録すべき恒等式 8 本・positive/negative/adversarial fixture(「target 恒等式だけ見ていると通る」型の adversarial 3 件 = 順序置換・θ = 0 削除・段の併合を最重視)。(7) **上申**: 設計判断の前に計器を — progress("admitted-parent", …) は role ごとに発火済みなので**経過秒を足すだけ**で n 項の機構が確定(GHA 費用 0・数学リスク 0)・n = 3 では per-layer 133.2 s/per-byte 136.5 s/per-(k·R) 138.9 s と 3 モデルが 4 % 以内に密集し**次 run の残差 1 数値では決まらない**。UNKNOWN 4 件(44.4 s/層の機構・層間の内容重複率 = 2 親 artifact の中央 directory 比較で費用 0 で測れる・checker の相別内訳・p1 相の R 依存性)・文献要請なし。**工房裁定**: ① 審査を採択し正本化 ② **裁定 2224 の「層を回転して n 一定なら k_max ≈ 425 で約 110 run で登り切れる」を撤回**(per-run Θ(k·R) 項の見落とし・2225 の射程固定と整合) ③ 登坂設計の本線を「過去証拠の冷保存(pin のみ)+ 現 run の必要量だけを荷重閉包で認証」へ転換することを Astra に委嘱(1111 の統合親案は数学的前提条件(N1〜N7・14 検問・8 恒等式・adversarial 3 件)として保持) ④ v6(18 親・一回)は実行可だが、driver に admitted-parent の role 別経過秒の計器を足すこと(driver 変更 = notify-and-go)・層間重複率の中央 directory 比較を依頼 ⑤ 次 run の残差を機構判定に使わない(2225 と整合)。
- 2227 補記: 研究可視化 Artifact(🌒・同一 URL)を裁定 2227 に合わせて訂正(章題「登り切るための設計転換」・図 3 の「回転すれば約 110 run」を撤回文に差替え・スコアボードの F-v5-1 行を冷保存への転換と v7 前提条件に更新・裁定範囲 2028〜2227)。工房の反省: 2224 で falsifier の線形外挿(約 110 run)を数学者審査前に裁定文へ入れた — 費用モデルの外挿は数学者審査を経てから裁定に載せる(memory 化)。
